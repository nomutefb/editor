# 파이썬 캐시 (썸네일 스크립트 import 시 생성)
__pycache__/
*.pyc

# 로컬 dev 토큰계측 shard (claude_py — CI는 run-id shard만 커밋, 로컬 run은 추적 안 함)
metrics/usage/local-*.jsonl

# 활성 계정 쿼터 신호(폴오버 SSOT ↔ account_failover.py 승격 공유 파일 · 커밋 대상 아님)
.nomute_active_quota

# Pages 빌드 산출물(빌드 시 생성)
viewer/articles.json
viewer/messages.json
viewer/fire_watch.json   # ⑭-i 추적 상태 = build-viewer 산출(원장 push/fire_watch.json 에서 구움) — messages.json 동축 빌드 산출물
viewer/picks-failed.json
viewer/triage-state.json
viewer/thumb-hist.json
viewer/*_out/index.json
viewer/detail/
viewer/cards/
viewer/assets/

# 에이전트 워크트리(격리 작업 — 추적 금지)
.claude/worktrees/

# 검증 도구 임시 설치(playwright-core 등) — 레포 유입 금지(zero-dependency 유지)
node_modules/

# FX 모델 바이너리(무해시 자동 다운로드 금지 = setup.sh sha256 핀 드롭 or 수동 드롭인 · 레포 커밋 대상 아님)
apps/fx/models/

# 레이아웃 작업대 산출물(채팅 첨부가 전달 경로 · 커밋 금지)
apps/layout/출력/

# 시크릿·자격 파일(평의회 260723 #7) — 광역 git add 유출 사전 봉인(히스토리 클린 실측 · 신규 유입만 차단)
token.json
credentials.json
.nomute_phone_env

# 영상 렌더 작업물(video/ — 기계산출물·결과 mp4 · 커밋 대상 아님)
video/.build/
video/out/

# 미리보기 실측 산출물(preview_shot.js base/diff 캡처·실측 json) = 로컬 확인용 · 커밋 대상 아님
docs/_shots/

# 그록 구독 자격 판정기 산출(자격증명 포함 · 커밋 금지)
grok_oauth_token.json
grok_probe_log.json

# Cloudflare wrangler 로컬 캐시(빌드 부산물 · 커밋 대상 아님)
.wrangler/
