# 노뮤트 디자인 기틀 SSOT 인덱스 — 「규칙2번」 (단일 진입점 · 운영자 정본 260704)

> **이 파일 = 규칙2번.** 디자인 기틀의 *모든* 정본 위치를 한 곳에 모은 **단일 진입점 인덱스**다. **CLAUDE.md [4] 제1 핵심명령(규칙1)이 이 파일을 *절대적으로 따른다*** — 디자인 관련 무엇이든(값·규칙·컴포넌트·확립본·거울·게이트) **정본 위치는 여기서 찾는다.** 지금까지 (구)§🔒·(구)§🎨·SessionStart 훅·CII·확립본에 *흩어져 있던* 참조를 여기로 집약했다(구 이모지 섹션 = 260701 해체 · 현행 = 번호 조항 [0]~[14]·[E1]~[E12] — 앵커 정정 260718 오퍼스 4갈래 감사).
>
> ⚠️ **값의 실체(hex·px·개수·셀렉터 본문·라인번호)는 여기 복붙하지 마라**(복붙 = 드리프트원 = 이 파일이 CLAUDE.md [4]/`:root`/`.lock`보다 먼저 낡음). 단 **규칙의 법·패턴 프로즈 요약 = §0에 단일 집약**(운영자 260718 · 값 실체는 여전히 포인터만 = §0도 값은 안 담고 규칙만). 실체는 각 정본 파일에 있고, 여기는 "어디에 있는지"만 가리킨다. 파일은 **경로·검색가능 조항키워드**로 가리키고 라인번호는 쓰지 않는다(라인은 편집 시 밀림). 새 디자인 기틀 파일이 생기면 이 인덱스에 **행을 추가**한다(누락 = 규칙2 위반 = [4] 위반 · ⚠️ 이 완전성은 기계 강제 아님 = 소프트룰이라 사람·모델이 챙긴다).

> 🗂 **폴더 = `디자인기틀/`**(운영자 260725 "정본 한 폴더에 다 모아줘" — 구 `docs/`·`구성도/` 분산 해소) · 폴더 안내판 = **`디자인기틀/00_진입점.md`**(이 폴더에 있는 것 / 코드로 살아 있어 못 옮긴 것[§1·§5·§6 축] 전량 지목). 안내판은 *위치*만, **규칙·값 정본은 여전히 이 파일(§0)과 `:root`** — 새 디자인 기틀 *문서*는 `docs/`가 아니라 이 폴더에 만든다.

## 🧭 위계 한 줄
**값(`:root`·`nm-svg.js`) → 규칙(이 문서 §0 + SessionStart 훅 주입 계약 — 구 CLAUDE.md [4]·[4-1]·§0 앵커는 260729 10줄 공통본 교체로 해체) → 컴포넌트(CII) → 확립본(사인오프) → 거울(build 파생) → 게이트(check_refs 강제).** 구체 충돌 시 **값(`viewer/index.html :root`)이 최종 정본.** 계승이 디폴트(안 물음), 갱신은 운영자 승인(SessionStart 훅 주입 계약 2·2-1·2-2).

---

## 0. 디자인 규칙 요약 (단일 요약본 · 운영자 260718) — CLAUDE.md가 링크하는 "그 한 곳"
> 260701 압축 때 CLAUDE.md `§🎨/§📐`에서 해체돼 [4]·훅·스킬로 **흩어졌던 디자인 규칙의 법·패턴을 여기 한 곳에 프로즈로 집약**한다(구 `CLAUDE.md §디자인` 죽은 참조 = 여기로 대체). **값의 실체(hex·px·셀렉터)는 담지 않는다 — 그건 §1~6 포인터**(여긴 "무엇을 지키나=규칙", 얼마인가=값은 `:root` 정본). **디자인 착수 전 이 절 정독 = [E1] 디자인 게이트.**

**A. 값·계승**
1. **계승이 디폴트** — 값·색 창작 금지 → `:root` `var()` 토큰 계승(정확 토큰 없어도 **근접 자동 계승**·안 물음). 근접조차 없을 때만 물어 운영자 승인 후 `:root` 갱신(신설). 값을 문서·스킬에 복붙 금지(드리프트원).
2. **팔레트 = 폐쇄형** — 참조 팔레트 색만(자유 hex 금지). 팔레트 예외(도구 툴톤·발행 콘텐츠색)는 재색칠·동행변경 **영구 금지**. **색 값 SSOT = `viewer/index.html :root` 단 하나** — 도구 뷰어(thumb·edit 등)는 색을 inline 복사하지만 공유 팔레트(accent/의미색)는 **`build`가 index에서 자동 전파(STAGE4 · §5 빌더)** = 손으로 베끼지 마라. 색 바꾸는 법 = 「`:root` 1곳 수정 → `python3 shared/build_design_mirror.py build` → 커밋」(build 빠뜨리면 `check_palette_sync` 게이트가 차단). 툴톤(`--bg`/`--line*` 등)은 전파 제외 = 위 팔레트 예외(뷰어별 정체성).

5. **스튜디오 레일 = 무조건 상속(운영자 260802 "항상 이미지 스튜디오나 영상 스튜디오는 저 로직을 따르게 만드셈 죽는 한이 있더라도")**: 이미지·영상 스튜디오의 **모든** 세부 메뉴는 미리보기 코너 옵션 레일 정본 로직을 예외 없이 따른다 — ① 픽토 기준 크기 = **버튼 22×22 / 글리프 12×12**(구현 = `<스코프> .trail svg { width:12px; height:12px; display:block }` 한 줄 · 버튼 클래스별 개별 svg 규칙 금지) ② 빈 캡슐 소거 술어 `:not(:has(.trail-g > button:not([hidden]))):not(:has(.trail-v:not(.none)))` ③ 캡슐·값 칩 규격 = CII 「미리보기 코너 옵션 레일」 값 사본. 강제 = `check_refs.check_trail_spec()` **5축 하드게이트 + 표면 자동 발견**(`viewer/*.html` 중 `class="trail` 보유 파일 전부 = 손 레지스트리 등재 여부 무관 → 새 탭이 조용히 빠질 수 없다). 결과 레일 2단 분기점도 같은 축 = **표면 간 한 값**(`check_refs.check_twocol_breakpoint` · 시그니처 `span 99` 자동발견 — 260802 실사고: thumb만 900 하향되고 영상 4탭이 1100 잔류 = 운영자 PC 실폭[900~1100]에서 이미지 2단·영상 1단으로 갈라졌는데 스모크 1280·폰 430 어느 실측기에도 안 걸렸다). 이진 토글 = **워드 점등**(기능 워딩 자체가 점등/소등 · ON/OFF 값 텍스트 금지 · 정본 = thumb #cnTog 「라벨 자체 점등」 · 운영자 260803) — 강제 = `check_refs.check_onoff_literal`(aria-pressed 요소의 ON/OFF 표시 텍스트 + `? 'ON' : 'OFF'` 이진 세팅 검출 · 면책 = _ONOFF_BASE{발사 페이로드·다값 순환} 스냅샷·축소 지향). 새 스튜디오 탭 = 레일 동반 계승(선택 아님).

**B. 일관성**
3. **같은 기능 = 같은 모양** — 같은 기능·메뉴·레이어 = 픽토 크기·액티브·마진·중앙정렬(높이)·좌우정렬(좌측 기준)·팔레트 통일. 강제 대상 = 기계 게이트 등재 축(CLAUDE.md `[4-1]` · 게이트 통과 = 완료).
4. **4분할 정렬 + 마진 균일 + 액티브** — 픽토/버튼/박스/팝업/이미지 = 4분할 중앙 기준 세로·가로 정렬 + 형제 마진 균일 + 클릭 액티브. 눈대중 금지 = **픽셀 실측**(디자인 계약 3-4 · `smoke_rank`/`smoke_popup`/`smoke_ghead`).
4-1. **스모크 승격 = 텍스트 잉크중심 하드게이트 금지**(운영자 260723 Q472 실증 등재) — 픽셀 실측 스모크의 어서션 대상은 **구조물**(배지·픽토·svg·블릿·체브론 = 위상 안정)·**잘림**(scrollWidth ≤ clientWidth)·**상대정렬**(형제 vs 형제)만. **텍스트 잉크중심을 절대 박스중심에 하드게이트하지 마라** = Y위상(위쪽 라이브 데이터로 요소 절대 y가 밀림)·글자별 잉크 산포로 ±1px 진동(동일 CSS가 +0.17↔+1.17 실측) = 위양성원 = 커밋 막힘·롤백 유발. 텍스트 정렬 보정값(translateY 등)은 **참고 로그**로만 관찰(위양성 판정 = 실캡처 육안 = 진위 정본). 눈대중 금지(픽셀 실측)와 별개 축 — 재는 건 픽셀, 게이트로 거는 건 구조물. **애니메이션 픽토(orb 등 = 프레임마다 잉크 이동)도 잉크중심 금지 → 기하 중심(getBoundingClientRect box) 게이트**(Q476).
4-2. **헤더/공유 골격 보정 셀렉터 = 탭 전용(`#tg-*`·`#cg-*`) 금지 → 공통(클래스·`[id$=…]`)**(운영자 260723 Q476 실증 등재) — 두 메뉴 이상이 같은 골격(예 `.tgroup-h`)을 공유하는데 한 탭 전용 id로 광학 보정하면 **다른 탭에 반드시 사각이 생긴다**(Q475 실증: `#tg-brief` 전용 ✨보정 → 채널 `#cg-brief` −1.0 미보정). 보정은 처음부터 탭 무관 셀렉터(공유 클래스 or `[id$="-brief"]`류)로 걸어 양 탭 동시 커버. 신설 스모크도 공유 골격은 전 탭(메뉴3·4) 순회 검증(smoke_ghead H2=트렌드·H6=채널).
5. **동심원 radius · 광학 > 기하** — 바깥 radius = 안쪽 + padding(`--r-*` 계산 · 임의 px 금지). 기하 중앙이 어색하면 광학 수동 보정.

**C. 컴포넌트·도형**
6. **컴포넌트 계승 = 100% 모방** — 새 버튼·모달·입력칸·아이콘 = CII 정본 셀렉터 복사·계승(재설계 금지 · 표에 없으면 운영자 질문 후 등재). 모방 = 부품·값만 아니라 구성·배치·문법까지 '그대로'가 1순위 · 판정 이원 = 99% = 0%(새 모양 = fail · CLAUDE.md 최우선 4 · 운영자 260720).
7. **누름 scale = press 토큰 티어** — `--press-pico/xs/s/m/l` 사다리에서 가장 가까운 형제 티어 계승(raw scale·임의 0.96 금지). 버튼·모션 = 가이드북(`디자인기틀/구성도/00_가이드북_버튼인터랙션.html`).
8. **표지판 도형 = SVG only** — 화살표·삼각형·×·✓ = 유니코드 문자 렌더 금지 → SVG 픽토(`nm-svg.js`). 문자 렌더 = 순수 텍스트·숫자만.

**D. 모션·폴리시**(정본 = `nomute-design` 스킬)
9. **깊이 = 글래스/그림자** — 딱딱한 1px 테두리 대신 글래스(`backdrop-filter: blur(var(--blur-*))`) 계승.
10. **전환** — 상호작용(hover/press/토글) = CSS `transition`(중단 가능) · `@keyframes` = 1회 등장 연출만 · 등장 stagger·아이콘 크로스페이드(opacity+scale+blur `.askclip`)는 **기존 패턴·커브 재사용**.
11. **타이포·이미지** — 동적 숫자 = `tabular-nums` · 제목 `text-wrap:balance`/본문 `pretty` · 이미지 아웃라인 = 순수 흑/백 저투명(틴트 금지).

**E. 표기·절차·게이트**
12. **표기** — 소머리 = 흰 텍스트 + 형광 블릿 · 모든 시각 = KST · 아이콘 = SVG.
13. **새 요소 등장 = 3중 안전장치** — 단일 커밋 분리(revert 1줄) + 보고 ⚠배치 딱지 + "이 위치·모양 맞나?" 선제(CLAUDE.md `[4]`). **정본 레퍼런스가 정 없으면 = 진행 말고 시안(플레이그라운드) 선행**(운영자 260720 · 최우선 4).
14. **제안·시안 = 플레이그라운드 HTML** — 정적 이미지·텍스트 나열 금지 → 만지는 **8요소** HTML(⑥미리보기 고정·⑦실물 주입·⑧전 조합 갤러리 = 260801 승격 · §2 플레이그라운드 행).
15. **게이트** — 커밋 전 `check_refs` rc=0 + UI 표면 `smoke_all` rc=0(§6). 값 충돌 = `:root`가 최종 정본.
16. **로딩·진행 표기 = orb 로더 단일 정본**(불러오는중·생성중·요약중·작업중·재수정중 등 · 버튼 안팎 공통) — `viewer/nm-loader.js → window.nmLoader(type,label[,opts])`(**그래픽 1종 = 통통 튀는 도트3**·shimmer · type = loading 불러오는 중 = **글자만**(도트 미부착 — 라벨 끝 …이 이미 점 3개 · 운영자 260731) / thinking 판단 / solving 산출·재수정 / prompting 설계 = **의미 라벨만**(data-orb) · **라벨 정본 = 「제작중…」 하나**(구 Thinking/Solving/Prompting 영문 3종 폐지 · 커스텀 한글 라벨은 존치) · **시간 표기 = 경과 `M:SS` · 예상 `(예상 : N분 NN초)`** 통일(구 `18s`·`(42s)`·`(보통 1–3분)`·`(1~3분)`·`(최대 ~15분)` 폐지) · **예상값 정본 = `window.nmEta`**(nm-loader.js · 운영자 260731 "예상도 일단 다 보이게, 점차 많이 쓰면 평균값으로 안정화") = 초기 시드(코드에 있던 기존 문구 우선 · 근거 없던 축은 잡 예산 기반 임시) → 성공 완료마다 `nmEta.done(key,초)`로 EMA(.3) 누적 → 표본 2건부터 실측 평균 채택. 표시 = `nmEta.label(key)` · 이상치(≤0·>10시간) 미학습 · 저장 = localStorage `nm-eta-v1`(실패해도 시드 폴백 = 표시 불변) · **계측 = `nmEta.dump()`**(키별 시드·표본수·현재값 + 학습/미학습 집계 = 조용한 학습 정지 검출 · CLAUDE [관측]) · **광학 잉크 정렬**(운영자 260731 · 증빙 `docs/reports/260731_로더도트단일화_전후.html`) = ① 라벨 `translateY(-.049em)`(한글 잉크 비대칭 asc10.336/desc2.320 → 잉크중심이 박스중심보다 +0.667px = 도트가 그만큼 위로 떠 보이던 것 보정 · 도트↔라벨 Δ≤0.04px 실측) ② **붙어 있는 글자도 같은 잉크선** = 활자(13.5·700·tabular)·`-.049em` 보정·`vertical-align:middle`을 **`.nm-shim` 공통으로 승격**하고 `.nm-load`에도 `vertical-align:middle` — `.nm-load`가 `inline-flex`라 인라인 baseline이 orb 박스 **하단**에 잡혀 인라인 흐름 표면에서 붙은 글자가 **7.66px 아래로 밀려 있던 결함**을 실측으로 잡아 교정(라벨↔붙은 글자 baseline Δ −0.089px · flex 표면은 `align-items`가 지배해 무해 = 두 표면 동시 성립). 사이트별 중복 활자 선언(색·굵기·tabular)은 공통으로 회수 · 운영자 260731 "솔빙 이런 것들 다 점 3개 통통 튀는 로딩 그래픽으로" → 구 소용돌이 링(thinking·prompting)·흩뿌린 입자(solving) 폐지 · 로더에 **붙어 있는 글자 전부**(경과시간·(1~3분)·재시도 회차)도 `.nm-shim` 빛 스윕 동참 = 도료 클래스 분리 `.nm-shim`(스윕) / `.nm-load>.nm-shim`(로더 라벨 타이포)). raw 점 로더(`gdots`·구 `nmld`) 신설 금지 = `check_loader_ssot()` **하드락(baseline 0)** · 컴포넌트 정본 = CII 「로딩·진행」 행 · 강제 = §6 게이트(운영자 260723 Q459~Q463).
16-1. **다운로드 버튼·결과 카드·이전 제작 카드 = 공용 CSS 단일정본**(운영자 260731 "모든 앱 이거로 다 적용 · 하나 바꾸면 다 연동되게 · 카드는 공통이라 1개만 있고 이를 참조해서 쓰는거임") — 정본 = **`viewer/nm-cards.css`**(라이브 뷰어 전부 `<link rel="stylesheet" href="nm-cards.css">`로 **참조만**). 색·플레이트(이미지 위 불투명)·✓팝·링·전체 시퀀스·결과 카드 컨텍스트(`.jvar-dl`·`.jst`·수정펜) 선언을 **뷰어 인라인에 복사 금지**(인라인 `<style>`이 링크보다 뒤 = 사본이 SSOT를 조용히 이김). 뷰어에 남길 것 = **호스트 기하(위치·크기·radius)만**. 다운로드 색 = 골드레몬 `--accent-4`(260731 일괄 통일 · 구 터쿼이즈 폐지). ⚠ 공용 CSS가 쓰는 토큰은 **전 뷰어 `:root`에 정의돼 있어야 한다**(미정의 = var() 댕글링 = 그 앱만 색 죽음 = 260731 실사고 · 값 동기는 STAGE4 자동 전파).
17. **신설 공용 부품·SSOT = 5축 동시 등재**(고아 SSOT 재발 방지 · 운영자 260723 Q465) — 새 공용 부품/정본을 만들면 **① 값·부품**(코드 · 예: `viewer/nm-loader.js`) **② 규칙**(§0 조항) **③ 컴포넌트 인덱스**(CII 행) **④ 기계 게이트**(`check_refs.py` 함수 + §6 행) **⑤ CLAUDE.md 구조**([15]) + 이 인덱스 각 정본 절에 행 추가 — 다섯 축에 **같은 커밋 계열로 등재**. 누락 = 발견 불가 고아(§0만 읽는 세션이 못 봄) = 규칙2 위반. (nm-loader 로딩 SSOT = 이 절차의 실증 선례 · Q459~464) · **얕은 기계 보조(WARN·비차단) = `check_ssot_linkage()`**(`viewer/nm-*.js`가 3축 미링크면 경보 · 코드에만 = 문서 비대 0 · Q466). · **명명 규칙 = 디자인 SSOT 공유 부품 신설 시 파일명 `nm-` 접두어 필수**(게이트 탐지 키 = `viewer/nm-*.js` · 안 붙이면 감시망 밖으로 샘 · 예: `nm-toast.js`) — 서드파티 vendored(`marked`·`purify`)·순수 동작 유틸(`cscroll`·`marquee`·`upload`·`sw`)은 디자인 SSOT 아님 = 별개 축 · rename 금지(참조 경로 절단 사고 방지) · 운영자 260723 Q471.

*→ 각 규칙의 값·셀렉터·라인·상세 = 아래 §1~6 정본 포인터를 따라간다(여기 복붙 금지 = 드리프트 차단).*

---

## 1. 값 (Value SSOT — 유일 정본)
| 파일 | 역할 |
|---|---|
| **`viewer/index.html` `:root`** | **값 SSOT = 유일 정본** — 색·radius·간격·blur·버튼·타이포·모션·z·press 토큰(개수 정본 = `design-tokens.lock`). 값은 **여기서만** 정의(라우터·문서에 hex/px 복붙 금지). |
| **`design-tokens.lock`** | 토큰명 락(도장) — 하드게이트: `check_refs` 토큰 락 정합(**토큰 신설·삭제 = 집합 불일치 차단** · 락 함수 실체 `check_lock()` = `build_design_mirror.py`). 개수 정본. 갱신 = `build_design_mirror.py lock`. |
| **`viewer/nm-svg.js`** | **공유 아이콘 도형 SSOT** — 뷰어 HTML들이 `<script>` 로드하는 아이콘/픽토그램 단일정본(CHECK·COPY·PASTE·DOWNLOAD·CHEV 등 · 로더 전수 = grep 실측이 정본 = 개수 복붙 금지 자기규칙). 색·치수가 `:root`면 **도형은 여기** = 값 SSOT 동급 축. 하드게이트 = `check_refs` `check_icon_ssot()`(인라인 재선언 rc=1). |
| **`viewer/nm-clip.js` + `viewer/nm-clip.css`** | **입력칸 클립 SSOT**(복사·붙여넣기·지우개/되돌리기) — 「모든 텍스트 입력창에 붙는다」가 계약(운영자 260803 "공식처럼가야함"). 동작 = `attachCopyPaste(el, true)` / 도형 = `nm-svg.js` COPY·PASTE·ERASE·UNDO·CHECK 의존(로드 순서 = svg **먼저**) / 스킨 = `.iobtn*`·`.pastefb*`. 상속 = 뷰어 head 2줄. 면제 = 숫자칩·PIN뿐. 하드게이트 = `check_refs` `check_clip_coverage()`(정적 입력칸 자동 발견 · 미부착 rc=1). |
| **`viewer/nm-cards.css`** | **다운로드·결과 카드·이전 제작 카드 공용 CSS SSOT**(운영자 260731) — 색·플레이트·✓팝·전체 시퀀스·결과 카드 컨텍스트 단일정본. 라이브 뷰어 12개가 `<link>`로 참조(인라인 사본 금지 = 인라인이 뒤라 SSOT를 이김). 규칙 = §0-16-1. |
| **`viewer/nm-hist.css`** | **이전 제작(공유 이력) 섹션 CSS SSOT**(운영자 260802 "결과·이전제작 일맥상통 공유") — 접이 헤더·그리드·카드·스태거 단일정본. thumb(이미지 스튜디오)·tr(번역)이 `<link>`로 참조. 규칙 = §0-16-1 동축(인라인 사본 금지). |
| **`viewer/nm-job.css`** | **제작 잡 카드(대기열·진행·완료) CSS SSOT**(운영자 260802 "이미지 스튜디오를 거의 따라서 가져온다") — `.jobs/.job/.jlab/.jvars/.jvar-dl/.jst/.jgrid/.jsave-row/.jdel` 단일정본. thumb(이미지 스튜디오)·index(설정 ▸ 다운로드 `#dlgrab` 받기 잡)가 `<link>`로 참조 = 두 화면이 **같은 부품**. 색 계약은 `nm-cards.css` 몫(역할 분리) · 규칙 = §0-16-1 동축(인라인 사본 금지). |

## 2. 규칙 (Rules)
| 파일 | 역할 |
|---|---|
| **`CLAUDE.md [4] 제1 핵심명령`** | **규칙1** — 계승/갱신 2택·raw 금지 최상위 선언(구 §🔒 = 260701 해체 · 현행 조항키워드 = "[4] 디자인 기틀을 흔들지 마라"). **이 규칙2 인덱스를 절대적으로 따른다.** |
| **`CLAUDE.md [4]` + `.claude/hooks/design_digest.py`(SessionStart 주입)** | 규칙 실체 — 구 `CLAUDE.md §🎨 디자인시스템` 전문(3계층·계승 디폴트·색 폐쇄형 팔레트·모달 셸 등)은 260701 인덱싱 후 압축서 해체(전문 = git 이력) → 현행 = CLAUDE.md [4] 골자 + 훅 자동 주입(계승/갱신 2택·CII·픽토 SVG·픽셀 실측) + `nomute-design` 스킬(아래 행) + §3 컴포넌트 정본. (포인터 정정 260717 — 유령 섹션 참조 제거) |
| **소머리·표기: `.claude/hooks/design_digest.py` 주입 3-1·CII §📐 인용 행 · 하드게이트 = `shared/check_refs.py check_soremeori()`** | 구 `CLAUDE.md §📐 명명·표기 표준`(머리표 계층·소머리 흰텍스트+형광 블릿·시각 KST·아이콘 SVG) 동일 해체 — 규칙의 살아있는 강제 = check_soremeori()·닫기X SVG 게이트(기계) + 훅 주입(세션). (포인터 정정 260717) |
| **`.claude/skills/nomute-design`** | 디자인 폴리시 스킬 — "느낌 좋게" 디테일 원칙(micro-interaction·광학정렬·hover/press·그림자). UI 작업 시 자동 적용. |
| **`.claude/hooks/design_digest.py`** | **SessionStart 계약 주입기** — 매 세션 `:root`에서 토큰 라이브 추출해 디자인 계약 자동 주입 + UI 프롬프트 리마인더. "읽어라"(소프트) 아닌 "떠먹인다". |
| **`디자인기틀/디자인토큰_제1핵심명령_복제전문.md`** | **(이식용·타 레포 메타규칙)** 일반형 개념규칙 + 바인딩표 9슬롯. ⚠️ 그 바인딩표의 「노뮤트 값」 열은 *예시* — **이 레포 실경로 정본은 이 인덱스**(둘 중 하나만 갱신해 stale 되지 않게 실경로는 여기로 단일화). |
| **`디자인기틀/플레이그라운드_포터블.md`** | **디자인 제안 형식 정본(+이식용 포터블)** — 제안/시안/튜닝 = 만지는 플레이그라운드 HTML(구성 **8요소**·튜닝 변수 규약·검증 7항 표준·전달 통로·안티패턴 골격 스펙 · 근거 이력 = `docs/reports/260801_플레이그라운드_룰_개진.md`). `CLAUDE.md [5]·[E9]`가 이 파일을 얇은 포인터로 따른다. ⚠️ 그 바인딩표의 `yeta(참조 구현)` 열 = 원조 레포 예시(이 레포 파일 아님) — **이 레포 실측 바인딩 정본 = CLAUDE.md 【레포 바인딩】 [5]**(값 재열거 안 함 = 복제전문 행과 동일 규율). |

## 3. 컴포넌트 (Components — 값과 별개 축)
| 파일 | 역할 |
|---|---|
| **`디자인기틀/CII_컴포넌트계승인덱스.md`** | **컴포넌트 정본 셀렉터 인덱스** — 새 버튼·모달·입력칸·아이콘은 이 표의 정본을 복사·계승(재설계 금지)·없으면 등재. |
| **`디자인기틀/구성도/00_가이드북_버튼인터랙션.html`** (+ `.md` + `버튼인터랙션_기틀북.css`) | 버튼·눌림(`--press-*` 사다리)·시맨틱 모션·탭 피드백·재확인·픽토그램 마스터 가이드북. 새 버튼·이식은 여기 패턴 계승. |
| **`디자인기틀/구성도/`** (13 HTML[허브 `index.html`·가이드북 포함] + `base.css`·`버튼인터랙션_기틀북.css` + `노뮤트_웹UI_공통규약.md`) | 사람이 보는 인터랙티브 사인오프 블루프린트(브랜드 에셋·진행 결과 상태·텍스트 위계·픽토그램·타이머 모프 등). |

## 4. 확립본·감사 (사인오프 스냅샷 + 감사 근거 — 시각 정본·참조용)
> 확립본 = 특정 시점에 운영자가 눈으로 사인오프한 *시각 스냅샷*. **값 정본은 여전히 `:root`** — 확립본은 "그때 이렇게 확정했다"의 증거·대조 기준(드리프트 판정 시 참조). append-only(새 사인오프 = 새 파일 + 여기 행 추가 · 옛것 삭제 금지).

**ⓐ 확립본(사인오프):**
| 파일 | 확립 대상 |
|---|---|
| **`docs/reports/260704_브랜드컬러_확립본.html`** | (구) 브랜드 컬러 팔레트 확립 — 260705 개편 전 시점 증거로 보존(append-only). |
| **`docs/reports/260705_브랜드컬러_확립본_v2.html`** | (구) 터쿼이즈 개편 1차 — 260705 2차 조정 전 시점 증거 보존. |
| **`docs/reports/260705_브랜드컬러_확립본_v3.html`** | **브랜드 컬러 확립본 v3(현행)** — 2차 조정(정치↔국제 크로스·accent-4 골드레몬·흰색 1종 단일화 · 운영자 지목 260705). 값 정본은 `:root`(여긴 포인터만). §0-2 **색 폐쇄형 팔레트 규칙**의 시각 정본. |
| **`docs/reports/260704_모달셸팔레트_확립본.html`** | 모달 셸 팔레트 4종(헤더 띠·탭 띠·컨테이너 글래스·앵커) + 글래스모피즘 데모. **모달·팝업 셸 규칙**(CII 정본 셀렉터 · `.modal-head`·`--modal-glass`)의 시각 정본. |
| **`docs/reports/260706_언락밝기_플레이그라운드.html`** | **플레이그라운드([5]) 완전판 확립본(yeta 원본 복사본)** — 만지는 시안 5요소{실물 재현·프리셋·슬라이더·선택값 복사[`near()` 계승/갱신 자동 표기]·한계 각주} 골격의 `이 수준` 참조 원본(포터블 `<확립본>` 슬롯 충족). ⚠️ 내부 `:root` = yeta 팔레트 사본 = **형식·골격 참조 전용**(이 레포 값 정본 아님 · 보고서 = check_refs 비대상). |
| **`docs/reports/260707_자막합성설정_플레이그라운드.html`** | **플레이그라운드([5]) 이 레포 자체 제작 완전판(첫 본토판)** — 자막기(/ly) 합성 설정을 9:16 완성 영상 좌표계에서 실물 재현{릴스 UI 목업·장면 3단·현행 고스트·카라오케 스윕 실재현·상단 캡 수식 동일} + 5요소 전부{프리셋 4(현행 3+★캡션 회피)·슬라이더(확장 축 구분)·선택값 복사(계승/갱신·⚠콘텐츠색 자동 표기)·한계 각주}. 내부 `:root` = viewer 실측 사본(형식 전용 · 값 정본은 viewer `:root` · 번인 수식 정본은 `ly_burn.py`). |

**ⓑ 결과·감사 근거(사인오프 아님 · 참조):**
| 파일 | 성격 |
|---|---|
| **`docs/reports/260704_팔레트개편_결과.html`** | 팔레트 개편 반영 결과 + 검증(작업 산출). |
| **`docs/reports/260628_UIUX_기틀_분신술10인.html`** | UIUX 기틀 분신술 10인 감사(드리프트 근원·핵심 규칙 표). CII 상세 감사 근거. |
| **`docs/reports/260628_디자인기틀_전수감사_분신술10.html`** | 디자인 기틀 전수감사. |
| **`docs/reports/260628_STAGE3_공유구조토큰_분신술10.html`** | 4뷰어 구조토큰 공유(tokens.css) 설계 감사. |

## 5. 거울·빌더 (파생 — 직접수정 금지)
| 파일 | 역할 |
|---|---|
| **`viewer/tokens.css`** | `:root` 구조토큰(색 제외) 거울 — 도구 뷰어들 `<link>` 계승용(로더 전수 = grep 실측이 정본 · 구 '4뷰어' 표기 = comp 폐지로 스테일 — 평의회 Q165 정정). **build 산출 = 직접수정 금지**(다음 build에 덮어씀). |
| **`디자인기틀/구성도/base.css`** (`AUTO-MIRROR` 블록) | `:root` 전체 거울 — 구성도 HTML 상속. **build 산출 = 직접수정 금지.** |
| **`shared/build_design_mirror.py`** | 거울 빌더 — `build`(SSOT→거울 투영: base.css STAGE1-2 + tokens.css 구조토큰 STAGE3 + **도구 뷰어 공유 팔레트 STAGE4**) · `lock`(토큰 락 갱신) · `check`(거울 정합 검사 = 부정합 하드차단). **STAGE4 = 색 전파(운영자 260723 Q466)**: index `:root` 공유 팔레트(accent/의미색 62색·`var()` 체인 해결)를 도구 뷰어 9개 inline `:root`의 동명 토큰 값에 자동 복사(뷰어 기정의 토큰만 값 교체) · 툴톤(`--bg`/`--pan`/`--line*`/`--fg`/`--mut`/`--glass*`/`--modal*`/`--thumb`)은 §0-2 팔레트 예외(뷰어별 정체성)라 제외 → **"index 색 1곳 수정 → `build` → 전 화면 자동 반영"**. 검증(드리프트 차단) = `check_refs` **`check_palette_sync()`**(build=전파 / check=차단 2중). |

## 6. 게이트 (강제 — 기계 차단)
| 파일 | 역할 |
|---|---|
| **`shared/check_refs.py`** | 디자인 게이트 묶음. **`check_design()`** = raw 강조색(accent 코어색을 var() 없이 raw로)·초록 시그니처 워시(green_wash) 하드차단 rc=1 / hex·blur·죽은토큰 WARN(baseline ratchet). **`check_icon_ssot()`**(아이콘 SSOT)·토큰 락 정합(락 함수 실체 = `build_design_mirror.py check_lock()`)·`build_design_mirror.check()`(거울)·**`check_palette_sync()`**(팔레트 핀 = 도구 뷰어 inline 공유 팔레트 accent/의미색이 index 해결값과 동값인지 대조 · 인라인 색 복사 드리프트 하드차단 · `build_design_mirror.py` STAGE4 전파의 게이트 짝 · 툴톤 제외 · 운영자 260723 Q464) = 하드차단 / `check_x_char`(문자 도형)·`check_soremeori`(소머리)·`check_autocomplete`·**`check_ssot_linkage`**(공유 부품 nm-*.js 3축 링크 WARN·비차단 · §0-17 5축 보조) = 서브체크. ⚠️ raw 값·패턴의 정본은 코드 그 자체(여기 hex 복붙 안 함). |
| **`shared/check_refs.py check_loader_ssot()`** | **로딩 표기 SSOT 하드락(운영자 260723 Q463)** — 전 뷰어 로딩·진행 표기 = `viewer/nm-loader.js window.nmLoader` orb 단일. raw 점 로더(`gdots`·구 팩토리 `nmld`) 신설 = rc=1 차단(**baseline 0** · tokens.html 데모 제외). 컴포넌트 정본 = CII 「로딩·진행」 행 · 규칙 = §0-16. |
| **`shared/check_refs.py check_track_parity()`** | **자간 판정 기준 단일화 게이트(운영자 260802 "잴 때 항상 한개의 기준에 따라서 짓던지, 두개의 기준에 다 맞추던지 하자")** — 3축. ① **판정 자 = advance 단일**(뷰어 `measureText().width` · 서버 오버레이 `draw_t getbbox` ≈ advance 반올림 · 서버 헤더 `font.getlength`). ⚠ **구 「잉크폭(actualBoundingBox) 기준」 서술은 폐기다** — 260802 2차가 그렇게 선언했으나 PIL 실측이 반증했고(단일 글자 getbbox = advance 반올림 · 공백 17.7→18 · 잉크 아님), 잉크 강제가 **자간 0에서도 공백 실종·글자 압착**을 그린 진범이라 260802 3차에 advance로 원복했다. 지금 게이트는 `basis:'ink'`면 **rc=1로 차단**한다 — 이 표가 옛 서술을 이고 있으면 정본을 읽고 만든 결과물이 게이트에 막히는 자기모순이 난다([4-2]의 반대 사고 · 260807 평의회7 검출). ② **한도 상수 3면 동일**(뷰어 `TRK` ↔ `nomute_overlay.SPECS` ↔ `thumb-make.yml fit_tracking`) + 힌트 문구에 한도·하한 하드코딩 0. ③ **미리보기 렌더 = 절단 통과분**(운영자 260807 "입력 불가인 거는 아예 출력 안 되게") — 자간을 그리는 `.cpv` 구간이 원문(`lines`·`csub`·`ctitle`)을 직접 그리면 rc=1이고, `ovFit`/`hdrFit` 반환을 받은 식별자를 실제로 쓰는지까지 본다(양성 축 = 변수 세탁·데코이·호출 누락 우회 차단 · 이름은 자유 = 리네임 허용).
| **`shared/check_refs.py check_gate_docs()`** | **게이트 문서화 메타 게이트(운영자 260723 Q468 "만들어놓고 안 봄 구조 차단")** — 모든 게이트(`def check_*`)가 정본 문서(CLAUDE.md·이 SSOT·규칙/큐레이션 문서)에 *이름으로* 등재됐는지 대조 → **미등재 신규 게이트 = rc=1 차단**. 이 인덱스 완전성 소프트룰(서문 "새 기틀 = 인덱스 행 추가·누락 = 규칙2 위반")의 **기계화**. 기존 미등재 = `_GATE_DOC_BASELINE` 면책(소급 문서화 TODO·축소 지향 · 대량 소급 강제 안 함 = 품질 유지). 신규 게이트 추가 시 = 이 §6(디자인 게이트 표)/`docs` 큐레이션/CLAUDE.md에 이름 등재 필수. |
| **`shared/check_refs.py check_tabs_headers()`** | **탭 헤더 등재 게이트(운영자 260724 한 수 · 순수 인프라)** — `index.html` `THUMB_TABS`·`CAP_TABS`·`ASK_TABS`의 모든 스튜디오 `/x.html` src가 `viewer/_headers`에 `/x`·`/x.html` 두 경로 `no-cache`로 등재됐는지 대조 → **미등재 = rc=1 차단**. 신설 스튜디오가 캐시 계약을 빠뜨려(tr 260721·song/nb/sb 과거 드리프트) 새 배포가 하드새로고침 없이 반영 안 되던 사각 봉쇄(`check_gate_docs`와 동일 '만들고 등재 안 함' 봉쇄 철학). |
| **`shared/check_refs.py check_model_ids()`** | **모델 ID 드리프트 게이트(운영자 260725 한 수 · 정본 = `shared/models.json`)** — 모델 ID가 20+ 호출처(스크립트·워크플로·functions·뷰어·지침·법령)에 리터럴로 흩어져 있어 승격마다 전수 grep = 빠뜨림(직전 승격 실측 = 22곳). 런타임 결합(호출처가 json을 읽게)은 Cloudflare Functions·정적 뷰어에 파일 접근이 없어 불가 → **「정본 1곳 + 기계 치환 + 이 게이트」 3점**. 검사 = ①스캔 경로의 `claude-*` 리터럴이 전부 정본 등재 ID인가(오타·미등재 모델 차단 — 신설 첫 런에서 `fb_classify.py` haiku 핀 채굴) ②정본 `retired`(구세대 ID·표시명)가 0곳인가(**승격 시 한 곳 빠뜨림 봉쇄 = 본체**) ③**벤더 축(비-Claude 종량제 = gemini·lyria·gpt·replicate RVC)**이 `family` 정규식 기준 정본 ID(+허용 알리아스)만 쓰는가(ID 어긋남 = 곧장 런타임 실패·오과금 · 구 "실제 ID 바뀌면 여기 교체" 주석 의존 해소 · 실측 = `gpt-5.6-sol`이 `sb_gpt.py`·`sbmake.sh` 2곳 동기 필요) → 위반 rc=1 차단. 승격법 = `python3 shared/apply_models.py <티어> <새ID> "<새 표시명>" "<새 한글명>"` → `git diff` → 이 게이트로 검산 → 커밋. 스캔 범위·제외(원장·보고서·동결본·산출물 = [11] append-only 보존)도 `models.json scan`이 정본이라 게이트에 하드코딩 없음. ⚠️ 표시명은 `Opus 5인/5명`(인원)·버전 연장(`Opus 6` ⊂ `Opus 6.5`) 오탐을 경계 가드(`shared/apply_models.py` name_rx·id_rx)로 제외 — **인원은 '명'으로 쓴다**(모델명과 붙어 읽힌다). |
| **`shared/check_refs.py check_sc_ts_contract()`** | **수집함 시간축(scTs) 계약 게이트(운영자 260725 한 수 · Q522 회귀 실물발)** — 나이 판정은 신규↔누적 칼럼 분배·랭킹 감쇠(timeAcc·ageMul)·배지 소멸선의 **공통 입력**이라 여기가 틀어지면 화면 전체가 조용히 뒤집힌다(실측: 과거튐 6h 가드가 어제 기사 439/800 회춘 → 신규 4h칼럼 오염 40건·누적 상위 20/20 점유 · 카드 표기는 `published` 원본이라 "1일 3시간 전"인데 자리는 최상단 = 코드를 열어야만 보이는 회귀·반나절 방치). 검사 = `viewer/index.html`의 `scTs`를 **원본 그대로 추출해 node로 실행**(파이썬 재현 0 = 게이트 자신이 드리프트원이 되는 것 차단 · `check_fast_max_h_parity`의 '값 대조'보다 한 단계 위 = **동작 대조**) + 고정 픽스처 5조(`_SCTS_CASES`) 판정 → 위반 rc=1 차단. ①늦수집 옛기사(pub −27h·fs −3h·rc1) = `published` 유지 + `FAST_MAX_H` 밖 ← **Q522 회귀축** / ②정상 지연 / ③미래 오기록 가드(260618) / ④역전+`rc≥FOLLOW_RC_MIN` 예외(260630) / ⑤역전+rc낮음 보수성(§★). 라이브 `candidates.json` 미사용 = 판정 결정성(스크래퍼 자동커밋 데이터로 코드 게이트가 오발하면 안 됨) · node 없으면 스킵 · 추출 실패 = fail-closed. |
| **`shared/check_refs.py check_follow_enters_parity()`** | **누적 보조진입(followEnters) 크로스랭귀지 패리티 게이트(260805 8인 평의회 · `check_fast_max_h_parity`와 동형)** — 누적 칼럼 진입 술어(`cross≥8 OR isBreaking OR followEnters`)의 파이썬 사본이 `scraper/daily_health.py` `_cum_enter` 에 있다(파이썬이 viewer 를 못 읽어서). 값이 갈리면 **"화면엔 떴는데 묻힘 계기판은 여전히 묻혔다고 보고"** 하는 무증상 드리프트 = §1 자동감시가 죽는데 화면은 멀쩡해 아무도 모른다. 검사 = viewer `FOLLOW_CROSS_MIN`·`FOLLOW_RC_MIN` ↔ `_cum_enter` 술어 리터럴 전건 동일 → 위반 rc=1 차단 · 추출 실패 = fail-closed. ⚠️ 신설 사유 = 구판은 같은 술어가 `daily_health.py` 안에만 **손복사 3벌**(`_dominance`·긴급부스트 신선창·묻힘 계측)이었고 패리티 게이트가 0이라 뷰어만 고치면 조용히 갈라졌다 — 이번에 1벌(`_cum_enter`)로 수렴시키고 게이트를 세웠다. ⚠️ 이 미러의 구조적 한계 = **병합 전 원자료 기준**이라 화면과 다를 수 있다(viewer 는 `applyAutoGroups`/`mergeDecorate` 로 `group_id` 형제를 접고 cross 를 **단순 합산**한다 — 주석 정본 "union 아닌 합"). 260805 실측 사고: 「평택 미군기지 무단침입」이 원자료 cross 6 이라 계기판엔 '묻힘'으로 잡혔으나 화면에선 조선판 6 + 뉴시스판 2 = **8 로 `CROSS_MIN` 단독 통과해 이미 노출 중**(누적 30위 · 렌더 캡처 실증)이었고, 8인 평의회 전원이 원자료만 보고 "진입선에서 1 부족해 소멸"이라 오진했다. **계기판 숫자로 진입선을 손대기 전에 반드시 실물 렌더로 대조하라.** |
| **`shared/check_refs.py check_ssot_coverage()`** | **정본 커버리지 역방향 게이트(운영자 260725 한 수 · `check_gate_docs`의 반대 방향)** — gate_docs가 「게이트 → 문서 등재」를 보는 동안 비어 있던 역방향: **문서가 「정본 = `<코드/데이터/워크플로>`」로 선언한 축 중 기계 게이트가 없는 것**을 센다 → 신규 = rc=1 차단 · 기존 8축 = `_SSOT_BASELINE` 면책(축소 지향). **왜**: 이 레포 게이트 4종(팔레트·로더·탭 캐시·모델 ID)이 전부 *사고 후에* 붙었고 사고 전 상태는 매번 「정본 선언 있음 + 기계 없음」이었다 — 그 상태를 사고 전에 목록으로 뽑는다. 신설 실측 = 선언 15축 중 보유 7·미보유 8. ⚠️ 산문(.md) 정본은 기계 대조 대상 아님(제외) · 해석은 `os.walk`(파이썬 `glob('**')`는 숨김 디렉터리를 안 봐서 `.github/` 파이프라인이 통째로 누락된다 = 신설 때 실측) · 베이스라인 리터럴은 대조 전 제거(안 그러면 면책 목록이 스스로 '게이트 있음'이 되는 자기무력화 — 첫 런에서 미보유 9축이 0으로 둔갑한 실측). |
| **`.githooks/pre-commit`** | **커밋 시점 강제** — `check_refs.py` 실행(어느 모델·도구가 커밋해도 게이트 통과 강제). `design_digest.py`가 `core.hooksPath` 자동 배선. |
| **`.claude/hooks/design_gate.py`** | **PostToolUse 훅** — UI 파일(`viewer/*`·`디자인기틀/구성도/*`) 저장 시 디자인 게이트 자동 실행(위반 즉시 표면화) + **diff 게이트(260713)**: 이 편집이 *추가한 줄*의 신규 raw px(font-size·radius·gap·padding·margin·blur 인자)를 즉시 차단(exit 2)하고 근접 토큰을 자동 제안 — 기존분 관용(diff 기반 = 상쇄 위장 면역) · `/* raw-ok: 사유 */` 같은 줄 주석 = 광학 보정 유일 통로 · letter-spacing = WARN(토큰 사다리 부재). 9.5px류 임의 창작 종전 축. |
| **`shared/smoke_rank.js`** | **랭크 필 광학 정렬 상비 실측(260717 Q05)** — 디자인 계약 3-4(필 4분할 중심 = 문자열 잉크 중심 Δ≤0.5)의 기계화: 정수 좌표 격리 스테이지 + DPR3 잉크 프로브(라이브 배치 캡처 = 위상 노이즈 부적격). 대상 = `.tcard-rank`·`.tpc-rank`·`.tcard-cov .trend-chg`(NEW·▲▼)·`.tpc-new`·`.qbadge`(+head) · 코어 14종(11타깃 · Q06 배지전수) · ⚠ 세로 잉크 = 베이스라인 1px 스냅이라 패딩 무력 → 이너 `<i>` transform이 정본 보정(260717 실측) · `smoke_all.sh` 5호기 · 포트 8811~(수동 실행 전용 · 훅 편입 금지 [15]) · 보정값 재튜닝 = 이 스모크가 수치 판정. |
| **`shared/smoke_popup.js`** | **앵커 팝업 셸 SSOT 글래스 패리티 상비 실측(260717 "팝업 패리티 스모크")** — 「홀로 튄 팝업」(SSOT 통일 전 태생 = 불투명 글래스로 형제 사이서 솔리드로 튐 · `.min-pick`·`.failmenu` 2연발이 사람 눈/수동 스윕에 의존했음) 부류를 커밋 전 기계 검출. 합성 프로브로 앵커 팝업 그룹(`.pmenu`·`.filterpop`·`#linkpop`·`.min-pick`·`.failmenu` · `.msgpop`=대기열 함 셸 대조축 · `.sc-rsn`=전용 축) 셸 글래스(배경·프로스트 blur·테두리·그림자) 멤버 동일성 판정(radius·padding·위치·애니는 개별 = parity 밖) + 전역 불투명 글래스 로그 스캔(backdrop-blur인데 배경 alpha≥.85 & 화이트리스트 밖 = 신규 고아 경보 · `.sc-rsn` 무채 saturate(0)은 §0-9 글래스 규칙의 무채색 의도 예외). 코어 5종 · `smoke_all.sh` 6호기 · 포트 8816~(수동 실행 전용 · 훅 편입 금지 [15]). |
| **`shared/smoke_sweep.js`** | **정렬 계약 상비 실측(운영자 260720 Q256 "한수 ㄱ")** — 70캡쳐 모바일 스윕(Q254)이 사람 눈으로 잡던 1px급 정렬 회귀의 '확정 계약 축'만 상비화: index `#totop` 픽토 4분할(계약 3-4) · 대기열/메시지함 X셀↔행 qact R-라인(운영자 260712 정본 · Q255 편심 회귀 가드) · thumb `.csec` 소머리 좌변 그룹 내 균일. 합성 `.qrow` 프로브(라이브 무접촉·데이터 무의존) · 코어 8종 · `smoke_all.sh` 10호기 · 포트 8836~(수동 실행 전용 · 훅 편입 금지 [15]) · ⚠ 휴리스틱 후보 스캔 = 비채택([4-1] — 스윕은 1회성 사냥, 상비는 정본 주석이 명시한 계약 축만). |
| **`shared/smoke_wip.js`** | **Image Studio 진행 타일(.wip) 계약 상비 실측(운영자 260723 Q470 — Q469 1회성 프로브의 상비 승격)** — 제작 진행 표시의 '결과 자리 이주'(Q469: edit 대기 스테이지 이식 타일) 계약을 커밋 전 기계 검출: 합성 잡 주입(페이지 전역 JOBS·renderJob 실호출 = 라이브 무접촉 · smoke_geni 선례)으로 {스택 최신 위 · 좌상단 경과 m:ss+라이브 틱 · 우상단 토큰 표기 · 스캔라인 애니 · Solving 로더 · 형제 마진 = --sp-2 해결값 · 빈 상태 안내 상호배제 · 목업 클론(id 스트립) · done/err 타일 제거·복귀} 판정. 코어 12종 · `smoke_all.sh` 15호기(직렬 꼬리) · 포트 8861~(수동 실행 전용 · 훅 편입 금지 CLAUDE.md 레포구조 절). |
| **`shared/smoke_tabdz.js`** | **하단 네비 메뉴 전환 디졸브 계약 상비 실측(운영자 260804 Q1356 — Q1355 1회성 프로브의 상비 승격)** — 「나가는 화면에 **사라지는 과정**이 있는가」를 기계 계약으로 박는다. Q1355 이전 구판은 `showTab`이 나가는 뷰를 `hidden`(display:none)으로 **한 프레임에** 지워(매 프레임 opacity 표본 `["1.000","hidden"]`) 「번쩍」였고, 이 결은 **CSS 두 줄·JS 한 경로**로 서 있어 누가 지워도 화면 말고는 아무것도 안 깨진다. 코어 9종 = {D1 사라지는 과정(단조 감소) · D2 교체 시점이 `--dur` **토큰**을 따름(JS 하드코딩 금지) · D3 뷰헤드 동행(등장용 `transition-delay:.06s` 누수 검출) · D4 점등 즉시(탭 반응 0ms) · D5 실속도 4메뉴 순회 착지·잔여 투명 0 · D6 연타 = 마지막 하나만 · D7 `showTab` 직행은 **동기 유지**(토스트·딥링크 `open()` 순서 가드) · D8 전정계 즉시 교체 안전낙하 · D9 페이지 에러 0}. D1~D4는 `--dur`를 런타임 1.2s로 **확대 관측**(파일 무접촉 · 헤드리스 rAF 스로틀 위양성 차단) · D5~D8은 실속도 착지만 = 시간 의존 어서션 0. **킬테스트 2종 실증** = ⓐ 디졸브 통째 제거(Q1355 이전본) → D1·D2·D3 FAIL rc=1 ⓑ 뷰헤드 지연 해제 **한 줄만** 제거 → D3 단독 FAIL(Δ0.055>0.02). 정본 = `viewer/index.html` `.tab-dz`·`showTabFx`·`tabDzMs` · `smoke_all.sh` 자동발견 편입 · 포트 8891(기존 8791~8885·8911~ 대역과 분리) · 전후 = `docs/reports/260804_메뉴전환_디졸브_전후.html`. |
| **`shared/playground_template.html`** | **플레이그라운드 동결 골격 템플릿(260713 · `플레이그라운드_포터블.md` 골격 규약)** — 시안 제작 = 이 파일 복사 후 CONFIG(변형 갤러리 = 부품 1개당 방식 N개·7축·미리보기·프리셋)만 교체. 골격 재작성 = 위반. 하드게이트 = `check_refs` `check_playground()`(`data-pg-*` 5요소 + **⑥미리보기 고정(PC+폰 티어 sticky · 골격은 하드·개별 시안은 WARN 소급면책)**·near() 계승판정·재렌더 스크롤 보존·자유 hex 금지 — `data-pg-template` 스탬프 세대만 게이트 = 레거시 소급 실패 0). |
| **`.github/workflows/check-refs.yml`** | PR마다 `check_refs` 자동 실행 → 드리프트면 PR ❌. |

---

## 🔧 유지 규칙 (이 인덱스 자체의 불변)
1. **새 디자인 기틀 파일 = 이 인덱스에 행 추가**(누락 = 규칙2 위반 = [4] 위반). ⚠️ **소프트룰 — check_refs가 인덱스 완전성을 기계 검사하지 않는다**(사람·모델이 챙김).
2. **포인터만 — 실체(hex·px·개수·라인번호·셀렉터) 복붙 금지**(드리프트원). 값은 `:root`, 규칙은 이 문서 §0(구 CLAUDE.md [4]·[4-1]·§0 앵커 = 260729 해체), 컴포넌트는 CII, 패턴은 check_refs 코드에.
3. **확립본은 append-only**(새 사인오프 = 새 확립본 파일 + §4ⓐ 행 추가 · 옛 확립본 삭제 금지 = 시점 증거 보존). 결과·감사는 §4ⓑ.
4. 이 인덱스 수정 = 디자인 기틀 = CLAUDE.md [9] 평의회 검증 대상(단순 포인터 정정은 [9] 260716 단서로 비대상 · 규칙2 편입은 운영자 승인).
