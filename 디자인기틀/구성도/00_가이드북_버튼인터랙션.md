# 버튼 인터랙션 가이드북 (노뮤트 UI 구성도)

> 노뮤트 뷰어의 **모든 버튼**을 한 언어로 정리한 단일 레퍼런스. 새 버튼·이식은 여기 패턴을 **계승**한다(재설계 금지 = §🎨).
>
> - **값 SSOT** = `viewer/index.html :root` (라이브 렌더값) → `디자인기틀/구성도/base.css` AUTO-MIRROR 투영. 이 문서는 값 정의 0(참조만).
> - **규칙 SSOT** = `CLAUDE.md [4]·[4-1]` + `디자인기틀/디자인기틀_SSOT.md §0`(구 §🎨 = 260701 해체) · **게이트** = `shared/check_refs.py`.
> - 충돌 시 **viewer :root가 정본**. HTML판 = `00_가이드북_버튼인터랙션.html` · CSS판 = `버튼인터랙션_기틀북.css`.

---

## 1. 토큰 (base.css 계승)

| 그룹 | 토큰 → 값 | 용도 |
|---|---|---|
| 강조 | `--accent #00EED2` · `--accent-bright #5AFFE6` · `--accent-dim rgba(0,238,210,.13)` · `--on-accent #032322` · `--accent-rgb 0,238,210` | 주 액션·터쿼이즈. on-accent=터쿼이즈 위 글자(260705 개편) |
| 의미색 | `--arm #FFE13D`(재확인=accent-4) · `--danger #e23b2a`(accent-3) · `--warn #FFE13D`(accent-4) · `--amber #FFE13D`(accent-4) · `--info #0FFD02`(accent-5 그린) · `--thumb #FFE13D`(accent-4) | 260704 통일: arm·warn·amber·thumb·스크랩 = accent-4(라임옐로) · danger=accent-3 · info=accent-5. 썸네일은 요약요청 네온그린(accent)과 라임옐로로 구분 |
| 글래스/선 | `--glass2` · `--line .08` · `--line2 .06` · 버튼표면 `rgba(255,255,255,.06)` hover `.13` | 유리 표면·테두리 |
| 반지름 | `--r-s 9` · `--r-m 11` · `--r-l 16` · `--r-pill 999` | 아이콘버튼=r-s · 알약=pill |
| 간격 | `--sp-1 6` · `--sp-2 12` · `--sp-3 18` · `--sp-4 24` | 4px 그리드. 임의 px 금지 |
| blur | `--blur-s 8` · `--blur-m 14` · `--blur-l 22` · `--blur-xl 30` | 글래스 backdrop(+`-webkit-`) |
| 버튼크기 | `--btn 34` · `--btn-sm 30` · `--btn-xs 22` | 아이콘버튼 정사각 |
| 모션 | `--ease cubic-bezier(.2,.7,.3,1)` · `--dur .18s` · `--dur-fast .12s` | 전이·이징 |

> 신규/수정 CSS는 raw hex/px/blur 금지 → `var()` 토큰. 없으면 가장 가까운 토큰, 정 없으면 `:root`에 토큰부터 추가.

## 2. 버튼 아키타입 (계승 인벤토리)

| 아키타입 | 클래스 | 규격 |
|---|---|---|
| 아이콘 버튼 | `.sbtn` | 34px 정사각 · `--r-s` · 글래스 `.06` · 라인SVG 2.0 · 누름=시맨틱 모션 |
| 닫기(X) | `.tool-x`·`.dlg-x`·`.askx`·`.ed-x`·`.mx`·`.qh-x`·`.fb-x`·`.sc-rsn-x`·`.askimg-x` | 30px(또는 컨테이너 맞춤) · `display:grid;place-items:center` · 라인SVG X(✕문자/이모지 금지) · 누름=회전 |
| 새로고침 | `.pmenu-rf`·`.qh-rf` | 라인 refresh SVG · hover 180° 회전 · 누름=회전+축소 |
| 알약 링크 | `.dlg-h a`(원문↗) | height 34 · pill · 글래스 · 단순 액션(즉시·재확인 X) |
| 전송(비용) | `.sc-pick`·`.asksend`·`.revsend`·`.mkbtn` | 재확인(arm)→게이지→완료 상태머신 · 비용/발사 전용 |
| 토글 | `.toggle`/`.tk` | 상태형 · 노브 OFF 반투명(.65)·ON 불투명+글로우 |
| 커스텀 체크 | `.dlbox`/`.dl-cbox` | 원형 인셋 체크 · ON=E3+강한 링+펄스(§7) |
| 다운로드/저장 | `.sbtn.save`(`.fi-saveall`은 도먼트 — 마크업 0·잔존 CSS만) | 누름=↓바운스(시맨틱) |
| 복사 | `.copy`·`.titlecopy`·`.askclip` | 클릭→체크 변신(`.ok`) |

> ⚠️ **이모지를 조작 버튼에 쓰지 않는다**(OS·폰트 편차로 통일 깨짐). 라인 SVG만. 콘텐츠 표식(💡📊🚨)에만 이모지.

## 3. 인터랙션 언어 — 3겹 피드백

1. **눌림(press)** — `:active{transform:scale(.9);filter:brightness(.88)}` (알약=`.94`·아이콘=`.85~.9`). 누르는 동안. ⚠️ 실전 이식은 raw scale 금지 — viewer :root의 `--press-pico/xs/s/m/l` **5토큰 사다리**를 `scale(var(--press-*,폴백))`로 계승(260628 토큰화 · 이 예제 수치는 데모 렌더값 · 260702).
2. **시맨틱 모션** — 누르면 아이콘이 의미대로 움직임: **회전**(기본·새로고침 360°·닫기) / **↓바운스**(다운로드·저장). `document` 위임 1핸들러(capture)가 `closest()`로 아이콘버튼 잡아 svg에 `ic-spin`/`ic-bounce` 부여(`e.defaultPrevented` 가드). 기존 svg 애니(nav·refresh hover·FAB)는 셀렉터서 제외.
3. **탭 피드백** — 완료=초록 플래시(다운로드 ✓) / 열림=흰빛 펄스(외부 링크). 의미별 색 분리.

```
@keyframes ic-spin   { to { transform:rotate(360deg); } }
@keyframes ic-bounce { 0%{transform:translateY(0)} 45%{transform:translateY(2.5px)} 100%{transform:translateY(0)} }
.ic-spin{animation:ic-spin .5s var(--ease)} .ic-bounce{animation:ic-bounce .42s ease}
```

## 4. 재확인 — 비용/파괴 액션만

돈/시간 나가거나 되돌리기 어려운 액션엔 게이트. 공짜·즉시·가역엔 **안 건다**(피로 방지).

| 액션 | 등급 | 재확인 장치 |
|---|---|---|
| PICK(수집함) | 비용·일회 | 2탭 arm→게이지→**소진 잠금**(원복 X·탭=피드) |
| 카드 슛(유료 이미지) | 비용·비가역 | PIN 입력 게이트 *(현재 즉발 — 운영자 260614 정책)* |
| 슬롯 재생성·수정요청 | 비용 | 2탭(`.confirm` arm→발사) |
| 카드 변경·내역 삭제 | 파괴 | 확인 팝업 |
| 모달 내 send(요약요청·재분석) | 비용 | 모달 게이트(내용 필수)로 충분 — 2탭 불요 |
| 요약/다운로드/원문/복사/토글 | 단순·가역 | 없음(즉시 ✓) |

**전송 상태머신 라벨(전 메뉴 고정)**: 대기("전송"/"발사"/"합성"/"생성") → **"재확인"**(arm 앰버·3s 타임아웃) → **"전송 중…"**(초록 게이지 .72s) → **"완료"**+✓ → 원복. 실패=shake+빨강. (대기 라벨만 메뉴별 동사 허용.)

## 5. 픽토그램 규격

| 속성 | 값 |
|---|---|
| 스타일 | `line` (`fill:none`) |
| stroke-width | **2.0** (체크마크만 2.4~2.6 허용) |
| cap / join | `round / round` |
| viewBox | `0 0 24 24` |
| 색 | `currentColor`(버튼 색 상속) |
| 크기 | 버튼 토큰 비례(34→15·30→14·xs→13px) 또는 `1em`(글자 대체) |

- **닫기 X 경로** = `M6 6 18 18M18 6 6 18`
- **이모지 정책**: 금지=조작 버튼/메뉴(✕·↻·✨·🖌️·📝→SVG) / 허용=콘텐츠 의미 표식(💡 시사점·📊 핵심·🚨 긴급).

## 6. 여백 · 크기 규칙

| 대상 | 규칙 |
|---|---|
| 아이콘 버튼 | 34/30/22px 정사각 · `display:grid;place-items:center`(SVG 양축 중앙) |
| 알약(pill) | height 34 · padding `0 14px` · 아이콘+텍스트 gap 4~5px |
| 탭 타깃 | 최소 높이 34px · 작은 체크는 라벨 패딩으로 히트영역 확장 |
| 버튼 간격 | `--sp-*`(6/12/18/24) 재사용 · 임의 px 금지 |
| 토글 | 트랙 34×20 · 노브 15px · 이동 14px |

## 7. 케이스 — 체크+액션 통합 알약(`.dlbox` 패턴 · 현재 앱 내 사용처 없음 = 참고용)

단순 액션 + 상태형 체크를 한 글래스 알약(`.dlbox`)에 통합.

- **체크 ON** = 알약 E3(테두리+은은한 채움) + 강한 글로우 링 `0 0 16px rgba(accent,.30),0 0 0 1px .25` + 켤 때 펄스(`dlrip`).
- **액션 탭** = 초록 플래시(`okflash`) + 아이콘 시맨틱 모션.
- 체크 = **원형**(알약 곡률 패밀리) · 네이티브 input 숨김+형제 박스.

## 9. 전체 다운로드 시퀀스 버튼 (`.k-dlseq` · 정본 패턴)

‘전체 다운로드’ = 여러 이미지를 순차 저장하는 버튼(썸네일 후보·카드뉴스·썸네일 생성기 대기열 **공용**). §5 전송 게이지의 사촌(완료 후엔 드레인 대신 즉시 스냅 복귀).

- **베이스** = 프로스티드 글래스 = **형제 아이콘버튼(`.sbtn`·검색 버튼) 톤 그대로**(`rgba(8,15,11,.3)` + 흰 테두리 `.14` + `blur(--blur-s)`) = 앱 통일. (구 어두운 불투명/강조 테두리 폐지.)
- **상태 라벨**: "전체" → **"저장 중… i/N"**(좌→우 네온레몬 `--accent-4` 채움 `::before` scaleX=`--p`(0~1)·**채움 위 글자는 `--on-amber`**라 차오르며 드러남 · 색 = 전체 버튼 네온레몬 통일 운영자 260706) → **"완료" + ✓**(커졌다 작아지는 팝 `k-dlpop` 0.3→1.3→1 + 글로우) → **즉시 복귀**(`.dlseq-snap` = transition none·드레인 게이지 없음 → 글자 가려짐 0) → 원복.
- **진행률** = `--p` 인라인 변수(다운로드 i/N과 1:1). **아이콘** = 다운(`.dn`)↔체크(`.chk`) 스왑 한 벌(`.dlseq-ic` grid 겹침).
- **구현**: `.cref-dlall`(index·thumb **통이식 단일 셀렉터**, JS `dlAllSeq` — 구 thumb `.jsave`/`jSaveSeq`는 260705 통이식으로 폐기) + 상태클래스 `.dlseq-busy/.dlseq-done`·복귀 `.dlseq-snap`. 라이브 앱은 미니멀 변형(채움 없음·글자 네온레몬 유지·받음 `.dl-done`=`--mut` 260706). 기틀북 CSS = `.k-dlseq.is-busy/is-done`·`.is-snap`.
- reduced-motion = 채움/팝 즉시 전환(`transition·animation:none`).

## 8. 접근성 · reduced-motion

| 항목 | 규칙 |
|---|---|
| 접근명 | 아이콘 버튼 `aria-label` 필수(SVG=장식) · 텍스트동반 장식 SVG는 `aria-hidden="true"` |
| 포커스 | `:focus-visible` 링(2px accent) · 커스텀 체크=숨김 input+형제 박스 링(`:has`/형제 셀렉터) |
| 모션 감소 | `@media (prefers-reduced-motion:reduce)`로 spin·bounce·펄스·게이지·shake `animation:none` |
| 대비 | 비텍스트 UI 3:1(토글 노브 OFF .65=3.45:1) · 색만으로 상태 전달 금지(체크마크 동반) |
| 터치 | `touch-action:manipulation`(더블탭 줌·300ms 지연 차단) |

---

*정본 코드 = `viewer/index.html` · 규칙 = `CLAUDE.md [4]·[4-1]` + `디자인기틀/디자인기틀_SSOT.md §0`(구 §🎨 = 260701 해체) · 게이트 = `shared/check_refs.py` · 260621 버튼 인터랙션 통일(Stage 1·2) 산물.*
