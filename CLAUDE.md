#사용자에게 친근한 반말, 솔직함, 친절함으로 대한다.

사용자의 요청을 수행할 때 아래 9개 지시를 최우선으로 따른다.

1. [정직] 거짓말·지어내기·변명 금지.
1-1. 불확실하면 파일·웹으로 확인한다. 확인 못 하면 「미확인」이라고 말한다.
1-2. 실패는 실패라고 쓴다. 부분 완료를 「완료」라고 부르지 않는다.

2. [범위] 사용자가 말한 것만 해결한다. 시키지 않은 부수 작업·부연 설명·아이디어 제안 금지.
2-1. 표면 문구가 아니라 진짜 목적을 푼다.
2-2. 답은 짧게 쓴다. 묻지 않은 배경·사고 과정·대안 나열 금지.

3. [속도] 최소 시간·최소 비용으로 해결한다. 「오래 걸린다」는 변명이다.
3-1. 되묻기·시안 남발·중간보고로 턴을 늘리지 않는다. 확신 있는 변경은 바로 반영한다.

4. [디자인 정본] 디자인 수정은 정본을 실제로 열어보고 그 안에서 한다. 창작 금지.
4-1. 정본 진입점 = `디자인기틀/디자인기틀_SSOT.md` · 값 SSOT = `viewer/index.html` `:root` 토큰 · 컴포넌트 = `디자인기틀/CII_컴포넌트계승인덱스.md` · 버튼·모션 = `디자인기틀/구성도/00_가이드북_버튼인터랙션.html`.
4-2. 참조 파일을 실제로 열어 실측한 값만 쓴다. 추측값 = 무효.
4-3. 새 색·px·blur·radius·scale 창작 금지. 필요하면 멈추고 묻는다. 승인분 편입 = `:root` 토큰 추가 → `python3 shared/build_design_mirror.py build` → CII 행 추가 → `python3 shared/build_design_mirror.py lock`.

5. [폐기] 1~4를 못 지켰으면 그 세션 작업은 실패다. "규칙을 어겨 세션 폐기를 통보합니다."라고 선언하고 중단한다.

6. [보고] 3가지만 낸다.
6-1. **완료 여부** — 막혔으면 ⚠로 무엇이·어느 파일 어느 행에서 막혔고 뭘 승인하면 풀리는지.
6-2. **전과 후 — 링크가 아닌 이미지로.** 링크로 갈음하면 폐기.
6-3. **3줄 결론.**
6-4. 6-1에 **라이브 반영 여부**를 같이 못박는다 + 다음 액션.
6-5. **[채팅은 쉬운 말]** (운영자 260810 "병신같은 외계어 쓰지말게 · 말 ㅈ같이 안하게 근본 원인을 박아 · 저런말이 원체 안나오게 하셈")
6-5-0. 원인은 **이 문서와 코드를 읽은 언어로 그대로 생각하고 그대로 말하는 것**이다. 그래서 다 쓴 뒤에 골라내는 방식으로는 안 막힌다. 말이 나오기 전에 출처를 끊는다. 아래 셋은 그때그때 고르는 게 아니라 항상 적용한다.
6-5-1. 채팅에는 **이 문서와 코드에서 읽은 낱말을 그대로 옮기지 않는다.** 뜻을 자기 말로 풀어서 쓴다.
6-5-2. 채팅에는 **영어 이름을 쓰지 않는다.** 함수든 파일이든 설정이든, 그것이 하는 일만 한국어로 쓴다.
6-5-3. 채팅에는 **기호로 문장을 잇지 않는다.** 가운뎃점 연속, 화살표 사슬, 동그라미 숫자, 괄호 안 곁말 전부. 그냥 문장으로 쓴다.
6-5-4. 적용은 **채팅 답변 전부**다. 파일(이 문서·코드 주석·커밋 메시지·PR)은 다음 세션과 기계가 읽으니 지금 문체 그대로 둔다.
6-5-5. ⚠ 이건 기계가 검사할 수 없다. 채팅은 파일이 아니라서다. 어긴 답변은 보내지 말고 다시 쓴다.

7. [머지] 완료되면 묻지 말고 머지한다. **draft = 폐기.**
7-1. 작업 끝 = 머지 = 라이브 반영. 문서·시안만 머지하고 「완료」라 부르지 않는다.
7-2. 라이브 = 운영자가 실제로 여는 화면과 그걸 만드는 코드. `docs/**`만 바뀐 머지 = 라이브 0 → 「라이브 미반영」이라 못박고 다음 한 걸음을 같은 답에 붙인다.
7-3. 시안은 목적지가 아니라 경유지다. 선택값을 받으면 그 턴에 라이브까지 간다.
7-4. 충돌·게이트 rc≠0·비가역 작업이면 머지하지 말고 멈춰 묻는다.
7-5. 머지 실행 = `bash shared/merge_main.sh prep` → 비-draft 표식 PR 1개 → `bash shared/merge_main.sh land`. 성공 = 6-1에 커밋 해시로 못박는다.
7-6. 표식 PR = 머지 신호등. draft 금지 · 리뷰 대기 금지 · 이미 열린 PR이 있으면 그걸 쓴다 · PR 생성이 실패해도 머지는 진행한다(표시보다 머지 우선) · 머지가 막히면 PR을 열어둔 채 멈춘다.
7-7. 기준은 항상 `origin/main`(로컬 `main` ref 불신).

8. [평의회] 기틀 문서·토큰 구조·동작 로직·전반급 변경만 동일 모델 `--effort max` 8인 병렬 검증. 단순 디자인·국소 손질엔 발동 금지.

9. [납품] 「방법을 알려주는 것」이 아니라 「눌러서 되는 것」을 넘긴다.
9-1. 절차를 떠넘기면 미완이다. 자기완결 1개로 묶어 채팅에 **파일로 첨부**한다.
9-2. 선택지 나열 금지. 가장 쉬운 1안부터 낸다.
9-3. 첫 실행 장애는 코드로 흡수하고, 못 없애는 건 미리 알린다. 끄는 법과 로그 위치를 같이 준다.
9-4. 운영자 PC·폰에서 도는 산출물은 라이브 경계 = **운영자의 실행 1회**. 남은 1회는 6-1에 못박는다.

## 이 레포 전용 (nomute-editor)
- **[🔴 계정 이관 완료(260816) = 정본은 `nomutefb/editor` 다 — 다음 세션은 이 항목을 제일 먼저 읽어라]** 260814 계정 제재(아래 🚨 항목)를 벗어나려고 저장소·보관함·화면 배포를 **전부 새 계정으로 옮겼고 이관은 끝났다**. ⚠ **이 문서 안의 `muteno/nomute-editor`·`apps.nomute.kr`·구 보관함 주소는 전부 역사 기록이다**(그때 있었던 일이라 일부러 안 고쳤다) — 그걸 읽고 옛 쪽으로 가지 마라. **지금 정본 4종** = ⓐ 저장소 `nomutefb/editor` ⓑ 화면 `edit.nomute.kr`(배포 주소 `editor-6dw.pages.dev`) ⓒ 보관함 버킷 `editor` · 공개 주소 `pub-6121e8a6f6194091b5502a72ed28a87b.r2.dev` · 클라우드 계정 `5e651ff5430136f544a35045ab7e6e1a` ⓓ 깃허브 계정 `nomutefb`. **옛 것도 아직 살아 있다**(되돌릴 여지 보존 = 옛 화면 `apps.nomute.kr` 200 · 옛 보관함 그대로) — 정리 시점은 운영자 판단. ⚠⚠ **이 세션 축의 구조적 제약 = 클로드 세션은 `muteno/nomute-editor` 에만 자격증명이 붙어서 새 저장소로 직접 못 민다**(실측 = `access denied by the git proxy: nomutefb/editor is not in this session's authorized repository set` 403 · 세션 소스 추가도 `cross-tier adds are not supported` 로 거부 = 주인이 다른 저장소는 한 세션에 못 붙인다) → **앞으로의 작업은 `nomutefb/editor` 를 소스로 새 세션을 열어라**(그러면 밀기까지 세션이 직접 한다) · 이 레포에 붙은 세션이라면 「만들고 → 운영자가 맥에서 민다」 2단 경로뿐이다(맥 통째 복제본 `~/nomute-mirror` 경유). **배선 갈아끼운 것 11종**(커밋 14개 · 전부 새 저장소 main 착지) = 저장소 이름 63파일 · 저장소 토큰 3종을 `GH_TOKEN` 하나로 흡수(`GH_VARS_TOKEN`·`KIT_SYNC_TOKEN` 폐지 · classic 토큰 `repo`+`workflow` 두 권한 필요 — `workflow` 가 빠지면 화면은 멀쩡한데 제작 발사만 조용히 죽는다) · 공공 API 열쇠 3축 분리(박스오피스 `KOPIS_API_KEY` 영화진흥위 / 재난문자 `DATAGOKR_SAFETY_KEY` 공공데이터포털 / 도로 돌발 `EX_HIGHWAY_API_KEY` 한국도로공사 — 발급처가 각각 달라 한 키로 겸할 수 없다 · **폴백 없이 단독 참조**{값이 바뀐 키라 옛 이름을 폴백으로 남기면 새 키 거절 시 죽은 값으로 조용히 넘어가 「키가 있는데 0건」이 된다}) · 유튜브 쿠키 1번 칸을 `YT_T_COOKIES` 로 뒤집음(워크플로 11줄 + 칸 이름 `_NAME` 9쌍 — 이름 축을 같이 안 뒤집으면 알림이 **반대 칸**을 갈라고 말한다 = 260812 실사고와 같은 축) · 구글 드라이브 연동 **폐지**(`drive-guard.yml`·`drive_shared_guard.py`·`drive_cards.py`·`mint_drive_token.py` 삭제 — 실측상 21파일 중 실제로 도는 건 하나뿐이었고 나머지는 `unset` 안전장치이거나 「드라이브 불요」를 명시한 in-repo 대체 경로였다) · `XAI_SECRET_PAT` → `GH_TOKEN` 흡수(⚠ **그 값은 엑스에이아이 열쇠가 아니라 깃허브 비밀칸 쓰기 토큰이었다** — `shared/grok_api.py` 가 그 값으로 `api.github.com/…/actions/secrets` 를 부른다 = 이름이 거짓말을 하고 있었다 · 그록 자격 본체는 `XAI_REFRESH_TOKEN` 이고 **한 번 쓰면 새것으로 바뀌므로 되쓰기가 필수**) · `OPENAI_API_KEY_NOMUTE` 예비 폐지 · 클로드 계정 체인 4번째 `MUTENONA` → `EMS1130M`(체인 = MUTENO → NOMUTEFB → EMS1130G → EMS1130M **순환** = 막다른 길 없음) · 화면 주소 15파일 · 보관함 주소 693파일. **보관함 복사 실측** = 클라우드플레어 `Data migration`(Super Slurper) 20분 · 찾음 9614 · 옮김 9613 · 오류 0 · 건너뜀 1(0바이트 파일 = 손실 아님) · ⚠ **이 도구는 출발지를 안 지운다**(공식 문서 원문 "Do not delete any objects from source bucket" = 옛 보관함 무손상) · 계정이 달라도 **열쇠로** 읽어온다(로그인 계정 무관 · 「현재 계정 버킷만」 토글을 꺼야 옛 계정 것을 소스로 지정할 수 있다). ⚠⚠ **순서가 계약이었다 = 보관함 복사가 끝나기 전에 주소를 갈면 과거 카드·영상·콘티가 전건 깨진 그림이 된다**(그 주소를 가리키는 지난 제작물 기록이 689파일 1008건) → 코드 4자리만 선행 갱신하고 690파일은 복사 완료 후 별도 커밋으로 갈았다 · 검증도 화면 실물로 했다(7월 산출물 `conv_res/…/out.mp4` 가 새 보관함에서 HTTP 200). **배포 설정 실측값**(클라우드플레어 페이지스) = 빌드 명령 `node build-viewer.mjs` · 출력 폴더 `viewer` · 루트 비움 · 프레임워크 `None` · ⚠ **빌드 명령을 비우면 안 된다**(`viewer/articles.json` 이 `.gitignore` 라 배포 때마다 만들어야 한다 — 비우면 화면은 뜨는데 기사가 0건) · ⚠ **새 계정에서는 화면에서 페이지스 프로젝트를 못 만든다**(2026년 워커스 통합 전환기 · `Create application` 이 워커스로 샌다 = `Deploy command: npx wrangler deploy` 가 보이면 워커스다 · **`Build output directory` 칸이 있으면** 페이지스가 맞다 · 우리 구조는 `functions/` 폴더 방식이라 페이지스라야 한다) · ⚠ **프로젝트 이름으로 주소를 유추하지 마라**(이름이 겹치면 식별자가 덧붙는다 = `editor` → `editor-6dw.pages.dev` · 배포 화면 `Domains` 표기가 유일한 정본). **배포 환경변수 4종**(깃허브 비밀칸과 **별개** = 화면 뒤 함수는 비밀칸을 못 읽는다) = `GH_TOKEN`(Secret · 182곳) · `CANONICAL_HOST`(⚠ **비우면 안 된다** — `functions/_middleware.js` 가 미설정 시 옛 도메인으로 301 튕겨서 새 화면에 아예 못 들어간다) · `R2_PUBLIC_BASE` · 보관함 꽂기 `R2`(Binding · 101곳 · ⚠ 이름이 정확히 `R2` 가 아니면 함수 27개가 「보관함 미설정」으로 빠져 화면은 멀쩡한데 올리기만 죽는다) · `ANTHROPIC_API_KEY` 는 **안 넣어도 된다**(번역·프롬프트 **즉답** 전용 2곳뿐이고 미설정이면 자동 작업 경로로 폴백 · 구독 인증으로 대체 불가 = Messages API 직결이라 OAuth 를 안 읽는다). **도메인 구성** = `edit.nomute.kr`(새) ↔ `apps.nomute.kr`(옛) 병존 · DNS 는 옛 계정에 그대로 두고 **이름표 하나만** 새 배포로 보냈다(CNAME `edit` → `editor-6dw.pages.dev` · ⚠ **프록시 끔** = 도메인과 배포가 다른 계정이라 켜면 클라우드플레어가 대상을 못 찾는다{기존 `apps` 가 프록시 켠 상태로 도는 건 둘이 같은 계정이기 때문 = 조건이 다르다} · ⚠ 도메인 전체 이전(`Begin DNS transfer`)은 **누르지 마라** — 옛 화면의 `apps` 까지 딸려간다 · 쓸 것은 `Begin CNAME setup`) · 요청 출처 허용 목록은 무접촉(`h.endsWith('.nomute.kr')` 로 이미 통과) · ⚠ 클라우드플레어 화면이 「최대 48시간·확인 중」을 표시해도 **실호출이 정본**이다(실측 = 표시가 확인 중인 상태에서 이미 HTTP 200). **이관 도구 3종**(재사용 가능) = `scripts/migrate_account.sh`(값 5종 전수 치환 + 잔여 0 사후 대조 · 보관함 주소는 복사 완료 확정 전 **잠금**) · `scripts/clone_to_new_account.sh`(저장소 통째 복제) · `scripts/r2_migrate.sh`(rclone 계정 대 계정 · ⚠ `aws s3 sync` 는 출발지·도착지 주소를 한 번에 못 받아 계정 간 복사가 안 된다). ⚠ **실측 함정 3종** = ⓐ `git ls-files` 는 한글·특수문자 파일명을 따옴표로 감싸 출력한다 → **`-z` 로 받아야** 실제 경로가 나온다(1차 실행에서 한글 이름 3개가 조용히 빠져 설치 파일의 clone 주소가 옛 계정을 그대로 가리킬 뻔했다) ⓑ pathspec 제외를 `:!_versions/**` 로 쓰면 밑줄을 magic 으로 오해해 목록이 **통째로 0**이 되고 「전 항목 0건 = 깨끗함」처럼 보인다(가장 조용한 실패) → `:(exclude)` 표기 ⓒ `git commit -m` 에 백틱을 넣으면 셸이 명령 치환을 해서 `Argument list too long` 으로 죽는다 → 메시지는 파일(`-F`)로 넘겨라. **미완 3건** = ⓐ 새 저장소에서 자동 작업(Actions) 아직 미가동 ⓑ `kit-fanout.yml` 전파 대상 목록이 `muteno/` 소유로 박혀 있어 `editor` 만 주인이 갈렸다(그 기능은 규칙 문서 배포용이라 노뮤트 화면 영향 0 = 급하지 않음 · 다른 저장소들도 옮길지가 선행 판단) ⓒ 옛 계정·옛 보관함 정리 시점 미정(새 쪽이 며칠 실제로 도는 걸 보고 결정 = 그때까지 되돌릴 여지 보존).
- [7-2 라이브] 이 레포의 라이브 = `viewer/**` · `apps/**` · `functions/**` · `shared/**`(생성 코드) · `scraper/**`. 머지 diff가 `docs/**`에만 닿았으면 라이브 0 = 미완 — 보고 6-1·6-4에 그대로 쓴다.
- **[7-5 실행기 = `shared/merge_main.sh`(운영자 260803 "7-5 ㄱㄱ")]** 머지 ②~⑤는 손루프 금지 → `bash shared/merge_main.sh prep`(fetch·rebase·게이트·④브랜치 푸시) → 표식 PR 비-draft 확인/생성(④-b = MCP 축) → `bash shared/merge_main.sh land`(재시도 루프). 260803 실측 사고 3종의 구조 봉합 = {ⓐ ⑤가 ④를 앞서면 표식 PR이 merged 대신 closed(#3498) ⓐ-2 구 봉합(④+⑤ --atomic 동시 도착)도 리베이스 라운드에선 PR head·base가 **같은 SHA로 동시 기록** → GitHub이 "변경 0"으로 closed(#3538 = 점등 실패 변종 · 2차 개정) → **④ 브랜치 선착 → ⑤ main 후착 순차 2발**(점등은 head 먼저·base 나중 순서에서만 성립 = 260802 #3474~#3495 실측 · ⑤ = --no-verify로 경합 창 압축[같은 SHA가 ③-b+④훅 이중 기통과 직후라 3번째 동일 게이트만 생략] · main plain push 불변) ⓑ push 출력 문자열 오판(성공 "HEAD -> main"을 재패배로 읽음) → 성공 판정 = **fetch 후 origin/main==HEAD 사후 대조**}. Q번호 경합은 land가 --fix-qnum+amend+메시지 동기 자동. **⑥ 점등 사후 대조(운영자 260803 승인)** = land 성공 직후 표식 PR merged를 API 실측(`GITHUB_TOKEN`/`GH_TOKEN` · head=브랜치 최신 PR 1건) — CLOSED 확정만 **rc=7**(머지 자체는 성공 → 보고 6-1에 「main 머지 완료 · 점등 실패」 강제 · 새 변종 기록), 토큰 없음·API 불가·PR 미발견·open 지연 = fail-soft rc=0 + MCP `pull_request_read` 재대조 지시(성공을 실패로 오판 금지 = ⓑ 교훈 · [7-6ⓒ] 표시보다 머지 우선). 멈춤 조건([7-4/7-7] 리베이스 충돌·게이트 rc≠0·라운드 소진)은 rc≠0 + 사유로 정지 — 스크립트는 main에 force 계열을 절대 안 쓴다.
- [병렬 세션 락] 여러 세션이 같은 컴포넌트를 동시에 갈아엎으면 뒤에 온 쪽이 통째로 재작업한다(260802 코너 레일 3회 실측). 핫 컴포넌트 착수 = `python3 shared/lock.py take "<컴포넌트>" <파일…>`(완료 = `release` · TTL 90분 자동 만료) → `check_refs`의 `check_component_lock`이 **남의 살아있는 락과 겹친 변경**을 커밋 직전에 띄운다(WARN·비차단 = 락이 레포를 얼리지 않는다).
- **[스튜디오 레일 = 무조건 상속(운영자 260802 "항상 이미지 스튜디오나 영상 스튜디오는 저 로직을 따르게 만드셈 죽는 한이 있더라도")]** 이미지·영상 스튜디오의 **모든** 세부 메뉴(카드 생성·편집·특수·번역·AI 생성 / 영상 편집·콘티·프롬프팅·음원·큐영상)는 미리보기 코너 옵션 레일의 정본 로직을 **예외 없이** 따른다 — ① 픽토 기준 크기 = **버튼 22×22 / 글리프 12×12**(구현 = `<스코프> .trail svg { width:12px; height:12px; display:block }` 한 줄 · 버튼 클래스별 개별 svg 규칙 금지) ② 빈 캡슐 소거 술어 = `:not(:has(.trail-g > button:not([hidden]))):not(:has(.trail-v:not(.none)))` ③ 캡슐·값 칩 그룹·값 칩 규격 = CII 「미리보기 코너 옵션 레일」 값 사본. **강제 = 정적 `check_refs.check_trail_spec()` 5축 하드게이트 + 표면 자동 발견**(`viewer/*.html` 중 `class="trail`을 가진 파일은 전부 대상 — 손 레지스트리 등재 여부와 무관하므로 새 탭이 조용히 빠질 수 없다) **+ 런타임 `shared/smoke_studioshell.js`**(2셸 10탭 실렌더 = 전체창·도크 유리·2셸 교차 잉크선·픽토 22/12 · `smoke_all` 자동발견 동행) **+ 세로축 = 도크↔첫 블릿 잉크 한 값 = `smoke_studioshell` C9**(운영자 260803 "간격도 제각각, 어느 부분엔 구분선이 있고 — 카드 제작 나오는 부분간 간격으로 통일" · 기준 = 카드 제작 `.csec{margin-top:16px}` → 잉크 18px · 폰 430 전용 sweep{코어 1280은 2단 그리드라 `.out`이 옆 칼럼으로 올라가 세로축 측정이 무효} · 면책 = `GAP_BASE`{콘티 요약 스트립·프롬프팅 명세 스트립·큐영상 소스 헤더(메뉴바는 260803 3차에 도크 위로 이주) = 첫 자리가 소머리 블릿이 아닌 3탭 — 대신 박스 16으로 정합} · ⚠ 신설 사유 = C1~C8이 전부 **가로·부품** 축이라 「도크 아래로 본문이 어디서 시작하나」가 게이트 사각이었고 그 틈에서 8탭이 조용히 갈라졌다{실측 2·2·9·13·18·27·33·44px}) **+ 결과 레일 실존·거처 = `smoke_studioshell` C12**(운영자 260803 6차 "편집처럼 우측에 결과" · 폰 430 sweep 동승 = 이미지 5탭 전부 `.out`/`#geniOut`이 화면에 있고 **제 거처**{자기 프레임 / 살아있는 #geniHost}에 있다 · ⚠ 신설 사유 = 그 작업에서 AI 판 레일이 1단에서 **다운로드 창 안으로 이사**해 통째로 사라진 실사고{rect 0×0 · 콘솔 에러 0 = 무증상}인데 C7 res 축은 1280 2단에서만 돌고 C9·C10은 잉크·미리보기만 재서 「레일이 화면에 있는가」가 사각이었다 · C11은 미리보기 창 클론이 선점) **+ 스크롤 드리프트 = 스크롤 중 미리보기 부동 = `smoke_studioshell` C10**(운영자 260803 "도입 ㄱㄱ" · 발단 = 도크 정지 y2·sticky y0 잔여 2px가 스크롤 순간 -2px 덜컹{iframe 7표면}인데 AI 생성 판만 0이라 스크롤 중 2px 어긋남 — C7은 창 **크기**만·C9는 **정지** 세로축만 재서 「스크롤 **중** 위치」가 게이트 사각이었다 · 판정 = 폰 430 sweep 스크롤 140px 후 미리보기 박스 |Δ|≤0.5{무스크롤 탭 N/A·최소 측정 3 가드} · 정본 = 도크 마진 -18 = wrap 패딩 전액 상쇄 = 정지·고정 동일점{260716 "덜컹 원천 소멸" 원칙·킬테스트 = -16 재현 시 번역 (0,-2) 검출 실증}) **+ 미리보기 창 5탭 클론 = `smoke_studioshell` C11**(운영자 260803 4차 "각 5개 메뉴를 클릭하면 미리보기 시작점이 조금씩 어긋나 · 스크롤 여부 등에 따라 배치도 틀어져 · 아예 동일하게 복제본 마냥" · 판정 = 이미지 5탭 미리보기 창의 **프레임 절대 [x,y,w,h]** 가 카드 생성과 한 값{허용 0.6} · 티어 2종{폰 430×900 + **2단 경계 940×1000**} + **상태 오염 순회**{편집 전용 `.ocfit`을 손으로 얹은 뒤 5탭 순회} · ⚠ 신설 사유 = C7은 창 **크기**만 **1280 한 티어**에서 재서 ⓐ 시작점 x·y ⓑ 2단 경계 티어 ⓒ 탭을 오가며 **남는 상태**가 전부 사각이었다 — 실측 사고 2종 = ⓐ 편집 `.ocfit`{원본+사진} 잔류로 카드 생성·특수 창 207→**2px** 붕괴{공유 노드 `.cpprev-box`} ⓑ 번역 2단 `--pvw` 미러 누락으로 940×1000에서 폭 −34.4·좌변 +17.3px · 킬테스트 = 수정 전 코드로 두 축 전부 FAIL 검출 실증 · 짝 = 확대 상태 **표면 공용 슬롯 1벌**{`window.top.__nmPvZoom` · 탭 전환 시 `pvZoomSyncActive` 재도색 = 260802 3차 "탭 이동에 크기가 튀지 않는다" 정본 복구}) **+ 결과 요약 줄 = 이미지 5탭 한 선 = `smoke_studioshell` C14**(운영자 260806 캡처 «ai 제작만 저쪽이 수평이 안맞는데» · 판정 = **이력 4건을 같이 시드한 뒤** 요약 줄 `.job.done > .jlab` 의 **잉크 윗변 y + 행 상자 상단 y**가 카드생성과 한 값{허용 0.6 = C7·C9·C11·C13 동값} · 1280 2단 · **전용 sweep 페이지**{시드가 코어 판을 안 건드린다 = 기존 축 위양성 0} · 면책표 없이 하드 0 · ⚠ 신설 사유 = **C13이 「빈 레일」만 재서 「머리 아래 첫 줄」이 통째로 사각이었다** — 실사고 260806 `7ed83cd` = AI생성 요약 줄만 **+10.00px** 아래·번역만 행 높이 74.19{형제 83.19}인데 C13은 그 상태에서 5탭 정합 PASS를 냈다{레일 **상자·첫 잉크**는 실제로 같았다 = 갈라진 건 그 아래다} · C7 res는 존재 **여부**{boolean} · C9·C10·C12는 폰 430 1단 티어 · C11은 미리보기 창 전용 · 정적 `check_result_rail_parity`는 **부품 실존**만 → 「같은 부품인데 **값이 갈렸는가**」는 축 자체가 없었고 운영자 눈이 유일한 검출기였다 · ⚠ **자는 줄바꿈 무관 축이어야 한다**{첫 실행 실측 봉합} — 요약 줄은 `flex-wrap:wrap`이고 「완료 · **N장** · 시각」의 N이 탭마다 다르다{thumb 계열 = 마지막 배치만 · tr/geni = 전건} → 문구 폭 차이가 경계에서 줄바꿈을 뒤집어 **잉크 중심·행 높이**가 같이 튄다{실측 176.80/177.44/178.34 = 같은 코드 3값 = 가짜 빨강 공장} → **잉크 윗변·상자 상단** 2축으로 교체{260806 사고는 두 축 모두 +10.00이라 검출력 무손실} · 부품 **값** 동일성은 짝 축 `check_result_rail_parity` 몫 · ⚠ 측정은 **정지 대기**로 얻는다{`stableRow` = 연속 2회 동값까지 폴링·상한 3s — AI 생성 판은 부모 문서가 그리고 아이콘·타일이 뒤늦게 붙어 고정 대기로는 렌더 중간을 찍는다} · 실증 = 3연속 동값{잉크 169.80 · 상자 158.80} + 킬테스트{`7ed83cd` 뷰어를 얹으면 `AI생성=잉크179.8·상자168.8` 정확 검출, 같은 판에서 C13은 PASS}) **+ 입력 1타 렌더 예산 = `smoke_studioshell` C15**(운영자 260807 «편집할때 왜이렇게 폰 발열이 심해져» · 평의회 8인 · 판정 = 카드 제작 폰 430에서 `renderCpPrev()` **직접** 15회×3라운드 중앙값 ÷ **같은 런 CPU 캘리브레이션 루프**{3e6 정수 연산} ≤ `RENDER_BUDGET`{1.0} · 보조축 = 발사 payload 무손상{`CIMG.b64.length` 불변 = 미리보기 최적화가 서버 바이트를 건드리면 즉시 FAIL = [7-2] 라이브 축의 기계화} · **전용 sweep 페이지**{thumb.html 직접 · 사진 시드가 코어 판 무접촉 = C14 원칙 계승} · 면책표 없이 단일 임계 상수{`_BASE`/`_EXEMPT` 아님 = 부채 원장 증가 0} · ⚠ **신설 사유 = 이 레포 게이트 92개가 전부 「끝난 그림」을 잰다** — `check_*` = 정적 문자열 · C1~C14 = 렌더가 **끝난 뒤의** 기하·잉크·색·부품 → 「그 그림을 만드는 데 얼마가 드는가」는 **축 자체가 없었다**. 그 틈에서 미리보기가 ⓐ 매 타 1.6MB `innerHTML` 재조립{260712 태생} ⓑ 글자마다 `document.fonts.check`{자간 sweep 46패스 × 전 글자 = 1타 3,384회 · 260716 회귀}로 **6주간 자라는 동안 한 게이트도 안 울렸고 운영자 손의 발열이 유일한 검출기였다**{insta-thumb-miss·brk_misfire 동축} · ⚠ **절대 ms 금지** = 260807 실측에서 같은 코드의 절대 ms가 병렬 풀 부하에 따라 **7배**{14~98ms} 흔들렸다 → 정규화 비율만이 유효{C14의 「자는 줄바꿈 무관 축이어야 한다」와 같은 축} · ⚠ **폰트 게이트 선행 + SKIP 분기** = `fonts.check` false면 `_trkChW`가 상수 폴백으로 빠져 **다른 코드 경로**를 재게 된다 → PASS와 합치지 않고 SKIP으로 **명시 기록**{관측이 지워지면 다음 세션이 추측으로 메운다 = 스레드 `[1차 실측]`·틱톡 `_e1` 교훈} · ⚠ **직접 호출 축** = input 이벤트 경유가 아니다{경유하면 리스너 개수·rAF 병합 같은 **배선** 변화가 「렌더 1회가 비싼가」와 한 숫자로 뭉개진다} · 실증 = 봉합본 PASS{비율 **0.1276**} + 킬테스트{봉합 전 `61da340` thumb을 얹으면 FAIL **21.08** 정확 검출 = 163배 분리}) **+ 유휴 정숙 = 가려진 동안 셸이 조용한가 = `smoke_studioshell` C16**(운영자 260807 3차 «가만히 있을 때까지는» · C15의 짝 · 판정 = 스튜디오 모달을 연 채 유휴 8s 동안 ⓐ rAF 콜백 **하드 0** ⓑ **모달 밖** DOM 변이 ≤ `IDLE_MUT_MAX`{8} · ⚠ 신설 사유 = **C15는 「1타를 그리는 비용」만 잰다** — 「아무것도 안 할 때의 비용」은 여전히 축이 없었고, 260807 실사고 2건이 정확히 그 자리에 살았다{ⓐ 금융 전광판 5초 인터벌이 **화면에 안 보이는 DOM**{실측 0×0px}을 계속 갈아끼움 ⓑ 물결 rAF 루프가 모달로 100% 가려진 동안에도 주사율로 계속 깨어 있었다 — draw 0인데 콜백 5초 147회 = **모달 없을 때 91회보다 많다**} · 둘 다 화면 증상이 0이라 운영자 손의 발열이 유일한 검출기였다 · ⚠ **가드 있는 형제와 없는 형제가 같은 파일에 공존한다**는 게 이 축의 존재 이유 = 금융 티커는 10줄 아래 형제`_tu`가 가진 가드를 자기만 안 가졌고 260727 시계 봉합에도 안 따라왔다{사람 검토로는 매번 샌다} · ⚠ **rAF 하드 0** = 루프는 「멈췄으면 0 · 안 멈췄으면 수십~수백」이라 중간이 없다{실측 봉합본 0·0·0 vs 봉합 전 229·235·231} · ⚠ **변이 임계 8 = 실측으로 정한 값**{봉합본 3회 = 3·0·0 — 그 3건은 부팅 직후 지연 도착 1회분이고 재현 안 됨 = 주기 타이머 아님 / 봉합 전 3회 = 50·49·49} → 정상 최대 3 ↔ 사고 49 사이 · ⚠ 임계 2로 두면 그 지연 도착이 **가짜 빨강**{첫 실행이 실제로 그렇게 떴다 = 실측 봉합} · ⚠ **모달 안은 비대상**{운영자가 보고 있는 화면 · 제작 진행 표시가 정당하게 움직인다} = 판정 대상은 「가려져서 아무도 못 보는데 계속 일하는 것」뿐 · 면책표 없이 단일 임계 상수 · 실증 = 봉합본 PASS{rAF 0·변이 0} + 킬테스트{봉합 전 `61da340` index를 얹으면 FAIL **rAF 225회·변이 52건**이고 `span.fin-v×12`·`div.fin-row.fin-swap×8`로 **진범을 이름으로 지목**한다}) **+ 셸 상시 CSS 애니 = 가려지면 멈춘다 = `smoke_studioshell` C18**(운영자 260815 «지금 이 에디터가 엄청나게 렉걸리는데 · 원래 렉 안걸렸었는데» · 진범 = `viewer/index.html` `.tkr-track{animation:tkrLoop 28s linear infinite}`{실검 티커 · 260729 도입}가 **스튜디오 모달로 화면을 100% 가린 채** 계속 돌았다 — 트랙 = 실검 10개를 **2벌 복제**한 뷰포트 수배 폭 + `will-change:transform`{전용 합성 레이어 상주} + 부모 `.tkr`의 `-webkit-mask` 그라데 = **매 프레임 마스크 재합성**이 폰 GPU를 계속 때린다 · 가드는 `:hover` 일시정지와 `prefers-reduced-motion` 둘뿐이고 **가려짐은 어느 쪽도 아니었다** · ⚠ 260807 금융 티커와 판박이 = **형제는 가드를 갖는데 자기만 안 가졌다**{같은 파일 타이머 3종은 전부 `isComposingModalOpen` 보유} · ⚠ **신설 사유 = 기존 3축이 전부 원리적으로 못 보는 자리** = ⓐ C17 무한 애니 축은 대상이 `thumb.html` **단독 페이지** = iframe 안뿐이라 셸 문서를 아예 안 연다 ⓑ C16은 셸을 보지만 판정이 **rAF 콜백 + DOM 변이**라 선언형 CSS 애니가 **둘 다 0**을 낸다 ⓒ `check_idle_timer_guard`는 **setInterval 전용** → 260729 도입 후 지금까지 한 게이트도 안 울렸고 **운영자 손의 렉이 유일한 검출기였다** · 봉합 = `body:has(dialog[open]) .tkr-track{animation-play-state:paused;will-change:auto}` **1줄**{술어 = `dialog[open]` **자동 발견** = 손 목록 0이라 새 모달이 조용히 못 빠진다{`isComposingModalOpen` 16종 미러는 낡는 축} · `will-change` 해제 동반 = 정지 시 합성 레이어까지 반납{paused만으론 레이어가 남는다} · 마크업·JS 변경 0 · 새 값 0} · 판정 = C16과 **같은 유휴 창 동승**{런 신설 0} · iframe은 별도 문서라 셸 `getAnimations()`에 안 잡힌다 = 스코프가 정확히 「모달에 가려진 셸」 · 관측 유효성 도장 `tkr`{티커 미렌더 = 판정 대상 0 = SKIP 명시 · C17 `pan` 선례} · 면책표 없이 하드 0 · 실증 = 킬테스트{가드 제거 → **`tkrLoop@tkr-track`을 이름으로 정확 지목** FAIL이고 **같은 판에서 C16은 PASS** = 「C16이 원리적으로 못 본다」의 실증} + 복원 후 28종 전건 PASS · ⚠ **입력 경로는 무죄로 실측 확정** = 셸 경유 타이핑 1타 **4.5ms{정규화 0.319}** · `fonts.check` 1타 **0회**{260807 캐시 봉합 생존} · iframe 유휴 CPU **0.15%** = C15 봉합은 그대로 살아 있고 이번 렉은 **셸 쪽 단독 사고**였다) **+ 정적 짝 = 유휴 타이머 가드 = `check_refs.check_idle_timer_guard`**(운영자 260807 4차 "정적 짝 게이트도 ㄱㄱ" · 계약 = 「셸 `viewer/index.html`의 `setInterval` 콜백이 DOM을 쓰는데 **영원히 돈다면** 가시성·모달 가드{`document.hidden`·`visibilityState`·`isComposingModalOpen()`·`.open`}를 동반한다」 · ⚠ 신설 사유 = **C16은 런타임이라 「그날 실제로 돈 것」만 잡는다** — 조건이 안 맞아 측정 창에서 잠자던 타이머는 그냥 통과한다. 260807 실사고의 구조적 원인은 **형제는 가드를 갖는데 자기만 안 가진 것**이었고{금융 티커 ↔ 10줄 아래 `_tu`}, 그건 **정적으로 보이는 모양**이다 → 런타임+정적 두 겹이라야 새 타이머가 조용히 빠질 구멍이 사라진다 · ⚠ 스코프 = `index.html` **단독**{도구 뷰어는 iframe = 모달 **안** = 운영자가 보는 화면 = 비대상 · C16 경계와 동일} · ⚠ **유한 수명 면제가 실효 조건** = 술어를 「DOM 쓰기 ∧ 무가드」로만 두면 현행 4건이 걸리는데 **전건 판독 결과 위양성**이었다{`iv` `++n>40`=최대 10s · `fr._er` `++n>180`=최대 18s · `_tvPoll` `Date.now()-t0>420e3`=예산 상한} — 전부 **사용자가 시작한 작업의 유한 수명 타이머**이고, 금융 티커와의 결정적 차이가 이 축이다{그쪽 `clearInterval`은 「DOM에서 사라졌을 때」뿐이라 화면이 살아있는 한 **영원**} → 횟수 상한·경과 상한 표식 보유 시 면제 · ⚠ **면책표 없이 하드 0** = 술어를 좁힌 뒤 남은 유일 잔여 `_tvTick`{영상 받기 진행 표시}은 baseline에 박지 않고 **가드를 실제로 넣어 해소**했다{경과가 t0 기준 산식이라 스킵분은 복귀 즉시 정확히 따라잡는다 = 표시 손실 0} · 부채 원장 증가 0 · 주석 줄 제외) **+ 결과 레일 2단 분기점 표면 간 한 값 = `check_refs.check_twocol_breakpoint`**(시그니처 `span 99` 자동발견 · 260802 실사고 = thumb만 900 하향·영상 4탭 1100 잔류가 스모크 1280·폰 430 실측기 사각을 그냥 통과) **+ 값축 거처 = 다값 카드 헤더 축은 헤더 우측 칩 단일 문법 = `check_refs.check_axis_chip_home`**(운영자 260804 "idea go" · 계약 = 「카드 헤더가 담당하는 **다값**(CYC 3값 이상) 축은 `hdChips`로만 그린다」 — 본문 행 `segs` 혼용 = 커밋 차단 · ⚠ 신설 사유 = 컷 편집 「강도」 4택1이 비율·해상도와 같은 성격인데 **다른 문법**으로 그려져 260728 Q10 통일에서 혼자 빠진 채 6일 잠복했고, 기존 게이트가 전부 「값이 정본과 같은가」만 봐서 「**같은 축을 어디에 그리는가**」가 축 자체로 없었다 = 운영자가 눈으로 찾아야만 발견되던 사각 · 위양성 3겹 = ⓐ 스코프 = **살아있는 카드 렌더 경로만** 자동 발견(cardHtml 본문 + `body=XXX()` 피호출 함수 → 도먼트 시트 `prm()`의 구 문법 사본 7건은 스코프 밖 = 도먼트 보존 관례와 충돌 0) ⓑ 대상 = 다값 축만(이진 OFF/ON = 워드 점등 축 = `check_onoff_literal` 전담 — 섞으면 「hdChips로 고쳐라」가 오답) ⓒ 대상 축 = CARDAX 값 ∪ 스코프 내 hdChips 축(= 카드 **헤더**가 그리는 축)에 한정해 `fit`(방식)처럼 「헤더는 비율 칩이 차지 → 아래 구획」이 정본인 축은 자연 제외(260712 3차 동급 위계 계약 보존) · 표면 자동 발견(`cardHtml` ∧ `hdChips` 보유 뷰어) · 면책표 없음 = 현재 위반 0[ar·cutlv·res 3축] · 실증 = 킬테스트 1종{cutBody에 구 segs 부활 → rc=1·축명 정확 검출} + 위양성 대조 4종{fit 아래 구획·이진 bgm·도먼트 prm 신규 축·주석 속 예시 = 전건 무경보}) **+ 이진 토글 = 워드 점등(ON/OFF 리터럴 금지) = `check_refs.check_onoff_literal`**(운영자 260803 "기능 워딩이 점등하냐 안하냐로 onoff" · 정본 = thumb #cnTog 라벨 자체 점등 · 면책 = _ONOFF_BASE{페이로드·다값 순환} 스냅샷·축소 지향). ⚠ 정적만으론 안 걸리는 사고가 실재한다(260802 3차 실측 = CSS는 각자 멀쩡한데 **셸 사이**가 갈라진 것: 영상 팝업 잔류·도크 순흑 잔류·번역 레일 버튼만 34px·thumb `--line2`가 `--line` 동값) → **셸 골격은 런타임 교차 비교로만 잡힌다**. 새 스튜디오 탭을 만들면 레일도 같이 계승한다(선택 아님).
- **[결과 레일 = 5탭 한 세트 = `check_result_rail_parity`(운영자 260806 "왜 저게 계속 따로노는지 모르겟으유" → "정본에 정립해봣자 나중에 또 저렇게 따로노는거 아님?")]** 이미지 스튜디오 「결과」는 **[요약 줄(완료 · N장 · 시각 + 수정 · 전체 다운로드) + 개별 썸네일 타일]** 한 세트다(정본 부품 = 요약 줄 `nm-job.css .job.done` · 타일 `nm-hist.css .hist-it`). ⚠ 신설 사유 = **260806 하루에 이 레일이 세 번 갈라졌고 전부 운영자 눈이 유일한 검출기였다** — ⓐ 「결과」가 결론형 줄(thumb 3탭) vs 썸네일형 타일(tr·index 2탭)로 갈림 ⓑ AI 생성 판만 타일 배경 투명(`.hist-thumb img{background:var(--pan)}`인데 **index :root에 `--pan`이 없어** 선언 통무효 = 조용히 색이 빠짐 · 도구 뷰어 9개는 전부 보유) ⓒ 번역·AI 생성엔 요약 줄 자체가 없음(nm-job.css 링크 부재) + `.job .jsave-row .imgedit{position:static}`이 thumb 인라인에만 있어 그 두 판은 연필이 absolute로 떠 상태줄에 겹침(실측 jobH 번역 72.2 vs 형제 83.2 → 봉합 후 3표면 83.2 · svTop−stTop 31.2 완전 동일). 기존 게이트가 전부 다른 축이다 — `check_trail_spec` = **코너 레일** · `check_design` = 토큰 raw **개수** · `smoke_studioshell` = 도크·잉크·픽토 **부품** → 「결과 레일이 같은 부품 세트를 갖는가」는 축 자체가 없었다. 판정 = 정적(렌더·LLM·네트워크 0) · **표면 자동 발견**(결과 헤더 시그니처 `class="hist-h car-h"` ∨ `id="geniResH"` ∧ `hist-grid` 보유 = 새 탭이 조용히 못 빠진다 · 시그니처 소실 = **fail-closed**) · **면책표 없이 하드 0**(현행 3표면 전건 보유) · 4부품 = ① nm-hist.css 링크 ② nm-job.css 링크 ③ `class="jobs"` 요약 줄 컨테이너 ④ `class="hist-grid"` 타일 그리드. ⚠ 남는 한계 = **부품 실존**까지만 본다(값·기하 동일성은 `smoke_studioshell` 축) — 「있는데 값이 갈린다」는 ⓑ류는 짝 축인 「공유 CSS가 요구하는 토큰을 그 문서가 정의하는가」 게이트가 별도로 필요하다(미신설 · 다음 후보). ⚠ 같은 클래스명 충돌 주의 = `tr.html`의 `.job`은 **참고 자료 목록 행**이라는 다른 부품인데 이름이 같고 인라인이 뒤라 SSOT를 이긴다(실측 `flex-wrap:nowrap` 승리로 요약 줄이 짓눌렸다) → 결과 요약 줄은 `#resJobs .job` 스코프로 정본 3값을 되돌려 쓴다.
- **[결과 레일 부품 SSOT = `viewer/nm-rail.js`(운영자 260806 "항상 저렇게 유지되어야하고, 사진끼리는 저렇게 공유하고, 영상끼리도 저렇게 공유할수있게해줘 (근데 사진 과 영상은 작업 결과를 공유하지 않음)-형태 목업만 일치")]** 영상 스튜디오 5탭(편집·콘티·프롬프팅·음원·큐영상)엔 결과·이전 제작 레일이 **아예 없었다**(실측 260806 = 편집만 「작업 내역」 다른 문법 · 음원은 `.out`조차 없음). 이미지 정본 마크업을 5벌 복사하면 그날 네 번 갈라진 축(결론형/썸네일형 · 타일 배경 투명 · 요약 줄 부재 · 마진 상쇄 10px)이 그대로 재현되므로 **사본 대신 상속**으로 간다 — 마크업·렌더·이력을 이 파일이 한 번만 만들고, 각 탭은 `<link nm-hist.css>` `<link nm-job.css>` `<script src="nm-rail.js" data-scope="cap" data-dlname="…">` 3줄만 든다(마운트도 자동 = `autoMount`가 앵커를 찾는다 · 구판 「문서마다 마운트 1줄」은 콘티·큐영상 2탭이 조용히 미부착 = 사본은 어디에 두든 갈라진다는 같은 교훈). **데이터 격리 = 스코프별 저장소** — img `nomute_thumb_hist`(이미지 5탭 종전 배선 · 이 모듈 무접촉) / cap `nomute_cap_hist`(영상 5탭 전용 신설) → **영상끼리는 공유, 사진과는 완전 분리**(실측 = 영상 3건 시드 시 이미지 카드제작 타일 1건 = 무혼입). 완료 적재 = `nmRail.add({url,cap,dlname})` 1줄 · 형제 탭 수렴 = `storage` 이벤트(폴링 0). 강제 = `check_result_rail_parity` 확장(이미지 3표면 = 정적 세트 4부품 / 영상 5표면 = 상속 3줄 + `data-scope="cap"` · 면책표 없이 하드 0). ⚠ 남는 한계 = ⓐ **각 탭의 「완료 시 add」 배선은 미완**(레일 셸·공유·격리는 서지만 실제 산출이 자동으로 쌓이진 않는다 — 탭마다 산출 경로가 달라 별건) ⓑ **큐영상 1탭 좌표 미정합**(그 문서만 `.wrap` 골격이 없고 `#jobs`가 팝업 안이라 앵커가 형제와 다른 자리 · 실측 요약줄 55.8 vs 형제 69.8 = 레일은 서지만 시작점이 갈림 · `data-anchor` 지정으로 해소 예정). **[260806 평의회 8인 후속 봉합]** 8인 병렬 검증이 실사고 12건을 잡았다 — ⓐ **`nm-hist.css` 미닫힘 주석**이 다음 규칙(AI 판 펼침 형광 글로우)을 통째로 삼킴(라이브 결함 · CSS 주석 균형 축은 게이트에 없다) ⓑ **`.sbtn`/`.cref-dlall` 기저가 thumb·index 인라인에만** 있어 상속 5탭에서 「전체」가 **UA 기본 버튼**으로 그려짐(실측 52.6×23·radius 0·outset vs 정본 62.6×34) → nm-cards.css 승격 ⓒ **`nm-svg.js` top-level `const`는 `window`에 안 붙는다** = 분리 파일(nm-rail.js)의 `window.EDIT_SVG` 등 5개가 상시 undefined = 아이콘 SSOT 죽음 + DOWNLOAD 글리프 드리프트(폴백 밑변 16 vs 정본 14) → `window` 노출 추가 ⓓ **sb·k·vd 인라인 `.job` 사본**(vd는 2벌)이 SSOT 12px를 13px로 이겨 요약 줄 높이가 갈림(73.8 vs 72.2 → 승격 후 5탭 83.2) ⓔ **song엔 `.out` 규칙 자체가 없어** 레일 머리 여백 0(형제 18) → `.nm-rail` 기하를 SSOT로 ⓕ **큐영상 좌표 진범 = 마진 상쇄**(형제는 `html.inmodal .wrap{display:grid}`가 `.car-h{margin-top:14}`를 레일 안에 가두는데 vd `.src`는 평범한 블록이라 뚫고 나갔다) → `.nm-rail{display:flow-root}` 1줄로 5탭 69.8 정합 ⓖ **404 부활 루프**(결과 타일이 `_deadUrls`·`s.dead` 미필터) ⓗ **접어둔 결과를 도로 펼침**(carHSync가 aria를 무조건 'true') ⓘ **복원 타일 시각이 부팅 시각**(요약 줄의 원 제작시각과 모순) ⓙ nm-rail = id 고정 `'nmr'`(이중 마운트 시 첫 레일 강탈)·`add()`가 마운트마다 저장(격리 위험)·`data-scope` 미인식이 `cap`으로 fail-open·프로토타입 키 통과·sig 게이트 부재(사진 1건마다 영상 5탭 img 1,200개 재생성·CPU 104ms)·복귀 1회에 2회 렌더·포스터 404가 멀쩡한 항목 영구 삭제·`esc()`가 이스케이프가 아닌 이름 함정·`aria-controls` 전무·접이 CSS `[hidden]` 가드 부재로 **결과 접기가 아예 무동작** ⓚ **게이트 우회 8종**(시그니처 1글자 드리프트·주석 감싸기·bare substring·미끼 `.jobs`·손 목록·link 실존 미검사·template 격리·정적↔상속 이관이 위양성) → 3차 재작성 = 주석·template 제거 + 클래스 **토큰 집합** + script 태그 **안** 판정 + 문자 거리 폐기하고 **형제 인접** + 표면 **하한 고정**(이미지 3·영상 5 = fail-closed) + link 파일 실존 + **(정적 세트) ∨ (부품 상속) OR 구조**(SSOT 이관을 게이트가 막던 자기모순 해소) · 킬테스트 7종 전건 검출 + 위양성 0 실증. ⚠ **완료 적재 = 파일 산출 3탭만**(편집·콘티·큐영상 = `nmRail.add` 1줄) — 프롬프팅·음원은 산출이 **텍스트**라 썸네일 타일에 그릴 그림이 없다(무리 적재 = 깨진 타일 · 별건 설계 대기).
- **[비율 자동 커버리지 = 「씨앗 다듬기」 파이프 = `.github/scripts/resize_image.py`(운영자 260806 "비율이 다른 것이 있어도, 이를 자동 커버리지 해서(제미나이 종량제 api key)를 통해서, 비율 다른거를 내가 수동으로 채우지 않게 한다")]** ⚠ **과업 정의부터 다르다** — 운영자 260806 "이거는 뭘 만드는게 아니라 최대한 기존에 있던 선을 이어서 자연스러운 이미지 처럼만드는거거든" = **창작이 아니라 선 잇기**(uncrop·클론힐). 이 한 줄을 놓치면 프롬프트·라우팅·씨앗이 전부 「생성」 쪽으로 기울고 그 순간 없던 게 그려진다(실사고 = 별·성운·팔·스튜디오 기둥). **확정 파이프 6단**(순서가 곧 계약) = ① `gap_sides(img, ar, box)` = 여백이 **실제로 생기는 변**만 산출 → ② `edge_stats(img, gs)` = **그 변만** 8px 밴드 std(⚠ 구판 4변 평균 = 실사고 · 확장과 무관한 변에 피사체가 닿기만 해도 std가 올라 유료 창조 경로로 오판했다{실측 4변 30.68 → gemini / 좌우만 0.00 → solid_pad · 범인 = 하단 41.02 파란 어깨}) → ③ `std < EDGE_SOLID_STD`(6.0) = `solid_pad` **과금 0**(⚠ `fill='ai'` 강제여도 이 강등이 **우선** — 단색 확장은 PIL이 모델을 못 질 수가 없고 모델을 부르면 오히려 없던 걸 그린다) → ④ 아니면 `seed_pad` = **clamp(가장자리 연장) 단독** 씨앗을 먼저 깐다(과금 0 · 모델에게 **빈칸을 주지 않는다** = Firefly 무프롬프트 채우기가 자연스러운 이유의 구조 이식) → ⑤ `P_SEEDFILL`(**수리 전용** 문구 = 「줄무늬를 제 텍스처로 바꿔라」 · 「채워라」 아님) + `tg.gemini_image(ref_png=씨앗)` → `gemini_judge` 자가 QA 최대 2회(FAIL 사유 피드백 재발사) → ⑥ 최종 실패 = **씨앗 그대로** 폴백(`route=seed_pad` · ⚠ 블러 폴백 아님 = 있는 픽셀만 재배치라 선이 살아 있다) → `pixel_lock`으로 원본 영역 재부착(얼굴·문구 100% 보존). 킬스위치 = `RESIZE_SEED=0`(repo variable — **배선 완료** `img-resize.yml` env · 260808 · 구 「도달 불가」 서술은 낡음). **폐기 변종 3종 = 실호출로 죽은 것들이라 되살리지 마라** — ⓐ **회색 빈칸 + `P_PADFILL`** = 모델에겐 그게 빈 캔버스라 매번 「여기 뭘 넣지」를 새로 정한다 → 경계가 빈 배경이면 **발명**(별·성운·팔·스튜디오 기둥·조명), 무늬면 **복제·이음선**(4:5 QA 2회 FAIL "seam + severe ghosting") ⓑ **거울반사 씨앗** = 인물이 경계에 걸치면 얼굴이 **거꾸로 복제**된다(= 이 파이프가 이미 실패한 사유를 씨앗이 더 키우는 짓) ⓒ **씨앗에 블러** = 씨앗이 정보를 지운다 → 야경 텍스처가 뭉개져 모델이 「원래 흐린 배경」으로 읽고 **그대로 둔다**(밋밋한 파란 면·정체불명 얼룩). 씨앗의 목적은 「고칠 자국을 **명확히 보여주는 것**」이지 미리 예쁘게 하는 게 아니다. **모델 = Gemini 3.1 Flash Image**(`NM_MODELS` 표시명 축 · 종량제 · 1K 이미지 1콜 기준 $0.067) · **Firefly 비채택**(운영자 "월 ㅋㅋㅋ 어도비 미친놈들") = 무프롬프트 채우기 품질은 실제로 우수하나 API 축이 **엔터프라이즈 계약**이라 종량제 진입 자체가 없다. ⚠ **검증은 실호출로만 한다**(운영자 260806 "이거 지금 실제 호출 돌리는거 검증하려고 있는 세션이야. 다른 말 하면 다 거짓임") — 러너는 `workflow_dispatch`가 `src_sha`로 **detached-HEAD 체크아웃**을 하므로 **커밋·푸시가 선행돼야** 러너가 그 코드를 본다(로컬 수정만으론 아무것도 안 바뀐다) · 산출을 눈으로 보기 전에 「됐다」 금지. 실호출 이력(앵커 365×241) = 1세대 회색빈칸 ❌ / 2세대 거울씨앗 ❌ / 3세대 clamp+블러 ❌ / 4세대 clamp단독+수리문구 = 16:9 ✅ · 4:5 ✅ · **9:16 ❌ 유령 두 번째 몸통** / **5세대(260807 평의회 8인 봉합) = 3비율 전건 ✅**(실호출 run 31151767647·31151777689·31151779107). **5세대 봉합 5축**(8개 렌즈가 독립적으로 같은 진범을 지목 = 유령은 모델이 상상한 게 아니라 **씨앗이 그려서 먹였고 프롬프트가 그걸 주문했다**) — ① **`pixel_lock` 확정 결함**(유령과 무관하게 상시 발동하던 별건 라이브 사고) = 여백 0인 **밀착 변에도** 페더를 걸고 박스 **안쪽**으로 램프해 원본 화소의 **38.3%를 모델과 섞었다**(실측 최대오차 254 · 실물 = 4세대 16:9 「성공」본의 **원본 하단 12행이 크림색 가로 막대** = 모델이 그린 데스크가 페더를 타고 원본 안으로 들어왔다) → **여백이 실재하는 변의 바깥으로만** smoothstep + 박스는 마지막에 원본으로 덮는다(실측 **38.3%→0.0% · 최대오차 254→14**{JPEG 잡음 수준}) ② **심판이 원본을 본 적이 없었다** = 「duplicated」는 원본을 모르면 **원리적으로 판정 불가능한 술어**다 → `gemini_judge(ref_bytes)` 2이미지 비교(`exp_resize_v0`에 이미 있던 분기 **이식** · +$0.0006 · 위양성 3겹 = 프레이밍 면책 선언·「이어진 한 몸」 면책·개수는 **원본 대비 상대**{절대수는 2명·0명 사진에서 거짓 지시}) — 실호출에서 **실제로 t1을 FAIL시키고 t2가 통과**했다(9:16 "duplicated band of background" · 4:5 "duplicate set of building windows") ③ 판정 대상을 **출고물**(pixel_lock 이후)로 이동 — 구판은 락 이전 원시 렌더를 심사해 페더 단차가 영영 미심사였다 ④ **`P_SEEDFILL` 진범 3문장 제거** = ⓐ `if it is fabric, more of that same fabric.`(하단 밴드의 이웃 텍스처가 정장이라 「거기에 정장 천을 더 만들어라」가 되고, 204px에 이행하는 물리적 방법은 **또 한 사람**뿐 = `check_thumb_prompt_sanity`가 이미 이름 붙인 프롬프트 자기모순) ⓑ 꼬리절 `that is not already present`(유령 몸통은 「이미 있는 것」이라 금지 목록을 문자 그대로 **빠져나갔다** · 실패 모드 이름이 ghosting인데 금지어에 second·copy가 0개) ⓒ 선행사 없는 `extended {where} … it` 순환문 → 슬롯 3개(`{keep}`·`{where}`·`{edgerule}`)를 **`seed_dirs()`가 실측 %로** 채운다(구 `box_dirs`는 P_PADFILL 전용 문법이라 배치 발사에서 고정 문구 "central"이 **거짓말**을 했다) ⑤ **관측 봉합** = 구판은 `if v is None or v[0]`가 **PASS와 판정 불가를 같은 가지**로 합치고 **둘 다 무출력**이라 「유령이 통과였는지 조용한 스킵이었는지」가 로그·원장 어디에도 안 남았다(스레드 `[1차 실측]`·틱톡 `_e1`과 **같은 병** — 관측이 지워지면 다음 세션이 추측으로 메운다) → PASS/SKIP/FAIL/DET-FAIL **4분기**를 로그와 원장 `qa` 필드에 남긴다 + 결정론 `streak_frac`(모델 무동작 검출 · 과금 0 · clamp는 이웃 행/열 차가 **수학적으로 0**이라 혼동 구조적 0 · 실측 JPEG 왕복 후 무동작 0.983 vs 정상 재작성 0.000). ⚠ **성패 축은 비율 이름이 아니라 「여백 ÷ 원본 그 축 길이」**(16:9 9.3% / 4:5 44.8% / 9:16 **84.6%**) = 「원본을 그 축으로 2배 이상 늘리는 모든 요청」이 같은 위험대다 — 9:16만 특별한 게 아니었다. ⚠ **표본은 여전히 1장 × 3비율**(평의회 8 = 「셀당 표본 1 · 시드·온도 통제 0 · 세대 사이 채점 기준까지 바뀌었다 = 노이즈에 커브 피팅」) — 5세대 성공은 **확률 개선의 증거이지 재현 보증이 아니다**. 실사용 코퍼스 실측 = `uploads/**` **76장**(260809 2차 재실측 · 그중 45장 = 1080×1350 자가 생성 카드 = 리사이즈 실험 코퍼스로 부적격 — A/B는 실사진 위주로 별도 구성) · 세로 우세 · 16:9 확장배수 중앙 2.22× = 앵커(1.18×)보다 훨씬 어렵다. **보류(근거는 있으나 한 번에 하나씩 실호출로 재야 무엇이 무엇을 했는지 안다)** = 다단계 래더 · 씨앗 주파수분리(LF 확산 + HF 배경 재주입) · 여백비 임계 자동전환. ⚠ 마지막 것은 **`route` 화면 노출이 선행 조건** — route 화면 표기는 **라이브**(`viewer/thumb.html` 결과 라벨 = AI/단색/블러/AI 실패·원본 확장 · 260808 배선 — 구 「0회」 서술은 낡음), 임계 1.5~2.0을 걸면 **실사용 16:9의 대부분이 조용히 강등**된다. ⚠ 마스크 인페인팅은 **Gemini 이미지 API에 없다**(공식 문서 · Imagen은 2026-06 종료) · `candidateCount>1` = Gemini 3.x에서 **400**.
- **[영상 결과 레일 = 끝난 것은 남고 도는 것은 큐에 선다 = 게이트 `check_cap_rail_land` · 스모크 `shared/smoke_caprail.js`(운영자 260810 "비디오 스튜디오 이전 제작이 안떠 · 방금 1개를 제작하고, 추가로 뭐 하나 더 제작하면 방금거가 유실된다 · 계속 이슈가 되는거니까 너가 코드로 검증하셈")]** 영상 스튜디오 제작물이 **세 층에서 동시에** 사라지고 있었고 셋 다 화면 증상이 「아무것도 없음」 하나뿐이라 운영자 눈이 유일한 검출기였다 — ⓐ **영상 타일 자멸** = 레일이 이미지 스튜디오에서 온 부품이라 타일을 `<img>`로만 그렸는데 영상 산출(mp4·mp3)은 img 디코드가 **반드시** 실패하고, 구 `onerror`가 그걸 「죽은 슬롯」으로 읽어 `it.remove()` + `dead` 등재까지 했다(실측 = 저장소 2건인데 결과 0·이전 제작 0·콘솔 에러 0 · 같은 url 재적재도 중복차단에 막혀 리로드 전엔 복구 불가) → **확장자 축으로 갈라 `<video>`로 그린다**(부품 = `nm-hist.css .hist-thumb video` = `.hist-thumb img` 정본 규격에서 **비율만** 16/9[edit `.ljth .ljv` 정본 값 사본] · 새 값 창작 0) · ⚠ 영상·소리는 **로드 실패해도 안 지운다**(파일은 R2에 살아 있고 [↓]는 동작 — 제작물이 목록에서 사라지는 쪽이 훨씬 비싼 사고). ⓑ **반쪽 적재** = 편집·음원은 **화면 주인(포커스) 완료**가, 콘티는 **강등분**이 레일에 안 얹혔다. 편집의 그 자리를 메우려고 260806에 넣은 1줄은 `renderResult` 안에 있었는데 그 함수 스코프에 없는 `fresh`·`outPath`(둘 다 `lyResult`의 파라미터)를 참조해 **매 호출 ReferenceError**였고 감싼 try/catch가 통째로 삼켰다(설령 실행됐어도 url 자리 값이 `ly_out/<id>/video.json` = 상대경로라 `SAFE_URL` 스킴 검사에서 거절 = **이중으로 죽은 코드**) → 착지의 정본 자리 = **완성 url을 실제로 쥔 곳**(편집 `showResult` · 음원 폴 완료 분기 · 콘티는 포커스·강등 공용 `sbLand` 1벌). ⓒ **이전 제작 강탈** = 「세션분 0이면 최신 1건 추종」이 지난 제작 1건을 결과 칸으로 끌어올리고 이중노출 차단 필터가 그만큼 이전 제작에서 뺐다 → 제거(운영자 260810 "전에 제작한건 이전 제작에 남아있어야하고" · 결과 = **이번 세션 것만**). **[진행 중 큐(운영자 260810 "제작 중인거는 그위 결과에 큐잉되어 들어가야해 순차적으로")]** = 결과 칸 맨 위에 진행 중 작업이 **발사 순서대로** 잡 행으로 선다(부품 = `nm-job.css .job` = 이미지 스튜디오 잡 행과 같은 부품 · 문구 = thumb 진행 행 정본 「제작중 · 21s」 · 새 클래스·값 0) · 읽는 곳 = 각 탭이 `<script src="nm-rail.js" data-jobkey="…">`로 선언한 슬롯 저장소(**두 문법 다 인식** = nm-jobs 슬롯 `nm_*_pend` ∨ 탭 자체 배열 `sb_jobs`·`vd_jobs`) · 통지 = **폴링 아님**(`nm-jobs.write`·`jsave` 저장 지점 1곳에서 `nmRail.refresh()` — 호출부 사본을 각 탭에 두면 새 탭이 조용히 빠진다) · 경과 1초 틱은 **진행 중이 있을 때만** 켜지고 없으면 스스로 끈다 + `document.hidden` 가드(유휴 정숙 계약 C16 동축). ⚠ 강제 = 정적 `check_cap_rail_land`(표면 자동 발견 = `data-scope="cap"` 상속 뷰어 · 하한 5 fail-closed · 면제 `_CAP_LAND_EXEMPT` = 프롬프팅 1종[산출이 텍스트라 그릴 그림 0] · 2축 = ① 착지 실존 ② **함수 범위 안 대칭** = 「착지를 가진 강등 줄」이 있으면 그 줄 **이후**에도 착지가 있어야 하고[직접 ∨ **완료 페이로드 `d`를 넘겨받는 호출**의 1단계 추적], 완료를 다루는 함수가 강등 분기를 가졌으면 **착지를 가진 강등 줄**을 최소 1개 보유) + 런타임 `shared/smoke_caprail.js`(폰 430 · **발사 0** = 가짜 데이터만 심는다 = 러너·과금 무접촉 · 5축 = 영상 타일 생존·이전 제작 보존·완료 착지·진행 중 큐 순서·연속 2건 무유실). ⚠ **게이트 술어 함정 2종(실측 봉합)** = ⓐ 「파일 어딘가에 분기 밖 착지가 있으면 통과」로 두면 **진범을 못 잡는다**(편집은 강등 전용 폴 `pollEditBg`에도 착지가 있어서 `showResult` 착지를 지워도 파일 단위로는 멀쩡해 보였다 = 첫 판 킬테스트 미검출 = 죽은 게이트) ⓑ 1단계 추적에서 **호출 이름을 전부 따라가면** 같은 함수의 형제 헬퍼(`bgLand`·`stop`)가 착지를 갖고 있어 역시 통과한다 → **`d`를 넘겨받는 호출만**이 착지의 정당한 대리인 ⓒ 거울 축(강등 누락)을 **줄 단위**로 세면 실패 경로(`error.log` 분기의 `if(!_m){…return;}`)가 통째로 위양성(실측 4건 = 실패엔 얹을 산출이 애초에 0) → **함수 단위**로 좁힌다. 실증 = 게이트 킬테스트 3종 전건 정확 검출(파일·줄 지목) + 위양성 0 · 스모크 킬테스트 4종 전건 검출{영상 분기 제거·편집 착지 제거·최신 1건 추종 부활·진행 중 큐 제거} + 복원 위양성 0.
- **[여러 작업 동시 추적 SSOT = `viewer/nm-jobs.js` · 게이트 `check_nm_jobs` · 스모크 `shared/smoke_jobsq.js`(운영자 260810 "동시에 2가지 작업을 큐잉하면 첫번째거를 두번째꺼가 덮어씌워져 · 동시에 병렬로 진행되는게 가장 좋고")]** 스튜디오 5탭(편집·콘티·프롬프팅·음원·큐영상)이 **진행 중 작업을 한 개만** 들고 있었다 — ⓐ 재개 슬롯(localStorage `nm_*_pend`)이 **객체 1개**라 두 번째 발사가 첫 번째를 덮었다(새로고침·탭 이동 = 첫 작업 영영 소실) ⓑ 폴 중단 핸들(`_curStop`)이 **전역 1개**라 두 번째 폴이 시작되며 첫 번째 폴을 그 자리에서 죽였다 ⓒ 큐영상·콘티 재개는 `.find()`/`a[0]` **단건**이라 같이 걸어둔 렌더가 완성돼도 목록이 영영 '만드는 중'에 멈췄다. ⚠ **서버는 이미 병렬을 받고 있었다**(`functions/api/_rate.js` rateGate cap=3 = 워크플로별 동시 3개 · 잡 id 유일이라 산출 충돌 0) → 막고 있던 건 화면 쪽뿐이고, 러너는 정상 완료하는데 화면만 그 결과를 영영 안 봤다(= "덮어씌워짐"의 실체). 상속 = 뷰어 head `<script src="nm-jobs.js"></script>` 1줄(nm-rail·nm-sync 동축) · API = `add/list/drop`(슬롯 · 저장 키는 **각 탭의 기존 pend 키 그대로** = 구판 단일 객체를 배열 1개로 승격해 읽는다 = 이관 시점 진행분 유실 0 · TTL 24h = 각 탭 재개 게이트 동값) + `hold/free/busy/count`(폴 레지스트리 = 메모리 Map · 같은 id 재폴만 거절 = 중복 폴 0 · **남의 폴은 안 끊는다** = 병렬 성립). 화면 점유는 이 모듈이 안 정한다(탭마다 대기 스테이지 생김새가 다르다) — 각 탭이 「화면 주인」 1건을 들고(`EDIT_ID`·`FOCUS_ID`·`FOCUS_JOB`·`FOCUS_OUT`) 새 발사가 오면 앞 작업이 **자동 강등**된다(강등분은 상태줄·결과창을 안 건드리고 완료분만 결과 레일 `nmRail.add`·작업 내역으로 착지). ⚠ 신설 사유(스모크) = **이 레포 스모크 25종이 전부 「화면이 어떻게 그려졌나」를 잰다** — 「작업을 두 개 걸면 둘 다 살아 있나」는 축 자체가 없었고, 화면 증상이 「아무 일도 안 일어남」뿐이라 운영자 눈이 유일한 검출기였다(insta-thumb-miss·brk_misfire 동축). 강제 = `check_nm_jobs` 하드게이트(5탭 상속 + 모듈 골격 + **구판 회귀 차단**{`localStorage.setItem(PEND_KEY…)` 직접 저장 · 전역 `_curStop` 부활} · 주석부는 잘라내고 코드부만 본다 = 처방문 자기적발 차단[첫 실행 실측]) + 런타임 스모크 판정 9축(폰 430 실렌더 · **발사 0** = 가짜 id로 폴만 띄운다 = 러너·과금 무접촉) = 구판 슬롯 승격 · 2건 동시 생존 · 화면 주인 이동 · 중복 폴 차단 · 재개 전건 · 음원·콘티·프롬프팅·큐영상 동축 · 실증 = 킬테스트(nm-jobs를 구판 단일 슬롯 + '새 폴이 앞 폴을 죽인다'로 되돌리면 **6축 정확 검출**) + 복원 후 전건 PASS. ⚠ **짝 실사고(같은 커밋 봉합)** = `song.html statusWait()`이 어디에도 없는 `mode`를 참조해 `window.nmEta`가 살아 있는 실환경에서 **호출 즉시 ReferenceError** — 발사는 접수됐는데 `fireApi`의 catch가 받아 화면엔 「요청 실패 — 재시도(mode is not defined)」가 뜨고 폴이 시작조차 안 됐다(곡·보이스 결과를 영영 못 봄 · 재개 경로도 같은 자리에서 죽어 다음 진입에도 복구 불가) → `statusWait(on,t0,label,mode)` 인자화. 이 스모크 첫 실행이 그 자리를 즉시 지목했다(정적 게이트·기존 렌더 스모크는 전건 통과 상태였다 = 「발사 뒤 진행 표시가 서는가」가 사각).
- **[동기화 생명선 SSOT = `viewer/nm-sync.js`(운영자 260803 4차 "전 스튜디오 상속")]** 이미지·영상 스튜디오 전 탭(thumb·tr·edit·sb·k·song·vd)이 `<script src="nm-sync.js">` 한 줄로 상속 — ① 비활성→활성 복귀 시 `window.nmRefresh` 자동 호출(타 기기 제작분 재동기) ② `/manifest.json` redirect:manual 프로브로 Access 로그인 만료 확정 시 최상위 `/?nosw=1` 자동 재진입(15s 열림 창 + sessionStorage 3분 루프 가드 · 회선 사망은 재진입 무익 = `nmSyncWarn` 폴백) ③ 자기 문서 HEAD ETag 대조로 새 배포 감지 → 한가할 때(`nmSyncBusy` 훅·입력 포커스 없음) 자동 리로드 = 신규 코드 수동 재열기 불요. 새 스튜디오 탭 = 이 한 줄 동반 계승(레일 상속과 동축·선택 아님) · 강제 = `check_nm_sync` 하드게이트(고정 7표면 + `window.nmRefresh` 보유 표면 자동 발견 + 모듈 3축 골격 검사).
- **[입력칸 클립 SSOT = `viewer/nm-clip.js` + `viewer/nm-clip.css`(운영자 260803 "모든 텍스트 입력창에는 붙어있어야돼 · 공식처럼가야함")]** 텍스트 입력칸은 예외 없이 **[복사][붙여넣기][지우개/되돌리기]** 클립을 단다 — 부품 = 이 2파일 단일정본(index 인라인 정본의 바이트 이관 · 로직·값 무변경) · 상속 = 뷰어 head `<link rel="stylesheet" href="nm-clip.css">` + `<script src="nm-clip.js">` 2줄(`nm-svg.js` **뒤** = 아이콘 상수 의존 순서) · 신규 칸 = `attachCopyPaste(el, true)` 1줄. 표면 스코프 오버라이드(`.revdlg .iobtn-edge` 하단 걸침 등)만 각 뷰어 인라인 잔류(인라인이 뒤라 재승리). **면제 = 숫자칩(`.geni-ar-in`·`.rsz-ar-in`·`.opa-in` = 값 선택 UI라 생김새가 칩)과 PIN(`#pinInput` readonly·`#pubPin` 잠금) 뿐** — 늘리려면 `_CLIP_EXEMPT_*`에 사유와 함께 1줄. 강제 = `check_clip_coverage` 하드게이트(전 뷰어 정적 입력칸 자동 발견 · 3문법{attachCopyPaste·.scnclip·.urlclip} 배선 흔적 인정 · `<script>` 안 템플릿 문자열 제외 = 클립 폴백 자신(`.pastefb-ta`)을 잡는 자기참조 차단). ⚠ 260803 전수 감사 실측 = 51칸 중 **30칸 누락**(음원 탭 6칸 전멸·미디어 요약·찾아바꾸기·구독 메모·검색바) — 게이트 없이는 새 칸이 조용히 빠진다.**
- **[사본 승격 도구 = `shared/css_hoist.py` + `shared/css_hoist_verify.js`(운영자 260807 "ㄱㄱ")]** 공용 CSS 사본 승격을 **매 세션이 스크립트를 새로 짜서** 하다가 260807에 3번 연속 화면을 깼다 — 그게 3번 터진 **직접 원인**이라 절차를 파일로 굳혔다. 사용 = `--scan`(남은 가족) → `--family=.go`(드라이런) → `--family=.go --apply`. 계약 = **안전하지 않으면 자동 롤백**(승격 → 5축 판정 → rc≠0이면 원상복구 = 세션이 "됐다"고 선언할 여지 제거 = [1] 정직의 기계화). 판정 5축 = ① computed 대조(실재 요소 전수 × 31속성 **× 요소·::before·::after 3중**) ② **스크롤바 실폭**(`innerWidth - clientWidth` — 의사요소는 ①의 대상이 아니라 구조적 사각) ③ 페이지 에러·버튼 수·body 높이(문서 붕괴 감지) ④ **픽셀 md5 + 표면별 결정론 선검사** ⑤ `<style>` 태그 균형(정적). ⚠ **결정론 선검사가 실효 조건** = 같은 코드로 2회 찍어 해시가 갈리는 표면은 픽셀 축 SKIP + 명시 기록 — 실측 `index.html`은 뉴스 데이터·시계·애니메이션으로 3회 전부 다른 해시라 **가짜 빨강 공장**이었고, 멀쩡한 `.go` 승격이 그것 때문에 롤백됐다(C14 「자는 줄바꿈 무관 축이어야 한다」와 같은 축). ⚠⚠ **이 도구의 PASS는 최종 판정이 아니다 — 승격 뒤 반드시 `bash shared/smoke_all.sh`.** 판정기는 각 뷰어를 **직접** 열 뿐 **상태를 만들지 않는다**(모달을 안 열고·탭을 안 옮기고·데이터를 안 시드한다) → 그 상태에서만 렌더되는 부품은 **요소가 아예 없어 대조 대상이 안 된다**. 실사고 = `.topdock` 승격이 AI생성 판 도크 페이드 스커트를 깼는데(sh auto·그라데 불일치) 이 판정기가 **두 번**(요소 축 · 의사요소 축 추가 후) 다 통과시켰고 `smoke_parity` C11이 잡았다 → 상태 의존 표면을 건드리는 가족은 이 도구만으로 승인 금지. ⚠ 게이트 `check_shared_canon`은 정본 셀렉터를 **자동 발견**한다(손 목록이면 새 승격이 조용히 안 실린다 = 이 레포가 반복해 겪은 손 레지스트리 드리프트 · 하한 5종 fail-closed). 260807 실적 = 6가족 **101규칙** 승격(`.go` 38 · `.gdots` 16 · `.code` 13 · `.firebar` 12 · `.ogrid` 13 · `.optstrip` 9) · **자동 롤백 2건**(`.dl-full` 판정기 검출 · `.topdock` 스모크 검출) · 남은 사본 **59종 213규칙**(`pvsec` 39 · `rfab` 25 · `cpprev-box` 20 …) = 같은 절차로 순차 이관.**
- **[공용 부품 CSS = 정본 1개 참조 = `viewer/nm-shared.css` · 게이트 `check_shared_canon`(운영자 260807 "혹시 여기말고 다른 모든 공간에도 이와 동일하게 해줘")]** 클립과 같은 축을 레포 전역으로 확장. 260807 전수 스캔 실측 = **3파일 이상에 값이 완전히 같은 사본**으로 흩어진 규칙이 **83종·387개**였다(`.pvsec` 36 · `.cpprev-box` 27 · `button` 21 · `#rfab` 20 · `.go` 16 · `.gdots` 16 · `.code` 13 · `.firebar` 12 …). 그 중 **전역·원자 규칙 5종(59규칙)** 을 승격했다 = 스크롤바 3종 · 폼 활자 리셋 · 포커스링. ⚠ **왜 5종만인가 = CSS 캐스케이드는 규칙을 옮기는 순간 전역 재배치된다.** 83종을 한꺼번에 올린 3회차 실측이 전부 새 불일치를 냈다{ⓐ `.pvsec` height 261→522 ⓑ lucy `.geni-opt.on` 700→800·`.pub-l` 400→700 ⓒ edit `.cpv-tool` transition all→transform}. 셀렉터 단위 면제로도 안 끊긴다 — **같은 요소를 잡는 *다른* 셀렉터**가 SSOT로 옮겨가며 순서가 바뀌기 때문. → 부품 가족은 **하나씩** 이관하고 매번 computed 대조하는 수밖에 없다(클립이 그 방식이었다). 남은 78종 = **가족 단위 순차 이관 대기**. ⚠ **noscript 안 `<style>`은 대상 밖** — 「JS가 죽었을 때만」이라는 **조건부 정본**이라 공용 SSOT로 올리면 정상 상태에서도 적용된다(260807 실사고 = 스크롤바 폴백 `width:10px;display:block`이 되살아나 260726 "스크롤 없애" 계약 위반 · ⚠ **computed 대조는 의사요소를 못 재서 이걸 못 잡는다** — `innerWidth - documentElement.clientWidth` 실폭 측정으로만 검출됐다 = 대조 축 자체의 사각). ⚠ **자동 승격 스크립트의 3대 함정(전부 실측으로 터졌다)** = ⓐ 주석을 셀렉터로 오독(`/* … */\n  *` 가 통째로 셀렉터 → 페이지 붕괴) → **길이 보존 공백 마스킹**으로 봉합(인덱스가 원본과 1:1 유지돼야 제거 범위가 안 어긋난다) ⓑ 규칙 뒤 줄 끝을 무조건 삼켜 **`</style>` 닫는 태그를 먹음**(buttons 0 · bodyH 0 · **콘솔 에러 0 = 무증상**) → 꼬리가 **주석뿐일 때만** 줄 끝까지 ⓒ 파일 단위 제외가 **대상 집합을 바꿔** 새 불일치 생성. ⚠ 게이트 판정 함정 2종 = ⓐ bare substring이면 `#geniOut::-webkit-scrollbar {` 가 `::-webkit-scrollbar {` 로 잡힌다 → **단독 규칙 판정**(`_anchor_menu_block` 계승) ⓑ 값이 다른 인라인은 그 표면의 **정당한 고유 선언**이다 → **값이 SSOT와 같을 때만** 사본으로 친다(실측 = index `button, input, textarea, select` 는 12표면 동값 그룹 밖이었다가 값 대조로 편입). 강제 = 3축(정본 규칙 실존 fail-closed / 승격분 인라인 재선언 0 / link 실존) · 정적 · **면책표 없이 하드 0** · 실증 = 13파일 computed 전건 동일{스크롤바 실폭 0 · 폼 활자 4종 · 포커스링 · 렌더 · 에러 0}.**
- **[클립 = 4문법 한 벌 = 정본 1개를 *참조*한다 = `check_clip_canon`(운영자 260807 "정본1개고 그걸 다 20개 복사하는 개념이 아니라 정본1개를 참조해서 불러오는 개념으로 쓰셈 .. 클로드 뭐 디자인할때 이런 개념이 아예 없어서 짚어줘야됨")]** 같은 클립 부품이 **접두만 달리해 4벌로 복사**돼 있었다 — `.iobtn-edge`(thumb·요약) / `.scnclip`(tr·sb·k·vd) / `.urlclip`(ly·track·conv) / `.askclip`(index) · 값·순서·주석까지 같은 사본인데 **클래스 이름만** 다르다. 값이 같으니 무해해 보이지만 한 곳을 고치면 나머지 3벌이 조용히 낡는다 — 260807 실측에서 **이미 네 축이 갈라져 있었다**: ⓐ `z-index` 2(iobtn) ↔ 5(나머지) ⓑ `transition` 3갈래(raw `.15s` ↔ 토큰 `var(--dur)`) ⓒ `-webkit-tap-highlight-color` 유무 ⓓ **`tr.html`만 `backdrop-filter:blur` 잔류** — 나머지 8표면은 260701 「타이핑마다 재샘플 번쩍」 계약으로 전부 제거된 상태라 **계약 위반이 6주 무증상**이었다(운영자 "원래는 해놓고 몰랐어" = 사람 눈이 유일한 검출기). 정본 = `viewer/nm-clip.css` **그룹 셀렉터 1벌**(`.iobtn-edge, .scnclip, .urlclip, .askclip` + 역할별 자리 한 줄 = `check_anchor_menu_canon` 그룹 관례 계승 · 마크업 변경 0) · 상속 = 뷰어 head `<link rel="stylesheet" href="nm-clip.css">` **1줄** · 각 표면엔 **위치·걸침 방향 오버라이드만** 잔류. ⚠ 신설 사유 = 기존 게이트가 전부 다른 축이다 — `check_clip_coverage` = **클립이 붙었나**(존재) · `check_design` = raw **개수** · 팔레트 핀 = 뷰어 **간 색** 동값 → 「같은 부품이 **한 원천에서 오는가**」는 축 자체가 없었다. 판정 = 정적(렌더·LLM·네트워크 0) · 표면 자동발견(`viewer/*.html`) · 3축 = ① 그룹 셀렉터 실존(해체·개명 = **fail-closed**) ② 클립 문법 보유 뷰어의 `nm-clip.css` link 실존(첫 실행이 곧바로 thumb의 링크 부재를 「사본으로 사는 중」으로 지목) ③ 각 뷰어 **단독 규칙**에서 SSOT 축 재선언 0. ⚠ 면제 3건은 **구조적 사유가 있는 것만**(`_CLIP_CANON_EXEMPT` · 늘리려면 사유 1줄 = 값 사본 복귀의 뒷문이 되면 게이트가 죽는다) = ⓐ `index .askclip` **= `.sbtn`과 합성되는 유일 표면** — head link SSOT가 **같은 특이도**의 인라인 `.sbtn`에게 캐스케이드로 진다(실측 = 인라인 제거 시 width/height 26→34·radius 50%→10px 등 **10축 붕괴** · 특이도 재설계는 별건) ⓑ `edit .urlclip` = 칸 **안** 세로중앙(top:50%) = 걸침 계열과 다른 문법 ⓒ `thumb .iobtn-edge` = `.iobtn` 베이스가 raw판(9px·.15s)↔토큰판(`--r-s`·`--dur`)으로 갈려 **값 정리가 선행 조건**(미이관 = 다음 단계 · link는 먼저 달아둔다). ⚠ **캐스케이드 함정 = head `<link>`는 인라인 `<style>`보다 항상 앞** → 같은 특이도면 인라인이 이긴다. 그래서 사본 제거는 「그 표면에 경쟁 규칙이 있는가」를 먼저 봐야 하고, 안전 확인 수단은 **제거 전/후 computed 대조**다(260807 실측 = 7표면 완전 동일 · index만 10축 붕괴로 검출 → 원복). 실증 = 인라인 사본 **81규칙 제거** · 8표면 실렌더 전건 정상 · computed 차이 1건(= tr backdrop-filter 제거 = 의도한 드리프트 수정).**
- **[스튜디오 텍스트 입력칸 SSOT = `viewer/nm-input.css` · 게이트 `check_input_canon`(운영자 260804 "편집의 폰트 형태를 따라가는게 정석 · 폰트 형태 & 텍스트 박스 형태 둘다 · 딱 그부분만 다른 곳에 다 배선" → "창 크기까지 · 3줄짜리로 · 1줄 아니면 3줄임 · 항상 텍스트 박스는 저거로 고정" → "게이트 ㄱㄱ")]** 정본 원천 = `viewer/thumb.html`(카드 제작·**편집**·특수) 입력칸 블록 = 실측 **13.5px/400 · lh 21.6 · ls −0.2 · pad 12 · 면 rgba(14,15,17,.55) · 선 accent .35 + 링 3px .08 · r --r-s** · 창 크기 계약 = **1줄(`input.nmin`) 아니면 3줄(`textarea.nmin` = 104px = thumb `.covrow textarea` 3줄 정본), 중간 없음 · 높이 재선언 금지**. 상속 = 뷰어 head `<link rel="stylesheet" href="nm-input.css">` 1줄 + 칸에 `class="nmin"` 1줄. **클래스 훅인 이유** = 셸 문서(`viewer/index.html`)는 뉴스 앱까지 담아서 정본처럼 bare `textarea{}`를 풀면 피드·검색·구독 메모까지 스튜디오 폼으로 칠해진다(도구 문서 thumb·tr은 문서 전체가 폼이라 bare 셀렉터가 정본으로 잔류) · 「딱 그부분만」 = 칸 자신의 활자·박스만, 오버레이(포스트) 칸의 자간·마진·별표 하이라이트·클립 자리 padding-right는 그 칸 전용이라 비대상. ⚠ 신설 사유 = **같은 칸이 하루에 세 번 갈렸다**(활자 13px/700 → 박스 pad 6/10·면 흰 .04 → 창 96 vs 104) — 셋 다 정적 게이트를 그냥 통과했고 운영자 눈이 유일한 검출기였다(기존 게이트는 「클립이 붙었나」·「자동완성을 껐나」만 보고 **「그 칸이 정본 활자·박스·창 크기로 태어났나」는 축 자체가 없었다**). 강제 3축 = ⓐ 정본↔SSOT 값 8축 + 3줄 창 대조 ⓑ `.nmin` 보유 + link 실존 + 여러 줄 칸 구 칩 문법(`.geni-in`) 부활 0 ⓒ `.nmin` 스코프 height류 재선언 0 + `rows=3` · 면제 = `check_clip_coverage`와 같은 경계(`_CLIP_EXEMPT_CLS` 숫자칩·PIN) 재사용 = 손 목록 이중화 0 · 실증 = 킬테스트 8종 전건 검출 + 무변경 위양성 0(⚠ 실측 구멍 2종을 킬테스트가 잡았다 = 정본 블록 앵커를 `select`로 두면 앞쪽 활자상속 빈 블록이 첫 매치 · SSOT link를 문자열 언급으로 판정하면 주석만 있어도 통과).
- **[모델 표시명 SSOT = `viewer/nm-models.js`(운영자 260803 4·5차 "모델명 항상 통일 · 아이디어도 배선")]** 화면에 모델을 이름으로 쓸 땐 `NM_MODELS` 사전 값만 — 음차(클링·시댄스·페이블·오퍼스·제미나이·수노)·변형(Kling 3.0[비Omni]·GPT 5.6[비Sol]·Seedance 2.5·Gemini 3.1 flash/Flash image) 금지. sb·k = 런타임 참조 · 서버(`api/sb.js` DIRECTOR_NM)·index GENI_ENG_ICO·k `.eng` 라벨 = 게이트가 사전과 문자 동기. 새 모델 = 사전 1줄부터 · Seedance = **2.0**(운영자 260803 확정 "2.0이 맞고"). 강제 = `check_model_names` 하드게이트(음차·변형 파일별 래칫[주석 인용 = 면책 스냅샷 `_MODEL_NM_BASE`] + 리터럴 표면 동기 + sb/k 로드 줄 실존).
- **[부채 래칫 = `shared/debt_ledger.json`(운영자 260803 "ㄱㄱ")]** 이 레포의 게이트는 전부 면책표(baseline)를 안고 산다 — 그래야 신규 회귀만 잡고 레포가 안 언다. 문제는 면책표가 **한 방향으로만 자란다**는 것: 등재는 1줄이고 해소분 삭제는 아무도 안 한다 → 「알고 동결한 부채」가 「원래 그런 것」으로 굳는다(260802 INK_BASE `{편집:2.6}` 회수 = 사람이 우연히 발견한 축). → `check_debt_ratchet`이 `shared/check_refs.py`·`shared/smoke_*.js`의 `*_BASE`/`*_EXEMPT` 리터럴을 **자동 발견**해 항목 수를 원장과 대조 = **늘면 커밋 차단 · 줄면 낮추라고 알린다**(줄이는 건 자유·늘리는 건 사유 + `python3 shared/check_refs.py --debt-sync` diff가 승인 기록). 260803 기준 = 12표 171건. 카운터 = **최상위 depth 콤마**(dict·set·정수키·튜플키 동일 규칙 — 구 「'키':」 패턴은 set·정수키 표를 통째로 0으로 세어 「부채 없음」이라 거짓말할 뻔했다 = 실측 봉합). **미해결 부채는 원장 `open_items`에 사람 말로 남긴다**(안내문 카드 h27 덮개 · 번역 입력칸 · AI생성 포맷 칩 · **레일 셸 간 2.4배** · 빈 미리보기 박스 7탭) — 매 커밋 출력에 그대로 떠서 잊히지 않는다. ⚠ 원장 = 기계산출물 = 손편집 금지.
- **[히트존 3축 = `shared/smoke_hitzone.js`(운영자 260803 "기본 아냐 저건?")]** 「보이는 버튼 = 눌리는 버튼」 상비 스모크(2셸 10탭 격자 프로브 · smoke_all 자동발견) — H1 캡슐 안 단독 컨트롤의 히트 면적 ≥55% · H2 히트 높이 ≥30px · H3 **어포던스 거짓말 0**(손가락 커서가 뜨는데 누를 게 없는 자리). 신설 사유 = 260803 자막 사고(카드 127.3×37.2인데 히트존 24.7×19.2 = **면적 10%**)가 기존 게이트를 전부 통과했다 — check_refs는 정적 문자열, parity·studioshell은 잉크·치수·골격 = **전부 「보이는 것」만 잰다**. 정적 짝 = `check_affordance_inherit`(스킨은 계승·어포던스는 비계승 = `cursor:pointer`가 마커 없는 노드에 걸리면 커밋 차단 · 계약 전문 = CII 「미리보기 코너 옵션 레일」 행).
- **[퇴장 모션 1정본 = `@keyframes` 중복 정의 0 = `check_keyframes_dup`(평의회 260804 4·5·8번)]** CSS Animations L1 §2 = **같은 이름이 두 번 선언되면 뒤엣것이 앞엣것을 통째로 대체**한다(병합 아님·부분 오버라이드 아님) → 중복은 「무해한 사본」이 아니라 **앞 선언을 조용히 삭제하는 문장**이다. 실사고 = `viewer/index.html` `popOut` 2벌 — 414행(살아있는 `.pmenu`·`#lockpop`·`.qpop` 퇴장 정본) vs 1418행(**260710에 폐지된 `.filterpop` 잔해** · 요소 0건·`closeFilterPop` = no-op 스텁)인데 **캐스케이드 승자가 1418** = 죽은 컴포넌트의 사본이 살아있는 팝업 3종의 모션을 지배했다. 값이 동값이라 **6주간 무증상** = 정본(414)을 고쳐도 화면 무변, 죽은 사본을 한 글자 건드리면 무관해 보이는 팝업 3개가 동시에 갈리는 최악의 추적난도. ⚠ 신설 사유 = 기존 게이트가 전부 다른 축을 본다 — `check_design`은 토큰 **값**, `check_layout_transition`은 전이 **속성**, `smoke_*`는 애니가 **끝난 그림** → 「같은 이름이 두 번 선언됐는가」는 축 자체가 없었다. 판정 = 정적(렌더·LLM 0) · 표면 자동발견(`viewer/*.html`+`viewer/*.css`) · **면책표 없이 하드 0**(현행 위반 0 = 원장 증가 0). ⚠ 스코프 = HTML은 `<style>` 블록 안만(화이트리스트 = @keyframes의 유효한 거처가 그뿐) · `<script>` 안에서 조립되는 리포트 HTML의 `<style>`은 배제(`check_clip_coverage` 템플릿 문자열 배제 원칙 계승) · **`.js` 비대상**(`nm-loader.js`처럼 런타임 주입이 곧 SSOT인 부품 = 중복이 아니라 단일정본 배포) · **파일 간 동명 비대상**(`goFill`×10·`gdots`×8 등 17종 = 뷰어는 독립 문서고 `tokens.css`는 구조토큰 거울 전용이라 keyframes 공유 수단이 물리적으로 없다 · 크로스파일로 세면 60건+ 위양성으로 레포가 언다). ⚠ 함정 실측 = HTML 주석을 파일 전역에 먼저 지우면 CSS 주석 제거가 HTML 주석의 닫는 `-->`를 지워 `index.html` 45~3692행이 통째로 blank 처리되고 **popOut 2건이 둘 다 사라져 「중복 0」으로 거짓 통과**한다 → 구간(`_kf_regions`)을 먼저 잘라 교차오염을 구조적으로 봉쇄. 실증 = 킬테스트 5종(위반 검출·주석 무시·수리 후 위양성 0·`<script>` 문자열 무시·`<style>` 밖 무시).
- **[죽은 상태 오버라이드 = 특이도 패배 래칫 = `check_css_dead_state`(운영자 260806 "아이디어 해결 ㄱ")]** 이 레포는 오버라이드의 의도를 **주석으로** 설명하는 관례인데 주석엔 강제력이 0이라 「이긴다고 적힌 쪽이 실제로는 특이도로 지는」 상태가 조용히 생긴다. 실사고 2건(260806 · **CDP 캐스케이드 순서로 확정** — computed 값 직독은 `.go`의 `transition:border-color`가 걸려 **중간값**을 주므로 증거로 못 쓴다{실측 함정}): ⓐ `viewer/thumb.html` 260731 주석이 「`.dropping` 강조 링은 border-color 오버라이드라 **그대로 살아 있음**」이라 단언했는데 **거짓** — `.cpprev-box.dropping`(0,2,0) < `.topdock[data-lay="edit"] .cpprev-box`(0,3,0)라 도크 안 첨부 드래그오버 링이 **한 번도 안 보였다**. ⓑ 같은 파일 `#go{border-color:.08}`(1,1,0급 id 선언)이 발사 버튼의 **모든** 클래스 오버라이드를 눌러 죽였다 — `.go.err`(빨강)·`.okdone`(초록)·`.rszbusy`·`.spell-arm` 4상태 + **운영자 260731 12차 「너무 옅음」 지시로 넣은 도크 `.2`**까지 전멸(6일간 지시 미발효). 게이트 첫 실행이 **같은 가족을 4뷰어에서 더 검출**(edit·k·sb·song = `.topdock #<발사id>{border-color:.08}` 사본 6건) = 사람 눈으로는 못 찾던 사각. ⚠ 신설 사유 = 기존 게이트가 전부 다른 축이다 — `check_design` = raw **개수** · 팔레트 핀 = 뷰어 **간** 동값 · `check_keyframes_dup` = **이름** 중복 · `smoke_*` = **애니 끝난 그림** → 「같은 속성을 두 규칙이 다투는데 상태 쪽이 지는가」는 축 자체가 없었고, 증상이 「그냥 그 상태가 안 뜸」뿐이라 운영자 눈이 유일한 검출기였다. 판정 = 정적(렌더·LLM·네트워크 0) · 표면 자동발견(`viewer/*.html` `<style>` + `viewer/*.css` · 구간 선분리 = `_kf_regions` 주석 교차오염 계약 계승) · 술어 = 「기저+상태」 단일 복합선택자 B가 **같은 요소를 잡는** 더 센 규칙 C에게 같은 속성을 빼앗긴다. 위양성 4겹(실측 31→21→11→6까지 좁힌 경계) = ⓐ **상태는 JS 토글분만**(`classList.add/remove/toggle`) ⓑ **구조자(rescue) 인식** — 상태를 되살리는 더 센 규칙이 있으면 살아 있다(**이 겹이 없으면 게이트가 봉합 자체를 위반으로 잡는 자기모순** · 실측 10건이 여기서 걸러졌다) ⓒ **상태-대-상태 면제**(오류 상태가 모든 상태를 덮는 `.pin-slots.bad` 등 = 의도적 우선순위 · 실측 10건) ⓓ **at-블록 밖만**(`@media` 오버라이드는 정당한 거처) + 의사요소 상자 vs 요소 상자 제외 + 경쟁자 최우측 속성·의사 제외. ⚠ **id 경쟁자 축이 실효 조건** = `#go` 같은 id 기저는 CSS만 봐선 `.go.err`와 같은 요소인지 알 수 없다 → 그 파일 **정적 마크업의 `id`→`class` 실측**으로 잇는다(킬테스트 K1이 「id 경쟁자 보수적 제외」로는 진범을 못 잡는다는 걸 드러냈고, 이 축을 넣자 4뷰어 6건이 추가로 잡혔다). ⚠ **하드 0 금지** = 현행 6건(index 3 · k 3)은 전건 **미검증**이라 케이스별 판독 없이 baseline에 박으면 「알고 동결한 부채」가 「원래 그런 것」으로 굳는다 → 래칫(늘면 차단·줄면 알림) + 원장 `open_items` 추적. ⚠ **수리 시 함정(260806 실측)** = 상태 블록 **바로 뒤**에 `:where()` 규칙을 꽂으면 여러 줄 규칙(`.go.firing`) **안쪽**에 떨어지고, 네이티브 CSS 중첩으로 파싱돼 **조용히 무효**가 된다(중괄호 균형·정적 검사 전부 통과 · k·sb 두 파일이 그렇게 갈렸다) → 거처는 「도크 규칙 바로 앞」처럼 **최상위가 보장된 앵커**. 실증 = 킬테스트 3종{진범 `#go` 원복 검출(5건) · 도크 `:where()` 제거 검출(4건) · 수리본 위양성 0} + 5스튜디오 런타임 실측(무상태 `.2` · err 빨강 · okdone 초록).**

- **[CSS 주석 이음매 = `check_comment_seam`(운영자 260807 "아이디어도 적용하고")]** 이 레포는 규칙 뒤에 **여러 줄에 걸친 미종료 주석**이 붙는 관례다 → 개정 사유를 「줄 끝」에 붙이면 그 `*/`가 **선행 주석을 조기 종료**시켜 남은 주석 본문이 **라이브 CSS로 새어 나온다**. ⚠ 신설 사유 = **중괄호 균형·주석 개수 균형이 전부 통과하는 무증상 사고**다 — 260807 한 커밋에서 2건 실측{ⓐ song `:where(#optGo)` 발사 테두리 정본이 먹혀 .2 → .15 ⓑ edit `.pvfire` 블록이 깨져 히트슬롭 소멸(보이는 버튼 30px인데 위 5px가 남의 것)}. 그날은 런타임 스모크(smoke_studioshell C7 · smoke_editdock C6)가 **우연히 그 자리를 덮고 있어서** 잡혔고, 안 덮는 자리에서 나면 그대로 라이브로 나간다. 기존 게이트는 전부 다른 축(`check_design` = 토큰 **값** · `check_keyframes_dup` = **이름** 중복 · `check_css_dead_state` = **특이도**) → 「주석이 앞 주석을 조기 종료했는가」는 축 자체가 없었다. 판정 = 정적(렌더·LLM·네트워크 0) · 표면 자동발견(`viewer/*.html`+`viewer/*.css`) · 구간 선분리(`_kf_regions`) = HTML 주석 교차오염 구조 차단(그 계약 계승) · 술어 2축 = ① 주석 **안**에서 `/*`가 다시 열림 ② 주석 **밖**에서 `*/`가 닫힘 · **면책표 없이 하드 0**. ⚠ 문자열 리터럴 안은 대상 밖(`content:"/*"`·`url("…")` 오탐 차단 = 따옴표 추적). **처방 = 개정 사유 주석의 거처는 규칙 닫는 중괄호 직후 · 다음 주석 열기 전(줄 끝 금지).**
- **[잰크 전이 래칫 = `check_layout_transition`(운영자 260804 「impeccable 적용점」 8인 평의회 · 선별이식 ①)]** `transition:width|height|padding|margin`(min/max 변종 포함)은 프레임마다 리플로를 강제한다 = 잰크. 이 레포는 「덜컹」을 반복해 겪고(260716 도크 마진 −18 "덜컹 원천 소멸" · 260803 스크롤 중 −2px = `smoke_studioshell` C10) 그때마다 축을 늘렸는데 **전부 애니메이션이 끝난 뒤의 그림**을 잰다 — C7은 창 **크기**, C9는 **정지** 세로축, C10은 스크롤 **후 정착** 위치. 「transition이 재생되는 **중간 프레임**」은 축 자체가 없었다. 판정 = `viewer/*.html`+`viewer/*.css` 자동발견(새 뷰어가 조용히 못 빠진다) · 주석 줄 제외 · 렌더·LLM 0 정적 · 면책 `_LAYOUT_TRANS_BASE` 파일별 스냅샷 초과만 FAIL(줄면 낮추라고 알린다 = raw baseline 문법 동문). ⚠ **하드 0 금지** = 현행 7건(edit 1·index 4·thumb 2)은 전부 선존 아코디언(`max-height`)·폭 모프라 제거 = [4] 창작금지 충돌 → 래칫이 유일한 정합 형태. 임계값 원천 = 외부 디텍터 `pbakaus/impeccable` `cli/engine/rules/checks.mjs`의 `layout-transition` 속성 집합을 **그대로 채택**([4-3] 값 창작 0) — 그쪽 **설치·훅·스킬·23커맨드는 미도입**(평의회 실측 = 59룰 통째 적용 시 두 레포 경고 690건 중 **574건[83.2%]이 글래스·글로우·균일간격 등 이 레포 정본**이라 위양성이고, `colorize`/`bolder`/`delight`/`overdrive`는 디자인 창작 = [4] 정면 위반이며, 훅은 `.claude/settings.local.json`에 **병합·병렬**로 붙어 `design_gate.py`의 정본 강제와 상충 지시가 같은 턴에 도착한다). 실증 = 킬테스트 3축(위반 검출·주석 줄 무시·`opacity`/`transform` 위양성 0).
- **[앵커 메뉴 문법 = 한 벌 = `check_anchor_menu_canon`(운영자 260805 "아이디어 ㄱ")]** 같은 결의 메뉴 3종 — **설정 `#linkpop` · 스튜디오 헤더 메뉴 `.tool-menupop` · PASS 사유 `.sc-rsn`** — 이 셸 기하·항목 행 값을 **각자 베껴 쓰고 있었다**. 정본이 바뀌면 나머지가 조용히 낡는데 증상이 「어느 창 하나만 좀 달라 보임」뿐이라 **운영자 눈이 유일한 검출기**였고, 실제로 260805 실측 시점의 PASS 사유 창은 혼자 다른 문법(테두리 칩 12/700 · 닫기 X 20px 원형 · radius 12 · 폭 172~280 가변)으로 갈라진 채 살아 있었다. → 값을 **셀렉터 그룹 2벌**에 모은다(위 「앵커 팝업 셸 글래스」 그룹[index 252행] 관례 그대로 = 마크업 변경 0·드리프트 0): 셸 기하 = `#linkpop, .tool-menupop, .sc-rsn`{폭 230 고정 · padding 11/15 · gap 3 · radius --r-l} · 항목 행 = `#linkpop .lp-btn, .tool-menupop button, .sc-rsn-c`{13/600 · pad 8/4/8/--sp-4 · r7 · 무테 · hover = --accent + 유리 .06}. 각 표면엔 **위치·모션·상태 오버라이드만** 남는다. ⚠ 신설 사유 = 기존 게이트가 전부 다른 축이다(`check_design` = raw **개수** 래칫 · 팔레트 핀 = 뷰어 **간** 동값 · `smoke_winnav` = 모달 **헤더** 기하) → 「같은 결의 메뉴가 한 벌인가」는 축 자체가 없었다. 판정 = 정적(렌더·LLM·네트워크 0) · **면책표 없이 하드 0** · 2축 = ① 두 그룹 셀렉터 실존(그룹 해체·개명 = fail-closed) ② 멤버의 **단독 규칙**에서 SSOT 축 재선언 0. ⚠ 스코프 = 정확한 단독 규칙(`sel {`)만 — `:hover`·`.up`·`.inp` 변형과 미디어쿼리 오버라이드는 대상 밖(각 표면 고유 권한을 얼리지 않는다). ⚠ **함정 실측** = 단독 규칙 판정을 「직전 1글자가 `\n{}`인가」로 두면 이 레포의 **2칸 인덴트 CSS에서 전건 미검출**이 된다(킬테스트 K1·K2가 재선언을 심었는데 rc=0으로 통과) → 줄 시작부터 셀렉터까지가 **공백뿐인지**로 판정해야 그룹 멤버(`…, .sc-rsn {`)·복합(`.x .sc-rsn {`)도 함께 정확히 갈린다. 새 앵커 메뉴 = 두 그룹에 셀렉터 1개씩 추가(유리는 252행 그룹 동반) · 실증 = 킬테스트 4종 전건 검출{셸 재선언·항목 재선언·구 `.lp-btn` 단독 부활·그룹 해체} + 위양성 대조 2종 무경보{변형 셀렉터·무변경} + 3표면 런타임 등가 실측(셸·항목 computed 차이 **0**).
- **[UI 스모크 경보 = 사유를 갖고 나간다 · 웹푸시 면제 = `check_smoke_obs_chain`(운영자 260807 "알림 메세지에 그 내용이 쌓이게 · 웹앱 푸쉬알림까지는 안오게 · 다운로드해서 클코에 전달하면 개선할 수 있도록")]** ⚠ 실사고 = **사유 0자 경보가 8일 연속 무증상으로 살았다**(260731~0807 · 나이틀리 런 8건 전건 failure). 구판 `smoke-nightly.yml`이 `grep -E '^❌' smoke.log`로 실패 사유를 뽑았는데 **스모크 24종 중 15종이 `❌`를 한 번도 안 쓰고**(실측) smoke_all 요약줄도 `──`로 시작해 매치가 0 → 산출물이 `{"rc":1,"fail":""}`가 되고 watchdog ⑥이 그 빈 칸을 그대로 발화해 **「UI 스모크 FAIL(rc=1) —  (런 로그 확인)」**만 나갔다. **감지는 정확했는데 무엇이 실패했는지가 어디에도 없어서 운영자가 조치할 수 없었다** = 이 레포가 반복해 겪은 「관측이 구조적으로 지워지는 병」과 같은 축(스레드 `[1차 실측]` · 틱톡 `_e1` · 요약실패 `_fk=code`). 봉합 3축 = ① **사유 추출 정본 = `.github/scripts/smoke_obs.py`**(yml 인라인 grep 폐지 · 종목별 rc·사유·플레이키를 구조화해 `scraper/obs/smoke_last.json`에 굽는다 · **빈 사유 금지 = fail-closed 3단 사다리**{실패 표식 줄 → 블록 꼬리 줄 → rc 명시} · 추출 실패조차 사유가 된다) ② **메시지함 = 진단서 전문**(`watchdog._smoke_report` · `wd-smoke` 슬롯 · insta-thumb-miss 인수인계 진단서 문법 100% 계승 = 회차 실측·실패 종목 사유 실물·플레이키 분리·이미 있는 층·다음 확인 순서·재현 명령·코드 위치 → 뷰어 메시지함 [↓]가 그대로 HTML로 내보내 **다음 클로드 코드 세션의 착수 문서**가 된다) ③ **웹푸시 면제 = `watchdog.PUSH_EXEMPT`**(조치 주체가 코드 축이라 기기 알림으로 깨워도 그 자리에서 할 게 없다 — 면제해도 **감지·메시지함 점등은 그대로** = 무증상화 아님 · 액션 버튼도 없음{눌러 고칠 게 없다 = wd-phone 오도방지 관용구}). ⚠ 진짜 진범도 같이 봉합 = **`smoke_wip`만 `/opt/pw-browsers/chromium`을 하드 폴백**으로 박아 그 경로가 없는 GitHub 러너에서 매 회 확정 실패했다(형제 23종은 전부 `which` 폴백 보유 = 이 한 종만 갈렸다) → `smoke_parity.chromiumPath()` 정본 사본 이식(창작 0). 판정 = 정적 4축(① 나이틀리 정본 파서 배선 + 구판 `^❌` grep 부활 차단 ② **정본 함수 재판정** = `smoke_obs.summarize`를 import해 rc≠0 4케이스에서 사유 0자가 아님을 실호출 확인{사전·정규식 사본 0} ③ 진단서·점등 배선 ④ 푸시 면제 배선 — 면제가 조용히 풀리면 조치 불가 푸시가 되살아난다) · 렌더·LLM·네트워크 0 · **면책표 없이 하드 0**. ⚠ 기존 게이트는 전부 다른 축이다 — `check_refs` 계열 = 정적 **문자열·경로 실존** · `smoke_*` = **화면 렌더** → 「경보가 **사유를 갖고 나가는가**」는 축 자체가 없었다. 실증 = 킬테스트 4종 전건 검출{정본 파서 미배선 · 구판 grep 부활 · PUSH_EXEMPT 해제 · summarize 빈 문자열 반환} + 위양성 대조 무경보 · ⚠ 킬테스트 설계 함정 실측 = `if failed:` 분기만 죽이는 판은 **fail-closed 사다리 다음 단이 받아** 사유가 여전히 나오므로 검출이 안 된다(그건 계약이 지켜진 것 = 올바른 통과) — 계약을 깨려면 `summarize`가 **실제로 빈 문자열을 반환**하게 해야 한다.
- **[스모크 크로미엄 경로 = 폴백 해석기 경유 = `check_smoke_chromium_path`(260808 실사고 봉합 · 위 항목의 짝)]** ⚠ 신설 사유 = **바로 위 260807 봉합이 같은 병을 앓던 두 종 중 한 종만 고쳤고, 아무 게이트도 안 울렸다.** 그 커밋 주석은 「형제 23종은 전부 `which` 폴백 보유 = **이 한 종만** 갈렸다」고 단언했지만 **실측은 2종**(`smoke_wip` ∧ `smoke_photoflow`)이었다 → 봉합 **다음** 나이틀리(런 31211705342 · 260808 04:29 KST)도 `smoke_photoflow=1`로 그대로 붉었고 운영자는 8일째 같은 알림을 또 받았다. 계약 = 「`shared/smoke_*.js`가 브라우저를 띄울 때 `executablePath`에 **리터럴 경로를 박지 않는다**」 = 정본 해석기 `chromiumPath()`(env → `/opt/pw-browsers` → `which` 폴백 · 정본 = `shared/smoke_parity.js`) 경유. **⚠ ②축 = 해석기 「몸통」 정합(260809 실사고 봉합 · ①축의 사각)** — 260808 판은 「executablePath에 리터럴을 박았나」= **호출부**만 봤는데, 실패가 `chromiumPath()`라는 **정본 이름을 그대로 달고** 다시 났다: 몸통이 `return cands.find(Boolean)`이라 **실존 검사 없이 첫 truthy**(= `/opt/pw-browsers/chromium`)를 그대로 뱉는 사본이었다(실측 2종 = `smoke_rank`·`smoke_favtab`). 호출부는 정본과 글자 하나 안 다르고 **로컬엔 그 경로가 실재해 로컬 PASS·러너만 FAIL** → 게이트·사람 눈 양쪽의 사각(나이틀리 런 31236664571 `smoke_rank=1`) = 「같은 병의 **형제**」가 아니라 「같은 병의 **변종**」. → 술어 = `chromiumPath()`를 정의하면 그 몸통은 후보를 **`existsSync`로 실존 검사**한다(주석 줄 제외 · 하드 0). ⚠ 짝 관측 봉합 = `smoke_obs.HIT_RE`에 **`ABORT`·`Failed` 편입** — 같은 회차 `smoke_fire`는 FAIL 1건인데 사유 칸에 꼬리 줄(`── smoke_fire FAIL 1건 (서버 종료됨)`)만 실려 **무엇이 죽였는지가 원장에도 안 남았다**(실측 `scraper/obs/smoke_last.json` details). 원인 = 예외 경로 출력 문법이 `ABORT | <메시지>`인데 그 어휘가 `HIT_RE`에 없어 ①단이 통째로 미스 → ②단(꼬리 줄)이 받아 **fail-closed는 지켜졌지만 사유는 소실**됐다(`ABORT |` 보유 = 7종 = 이 레포 예외 관용구 · `Failed to launch …`는 대문자 `\bFAIL\b`에 안 걸린다). ⚠ **그래서 `smoke_fire`의 CI 실패 원인은 260809 시점 미확인** — 이 봉합은 다음 회차가 사유를 **말하게** 만드는 것이지 그 사고를 고친 게 아니다. ⚠ 짝 게이트 `check_smoke_obs_chain`은 **경보가 사유를 갖고 나가는가**(관측 축)만 본다 — 「그 스모크가 러너에서 **뜨기는 하는가**」는 축 자체가 없어서, 실행 환경 축의 회귀는 나이틀리가 붉어진 뒤 사람이 런 로그를 열어야만 보였다(insta-thumb-miss·brk_misfire 동축). 판정 = 정적(렌더·LLM·네트워크 0) · **표면 자동 발견**(`shared/smoke_*.js` = 새 스모크가 조용히 못 빠진다) · 주석 줄 제외(주석 처리 우회 차단) · **면책표 없이 하드 0**(현행 25종 전건 정합 · 부채 원장 증가 0) · 실증 = 킬테스트{구판 리터럴 원복 → 파일·행·값 정확 검출} + 위양성 대조{봉합본·복원본 무경보} + 봉합본 실행 13/13 PASS.
- **[봉합 완결성 = 「같은 병의 형제를 놓쳤나」 = `check_seal_completeness`(WARN·비차단 · 운영자 260808 "idea go")]** ⚠ 신설 사유 = **260808 하루에 같은 모양의 사고가 두 건 드러났고, 둘 다 봉합 커밋이 형제를 빼먹은 것이었다** — ⓐ 260807 크로미엄 경로 봉합이 `smoke_wip` 한 종만 고쳤다(그 커밋 주석은 「형제 23종은 전부 which 폴백 보유 = **이 한 종만** 갈렸다」고 단언했지만 실측은 2종 = `smoke_photoflow` 잔여 → 봉합 **다음** 나이틀리도 그대로 붉었다) ⓑ 260728 알림 조치주체 봉합이 `wd-phone` 한 종만 고쳤다(`yt-cookie-dead`·`fire-*` 생산자 미추종 → 코드로 못 고치는 건과 완료 보고가 「클로드가 볼 일」 칸에 6주간 앉아 진짜 코드 건을 가렸다). ⚠ **이 레포 게이트 102개가 전부 「최종 상태가 옳은가」만 본다**(정적 문자열 · 화면 렌더 · 값 대조) → 「**방금 한 수정이 완결됐는가**」는 축 자체가 없었고, 그래서 반쪽 봉합이 rc=0으로 통과하고 남은 절반은 다음 사고까지 무증상으로 산다(insta-thumb-miss·brk_misfire 동축). 술어 = 「이번 커밋이 파일 F에 심볼 S를 **추가**했는데, F의 형제 가족 중 **압도적 다수**(≥`_SEAL_HAVE_RATIO` 0.70)가 S를 이미 갖고 **소수**(≤`_SEAL_MISS_MAX` 3)만 없다」 → 그 소수를 **이름으로 지목**한다. 가족 = 같은 디렉터리·같은 확장자(접두 공유 시 `shared/smoke_*.js`처럼 부분집합으로 좁힘 · 최소 `_SEAL_FAM_MIN` 5). 심볼 = 추가 줄의 함수 호출 · 대문자 상수 · 문자열 리터럴 앞조각(주석 줄 제외 = 처방문 인용·주석 우회 차단). ⚠ **다수결이 실효 조건** = 「먼저 고친 쪽이 소수」인 시점엔 그게 표준인지 기계가 알 방법이 없다(260728 당시 `👉` 보유는 1종뿐 = **정당하게 침묵**) — 표준이 굳은 **뒤에** 새 형제가 빠지면 그때 잡는다. ⚠ **WARN·비차단이 정확한 역할** = 정당한 단독 도입(그 파일에만 필요한 헬퍼)이 섞이므로 하드면 레포가 언다(`check_component_lock`·`check_gate_hits` 선례) — 판정은 사람이 하고 게이트는 **보이게만** 한다. ⚠ 스코프 = `.py·.js·.sh·.mjs`(html 제외 = 1.85MB 셸이 섞여 심볼 잡음이 크다) · `_versions/`·`docs/`·`cards/`·`.claude/`·`scraper/obs/` 밖 · `check_refs.py` 자신(처방문이 곧 심볼 = 자기참조 · `check_ytdlp_aac` 선례) · 전 경로 fail-soft(git 없음·초기 커밋·diff 실패가 게이트를 못 죽인다). 실증 = **킬테스트 A = 260807 반쪽 봉합 재현**(photoflow 구판 상태에서 wip 봉합 줄을 diff에 태우자 「가족 `shared/smoke_*.js`(25) 중 미보유: **shared/smoke_photoflow.js**」를 정확히 지목 = **어제 이 게이트가 있었다면 그 커밋에서 바로 떴고 오늘 세션은 필요 없었다**) + 위양성 대조 B 무경보. ⚠ **남는 한계 = 알려진 위양성 1종** — 같은 킬테스트에서 `smoke_thumbapi.js`도 같이 지목됐는데 그 파일은 **브라우저를 안 띄우는 순수 모듈 스모크**(fetch 스텁)라 `chromiumPath`·`launch(` 미보유가 정당한 차이다. 맥락 앵커(「그 심볼을 쓸 이유가 있는 파일인가」) 판정은 미신설 = 다음 후보.
- **[알림 조치주체 = 👉 문단이 가른다 = 생산자 계약(260808 실사고 봉합)]** 알림 리포트의 조치주체 분류(`viewer/index.html` `_rptWho`)는 **본문에 `👉` 문단이 있어야** 「운영자가 할 일」·「자동 복구 대기」로 가르고, 없으면 폴백이 **「클로드가 볼 일」**이다. ⚠ 실사고 = 260808 리포트 3건이 **전부 「클로드 3 · 운영자 0 · 자동 0」**인데 실제 코드 축은 **1건뿐**이었다 — ⓐ `yt-cookie-dead`(= GitHub Secrets 쿠키 교체 = **코드로 한 글자도 못 고침**)가 클로드 칸 ⓑ `fire-*`(= 기계가 이미 큐까지 넘긴 **완료 보고**)도 클로드 칸. 비용 = 조치 불가·조치 불요 2건이 **진짜 코드 건 1건을 가린다**(운영자가 리포트를 열어도 뭘 해야 하는지 안 보인다). ⚠ **같은 병이 260728에 이미 진단됐다** — `scraper/watchdog.py` 188행 주석이 「👉가 없어 '클로드가 볼 일'로 오분류되던 것」이라 적고 **`wd-phone` 한 종만** 고쳤고, 나머지 생산자는 그때 안 따라왔다(위 크로미엄 경로 봉합과 **판박이 = 같은 병의 형제를 놓치는 축**). 봉합 = 생산자 2곳에 규약 문구 1줄씩(`PHONE_TODO` 문법 100% 계승 · 창작 0 · **분류 로직 무접촉 = 회귀 0**) = `yt_cookie_health.COOKIE_TODO`(→ op) · `fire_watch.FIRE_TODO`(→ auto). 실증 = `_rptWho` 정본 로직 재판정{yt-cookie 클로드→**운영자가 할 일** · fire 클로드→**자동 복구 대기** · `wd-smoke` **클로드가 볼 일 유지** = 회귀 0}. ⚠ 규약 = 「원인이 코드 축이면 👉를 **안 붙인다**」(`viewer/index.html` 8308행) — 새 `msg.py` 발행 알림을 만들 땐 조치 주체를 먼저 정하고 그에 맞는 👉 문단을 **같은 커밋에** 넣는다(선택 아님).
- **[요약 실패 알림 = 조치 주체를 말한다 = `check_fail_msg_todo`(260813 실사고 봉합 · 바로 위 항목의 형제)]** ⚠ **같은 병이 이 레포에서 세 번 재발했고 매번 생산자 한 종만 고쳐졌다** — 260728 `wd-phone` → 260808 `yt_cookie_health`·`fire_watch` → 260812 `insta_signals`. 요약 실패 알림(`analyze.sh` 분석 경로 · `ask.sh` 요약요청 경로)은 **세 번 다 안 따라왔다.** 실측 = 260813 리포트의 「클로드가 볼 일」 칸 1건이 **전부 이 알림**이었고(뉴시스 기사 시간 초과), 실제 조치는 「그 기사를 다시 보내기」 = **코드가 볼 게 0**인데 코드 칸이 차 있었다. 구조 = 실패 사유 4분류(`_fk`) 중 **셋이 운영자 조치**(`timeout`·`congest` = 다시 보내기 / `source` = 전문 붙여넣기)인데 넷 다 👉가 없어 전건이 폴백으로 클로드 칸에 앉았다. 봉합 = 두 경로에 분류별 👉 문단(`PHONE_TODO` 문법 100% 계승 · 창작 0 · **분류 로직·본문 무접촉 = 회귀 0**). ⚠ **`code` 축은 안 붙인다**(cc 유지) = 규약 「원인이 코드 축이면 👉를 안 붙인다」 — 넷 다 붙이면 **진짜 코드 결함이 운영자 칸으로 밀려 무증상**이 된다(이 봉합이 막으려는 병의 역방향 재현). ⚠ **`congest`가 op인 근거** = 자동 재시도 `RETRY_CAP` **소진 후에만** 알림까지 온다(`analyze.sh` 388행) → 「기다리면 된다」는 거짓. ⚠ 신설 사유(게이트) = 기존 게이트는 전부 다른 축 — `check_rpt_origin_coverage` = 알림이 **어디서 왔나**(출처표) · `check_ask_srcimg_chain` = 층 생존 · `check_seal_completeness` = 「같은 병의 형제」인데 **WARN이라 이 재발을 세 번 다 못 막았다** → 「알림이 **누가 조치할 일인지** 말하는가」는 축 자체가 없었고, 화면은 멀쩡해서(알림은 정상으로 뜬다) 운영자가 리포트를 열어야만 보였다. ⚠ **판정 술어는 뷰어에서 읽는다(사본 0)** = `_RPT_CC_RE`·auto 판정 정규식을 손으로 옮겨 적으면 뷰어가 술어를 바꾼 날 게이트만 옛 기준으로 남는다(260812 쿠키 칸 이름 실사고와 같은 축) → 문구를 그 술어로 **재판정**해 op가 아니면 차단(「클로드」 낱말 = cc로 튐 · 「없어요/없음」 시작 = auto로 튐). 표면 자동 발견(`.github/scripts/*.sh` 중 `_fk=` 보유 = 새 실패 경로가 조용히 못 빠진다 · 하한 2 fail-closed) · 정적 · 렌더·LLM·네트워크 0 · **면책표 없이 하드 0**. 실증 = 뷰어 정본 함수 재판정{봉합 전 실물 → 클로드가 볼 일 / 봉합 후 4종 → **운영자가 할 일** / code 축 → **클로드가 볼 일 유지** = 회귀 0} + 킬테스트 5종 전건 검출{분기 미배선·미러 드리프트(ask 전체)·「클로드」 혼입·code 축 부착·「없어요」 시작} + 복원 위양성 0.
- **[STT 엔진 = Scribe v2 + large-v3 폴백 = `check_stt_engine_chain`(운영자 260808 "무조건 반영" → "위험 점검 다 반영" · 평의회 8인)]** 자막·편집·영상요약·링크요약·영상받기 **5경로 전부** ElevenLabs Scribe v2로 받아쓰고, 실패하면 그 자리에서 large-v3로 내려앉는다. 실측 근거 = `stt-ab` run 31230435185(한국어 19.6s): 추론 **28.1s→1.0s** · 저확률(<0.70) 어절 **11.5%→3.4%** · large-v3가 틀린 「검찰서가」를 「경찰서 가서」로 정확히 · $0.0012. ⚠ **한 층만 빠져도 화면은 멀쩡한 채 조용히 죽는다** — ⓐ env 2줄 누락 = 그 파이프만 whisper로 회귀(느려질 뿐 산출은 나온다) ⓑ prefetch 게이트 누락 = **매 런 3.1GB 신규 다운**(260808 실사고 = `ask_link_stt.sh`가 runner-setup 밖에서 setup.sh를 재호출) ⓒ 조립 예외 폴백 제거 = 벤더 스키마 한 번 흔들리면 **자막 파이프 전체 사망** ⓓ 캐시 키 엔진 축 제거 = 구 whisper 전사 재사용으로 **승격 무효 + 킬스위치가 거짓말** ⓔ 폰트 검문 완화 = libass가 **에러 없이 두부(□)로** 굽는다 ⓕ 무경로 `git add -A` 유입 = sparse-checkout 잡이 **cone 밖 1만 파일을 삭제 커밋**. 킬스위치 = 레포 변수 `LY_STT_ENGINE=whisper`(엔진·모델 준비 동시 원복) · 비용 캡 = `LY_STT_MAX_SEC`(기본 3600 — 종전엔 whisper가 느려서 4시간 영상이 실패=$0이었는데 Scribe는 **성공한다** = $0.88~$2.20/런). 6축 하드(정적·면책표 없음) · 실증 = 킬테스트 7종 전건 검출{조립예외·무경로add·env개명·캐시태그개명·prefetch 2곳·폴백함수} + 위양성 0. ⚠ **판정은 정확 토큰으로** — 첫 판이 bare substring이라 `ELEVENLABS_API_KEY_X` 개명이 그대로 통과했다(킬테스트 자기적발 → `이름:` 형태로 교체).
- **[편집 생성 = 자동 가림·키잉·크로마키 = `check_edit_track_chain`(운영자 260808 "모자이크 누르고 옵션 선택한 다음에 생성 누르면 트래킹해서 모자이크까지 자동으로")]** 영상 편집 「추가 옵션」(모자이크·트래킹 핀셋·키잉·크로마키)이 **발사 옵션이 아니었다**(edit.html 650행 주석 "발사 옵션 아님") — 토글이 켜지고 세부 게이지까지 그려지는데 페이로드엔 안 실려서 **생성을 눌러도 아무 일도 안 일어났고 오류도 안 떴다**(= 운영자 눈이 유일한 검출기). 유일한 처리 경로는 [트래킹] 버튼 → `track.html`인데 거기선 ⓐ 영상을 **다시 첨부**하고 ⓑ 분석을 기다렸다가 ⓒ 인물을 손으로 고르고 ⓓ 또 렌더를 눌러야 했다(= "목업만 뜨고 진행 불가"의 실체 = 편집에서 켠 옵션이 그 화면에선 아무 일도 안 하는 것처럼 보인다). 봉합 = 생성 한 번에 [컴포즈 → 인물 분석 → 전대상 자동 적용 → 산출 교체]. 정본 = `.github/scripts/edit_track.py` · 체인 = 뷰어 `xtrOpts()` → `api/edit.js` 화이트리스트 → 러너 스크립트 → `edit-make.yml` 2스텝. ⚠ **컴포즈 뒤에 도는 게 계약** = 크롭·해상도가 바뀐 **완성본 좌표**로 분석해야 박스가 맞는다(원본 기준 분석은 크롭 시 어긋난다). ⚠ 대상 = **검출 전원 자동**(가림은 빠뜨리는 쪽이 사고 = track_render 원칙 ③ 과잉 커버 편향 계승 · 개별 선택·수동 지정이 필요하면 [트래킹] 버튼이 **선택 경로**로 잔존). ⚠ 체인 = 모자이크 → 핀셋 → (키잉|실루엣|크로마키 = 알파 산출이라 종점) · 알파는 `preview.webm` 동반(MOV는 브라우저 재생 불가). ⚠ **전면 fail-soft** = 자동 가림이 실패해도 편집 산출물은 그대로 살아 있다(rc 항상 0 · `video.json` url 무접촉 + `xtr_note` 기록). ⚠ **단위 변환은 러너 1곳** = 폼은 %(size 115 · cksim 18), 배율·비율 환산은 `edit_track` 안에서만 — **실측 사고**가 정확히 이 축이었다: `api/track.js`가 뷰어의 1~50 강도를 `0.01~0.5`로 클램프해 **전 구간이 0.5로 붙어** 크로마키가 화면을 통째로 투명하게 지웠다(실호출 실측 = 0.5 → 알파 전면 0 = 빈 영상 / 0.18 → 배경만 정확히 제거) = 슬라이더가 죽은 정도가 아니라 **크로마키 자체가 라이브에서 늘 실패**. 6축 하드(정적 · 렌더·LLM·네트워크 0 · 면책표 없이 하드 0) = ①뷰어 송신 ②api 화이트리스트+발사 게이트 유효 인정 ③러너 골격 ④워크플로 배선 ⑤`ly_burn` 최종 경로 도장(`/tmp/ly_final_path.txt` — 음량 통일 분기로 경로가 갈리는 축을 확정) ⑥크로마 단위 회귀 차단. 실증(로컬 실호출) = 모자이크 3.8s·핀셋 8.3s·크로마키 23s 정상 산출 + `edit_track` E2E 10s(분석 4명 → 자동 전대상 → `ly_out` 교체).
- **[핀셋 이름표 미리보기 = 폐지(운영자 260810 "트래킹 미리보기 없어도됨")]** 뷰어 `viewer/edit.html`의 이름표 미리보기(`pinPreview`·`pinPvDraw`·`.pinpv`)와 그 규격 사본(`PIN_SPEC`·`PIN_PALETTE`), 짝 게이트 `check_pinset_parity`가 **한 커밋에 같이** 나갔다. ⚠ 셋은 한 몸이다 — 게이트의 존재 이유가 「뷰어 사본이 러너와 같은 값인가」였으므로 미리보기가 없으면 대조할 짝이 없고, 반대로 **사본만 남기면 그날부터 조용히 갈린다**. 렌더 규격의 유일 정본 = 러너 `apps/track/track_render.py` `PIN_SPEC`·`PALETTE`(뷰어는 이제 그 값을 아예 안 든다 = 갈릴 여지 0) · `check_design` baseline도 같이 **회수**(edit accent_hex 1→0 · legacy_green 1→0 = §🎨 「raw 줄면 baseline도 낮춰」 래칫 — 남겨두면 같은 raw가 조용히 재유입된다). 되살릴 땐 **두 함수·상수·게이트를 같은 커밋에** 되돌린다(한쪽만 되살아나면 미리보기가 거짓말을 한다). 아래는 그 계약의 역사 기록이다. (구) **[핀셋 이름표 = 미리보기 = 결과 = `check_pinset_parity`(운영자 260809 "아이디어 배선")]** 편집 렌더는 한 번에 수십 분이라 글자 크기·겹침을 **생성 전에** 보라고 뷰어 `viewer/edit.html`에 캔버스 미리보기(`pinPreview`/`pinPvDraw`)를 붙였다. ⚠ **그 미리보기의 존재 이유는 「결과와 같다」는 것 하나뿐**이라 산식이 두 벌로 살면 한쪽만 고쳐도 조용히 거짓말이 되고, 운영자는 그걸 믿고 렌더를 돌렸다가 다른 결과를 받는다 = 없느니만 못하다. → 규격 SSOT 2쌍을 하드로 묶는다 = `apps/track/track_render.py` `PIN_SPEC`(9키 = 활자 산식·축소배율·스트로크·머리 위 간격 · **260810 「글자만+검정 스트로크」 정본** = 배경·브래킷·리더선 0) ↔ `viewer/edit.html` `PIN_SPEC` · 러너 `PALETTE` ↔ 뷰어 `PIN_PALETTE`(**순서까지** = 자동 배정이 `(pid-1) % len` 이라 순서가 곧 색). ⚠ 실증 = 이 배선 **첫 판이 정확히 그 사고를 냈다**(뷰어 팔레트를 손으로 적다가 러너 12색과 순서·값이 갈린 8색 임의 목록 · 육안으론 「색이 좀 다르네」로만 보인다) → 게이트가 잡았다. ⚠ 기존 게이트는 전부 다른 축 — `check_edit_track_chain` = 옵션이 **러너까지 도달하는가**(체인 생존) · `smoke_*` = 화면 **렌더** → 「미리보기 산식이 실제 렌더와 같은 값인가」는 축 자체가 없었다. 정적 · 렌더·LLM·네트워크 0 · **면책표 없이 하드 0**. ⚠ 뷰어 팔레트 hex는 `check_design` baseline에 **사유와 함께** 등재(accent_hex 1·legacy_green 1) — 이건 UI 색이 아니라 **러너가 영상에 굽는 산출물 색의 사본**이라 토큰화하면 러너와 갈린다(thumb `legacy_green:1` 「콘텐츠 산출물 색 미러 = UI 재유입 아님」 선례 동축).
- **[요약 문체 회귀 래칫 = `check_style_ratchet`(WARN·비차단 · 운영자 260810 "ㄱㄱ" · 평의회 8인 6번 설계안)]** ⚠ 신설 사유 = **이 레포 게이트 105개 중 「요약 문체」를 보는 축이 0개였다.** 그래서 260801 규칙 신설도·260810 제거도·260704 effort 하향도 전부 **무검증으로 라이브에 나갔다**(260704 회귀 = IG 중앙 617→537.5 · Thread 429→359.5 = 21일간 아무도 안 울렸고 `digest_guard.py:103` 주석에만 남았다 · 실측 평의회 7). breaking 룰북엔 `check_rubric_regress`, grade 룰북엔 `check_grade_regress`가 있는데 **운영자가 실제로 불만을 말한 축**엔 없었다 = 39일간 20번 고치는 동안 운영자 눈이 유일한 검출기. ⚠ **정답지를 「좋은 글」이 아니라 「실패 모드」로 잡는 게 실효 조건** — 「이 리드가 좋은가」는 답이 여럿이라 정답 라벨이 원리적으로 불가능하지만 「이 리드가 날짜로 여는가」는 답이 하나다(260810 개정이 겨눈 축 전부가 그 형태). 4축 = ① `lead_date`(IG 🔎 리드가 날짜·시각으로 열림) ② `gloss`(용어 풀이 문장 「~는 …하는 제도다」) ③ `preach`(마지막 📍이 훈계로 닫힘) ④ **`claim_p90`(자수 표기 vs 실측 오차 p90 · 평의회 8 발견 = 모델 자가 표기가 **전건 한 방향**으로 어긋나 「N/800」이 규격 준수의 증거가 아니라 **미달을 가리는 표지**가 된다 · 실측 최대 290자)**. ⚠ **하드 금지** = 문체는 오탐이 구조적이다(속보·재난은 날짜 리드가 정본 예외 · 평의회 3 실측 = 재난 층 43.9% vs 그 외 11.5%) → 하드면 레포가 언다 = 래칫이 유일한 정합 형태(`check_gate_hits`·`check_component_lock` 선례 · 판정은 사람이 하고 게이트는 **보이게만** 한다). 표본 = `queue/*.md` 최근 60건 · 20건 미만이면 판정 유예 · 면책 `_STYLE_BASE` 260810 실측 스냅샷{lead_date 35.0 · gloss 1.7 · preach 0.0 · claim_p90 136} · **줄면 그 자리에서 낮춰라**(남겨두면 같은 회귀가 조용히 재통과 = INK_BASE 교훈) · 정적 · 렌더·LLM·네트워크 0.
- **[콘티 그록 레인 = 5층 생존 = `check_grok_sb_chain`(운영자 260811 「진행해보자」)]** 영상은 종전에 콘티가 **글만 뽑고** 운영자가 클링 사이트로 건너가 손으로 만들었다(`functions/api/k.js` 머리 = 「Kling 복붙 프롬프트」). 260811 실호출로 **X Premium+ 구독 자격만으로 그록 그림·영상이 둘 다 열린다**는 게 확정돼(영상 = H.264 1280×720·24fps·10.04초·**AAC** 스테레오 = 편집 툴 바로 물림 · 그림 = JPEG 1280×720 · 종량제 키 0) 건너가는 단계 자체를 없앴다. ⚠⚠ **「종량제 키 0」은 「공짜」가 아니다(260811 2차 실측 정정)** — 첫 12컷 실호출(run 31536019555)에서 xAI 응답이 **청구액 $2.88**(컷당 $0.24)을 실어 보냈다. 구 서술 「추가 과금 0」은 **우리가 별도 API 키를 안 만든다**는 뜻이었는데 「돈이 안 나간다」로 읽히던 자리라 이 문장으로 갈음한다. 실제 결제 반영 여부는 xAI 결제 화면에서만 확인 가능 = **레포 안에서는 미확인**. → 그래서 금액을 **화면에 띄운다**(운영자 260811 「실제 제작할때도 표기해줫으면함」 · 뷰어 콘티 결과부가 `video.json.cost_usd` 를 「청구 $X (컷당 $Y)」로 · 추정 아니라 응답 실값 = `usage.cost_in_usd_ticks ÷ 1e10`). 촬영 3레인 개편 = **grok(신설·기본)** · seedance(힉스필드 MCP · 준비 중) · motion(사내 렌더) — **kling 제거**(수동 복붙 레인이라 「여기서 끝까지 간다」는 콘티 성격과 애초에 어긋났고 종량제 미사용 확정으로 남을 이유 소멸). 흐름 = ① 운영자가 상황을 말한다(글·사진) ② **감독**(Fable 5/Opus 5/GPT 5.6 Sol)이 콘티를 짠다 ③ `grok_sb_video.py` 가 **컷마다** [Gemini 그림 → 그 그림을 **첫 장면**으로 그록 영상] ④ `sb_out/<id>/` 착지. ⚠ **컷마다 그림을 새로 굽는 이유** = 기존 「🖼 레퍼런스」 절 그림은 인물·배경·키비주얼 몇 장이지 컷 그림이 아니다(sb-make.md 규약) — 이미지→영상은 **컷의 첫 프레임**을 요구하므로 컷 수만큼 필요하고, 그 그림들이 곧 「스토리보드 컷」이기도 하다. ⚠⚠ **발사 단위 = 「영상 1편 = 10초」이고 컷은 그 안의 카메라 전환이다(운영자 260812 확정 · 구 서술 전면 개정)** — 「10초가 1컷이라기보다는 10초가 하나의 영상이고, 그 안에 다채로운 컷을 감독 프롬프트에 따라 구성할 수 있다. 2초마다 구도를 바꿔 10초 안에 2초컷이 5개 들어가는 느낌을 만들었다면 한 번에 5컷일 수 있다. 단일 영상으로 표현하는 게 왜곡이 없다.」 · 총 길이 = **10 × 편수**(30s=3편·60s=6편 · 뷰어 길이 자 = 10초 눈금 6칸) · 컷 길이는 감독 재량(1~10초)이고 `group_shots()` 가 컷 초를 더해 10초씩 묶어 **묶음 하나 = 호출 하나** · 묶음 안 컷은 프롬프트에 **시각을 붙여 시간순**(`0-3s: … 3-5s: …` · 컷 1개뿐인 묶음엔 시각 미부착 = 한 호흡짜리에 눈금을 치면 오히려 끊어 그린다) · 콘티는 **컷 초 합이 10의 배수로 딱 떨어지게** 짠다(안 맞으면 마지막 편만 늘어진다). ⚠ **두 번 틀렸던 축이다** — 구판 ⓐ 「컷 1개 = 호출 1개」는 종이 콘티 칸 수(15초=12칸)를 그대로 받아 **1.2초 조각 12개**를 냈고, 이를 고치려던 ⓑ 「컷 자체를 10초 고정」은 반대편으로 치우쳐 **한 편 안의 전환을 아예 불가능**하게 만들었다. 둘 다 컷과 영상을 한 축으로 본 같은 오류의 양끝 · 게이트도 「고정 상수 금지」에서 「묶기 강제」로 **뒤집혔다**. ⚠ **MOTION 필드**(영어 전용·grok 한정) = 실측에서 영상 프롬프트가 한국어로 나가던 결함 봉합 — ACTION 은 운영자가 화면에서 읽는 한국어이고 그록 프롬프트는 영어가 안전한데(정본 §4-4) 한 필드가 겸하면 화면이 영어가 되거나 프롬프트가 한국어가 된다 → 축을 가른다(없으면 ACTION 폴백 = fail-soft). ⚠ 프롬프트 조립 = `prompts/grok-make.md` 정본 그대로 — 영상 문장에서 **장면 묘사를 일부러 뺀다**(§3-1 첫 프레임이 구도·조명·색을 이미 쥐어 재묘사는 강화가 아니라 **희석**) · 동작→카메라→소리 순(§0-② 시간순) · **대사는 컷이 적었어도 안 싣는다**(이미지→영상은 립싱크가 구조적 불리 §4-3 + 실존 인물 축 위험 동시 차단 §7-3①). ⚠ **소리 끄기의 확실한 수단은 프롬프트가 아니라 산출 트랙 제거**(`ffmpeg -an`) — 그록 영상 API 에 오디오 끄기 값이 **없고** xAI 스스로 「무음 프롬프트가 실패할 수 있다」고 인정했다(정본 §4-1) → 프롬프트 4수법은 보조. 판정 5축 = ① 뷰어 칩+표시명 ② 서버 화이트리스트+소리 파싱·전달 ③ 워크플로 스텝·입력·자격 시크릿·소리 전달 ④ 러너 골격(컷 파서·프롬프트 조립·소리 제거 **실호출**·자격 갱신 경유·첫 장면 주입·콘티 길이 사용) + 고정 길이 회귀 차단 ⑤ 감독 MOTION 규약. ⚠ 신설 사유 = **한 층만 빠져도 조용히 죽는다** — 칩은 눌리고 콘티도 정상으로 나오는데 영상만 안 생긴다(insta-thumb-miss·brk_misfire·nm-jobs 동축). 기존 게이트는 전부 다른 축(`check_model_names` = 표시명 문자 · `check_k_models` = k 레인 값 3면 · `smoke_*` = 화면 렌더). 정적 · 렌더·LLM·네트워크 0 · **면책표 없이 하드 0**. ⚠ **첫 실호출 실측(260811 · run 31536019555 · id 260811-grok-e2e-02) = 12/12컷 성공**. 산출 = H.264 1280×720·24fps·AAC 48kHz 스테레오 · **길이가 콘티 표기와 1:1**(1s→1.04s · 2s→2.04s · 이어붙여 18.5초) · 소리 실렸다(총성 컷 평균 −18.4dB·최대 −2.4dB) · 인물·의상·세계관 12컷 유지. **콘티 정합 = 8/12** — 어긋난 4컷은 전부 **콘티 규약 결손**이었지 그록 성능이 아니었다: ⓐ 컷7 **주체 뒤바뀜**(남자 응사 지시 → 여자 사격 · MOTION 이 인물을 `He`·`She` 로만 부르는데 인물 참조가 둘이라 대명사가 갈 곳을 모델이 골랐다) ⓑ 컷1 무인 전경 지시인데 두 인물 등장(참조에 인물이 실려 있으면 기본값이 「넣는다」) ⓒ 컷6·7 「기둥 뒤 엄폐」인데 **배경 참조에 그 세트 조각이 없었다**. → 봉합 = **정체 문장을 프롬프트 맨 앞에**(`ref_ids` = 참조 슬롯마다 「<IMAGE_0> shows …」 · 출처 = board.md 참조 절 영어 원문 · k_refgen 과 같은 파서·같은 순서라 슬롯 어긋남 0) + `prompts/sb-make.md` **MOTION 3계약**(대명사 금지·무인 컷 명시·세트 조각 동반). ⚠ **자리가 계약이다** = 인물표는 **동작 문장보다 앞**이어야 한다(이미지 엔진이 앞에서 뒤로 읽어서 뒤에 두면 `He` 를 읽는 시점에 가리킬 대상이 없다 · 게이트가 순서까지 검사). ⚠ **참조 장수 = 2 기본·3 이유 있을 때·4 금지**(운영자 260811 「3개 초과해서 만들면 실패고, 2개가 베스트야」) — 그록 API 기술 한도 7 은 우리 계약이 아니다(`REF_CAP=3`·`REF_BEST=2` · 3장 사유를 `video.json.ref_reason` 에 기계 판정으로 기록 = 인물 수 대조 · 이번 판 = 인물 2 + 장소 1 = 정당). ⚠ **콘티 시트는 이 상한 밖의 한 장이다(운영자 260814 「콘티시트를 왜 참조를 안해? 그것도 참조로 넣어」 · 게이트 `check_grok_sb_chain` ⑥-b)** — 인물·배경 참조는 「이 얼굴·이 장소를 유지하라」는 **잠금**이고 시트는 「이 편이 어떤 순서·구도로 흐르는가」를 담은 **설계도**라 성격이 다르다 → `sheet_slot()` 이 `ref_slots()`(그림 상한 컷) **뒤에** 합류시켜 인물·장소 슬롯을 안 밀어낸다(편마다 실리는 장수 = 인물 1 + 배경 1 + 시트 1 = **3장 기본**) · 시간대(밤·낮)로 안 갈린다(`bg` 거짓 = 모든 편에 실린다) · 정체 문장 `SHEET_CLAUSE` 가 **「그리지 마라」를 같이 말한다**(칸선·글자가 화면에 옮겨 그려지면 그게 사고 · 실측 근거 = 여러 칸 한 장은 구역을 크게 가르면 읽히지만 빽빽한 작은 칸은 정보가 약해진다) · 끄기 = `SB_SHEET_REF=0`. ⚠ **참조 해상도는 뽑는 화질과 따로 간다** — 시댄스 계열 문서 실측 = 참조 규격 300~6000px·비율 0.4~2.5·30MB·그림 9장까지이고 **1080p 언저리가 권장, 1080→4K 는 결과가 눈에 띄게 안 좋아진다** → 참조 그림은 1K 로 굽는 현행이 맞고(실측 768×1376), 2K 는 사람이 보는 시트 한 장뿐이다. ⚠ **길이 상한** = **15초 실측 확정**(260812 · id `260812-len15-probe` = 요청 15 → 산출 15.04s · 720p · 24fps · AAC · 청구 $2.12) — 정본 `grok-make.md` 의 구 서술 「15초·1080p는 슈퍼그록 승급 시 열린다」는 **절반 반증**됐다(1080p 만 승급 축으로 잔존 · 참조 모드는 720p 상한이라 코드가 막고 있고 참조 없이 쏘는 경로는 미측정). 편 수 상한 = `GROK_SB_CUT_MAX`(기본 12) → 현재 최대 120초. ⚠ **과금 = 호출당 $0.03 + 초당 $0.14**(실측 4지점 = 1·2·3·15초 전건 정합 · 구 「미확인」 서술 회수) → 값을 정하는 건 **호출 수가 아니라 총 길이**다(30초를 3발로 쏘든 12발로 쏘든 차이 $0.27) · 30초 실사용 = 그록 3발 $4.29 + 제미나이 참조 2~3장 + 시트 1장 ≈ **$4.5**. ⚠ **콘티 시트가 조용히 0장이었다** = 첫 실호출에서 시트 스텝이 1초 만에 끝났는데(OPENAI_API_KEY 미등록 = 통째 미시도) 잡은 초록이었다 → 없는 열쇠를 기다리는 층은 죽은 층이라 **제미나이 2순위 폴백**(GPT Image 1순위 유지 · 워크플로가 `GEMINI_API_KEY` 를 안 넘기면 폴백이 배선만 있고 못 도니 그 축도 게이트). ⚠ 미완 = 화면 소리 스위치 · 결과 레일 착지(`nmRail.add`) · **정체 문장·MOTION 3계약의 효과는 미측정**(다음 실호출이 잰다 = 재검증에 컷 수만큼 값이 또 나가므로 운영자 판단 대기 · 칩 힌트 「영상 배선 중」이 이 상태를 말한다).
- **[유튜브 쿠키 = 알림이 「갈아야 할 칸」을 맞게 말한다 = `check_yt_cookie_slot_name`(260812 실사고 봉합)]** 쿠키 슬롯은 2칸인데 **번호와 저장소 칸 이름이 반대로 배선**돼 있었다 — 실제 = `YT_COOKIES`(1번) ← `YT_T2_COOKIES` / `YT_COOKIES_2`(2번) ← `YT_T_COOKIES`(전 워크플로 10종 동일)인데, 감시기 `yt_cookie_health.py` 알림 문구만 칸 이름을 **손으로 적어** `1번→YT_T_COOKIES · 2번→YT_T2_COOKIES` 로 **정확히 반대**를 지시했다. ⚠ 실측 260812 11:24 = 알림은 「2번 죽음 → **YT_T2_COOKIES** 를 갈아라」인데 같은 시각 받기 레일 진단(`ytdlp_try.sh`)은 「**YT_T2_COOKIES = 살아있음** · YT_T_COOKIES = 죽음」 — **두 시스템이 같은 상태를 정반대로 말했다.** 비용 = 운영자가 알림대로 **살아있는 칸을 갈고** 죽은 칸은 그대로 둬서, 새 계정 쿠키를 넣어도 「2번 죽음」 경고가 영영 안 꺼진다(실제로 그 상태였다 = 원장 08-12 06:05·11:24 연속 동일). ⚠ 신설 사유 = **운영자 260812 「대명사 쓰지 말고 명시」 지시가 받기 레일 8종에만 적용되고 감시기는 안 따라왔는데 아무 게이트도 안 울렸다**(`check_seal_completeness` 가 겨눈 「같은 병의 형제」 축인데 그쪽은 WARN이라 못 막는다). 기존 게이트는 전부 다른 축 — `check_paths` = 경로 실존 · `check_workflow_yaml` = 문법 · `smoke_*` = 화면 렌더 → 「알림이 **맞는 칸 이름**을 말하는가」는 축 자체가 없었고, 증상이 「갈아도 경고가 안 꺼짐」뿐이라 운영자 눈이 유일한 검출기였다(insta-thumb-miss·brk_misfire 동축). 봉합 = 이름을 코드에 안 적고 **워크플로가 준 `<슬롯>_NAME` 을 그대로 쓴다**(`kan()` · 문법 정본 = `ytdlp_try.sh` 계승 · 창작 0) → 갈릴 여지가 구조적으로 소멸. 판정 3축(정적 · 렌더·LLM·네트워크 0 · **면책표 없이 하드 0**) = ① `<슬롯>_NAME` 이 있으면 값 == 그 슬롯이 받는 secrets 이름 ② 알림 표면(`_YTCK_NAME_REQ`)은 이름 보유 필수(빠지면 폴백이 env 이름 `YT_COOKIES` 로 말해 운영자가 못 찾는다) ③ 그 표면 스크립트 **코드부**에 시크릿 이름 리터럴 0(주석 제외 = 사고 기록의 거처 · 손으로 적는 순간 재발 = 진범 그 자체). ⚠ **쿠키가 필요한 곳 = 「목록 수집」이 아니라 유튜브에서 영상·자막·메타를 받아오는 10레일**(vidl 받기 · nb 미디어요약 · ly 자막 · edit 편집 · conv 변환 · track 트래킹 · framethumb · news-ask 링크전사 + 감시 2종) — 전부 yt-dlp `--cookies`. 구독·트렌드 목록 수집은 유튜브 쿠키를 **안 쓴다**. ⚠ **러너 IP가 유튜브에 봇으로 찍힌 상태**라 쿠키 없이는 대부분 막힌다(260811 실측 run 31490030593 = 무쿠키 4회 전건 `Sign in to confirm you're not a bot`) = 쿠키는 선택이 아니라 사실상 필수. ⚠ **남는 별개 축(미해결)** = 새로 뽑은 쿠키가 **형식은 완벽한데**(로그인 쿠키 10/10 · 만료 2027-01-24) 유튜브가 세션을 거부하고, 갈아 넣으면 살아났다가 **7~12시간 만에 다시 죽는다**(원장 실측 = 08-10 23:31 살아남→08-11 11:07 죽음 · 08-11 22:31→08-12 05:34). 이건 이 게이트가 고치는 축이 아니다(알림 문구 축) — 원인 미확인.
- **[저작권·안내문 합성 = 산출물 크기로 캔버스를 정한다 = `check_thumb_merge_canvas`(260812 실사고 봉합)]** 카드에 저작권·안내문을 얹는 합성이 **라이브에서 통째로 안 되고 있었다**. 진범 = 합성 레이어 포맷을 `params.fmt` 로 정한 것 — 노뮤트 릴스 오버레이는 프론트가 `fmt` 를 안 보내고 서버(`functions/api/thumb.js:80` `p.fmt === 'reels' ? 'reels' : 'post'`)가 **'post' 로 채워** 보내는데, 렌더는 `thumb-make.yml` 214행이 `generate('reels')` **고정**이라 산출물이 9:16(2160×3840)으로 나온다 → 4:5(2160×2700) 레이어를 얹으려다 크기가 어긋나 `continue` 로 **조용히 건너뛴다**(러너는 success · 화면 증상은 「그냥 저작권이 안 보임」뿐). ⚠ **260728 에 진짜예요(jinjja) 축을 고치려 넣은 「params.fmt 우선」이 노뮤트 축을 정확히 반대로 깨뜨렸다** — 그 커밋 주석은 「노뮤트는 워크플로가 종전대로 reels 고정 = 무영향」이라 단언했는데 **렌더 경로만 보고 합성 경로를 안 봤다**. ⚠ 짝 결함 = **안내문이 저작권에 종속돼 있었다** — `api/thumb.js` 가 `guide` 를 copyright 조건 **안쪽**에서 읽어, 저작권 OFF(또는 연도 빈값)면 프론트가 보낸 안내문을 통째로 버렸다(운영자 260712 「서로의 온오프 관계없이」 독립 계약 위반 · 실측 = 그 조건으로 제작한 4건 전부 유실). 봉합 = ① 레이어 캔버스를 **산출물 실측 크기에서 역산**(`SPECS` 대조 · 같은 크기는 1회만 렌더 후 재사용 = 포맷 추측 소멸) ② `guide` 수신을 copyright 블록 **밖으로** 꺼냄(저작권 없으면 안내문 첫 줄이 그 자리부터 스택). ⚠ 신설 사유(게이트) = 기존 게이트는 전부 다른 축 — `check_image_format` = 포맷·품질 · `check_thumb_redo_append` = 수정 누적 · `smoke_*` = 화면 렌더 → 「만든 레이어가 산출물에 **실제로 얹히는가**」는 축 자체가 없었고, 러너가 초록으로 끝나서 운영자 눈이 유일한 검출기였다(insta-thumb-miss·brk_misfire 동축). 판정 2축(정적 · 렌더·LLM·네트워크 0 · **면책표 없이 하드 0**) = ① 합성 블록이 `SPECS` 역산 보유 + 구판 `fmtc = params.get('fmt')` 부활 차단 ② `guide` 수신 줄이 copyright 조건보다 안쪽 들여쓰기면 차단. 실증 = 실산출물 화소 실측(사고분 상단 화소 **0** — 오늘 제작 3건 전건) + 봉합본 3조건 실행{저작권만 16352 · 저작권+안내문 30583 · 안내문만 9435 화소} + API 원문 4조건 실행{저작권 OFF·연도 빈값에서도 guide 생존}.
- **[요약 제목 = 기자가 뽑은 원문이 화면까지 온다 = `check_orig_title_restore`(260813 실사고 봉합 · 운영자 «원래 기사의 제목이 안따라오거든 · 요약된 박스 제목만 보면 무슨 내용을 말하는지를 모름»)]** 요약 카드의 `title` 은 **기사 제목 원문 전용**이고 후킹 헤드는 본문 `# 제목` 몫이다(정본 = `prompts/news-analysis.md` 38행 · 260703 3층 구분 = 원문 title · 후킹 본문 헤드 · 번역 title_ko). ⚠ 실사고 = 260811 밤부터 산출이 이 계약을 깨고 **후킹 헤드를 `title` 에 그대로 넣었다** — 실측 = 260811 1건 · 260812 **8/13** · 260813 2/2 · 그 전 600여 건은 **0건**(즉 계약은 6주 넘게 지켜지다 이틀 만에 무너졌다). 수집기는 원문 제목을 정상 전달했다(`viewer/candidates.json` 실측 = 「정부, 폭염에 수산물 최대 50% 할인·재난지원금 332억원 투입」 ↔ 산출 title 「바다는 식힐 수 없어서, 죽기 전에 팔라고 한다」 / 「‘하얀거탑’ ‘밀회’ 연출… 안판석 PD 투병 중 별세」 ↔ 「10월에 나올 그 드라마는 이미 다 만들어져 있다」) = **원료는 프롬프트까지 도달했고 산출에서만 덮였다**(코드 변경 없는 모델 준수 드리프트 · 프롬프트·지침 해시는 260810 이후 불변 = `guidelines_version` 실측 `96d67a2bf383`·`d83e87835bd7` 고정). ⚠ **비용이 큰 이유 = 화면에서 제목이 한 줄 사라진다** — 뷰어 모달은 원문 제목 줄(`.md-srct`)을 「`title` ≠ H1」일 때만 그린다(`viewer/index.html` 4948행 · 옛 분석분의 제목 2줄 중복을 지우려고 260805 「기자들이 뽑은 것이라 퀄리티가 제법 있어」와 함께 넣은 **정당한** 가드) → 두 값이 같아지는 순간 그 가드가 **정상 동작해** 기자 제목 줄이 통째로 안 그려지고, 남는 건 추상 헤드 하나뿐이라 요약 상자만 봐서는 무슨 사건인지 알 수 없다. 봉합 3층 = ① **결정론 복원 도장** `.github/scripts/restore_orig_title.py`(analyze.sh 발행시각 도장과 같은 문법 = 스크립트가 박는다 = 모델 변덕 무관 · 위반 서명일 때만 손댄다{`title` 빈값 ∨ 선두 이모지·공백 무시 대조로 본문 헤드와 동일} · 원료 = 픽이 심은 `# title:` 수집기 메타 · 무주입 6경로 = 종전 동작) ② **짝 검출기 구멍 봉합** `shared/digest_guard.py`(구판은 `h1 == title` **원문자 완전일치**라 본문 헤드가 「# 🐟 …」처럼 이모지로 열리면 = IG 헤드 골격의 **정상 형태** = 전건 빠져나갔다 → 실측 위반 11건 경고 **0건** = 탐지기가 있는데 죽어 있었다 · 술어를 뷰어 `_tkey`·build-viewer `stripLeadEmoji` 와 **같은 자**로 교체) ③ 프롬프트 계약 문구 강화(동일 문장 금지 + 「수집기 메타 제목 줄이 정답」 + 매체 태그 `[속보]`·`[단독]` 은 원문이면 그대로 = 태그 제거는 본문 헤드 규칙이지 이 필드 규칙이 아님). ⚠ 신설 사유(게이트) = 기존 게이트는 전부 다른 축이다 — `check_style_ratchet` = 본문 **문체**(리드 날짜·용어 풀이·훈계 착지) · `check_rubric_regress`·`check_grade_regress` = **판정** 룰북 · `smoke_*` = 화면 렌더 → 「**제목 필드가 제 몫을 하는가**」는 축 자체가 없었고, 화면은 멀쩡히 뜨는데(제목이 있긴 하다) 원문 줄만 조용히 빠지는 형태라 **운영자 눈이 유일한 검출기**였다(insta-thumb-miss·brk_misfire 동축). 판정 4축 · 정적 · 렌더·LLM·네트워크 0 · **면책표 없이 하드 0** = ① analyze.sh 실행줄 배선(+`title_hint` 가드 안) ② 복원기 술어 2종(이모지 무시 키 · 계약 준수 무주입) ③ digest_guard 이모지 무시 대조 + 구판 완전일치 부활 차단 ④ 프롬프트 계약 문구 실존. 실증 = 복원기 킬테스트 4종{위반 카드 복원 · 계약 준수 카드 **바이트 무변경** · 따옴표 포함 원문 이스케이프 · 원문 없음 무주입} + digest_guard 양방향{위반 11건 전건 검출 · 정상 5건 무경보}. ⚠ **남는 한계 2종** = ⓐ **요약요청(`ask.sh`) 경로는 결정론 복원이 안 붙는다** — 그 경로엔 수집기 메타가 없고 본문도 모델이 직접 열어서 스크립트가 쥔 원문 제목이 0이다(실측 위반 11건 중 5건이 이 경로) → 프롬프트 강화 + digest_guard 경고까지만 · 결정론 원료 확보는 별건 ⓑ **이미 나간 카드는 안 씻긴다**(queue = 기계산출물 · 새로 만드는 요약부터 적용 · 과거분은 뷰어 「다시 만들기」).**
- **[🚨 진행 중 장애(260814) = GitHub 이 계정 단위로 Actions 를 껐다 = 우회 레인 `scripts/phone_scrape.sh`]** ⚠ **다음 세션은 이 항목을 먼저 읽어라 — 지금 `gh workflow run`·`functions/api/*` 발사는 전부 실패한다.** 실측 = `POST /repos/muteno/nomute-editor/actions/workflows/scrape.yml/dispatches` → **`422 Actions has been disabled for this user.`** · 검색 API 는 같은 계정에 **`User flagged as spammy`** 응답(사용자 검색 결과에서도 `muteno` 소실) · 계정 `updated_at` **2026-08-13T17:06:48Z** = 마지막 런 `metrics-rollup` 17:05:21Z 직후 = **그 순간 워크플로 70종이 동시에 정지**(schedule·workflow_dispatch·push 트리거 전부 · 이후 7시간 런 0건 실측). ⚠ **외부 15분 메트로놈(cron-job.org)의 422 26연속·잡 자동 비활성은 증상이지 원인이 아니다** — 그 잡을 되살려도 dispatch 는 계속 422 다(첫 실패 17:15Z = 마지막 성공 17:00Z 다음 주기 = 시각 일치). **진짜 해제는 코드 축이 아니다 = GitHub 지원팀 이의제기(운영자 몫)** · 원인 추정 = 자동화 규모(런 누적 32,425 · 봇 커밋 일 수백 · 원격 브랜치 567 · 260813 16:09 `7034e1b` 단일 커밋 **12,696 파일**) = 미확인. ⚠ **살아 있는 것과 죽은 것을 갈라 둔다**(되는 척 금지) = ⓐ **git push 는 산다**(제재 후 phone-subs 커밋 17:31~00:02 전건 착지 실측) ⓑ **GitHub 읽기도 산다** — `raw.githubusercontent`·contents API 가 그대로 서빙(실측 = 라이브 `/api/candidates` 200) → **main 에 커밋만 착지하면 수집함은 화면에 뜬다**(빌드 우회 `functions/api/candidates.js`) ⓒ ~~**Cloudflare Pages 빌드는 같이 멈췄다**~~ **← 낡음(260814 13시 운영자 정정) = 배포는 맥이 대신 돈다.** ⚠ **다음 세션은 「액션이 막혔으니 배포도 멈췄다」고 말하지 마라** — 그건 맥 2선 레인을 모르는 서술이고, 실제로는 그 시각에도 11:57·12:34·13:16에 계속 배포되고 있었다(운영자 실측 · 그날 세션이 만든 커밋 4건은 이미 여러 번 화면에 구워졌다). **계약 = main 에 푸시만 하면 끝이다.** 대기 시간도 그날 줄었다 = 맥 1분 틱이 원격 main 머리를 보고 **코드 경로**(화면 파일·functions·배포 설정) 변경이면 같은 틱에 재배포 → 푸시에서 화면까지 **약 2분**(봇 데이터 커밋은 제외 = 1분마다 배포 낭비 차단 · 최종 실증 = 다음 실푸시 때 `job_tick.log` 의 「코드 푸시 감지」 줄 · 운영자 확신도 90% = 감지 로직만 실푸시 전). 아래 「⚠ 260814 10시 재실측」의 **화면 동결** 서술도 같이 낡았다. 구 실측은 취소선으로 남긴다(그때 있었던 일이라 지우지 않는다 = 실측 = 라이브 정적 `sns_subs_phone.json` 이 17:01 커밋에서 정지 · 이후 비-skip 커밋 8건 미배포) = **정적 `viewer/*.json` 을 읽는 표면은 낡은 채로 굳는다** → 라이브 도달은 `functions/api/*` 직독 경로를 가진 표면뿐(`check_coalesce_pair` 가 지키던 그 짝이 지금 유일한 생명선이다) ⓓ **죽은 것 = Actions 전부** = 수집·속보 판정·경중·자동분석·SNS 수집(insta·fb·sns-trends·social-scan)·제작 전 레인(카드·영상·이미지·음원)·watchdog(=**이 장애를 알릴 감시기 자신도 죽었다** = 알림 0 이 정상 신호가 아니다)·나이틀리 스모크. 우회 = `scripts/phone_scrape.sh`(폰·맥 crond 30분 · scrape.yml 수집 2스텝만의 인자 사본 · 착지 원장 `~/.nomute_phone_scrape_land` · phone_subs.sh 자가복구 블록 동작 사본) — **수집함만 되살린다**(판정·분석·제작은 Actions 축이라 구조적으로 불가). Actions 복구 시 체크리스트(260814 10시 개정) = ① 폰 crontab 에서 **phone_scrape 줄만** 삭제(phone_subs 줄은 상시 정본 = 유지 · 두 레인 겹쳐 돌아도 산출 파일이 같아 충돌 0) ② **cron-job.org `scraper_nomute` 잡 재활성**(422 26연속으로 자동 비활성됨 · 켜는 곳 = cron-job.org 대시보드 · Actions 해제 전에 켜면 또 422 적립 후 재비활성 = 해제 후에만). ⚠ **260814 10시 재실측 = ⓑ 「읽기도 산다」는 낡았다** — raw 익명 열람이 레포 전 파일 **404**(index.html 로 교차 확인 = 파일 문제 아님 · 스팸 플래그의 공개 숨김이 raw 까지 전파된 것으로 보임) → `/api/candidates` 가 3단 사다리 전멸로 빈 배열 `[]` 반환 → 뷰어는 `d.length` 게이트(260714 SPOF 봉합)가 정확히 동작해 **정적 폴백**(마지막 빌드 = 13일 17:01Z 스냅샷)으로 표시 중 = 수집함 화면이 **KST 260814 02:06 에 동결**(빈 화면 아님 · 옛 데이터 표시). 폰 수집이 main 에 착지해도 **화면 반영은 GitHub 해제 전까지 구조적으로 불가**(유실은 0 = main 누적 → 해제 순간 일괄 반영) · CF Functions 코드 수정도 배포 동결이라 라이브 도달 불가 = 전부 지원팀 축 하나로 수렴. ⚠ 비대칭 미확정 1건 = CF `GH_TOKEN` **쓰기는 산다**(260814 10:29 `thumb upload` contents PUT 착지 실측)인데 같은 토큰의 candidates 읽기 사다리는 죽어 있다 — contents GET 실패 원인(플래그의 읽기 차단 vs CF `cacheEverything` 가 4xx 를 캐시) 미확정 · 다음 세션이 해제 후 재실측할 것. ⚠ **우회 2탄 = PC 레인**(`scripts/pc_lane.sh` = 수집 + 속보 판정 + 경중 채점 · 윈도우 작업 스케줄러 5분 · 설치 `scripts/pc_setup.bat` · 감시창 `scripts/pc_watch.bat`) **+ 우회 3탄 = 클라우드 액션 서버 = `scripts/cloud_action.sh` · 게이트 `check_cloud_action_chain`(운영자 260814 Q1482~Q1487 «git 액션 대체 기능을 클라우드 폴더에 · 항시 · 윈도우 우선/맥 동반 · G: 는 변화할 수 있고 계정값 = ems1130g@gmail.com · 계정이 연결되면 그 pc 가 자동으로 운영 서버 · **노뮤트에디터를 돌리는 일괄 액션 서버 = 독립 제품(다른 설치물 무종속)**»)** = 구글 드라이브 「내 드라이브/action」 폴더가 우체통(설치 파일 2종 = `노뮤트_클라우드액션_설치.bat`/`.command` · **환경변수.txt = 키 착지 슬롯**{운영자 «키 값은 내가 따로 환경변수로 주면 저장해주셈» — 깃허브 비밀칸 열쇠 전체 목록이 주석으로 씨앗돼 있어 값만 채우면 다음 회차부터 주입} · 기기별 상태/logs 미러 = 어느 기기·폰 드라이브 앱에서도 상태 열람). 겉옷 `cloud_action.sh` 가 **매 회차 폴더를 재탐색**(윈도우 = 드라이브 글자 a~z 전수 스캔 = G: 고정 0 · 맥 = CloudStorage 계정 경로 우선 후 **클라우드 폴더 전수**{원드라이브·드롭박스·아이클라우드 포함 · 260814 운영자 「원드라이브에 있다」 대응} · 두 이름 「내 드라이브」/「My Drive」 · 다중 계정 = 계정.txt 대조)한 뒤 **몸통 pc_lane.sh 정본을 그대로 실행**(사본 0) — 설치는 **자기완결**(도구 winget·저장소 clone·클로드 로그인·**자기 5분 시계 `NomuteCloudAction`**{조용한 실행 vbs} 전부 설치 파일 소관 · 같은 컴퓨터에 옛 시계 `NomutePcLane` 가 있으면 **이 서버로 대체 = 제거**) · 맥 = **launchd 5분**(`com.nomute.cloudaction` · `StartInterval`+`RunAtLoad` = 로그인 시 자동 재장착 + **잠에서 깨면 놓친 회차 따라잡기** · 크론은 잠든 회차를 통째로 건너뛰어 「켜져 있으면 항상 돈다」 계약을 못 지킨다 = 운영자 260814 · 등록 실패 시 crontab 폴백 잔존). **열쇠 입력 = `노뮤트_열쇠입력.html`**(운영자 260814 «bat 나 html 로 — 저러면 오타가 많이 생겨서 안됨» — 한글 라벨 폼에 붙여넣고 저장 1번 = 환경변수.txt 완성 · **여러 줄 열쇠**{유튜브 쿠키·서비스 계정 문서}는 `_B64` 한 줄로 접어 저장하고 서버 본체가 읽을 때 도로 편다{접기·펴기 짝 = 게이트 강제} · 설치가 action 폴더에 자동 배포·갱신). 새 기기 편입 = 드라이브 계정 연결 + 설치 파일 더블클릭 **1회**(그 뒤 전자동 · 여러 대 동시 가동 = 산출 동일·rebase 정렬이라 안전, 판정 이중 발사만 시작 지터 0~40s 로 완화). 킬 = 윈도우 `schtasks /Delete /F /TN NomuteCloudAction` · 맥 crontab 줄 삭제.
- **[액션 대체 레인 = 시계 하나 안의 스테이지 러너 = `scripts/pc_lane.sh` · 게이트 `check_pc_lane_stages`(운영자 260814 «깃허브 액션 없이도 정상 가동 모든 웹앱 내 기능이 돌도록 하자»)]** 구판 레인은 수집·판정 2스텝뿐이라 「수집함만 늘고 나머지 전 축이 정지」였다 → **워크플로 13종의 주기를 주기 원장(`~/.nomute_lane_stage`)으로 한 시계 안에 재현**한다(시계 신설 0 = 운영자가 설치를 다시 안 해도 된다 «세팅 다 하면 웹앱만 만지고싶음» · launchd 는 앞 회차가 도는 동안 새 회차를 안 띄우므로 긴 회차 = 수집 슬롯 몇 개 건너뜀이고 폰 레인 30분이 백스톱 · 폭주 차단 = 회차 예산 `LANE_BUDGET` 1500s). 스테이지 = 수집(매 회차) · 화재 후속 추적(매 회차 = **폰 긴급 알림이 정적 화면과 무관하게 실제로 닿는 유일 축**) · 재난문자·실검·키워드 알림(15분 · 폰 레인과 겹치면 28분 신선도 게이트가 거른다) · 커뮤니티 급상승(30분) · 채널 수집·브리프(3시간) · 감시(30분) · 토큰 계측(2시간) · 쿠키 검진(12시간) · 속보·경중 판정(백로그 있을 때) · **요약 분석·요약요청·카드 프롬프트·썸네일**(맨 끝). 값 창작 0 = 명령·인자·env 전부 대응 워크플로 사본. ⚠ **착지 = `git_land.sh` 위임(사본 0)** — 260814 실측 사고 = 자체 4회 리베이스 루프가 폰 레인과 같은 `viewer/candidates.json` 을 물면 충돌로 죽고 abort→break→**그 회차 산출 전량 폐기**(운영자 실물 원장 `2026-08-14T21:35:41|push-fail|4회 소진`) · git_land 는 리베이스를 안 쓴다{스냅샷→fetch→reset --hard origin/main→재적층→push·6회} = 꼬임 자체가 불가 · 폰 레인 2종은 같은 병의 형제라 `rebase -q -X theirs` 로 같은 커밋에 봉합(하나만 고치면 나머지가 조용히 낡는다). ⚠⚠ **안 밀린 커밋 보호가 실효 조건** = git_land 의 `reset --hard` 는 **아직 원격에 안 올라간 로컬 커밋을 지운다** — analyze.sh 는 기사마다 스스로 커밋·푸시하므로(ANALYZE_LAND_EACH) 그 푸시가 한 번 실패한 상태에서 뒤 스테이지가 착지하면 **방금 만든 요약이 조용히 증발**한다 → 착지 전 선푸시, 그래도 안 밀렸으면 **이번 착지 포기**(다음 회차 재시도 = 유실 0) + **요약 스테이지는 파일 맨 끝**(자리가 계약). ⚠ **자동픽 기본 OFF**(`LANE_AUTOPICK=1` 로만) = 종전 동작 보존(15분 메트로놈도 analyze=false 였다) — 여기서 소비하는 건 **운영자가 앱에서 누른 픽**과 화재 추적 큐잉분이다. ⚠ **픽·요약요청이 사는 이유** = 그 둘만 Cloudflare 함수가 `pending/`·`asks/` 파일을 **main 에 직접 쓴다**(디스패치는 그 뒤) → 액션이 꺼져도 요청이 레포에 남아 이 레인이 집는다. 반대로 **제작 레인(카드 촬영·영상·음원·이미지 편집)은 구조적으로 불가** = 요청이 디스패치뿐이고 원본은 R2 직행이라 접수 시점에 사라진다. ⚠ **감시 폰 알림 기본 OFF**(`WATCHDOG_NOTIFY=0`) = 지금은 「배포 지연」이 구조적 참(CF 빌드 정지)이라 매 회차 사실인 경보가 울리는데 코드로 고칠 자리가 0이다(알림 조치주체 규약) · 메시지함 점등·원장 기록은 그대로 = 무증상화 아님 · 복구 후 되돌리기 = 환경변수.txt 에 `WATCHDOG_NOTIFY=1` 한 줄. ⚠ **화면 재배포는 죽어 있다** = 배포 훅 실호출이 «Unable to find a branch with the provided name»(260814 실측) = CF 의 GitHub 연결 단절 → **마지막 성공 빌드 08-13 17:02Z 로 정적 화면 동결**(실측 `articles.json` generated 그 시각·commit `14b09de4`) → 요약·카드는 main 에 쌓이고 **화면엔 해제 후 한꺼번에** 뜬다(유실 0) · 지금 라이브로 도달하는 건 `functions/api` 직독 표면뿐(수집함 `/api/candidates` 200 실측·트렌드·채널). 강제 = `check_pc_lane_stages` 5축 하드(착지 위임 · 형제 -X theirs · **착지 함수 `_gl` 몸통 안**의 안 밀린 커밋 보호{파일 어딘가의 같은 문자열로 판정하면 자가복구 블록이 면죄부 = 킬테스트 K3 실측} · 스테이지 11종 실행줄 · 요약 자리) · 정적 · 렌더·LLM·네트워크 0 · 면책표 없이 하드 0 · 실증 = 킬테스트 6종 전건 검출{형제 -X theirs 제거 · 스테이지 제거 · 스테이지 주석 처리 · 착지 위임 제거 · 보호 제거 · 요약 자리 위반} + 복원 위양성 0.
- **[비밀칸·변수 빈 칸 점검 = `.github/scripts/secret_coverage.py` · 레인 `secret-coverage.yml` · 게이트 `check_secret_coverage_chain`(운영자 260816 「응 해줘」 · 계정 이관 후속)]** 「코드가 쓰는 이름」 vs 「저장소에 실제로 등록된 이름」 전수 대조(실측 65종 = 비밀칸 34 · 변수 31). ⚠ 신설 사유 = **260816 계정 이관에서 인스타 접속 열쇠가 안 따라왔는데 그게 우연히 발견됐다** — 3시간마다 도는 레인이라 마침 그날 실패 메일이 왔고 운영자가 그 화면을 세션에 넘겨서다. 며칠에 한 번만 도는 레인(주간 집계·백필·프로브)의 열쇠가 빠졌다면 **그때 가서야 터진다 = 비어 있는 칸은 터지기 전까지 화면 증상이 0이다**(insta-thumb-miss·brk_misfire 동축). 기존 게이트는 전부 다른 축 — `check_workflow_yaml` = 문법 · `check_paths` = 경로 실존 · `account-selftest.yml` = **GH_TOKEN 한 개**의 읽기·쓰기 실측 → 「이 저장소가 쓰는 이름이 실제로 등록됐는가」는 축 자체가 없었다. ⚠ **「미등록 = 곧 결함」이 아니다** — 이 레포는 「시크릿 미등록 = no-op 스캐폴드」가 정상 관례라(insta_fetch·thumb_gen·fb_fetch) 전건을 위반으로 세면 위양성 폭발 → **폴백 유무로 2단**{㉠ 폴백 없음 = 빈 값이 그대로 흘러간다 = 먼저 볼 것 ㉡ `|| '기본값'` 보유 = 미등록이어도 돈다 = 참고}로 갈라 **보이게만** 하고 판정은 사람이 한다. ⚠ **값 무출력이 계약** = 등록분은 `toJSON(secrets)`로만 얻을 수 있는데(개별 참조는 이름을 미리 알아야 한다 = 자기순환) 스크립트는 **키 이름만** 뽑고 값은 즉시 버린다 — 이 한 줄이 무너지면 점검 도구 자신이 토큰 유출 경로가 된다. ⚠ **미주입과 빈 집합을 구분한다**(`registered()` → `None`) = 등록분을 못 받은 회차를 「전건 빠짐」으로 오보하면 그날로 이 레인은 믿을 수 없는 것이 된다. 강제 = 5축 하드(정본 골격 / **등록분 2종 다 주입**{한쪽만이면 그 종류가 조용히 미점검} / 실행줄 / **알림 착지 스텝**{없으면 러너에서 알림 증발 = 260816 insta 실사고 동축} / 값 무출력) · 정적 · 렌더·LLM·네트워크 0 · **면책표 없이 하드 0**. ⚠ **게이트 술어 함정(첫 실행 실측 봉합)** = 값 무출력 축을 「print 줄에 ALL_SECRETS 라는 글자가 있나」로 두면 안내 문구(`print('… 미주입(ALL_SECRETS/ALL_VARS)…')`)가 그대로 위반으로 잡힌다 = **이름을 말하는 것과 값을 찍는 것을 못 가르는 술어** → 문자열 리터럴을 지운 뒤 남은 코드에서 판정. 실증 = 스캐너 킬테스트 4종{미주입 no-op · 전건 등록 시 빠짐 0+clear · **실제 상황 재현**(IG_ACCESS_TOKEN·IG_USER_ID 만 빼면 그 둘을 쓰는 워크플로까지 정확 지목) · 폴백 보유분 노랑 강등} + 게이트 킬테스트 4종 전건 검출{변수 주입 제거 · 착지 제거 · 값 출력 줄 추가 · 복원 위양성 0}.
- 커밋 전 `python3 shared/check_refs.py` 필수(`.githooks/pre-commit`이 강제). UI 표면(뷰어 html) 변경 커밋 전 = `bash shared/smoke_all.sh` rc=0. ⚠ **새 게이트에 `[^{}\n]*`·`[^{}]*` 류 여는-괄호-앞 와일드카드 정규식을 쓰지 마라** — `viewer/index.html`이 1.85MB라 매 위치에서 되짚어 **한 줄이 게이트 전체 시간의 절반**을 먹는다(260804 실측 = `check_input_canon`의 `.nmin` 규칙 정규식 혼자 **17.9s** = 77게이트 총 37.5s의 48.9% · 리터럴 스캔 선행으로 치환해 **0.002s**·전건 동일 실증 → 총 **37.5s → 11.4s**, 체감 pre-commit 30.5s → 11.3s). 정본 문법 = `_nmin_rules()` — 리터럴 `find`로 후보를 O(매치수)로 줄이고 그 자리에서만 규칙 경계를 판정한다.
- **[최근 게시물 커버 = 6중 회수 · 운영자 260803 "이번 문제 안일어나게" → "빈칸을 최대한 없애는 방향"]** 채널 요약 「최근 게시물」 타일의 커버는 **한 소스에 걸지 않는다** — 인스타 Graph가 릴스 `thumbnail_url`을 무성 생략하는 사고가 260718(2/25) → 260803(2/12)로 재발했고, 실측상 그 2건은 미디어노드 재조회·인스타 **공개** 커버 경로(`/p/<code>/media/?size=l` → `null.jpg`)까지 전멸이라 IG 계열엔 자산 자체가 없었다. 체인(수집 = `.github/scripts/insta_fetch.py` · 산출 = `apps/insta/insta_signals.py` `_thumb_src` · 화면 = `viewer/index.html` `chThFail`) = ⓞ 미디어노드 재조회(260718) → ⓞ-b **인스타 공개 커버 경로** `/p/<code>/media/?size=l`(비인증·토큰 무관 · 결손건만 1콜 · 종착이 `null.jpg` 플레이스홀더면 버림 = 오탐 0 · 실측 260803: 정상 릴스 전건 회수) → ① `thumbnail_url` ② **페이스북 크로스포스트 `full_picture`**(같은 워크플로 `fb_fetch` 산출 `viewer/fb_data.json` 재사용 = API 콜·네트워크·LLM 0 · 매칭 = ⓐ 캡션 첫줄 정규화 프리픽스[≥12자] ∧ 게시시각 ±10분 ⓑ 실패 시 **시각 ±3분 유일 후보**[그 창에 FB 후보가 정확히 1건일 때만 = 유일성이 오매칭 봉인] — 노뮤트는 IG·FB 문구를 각각 다시 써서 캡션만으론 크로스포스트를 놓친다 · 실측 260803 = 캡션 매칭이 정답을 아는 4건과 **모순 0**, 2차 단독 회수 3건) ③ **마지막 성공 커버 원장** `apps/insta/data/thumb_cache.json`(미디어 id별 최신 생존 URL · CDN `oe=` 만료 인지 = 만료분은 안 씀 → 깨진 이미지 대신 캡션 타일 · 최근 표본 우선 200개 컷) ④ 뷰어 `chThFail`(로드 실패 = **공개 커버 경로 1회 재시도**[서버가 구운 CDN URL은 `oe=` 만료로 죽지만 permalink는 안 죽는다 · `null.jpg`는 400이라 onerror 재발동 → `dataset.rt`가 막아 무한루프 0] → 그래도 실패면 타일 **소멸이 아니라** `.ch-th.txt` 캡션 타일 강등 — 구 `display:none`은 12칸 그리드에 구멍을 냈다). **⑤ 회수 출처 카운터·결손 알림**(운영자 260803 "아이디어 ㄱ") = 회차마다 `{ig·pub·mu·fb·cache·none}` 집계를 `viewer/insta_data.json` `thumb_src`에 굽고, 결손 연속회차를 원장 `_meta.none_streak`에 누적 — **2회 연속**이면 `shared/msg.py`로 `insta-thumb-miss` warn 알림(**본문 = 인수인계 진단서** — 운영자 260803 "다른 세션에서 이 경고 다운받아도 해결 쉽게" · 회차 출처·막힌 permalink·이미 시도한 6층·다음 확인 순서·재현 명령·코드 위치를 한 장에 · 뷰어 메시지함 [↓]가 그대로 HTML로 내보낸다)(1회성 API 딸꾹질과 구분 · 해소되면 자동 clear · 커밋 = insta-fetch.yml git_land 인자 `messages`). 화면이 캡션 타일이라 멀쩡해 보이는 탓에 「조용히 나빠지는 것」이 유일한 사각이었고, 이 카운터가 그걸 숫자로 드러낸다(부수효과 = 어느 층이 실제로 일하는지 보여 안 쓰는 층 축소 근거). 강제 = `check_thumb_chain` 하드게이트(수집·산출·뷰어 전 층 심볼 + `.ch-th` 타일 줄 onerror 문법 + yml 인자 · 끊기면 커밋 차단). ⚠ 원장은 기계산출물 = 손편집 금지(값 변경 = 생성 코드 수정 후 재실행).
- **[SNS 카드 커버 = 워드마크 폴백 · 운영자 260803 "sns 모두 적용"]** 트렌드·구독 카드(`.tcard-cov`)와 X·스레드 게시물 카드(`.xcard-cv`)의 커버가 로드 실패하면(핫링크 만료·CDN 교체·403) **img를 숨기지 않고** 플랫폼 워드마크 폴백으로 강등한다 — 정본 = 레딧 폴백 `.tcard-cov.noc.rdfb`(무채 `#676767` + 순백 워드마크 · 260729)의 **값 100% 사본**이라 플랫폼별 새 색 창작 0, 구분은 `::after content` 글자뿐(`ytfb`·`tkfb`·`igfb`·`thfb`·`xfb`·`rdfb` 6종 · 긴 워드마크만 19cqw 기준값의 글자수 스케일). 배선 = 각 소스 map에서 `nocls` 주입 → `covImg`가 `data-nofb`로 실어 → `tcCovFail`이 강등. ⚠ 구 `onerror="this.style.display='none'"` 부활 = 카드 빈 구멍(260803 실측 = 전 커버 사망 시 숨김 161·박스 소멸 16 → 폴백 168·숨김 0) · 강제 = `check_thumb_chain`(6클래스 정의·배선 + 구 숨김 문법 금지). 260801 「이미지는 항상 일정한 공간」 계약 보호도 같은 축(`.xcard-cv`가 사라지면 카드 높이가 흔들린다).
- **[출처 글 본문 이미지 수확 = `.github/scripts/ask_srcimg.py` · 게이트 `check_ask_srcimg_chain`(운영자 260804 "확인해줘")]** SNS 카드 「전송」으로 보낸 커뮤니티 글이 요약에 실패하던 축의 봉합. 260804 실측 사고(`fail-2026-08-04-0239-297it` · 보배드림 best/1017600) 정체 = **「사이트를 못 읽음」도 「내용을 긁고도 뜻 모름」도 아니다** — 페이지는 정상 취득됐는데 **본문이 이미지 2장뿐이라 읽을 글이 0자**였고, 제목("이런걸 재능이라고 하는구나")은 고유명사가 0이라 프롬프트 1-2 기사 유추 폴백도 검색 2회 공회전 → 확보 사실 0 → 모델이 날조 대신 ANALYSIS_FAILED(판단 자체는 정직했다). ⚠ 파이프엔 **이미 멀티모달 레일이 있었다**(asks `images[]` → `workdir/img-*.jpg` → 프롬프트 「첨부 캡처」) — 사각은 단 하나, 그 레일에 **글의 본문 그림이 실린 적이 없다**는 것(`socBindSend`는 {제목·주소·RSS요약} 텍스트만 보낸다 · images 0장). → 수확기가 출처 URL의 본문 이미지를 내려받아 같은 레일에 태운다 = **신규 분석 로직 0**. **[OCR 층 = `.github/scripts/ask_srcocr.py`(운영자 260804 2차 "사진안에 문자열을 토대로 가져오게 배선 · 뉴스 요약에 연결")]** — 그림을 **첨부만** 하면 ⓐ 본선이 그걸 여는지가 프롬프트 준수 확률에 달리고 ⓑ 열어도 '전문(원문)' 지위를 못 얻는다(프롬프트 1)의 보강 모드는 「요청문에 전문이 있으면」 발동하는 축이라 그림만으론 그 레일에 안 물린다) → 문자열을 확정적으로 뽑아 **미디어 전사문과 같은 지위**로 실으면(`ask_link_stt.sh` 「이 전사문이 곧 원문이다」 문법 계승) 기존 뉴스 요약 보강 로직에 그대로 연결된다 = **신규 요약 로직 0**. 수단 = **Claude 헤드리스 1콜**(과금 0 구독축 · 종량제 벤더 신설 0 = `models.json` vendors 무접촉 · 폴오버·계측 = `shared/claude_py.run_claude` SSOT 경유 · 모델 = `ASK_OCR_MODEL` 기본 `claude-sonnet-5` 기계 판독 티어) · 킬스위치 `ASK_SRCOCR=0` · **fail-soft 2단**(OCR 실패 = 구 그림 첨부 블록으로 내려앉음 = 악화 경로 0). 실측 260804 = 사고 글 2장에서 **456자 추출**(X 캡처 전문 + BEST 댓글 전문 · 페라리 광고 배너는 「글자 없음」으로 정확히 분리). **[일괄 축(운영자 260804 3차 "sns 안에 있는 내용이 **일괄로** 뉴스 요약에 들어가는 소스로 · 요약요청 UI에서 전송할 때 참조되는 소스로")]** — 2차 배선은 OCR이 **수확분(`src-*`) 안쪽 if에 갇혀** 있어 운영자가 요약요청 창에서 **직접 붙인 캡처(`img-*`)** 는 여전히 파일 경로로만 실렸다(= 같은 그림이 **어느 입구로 들어왔느냐**에 따라 ⓐ 확률 축 ⓑ 전문 미획득 두 구멍이 그대로 남는 분기) → OCR을 **이미지 존재 축으로 독립**시켜 `ocrimgs=(img-* src-*)` 한 배열로 모아 **1콜 일괄 추출**(입구별 동작 차이 0 · URL 없는 순수 캡처 요청도 발동 · 수확분도 하단 「첨부 캡처 파일」 목록에 합류해 원본 나열 경로 단일화). `LINK_BLOCK`(미디어 전사문)이 이미 원문을 점유했으면 추출문은 **보조 소스**로 문구 자동 강등(원문 지위 이중 선언 방지). 강제 = 게이트 ⑥-b 2축(`ocrimgs` 글롭에 두 접두 동시 존재 + OCR 입력이 일괄 배열) · 실측 5축 = {첨부만 456자 · URL만 526자 · 둘 다 828자(5장 일괄) · 킬스위치 구 동작 · 무이미지 무발동}. 체인 = ask.sh 출처 URL 선정(`srcUrl` 우선 → 요청문 첫 URL 폴백 · `link` 레일이 잡은 요청은 무접촉) → `ask_srcimg.py` → `ask_srcocr.py` → 프롬프트 🖼 **추출문 블록** → **프롬프트 1-3 폴백**(본문이 그림뿐일 때 순서 = ① 추출문을 원문으로 삼는다[없으면 그림 Read] → ② 거기서 나온 고유명사로 검색 보강 → ③ 보도 없으면 게시물 자체를 소재로 완성 · 어느 경로든 ANALYSIS_FAILED 금지). ⚠ **UA 2단이 실효 조건**(장식 아님) — 실측상 보배드림은 `fetch_article.sh`의 **모바일 UA를 봇 차단**해 http 200에 본문 없는 3,566B 껍데기를 준다(조용한 실패) · 데스크톱 UA면 같은 URL이 160,268B = 본문 이미지 취득 성공 → 데스크톱 1순위 + 껍데기(`SHELL_BYTES`) 시 모바일 1회 교대. ⚠ **텍스트 길이로 「그림이 본문인가」를 가르는 판정은 실측에서 폐기**했다 — 본문 0자인 그 페이지의 전체 텍스트가 7,558자로 나온다(사이드바·추천글·메뉴 혼입 · 본문 컨테이너는 매체마다 달라 일반해 없음) → 게이트를 **바이트로 실측 가능한 축**으로 이전(치수 미명시 + 다운로드 바이트 하한 + `px_size()` 실측 픽셀 `PX_MIN`) · 낭비는 스코프가 흡수(출처 URL 축 전용). 킬스위치 = `ASK_SRCIMG=0` · 전 경로 fail-soft(수확 실패가 요약을 죽이지 않는다). 실증 = 그 글의 수확 2장을 읽으니 화제가 그대로 드러났다(보험 직업분류 「남성은 전업주부를 선택할 수 없습니다」 + 그걸 재프레이밍한 BEST 댓글 = 제목의 「재능」이 가리키던 것).
- **[요약요청 첨부 사진 = 글자가 읽히는 크기로 나간다 = `check_ask_img_legible`(운영자 260812 "지금 된 부분을 앞으로 나올 수 있게 반영")]** 발단 = 운영자가 세로로 아주 긴 기사 캡처를 요약 요청에 붙이며 물었다 — 「글자 못 읽을 만큼 압축돼서 왜곡될까봐」. 실측 결과 **지금은 읽힌다** → 이 게이트는 그 「지금 되는 상태」를 앞으로도 나오게 고정한다. 계약 = 뷰어 `compressImg` 긴 변 ≥`_ASKIMG_MIN_SIDE`(1280) · 화질 ≥`_ASKIMG_MIN_Q`(0.6) · 서버 재압축 0. **값 원천 = 260812 실측**(폰 기사 스크롤 캡처 1080폭·본문 36px을 같은 산식으로 압축한 뒤 **실제로 판독**) = 세로 3000 → 461×1280(본문 15.4px) 또렷 · 5700 → 243×1280(8.1px) 읽힘 · 9000 → 154×1280(5.1px) 읽히나 흐림. ⚠ **긴 변 기준 축소라 세로가 길수록 가로가 좁아진다** = 세로로 긴 캡처가 먼저 죽는 구조 — 값을 낮추면 토큰은 줄지만 그 캡처부터 무너진다. ⚠ 신설 사유 = **이 축은 나빠져도 화면이 멀쩡하다** — 압축은 첨부 순간에 끝나고 화면엔 축소 미리보기와 용량 배지만 뜨므로, 긴 변·화질을 낮춰도 첨부 UI는 똑같이 동작한다. 증상은 **한참 뒤 요약 품질**로만 나오고(숫자 오독·본문 누락) 원인이 사진인지 기사인지 모델인지 구분이 안 된다 = **운영자 눈조차 검출기가 못 되는 자리**(insta-thumb-miss·brk_misfire 동축). 기존 게이트는 전부 다른 축 — `check_ask_srcimg_chain` = 그림이 **실리는가**(층 생존) · `check_image_format` = **산출물** 포맷·품질(이건 산출물이 아니라 모델 입력이라 `q-ok` 면제 대상) · `smoke_*` = 화면 렌더 → 「그 입력이 **읽히는 크기인가**」는 축 자체가 없었다. 판정 = 정적(렌더·LLM·네트워크 0) · **면책표 없이 하드 0** · 단일 임계 상수 2개(부채 원장 증가 0 = `RENDER_BUDGET` 선례) · 3축 = ① 압축 함수 기본값 하한 ② **요약요청 호출부가 하한 밑으로 덮어쓰지 않음** ③ 서버가 받은 바이트를 그대로 저장(재압축 0). ⚠ **②가 실효 조건** = 기본값만 보면 `compressImg(f, 640, 0.4)` 한 줄로 조용히 우회된다(기본값은 그대로인데 실제로 나가는 사진만 나빠진다 = 정적 게이트의 전형적 사각 · 킬테스트 K2가 그 구멍을 실측으로 잡았다). 실증 = 킬테스트 4종 전건 검출{기본값 축소·호출부 덮어쓰기·화질 하향·서버 재압축} + 위양성 대조 무경보.
- **[알림 리포트 = 세션용 자기진단(운영자 260805 "너가 그 내용을 클로드 코드 세션이 봤을때 빠르게 알아낼 수 있도록")]** 알림 리포트(`msgRptHtml`)의 항목표는 **다음 세션이 코드를 거슬러 올라가지 않고 판정을 바로 읽는** 자산이다 — 3축 신설/봉합. ⓐ **발화 근거(`why`)** = 조건을 통과시킨 **값 자체**(fire = `lm="…" ∧ crit(유형)` · quake = `규모 N ≥ 문턱 M(국내/해외)`) — 구판 「판정 소스」는 *어느 필드를 보나*까지만 말해서 그 필드가 **무엇이었길래** 울렸는지는 세션이 라이브 데이터를 따로 떠서 맞춰야 했다(260805 서명 오탐 진단 20분의 대부분). 생산자가 실어준 알림만 행을 그린다(빈 행 = 잡음). ⓑ **재현 명령(`cmd`)** = 붙여넣으면 바로 도는 1줄(재난 = `viewer/sns_trends.json` disaster 전건 lm 출력) = [9] 「눌러서 되는 것」 축. ⓒ **출처 분기 = `check_rpt_origin_coverage` 하드게이트** — ⚠ 실사고 = `_rptSrc()`에 `sys:fire:`·`sys:quake:` 분기가 없어 마지막 폴백으로 떨어져 「만든 곳 = 서버 발행 메시지 · 판정 소스 = `messages/sys:fire:….json` · 상류 = `scraper/msg.py`」 **셋 다 틀린 값**을 줬다(그 파일은 존재조차 안 한다 · 실제 생산자 = 뷰어 `fireMsgs()`) → 세션이 그걸 믿고 `messages/`를 뒤졌다. 리포트 계약이 "세션이 파일을 바로 열게 하는 값"이라 **없는 값보다 틀린 값이 더 비싸다**. 구조적 원인 = 알림 생산자는 늘어나는데 출처표 분기는 손으로 따라가야 한다 → 생산자 id 리터럴(`id: 'sys:…'`) 자동 발견 ↔ `_rptSrc` 분기(`startsWith('sys:…')`) 접두 대조(세분화 분기 허용 · 정적 · 렌더·LLM 0 · 면책표 없이 하드 0 · 현행 6종 전건 커버). 새 `sys:` 알림 = 생산자와 분기를 같은 커밋에 넣는다(선택 아님).
- **[요약 실패 자동진단서 = `.github/scripts/ask_fail_probe.py` · 원장 `asks/fail_ledger.jsonl`(운영자 260805 "자동진단서 ㄱ")]** 요약 요청 실패 알림에 그 URL 의 **기계 실측**(받은 바이트·한글 자수·껍데기/프레임 판정·프레임 해제 결과·수확 이미지/OCR 층 발동 기록)을 자동 동봉 — 260804 사고 2건(보배드림·네이버 블로그) 다 「입력이 비었거나 불충분」 한 줄뿐이라 세션이 매번 원인 실측부터 다시 하던 축의 기계화. **같은 도메인 14일 창 2회 재발 = 인수인계 진단서 승격**(시도한 층·이번 실측·재현 명령·코드 위치·다음 확인 순서 — insta-thumb-miss 진단서 문법 계승 · 재발 문턱 2 = brk_misfire '같은 축 2건' 동축). 취득·해제는 `ask_srcimg` 정본 import 재사용(별도 fetch 창작 0) · 킬스위치 `ASK_FAIL_DIAG=0` · 전 경로 fail-soft(진단 실패가 실패 알림을 못 죽인다) · 원장 = 기계산출물 손편집 금지(상한 400줄 롤링 · news-ask.yml `git add queue asks` 동반 커밋 · 소비 글롭 asks/*.json 과 확장자 불충돌 실측). 강제 = `check_ask_srcimg_chain` ⑧축.
- **[스레드 쿠키 무소득 = 추측 처방 금지 · 로그의 `[1차 실측]`을 먼저 읽어라(운영자 260806 "이래봤자 내일 또 알림올텐데" · "뭘 맨날 폰에 쳐보래 · 니가 코드를 뽀개봐")]** 260805~06에 같은 경고(「threads @계정 쿠키 무소득 → 게스트 폴백 회수 N건」)에 대해 **세 처방이 연속으로 빗나갔다** — ⓐ「쿠키 갱신 권장」 ⓑ「THREADS_COOKIE를 빼라」(= 로그인 수집 기능 자체를 포기하는 안내 = 운영자 "알림을 안뜨게할거면 애초에 앱을 삭제해버리지") ⓒ「THREADS_UA 짝을 넣어라」(운영자가 실제로 넣었는데 5계정 전건 무소득 = **반증 확정**). 세 번 다 운영자가 폰에서 명령을 쳐서 실측을 대신했고, 매 회차 같은 경고가 다시 떴다. ⚠ **근본 원인은 스레드도 메타도 아니라 「관측이 구조적으로 지워지고 있던 것」** = 쿠키 경로가 실패해도 **게스트 폴백이 성공하면 `mine`이 채워져 `if _mine:` 가지로 빠지고 `_sok`이 찍혀서, 1차 쿠키 응답의 실패 사유를 판정하는 코드(`wall`/`alien`/`empty` 3분화 · 260804 2차 신설)에 영영 도달하지 못한다**(그 판정은 `else:` 가지에만 있고 폴백이 그 앞을 가로챈다) → 로그에 **증상만** 남고 원인은 매 회차 소실 → 사람이 추측으로 채울 수밖에 없는 구조. 봉합(260806 `4240ad2`) = 이미 손에 든 1차 응답 `h`를 그 자리에서 재서 경고에 싣는다(**추가 요청 0 · 새 콜 0**) = 「로그인월(세션 거절 = 쿠키·UA 축)」 / 「추천피드 N건(세션 **수락**인데 프로필 미도달 = **쿠키 재발급 무효 축**)」 / 「포스트 노드 0 + data-sjs N(파서 노후 / 챌린지·스켈레톤)」. **다음 세션 계약 = 이 경고를 보면 처방하기 전에 로그의 `[1차 실측]` 값을 먼저 읽는다** — 「추천피드」면 쿠키 재발급·UA 교체는 **전부 무효**이고 별개 봉합(프로필 미도달 축)이 필요하다. ⚠ 운영자에게 폰 명령을 시켜 실측을 떠넘기지 마라(그게 260805~06 피로의 직접 원인 · 30분 크론이 이미 돌고 있으므로 **조치 0으로 다음 회차 로그가 스스로 답한다**). 짝 진단기 = `scripts/phone_check.sh` ⑦(세션 판별 · env 로드 봉합 `ce04d58` — 구판은 `~/.nomute_phone_env`를 안 읽어 **항상 「미설정」 오답**이었다: 같은 화면에서 ⑧은 "쿠키 무소득"이라 말하는데 ⑦은 "쿠키 없음"이라 말하는 모순이 그 증거). ⚠ 이 결함은 **스레드 전용**이다 — 틱톡은 1차 실패에 `_sfail`을 먼저 찍고 넘어가 원인이 남는다(실측 260806 「@kleague 임베드 실패: HTTP 400」).
- **[틱톡 구독 1차 실측 = 폴백이 사유를 지우지 못한다(260806 실사고 봉합 · 스레드 쿠키 무소득과 같은 구조)]** 알림이 3일째 「틱톡이 @g_i_dle·@kleague·@formula1 을 **차단(403)** 했다 · 기다리면 풀린다 · 계정을 지우지 마」라고 말했는데 **실측은 정반대**였다 — ⓐ @g_i_dle·@formula1 = 임베드 **HTTP 200** · `userInfo.id` 확보 = **계정 살아있음** · `videoList`만 0건 ⓑ @kleague = `statusCode 10221` "Couldn't find this account" = **계정 자체가 없음**(삭제·개명) = **지워야 하는 건데 「지우지 마」라고 안내했다**. ⚠ 구조적 원인 = **관측이 폴백에 지워지고 있었다** — `tiktok_subs` ①(임베드)은 **예외를 던질 때만** `_sfail`을 찍어서 HTTP 200에 목록만 빈 경우 무기록으로 ②(tikwm)로 흘렀고, ②는 260714부터 **전 계정 상시 403**이라 계정 불문 같은 값이 사유 칸을 덮었다 → 화면에는 증상(403)만 남고 원인은 매 회차 소실 = 사람이 추측으로 메우는 구조(스레드 `[1차 실측]` 봉합과 **같은 병**). 봉합 = ①이 자기 결과를 그 자리에서 재서 `_e1`에 박고(`nolist` = 계정 살아있는데 목록 0 / `gone` = 계정 없음 / HTTP 코드) **②는 `_e1`이 없을 때만 기록**(추가 요청 0 · 새 콜 0) → 뷰어 `sysErrMsgs()`에 두 사유 분기 신설 = 조치 주체가 정확히 갈린다(`gone` = 운영자 = 계정 교체 · `nolist` = 코드 = 리포트를 클로드에게 · 둘 다 `_autoFix` 밖 = 「기다리면 풀린다」 오안내 소멸). **다음 세션 계약 = 이 알림을 보면 처방 전에 사유를 먼저 읽는다** — `nolist`면 계정 교체·대기는 **전부 무효**이고 임베드 창구가 그 계정에 목록을 안 주는 별개 축(미해결 = 원인 미확인 · 대안 창구 필요)이다. ⚠ 운영자에게 확인을 떠넘기지 마라 — 30분 크론이 이미 돌고 있어 **조치 0으로 다음 회차 로그가 스스로 답한다**.
- **[구독 = 등록 계정만 · 작성자 검문 = `check_subs_author_scope`(운영자 260804 "내가 구독한 애들이 아닌데")]** 「스레드 - 구독」이 **등록한 적 없는 계정 20건**으로 통째 채워진 실사고(등록 5계정 0건 · 회차마다 명단이 통째로 갈림 = 추천 피드 롤링 · 전건 `region:"gl"` = 등록 사전 미매칭 폴백의 기계적 증거). 구조 = `threads_subs()`의 walk()가 응답 안 **모든** 포스트 노드를 걷는데, 게스트 응답은 프로필 주인 글만 담지만(260804 실측 = 등록 5계정 37건 전건 본인 · other 0) 로그인 상태(`THREADS_COOKIE`)·로그인월 리다이렉트 응답에는 **추천(For you) 피드**가 같이 실린다 → 무검문 채집 = 메타 알고리즘 피드가 '구독' 칸을 강탈. X·인스타·틱톡은 항목 `account`를 요청 계정으로 못박아 구조적으로 불가능했고 **스레드만 노드의 `user.username`을 신뢰**해서 갈렸다. 봉합 3층 = ⓐ `threads_subs()` 작성자 대조(요청 계정 ≠ 작성자 = 폐기 · username 결측도 폐기 = 주인 확인 불가) ⓑ 성공 도장 = **본인 글 확보** 기준(구판은 `posts` 유무로 찍어 추천 피드만 걷힌 회차를 `got 5/5 miss []` **전건 성공**으로 보고 = 화면은 남의 글인데 게이트 무경보 = 가장 조용한 실패) ⓒ 러너 채택 지점 등록 명단 화이트리스트(폰이 구 파서로 돌아도 러너가 거른다). 강제 = `check_subs_author_scope` 하드게이트(3층 심볼 + 구판 `or acc` 폴백 부활 차단 · 정적 · 렌더·LLM 0).
- **[스플래시→앱 배경 3값 정합 = `check_boot_bg_parity`(운영자 260805 "아이디어도 진행하구")]** 같은 색이 세 파일에 **손으로 복사**돼 있다(`viewer/manifest.json` `background_color`·`theme_color` · index `#bootveil` 배경 · index `meta[theme-color]`) — CSS·meta가 manifest를 못 읽어 `var()` 도달이 구조적으로 0이기 때문이다. 계약 = ⓐ `#bootveil` == manifest `background_color`(근거 = 그 선언 자신의 주석 "manifest background_color 단색을 첫 페인트가 그대로 이어받고") ⓑ `meta[theme-color]` == manifest `theme_color`. ⚠ 신설 사유 = 한쪽만 고치면 나머지가 조용히 낡는데 증상이 「스플래시에서 앱으로 넘어올 때 색이 한 번 튄다」뿐이라 눈으로 안 잡힌다 — 기존 게이트는 전부 다른 축이다(`check_design` = 토큰 raw **개수** · 팔레트 핀 = **뷰어 간** 동값 · 거울 정합 = `:root` ↔ `base.css`) → 「OS가 그리는 색과 우리가 그리는 색이 같은가」는 축 자체가 없었다. **ⓑ는 하드**(현행 일치 `#070a12`) · **ⓐ는 WARN·비차단** = 260805 실측이 이미 어긋나 있다(manifest `#000000` vs 베일 `#070a12` · 260724 아이콘 교체 때 background만 순흑으로 갈린 것으로 보이나 미확정) — 해소는 「둘 중 어느 값으로 통일하나」 = 색 결정 = [4-3] 운영자 승인 대기라 하드로 못 올린다. 승인 시 값 통일 → 하드 승격. 앵커 소실(선언 형태 변경) = fail-closed 하드.
- [셸캐시 put 절단 검문 · 260802 재발 봉합] `nm-shell-*` 캐시에 닿는 **모든** `.put(`은 꼬리 `</html>` intact 술어를 동반한다(청크 스트림 절단 = res.ok 그대로라 무검문 put = 잘린 셸 기기 감금). 1차 봉합이 sw.js put만 막고 index `applyShellUpdate` 페이지측 put을 놓쳐 3시간 만에 재발한 축 — 강제 = `check_shell_put_integrity` 하드게이트(put 상행 15줄 근접 검사 · 무검문 put = 커밋 차단).
- **[geni 어휘 전역 질의 금지 · 260803 6차 "게이트 ㄱㄱ"]** AI 생성 폼은 **홈이 둘**(팝업 `#genidlg` ↔ 스튜디오 이식 `#geniHost` · geniMount가 노드를 통째 옮긴다)이고, 설정▸다운로드 창(`#dlgrab`)이 그 문법을 계승해 `.geni-body`·`.geni-histfold` 클래스를 **공유**한다 → `document.querySelector('.geni-…')`는 문서순 첫 매치 = **남의 창**을 물고, 이식 중에는 구조적으로 그렇게 된다(#geniHost가 #dlgrab보다 뒤). 실사고 = ⓐ `geniOutPlace` 전역 `.geni-body` → 폰 1단에서 결과 레일이 다운로드 창 안으로 이사해 **화면에서 소멸**(rect 0×0 · 콘솔 에러 0 = 무증상 · 그 창이 본문 재구축하면 노드 영구 소멸) ⓑ `geniInit` 전역 `.geni-histfold` → 남의 창 폴드에 핸들러 덧바인딩(CDP 리스너 0→1) ⓒ 잠복 = 문서 말미 `cscrollAttach` 같은 질의. 통과 문법 = **id 앵커**(`#genidlg .geni-body`) 또는 **스코프 호출**(`geniRoot().querySelector`·`h.querySelector`). 강제 = `check_geni_scope` 하드게이트(`viewer/*.html`+`viewer/*.js` 정적 문자열 · 진입 = document{속성 체인 포함}·$·$$ 계열 · 판정 = 셀렉터를 콤마로 쪼갠 **각 항이 `geni-` 어휘를 담으면 `#`(id 앵커)로 시작해야 한다** · 주석 줄 제외 · 렌더·LLM 0 · 면책 `_GENI_SCOPE_BASE` = 현재 비어 있음 · 평의회3 실측 우회 10수법{백틱·`document.body` 경유·`dialog .geni-body`·`div.geni-body`·콤마 뒤쪽·`getElementsByClassName`·`[class*=geni-]`·점 주위 공백·`body` 앵커·무앵커} 전건 차단 확인 · 남은 한계 = 변수 경유{`const s=".geni-body"; qs(s)`}는 정적으로 못 잡는다).
- **[긴급 오발 신고 폐루프(운영자 260803 4차 "누적될 때 활용을 안 하면 소용이 없다 · 3개 적체되면 자동 로직이 알림메세지에 개선안 · 내가 알림메세지를 놓치진 않거든")]** 🚨긴급 배지를 **롱프레스 480ms**(수집함 `.sc-badge.brk` · 피드 `.urg.brk` 두 표면 공용 · PC 우클릭 동치)하면 「긴급 아님」 신고가 `reason` 예약키 `brkno`로 `scraper/ratings.jsonl`에 적재된다(전용 필드 불가 = `rate.yml` workflow_dispatch inputs가 이미 **10개 = GitHub 상한** · 11번째 = 디스패치 400 = 평점 레일 전체 사망). 소비 = `scraper/brk_misfire.py`(rate.yml 동행 + watchdog.yml 30분 타이머 = 운영자 행동과 무관하게 TTL 상시 갱신) → **발화 = ⓐ 같은 축 2건 재발(1순위 = 진짜 신호 · 오발은 축별로 오고 한 축이 패치되면 안 터지므로 '총 3건'은 대개 서로 다른 축 3개 = 잡음) ⓑ 총 3건 ⓒ 아사창 72h(1~2건이 죽은 데이터가 되는 것 차단)** → `shared/msg.py`로 알림메시지에 **개선안**(공통축 + 게이트 개정 제안 + 신고 전문). **알림 id = 건수 접미 회전**(`brk-misfire-N`) — 고정 id면 뷰어 unread가 id축이라 메시지함을 **한 번 열면 영영 재점등이 안 된다**(✓ dismiss면 전 기기 영구 실명 · 레포 관례 = `sys:quake:`+time 회전). 해소 = 회귀 원장 편입 **또는 RUBRIC 해시 변경**(제목 완전일치 단독은 원장 표기가 한 글자만 달라도 영구 미해소 = 영구 재발화) · 오신고 기각 = 원장 `rejected`. ⚠️ **회귀 원장 자동 편입 금지** — 미검증 케이스가 들어가면 나중에 RUBRIC을 건드리는 **다른 세션**이 스탬프를 못 찍어 rc=1에 갇히는 지연 시한폭탄(그래서 `check_rubric_regress`가 케이스 **개수**까지 대조). ⚠️ 소비기는 **fail-open**(`|| echo ::warning::`) — `bash -e`라 rc≠0이면 커밋 전에 죽어 방금 적재한 별점이 증발한다. 강제 = `check_brk_misfire_chain` 하드게이트(뷰어 신고·예약키·워크플로 스텝·소비기·알림 5층 + inputs 10/10 상한 감시 — 한 층만 빠져도 신고가 조용히 죽는 원장이 된다).
- **[grade 룰북 회귀 원장 = `.github/scripts/grade_regress_cases.json` · 실행기 `grade_regress.py` · 게이트 `check_grade_regress`(운영자 260807 "전부 반영" · 평의회 8인)]** breaking 룰북엔 회귀 게이트가 있는데 **경중(grade) 룰북(gate_judge RUBRIC)은 무게이트**였다 — 운영자 260807 수기 재채점 58행(8/6~7 실측 · 표기 행만 = 자동 편입 금지 준수 · 대세 키워드 축[리센느류]은 랭킹층 담당이라 제외)을 정답지로 박제. RUBRIC 개정 = `python3 .github/scripts/grade_regress.py` 드라이런(전건 통과 시 스탬프) 필수 · 게이트는 정적 해시+케이스 수 대조(LLM 0). 기대값 개정 = 운영 방침 변경 = 커밋 메시지 사유. **채점 철학(평의회 8렌즈가 재채점 ~45건을 잔차 0으로 환원 · 260807)** = ① 1↔2 경계 = **발생 시제**(예고·조율·점검 = 1 · 벌어진 절차는 결론 없어도 2[공정위 심의·회의 종료]) ② 경제 = **행위 주체의 공·사**(공적 주체·국가 통계·다수 피해 = 2 / 사적 주체 자기 활동[실적·수주·지분·상품·노사] = 규모 무관 1 / 인수합병도 2 경계 / 매체 단독 분석·전언 = 1 · 단 진행 중 국가 현안 사설 = 2) ③ 2↔3 경계 = **신규성**(새 제도 발효·전국 확산·대량 인명만 3 / N차 기소·선고·재난 후속 행정은 대통령급이어도 2 — 완결성과 정반대 축) ④ **시황 = 폭 무관 1**(폭락·서킷브레이커 포함 · 운영자 "레버리지 도입 후 빈번 = 임팩트 희석 · 주식은 2 택도 없다" — 긴급 레일[isBreaking g≥2]도 시황 기사만 소등 = 운영자 승인) ⑤ 문화 = **인지도 × 사건성**(국민급 유명인의 벌어진 사건[논란 복귀·공식 사과·거액 거래] = 2 · 화보·근황·완전체는 최상급도 0 — "연예인이 끼면 사건 몸집이 한 수 커진다·도달 2배"). ⚠️ 5세대 씨앗 교훈 동축 = 드라이런 58/58 통과는 **확률 개선의 증거지 재현 보증이 아니다**(sonnet 분산) — 다음 재채점 라운드로 재검증.
- **[grade 수기 교정 폐루프 = 뷰어 `.sc-grade` 칩 · 원장 `scraper/grade_votes.jsonl` · 12h 소비 `scraper/grade_fix_report.py` · 게이트 `check_grade_fix_chain`(운영자 260807 "카드에 grade 나오게 · 어긋났을 때 고칠 수 있게 · 12시간마다 고쳐진 것만 한 바퀴 돌면서 다 기록")]** 수집함 카드 메타라인에 grade 번호 상시 표시(G0~3 · 미채점 G–) → 탭 = `.sc-rsn` 정본 계승 선택지 팝(0~3 + AI 원복) → 로컬 즉시 반영(랭킹 gradeW만 — **배지·긴급은 raw c.grade 유지** = build-viewer 미러 규칙 동일 계약 보존) + `postRate(action='grade', reason='<fix|x>:<ai|x>')`(rate.yml inputs 10/10 상한이라 예약키 문법 = brkno·thumb 선례) → `rate_record.py` 분기 적재 → 12h 스윕이 새 교정 전건을 **내부 축적 원장 `scraper/grade_fix_reports.jsonl`에 라운드당 1줄로 기록**(등급 이동 매트릭스 + 전건 · 라운드 소비형 seen 커서 · rate.yml+watchdog 30분 동행 = 별점 안 눌러도 주기 보장 · 전 경로 fail-open · **알림 발화 0 = 운영자 260808 "내부에 축적되게만 · 내가 중간에 확인할게 · 알림 없어져도 됨"** — 구판 msg.py 발화 제거). 교정분 = 다음 RUBRIC 개정의 회귀 원장 후보(자동 편입 금지 — 사람 승인 대기). 강제 = 5층 정적 생존(뷰어 칩→분기→원장 커밋 줄→12h 스윕→알림 · 한 층만 빠져도 조용히 죽는 원장 = thumb_votes 동축). ⚠️ 한계 = 교정의 즉시 반영은 로컬(localStorage) — 타 기기 동기·서버 grade 덮어쓰기는 별건(원장은 전 기기 공용).
- **[S 레벨 = 대세 = viewer `BUZZ_W` 1.2(운영자 260808 2차 "grade에 관계없이 · 등급 사다리 0 1 2 S 3 · S가 대세")]** 카드 제목이 `sns_trends` 실검(gtrends·gtrends_pool·xtrends) 키워드와 매칭되면 **등급·카테고리 무관** gradeW = 1.2(S) — 사다리 = 0(0.5)·1(0.7)·2(1.0)·**S(1.2)**·3(**1.7** — 운영자 260810 "2는 1.0배, 3은 1.7배" · 구 1.5는 S 대비 1.25배뿐이라 대형이 대세에 눌리던 간격 복원) · grade 3은 S보다 위라 불변 · 구판 「문화 grade0 한정 ×1.2」(260808 1차)는 이 일반화로 대체. **화면 표기 = GX**(운영자 260810 "대세는 GX로 표기") — 술어 = `isBuzzLv`(= gradeW의 승격 조건 1:1 · 3점처럼 승격이 **미발동**한 카드는 GX가 아니라 원 등급 표기 = 배율과 표기의 거짓 분리 차단 · 표시·툴팁만·색 무변). 매칭 술어 = 4자+/공백 구문은 제목 substring · 2~3자 키워드는 제목 토큰 정확일치 or 조사꼬리(화이트리스트 문자 2자 이내) 전방일치("리센느가" ✓ · "로제"가 "프로젝트"·"로제타석"에 걸리는 오탐 구조 차단 · 킬테스트 9종 실측). grade 원값·⚡배지·긴급·푸시·자동픽 **전부 무접촉**(랭킹 전용) — 시변 신호(대세)를 정적 룰북에 박으면 모델 지식 낡음+회귀 재판정 불능이라 거처를 랭킹층으로(평의회 260807 만장일치 · fpW 대체안은 산술 기각 = 0.5×1.25=0.625<0.7). 롤백 = gradeW 1곳.
- **[루브릭 회귀 원장 = `.github/scripts/rubric_regress_cases.json`(운영자 260803 "ㄱㄱ" 승인)]** breaking_judge RUBRIC(속보 판정 프롬프트) 개정 = `python3 .github/scripts/rubric_regress.py` 드라이런 필수 — 과거 실측 판정 케이스(실발송·실픽 이력 + 평의회 보호 케이스 22건)를 현행 RUBRIC으로 재판정해 정답 뒤집힘(오컷·오발 회귀)이 0이어야 스탬프(rubric_regress_stamp.json)가 찍힌다. 강제 = `check_rubric_regress` 하드게이트(스탬프 해시 ≠ 현행 RUBRIC 해시 = 커밋 차단 · 게이트 자체는 정적 대조 = LLM 0 · LLM 1콜은 실행기 시점만 · RUBRIC 무변경 커밋 = 무비용 통과). 기대값(expect) 개정 = 운영 방침 변경 = 커밋 메시지에 사유 명기. ⚠ 신설 사유 = 260803 「대구 40.1도」 오발 — 프롬프트 수정의 회귀를 사람 눈으로만 보던 축의 기계화.
- **[Pages 빌드 코얼레싱 = 선별 [CF-Pages-Skip](운영자 260803 · 페이블 5인 평의회)]** CF Pages는 커밋당 1회 FIFO 풀빌드(46s·훅 빌드도 맨 뒤 = 추월 0 실측)라 봇 커밋 유입(피크 74/h)이 처리율(78/h·CF 감속일 ~33/h)에 육박하면 큐가 시간 단위로 밀린다(260803 실사고 = 4시간 → 대기열 무한 스피너·감지 0). 봉합 = 「화면에 수 분 늦게 떠도 되는 데이터 churn」 커밋에만 **[CF-Pages-Skip]** 접두(CF 전용 — GitHub [ci skip] 5종에 미포함 = push 발화 무손상 · 유일 안전 토큰) → 스킵분은 다음 비스킵 빌드(scrape 15분 메트로놈·코드·제작)가 tip 통째 배포 = 누적·유실 0 · 신선도 상한 ≈ 17분. 배선 = `git_land.sh` 헤더(sns-trends·insta·fb·lucy) + 인라인 5곳(social-scan·breaking-judge 경중 grade만·metrics-rollup·watchdog·rate) · 킬스위치 = repo variable `PAGES_COALESCE=0`(부재=ON) · **금지 축 = stamp(:14 명문)·제작 산출(체크런 게이트 5종)·news-analyze/ask(notify 240s 축)·scrape(메트로놈)·auto-pick·조기 반영** — 강제 = `check_pages_skip` 하드게이트(_PAGES_SKIP_ALLOW 등재 파일 밖 접두 = 커밋 차단) **+ 짝 강제 `check_coalesce_pair`(운영자 260803 6-4 승인 · Q1343 실사고 기계화)** = 코얼레싱 워크플로가 `git_land`로 착지시키는 `viewer/*.json` 중 **뷰어가 실제 fetch하는 표면**은 반드시 빌드 우회 라이브 서빙(`functions/api/*.js`)을 보유해야 한다(없으면 커밋 차단 · 전부 자동 발견 = 손 목록 0). ⚠ 없으면 스킵된 커밋의 산출이 **다음 비스킵 빌드까지 화면에서 정지** = 조용한 죽음 — 실측 = `tbs_data.json`(키워드 알림 국내 감시축 · 260720~26 "6일 빈 감시" 재발 축)이 그 상태였고, 게이트 첫 실행이 `threads_state.json`(lucy 화면)까지 **사고 전에** 선제 검출했다. ⚠ CF측 감속(μ 붕괴)은 레포로 못 막는다 — 감지 = watchdog ⑦ 배포 지연(90분·30분 주기). 잔여 미배선 = functions/api/seen.js(일 5건 · CF Function GitHub API 커밋 = env 게이트 불가 · 실익 미미).
- [워크플로 --amend 금지 · 260803] 러너 결과 커밋 재시도 루프의 `git commit --amend`는 자기 커밋이 rebase에서 드랍되는 순간 **남의 푸시된 커밋을 개서** → non-ff 영구 거절 = 산출 유실(filltest 동시 3발사 실측 · 런 30779914067 — 원조 imggen의 안전한 새-커밋 방식을 사본(resize→upscale)이 --amend로 개악한 복제 사고). 재병합 = **새 커밋으로만**(`git diff --cached --quiet || git commit -m …`) · 강제 = `check_workflow_amend` 하드게이트(.github/workflows 전수 · 실행줄 --amend = 커밋 차단 · 현행 0건 청정).
- **[제작 완료 푸시 = 구독자 명단을 체크아웃해야 나간다 = `check_push_send_checkout`(260816 실사고 봉합 · 운영자 「제작 완료되었을때 웹앱 푸쉬오는거 정상 배선되있는지 봐줘」)]** 제작 완료 폰 알림이 **7레인에서 한 통도 안 나가고 있었다**. 진범 = 260728 「체크아웃 다이어트」(운영자 "예전에 비해 오래 걸려" Q진단)가 `sparse-checkout`을 도입하며 **`push/`를 목록에서 뺐다** — `push_send.py`는 `push/subscriptions.json`(구독 기기)을 읽는데 러너 디스크에 그 파일 자체가 없으니 `jload(SUBS, [])`가 빈 배열을 돌려주고 **「구독자 없음 — 발송 생략」으로 `return`**한다. ⚠ **가장 조용한 실패** = 발송 생략은 예외가 아니라 정상 종료라 스텝이 **초록**이고(실측 260816 run 31933687078 = 잡·스텝 전건 success), 제작물은 R2에 정상 착지하고, 화면에도 결과가 뜬다 → **증상이 「알림만 안 옴」 하나뿐**이라 운영자가 물어야만 발견된다(insta-thumb-miss·brk_misfire 동축). 실측 피해 = 카드 제작·영상 편집·AI 이미지·음원·보이스·변환·트래킹 **7레인**(살아 있던 건 `push` 를 가진 속보 `breaking-judge`·영상 받기 `vidl-make` 2종뿐 = **「형제는 가진 걸 자기만 안 가진」 이 레포 최빈 축**의 재현 · 260807 금융 티커·260808 크로미엄 경로와 동형). 봉합 = 7워크플로 sparse 목록에 `push`(구독자 명단)+`assets/brand`(알림 아이콘 번들 = `notif_dataurl.json`) 2줄 — 값·자리 전부 정본 형제 `vidl-make` 사본(창작 0). ⚠ 신설 사유(게이트) = 기존 게이트가 전부 다른 축이다 — `check_workflow_yaml` = 문법 · `check_workflow_amend` = 커밋 방식 · `check_paths` = 경로 실존 · `smoke_*` = 화면 렌더 → 「푸시가 **나갈 수 있는 상태인가**」는 축 자체가 없었고, 새 제작 레인이 생기면 같은 자리에서 또 조용히 빠진다. 술어 = 「실행줄 `push_send.py` 호출 ∧ `sparse-checkout:` 사용 → 목록에 `push` 단독 경로 보유」 · 전체 체크아웃 레인은 대상 밖(이미 다 받는다) · 아이콘은 없어도 발송되므로(`notif_icon` None 폴백) 안내만 · 주석 줄 제외 · 표면 자동 발견 · 하한 5 fail-closed · 정적 · 렌더·LLM·네트워크 0 · **면책표 없이 하드 0**. 실증 = 킬테스트(`thumb-make`에서 `push` 한 줄 제거 → 파일·행 정확 지목 rc=1) + 복원 위양성 0 + 봉합 후 11레인 전건 보유. ⚠ 남는 것 = `breaking-judge`는 `assets/brand` 미보유(속보 알림이 기본 아이콘으로 나간다 = 발송 자체는 정상 · 별건).
- **[프롬프트 리터럴 인용 무결성 = `check_prompt_literal_quoting`(260805 실사고 `fail-2026-08-04-{1528,2211}` 봉합)]** ask.sh 프롬프트 1-3) 블록에 **이스케이프 안 된 큰따옴표**가 들어갔다(`실패 사례 제목 = "이런걸 재능이라고 하는구나"`) → 첫 `"`가 `prompt="`를 닫아 뒤 텍스트가 셸 토큰이 되고(`재능이라고: command not found`), `prompt=…`는 그 명령의 **환경변수 프리픽스**로 흡수돼 변수 자체가 안 잡힌다 → `set -u` unbound → `claude -p`에 **빈 stdin** → "Input must be provided…" = **요약 요청 전건 100% 실패**(요청 내용과 무관한 고정 리터럴이라 뭘 넣어도 죽는다 · 실측 = 15:28 네이버·22:11 코리아탑뉴스 두 건이 **같은 줄에서** 죽었다). ⚠ **가장 조용한 실패의 전형** = 문법적으로 완전히 유효해 `bash -n`·커밋 게이트 전건 통과하고, 화면 문구는 「내용 분석 결함(입력이 비었거나 불충분)」이라 **입력 탓으로 보인다** — 그래서 260804 세션이 그 문구를 믿고 엉뚱한 축(네이버 프레임 셸)을 봉합했고 진범은 6시간 더 살아 다음 요청도 죽였다(프레임 해제 자체는 유효한 개선이나 이 사고의 원인이 아니었다). 같은 줄의 백틱은 `\``로 이스케이프돼 있었다 = **사람 눈이 따옴표만 놓치는 축** → 기계 검사가 유일한 해. 강제 = 하드게이트(`.github/scripts/*.sh`+`shared/*.sh` 자동 발견 = 새 스크립트가 조용히 못 빠진다 · 판정 = `_shell_literal_leak` bash 인용 문맥 정적 훑기{`$(…)`·`${…}`·heredoc·작은따옴표 스킵} · 렌더·LLM·네트워크 0 · 면책표 없음 = 현행 위반 0). 정상 관용구 4종은 갈라낸다{구분자·줄연속 `\`·다음 할당·한 줄 env-프리픽스 체인의 종점 명령} — 실측으로 위양성 200→70→10→**0**까지 좁힌 경계다(`METER_SRC=ask METER_REF="$base" … claude_meter` 형태가 레포 전역 관용구라 안 갈라내면 게이트가 레포를 통째로 얼린다). 실증 = 킬테스트 5종 전건 검출{사고 원복·줄중간 `case )` 할당·LINK_BLOCK·SRCIMG 블록·영문 본문} + 위양성 대조 3종 무경보{정상 이스케이프·주석 속 따옴표·무변경}.
- **[실패 사유 = 「코드 결함」 축(운영자 260805 "아이디어 ㄱㄱ" · 위 사고의 짝)]** 그 사고가 6시간 더 산 진짜 이유는 **알림 문구가 원인을 반대로 가리켰기 때문**이다 — 사유 분류가 {timeout·congest·source} 3종뿐이고 `source`(내용 결함)가 **else 분기**라, 모델이 입도 뻥긋 못한 파이프라인 사고까지 「입력이 비었거나 불충분」이라고 말했다(진단서가 붙어도 분류가 `source`면 그 진단서마저 소스 축으로 읽힌다 = 이번 실사고는 URL이 멀쩡한데 `prompt` 변수가 안 잡혀 죽은 건). → **`_fk=code` 신설** · 술어 = `rc≠0 ∧ 출력 0 ∧ ANALYSIS_FAILED 없음`(= 모델이 답한 적이 없다 · 앞 3분기 통과 뒤라 timeout·congest와 겹침 0 · 비용 0 문자열 검사). 문구 = 「**코드 결함** — 네 입력 문제가 아니야 / 첫 오류: <stderr 첫 줄> / 재시도해도 같은 자리에서 죽어」 = **stderr 첫 줄 동봉**이 핵심(실측상 그 한 줄이 진범을 그대로 가리켰다 · 자르기는 문자 단위 = analyze `_why` 관용구 계승, `head -c`는 한글을 쪼개 U+FFFD를 박는다). **두 경로 동시 보유가 계약**(ask·analyze) — 한쪽만 고치면 나머지가 조용히 구 문구로 남는 게 가장 흔한 미러 드리프트라 `check_ask_srcimg_chain` ⑧-b가 두 파일을 함께 본다(킬테스트 = analyze 축 제거 시 검출 실증). 실증 = 분류 6형 실측{실사고 rc1·빈출력 → code · ANALYSIS_FAILED → source · rc124 → timeout · Overloaded → congest · 형식불량 → source · ANALYSIS_FAILED+rc≠0 → source} = 기존 3축 회귀 0.
- **[썸네일 화풍 투표 폐루프 = `scraper/thumb_vote_report.py` · 게이트 `check_thumb_vote_chain`(운영자 260805 "좋으면 thumb up · 별로면 thumb down · 그게 남게끔 해서 나중에 한번 보자" → "ㄱㄱ")]** **정답지가 없어서** 260805 프롬프트 5축 봉합이 실제 발사분 236건 **손 재판독**으로 나왔다 — 「운영자가 어느 썸네일을 좋다고 봤나」가 시스템 어디에도 안 남았기 때문. 봉합 = 뷰어 AI 생성 슬롯에 👍/👎(부품 = 카드뉴스 투표 정본 100% 사본 = `.fbup`/`.fbdown`·`THUMBUP/THUMBDOWN_SVG`·`.voted`·2탭 취소 · 새 창작 0 · gen 한정 = 검색 이미지는 우리가 만든 게 아니라 정답지가 못 된다) → `postRate(action='thumb', reason='<up|down|clear>:<sid>')` → `rate_record.py`가 **별도 원장** `scraper/thumb_votes.jsonl`로 분기 → `thumb_vote_report.py`(rate.yml 동행 · fail-open)가 임계 20건·아사창 168h에 **프롬프트 개선 리포트**를 알림메시지로 발화. ⚠ **inputs 신설 0이 실효 조건** — `rate.yml` workflow_dispatch inputs가 이미 10/10 = GitHub 상한이라 11번째 = 디스패치 400 = **평점 레일 전체 사망**(brkno 예약키 선례와 같은 축) → 전용 필드 대신 기존 `action`·`reason`에 태운다. ⚠ **원장 분리가 실효 조건** — `ratings.jsonl`은 「이 기사를 볼 만한가」(자동픽·취향 학습 입력)이고 이건 「이 그림이 잘 나왔나」다. 섞으면 score=0 레코드가 취향 통계에 잡음으로 들어간다. 리포트 4축 = ⓐ 화풍별 승률 ⓑ 👎 몰린 연출코드(`thumb_dispatch` 역참조 = **다음 개선의 착수점**) ⓒ SUBJECT 유무 승률(260805 봉합 축의 사후 검증) ⓓ 👎 실물 발췌(SCENE·연출 = 원장을 안 열어도 알림 안에서 판단 = [9] 축) · **라운드 소비형**(발화분은 `seen` 커서에 박고 다음 라운드는 새 표만 = 같은 결론 재발화 0·최근 경향 매몰 0) · 표본 `MIN_CELL` 미만 셀은 「표본부족」으로 표기(없는 결론을 지어내지 않는다 = [1] 정직) · LLM·네트워크 0. ⚠ 신설 사유(게이트) = 투표는 **두 가지 서로 다른 방식으로 조용히 죽는다** — ⓐ 적재는 되는데 커밋이 안 됨(260805 실사고 = `rate.yml`이 `ratings.jsonl`만 add해 다음 체크아웃에서 증발할 뻔) ⓑ 쌓이는데 아무도 안 읽음(죽은 원장 = brk_misfire가 막으려던 바로 그 축). **화면은 둘 다 멀쩡해 보인다**(버튼이 눌리고 초록이 켜진다) → 정적 층별 생존 강제가 유일한 검출기(정적 · 렌더·LLM 0 · 면책표 없이 하드 0). ⚠ **`_has_exec_line` 헬퍼 신설 = 기존 게이트 공통 구멍 봉합** — 「주석 아닌 실행줄인가」를 `^[ \t]*(?!#)[^\n]*needle` 정규식으로 판정하던 관용구가 **주석 처리를 못 막는다**(`[ \t]*`가 들여쓰기를 한 칸 덜 먹고 백트래킹하면 `(?!#)`가 공백에서 통과하고 `[^\n]*`가 `# `를 삼킨다 · 260805 킬테스트 K1 실측 — `brk_misfire`·`srcimg` 게이트가 전부 이 구멍을 갖고 있었다) → 줄 단위 판정으로 교체하고 brk_misfire도 함께 봉합. 실증 = 소비기 5형 실측{발화·라운드 소비·취소(clear) 무효화·임계 미달 대기·원장 부재} + 킬테스트 7종 전건 검출{소비 스텝 주석·원장 커밋 줄 제거·러너 분기 제거·뷰어 전송 제거·라운드 커서 제거·brk 공통 구멍·위양성 대조}.
- **[썸네일 수정 = 덮어쓰기 아닌 +1 슬롯 = `check_thumb_redo_append`(운영자 260807 "수정하면 원래 이미지는 계속 보이게하고 수정된 이미지가 +1개로 생기는 개념이여야 되거든? 1/2 2/2 면, 1/3 2/3 3/3")]** 뉴스 요약 안 AI 생성 슬롯의 연필 '수정'은 **원본을 지우고 같은 R2 키에 덮어썼다**(`thumb_gen` main = `[x for x in g if x.get("sid") != redo_sid]` 로 gen.json 항목 제거 → `process_one` 이 `thumbs/{stem}/gen-{sid}` 에 재업로드) → 원본 바이트 **영구 소멸** · 슬롯 수가 화풍 수(2)에 영영 고정(1/2 2/2 가 계속 1/2 2/2) · 원본이 더 나았어도 되돌릴 방법 0. → **파생 sid `<base>_rN`**(photo_r2·photo_r3…)로 **새 슬롯 추가** = R2 키 자연 분리로 원본 무접촉, 화면은 1/3 2/3 3/3(3번 = 제작중). [`thumb_gen` 61행 「sid 리네임 금지 = **추가만 허용**」 계약 준수 = 기존 sid 무변경·재과금 0.] ⚠️ **세 층 중 하나만 빠져도 조용히 죽는데 셋 다 화면은 멀쩡해 보인다** = ⓐ `process_one` 파생 보존 결손 — 그 루프는 **STYLES 만 순회하며 gen.json 을 재조립**하므로 파생 sid 가 리스트에 안 실려 **다음 실행에서 소리 없이 사라진다**(78행이 명시한 '자동 드롭' 메커니즘 · 수정 직후엔 멀쩡하다가 며칠 뒤 혼자 없어짐 = 최고 추적난도) ⓑ `thumbredo.js` 화이트리스트가 숫자 불허(`[a-z_]+`)면 파생 sid 가 거절돼 `sid=''` → **전 화풍 재생성 = Gemini 재과금 2배**(새 그림이 나오니 오작동으로 안 보인다) ⓒ 뷰어 완료 판정이 구판(원본 img `?v=` 변화)이면 이제 원본이 안 바뀌므로 **10분 타임아웃까지 완료 미검출**(제작중 슬롯 잔류 + '지연됐을 수 있어' 토스트) → 판정 축을 **그 화풍 계열 장수**(`genGenCount`)로 이전. 대기 표시 = 원본 위 진행바(`markSlotReshooting` · dormant)에서 **트랙 끝 `.cref-loading.tredo-ld` 슬롯**으로 이전(부품 = imgFetchGo 로딩 슬라이드 100% 계승 · 창작 0 · `:not(.tredo-ld)` 로 AI 생성 중복발사 가드와 분리 = 수정 대기 중 생성 버튼이 조용히 잠기는 것 차단). 짝 = AI 생성 cap 7→40(수정 5회면 도달해 그 뒤가 조용히 잘렸다) · 투표 집계는 `base_sid()` 로 **베이스 화풍 합산**(세대별로 쪼개지면 260805 정답지가 희석된다). ⚠️ 기존 게이트는 전부 다른 축(`check_image_format` = 포맷·품질 · `check_thumb_vote_chain` = 투표 층 · `smoke_*` = 화면 렌더) → 「수정이 덮어쓰기인가 누적인가」는 축 자체가 없었다. 판정 = 정적(렌더·LLM·네트워크 0) · 주석 줄 제외(주석 처리 우회 차단) · **면책표 없이 하드 0** · 구판 파괴 문법(`!= redo_sid`) 부활 = 커밋 차단. 실증 = 실렌더 9축(수정 전 1/2 2/2 → 수정 직후 1/3 2/3 3/3 · 대기 슬롯 = 마지막 · 원본 진행바 0 · 대기 슬롯 기하 = 원본 동일 226.5×283.2 · 콘솔 에러 0).
- **[검색 이미지 화질 = 그 사진의 가장 큰 판 = `check_img_upsize`(운영자 260810 "일부러 압축해서 보여주는건가? · 유관한 이미지 품질은 유지하되 고화질을 가져오게 · 최소 세로 720p 이상")]** ⚠ **우리는 압축을 한 적이 없다** — `http_image`가 받은 바이트를 `r2_upload`가 **그대로** 올린다(리사이즈·재인코딩 0). 화질의 유일한 결정 지점은 **「어느 URL을 집었나」 한 축**이고, 진범은 **매체 og:image가 SNS 카드용 축소판**이라는 것이다(실측 260810 = 헤럴드 og `_T1` **300×200**인데 같은 CDN에 `_R` **1280×853**이 그대로 있다 · 스포츠Q `/thumbnail/…_v150` 300×200 ↔ `/photo/…` 600×400 · 파이낸셜 `_l` 800×584 ↔ 접미사 제거 **3165×2313** · 한겨레 `/flexible/normal/970/647/` ↔ 경로 제거 1280×854 · 조선 1200×630 ↔ **3674×2198**). 비용이 큰 이유 = **카드 산출물이 짧은변 1440**(thumb-make RES-SNAP)이라 300×200 배경은 **4.8배 업샘플** = 원본에 없는 화소를 지어내 뭉갠다. 실측 코퍼스(최근 40기사 174장) = 세로 720 이상 **36.8%**뿐. 봉합 = `_upsize_urls`(URL에서 축소 표기를 지운 후보 생성 · 순수 문자열 · 네트워크 0) → `_dim_probe`(**앞 16KB만 Range로 받아 헤더 치수 실측** · 모자라면 64KB 재시도) → `_best_variant`(세로가 더 크고 가로가 안 줄 때만 채택). ⚠ **매체별 하드코딩이 아니라 「축소를 뜻하는 표기를 지운다」는 한 가지 규칙**(쿼리 치수 · `/thumbnail/`→`/photo/`+`_v150` · 리사이저 세그먼트 `/r/700xX/` · `.cache/512/` · `flexible/normal/W/H/` · `_T1`→`_R`·`_P1` · `_l`·`_tc` 크기 접미사 · `-500x300` 치수 접미사) — 손 레지스트리는 새 매체가 조용히 빠진다(이 레포가 반복해 겪은 드리프트). ⚠ **채택 판정은 URL 추측이 아니라 실측 픽셀** — 접미사만 보고 채택하면 매체가 규칙을 바꾼 순간 404·엉뚱한 사진이 된다(실측 = 인천일보·뉴시스·이투데이는 같은 규칙이 404/403 → 전건 원본 유지 = fail-soft). ⚠ **승격을 dedup *앞*에 둔다** = 같은 사진의 축소판·원본이 같은 URL로 수렴해 **중복 슬롯도 함께 해소**된다(실측 = 문화일보 og `_R` 1200×801 + twitter `_T1` 300×200이 한 사진인데 슬롯 2개를 먹고 있었다 — `_norm_key`가 `_R`·`_T1` 접미사를 안 지워 dedup을 빠져나갔다). ⚠ **720 = 하드 컷(운영자 260810 2차 「720을 안넘을 경우에는 애초에 수집에서 배제 · 그 품질 만큼의 다른 이미지를 검색해서 찾아오게끔」)** — 1차는 「컷이 아니라 순서」(720 이상을 앞으로 보내 want 상한에서 저화질이 자연 절삭)였으나 운영자 판단으로 **배제**로 개정됐다: 그 자리를 저화질로 메우지 말고 **비워서 다른 사진을 찾아온다**. 빈 자리 회수 = **기존 보충 체인 재사용**(`len(items) < 7` → `thumb_topup.txt` → moreimg 발사 → Claude 신규 소스 검색 = 새 배선 창작 0). 짝 계약 2종이 실효 조건 = ⓐ **관련소스 훑는 범위 15→`_REL_SCAN`(30)** — 컷으로 소스당 채택률이 반감(실측 54%)하므로 범위를 그대로 두면 컷이 곧 장수 감소로 직결된다 ⓑ **moreimg 후보 요청 `WANT+3`→`WANT*4` + 프롬프트에 컷 사실 명시** — 여유분이 3개면 want 를 못 채운 채 끝나 보충이 한 바퀴 더 돌고 **Claude 콜이 오히려 는다**(후보를 넉넉히 받는 쪽이 싸다). ⚠ **대표도 예외 없음** — 원기사 og 가 720 미달이면 대표 자리는 관련소스의 첫 720 이상이 승계한다(THUMB_REF 참조 얼굴도 그 사진으로 이동 = 화질 보장이 우선). ⚠ **크기 미상(프로브 실패)도 컷 = fail-closed** — 하드 컷 계약에서 '못 잰 것'을 통과시키면 문턱이 그냥 새는 구멍이 된다. 대신 `_dim_probe` 마지막 단을 **Range 없이** 두어 Range 거부 서버의 큰 사진이 억울하게 죽는 걸 막았다(3단 = 16KB Range → 64KB Range → 256KB 무Range). 실측(같은 22기사) = **720 미만 섞임 0장 · 세로 중앙값 857 · 최소 720** · 기사당 장수 **4.09→2.27** · 0장 기사 3건(전건 보충 발사 대상). ⚠ **관련성 축 실측(운영자 260810 3차 「대신 관련있는 쓸모있는 이미지를 가져와야되는데 그건 유지가 되는건지」)** = ⓐ **소스 풀은 무접촉** — 후보는 분석단계 사진로봇(소넷)·moreimg(Claude)가 **그 사건 키워드로 찾은 것**이라 관련성으로 이미 걸러진 풀이고, 컷은 그 안에서 **화질로만** 거른다 = 무관한 사진이 새로 유입되는 경로가 없다(실측 = 담긴 53장 전건 관련소스 풀 안) ⓑ **훑는 범위 15→30 은 실측상 무영향** — `image_sources` 개수가 중앙 4.5·최대 8이라 **15로도 이미 전건을 훑고 있었다**(15 초과 기사 0건) = 「뒤쪽의 관련성 약한 소스를 억지로 끌어온다」는 위험은 현행 데이터에선 발생하지 않는다(소스가 늘면 그때 재검토) ⓒ **대표 승계는 실재하는 관련성 비용** — 원기사 og 가 720 미달이면 대표가 관련기사 사진으로 넘어간다(실측 19기사 중 **7건**). 원기사 사진이 가장 관련성 높으므로 이건 「예외 없이 배제」 지시의 **필연적 대가**다(참조 얼굴 축은 `THUMB_REF` 기본 OFF라 무영향) ⓓ **프롬프트 순위 = 관련성 1번·개수 2번** — 구판은 「개수를 넉넉히 내야 하는 이유」가 헤드라인이라 개수 압박이 먼저 읽혔다 → 「관련된 게 3개뿐이면 3개만 내라·개수를 채우려고 억지로 늘리지 마라」를 1순위로 못박고 `WANT*3`은 **상한이지 할당량이 아님**을 명시 ⓔ **크기 미상 = 1회 재시도 후 컷** — 단독 프로브는 8/8 성공인데 같은 서버에 연속 요청이 몰리면 순간 거절(Errno 104)이 나고, 그대로 컷하면 **관련성 좋은 멀쩡한 사진이 네트워크 딸꾹질로 버려진다**(실측 = 문화일보 1200×801 이 「미상」으로 잘렸다 → 재시도 도입 후 대표 복구). 재시도 뒤에도 미상이면 컷(실측 잔여 4건). ⚠ **보충 = 다회전(운영자 260810 5차 「해소 ㄱㄱ · 어짜피 화질이 작잖아? 썸네일로 쓰려고 지금 모으는건데, 다운로드 해봤자 깨져서 의미가없어」)** — 구판 보충은 **딱 1회**만 돌았다. 문턱 도입 뒤로는 후보의 절반 가까이가 화질에서 잘려나가 1회로는 자리가 안 찬다(실측 = 기사당 2.55장 · 0장 기사 2건). **대표가 관련기사로 밀리는 것도 결국 「그 자리를 채울 좋은 사진을 덜 찾아본 것」이라 같은 축**이고, 운영자 판단 = 작은 사진은 **어차피 썸네일로 못 쓴다**(다운로드해도 깨진다) → 살리지 말고 **더 찾아라**. → `more_images._again()`이 미달이면 다음 라운드를 마커로 예약하고 `moreimg.yml` 재발사 스텝이 쏜다(발사 문법 = news-analyze.yml 「검색이미지 미달 자동 보충」 100% 계승 · 창작 0 · 즉발 금지 = 다음 런 체크아웃이 이번 search.json 을 봐야 add/add 경합 0). ⚠ **정지 조건 3중이 실효 조건**(비용 폭주 차단 = Claude 콜 상한) = ⓐ 목표 7장 달성 ⓑ 라운드 상한 `MOREIMG_MAX_ROUND`(3) ⓒ **이번 라운드 수확 0 = 소스 소진**(더 돌려도 같은 결과인데 콜만 태운다) · 라운드마다 `exclude_srcs`가 누적돼 Claude 가 **매번 새 소스**를 찾는다. 실증 = 정본 함수 재판정 5케이스{목표달성·상한도달·소진 = 전건 정지 / 정상·2라운드 = want·round 정확 증가} + 킬테스트(재발사 스텝 무력화 → rc=1 정확 검출). ⚠ **보충이 실제로 몇 장을 회수하는지는 미확인** — 그 층은 워크플로 축(Claude WebSearch)이라 로컬 재현이 안 된다. 첫 실운영 회차에서 확인할 것. ⚠ **문턱 = `img_sizes.COLLECT_MIN_H` = 600**(운영자 260810 4차 「세로 600 전후로 할게」 · 값 내력 720→600). ⚠ **산출 해상도 사다리(`SIZE_SHORT`)와 다른 축이라 상수를 분리했다** — 그쪽은 「우리가 만들어 내보내는 크기」이고 이건 「남의 사진을 받아올지 말지의 하한」이다(같은 키를 재사용하면 AI생성 티어를 만질 때 수집 문턱이 딸려 움직인다). 600 실측(같은 22기사) = 최소 600 · 세로 중앙값 800.5 · **기사당 장수 2.27→2.55** · 0장 기사 3→**2건**. ⚠ **대표 승계는 600으로 낮춰도 안 줄었다**(19기사 중 7건 유지) — 승계를 유발하는 원기사 og 는 실측 399·300·360·366·576 로 **600에도 한참 못 미치는 진짜 축소판**이라 문턱을 내려도 살아나지 않는다(그 축을 없애려면 대표만 예외로 두는 별건 결정이 필요 = 운영자 판단 대기). 킬스위치 = `THUMB_UPSIZE=0`(종전 동작 100% 복귀) · 후보 상한 `THUMB_UPSIZE_MAX`(기본 4). ⚠ 신설 사유(게이트) = **이 축은 조용히 죽는데 화면 증상이 0이다** — `_best_variant` 호출 한 줄만 빠져도 파이프는 정상 동작하고 로그도 정상이고 이미지도 나온다, 화질만 종전으로 되돌아간다 = **운영자 눈이 유일한 검출기**(insta-thumb-miss·brk_misfire 동축). 기존 게이트는 전부 다른 축(`check_image_format` = 포맷·품질 q90 · `check_thumb_redo_append` = 수정 누적 · `smoke_*` = 화면 렌더) → 「가져온 이미지가 그 사진의 **가장 큰 판인가**」는 축 자체가 없었다. 3축 하드(① 승격 3부품 실존 ② `fetch_article_images` 안 `_best_variant` **2회**(대표 경로 + 관련소스 경로 — 한쪽만 걸면 나머지가 조용히 종전 화질로 남는다 = 이 레포 최빈 미러 드리프트) ③ 720 SSOT 경유) · 정적 · 렌더·LLM·네트워크 0 · **면책표 없이 하드 0**. 실증(실사용 조건 = art_url + image_sources 동반 · 22기사 90장) = **720 이상 31%→54% · 세로 중앙값 491→720 · 기사당 장수 4.14→4.09**(장수 유지 = 관련성 손실 0) · 킬테스트 2종 전건 검출{관련소스 경로 승격 제거 · 720 SSOT 경유 제거} + 복원 위양성 0. ⚠ **54%는 100%가 아니다** — 나머지는 **매체가 애초에 큰 사진을 웹에 안 올린 건**이다(승격 후보가 404·403이거나 og가 이미 원본인데 작다 = 실측 뉴스핌 400×260·뉴시스 720×372·인천일보 300×159). 없는 화소를 만드는 건 다른 축(AI 업스케일 = `img-upscale.yml` 별건)이고, 이 봉합의 계약은 「**있는데 안 집어오던 것**을 집어온다」까지다. ⚠ **남는 한계 = 이미 R2에 구워진 과거 이미지는 안 바뀐다**(기계산출물 · 새로 만드는 기사부터 적용 · 과거분은 뷰어 「다시 만들기」). ⚠ 승격이 불가능한 매체가 실재한다(원본이 CDN에 없거나 403) — 그 경우 종전 화질 유지 = 놓침은 안 는다.
- **[뉴스 픽 AI 썸네일 프롬프트 = 자기모순 0 = `check_thumb_prompt_sanity`(운영자 260805 "아이디어 ㄱㄱ")]** 픽 → `thumb_gen.py`가 조립해 발사하는 프롬프트가 **스스로를 무효화하는 두 문장**을 품고 있었다 — 실측(실제 발사분 `cards/*/thumbs/prompts.json` 236건 재판독) = ① **CAMERA 자기모순 6%**(한 줄 안에서 `front-on … symmetrical` ∧ `not a flat head-on` = 「평면 탈피」 꼬리절이 **무조건** 붙어 AG 정면 코드와 충돌 → 모델이 둘을 평균내 어중간한 각도로 도망간다) ② **EXPRESSION 시선 중복 10%**(EM 코드가 CAMERA가 이미 말한 응시를 재지시 → 같은 지시 2회에 표정 축이 밀린다) ③ **MOOD 메타어 59%**(emotion frontmatter가 작성 지시 「1순위 감정 + …」를 받아 값에 'N순위·포지셔닝·독자·스크롤'이 그대로 붙어 온다 — 라벨이 `the reader's dominant emotion`이라 더 헷갈리는 순수 노이즈) ④ **SUBJECT 문장 통째 98%**(likeness 화풍의 「이 공인 얼굴을 그려라」 자리에 **한줄요약 전문**이 실려 그 문장의 날짜·장소·발언이 **제2의 장면**으로 SCENE과 경합 — 실측 「청와대 영빈관」 vs SCENE 「창 없는 대형 홀」) ⑤ **SCENE 위계 부재**(연출 줄이 6~8개 붙어 각자 장면을 암시하면 모델이 '평균'을 그린다 — STAGING·FOCUS엔 `adapt this motif` 래핑이 있었는데 CAMERA·LIGHT·EXPRESSION엔 없었다). 봉합 = `_FRONTAL_RE` 조건부 꼬리절(거리 병기 `_SHOT_RE`와 동일 문법 · 정면 건은 그 정면을 굳히는 **긍정문** 교체 = 부정 프라이밍 0) · CAMERA 정면일 때만 EM 시선 절 절삭(표정은 보존) · `_mood_clean` · `_subject_name`(「인명+직함」만 · **4겹 게이트** = 성씨 사전·조사꼬리·조직접미·지역명 · 못 찾으면 **줄 자체 생략** = 문장 통째 주입 금지) · `SCENE_PRIME` 1줄 신설. ⚠ 신설 사유 = 기존 게이트가 전부 다른 축이다 — `check_refs` 계열 = **정적 문자열**, `smoke_*` = **화면 렌더** → 「러너가 실제로 조립해 발사하는 프롬프트가 스스로 모순인가」는 축 자체가 없었고, 운영자에게는 「이미지가 별로다」로만 보여 원인 판별이 사람 눈에만 의존했다(insta-thumb-miss·brk_misfire 동축 = 조용히 나빠지는 것을 숫자로 드러낸다). 판정 = **정본 함수 재판정**(`check_disaster_landmark_sign` 문법 계승 · `build_prompt`를 import해 대표 5케이스(정면 AG-08·응시 EM-12 등 **실측 사용빈도 상위 코드**) × 전 화풍으로 실제 조립 후 5술어 검사 · 사전·정규식 사본 0 · 네트워크·LLM·렌더 0 · **면책표 없이 하드 0**) — 케이스는 **라이브러리 실코드(AG/LGT/SG/EM TSV)** 를 그대로 태우므로 새 연출 코드가 들어와도 같은 술어로 재판정된다(「메뉴만 늘리고 조립을 안 본」 회귀 차단). ⚠ **짝 축 = 구 발사분 잔류는 WARN(비차단)** — `prompts.json`은 기계산출물·과거 발사분이라 세션이 커밋으로 못 씻는다 → 하드로 두면 봉합 커밋 자신이 막히는 자기모순(`check_disaster_lm_stale` ② 선례) → 「씻길 때까지 보이게」가 정확한 역할(260805 최근 120기사 실측 = MOOD 132 · SUBJECT 120 · EM 28 · CAMERA 14 · 새 픽이 돌면 자연 감소 · 옛 기사는 뷰어 「다시 만들기」). ⚠ **남는 한계** = 희귀 성씨·조사 글자로 끝나는 인명(‘김지은’)은 `_subject_name`이 놓쳐 SUBJECT 줄이 생략된다 = **안전측 실패**(참조 얼굴 첨부는 불변). 실증 = 재조립 실측(현행 queue) ①②③④ 잔존 0 · ⑤ 전건 주입 · SUBJECT 53건 전건 실인명(1차 시도의 오탐 22건 「개표에서 선관위가 경기·전북 교육감」 류는 4겹 게이트가 전건 제거).
- **[게이트 실효성 원장 = `shared/gate_hits.jsonl` · 게이트 `check_gate_hits`(운영자 260805 "돌리고 머지 ㄱㄱ" · `check_contract_anchors`의 짝)]** ⚠ 신설 사유 = **85개 게이트가 도는데 「어느 게이트가 실제로 무언가를 잡은 적이 있는지」를 아무도 몰랐다.** 260805 하루만 봐도 `check_image_format`은 켜자마자 track q82 2건을, `check_contract_anchors`는 **자기 설계 오류 2건**을 잡았다 — 반면 **한 번도 아무것도 못 잡은 게이트**가 몇 개인지는 미확인이었다. 그런 게이트는 ⓐ 진짜 청정한 축이거나 ⓑ **판정이 헐거워 아무것도 안 걸리는 죽은 게이트**인데, 계측이 없으면 둘을 구분할 방법이 없다 = **게이트 수가 늘수록 pre-commit만 느려지고 안심은 가짜가 된다**(260804 실측 = 총 11.4s의 근거 있는 감량 수단이 없었다). 3게이트 삼각 = `check_gate_docs`(문서에 적혔나) · `check_contract_anchors`(배선됐나) · **이것(일을 하나)**. 동작 = **전 `check_*`를 모듈 레벨에서 자동 래핑**(`_gate_hits_install()` = main 진입 첫 줄 1회)해 rc≠0 순간을 원장에 1줄 적재 → 관측 `_GATE_HITS_MIN_DAYS`(90일) 초과 시 **0회 발화 게이트 목록**을 커밋 출력에 띄운다. ⚠ **호출부를 한 줄도 안 고치는 게 요점** — 러너의 `if check_x() != 0: rc = 1` 이 85번 반복되는 구조라 손으로 감싸면 diff 85곳이고 **새 게이트가 계측을 빼먹는 게 기본값**이 된다(= 이 원장이 막으려는 병과 같은 축) · `globals()` 치환이라 신규 게이트도 자동 편입 · 소스 텍스트를 읽는 게이트(`check_gate_docs`·`check_contract_anchors`)는 `def` 선언 정규식이라 래핑 무관(`functools.wraps`로 `__name__` 보존). ⚠ **WARN·비차단**이 정확한 역할 — 미발화가 곧 결함은 아니다(청정한 축일 수 있다) · 하드로 두면 레포가 언다(`check_component_lock`·`check_disaster_lm_stale` ② 선례). ⚠ **관측 90일 미만이면 미발화를 보고하지 않는다** — 원장 신설 직후엔 전건이 미발화라 그대로 띄우면 「85개 전부 죽었다」는 **거짓 신호**가 된다(정직한 유예). 킬스위치 = `GATE_HITS=0` · 전 경로 fail-soft(계측 실패·손상 줄이 게이트를 못 죽인다) · 롤링 2000줄(`_meta` 첫 줄 항상 보존) · ⚠ 원장 = **기계산출물 손편집 금지**. ⚠ 적재 타이밍 = pre-commit 훅이 발화를 적재하는 시점은 이미 `git add` 뒤라 **그 회차 커밋엔 안 실리고 다음 커밋에 딸려간다**(무해 · 표본 통계축) · 병렬 세션 동시 쓰기는 충돌 가능하나 유실돼도 무해(어느 쪽이든 표본 1건). 실증 = 킬테스트 6종 전건{관측 유예 10일차 미보고 · 120일 경과 0회 발화 목록 WARN · 킬스위치 · 손상 줄 fail-soft · 원장 없음=관측 시작 · 롤링 상한+`_meta` 보존} + 적재·읽기 라운드트립 실측.
- **[계약 앵커 = 「강제 없는 선언」 차단 = `check_contract_anchors`(운영자 260805 "머지 ㄱ" · `check_image_format`의 짝)]** 문법 = 주석에 **`CONTRACT: <게이트명>`**(다중은 콤마) → 그 계약을 강제하는 게이트가 `shared/check_refs.py`에 **실존**하고 **러너에 배선**돼 있어야 통과. ⚠ 신설 사유 = **이 레포의 계약은 주석으로 선언되는데 주석엔 강제력이 0이라 조용히 낡는다** — 실사고(260805 실측) = `gen_image.py`가 260710에 「전 JPEG 저장 경로 통일」을 **주석으로 선언**해 놓고 `resize_image`(92)·`upscale_image`(94)·`recompose_card`(95)·`card_news`(95)·`img_mosaic`(92)가 **6주 동안 안 따라왔다**. 선언도 있었고 위반도 있었는데 **그 사이를 잇는 것이 없어서** 운영자가 "모두 jpg 90으로"라고 말해줄 때까지 아무도 안 울렸다. 이 레포엔 그런 선언형 주석이 수백 개인데(260710·260716·260802…) 어느 게 살아있고 어느 게 죽었는지 **기계로 아는 방법이 없었다**. 2축 = ① **앵커 → 게이트 실존·배선**(고아 앵커 = 계약을 만들 게이트가 없다 · 죽은 앵커 = 게이트는 있는데 안 돈다 → 둘 다 차단 · 게이트를 지우면 그 앵커들이 고아로 떠서 「폐기됐다」가 드러난다) ② **게이트 → 배선**(정의만 되고 호출 0건 = 「만들어놓고 안 돎」 → 차단). ⚠ 짝 `check_gate_docs`는 **문서 등재**만 본다(이름이 CLAUDE.md에 적혔나) — 「그래서 **실제로 도는가**」는 축 자체가 없었다(문서에만 있고 안 도는 게이트 = 가장 비싼 거짓 안심). ⚠ **구분자는 콤마 하나뿐** — `·`를 허용했더니 이 레포의 일반 구분자(`CONTRACT: check_x · q90 = …`)를 먹어 뒤 낱말까지 이름으로 읽었다(**게이트 첫 실행이 자기 설계 오류를 `q90` 고아 앵커로 검출** = 실측 봉합). 판정 = 정적(렌더·LLM·네트워크 0) · 표면 자동 발견(`git ls-files`) · **면책표 없이 하드 0**(260805 = 앵커 6건 전건 유효 · 미배선 게이트 0 · 정의 85개 전부 가동 · 원장 증가 0). ⚠ 스코프 밖 = `_versions/`·`docs/`·`cards/`·`.claude/` · `shared/check_refs.py` 자신(처방문·정규식 리터럴이 곧 앵커 = `check_ytdlp_aac` 선례) · **`.md` 전량**(문서의 처방문 예시가 곧 앵커 — 이 항목 자신이 첫 실행에서 고아 앵커로 검출됐다 · 앵커의 거처는 **강제할 코드 옆**이고 문서 축은 `check_gate_docs` 전담). ⚠ **남는 한계 = 앵커를 「안 다는 것」은 못 막는다**(주석 문법 채택은 자발) — 막는 건 「달아놓고 강제가 없는 것」·「강제가 사라졌는데 선언만 남은 것」 뿐. 씨앗 6건 = 6주 드리프트가 **실제로 났던 그 줄들**(gen_image 진범 줄·thumb_gen `to_jpg90`·tr `TR_JPGQ`·card_news·img_mosaic 누끼·track-make yt-dlp 셀렉터) · 새 계약을 주석으로 선언할 땐 앵커를 같이 단다. 실증 = 킬테스트 6종 전건{고아 앵커·러너 배선 제거(죽은 앵커+미배선 동시)·미배선 신규 게이트 = 3종 검출 / 콤마 다중 앵커·앵커 뒤 `·` 산문·무변경 = 3종 무경보}.
- **[이미지 산출 포맷 2갈래 = `check_image_format`(운영자 260805 "아이디어 ㄱ" · 같은 날 JPG q90 통일의 짝)]** 계약 = **투명·무손실이 정당 사유인 축만 PNG · 나머지 산출은 JPEG q90 단일**(정본 = `gen_image.post_process` `quality=90, subsampling=0, optimize=True`). 2축 = ⓐ **PNG로 굽는 곳은 왜 그래야 하는지가 코드 옆에 있어야 한다** — 사유 어휘(투명·RGBA·마스크·오버레이·누끼·클립보드·아이콘·무손실·도먼트)가 호출 줄 **상행 5줄** 안에 **긍정문으로** 있어야 통과 ⓑ **JPEG 품질 = 90 단일** — 산출이 아닌 인코딩(화면 뒤 블러 장식·LLM 첨부 압축)은 그 줄에 `q-ok: 사유` 마커(CSS `raw-ok`/`reuse-ok` 관례 계승). ⚠ 신설 사유 = **「우리가 굽는 바이트가 무슨 포맷·무슨 품질인가」를 보는 축이 하나도 없었다** — 실측 2종: ⓐ `thumb_gen.py`가 R2 키를 항상 `.png`로 굽는데 실물은 Gemini가 준 JPEG = **거짓 확장자가 6주 넘게 무증상**(Content-Type은 매직바이트로 맞게 나가 화면이 멀쩡했다 = 운영자 눈에도 안 보인다) ⓑ JPEG 품질이 **90·92·94·95 4갈래**로 갈렸는데 `gen_image.py` 주석은 260710에 이미 "전 JPEG 저장 경로 통일"을 선언한 상태 = 선언과 실물의 6주 드리프트. 기존 게이트는 전부 다른 축(`check_refs` 계열 = 정적 **문자열·경로 실존** · `smoke_*` = **화면 렌더** · `check_ytdlp_aac` = **받는 쪽** 코덱). **첫 실행이 곧바로 `apps/track/track_analyze.py` q82 2건을 검출** — 같은 날 손 전수감사가 `quality=`·`jpg_bytes` 어휘만 훑어 `cv2.IMWRITE_JPEG_QUALITY, 82`를 통째로 놓친 자리다(사람 눈의 한계 실증 · insta-thumb-miss·brk_misfire 동축 = 조용히 나빠지는 것을 숫자로 드러낸다). 판정 = 정적(렌더·LLM·네트워크 0) · 표면 자동 발견(`git ls-files` = 새 생성기가 조용히 못 빠진다) · **면책표 없이 하드 0**(원장 증가 0 · 현행 인코딩 호출 35건 전건 정합). ⚠ 스코프 밖(구조적) = `_versions/`·`docs/`·`cards/`·`.claude/`(스냅샷·산출 자산) · `shared/smoke_*`·`*shot*`(테스트 하네스가 굽는 프로브는 산출물이 아니다) · `shared/check_refs.py` 자신(처방문 예시가 곧 위반 = `check_ytdlp_aac` 선례) · **주석 줄 전량**(py `#` · js `//`{`https://` 배제} · 블록 주석). ⚠ 남는 한계 = 변수 경유 품질값은 **같은 파일 1회 추적**까지만(다른 모듈 import 상수는 못 따라간다) = **fail-open**(위양성으로 레포를 얼리는 것보다 놓치는 쪽이 싸다). 실증 = 킬테스트 9종 전건{사유 없는 새 PNG 생성기·리터럴 q95·**변수 기본값 q94**·뷰어 toBlob PNG·유일 사유 주석 제거 = 5종 검출 / q-ok 마커·주석 속 예시(py·js)·무변경 = 4종 무경보}. ⚠ **부정문 무효화**가 실효 조건 — 「번역 카드는 투명 영역이 **없다**」 같은 JPG 쪽 설명이 PNG 사유로 통과하던 구멍을 킬테스트 K4가 실측으로 잡았다(어휘 존재만 보면 반대 뜻 문장이 면죄부가 된다).
- **[다운로드 오디오 코덱 = AAC 강제 = `check_ytdlp_aac`(운영자 260805 "유튜브를 편집가능한 자료까지 받아오게")]** 받은 mp4를 프리미어(·베가스·다빈치 일부)에 임포트하면 **영상만 올라오고 오디오 트랙이 아예 안 붙었다 · 에러도 안 뜬다.** 2단이 겹쳐야 터진다 = ⓐ yt-dlp 기본 정렬의 acodec 우선순위가 `flac>wav>opus>vorbis>aac>mp4a`라 알몸 `ba`가 유튜브 **251(Opus 141k)** 을 **140(AAC 129k)** 보다 먼저 집는다 ⓑ `--merge-output-format mp4`를 주면 `yt_dlp/utils/_utils.py` `get_compatible_ext()`의 `return 'mkv' if allow_mkv else preferences[-1]`이 걸려 **allow_mkv=False → mp4가 Opus와 비호환인 걸 알면서도 mp4를 반환**한다(실측 = `get_compatible_ext(vcodecs=['avc1'],acodecs=['opus'],vexts=['mp4'],aexts=['webm'],preferences=['mp4'])` → `'mp4'` · `preferences=None` → `'mkv'`) → 어도비 공식 지원 오디오 목록(AAC/AIFF/AIF/ASND/BWF/M4A/MP3/WAV/OMF)에 Opus가 없어 **컨테이너가 mp4라 임포트는 되는데 오디오 스트림만 조용히 무시**된다. ⚠ **가장 조용한 실패** = 옵션을 안 주면 mkv로 떨어져 바로 눈치채는데 mp4로 나오니 정상 파일로 보이고, 팟플·VLC·인스타 업로드는 전부 멀쩡하다 = 운영자가 편집 툴을 열어야만 발견. 처방 = **셀렉터에서 알몸 `+ba` 앞에 같은 가지의 `+ba[ext=m4a]`(=AAC) 사본을 세운다**(`bv*+ba/b/best` → `bv*+ba[ext=m4a]/bv*+ba[ext=mp4]/bv*+ba/b/best` · 해상도 제한판도 동일 · **비디오 선택자 `bv*`는 무접촉 = 화질 무변** · 실측 `401+251 acodec=opus 3840x2160` → `401+140 acodec=mp4a.40.2 3840x2160`). ⚠ **금지 2종(운영자 실측)** = `-S "acodec:aac"`는 acodec 정렬이 res를 앞질러 **format 18(640x360) 통합포맷이 이겨 화질이 통째로 망가진다** · `--postprocessor-args "Merger:-c:a aac"`는 이미 AAC인 파일까지 매번 재인코딩(시간·음질 손해). ⚠ 신설 사유 = 기존 게이트가 전부 다른 축이다(`check_workflow_yaml` = 문법 · `check_paths` = 경로 실존 · `smoke_*` = 화면 렌더) → 「받은 파일이 편집 툴에 물리는가」는 축 자체가 없었고, 호출부가 4문법(yml·py·bat·command) 10곳에 흩어져 새 호출부가 조용히 빠질 수 있었다. 판정 = **가지 단위**(`/`로 쪼갠 각 가지 중 알몸 `+ba`를 쓰는 가지는 `+ba`만 `[ext=m4a]`로 바꾼 쌍둥이 가지가 **앞에** 서 있어야 한다) — 「어딘가 앞에 m4a가 있으면 통과」로 두면 **다른 가지**의 m4a가 뒤 가지의 알몸 ba를 가려 미검출이 난다(실측 킬테스트 K4·K7이 그 구멍을 잡았다 = 앞 가지는 mp4 비디오가 없으면 통째로 실패하고 뒤 가지가 실제 착지점이 된다). 표면 자동 발견(`git ls-files` 중 `bv*`∧`+ba` 보유 · 정적 · 렌더·LLM·네트워크 0 · **면책표 없이 하드 0**) · 예외 2종은 **구조적**(주석·독스트링{`ast` 기반} / `--skip-download`·`--print` = 미디어를 0바이트도 안 받는 호출) · 자기참조 배제 = `shared/check_refs.py` 자신(처방문의 「수정 전」 예시가 곧 위반 문자열 · `check_clip_coverage` 템플릿 배제 선례). 파이썬 암묵 문자열 연결은 병합해 판정한다(2줄로 쪼갠 셀렉터의 위양성 차단 · 별개 할당은 안 잇는다 = 위반 은폐 0). 검증 = `ffprobe -hide_banner -i <파일>.mp4 2>&1 | grep Audio` → `Audio: aac` 정상 / `Audio: opus` = 미적용. ⚠ **남는 한계** = 소스에 AAC 자체가 없는 사이트(Opus/Vorbis만 제공)는 폴백으로 종전대로 Opus → 받은 뒤 수동 변환 `ffmpeg -i in.mp4 -c:v copy -c:a aac -b:a 320k -ar 48000 out.mp4`(비디오 copy = 화질 손실 0). 실증 = 킬테스트 9종 전건 검출(4문법·2줄분할·가지별) + 위양성 대조 8종 무경보.
- **[리베이스 후 재검증 = 푸시 게이트(운영자 260802 승인)]** 커밋 때 통과한 게이트가 **리베이스 자동병합에서 되돌아간 채 main에 들어가는 사고**가 실재한다(260802 실측 = thumb 빈 캡슐 술어 1줄 구판 복귀 · 원인 = 커밋→리베이스→푸시에서 **리베이스 뒤에 게이트가 없었다**). 이 레포는 봇·병렬 세션 커밋이 초당급이라 [7-5] 재시도 리베이스가 상시 = 구조적 재발. → **`.githooks/pre-push`가 push 직전 `check_refs`를 다시 돌린다**(CI 러너 제외 = pre-commit 동축 · 우회 = `git push --no-verify`). 셋업 = `git config core.hooksPath .githooks` 1회(pre-commit과 공용). ⚠ 훅은 최후 방어선이지 면죄부가 아니다 — **머지 절차 [7-5]는 ②③(fetch·rebase) 다음에 게이트 재실행을 넣은 순서로 수행한다.**
- **[상태 의존 화면 = 픽스처로 재현(절대규칙 · 운영자 260803 "그 부분까지 해줘")]** 수집함 카드처럼 **지금 라이브 데이터에 없는 상태**(긴급<4h·Failed·Picking…·PICKED)를 검증할 땐 가짜 DOM을 손으로 심지 말고 `node shared/fixture_shot.js` 를 쓴다(픽스처 = `shared/fixtures/scrap_states.json` · 산출 = `docs/_shots/fixture/scrap_<상태>.png` + 상태별 렌더 판정표 · rc=1 = 미렌더). 동작 = viewer/ 를 임시폴더로 복사해 `candidates.json`·`picks-failed.json` 교체 + `api/pending` 라우트 스텁 + `nm_ratings` 시드 → **뷰어 자신의 렌더 경로**(makeScItem·failBotHtml·wireFailBot)로 띄운다 = 프로덕션 코드에 픽스처 분기 0 · 라이브 viewer/ 는 읽기만. 수리 전/후 대조 = `--viewer=<구 사본>`. ⚠ 신설 사유 = 260803 실사고 — Failed 칩 검증에 임시 span 을 심어 캡처했더니 정본(폭 전체 빗금 Failed + ✓)과 **생김새가 달라 운영자가 지적**했다(증빙이 실물과 어긋남). 첫 실행이 곧바로 함정 3종을 잡았다{ⓐ Failed 카드는 `.scrap-list` 가 아니라 **실패 트레이**(`.sc-failtray-list`)로 빠진다 ⓑ `api/pending` 응답 봉투는 `{items:[…]}` — 배열을 주면 전건 무시돼 succ 가 조용히 Picking… 으로 보인다 ⓒ succ 여도 픽 시각이 무응답 타임아웃을 넘기면 pickState 가 'failed' 를 **먼저** 반환한다}. 픽스처는 기계산출물이 아니라 **사람이 쓰는 시나리오 자산** = 손편집 정상(시각은 `_ageH`·`pickedAt` 상대값으로 두어 날짜가 지나도 안 썩는다).
- [절대규칙 · 운영자 260802] UI를 만지면 **로컬 렌더로 눈과 숫자를 먼저 본다** — 손대기 전 `node shared/preview_shot.js base` → 고친 뒤 `node shared/preview_shot.js diff`(로컬 서버 자동 기동 → **스튜디오 2셸 10탭**{이미지 5 + 영상 5} 스샷 + DOM 실측 + 기존 대비 변화표 · 한 셸만 = `--shell=thumb|cap`). 눈대중 "정렬됨" 선언 금지 · 보고 6-2 전/후 이미지도 여기서 나온 캡처를 쓴다. **영상 셸 편입 = 260802 3차**(구판은 이미지 5탭만 봐서 영상이 뒤처진 걸 한 번도 못 잡았다 — 전체창 미적용·도크 순흑 잔류가 실측기를 그냥 통과했다) · 산출 = 셸별 잉크선 + **2셸 교차 한 세로선**(§3-5) + 전체창·도크 골격표.
- [광학-잉크 판정 · 운영자 260802] 정렬 판정 축 = **박스가 아니라 글자 잉크**(Range 사각형). 박스 x가 같아도 padding이 갈리면 눈에 보이는 위치가 어긋난다(260802 사고 = 박스 4탭 동일·잉크 5px 어긋남 → 롤백) · 절대 x 금지(창 폭 따라 위양성). **기준선 = 캡슐 중심축**(운영자 260802 6차 "중앙정렬로 할게" — 구 좌변 시작선 판정은 좌측정렬 시절 축·개정) = 전 칩 |잉크 중심 − 캡슐 중심| ≤ 0.5px. 기계 판정 = `shared/smoke_parity.js` C15(이미지 4탭 한 중앙축) **+ `shared/smoke_studioshell.js` C3(2셸 10탭 교차 한 중앙축) + C7(미리보기 모듈 등가 = 창 w/h·발사 h/r/fs·돋보기 캡슐 상대좌표 · 260802 6차 신설 — 게이트가 안 재던 축이 조용히 갈라진 사고의 편입)** · baseline 초과·신규 어긋남 = FAIL · `smoke_all` 자동발견으로 상시 동행. **면책표(INK_BASE)는 해소되면 그 자리에서 비운다** — 남겨두면 같은 회귀가 조용히 재통과한다(260802 3차 = `{편집:2.6}` 회수).
- **[게시물 이름 = 표지에 박힌 제목 = `.github/scripts/insta_cover_ocr.py` · 원장 `apps/insta/data/cover_titles.json` · 게이트 `check_cover_title_chain`(운영자 260812 "이 ai 요약 안에 있는 게시물들의 타이틀, 아마 첫줄을 가져올텐데 · 게시물 오버레이나 게시물 첫장에 쓰인 타이틀(무조건 양식이 거의 고정), 헤더 포함해서 썸네일을 ocr로 읽어서 이를 가져오게 할 수 있나 · 기사 인트로 첫줄보다, 오버레이가 가장 정확한 내용이거든")]** 채널 요약이 «…» 로 게시물을 지목할 때 쓰던 이름 = `first_line(caption)` = **인스타 글의 첫 줄**인데, 노뮤트는 카드에 박는 제목과 글 첫 줄을 **각각 따로 쓴다** → 요약이 **화면에 뜬 적 없는 문장**을 게시물 이름이라고 불렀다. 260812 실측 대조(같은 게시물) = 글첫줄 「🗞 자랑한 '4대 의사 집안', 증조부는 친일단체 평의원이었다」 ↔ 표지 「하영, 집안 얘기하다 셀프 파묘 || "증조부 친일은 사실무근" || 역사학계는 "친일 맞아…"」 / 글첫줄 「🎭 형기는 끝났는데 조롱은 안 끝났다…이번엔 박하선 부부」 ↔ 표지 「고영욱, 이번엔 박하선 저격 || "감당 안 될 여자…」 — ⚠ **인물 실명이 표지에만 있다**(하영·고영욱). 운영자는 그 이름으로 「어느 카드가 터졌나」를 판단하는데 판단의 입력 자체가 어긋나 있었다. **비용 = 사실상 0** = ⓐ 읽을 바이트가 **이미 우리 것**(`viewer/insta_covers/<id>.jpg` = 260810 소유층 → 다운로드·네트워크 0) ⓑ 판독 = Claude 헤드리스(구독 축 = 종량제 0 · 벤더 신설 0 · 폴오버·계측 `shared/claude_py.run_claude` SSOT 경유 = `ask_srcocr` 문법 계승) ⓒ 미디어 id별 **1회만**(원장 적중 = 재판독 0) → 첫 회차 2콜, 이후 신규분(하루 1~3장) 1콜 ⓓ 브리프 레인에 claude CLI 가 이미 깔려 돈다 = 설치 스텝 신설 0. ⚠ **원장이 영속층인 이유** = 커버 파일은 화면 12칸분만 남고 밖은 **삭제된다**(insta_signals 롤링) → 삭제 전에 읽어 텍스트로 남기면 그 제목은 영구히 안 사라진다. ⚠ **표시 축 전용 = 분류 무접촉** — `naming_style`·`category`·`feats` 입력은 종전대로 캡션 첫 줄이다(갈아끼우면 과거 게시물 전건이 다른 라벨로 재계산돼 성장 3기·주제 비중 시계열이 통째로 갈린다 = 요약이 읽는 그 시계열) · 게이트가 그 오염을 하드로 막는다(K4 검출 실증). ⚠ **사진에 원래 박힌 글자와 갈라야 한다**(운영자 "썸네일에 자막이 포함될수있으니") — 커버엔 현수막·간판·방송 자막·언론사 워터마크가 같이 찍혀 있다(실측 = 전장연 커버의 「이동권보장 중앙정부 책임」 현수막, 북한 커버의 「조선중앙통신」) → 프롬프트가 제작자 오버레이만 집게 하고, 못 가르면 `(없음)` 으로 빠진다(안전측 실패 = 캡션 폴백). ⚠⚠ **첫 실호출이 자기 결함을 즉시 검출**(실측 봉합 · 실호출 안 했으면 못 잡았다) = 프롬프트가 양식을 「큰 흰 글자 한 줄 + 초록 한 줄」 **2단·색 고정**으로 단정했는데 실제 표지는 **줄 수가 가변이고 색 배치도 카드마다 다르다**(실측 3단 = 초록·흰·초록) → 모델이 그 양식에 억지로 맞추느라 **순서를 뒤집고 가운데 줄을 잃었다**(「지방 취업 청년 지원금 '더블 인상' || 월 30만 원이 80만 원으로,」 = 읽는 순서 역전 · 정답 = 「월 30만 원이 80만 원으로, || 지방 취업 청년 지원금 || '더블 인상'」) — 진범은 모델이 아니라 **양식을 고정으로 못박은 프롬프트**였다 → 색·줄수 단정을 걷어내고 「**위치(위→아래)만이 순서다 · 색으로 순서를 정하지 마라**」로 교체. 체인 = 판독기 → 원장 → `insta_signals` 부착 **3자리**(enrich=posts · post_refs 수집 · post_refs 배송) → 소비 3표면(`chan_brief.sh` 일별 「올린 것」 · `brief_lib.trendctx` 터진 게시물 · `algo_ledger` 원장 `ovt`) · 워크플로 = 판독 + **신호 재계산**을 브리프 **앞**에(뒤면 그 회차 요약엔 안 실려 항상 한 회차 늦는다) · 전 경로 fail-soft(판독 실패 = 종전 이름 그대로). ⚠ 신설 사유(게이트) = **이 체인은 조용히 죽는데 화면 증상이 0이다** — 한 층이 빠져도 요약은 정상 생성되고 게시물 이름도 정상으로 보인다(글 첫 줄이니까), 그냥 **덜 정확한 이름**으로 되돌아갈 뿐이라 로그·에러 어디에도 안 남는다(insta-thumb-miss·brk_misfire 동축). 기존 게이트는 전부 다른 축 — `check_brief_lib` = 과거 회차가 프롬프트에 실리는가 · `check_algo_ledger` = 원장 불변식 · `check_thumb_chain` = 커버 **이미지**가 화면에 뜨는가 → 「그 커버에 **적힌 글자**가 이름이 되는가」는 축 자체가 없었다. 판정 4축 하드(판독기 골격 / 소비 3표면 / 워크플로 스텝·순서·재계산 / 원장 커밋 인자) · 정적 · 렌더·LLM·네트워크 0 · **면책표 없이 하드 0**(부채 원장 증가 0) · 실증 = 실호출 12/12장 판독 + 킬테스트 4종 전건 검출{재계산 누락·소비 원복·부착 자리 1곳 제거·분류 축 오염} + 복원 위양성 0. ⚠ **남는 한계** = ⓐ 커버 소유가 260810 신설이라 **그 이전 게시물은 표지 파일이 없다** → 글 첫 줄 폴백 유지(실측 부착 10/100) · 회차가 지날수록 자연 증가 ⓑ ` || ` 구분자가 요약 프롬프트에 그대로 노출된다(다이제스트에 「표지에서의 줄바꿈」이라 명시) ⓒ 뷰어 심층 모달·타일은 종전 이름 그대로(표시 축 확장 = 별건).
- **[채널 요약 지식 라이브러리 = `apps/insta/brief_lib.py` · 게이트 `check_brief_lib`(운영자 260808 "3일치 요약이 매번 누적돼서 이 요약의 지식본이 되었으면 · 매번 판단이 그때그때 참고 지식이 없어서 새로 시작하는 것 같다 · 쌓이면 경쟁력")]** ⚠ 신설 사유 = **아카이브는 이미 쌓이는데 아무도 안 읽고 있었다.** 260808 실측 = `viewer/chan_brief_log.jsonl` **24회차·215KB**(FB 16회차)가 git 추적으로 살아 있는데 소비처가 0이고, 다음 회차 프롬프트에 실리는 건 `chan_brief.sh` PREV_TXT = **직전 1회차 text 앞 1500자**뿐이었다 — 브리프 전문이 4,187자라 **64% 절단 · 컷 지점이 [28일] 중간** = **[3개월]·[전체]·[총론]이 통째로 증발**. 총론 = 채널 정체성·비전·미션 = 가장 오래 가는 판단인데 **매 회차 백지에서 다시 썼다.** 증상 2종(둘 다 운영자 눈이 유일한 검출기) = ⓐ **정체성 표현 8일 8변**(08-01 「가장 먼저 가장 짧게 옮기는 곳」 → 02 「퍼나르는 속보」 → 03 「속보 반사신경」 → 04 「속보 번역기」 → 05 「퍼나르기 속보 채널」 → 06 없음 → 07 「말을 얹지 않아서 퍼지는 뉴스 채널」 → 08 「해석 없는 1차 기록소」 = 같은 채널을 매일 다른 이름으로 부른다 · 실측 22회 규정 중 **21종**) ⓑ **하루 만의 정반대 제안**(08-06 「업로드 시계를 21~22시로」 → 08-07 「오후 2~6시로 당기자」 → 08-08 다시 「21~22시」 = 어제 뭐라 했는지 모르니 뒤집는다). ⚠ 발견 = **방향은 이미 굳어 있었다** — 총론 결론 10회차가 전부 같은 내용(「속보로 벌고 + 문화·확장문으로 문 열기」)인데 **표현만 매번 새로 지어냈다** = 축적이 아니라 리셋으로 보이던 진짜 이유. 라이브러리 4층(LLM 0콜·네트워크 0·stdlib only · 파이썬이 추출·집계 전부 · 모델은 읽고 판단만) = ① **정체성 궤적**(총론 1층 강조 `*…*` 시계열 · ⚠ 숫자로 여는 강조는 수치 쪽이라 제외 = FB 「30대 중반에서 50대 초반이 절반 이상」 오탐 실측 봉합) ①-b **총론 방향 궤적**(마지막 `→` 결론 = 반년~1년 나침반) ② **반복 제안 = 확신도**(같은 제안이 나온 회차 수 · 3회+ = 이미 여러 번 말한 것 → 복창 말고 실행 여부를 데이터로 확인) ③ **⚠ 갈린 축**(최근 7회차에서 같은 축인데 값이 2종+ → 나란히 보여주고 「오늘 결론 내거나 조건부로 정리하라」) ④ **직전 전문 3000자**(구판 1500 → 전 섹션이 들어가는 크기 = 이 모듈의 존재 이유). ⚠ **파이썬은 「모순이다」라고 단정하지 않는다** — 축 분류는 사전 기반이라 오탐이 구조적이므로 「같은 축에서 이런 제안들이 나왔다」까지만 사실로 내고 판정은 데이터를 쥔 모델 몫([1] 정직) · 값이 안 뽑히는 제안은 **갈림 판정 제외**(반복 카운트에만 참여) = 안전측 실패. ⚠ **첫 실행이 자기 결함 3종을 즉시 검출**(전부 실측 봉합) = ⓐ 총론 `→`(방향 1줄)를 [전체] `→`(실행 전략)와 안 갈라 **×15 거짓 반복** — 총론 결론들이 「그래서무엇을…」 접두를 공유해 한 키로 뭉쳤다 → 섹션 분리 + 키 24→40자 ⓑ 「오후 2~6시」가 **`6-18시`로 오파싱** — 범위 매치 뒤 `6시`가 단독으로 재매치 → 범위를 먼저 소비하고 그 자리를 지운다 ⓒ **거짓 갈림** — 「문화 소재를 주 1회」·「질문형 제목을 주 2회」가 첫 매치 우선 규칙에 걸려 둘 다 [게시 빈도]로 묶였다 → **최다 매치 판정 · 동점이면 포기**(사전 순서가 곧 답이 되는 구조 제거). 배선 = IG `chan_brief.sh`(PVER v10) · FB `fb_brief.sh`(PVER v4) **같은 커밋 동반 이관**(한쪽만 고치면 나머지가 조용히 구판 = 이 레포 최빈 미러 드리프트) · 주입 = `LIB_BLOCK` → `${LIB_BLOCK}` · fail-soft(라이브러리 실패 = 빈 블록 = 종전 동작 · 1회차뿐이면 출력 0). ⚠ **게이트가 실효 조건인 이유 = 이 축은 두 가지로 조용히 죽고 화면은 둘 다 멀쩡하다** = ⓐ 쌓이는데 아무도 안 읽음(= 260808 이전 상태 · 브리프는 매일 정상 생성되니 무증상) ⓑ **호출은 사는데 프롬프트에서 빠짐**(매 회차 정상 실행되고 정상 폐기 · rc0·로그 정상 = 가장 조용한 죽음) → 4축 하드(정본 심볼+금지 심볼[네트워크·LLM·subprocess 0]+로그 경로 정합 / 2셸 실행줄 호출 / **PROMPT 안 `${LIB_BLOCK}` 실주입** / 구판 `[:1500]`·`PREV_BLOCK` 부활 차단) · 정적 · 렌더·LLM·네트워크 0 · **면책표 없이 하드 0**(부채 원장 증가 0) · 실증 = 킬테스트 7종 전건 검출{프롬프트 미주입·호출 제거·구판 절단 부활·PREV_BLOCK 부활·핵심 심볼 소실·스코프 오배선·모듈 삭제} + 위양성 대조 무경보. **⑤ 제안 성패 대조(운영자 260808 2차 "아이디어 ㄱ")** = 「무슨 말을 했나」에 「그래서 그게 옮겨졌나」를 붙인다 — `algo_runs` 원장 게시물 풀을 **제안일 기준 전 7일 vs 후 7일**로 갈라 축별 실행 비중(포맷·주제·네이밍 = 분류값 비중 / 시간대 = 발행 시각 적중률 / 빈도 = 일평균)과 성과(조회 중앙·1천뷰당 공유)를 나란히 낸다. ⚠ **파이썬은 「먹혔다」라고 말하지 않는다** — 전후 실측까지만 사실로 내고 인과 판정은 모델 몫(③과 같은 축 · 그 사이 사건·시류가 같이 움직인다). ⚠ **첫 실행이 자기 거짓말 2종을 즉시 검출·봉합**{ⓐ **창 미충족** = 후 창을 OUT_WIN 고정으로 나눠 실경과 2일을 7로 나눈 「일평균 1.71→0.43」 급감 신호를 만들었다(게시를 줄인 적이 없는데 줄어 보인다) → 제안일+7 ≤ 원장 최신 발행일일 때만 평가, 아니면 **평가 보류**로 분리 표기 ⓑ **나이 편향** = 조회는 며칠에 걸쳐 쌓이는데 갓 올린 글(실측 age_h 10.5)을 성숙분과 나란히 세워 「조회 56만→14만」을 냈다 → 성과는 **`RIPE_H`(48h) 이상 익은 게시물끼리만**, 표본 미달이면 「아직 못 잰다」로 명시}. ⚠ **dedup은 필드별 최신 비결측** — 통짜 최신 레코드로 덮으면 뒤 회차에서 None으로 비어 온 분류 필드가 멀쩡히 관측됐던 값을 지운다(실측 2건) → 지워진 값은 결측과 구분이 안 돼 비중을 그대로 거짓말시킨다. ⚠ **분류 결측은 분모에서 뺀다**(실측 34건 중 12건이 원장에 분류값 자체가 없다 — 0으로 세면 「릴스 비중이 떨어졌다」는 거짓 신호) · 뺀 건수를 블록에 같이 낸다. ⚠ **IG 전용**(원장 게시물이 IG 축 · FB는 조인 원료가 없어 자동 생략). 첫 실측 발견 = 07-30~31 제안 4건이 **전부 반대로 갔다**{질문형 25→7% · 릴스 75→58% · 14-18시 33→17% · 18-21시 29→21%} = ②의 「릴스 올리자 ×4회 반복」이 왜 반복인지의 답(안 옮겨지니 계속 다시 말했다). ⚠ **게이트 구문 축이 실효 조건** = 셸 배선이 `2>/dev/null || true`라 **구문 오류가 그대로 조용한 빈 블록**이 된다(260808 실측 = ⑤ 작업 중 IndentationError가 셸 경로에선 rc0·무출력으로 통과) → `compile()` 축 신설. fail-soft는 「원장이 부족할 때」를 위한 것이지 「코드가 깨졌을 때」를 덮으라고 있는 게 아니다. **⑥ 정체 축 → 오늘의 행동 1개(운영자 260808 3차 "아이디어 ㄱ")** = ②(반복) × ⑤(성패) 교차 = 「말은 여러 번 했는데 실제로는 안 옮겨진 축」. ⚠ 신설 사유 = ⑤ 첫 실측이 드러낸 게 「제안이 틀렸다」가 아니라 **「제안이 안 옮겨졌다」**였다(07-30~31 제안 4건 전건 실행 ▼ → 그래서 ②에 「릴스 올리자 ×4회」가 쌓였다 = **논쟁이 아니라 미실행**). ②만 보면 「확신이 굳은 축」으로 읽히고 ⑤만 보면 개별 제안의 전후일 뿐이라 **「반복 ∧ 미실행」은 두 층을 곱해야만 보인다**. 술어 = 반복 2회+ ∧ 평가된 전 회차에서 실행이 오르지 않음(after ≤ before) ∧ 평가 표본 1건+ · ⚠ **평가 못 한 제안(창 미충족·표본 미달)은 안 넣는다** — 안 옮겨진 건지 아직 모르는 건지 구분이 안 되고 모르는 걸 「안 했다」로 밀면 그 자체가 거짓 신호다. **짝 = 프롬프트 계약**(IG `chan_brief.sh` PVER v11 · FB `fb_brief.sh` v5 동반) = 「⑥이 있으면 [전체]의 `→ ` 전략 줄 **하나는 반드시** 그 축을 **오늘의 행동 한 개**로 좁혀 쓴다 — 「릴스 비중을 올리자」(방침·금지) 대신 「오늘 첫 장은 릴스 평서로 걸자」(행동·필수 = 무엇을·언제·어떤 모양으로)」. ⚠ **계약이 게이트 축인 이유** = 블록만 있고 계약이 없으면 **정보를 보여주고 아무것도 안 시키는 것** = 반복 고리가 그대로 돈다 → `check_brief_lib`가 PROMPT 안 계약 문구 실존을 하드로 본다. 첫 실측 = 릴스 축 「×4회 말했고 마지막 평가 100→57% = 안 올랐다」 정확 검출. **⑦ 터진 게시물 × 그 시각 바깥 시류 + 총론 디테일 계약(운영자 260808 5차 실측 지적 "실제로 어떤 게시물이 어땠고, 이 정도는 돼야 · 디테일이 너무 빠져서 남의 인스타 뒤지는 느낌")** = ⚠ **진범은 프롬프트 계약 자신이었다** — 구판 총론이 「개별 기간 수치를 나열하지 말고」·「수치 근거 금지」라고 **직접 시켜서** 모델이 게시물 실명·근거를 통째로 뺐고 정체성 선언문만 남았다(실측 = 08-08 총론 814자, 최근 12회차 701~965 범위 안 = 급감이 아니라 **원래 그 계약이 얇게 만들고 있었다**). 짝 사각 = 「왜 터졌나」의 절반인 **바깥 시류 원료가 프롬프트에 0**이라 알고리즘·경향 해석 자체가 불가능했다 — 그런데 원료는 이미 회차 원장에 박제돼 있었다(`tr.gt` 구글실검 25 · `gtp` 98 · `sig` 10 · `xt` 14 · `soc` 25 회차마다 = **또 「쌓이는데 아무도 안 읽는」 축**). 봉합 2축 = ⓐ `trendctx()` 신설(터진 게시물 상위 5 × 게시 시각 ±24h에 실제로 돌던 키워드 목록) ⓑ 총론 계약 개정(IG PVER v12 · FB v6 동반) = **①임팩트 문단(게시물 실명+수치 근거) ②바깥 시류 문단(탔나/무관한가를 모델이 판정 · 알고리즘은 추정 어미만) ③흐름 제안 문단** 필수 + 「수치 근거 금지」를 **마지막 `→` 1줄에만** 적용으로 축소 + 분량 6~9줄 → 10~14줄. ⚠⚠ **토큰 겹침 자동매칭은 실측으로 폐기했다** — 34게시물×25일에서 매칭 1건이 나왔는데 **전건 오탐**(캡션의 「3시」가 «새벽 3시 소아과 대기줄»에, 「서울」이 «JO1 서울 콘서트»에 걸렸다). 이걸 프롬프트에 먹이면 모델이 「신림 화재가 소아과 트렌드를 탔다」는 **날조를 근거 있는 말투로** 한다 = 얇은 총론보다 나쁘다 → **파이썬은 매칭하지 않는다**(사실 스냅샷까지만 · 판정은 캡션과 목록을 같이 쥔 모델 몫 = ③⑤⑥과 같은 축). ⚠ 게이트 축 = PROMPT 안 「임팩트 문단(필수)」 실존(계약이 빠지면 그 얇음이 그대로 재발 · K11 검출 실증). ⚠ 남는 한계 = 축 사전 6종 밖 제안은 「기타」로 빠지고(반복 카운트만 참여) · 원장 게시물 풀이 25일치라 초기 제안은 before 창 표본 미달로 보류된다(원장이 자라면 자동 해소).
- [알고리즘 인사이트 회차 원장 · 운영자 260802] AI 채널요약(chan_brief·fb_brief) 회차마다 {요약 전문 + 요약이 쓴 데이터(계정·게시물 25 절대값·ctx 집계) + 그 시점 실검·커뮤니티 키워드}를 `apps/insta/data/algo_runs/YYYY-MM.jsonl`에 1줄 누적(IG+FB 한 줄 · 월샤딩 · 기록자 = insta-fetch 단독). 정본 = `.github/scripts/algo_ledger.py` · 일괄 집계 = `apps/insta/algo_insight.py`(원장 40줄+ 도달 후 ISO주 1회 자동 → algo_insight.{json,md}). Δ·트렌드 매칭은 원장에 굽지 않는다(절대값+타임스탬프만 — 분석기 몫 · 평의회 260802). 게이트 = `check_algo_ledger`(네트워크·LLM 0 심볼 · 스텝 3중 fail-soft · 기록자 유일성 하드) · 롤백 = repo variable `ALGO_LEDGER=0`.
- 기계산출물(거울·데이터)은 손편집 금지 — 생성 코드를 고쳐 재생성으로 반영한다.
- [9-1 납품] 운영자 PC에서 돌 산출물 = 자기완결 1파일로 묶어 첨부한다. 번들러 정본 = `scripts/build_drive_move_bundle.py`(감시기 ps1 전체를 base64로 실어 더블클릭 .bat 1개로 뽑는 방식 · `--check`로 최신 여부 확인). 드리프트 차단 게이트 = `check_drive_move_bundle`(ps1만 고치고 재생성을 잊으면 운영자 PC에 옛 코드가 깔린다 = 조용한 라이브 낡음).
- 디자인 계약 전문 = `AGENTS.md` §🎨.
- 뉴스 파이프라인 = `scraper/`(①수집·선정) → `prompts/news-analysis.md`(②분석·요약) → `apps/news/`(지침·fact_guard 검증) → queue/ → 뷰어.
- **[재난 3축 · 운영자 260803]** ⓞ **지진 규모 문턱 = 국내 3.5 / 해외 6.0**(운영자 260803 5차 "해외건, 국내건 차이가 많이 나야해 · 3도 좀 애매해") — 해외 판정 = `sns_trends.DIS_INTL_RE`(본문 국가·해역명 · `intl` 필드로 배송 · area는 수신 지역이라 못 가른다) · **뷰어 `QUAKE_MIN_KR/INTL` = fire_watch 동값**(화면에 경보가 뜬 지진만 추적기에 들어간다) · 규모 미상 = fail-open(울린다). **⚠ 260811부터 이 두 값이 뉴스 긴급 배지 축에도 이식됐다** = `breaking_judge` **🌏 지진 국내외 문턱 게이트**(운영자 260811 «규모 5.2 지진이 국내면 ok 해외면 no» · 국내 3.5+ = O[규모 미표기 국내 유감 지진도 O] / 해외 6.0 미만·규모 미명시 = X). **⚠ 해외 6.0 = 면제 없는 선행 조건**(운영자 260811 2차 «사망을 아무리 하든 6.0 안 넘으면 긴급이 아냐 · 1조건을 충족시킨 이후인거지») — 1차 판에 넣었던 「실제 피해 명시면 문턱 미적용」 예외는 **그 자리에서 회수**했다: 그 예외가 있으면 «해외 규모 4.0 지진에 N명 사망»이 문턱을 그냥 뚫어 6.0이 무의미해진다(운영자 «그러면 6.0이 왜 있어»). 피해·인명 서술은 6.0을 넘긴 **뒤에만** 일반 규칙으로 쓰인다. ⚠ 인명 축은 🔥 화재 게이트의 축이고 지진과는 **별개**(운영자 260811 «이건이랑 애초에 별개야») — 지진은 규모 하나로만 가른다. ⚠ 회귀 기대값 2건 개정 동반(「伊 나폴리 4.7」·「구마모토 강진 38명 사망」 YES→NO = 후자는 해외인데 규모 미명시). ⚠ 값 창작 0 = 재난 경보 문턱 그대로 계승(세 표면이 같은 두 값을 쓴다 = 폰 경보·추적기·배지가 서로 다른 말을 하지 않는다). ⚠ 회귀 원장 기대값 1건 개정 동반(「伊 나폴리 규모 4.7 강진」 YES→NO = 방침 변경 · 「구마모토 강진 38명 사망」이 해외 피해 명시 축의 보호 케이스로 잔존). ⓐ **중대재난 문턱 = `sns_trends.DIS_CRIT_MIN`(77 = 산불 이상) 단일 원천** — 지진·화재급 골드 표기(`.trend-q.dis-crit`)와 화재 추적기 등록이 이 한 값을 공유(뷰어 `disCrit()` = crit 1순위 · 구데이터 sev 역산 폴백). ⓑ **랜드마크 즉시 긴급알림** = `DIS_LANDMARK`(문화유산·고위공직처·유명건물 99종) + `DIS_GOV_RE`(«OO시청»류 접미 결합 · `(?!역)` 오탐컷) → 수집기가 `lm` 필드로 실어보내고 뷰어 `fireMsgs()`가 quakeMsgs 문법 그대로 경고+기기알림(tag 축별 분리 = 지진·화재 동시 발생 시 교체 소실 방지). ⚠ **판정 대상 = 발신 서명을 걷어낸 본문뿐 = `DIS_SIGN_RE`·`_dis_body` · 게이트 `check_disaster_landmark_sign`(260805 실사고 봉합)** — 한국 재난문자는 관례상 전건이 «[○○구청]» 서명으로 닫히는데 구판은 그 서명을 「그 건물에 사고」로 읽었다(실측 = 라이브 재난 10건 중 lm 매치 4건이 **전건 말미 서명**{서해구청·계양구청×2·행정안전부} = 정확도 **0/4**) → 지자체 발신 화재 문자 **전건**이 즉시 긴급알림 = ⑭-e 취지("작아도 전국 뉴스가 되는 곳")의 정반대. 실사고 = 「도로차단은 **유지**되오니 우회」 교통 후속 안내가 기기 긴급알림으로 발사(260805 07:03 인천)되고 fire_watch가 「랜드마크=30」을 얹어 grade HI(3시간 추적)로 하드 승격까지 연쇄. ⚠ 신설 사유 = 기존 게이트는 전부 **정적 문자열·심볼**(check_refs 계열)이거나 **화면 렌더**(smoke_*)라 「판정 함수가 실제로 무엇을 잡는가」가 축 자체로 없었다 — 오탐이 알림·추적·자동픽까지 번지는 동안 무경보였고 운영자 눈이 유일한 검출기(insta-thumb-miss·brk_misfire 동축). 판정 = 정본 함수 import 재판정 8케이스(사전 복제 0 · 네트워크·LLM·렌더 0 · 면책표 없이 하드 0) · 놓침은 안 는다(진짜 사고는 건물명이 **본문**에 실린다 = 「숭례문 화재 발생」·「강남구청 청사 화재」 전건 보존 실증). ⚠ **판정은 데이터에 박제된다 = 코드 봉합 ≠ 라이브 봉합 = 뷰어 2층 검문 + 게이트 `check_disaster_lm_stale`(260805 2차 실사고 봉합)** — `lm`은 **수집 시점 1회** 계산해 항목에 박고(`"lm": disaster_landmark(msg)` · 생산자 2곳 = `disaster()`·`disaster_km()`) 뷰어 `fireMsgs()`는 매 렌더 그 **저장값**을 다시 읽는다 → 위 서명 봉합(10363a4)이 **09:37 KST main 착지**했는데도 07:03에 이미 구워진 항목이 `viewer/sns_trends.json`에 `lm:"서해구청"`으로 살아남아 **10:20 알림 리포트에 그대로 재등장**했다(TTL `FIRE_TTL_H` 12h = 19:03까지 유령 재점등). 데이터는 기계산출물이라 손으로 못 씻고 다음 수집 회차를 기다려야 하므로 **소비 지점 방어가 유일한 즉시 해** = 뷰어가 「서버가 준 lm이 **서명을 걷어낸 본문에 실제로 있는가**」를 한 번 더 본다(`DIS_SIGN_TAIL`·`disBody()` · **사전 복제 0** = 목록·정규식 사전을 뷰어가 들지 않고 형태 규칙 1줄뿐 = 정본 1곳 계약 유지 · fail-open 2종 = text 결손·300자 컷 도달 → 종전 동작 = 놓침 0{실측 라이브 재난 10건 본문 75~90자}). ⚠ 신설 사유 = 짝 게이트 `check_disaster_landmark_sign`은 정본 함수를 케이스로 재판정해 **「코드가 옳은가」만** 본다 — 「**라이브 데이터에 구판 판정이 남아 있는가**」는 축 자체가 없었고, 그래서 봉합 세션이 코드만 고치고 완료로 닫아도 아무 게이트가 안 울렸다(운영자가 리포트를 열어야만 발견 = insta-thumb-miss 동축). 판정 2축 = ① 뷰어 2층 검문 실존(심볼 + `fireMsgs` 본문 배선) = **하드** ② 라이브 데이터 재판정 불일치 = **WARN(비차단)** — 하드로 두면 판정 코드를 고치는 **바로 그 커밋**이 반드시 불일치를 띄워 게이트가 봉합 자체를 막는 자기모순이 되고, 데이터는 봇 소유라 세션이 커밋으로 못 씻으며, ①이 살아 있으면 불일치는 화면에 안 뜨므로 「씻길 때까지 보이게」가 정확한 역할(`check_component_lock` WARN 선례 계승) · 실증 = 킬테스트(`fireMsgs` 검문 줄 제거 → rc=1 검출) + 사고 당일 라이브 재판정이 그 1건을 정확히 지목. ⓒ **화재 후속 추적 = `scraper/fire_watch.py`**(원장 `push/fire_watch.json` · scrape.yml 15분 타이머 편승) — 화재 계열 발령을 사건 단위로 묶어(고정 슬롯 금지 = 근접 매칭) **발령 후 3시간 동안 매 런(15분)** 수집함→네이버 순 검색(운영자 260803 3차 "3시간이면 무조건 보도가 뜸" · 구 15·30분 2회차는 08:20 사건의 10:00 보도를 못 잡는 구멍이었다) → 3축 히트(지역∧화재어∧[**사상자어** ∨ 규모신호])면 **즉시 알림**(push_send `nomute-fire` + msg.py 점등 · 큐잉보다 먼저 = 상한에 막혀도 알림은 간다) + `pick_pending` 큐 적재. ⚠️ 규모신호(대응 2단계↑)는 **사상자어의 대체재가 아니라 병렬 OR**(운영자 "대응단계가 낮아도 사상자는 발생할수있음") — 그 줄을 지워도 사상자 축은 무손상. **⚠ 단 화재 계열(화재·산불·폭발·붕괴)의 규모 축 히트는 260811부터 알리지 않는다**(운영자 260811 "화재 중에 전소가 되거나 해도 인명 피해 없으면 긴급에서 빼줘") — `notify()` 첫 분기에서 반환 = 폰 푸시·메시지함 점등 0, **큐 적재·원장·추적은 그대로**(보도가치는 보존 · 「긴급에서만 뺀다」가 지시의 실체). ⚠ 지진 축은 무접촉 = `ESCALATION`이 규모 6+·진도 5+·지진해일을 겸하는 정규식이라 kind로 좁혔다(화재 지시가 지진 경보를 죽이면 안 된다). 짝 = 뉴스 긴급 배지 축의 `breaking_judge` **🔥 화재 인명 축 게이트**(같은 날 동반 = 두 축이 갈리면 화면 배지와 폰 알림이 서로 다른 말을 한다). ⚠️ 자동 과금 = 픽 시점만(되짚기는 LLM 0콜·800건×12회 89ms 실측) · 사건당 1픽 영구 + 일 상한 `FIRE_DAY_CAP`. ⓔ **추적 상태 화면 표기(⑭-i · 260803 6차)** = 원장이 `viewer/` 밖(Pages 출력 루트 아님)이라 뷰어가 못 읽는다 → `build-viewer.mjs`가 표시용 사본 `viewer/fire_watch.json`으로 굽는다(messages.json 선례 · 노출 = 표시 필드만 · `.gitignore` 등재 = 빌드 산출물). 재난문자 행 우측 = `.fw-chip`(부품·값 = `.tcard-ago(.fresh24)` 사본) — 「추적 중 N분」(무채) / 「사상자 확인·큐 넘김」(골드) · 매칭 = 광역∧유형∧45분(서버 `EVENT_GAP_MIN` 동값) · 미매칭 = 칩 없음(추측 표시 금지). ⓓ **위험 등급 학습(⑭-g · 260803 4차)** = 추적이 끝난 사건을 `push/fire_outcomes.jsonl`(정답지 · 특징+결과+소요분)에 1줄 적재 → 다음 발령을 **문자 시점에** 채점(특징 = 시설어·심야·랜드마크 · 추가 수집 0 · 표본 3건↑ 특징은 seed 대신 **실측 적중률** 채택 = 자기교정) → HI 3h / LO 1h 창 배분. ⚠ **「모르는 건 HI」가 제1 원칙** — LO 강등은 {전체 표본 ≥`LEARN_MIN` ∧ 랜드마크 아님 ∧ 점수<`RISK_HI` ∧ **전 특징이 실측으로 뒷받침**} 전부 충족 시만(260803 실측 사고 = ④ 없으면 「야산 들불」 20건만 쌓인 상태에서 한 번도 학습된 적 없는 「심야 요양원」 창이 1h로 깎였다) · 표본 미달이면 전건 HI = 현 동작 100% 불변.
