# 노뮤트 에디터 — 현재 작업 규칙

## 목적과 진입점
- 운영자에게 친근한 반말로, 확인한 사실과 미확인을 구분해 짧게 보고한다.
- 사용자가 요청한 목적을 구현·검증·반영까지 끝낸다. 사용자 명시 지시가 과거 규칙보다 우선한다.
- 저장소: `nomutefb/editor`. 운영 화면: `edit.nomute.kr`. 과거 계정·도메인·사고 상황을 현재 상태로 사용하지 않는다.
- 시작은 `README.md`, 작업 공통 계약은 `AGENTS.md`. 기능별 상세는 해당 앱 지침을 필요한 때만 읽는다.
- 뉴스 내용·문체 정본은 `apps/news/00_에디터_뉴스_운영.md`와 여기서 연결하는 지침, 고정 채널 사실은 `PROJECT_MEMORY.md`다. 생성 품질 규칙은 개발 작업 이력과 섞지 않는다.

## §🎨 디자인
- 기존 토큰과 컴포넌트를 계승한다. 사용자가 개편을 허용한 경우에도 기존 기능과 입력·결과 복원 동작을 보존한다.
- 디자인 진입점: `디자인기틀/디자인기틀_SSOT.md`. 값 정본은 화면 원본의 `:root`, 컴포넌트는 `디자인기틀/CII_컴포넌트계승인덱스.md`.
- 거울 파일은 직접 고치지 않는다. 값 변경 뒤 `python3 shared/build_design_mirror.py build`로 생성한다.
- 스튜디오 코너 레일과 버튼 규격은 `AGENTS.md`의 계약을 따른다. 새 탭도 공용 레일·작업 관리·동기화를 계승한다.
- 화면 변경 전후 로컬 미리보기와 스모크로 모바일·데스크톱, 기존 입력과 결과를 확인한다.

## §📰 기능과 데이터
- 뉴스 흐름: 수집·선정 → 분석·요약 → 원문 사실 검사 → 기사 및 카드 기록 → 화면 빌드.
- 요약은 원문의 초점과 사실 비트를 보존하고 충분히 쓴 뒤 상한에 맞춘다. 원문 제목과 요약 후킹 제목은 구분한다. 플랫폼별 상한은 현재 앱 지침·검증기 정본을 따른다.
- 한국어 문체(AI 번역투 소거·Sunny 7) 정본은 `shared/ko_tone_rules.md`다. 요약 = 공용 문장축 + 기사체 상한선, 카드 = 공용 문장축 + 윤문 추가축(윤문체), SNS = 공용 문장축만. 윤문 후처리 콜은 OFF. 규칙 본문을 다른 파일에 복사하지 않는다.
- 모델과 호출 설정은 `shared/models.json`에서 관리한다. 인증을 읽지 않는 실행 옵션으로 바꾸지 않는다. 지침은 호출별 필요한 부분만 주입한다.
- 제작은 접수·실행·저장·화면 반영을 구분한다. 일시적 읽기 장애로 진행 기록을 삭제하지 않는다. 저장 실패를 성공으로 보고하지 않는다.
- 설정의 목록 변경은 사용자가 읽은 기준값과 함께 병합한다. 다른 기기의 추가·수정·삭제를 낡은 전체 목록으로 덮지 않는다.
- 제작 이미지의 미리보기와 실제 크롭은 같은 배치값을 쓴다. 기존 이미지 수정은 원본 보존 후 새 결과를 추가한다.
- 실패에는 실제 사유와 다음 행동을 표시한다. 입력·외부 장애·코드 오류를 같은 원인으로 분류하지 않는다.

## 검증과 반영
- 변경 전 현재 상태를 확인하고 관련 회귀 사례를 확보한다. 기계 산출물은 생성 코드에서 변경한다.
- 커밋 전 `python3 shared/check_refs.py` 필수. 동작 검사는 `node --test tests/*.test.mjs`와 `python3 -m unittest discover -s tests -p 'test_*.py'`.
- 화면 변경은 `bash shared/smoke_all.sh`도 통과해야 한다. 스냅샷 비교는 `node shared/preview_shot.js base`와 `diff`.
- 넓은 동작·구조 변경은 독립 검토를 수행한다. 검사 실패를 숨기거나 기존 기준을 근거 없이 낮추지 않는다.
- 반영은 `bash shared/merge_main.sh prep` 후 검토 가능한 PR을 만들고 `bash shared/merge_main.sh land`. 기준은 항상 최신 `origin/main`.
- 충돌은 변경 의도를 비교해 해결하고 다시 검사한다. main 강제 푸시는 하지 않는다.
- 코드 반영과 운영 화면 반영을 별도로 확인한다. 실행하지 못한 검사나 외부 반영 실패를 분명히 보고한다.
- 전체 검사 목적 색인은 `docs/current-contracts.md`. 각 조건은 실행 코드가 정본이다. 지침에 사고 서술이나 코드 사본을 추가하지 않는다.

## 가벼운 유지
- 과거 백업·작업 보고서·임시 결과를 저장소에 누적하지 않는다. 복원은 Git 이력, 실패 결과 회수는 짧은 기간의 작업 첨부물을 쓴다.
- 현재 작업·설정·필요한 원본과 역사 데이터는 구분한다. 로그 보관은 최근 14일을 기본으로 하고 입력 원본·미완료 작업은 임의 만료시키지 않는다.
- 새 규칙은 기존 규칙을 갱신한다. 현재 결정과 폐기된 사고 기록을 한 문서에 섞지 않는다.
- 기능별 공용 부품은 아래 색인으로 찾고, 변경할 때 호출부와 생성·검사 경로도 함께 확인한다.

## 공용 부품 색인
- `viewer/nm-clip.js`
- `viewer/nm-jobs.js`
- `viewer/nm-loader.js`
- `viewer/nm-models.js`
- `viewer/nm-rail.js`
- `viewer/nm-seq.js`
- `viewer/nm-styles.js`
- `viewer/nm-svg.js`
- `viewer/nm-sync.js`
