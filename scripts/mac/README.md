# 맥 2선 실행기 — 정본은 여기(깃) 하나다 (260815 버전 단일화)
# 수정 = 반드시 이 폴더에서 커밋 → 맥 1분 틱이 감지해 ~/에 자동 배포(자기갱신).
# ~/ 에 있는 실물은 "설치본"일 뿐 — 직접 고치면 다음 틱에 깃 판으로 덮인다(분열 원천 차단).
# plist 2개만 수동(맥 재설치 시 ~/Library/LaunchAgents/ 복사 + launchctl 등록).
