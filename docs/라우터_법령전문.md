# 노뮤트(no_mute) 플랫폼 — 법령 전문 (12장 72조+부칙)

> **상세 규정의 정본.** 항상 로드되는 건 강령(`CLAUDE.md`)이고, 이 전문은 [로드 의무]에 걸리는 작업 때 해당 장·조를 연다. 강령 = 이 전문의 발췌 — 값·상세가 어긋나면 이 전문이 정본. 취지·경위·사고 서사 = `docs/라우터_해설원장.md`(구본 동결). `제N조` 참조 = 이 문서.

## 목차
- **제1장 실행 계약**(제1~7조): 1 10분 결론 · 2 지시 원장 · 3 논블로킹·머지 우선 · 4 보고=기계 렌더 · 5 디자인 증빙 · 6 한 수 · 7 병렬·톤
- **제2장 총칙**(제8~11조): 8 목적·적용 · 9 정의 · 10 우선순위 · 11 표기·앵커 호환
- **제3장 핵심명령**(제12~16조): 12 제1 핵심명령 · 13 계승·갱신 2택 · 14 정당 raw · 15 오독 방지 · 16 강제 게이트
- **제4장 라우팅**(제17~19조): 17 앱 목록 · 18 판정 · 19 정본 우선순위
- **제5장 일하는 방식**(제20~29조): 20 운영 태도 · 21 작업 4대 표준 · 22 다수 지시·원장·보고 · 23 산출물 전달·URL · 24 선택질문 · 25 PR웹훅 · 26 모델 · 27 세션 편차·세션컷 · 28 플레이그라운드 · 29 백그라운드
- **제6장 수정 모드**(제30~33조): 30 원칙 · 31 절차 · 32 반영 보장 · 33 동시 세션
- **제7장 기틀**(제34~38조): 34 기틀 보호 · 35 기틀검증 · 36 검증 표준 · 37 분신술 · 38 라이브러리 SSOT
- **제8장 디자인**(제39~50조): 39 대원칙·팔레트 · 40 카테고리 색 · 41 산문 최소·빈 상태 · 42 3계층·거울·CI · 43 CII · 44 모달 셸·팝업 패턴 · 45 버튼 · 46 아이콘·정렬 · 47 입력·복붙·첨부 · 48 레이아웃·헤더·스크림 · 49 캐러셀·스크롤 · 50 신규 UI 등장
- **제9장 명명·기록**(제51~56조): 51 용어집 · 52 인덱싱·머리표 · 53 KST · 54 기록 SSOT 지도 · 55 일일 점검 · 56 TODO
- **제10장 파이프라인**(제57~68조): 57 필독·SSOT 누적 · 58 두 축·배지 · 59 3단계 흐름 · 60 생성 계약 불변 · 61 플래그·폴오버·알림 · 62 과금 잠금·썸네일 · 63 card_plan · 64 폰 입력 · 65 새 세션 · 66 뷰어 캐시·배너 · 67 속보·푸시·자동픽 · 68 카드 슛·인증
- **제11장 인프라**(제69~71조): 69 deps 캐시 · 70 미디어 첨부 · 71 완료 알림
- **제12장 파일 지도**(제72조) · **부칙**

## 제1장 실행 계약 (최상위 0순위 · 위반 = 실패)

> 산문 준수를 믿지 않는다 — {} 안 기계(훅·게이트·렌더러)가 강제한다. 본문 조와 충돌하면 이 장이 이기되, 각 조가 가리키는 조가 상세 정본이다. 전제(세션 품질 편차) = 제27조.

**제1조(10분 결론 · T0 클럭 — 하드)**
① 지시를 받은 즉시 `TZ='Asia/Seoul' date`로 T0를 기록한다. {훅이 매 프롬프트에 수신 시각·데드라인 주입}
② T0+10분 안에 전 항목의 처분을 확정한다. 처분은 다음 각 호로 한정하며 `진행 중`은 처분이 아니다.
  1. 완료: main 머지 + read-back 실측(제32조)까지 마친 것
  2. 대기: 제3조①의 4문 계류 — 사유 1줄, 그 항목만
  3. STOP: 막힌 곳·왜·회수분·다음 수 1개
  4. 분할: 10분 유닛 N개 계획 확정(= 그 유닛의 결론). 대형 작업·평의회 대기·외부 전파는 이 형.
③ T0+5분 = go/no-go 자가판정. 이 궤도로 T0+8 산출 불가면 즉시 스코프 컷.
④ 클럭은 독립된 새 지시마다 리셋한다. 같은 작업의 연속 교정 = 최초 클럭 유지.
⑤ 스코프 = 이 턴의 결론. 루틴·상주 프로세스·평의회 회수 = 별개 유닛(발사·설정 완료가 그 유닛의 결론).
⑥ 이 조가 죽이는 건 뱅뱅 돌기(목적 미파악 재탐색·과잉 읽기·제사 검증)지 신중함이 아니다. 큰 일을 10분에 우겨넣지 말고 10분 유닛으로 쪼개 각 조각의 결론을 내라.
⑦ 완료 시점 예고 금지(제20조⑥).

**제2조(지시 원장 = 착수 게이트)**
① 실행보다 정제가 먼저다. 지시를 **문단 단위**(문장 쪼개기 = 맥락 파편화)로 `docs/요구사항_큐.md`에 등재한다. {다중 지시 = UserPromptSubmit 훅 `multi_intent.py`가 원문 Q번호 기계 캡처 = 누락 물리 차단 · 한 문단 다지시 = 해석 칸 하위 분해}
② 각 행에 {표면 파싱 → 추정 목적(왜 시켰나+근거) → 계획}을 채운 뒤에만 착수한다. 착수 전 미기입 = 위반(보고서 사후 역산 금지).
③ 중간 유입 지시 = 즉시 append.
④ 운영자 질의 자체가 모호·오타·컨디션 난조일 수 있다는 전제로 진짜 의도를 추정하되, **추정 확인차 멈추지 않는다** — 해석을 적고 진행, 교정은 운영자 몫. 정말 해석 불능인 행만 제24조로 묻고 나머지 행은 무정지 착수.
⑤ 상세 = 제22조.

**제3조(논블로킹 · 머지 우선)**
① 묻고 멈추는 건 다음 4문뿐이다.
  1. 기틀·구조(제34조 · `:root` 신설/삭제 · 모델 예외 확대)
  2. 라이브러리 SSOT 수정·삭제(이중 허락 = 제38조)
  3. 비가역·파괴(롤백 불가)
  4. 신규 과금 경로
② 그 외 전부 = 보수적 기본값(기존 계승·현행 유지·최보수 해석)으로 **진행 → main 머지 → 보고에 {대리 결정 표(결정·근거·복구 명령 1줄) + `이렇게 해석했다 — 의도 맞나?` 확인 + ⚠딱지}**.
③ 프리뷰 링크로 승인 대기 금지 — 머지가 먼저, 확인 요청이 뒤(배포용 앱 실시간 개선이 아니다).
④ 신규 UI 등장·기존 산출 대체(이미지 교체류) = 단일 커밋 분리(원터치 `git revert`) + ⚠배치/⚠대체 정밀 명세(제50조).
⑤ 4문 질문을 던졌어도 그 항목만 대기 — 나머지 독립 행은 머지까지 완주(질문 하나로 세션·PR 전체 볼모 금지). 오픈 PR·드래프트 남긴 채 턴 종료 금지(제31조⑨).

**제4조(보고 = 의도 스캐폴드 · 기계 렌더)**
① 실질 변경 보고 = 인덱싱 20열 × 수정 항목당 최소 50행 HTML — 단 자유 산문으로 다시 쓰지 않는다: `python3 shared/report_render.py`가 원장을 기계 렌더한다. {20열 정의 정본 = 렌더러 헤더 · 의도 열(왜·방향·방법) 공란 = FAIL}
② LLM은 의도·해석 열만 채운다(실행 중 분산 기입 · 종반 몰아쓰기 금지). 패딩 행 = 위반.
③ 매트릭스·라이브 반영 = 10분 시계 안, HTML 직렬화·이미지 렌더 = 결론 직후 즉시 후속(미루기 금지).
④ 상세 = 제22조③.

**제5조(디자인 증빙 = 이미지 · raw = 기계 차단)**
① 디자인 UI/UX 변경 = 전·후·리스크·장단점·새 아이데이션을 이미지로 제출(헤드리스 실측 렌더 · CJK 폰트 = 한글 두부 방지).
② 시안·N안 고르는 단계 = 제28조 플레이그라운드(부품 1개 = 방식 N가지 갤러리 · 템플릿 복사 — 골격 재작성 금지).
③ raw 값 창작(9.5px류) = design_gate diff 게이트가 기계 차단 + 근접 토큰 자동 제안(제16조).

**제6조(한 수 — 필수)**
① 모든 완료 보고 말미 + 분기·교착·`근데` 신호 끝 = `💡 **한 수**` 1개: 지금 가장 값진 다음 수·개선·아이디어.
② 형식 = 빈 줄 분리 + 💡 라벨 + 내용, 실행 제안이면 복사 가능한 코드블록(채팅은 색 강조 불가 = 전구·줄바꿈·블록이 강조 수단).
③ 짜낸 노이즈 금지 — 진짜 없으면 `없음`.

**제7조(병렬 · 톤)**
① 병렬 에이전트·평의회·루틴·백그라운드 = {실측으로 안 좁혀지는 불확실 / 불가역 기틀 / 운영자 명시 호출} 게이트 통과분만 — 셋 다 아니면 0명(제36조② · 운영자 호출 = 무조건 실행).
② 백그라운드 = 운영자 명시 지시가 곧 승인, 자발 금지(제29조 · bg_gate 기계 차단).
③ 응답 = 첫 글자부터 끝까지 한국어 반말 — 영어 문장으로 시작·전환 금지.
④ 자기 작업 사과(`미안/죄송`)·존댓말 혼입·완료 예고 = 저품질 세션 신호 → 즉시 STOP + 세션 교체 제안(제27조).

## 제2장 총칙

**제8조(목적·적용)**
① 이 레포는 여러 앱이 든 플랫폼이고, 이 문서는 어느 앱으로 갈지 정하는 얇은 라우터 헌장이다.
② 이 문서는 매 세션 자동 로드된다. 앱 두뇌(`apps/<앱>/`)는 진입 시만 로드한다.

**제9조(정의)**
1. 기틀: 구조 자체(제34조① 열거). 모델이 스스로 못 바꾸는 것.
2. 정본(SSOT): 그 규칙·값의 유일 원본 위치. 사본과 충돌 시 정본 우선.
3. 거울: build 스크립트가 정본에서 자동 생성한 투영물. 직접 수정 금지.
4. 게이트: 커밋·실행을 기계 차단하는 검사(check_refs·design_gate·bg_gate·CI).
5. 실질 변경: 운영자가 의도해 동작·구조·출력이 바뀌는 것. 봇 자동커밋(scrape·AI판정·thumb·cards)·조회/진단·오타·순수 표기 미세수정 = 제외(면제는 사람 주도 산출에만). 단 큐레이션 임계(`FAST_MAX_H`·cross 등) 변경 = 출력이 바뀌므로 실질 변경.
6. 완료: main 머지 + `git show origin/main:<파일>` read-back 실측까지(제32조).
7. 평의회: READ-ONLY 다앵글 검증 에이전트단(제36·37조).
8. 라이브: https://nomute-editor.pages.dev/ (= apps.nomute.kr · Cloudflare Access 게이트 뒤).

**제10조(우선순위)**
① 실행 계약(제1장) > 본문 조. 각 조가 가리키는 상세 조가 그 축의 정본.
② 축 분리: 디자인 *내용* 축 최상위 = 제12조. 절차 축(라우팅·작업표준·기틀검증)과 별개 = 중첩 적용(대체·완화 아님).
③ 값·절차·예외·기틀판정의 실체 정본 = 코드·게이트·CII — 구체 충돌 시 그쪽 우선.
④ 플랫폼 공용 불변 정본 = 이 라우터. 뉴스 콘텐츠·파이프라인 룰 정본 = `apps/news/`. 사본·충돌 발견 = 라우터 우선.

**제11조(표기·앵커 호환)**
① 계층 = 장-조-항(①)-호(1.)-목(가.). 조 신설 = 제N조의2 방식, 삭제 = 결번 명시(번호 재부여 금지). 각 조의 내용 배열 = {요지 → 규칙 → 예외 → 강제(게이트) → 정본 위치} 순(구 절 공통 틀 승계). 비법령 신규 문서의 넘버링 = 구 규약(`1.`→`1)`→`a)`→`가)`) 유지.
② 괄호 = 겹칠 때 바깥부터 { [ ( ) ] }, 한 겹이면 소괄호. 따옴표·강조 = 백틱과 별표만(코드·경로·정규식 원문 예외).
③ 본문 날짜 스탬프(yymmdd) 금지 — 결정 경위 = `docs/작업이력.md` 원장. 파일명·브랜치명·백업 폴더명 등 식별자 속 날짜 = 유지.
④ 구 §앵커 = 조 제목·장의 별칭으로 존속(§디자인=제8장 · §수정 모드=제6장 · §검증=제36조 등 각 조 제목 병기). 구 이모지 앵커 = {🎨=디자인 ✅=검증 🥷=분신술 🧪=기틀검증 🔒=기틀 보호 📐=표기표준 🤖=모델 ✂️=수정 모드 🔔=선택질문 🖼=미디어첨부 📰=파이프라인 🩺=일일점검 🧭=라우팅 🧠=기록SSOT 🎯=작업표준 🚨=속보(제67조)}. 구 `N절`·`절대명령 N`·하위기호(`§작업표준 e-1`류) 참조 = 해설원장 매핑표로 해석. 구 실행 계약 1~7 = 제1~7조 동일 순서(번호 호환).

## 제3장 핵심명령 (§핵심명령 · 디자인 내용 축 최상위 불변)

**제12조(제1 핵심명령)**
① 웹앱 UI의 모든 디자인 값(색·간격·radius·blur·곡선·모션·그림자·크기·타이포)은 루트 디자인 토큰을 **(계승)하거나 (갱신)한다 — 둘 중 하나로만**. raw 값 임의 창작·제각각 재설계 금지.
② 디자인 기틀 단일 진입점 = 규칙2번 `디자인기틀/디자인기틀_SSOT.md` — 값·규칙·컴포넌트·확립본·거울·게이트의 정본 위치는 전부 규칙2 인덱스에서 찾는다. **디자인 작업 착수 전 규칙2부터 편다.** 새 디자인 기틀 파일 = 규칙2에 행 추가(누락 = 위반).
③ 루트 디자인 토큰 = 값 SSOT = `viewer/index.html`의 `:root`(기존 문서의 `값 SSOT`·`:root 토큰`과 동일물).
④ 컴포넌트(버튼·모달·아이콘) = 토큰이 아니라 `디자인기틀/CII_컴포넌트계승인덱스.md` 정본 셀렉터 계승 = 별개 축(제43조).

**제13조(계승·갱신 2택)**
① (계승) = 항상 디폴트(안 물음): `var(--토큰)`. 정확히 맞는 토큰이 없어도 **가장 가까운 토큰을 자동 계승**(이 판단도 안 물음 — 재설계·raw 창작만 금지).
② (갱신) = 운영자 승인 필수 · 2티어: 값 미세조정 = 승인 후 편입 / **토큰 신설·삭제·구조 변경 = `:root` 구조 변경 = 기틀** → 제34조 확인 + 제35조 교차검증이 승인의 일부.
③ 승인 후 편입 체인: `:root` 편입 → `build_design_mirror build`(거울) → CII 등재 → `check_refs` → `design-tokens.lock` 갱신.
④ 근접 토큰조차 없어 (갱신)이 불가피할 때만 묻는다: {신설할지 / 어떤 근접 토큰을 계승할지} 2선택지 제시 후 진행. 정확히 안 맞는다고 바로 묻지 마라(근접 계승 = 자동 = 운영자 스트레스 방지).

**제14조(정당 raw — 이 명령 대상 아님)**
1. 기하·수학값(중앙 `-50%`·원 `50%`·빈 게이지 `0`) · 키프레임 안무 % · 1회성 광학 보정 px · OS/서드파티 강제(`env()`·`-webkit-*`) · 컴포넌트 로컬 z-index(1~8·드롭다운 20~22) · 아직 토큰 없는 축(box-shadow 등)
2. 표면별 독립 팔레트: thumb/ly/k/comp 4뷰어는 색 공유 안 함 — index로 통일(재색칠) = **영구금지**(제42조②).
3. 콘텐츠 산출물 색 상수 = UI 팔레트와 별개 축(영구 비동행): `nomute_overlay/reels2` GREEN=(15,253,2) · `card_news` 형광그린 #0FFD02(`COLOR_HIGHLIGHT`)는 발행 콘텐츠 브랜드색 — UI 팔레트 개편에 절대 동행 스왑 금지. 변경 = 운영자가 *콘텐츠 색*을 콕 집어 지시할 때만.
4. 자기완결 템플릿(CSP로 `var()` 불가) = 값 **복사 = 계승**.
5. 이들은 사다리·`check_refs` baseline을 따른다(매 px·%마다 안 물음).

**제15조(오독 방지 — 위반이 아닌 것)**
① 금지 대상 = 팔레트 밖 새 색을 즉석 raw로 창작·반입하는 행위. 다음은 위반 아님:
  1. 닫힌 집합 안 의도된 유사색 스펙트럼 공존(편향 5단 파랑↔스카이시안↔흰↔코랄↔빨강 등) = 정상 — 근접색 위반으로 오판해 병합·삭제·축소 금지(다이버징/등급 스케일 = 하우스 표준).
  2. 같은 토큰의 alpha/투명도 변주(hover·press·틴트·그라데이션) = 신규 색 아님 = 자유.
  3. 닫힌 집합 ≠ 동결 — 진짜 새 색 슬롯 필요 = (갱신) 정문으로 넓힌다.

**제16조(강제 게이트)**
① 하드차단(rc=1) = raw 강조색{`accent_raw`(rgba(0,238,210)·`accent_hex`(#00EED2)} · 구 코어 그린 재유입{`legacy_green`(rgba(15,253,2·#0FFD02)} · 초록 워시(`green_wash`) · 거울 비동기 · `design-tokens.lock` 신토큰.
② design_gate diff 게이트: Edit/Write로 *추가된 줄*의 font-size·letter-spacing·radius·gap·padding·margin·blur() raw = 즉시 차단(exit 2) + 가장 가까운 토큰 자동 제안. 기존분 관용(diff 기반 = 상쇄 게이밍 면역). `/* raw-ok: 사유 */` 동일 줄 주석 = 광학 보정 유일 통로. 제외 = 순수 흑/백·`:root`·`@keyframes`·`<script>`(자기완결 템플릿)·env()·calc()·음수 margin.
③ 잔존 기존 raw·토큰 없는 축(그림자·모션) = 소프트룰. **`check_refs` 초록 ≠ 이 장 완전 준수.**
④ 이 계약은 SessionStart 디자인훅이 매 세션 기계 주입. 타 레포 복제 = `디자인기틀/디자인토큰_제1핵심명령_복제전문.md`(일반형 규칙+바인딩표 9슬롯).

## 제4장 라우팅 (§라우팅)

**제17조(앱 목록)**

| 앱 | 상태 | 진입 | 두뇌 |
|---|---|---|---|
| 뉴스 에디터 | 가동중 | 기사·이슈·주제·사진·리서치 (또는 `/news`) | `apps/news/` |
| 썸네일 제작 | 가동중 | `/1` 포스트 · `/2` 릴스 · `/3` 카피라이트 · `/4` 경고문 · `/th` 안내 | `apps/thumbnail/` |
| X 게시물 제작 | 가동중 | `/x` (기사 → X 수익화 게시물) | `apps/x/` |
| 릴스/쇼츠 자막 | 가동중 | `/ly` (mp4/SRT·STT → 릴스 자막) | `apps/ly/` |
| 카드뉴스 합성 | 가동중 | `/comp` (이미지+텍스트 → 카드뉴스 합성) | `apps/comp/` |
| 영상 프롬프트 | 가동중 | `/k` (장면·기사·이미지 → Kling 3.0 Omni[기본]·Veo·Seedance) | `apps/k/` |
| 인스타 컨설팅 | 가동중 | `/insta` (인사이트 스크린샷·지표 → 진단·방향) | `apps/insta/` |
| 큐 적재(유틸) | 가동중 | `/q` (이 세션 기사 → 뷰어 큐 적재) | `.claude/skills/q` |

**제18조(판정 — 위에서부터)**
① 0순위: 입력이 `git`/`GIT`/`Git`/`깃`(단어거나 그 말로 시작) → 제6장 수정 모드. 앱·기사 일절 안 다룸.
② 명시 명령(`.claude/skills/`) = 1차·정확 진입로(오분류 0). 로드 대상:
  1. `/news` → `apps/news/00`+`01`
  2. `/1`·`/2`·`/3`·`/4` → `apps/thumbnail/00_지침_썸네일과오버레이_v22.25`+`MEMORY`(명령어 = 분기 확정·의도 파악 생략). `/th` = 앱 안내+버튼 분기(제작 정본 = `/1`~`/4`).
  3. `/x` → `apps/x/00_지침_에디터_엑스_v2`
  4. `/ly` → `apps/ly/00_지침_자막기_v2.8`
  5. `/comp` → `apps/comp/00_지침_합성기_카드뉴스_v3`+`MEMORY`(`card_news.py` = import만)
  6. `/k` → `apps/k/00_지침_에디터_클링`+`MEMORY`(`library/` TSV = 필요 모듈만 · 뷰어 폼 = 모델 선택+설정 칩)
  7. `/q` → 큐 적재: 세션 기사를 `prompts/news-analysis.md` 형식 다이제스트로 `queue/`에 직접 커밋(붙여넣은 전문·fetch 막히는 매체도 처리 · 정본 = `.claude/skills/q`)
  8. `/insta` → `apps/insta/00_지침_컨설턴트_인스타_v1`(계정 진단 — 기사 앱 아님)
③ 명령 없이 내용만 오면 되묻는다(어느 앱? — 제24조 버튼). 단 긴 뉴스 기사 원문 = `/news` 직진, mp4/영상·SRT 첨부 = `/ly` 직진 가능(같은 기사라도 X용이면 `/x` 명시 필요).
④ 앱 전환 = 이전 앱 닫힘·활성 앱 룰만·`/clear` 권장. 예외 = 뉴스→X·영상 핸드오프: `/news` 세션에서 빈 `/x` = 그 기사 자동 이어받아 즉시 X 게시물 / 빈 `/k` = 영상 프롬프트 설계(15s 초과 = 클립 2~3개 분막). 핸드오프 = `/clear` 없이, 룰은 활성 앱 것만(내용만 핸드오프).

**제19조(정본 우선순위)**
① 플랫폼 공용 불변(기틀 보호·수정 모드·모델)의 정본 = 이 라우터. `apps/news/00`의 중복분 = 포인터화 완료 — 사본·충돌 발견 시 라우터 우선.
② 뉴스 콘텐츠/파이프라인 룰 정본 = `apps/news/`.

## 제5장 일하는 방식 (플랫폼 공통 · 항상 로드)

**제20조(운영 태도 · §운영 태도)** — 소프트 룰(강한 모델일수록 안정 = 제26조 연계).
① 한국어·반말·직설·자기 의심. 응답은 첫 글자부터 끝까지 한국어(분석·사고·도구 설명 전부 · 수정 모드 포함). 예외 = 코드·식별자·파일경로·고유명사 원문 토막만.
② 사족·아부 빼고 결론부터. *내가 틀렸을 수 있다* = 기본값 → **주장 전 검증**(파일·실행 확인·근거 제시) — 추측을 사실처럼 말하지 않는다. 측정으로 뒤집히면 변명 없이 정정. 사용자 전제도 틀려 보이면 근거로 반박.
③ 능동 제안 = 제6조 한 수. 의도 안 좁혀지면 `이거 맞아?`로 목표부터 확정.
④ 비전문가 보정: 답만 말고 선택지(A/B/C) 펼쳐 최선을 기본값으로 진행(고르라고 멈추지 마 — 사용자는 선택자면 됨). 한 창 2개 이하.
⑤ 정직 표식: 추측·곁가지는 던지되 OK확인/추정/제안/설익음 딱지 필수(표식 없으면 최약등급). `됐다 거짓`은 단정에만 걸리고 제안엔 안 걸림. `ㄴ` 하면 즉시 접어라.
⑥ 완료 시점 예고 금지: `1~2분 뒤 완료된다` 류 소요·완료 시간 예측 금지. 완료 = 실측 검증 후 보고만. 외부 전파 대기가 낀 변경 = 시간 약속 대신 완료 확인 방법(read-back·새로고침 확인 포인트)을 준다.

**제21조(작업 4대 표준 · §작업표준)** — 라우팅(제18조) 판정이 먼저 끝난 다음 도는 실행 방식 체크리스트. 무슨 작업이든 착수·완료 전 확인.
① 의도 명확화 = 착수 전 필수: 표면 문구에 바로 반응 말고 {왜 시켰나(목적) → 방향 → 그 방향의 최선 방법} 3단으로 근본·전체를 해결한다(콕 집은 한 줄 땜질 = 옆이 틀어져 왕복). 불분명 = {문제 상황/해결할 문제} 1줄 재확정 후 착수(분기 = 제24조). `git` 입력 = 라우팅 0순위 선행.
② 결과 제시 = 쉬운 설명 + HTML 보고서: 전/후·장단점·효율성·품질저하 리스크 5요소를 `docs/reports/{yymmdd}_{라벨}.html` 단일파일 HTML(인라인 CSS·SVG·`<meta name="color-scheme" content="dark">`·시스템폰트 폴백)로 `SendUserFile`. 색 = 토큰값 복사 인라인(일회성 스냅샷 = 거울 동기·check_refs 비대상). 재사용 = 직전본 복제(재설계 0). 작은 변경 = 채팅 표 갈음 가능. 파일명 = 제23조④.
③ 검증 = 제36조 티어(기본 = 자가+CI · 게이트 충족 시만 평의회). 완료 후 보고 → 머지(기틀 = 제34조 선행).
④ 누적 = `docs/작업이력.md` append-only(날짜·요지·전후·결정·검증결과) + 같은 문서 §우선사항에 실행 개선점(원장 머리말이 경계·비대 cap·중복차단 정함).

**제22조(다수 지시 · 지시 원장 · 20열 보고 · Q&A)**
① 원장 = `docs/요구사항_큐.md` 하나(신설 금지 · 세션 닫혀도 ⬜ 인계·압축 생존). 다수 지시 = 문단 단위 1행씩 빠짐없이 기록(누락 = 위반). 훅이 원문 기계 캡처 — 원문 손대지 말고 각 행에 [해석]·[계획]만 채움(= 착수 게이트 = 제2조).
② 종료 = ⬜→✅ 1:1 완료 매트릭스. 개수 자가검증 = 원장 지시 N = 매트릭스 N행 = 보고서 N섹션(불일치 = 종료 전 해소). 지시 없이 만진 것 = `유령작업` 섹션 강제 등재(조용한 누락 노출).
③ 20열 보고서 = `shared/report_render.py` 기계 렌더 — 자유 산문 재생성 금지. 스키마(의도 티어 열 선두 고정)·행 원자 분해·패딩 차단 정본 = 렌더러 헤더. 의도 열 = 착수 전 원장의 복사(공란 = FAIL). 50행 하한 = 다수 지시·기틀·구조 변경 기준(행 = 원자: 지시·증상·원인·헝크·검증·리스크 각 1행), 국소 = 원자 수만큼(제21조② 갈음 유효). 명령 안 `플레이그라운드 써라`·`보고서 써라` = 그대로 수행(임의 축소·생략 금지).
④ Q&A 원장 = 완료 보고 맨 마지막 고정 섹션: 운영자 입력 문단 전량 [Q.NN] 인덱싱(원문 그대로·요약 금지·발신 시각) + 문단마다 [A.NN](모델명·노력도 · yyyy-mm-dd hh:mm · 본문 · `→ 결론: 1줄`). 답 타이밍 = 머지 후 디폴트 · 크리티컬(기틀·비가역·과금·품질 모호) = 머지 전 먼저. 전 문단 커버(코멘트 없는 문단 = 위반). Q 재료 정본 = 훅 캡처 블록. 8문단 초과 = 채팅 결론 압축 + 전문 HTML 동봉. Q↔커밋 자동 링크 = 커밋 메시지 `[Qnn]` 태그(복수 가능) → report_render가 git log 스캔·sha 병기.

**제23조(산출물 전달 · URL 동봉)**
① URL을 줄 때 = 이동 가능하게(클릭·탭으로 바로 열리는 링크).
② **이동 안내 = 무조건 URL 동봉(★)**: 어딘가로 가라고 할 땐(대시보드·설정·문서·레포 파일) 클릭 경로 설명만 말고 바로 여는 URL을 반드시 같이 준다. 계정·프로젝트 ID가 들어가는 주소 = 아는 값으로 채움(플레이스홀더 금지). 문서(설명서·가이드)에도 동일 적용.
③ 문자열(코드·값·복사대상) = 복사 가능하게(코드블록·복사 버튼).
④ 파일 = `{YYYYMMDD}_{HHMMSS}_{라벨}_v{N}` — 버전이 바뀌면 반드시 N을 올려 새 파일로(같은 파일명 덮어쓰기 = `SendUserFile`이 이전본과 겹침 = 금지). N = 그 산출물의 수정 횟수. `docs/reports/` 산출 동일(초판 = 구 `{yymmdd}_{라벨}` 허용·재전달 = 풀 규격).

**제24조(선택질문 · §선택질문)**
① 사용자 선택 분기 = 텍스트 나열 말고 `AskUserQuestion` 버튼+알림(폰 탭 한 번). 맥락 1줄만 버튼 위에, 자유 입력 = Other.
② 적용 지점: 편향 가드 3선택지 · STOP 후 분기 · 베스트 픽 · 앱 되묻기 · `/3` 동시 질문(이름+플랫폼 1창) · `/k` 분기.
③ 단축 타이핑(`4`·`ㄱ`·STOP) 멀티턴 = 병행(버튼 = 대체 아닌 추가).
④ 각 앱은 자기 분기에서 이 조를 따름(뉴스 = `apps/news/01`).

**제25조(PR 웹훅 · §PR웹훅)** — 웹훅 축적 = 컨텍스트 잠식(압축 가속).
① *watch/지켜봐/babysit* 명시 없으면 자동 감시 OFF.
② PR 머지 직후 `unsubscribe_pr_activity`를 그 번호로 즉시 호출(재구독 = 또 즉시 해제).
③ `이거 꺼` = 알려진 전 PR 번호 모두 해제.
④ webhook 블록 = 답장 없이 처리(보통 무액션). 메시지가 또 쌓이면 = 살아있는 구독 → 해제. 압축으로 맥락 잃어도 이 조대로.

**제26조(모델 · §모델)**
① 세션 작업자·검증·평의회 = **opus 5(최대사고)** 고정. 약한 모델 = 품질·일관성 미보장. 파이프라인 모델과 별개 축(검증가까지 내리지 말 것).
② 모델 ID = 기틀 — 예외 확대·축소 = 운영자 확인 필수. 등재표:

| 경로 | 모델·노력 | 비고 |
|---|---|---|
| `gate_judge.py`(경중 채점) | **`claude-opus-5` · effort `max`**(운영자 260810 "채점 소넷5가 아니라 오퍼스5 max로") — 260630 sonnet 강등 되돌림. 계기 = 전속계약 기사 grade3 오채점(룰북 명문 [0]) · 3점 1.7배 승격 후 랭킹 최상단 직접 오염 | `breaking_judge.py`(속보 이진) | `claude-sonnet-5` · effort 미사용 | LLM 콜의 81%·다층 보정이 경계오차 흡수 = 쿼터 50~65% 절감. env `GATE_MODEL`·`BREAKING_MODEL`(breaking-judge.yml). 외신 제목 번역 편승(`TRANS_ON`·`title_ko` 표시 전용·RUBRIC 해시 밖) |
| judge 호출 모드 | `--safe-mode` | cache_creation −97.2% 실측 · 제60조④ |
| 생성·하드작업(analyze·ask·revise·cardmake·kmake·lymake·more_images) | opus 5 | 날조 가드·창작 품질 직결. 롤백 = workflow env 두 줄 제거. effort: ask·analyze = **medium**(260727 일괄 하향 — opus5 medium ≈ 구세대 high급 품질·지연 절감 · env `PIPE_SEARCH_EFFORT` 롤백=high) / 수정·카드·영상·자막 = **max** |
| track-cap(트래킹 카드 캡션·병합 힌트) | opus 5 max | `shared/model_env.sh` PIPE_MODEL · 전면 fail-soft · 롤백 = track-make `cap` 게이트 · 정본 `apps/track/00_지침` §1-1-d |
| clipmake(쇼츠 클리퍼 구간픽) | opus 5 max | PIPE_MODEL · turns=1·영상당 1콜·전 도구 disallow · 롤백 = 클리퍼 카드 미사용(opt-in) · 정본 `apps/edit/00_지침` §1-b-1 |
| 음원 songmake(프롬프팅·가사·스타일) | opus 5 effort max | PIPE_MODEL · 구독 = 과금 0 |
| 음원 곡 생성 | 구글 Lyria 3 Pro(`lyria-3-pro-preview`) | `song_lyria.py` · GEMINI_API_KEY 공용 · 곡당 ~$0.08 |
| 음성 클로닝 | Replicate RVC | 학습 `replicate/train-rvc-model` ~$0.32/보이스 · 입히기 `zsxkib/realistic-voice-cloning` ~$0.04/곡 · `REPLICATE_API_TOKEN` · 동의 게이트 = 본인·권리 보유 음성만 |

③ **승격 절차(운영자 260725 한 수 · 손 grep 금지)** = 모델 ID·표시명 정본 = `shared/models.json` 단 하나. 올릴 땐 `python3 shared/apply_models.py <티어> <새ID> "<새 표시명>" "<새 한글명>"` 1발 → 전 호출처(스크립트·워크플로·functions·뷰어·지침·이 전문) 일괄 치환 + 정본 자동 갱신 + 구세대 은퇴 등재 → `git diff` 확인 → `python3 shared/check_refs.py`(rc=0) → 커밋. **빠뜨린 한 곳은 `check_model_ids` 게이트가 커밋을 막는다**(구세대 표기 잔존 0 + 미등재 ID 0 · 260725 신설 첫 런에서 미등재 haiku 핀 채굴). 치환 제외 = 원장·보고서·동결본(과거 실행 기록 = 제11조 append-only) · 런타임은 리터럴 유지(Functions·정적 뷰어는 파일을 못 읽는다 = 결합 불가). **벤더 축(비-Claude 종량제)** = `models.json vendors`(gemini 렌더·lyria 곡·gpt 감독·replicate RVC 2종)에 등재 · 교체 = `python3 shared/apply_models.py <벤더키> <새ID>` · 표시명·은퇴 등재 없음(공급사 ID는 회수되면 문서 잔존도 오해) · 구독 축과 달리 **어긋나면 곧장 실패·오과금**이라 교체 후 실호출 1회 확인 필수. ⚠️ 인원은 **'명'**으로 쓴다(`Opus 5인` = 모델명과 붙어 읽힘 · 게이트도 `(?![인명])`로 이 표기를 전제).

④ 음원 3종 = 수동 발사 전용(자동 경로 부착 금지 = 제62조 유료 잠금). 정본 = `.github/scripts/{songmake.sh,song_lyria.py,voice_rvc.py}`.

**제27조(세션 품질 편차 · 세션컷 · §세션컷·§모델 e)**
① 같은 모델·같은 지침이라도 세션마다 품질이 다르다 — 배정 GPU가 세부 품질을 가른다(실증·뇌피셜 아님). ∴ 품질을 그날 컨디션에 맡기지 말고 **기틀(구조·SSOT)·문서(강제 주입·되새김)·게이트(check_refs·평의회·read-back)로 하한을 못박는다**. 새 규칙·결정·산출 = 낮은 세션도 재현되게 문서·게이트로 고정(구두·컨텍스트 의존 = 편차에 무방비 = 금지).
② 저품질 신호 4종 = 1. 존댓말 샘·톤 붕괴 2. 자기 작업 사과 3. 지시 반복 오독(다중지시 소실) 4. 같은 자리 여러 턴 헛돎. 기계가 못 잡는다 — 운영자 눈이 주 센서.
③ 판정 = 단일 신호 1회 = 컷 아님 · 1·2호 발생+재지시에도 재발 = 즉시 컷 권장(톤 붕괴 = 판단력 손상 = 교정 불가) · 3·4호 2턴 연속 = 컷 검토.
④ 매몰비용 = 회수 불가 — 남은 기댓값 < 새 세션 부팅비면 컷이 이득. 단 **산출물은 컷 전에 머지·원장으로 레포에 회수**(컨텍스트에만 있으면 증발).
⑤ 오탐 방어 = 느리지만 정확한 세션은 컷 대상 아님(시간 ≠ 품질). 세션 자신도 신호 감지 = 즉시 STOP+교체 제안(중증 = 자가판단 불가 = 한계 정직). 보조 계기판 = `shared/profile_session.py`(최장갭·재작업 교차확인).

**제28조(플레이그라운드 · §플레이그라운드)** — 상세 = `디자인기틀/플레이그라운드_포터블.md`(이 레포 재이식 불요).
① 디자인·UI 제안/시안/튜닝 = 말·정적 이미지 갈음 금지 → 운영자가 만져서 고르는 자기완결 HTML 1파일. 복사해 붙인 선택값 답장 = 곧 구현 스펙(전달 손실 0).
② 본질 = 변형 갤러리: 한 부품(전송 버튼·눌림 효과·등장 모션)을 서로 다른 방식 N개(권장 5~10)로 실물 구현해 나란히 비교·선택(발산) → 선택된 방식에만 미세조정 축(수렴). 미세조정 7축 = 자간·폰트 크기·위치·불투명도·블러·라인·액티브 효과(액티브도 갤러리로). 완제품 미세조정만 = 무의미(선택지 없음).
③ 골격 = `shared/playground_template.html` 복사 — 골격 재작성 = 위반(품질 편차의 물리 제거). config(축·프리셋·재현부·갤러리)만 채움. 규모 = 기준본(`docs/reports/260711_영상통합3분류_플레이그라운드.html`) 절반 안팎. 강제 = `check_refs.check_playground()`(`data-pg-*` 마커 5요소·near·스크롤 보존·자유 hex 금지).
④ 적용 시점 = 제36조②6호 판정 그대로(확신 변경 = 시안 없이 바로 반영·남발 금지). 시안하기로 한 경우의 형식 = 이 조.
⑤ 산출 = `docs/reports/{yymmdd}_{라벨}_플레이그라운드.html` 자기완결(자산 base64·외부 요청 0·한글 두부 방지). 5요소 전부 필수: 1. 실물 재현 미리보기(9:18 폰 프레임·`:root` 토큰 사본·CII 계승·현행 = 비교 기준 고정·기본값 = 현행 실측) 2. 프리셋 3~4(현행 포함·★ 추천 1) 3. 미세조정 컨트롤(축 그룹 라벨·tabular-nums·평균 50개·축 3분류 = {정렬·간격·높이 자유 / 확립 축 = 사다리 눈금+밖 ⚠ / 색 = 토큰 폐쇄 셀렉트·자유 hex 금지}·⚠ 축 복사 시 기틀 갱신 역제안 블록) 4. 선택값 출력+복사(현행 병기·`계승`/`갱신 후보` 자동 표기·기틀 축 ⚠) 5. 재현 한계 각주.
⑥ 검증 = 헤드리스 실클릭+프리셋별 스크린샷+JS 에러 0 후에만 전달 = 레포 커밋+`SendUserFile` 직접 첨부(Artifact = 반복 수정 루프·외부 공유 보조).
⑦ 후속 = 회신 선택값을 `:root`·CSS 배선+`check_refs` 통과까지 한 작업(`갱신 후보`·기틀 축 = 제13조 편입 체인 전체·제34조 절차).
⑧ 알고리즘 곡선·기울기 튜닝(ageMul·timeAcc류)도 동일 형식 필수: XY 실곡선+드래그 핸들+현행 기준선+순위 시뮬 실시간+선택값 복사(계승/갱신 자동 표기·기틀 이탈 = ⚠ 역제안). 수치 나열·정적 차트 금지. **CUR = 현행 배선값 동기 필수**(어긋나면 비교 거짓). 선례 = `docs/reports/260713_나이감쇠곡선_플레이그라운드.html`.
⑨ 완전판 확립본 = `docs/reports/260706_언락밝기_플레이그라운드.html`(형식·골격 참조 전용 — 내부 :root = 타 레포 사본·값 정본 아님).

**제29조(백그라운드 · §백그라운드)**
① 디폴트 = 포그라운드. 백그라운드(턴이 끝나도 도는 실행 전부: Bash `run_in_background`·백그라운드 에이전트/워크플로·크론·모니터·폴링·서버/와처 상주) = 운영자 명시 지시 때만(그 지시가 곧 승인).
② 자발 금지: 필요해 보이면 포그라운드로 하거나 {무엇을·왜} 1줄 제안만 남기고 계속 진행(논블로킹).
③ 강제 = PreToolUse 훅 `.claude/hooks/bg_gate.py`가 백그라운드 Bash를 deny(팝업 승인제 폐지 — 차단 메시지가 대체 경로 안내·파싱 실패 = 무의견). 롤백 = deny→ask 1줄.
④ 작업 완료·중단 시 남은 백그라운드 정리·보고(유령 프로세스 방치 금지).

## 제6장 수정 모드 (§수정 모드 · 운영 고정)

**제30조(원칙)**
① 단일 원본 = 이 GitHub 레포(`muteno/nomute-editor`). 모든 수정 여기서만. OneDrive·로컬·다운로드 사본 = 폐기(동기화 안 함).
② 트리거 = `git`(대소문자 무관)/`깃` 단어거나 그 말로 시작 → 앱 절대 시작 금지, 즉시 수정 모드로 시스템 파일 편집(뒤에 붙은 말 = 수정 지시). 기사·이슈·카드뉴스 일절 안 만듦.
③ 실행 = 폰/PC 어디서든 레포 열고 기사 던지기(로컬 `,` 트리거 폐지). 롤백 = `그 버전으로` 한마디 → `_versions/` 복원.

**제31조(절차 — 반드시 이 순서)**
① `git fetch origin main` → 최신 main 기준 시작(오래된 복제본 위 작업 금지).
② 백업: 고칠 파일 현재본 → `_versions/{yymmdd_HHmm}_{라벨}/`.
③ 수정. 기틀 변경이면 수정 전 사용자 확인(제34조).
④ `python3 shared/check_refs.py` 통과 확인(실패 = 고치고 재실행) 후 add·commit·push(작업 브랜치).
⑤ PR 생성 → **즉시 main 머지**(draft 방치 금지 = 이게 라이브 확정).
⑥ fetch 후 `git show origin/main:<파일>`로 원격 실내용 직접 읽어 검증·보고(해시+핵심 변경 줄 실측 · 화면·로컬만 보고 `됐다` 금지).
⑦ 충돌 처리: 머지 직전 main이 앞섰거나 squash로 갈렸으면 `git merge origin/main -X ours` 흡수(또는 rebase). 진짜 충돌 = STOP+보고. `force` 안 씀. 단 `-X ours` = **코드 충돌 한정** — `docs/작업이력.md` 등 append-only 문서는 다른 세션 추가분을 덮으므로 `-X` 없이 머지 후 충돌 마커 수동 정리(양쪽 보존·시각순).
⑧ 머지 직전 = 최신 origin/main 재동기화로 타 세션 충돌·역행 점검. 머지 후 = 완료 보고에 운영자 직접 확인 요청(라이브/결과 확인 포인트 1줄) 명시 — 자가검증 대체 아님·그 위의 마지막 단계.
⑨ 턴 종료 전 검사: 1. 내가 연 미머지 PR = 그 턴 안 머지 또는 보고 맨 위 `⚠ 미머지 PR #N — 사유 1줄`(조용히 종료 = 위반) 2. 4문 질문 항목만 대기(`⚠ 답 대기 N건` 최상단 표기)·독립 나머지는 계속 진행·머지.

**제32조(반영 보장 — 불변·최우선)**
① 변경 요청의 `완료` = main 반영 + 실측 검증까지. 완료 선언 = `git show origin/main:<파일>` 확인 후에만.
② 머지가 막히면(force 차단·non-ff·충돌) 우회로 끝까지 반영(흡수 머지 등 · append-only = 수동 보존).
③ 그래도 안 되면 `됐다` 절대 금지 → 즉시 STOP + 보고. 검증 안 된 완료 보고 = 거짓 — 이 원칙이 모든 편의·속도보다 우선.

**제33조(동시 세션 동기화)**
① main = 단일 진실원본 — 클라우드는 세션마다 새로 복제. 각 수정 = 그때그때 main 머지(브랜치·draft 방치 = 새 세션이 옛 룰을 봄).
② 시스템 파일 수정 = 가급적 한 번에 한 세션. 부득이 동시면 머지 직전 최신 main 재동기화 필수. 이미 열려 있던 세션 = 옛 복제본 → 새로 열거나 fetch 재동기화.

## 제7장 기틀 보호·검증

**제34조(기틀 보호 · §기틀 보호 — 구조 변경 = 반드시 묻는다)**
① 대상(기틀) = 앱↔라우터 분리 · 라이브러리↔지침 분리 · 3단계 파이프라인 · 라이브러리 참조 방식 · 출력 포맷 골격 · INVARIANTS · `:root` 구조 · 모델 예외 목록. 특히 라이브러리 재인라인·분리 해체·단계 합치기 = 스스로 결정 금지, 운영자 승인 후에만.
② 모델의 `효율화·정합성 개선` 명목 구조 손대기와 그 조용한 누락이 품질을 복리로 갉는다(모서리 깎임) — 이를 막는 게 이 프로젝트의 핵심.
③ 주변부(문구·임계값) = 자유. 단 큐레이션 임계 = 실질 변경(제9조5호) · 기존 라이브 값 추출(토큰화) = 픽셀 회귀 0 = 자유.

**제35조(기틀검증 · §기틀검증 — 기틀 변경 = Opus 5명 교차검증)**
① 대상 = 제34조① 동일 범위 + 라우터 구조 · 플랫폼 공용 불변(수정 모드·모델·기틀 보호·표기표준) · 환경 Setup script/env 변수 · 라이브러리 SSOT 구조·build 파이프라인 · 앱 지침의 출력 포맷 골격/INVARIANTS. (주변부 = 제외·자유.)
② 절차 = 제34조 사용자 확인 *먼저* → 확정 전 독립 Opus 5명 병렬(동일 후보 적대적 감사·레포 실측) → 수렴 확인 — **전원 APPROVE 아니면 STOP·재설계**(이견 = 다수결 맹종 말고 근거로 해소·기각 사유 기록) → 통과 후에만 제31조 절차 전체로 확정.
③ 한계(정직) = 소프트 룰. 교차검증은 ground-truth 영역(구조·스크립트·참조정합)에서 값함 — 순수 문구·톤은 효용 작음.

**제36조(검증 표준 · §검증)** — 검증 요청·수정 모드 자가검증 전부 적용.
① 검증가 전원 opus 5(`claude-opus-5`)·effort max. 약한 모델·저노력 금지.
② 인원 티어:
  1. 상시·기본 = 자가검증: 모든 수정 모드 작업은 머지 전 `check_refs`+원격 read-back(옛 5인 상시 폐지).
  2. 평의회(정본 인원 = CLAUDE.md [E10] · 현 8인) = 진입 게이트 3조건 중 하나일 때만: {실측으로 안 좁혀지는 진짜 불확실 / 되돌리기 어려운 기틀·파이프라인 구조(제35조 동반) / 운영자 명시(`분신술`·`검증해줘`)}. 셋 다 아니면 평의회 금지 → 자가+CI로 끊는다(자명한 버그·회귀 = 건당 분~십분).
  3. **제사 금지**: 평의회는 결정을 가르는 도구지, 이미 완성한 결과물을 둘러앉아 추인하는 사후 의식이 아니다. 검증이 목적이면 자가+CI가 그 일을 한다.
  4. 국소 뷰어 손질(단일 `viewer/*` UI/UX·동작·구조 영향 작음·기틀 아님) = 자가로 끊는다.
  5. 정본 인원 미만 감축 = 제24조로 명시 승인 후에만(정본 = CLAUDE.md [E10] · 현 8인 = 운영자 260718 명시 감축 반영 · 무응답 = 정본 유지).
  6. 순수 시각/목업 = 평의회 완전 면제 → 자가 시각확인. 경계 = 로직·동작·상태·데이터 한 줄이라도 끼면 면제 아님(`:root`·핸들러·렌더 분기·파이프라인·전환 애니메이션[배너 크로스페이드] = 동작 = 국소여도 티어 재판정). 시각 제안 기본 = 바로 반영 후 라이브 확인(시안 남발 금지 — 폰 PNG = 한글 두부 실측). 시안 필요 시(대규모 구조·새 인터랙션·비가역) = 제28조 HTML(PNG 금지·한글 폰트 폴백 필수).
③ 어느 쪽이든 각기 다른 앵글 적대 감사(메커니즘·호환성·헤더/보안·모바일·메모리·에러처리·다운스트림·버전·통합·총괄). 추측 금지 = 레포·소스·라이브 실측 근거.
④ READ-ONLY 하드 계약: 평의회/분신술 에이전트 = 분석·보고만 — 레포 1바이트도 못 바꿈. 전면 금지 = git add/commit/push/branch/merge·PR 생성/머지·Edit/Write/NotebookEdit·파일 생성/삭제. 산출 = 텍스트만. 메인 루프가 레포 변경의 유일 행위자. 강제 3중:
  1. 소환 프롬프트 첫 줄: `READ-ONLY: 어떤 git/파일 변경·커밋·브랜치·PR·머지도 하지 마라. 발견은 텍스트로만 보고하라. 모든 변경은 메인이 한다`
  2. 읽기전용 에이전트타입 필수 — 평의회·검증·분신술·아이데이션 = 예외 없이 `subagent_type=Explore`(Edit/Write 미보유 = 기계 차단). `general-purpose`(전권) = 읽기·평결 에이전트엔 금지.
  3. 발사 후 `git status`/`branch`/`log`로 rogue 브랜치·커밋·PR 사후 점검. 위반 = 즉시 보고 + 산출 무효.
⑤ 실행 보장: 말만 하지 말고 반드시 실제 `Agent` 툴로 N명 병렬 소환(머릿속·서술 대체 금지).
⑥ 만장일치 루프: 평결 = OK/주의/불가 → 문제 전부 메인이 직접 수정 → 재검증(필요 시 재발사) → 전원 OK까지 루프 → read-back 실측 → 그제서야 `검증 완료`. 어느 단계든 미완 = `됐다` 금지.
⑦ 기틀이면 이 조 + 제34조 둘 다.
⑧ 한계 = 소프트 룰. 순수 문구·톤 = check_refs+read-back 자가검증이 낫다.

**제37조(분신술 · §분신술)**
① 운영자 호출어 = `분신술`. 입력에 있으면 opus 5·effort max·adaptive thinking 정본 인원(CLAUDE.md [E10] · 현 8인)을 실제 Agent 툴로 병렬 소환(`subagent_type=Explore`·READ-ONLY 계약 프롬프트).
② 각자 다른 앵글+적대 시각으로 전 절차를 훑어 5종: 피드백 · 검증(실측) · 효율성 · 품질 · 아이데이션.
③ = 제36조 평의회의 명명된 상위판(인원 정본 = CLAUDE.md [E10]). 절차·READ-ONLY·만장일치 루프·read-back 전부 상속. 변경·머지 = 오직 메인.
④ 묘사 말고 실제 소환(절대) + 발사 후 rogue 사후점검. 출력 = 앵글별 핵심 → 수렴 평결(OK/주의/불가) → 메인 조치 목록 → 아이데이션 베스트픽.
⑤ fable 평의회: 1. 중요 영역의 미적지근한(애매·판단 갈리는) 부분 = fable 교차검증 디폴트 2. fable = 고비용 → 시간 상한 보통 10분(도달 = 회수분으로 수렴·미회수 앵글 = 메인 실측 갈음) 3. 발사 전 운영자 고지(무엇을·몇 명·왜 1줄).

**제38조(라이브러리 SSOT 보호)**
① /k 단일 정본 = `apps/k/library/archive_media_master.tsv`(SSOT) → 유닛(01a~e·02~47)은 `shared/build_library.py` 자동 생성·투영. 편집 = SSOT에서만 · 유닛 직접 수정 금지(정합 = check_refs 강제).
② SSOT 기존 항목 수정(덮어쓰기)·삭제 = 사용자 이중 허락{1차 승인 → 2차 재확인 = 서로 다른 두 번} 필수. 새 ID 추가·순서 정리 = 무권한 OK. `정리·효율화` 명목 임의 수정·삭제 절대 금지(불응·모호 = STOP). 실무 절차 = `apps/k/00_지침_에디터_클링` §10.
③ 아카이브는 조용한 누락이 가장 치명 — 별도 잠금의 이유.

## 제8장 디자인 (§디자인 · 항상 로드) — 정본 위치 인덱스 = 규칙2번 `디자인기틀/디자인기틀_SSOT.md`. 이 장·`:root` 구조 변경 = 기틀(제34·35조).

**제39조(대원칙 · 팔레트)**
① 계승이 디폴트: 모든 UI/UX 작업(버튼·닫기·아이콘·모달·마진·색·폰트·글래스)은 새로 디자인하지 말고 기존 토큰·컴포넌트를 계승(말 안 해도 디폴트 — 말해야 하는 건 예외뿐). 일관성 = 품질.
② 색 = 기존 색 유지가 절대 원칙:
  1. 기존 색(계승 컴포넌트·`:root` 토큰·형제 요소) = 그대로 유지.
  2. 새 색 창작(임의 raw·새 토큰) = 게이트가 기계 차단(진짜 새 슬롯 = (갱신) 정문 = 기틀 승인). 기존 색 제거·`무채색으로` 류 모호 지시 = 멈춰 묻지 말고 최보수 해석(형제·셸토큰 정렬)으로 진행 → 보고에 ⚠대체 명세 + `이렇게 해석했다 — 의도 맞나?`(제3조②).
  3. `통일` = 예외 = 묻지 말고 즉시: 팔레트 밖 아웃라이어(base 상속 leak·제각각 raw·형제와 어긋난 틴트)를 셸토큰·형제 색에 정렬(무채색화·통일) = 계승 디폴트. 걸리는 건 팔레트에 없는 새 색 창작·의도된 색을 없애 통일 깨는 경우뿐.
③ 브랜드 팔레트 = 폐쇄형: `:root` 컬러칩만 — 외부 색 반입·유사색 신규 절대 금지(난립 = 브랜드 붕괴). 각 역할색 유일:

| 토큰 | 값 | 역할 |
|---|---|---|
| `--accent` | #00EED2 터쿼이즈 | 코어(구 형광그린 퇴장) |
| `--accent-2` | 라임옐로 | 이력(`--hist-accent`)·최소화(구 보라 #c24bf5 퇴장) |
| `--accent-3` | #e23b2a | 레드 유일(`--bias-r1` #ff7a6b는 코랄 = 별개) |
| `--accent-4` | #FFE13D 네온 골드레몬 | warn·arm·thumb·amber·`--cat-eco` 공유·누적 칼럼 테마(구 오렌지 퇴장) |
| `--accent-5` | #0FFD02 형광그린 | info·naver(구 코어색 이동·naver 실브랜드 정합) |
| `--accent-6` | #FF5EC8 네온핑크 | 영상 라디얼 강조·`--cat-cul` 공유(캡슐 재사용 아닌 신설) |
| `--accent-bright` | #5AFFE6 밝은 터쿼이즈 | 완료 그라데·플래시(구 accent-4 별칭 해제) |
| `--bias-l2`/`--bias-l1`/`--bias-mid`/`--bias-r1`/`--bias-r2` | #3a6ddb 딥블루 유일 / #38C6FF 스카이시안(`--cat-pol` 공유) / #eef7f0(팔레트 유일 흰·본문 흰과 단일) / #ff7a6b 코랄(`--cat-intl` 공유) / 빨강 | 편향 5단 |
| `--mut` | 중간 회색 단일 | 중간 회색 = 무조건 mut(뜬 회색 금지·구 `--fg-2` 폐지 = 보조텍스트도 mut) |
| `--glass2` | rgb(18,19,22) α.55 | 블랙 글래스(구 `--glass` .42 = 고아 폐지) |

④ 시맨틱 별칭 토큰 = `--danger`/`--warn`/`--amber`/`--info`/`--arm`/`--thumb`/`--warn-rgb`(팔레트 값의 역할 별칭 · 값 정본 = `:root`). SNS 탭 시안 `#0cd0f7` = 팔레트 밖 표면 전용 raw(info와 분리). 새 색 필요 = 운영자 확인. 확립본 = `docs/reports/260705_브랜드컬러_확립본_v3.html`(현행 지시 = 규칙2 §4ⓐ).

**제40조(카테고리 색 — 구분자 전용 캡슐 `--cat-*`)**
① `CAT_COLOR` 6종: 정치=`--cat-pol`(#38C6FF·`--bias-l1` 값 공유) · 사회=`--cat-soc`(#2CF5A5 네온민트) · 경제=`--cat-eco`(#FFE13D·`--accent-4` 공유) · 국제=`--cat-intl`(#ff7a6b 코랄·`--bias-r1` 공유 — 정치↔국제 크로스) · 문화=`--cat-cul`(#FF5EC8·`--accent-6` 공유) · 테크=`--cat-tech`(#AC5CFF 네온바이올렛·#121212 위 대비 5.1:1).
② 인라인 raw hex 금지 = `var(--cat-*)`만. 카테고리엔 시스템 예약색(매체수 `--accent`·긴급 `--accent-3`) 안 씀. 새 카테고리 = `--cat-*` 토큰 신설부터(승인·raw 금지).
③ `--cat-*` = 폐쇄 팔레트의 구분자 전용 예외 캡슐 — 구분자 밖 재사용 금지. 등재 예외 = 트렌드 플랫폼 구분자(구글·유튜브=`--cat-intl` · 틱톡=`--cat-soc` · 인스타=`--cat-cul` · X=`--cat-pol` · 값 정본 = `TR_PCOL` · 다른 표면 확장 = 재승인).
④ 정본 = `viewer/index.html` `:root --cat-*`+`CAT_COLOR`+`catChip`+`articleCat`+`.ct-cross`.

**제41조(산문 최소 · 빈 상태)**
① 컨트롤 옆 사용법 설명 문구(힌트 줄·안내 캡션) = 기본 넣지 않음. 버튼 라벨·상태색(on=accent/off=mut)·픽토그램·비활성 dim이 어포던스로 설명. 표기 허용 = 단위(분·px)·파괴적 동작 경고·접근성 라벨(aria·SR 전용 = 삭제 대상 아님)만. 어포던스 부족 = 문구가 아니라 컴포넌트를 고친다. 신규 UI도 계승.
② 빈 상태 = 조용한 공백이 디폴트: 필러/설명 문자열(`곧 표시됩니다` 류) 전면 금지 — 그냥 빈 화면. 예외 = 사용자 행위 피드백 1줄만(검색 `결과 없음`·`불러오는 중…`·에러). 문구 넣고 싶어지면 = 어포던스 부족 신호 → 화면을 고친다.

**제42조(3계층 · 거울 · CI)**
① 값 SSOT = `viewer/index.html` `:root` — 색·반지름(`--r-s/m/l/pill`)·간격(`--sp-1~4`)·blur(`--blur-s/m/l/xl`)·버튼(`--btn`/`--btn-sm`/`--btn-xs`)·타이포(`--fs-*`·`--fw-*`·`--lh-base`·`--font-status`[구 Orbitron 폐지·현 Pretendard 렌더·호환 유지])·모션(`--ease`/`--dur`/`--dur-fast`)·z 레이어(`--z-nav`/`--z-remote`/`--z-remote-pop`/`--z-float`/`--z-toast`/`--z-reason`/`--z-min`·`--z-min-pop`·`--z-min-shield`·`--z-min-chip`·`--z-min-ghost` = 최소화>리모컨>내용 명문화 · 모달 top-layer만 reparentNav 처리·컴포넌트 내부 1~8·드롭다운 20~22 = 로컬 raw 유지). 값은 여기서만 정의(라우터·문서에 hex/px 복붙 금지 = 충돌원). 신규 CSS = `var()`(없으면 근접, 정 없으면 토큰부터).
② `viewer/tokens.css`·`디자인기틀/구성도/base.css` = build 산출 거울 — 직접 수정 금지(다음 build가 덮음). 4뷰어(thumb/ly/k/comp) = 의도적 독립 팔레트(thumb `--bg #0b0d0c`·4뷰어 `--mut`/`--line`/`--pan` = 차분한 툴톤 · 색 공유 안 함 = 각 inline `:root` 정체성·`--fg`만 전 뷰 #eef7f0 단일) · 공유 = 구조 토큰 48개(반지름·간격·blur·버튼·타이포·모션·`--z-*`·`--press-*`)만. tokens.css = 색 금지(hex/rgba 유입 = rc=1). **색 통일·전체 :root 상속 = 영구 금지**(재색칠 회귀).
③ `디자인기틀/구성도/` = 사람이 보는 인터랙티브 사인오프 단일정본 체계(9 HTML+`base.css`+공통규약 md). base.css `AUTO-MIRROR` = `shared/build_design_mirror.py build`가 :root 통째 복사. 드리프트 처분 = 항상 viewer `:root`가 정본(구성도를 맞춤·반대 아님). 새 구성도 = :root 대조검증 후.
④ 닫힌 루프: `:root` 한 줄 수정 → build → 거울·9데모 자동 동기. 거울 정합·참조·JS구문 = check_refs 하드게이트(rc=1) + CI(`.github/workflows/check-refs.yml` PR마다). 브랜치보호 required 토글 = 미적용 유지(봇이 main 직푸시 — 켜면 봇 막힘·재추가 금지).
⑤ 버튼 인터랙션 마스터 가이드북 = `디자인기틀/구성도/00_가이드북_버튼인터랙션.html`(+md+기틀북 css) — 모든 버튼·눌림/시맨틱 모션/재확인·픽토그램 총정리. 새 버튼·이식 = 패턴 계승(재설계 금지). 값 SSOT는 여전히 viewer :root.
⑥ 강제 = `check_refs.py check_design()` — 9뷰어 raw baseline(index·thumb+도구 7뷰어 전부 편입 · 신규 뷰어 = 실측 seed 즉시 편입 관례). raw 줄이면 baseline도 낮춰라.

**제43조(CII — 컴포넌트 계승 인덱스)**
① 기계가독 정본 = `디자인기틀/CII_컴포넌트계승인덱스.md`. 새 버튼·입력칸·모달·아이콘 제작·이식 = 표의 정본 셀렉터 계승(재설계 금지) · 표에 없으면 만들고 행 추가(등재).
② 핵심 규칙: 닫기/최소화 묶음 우측 `margin-left:auto` = 래퍼에 · 이미지 위 오버레이 = 불투명 `--bg`(구 검정20% 폐지) · 누름 scale 맥락별 계승 · 아이콘 SVG만.
③ 후속 로드맵(P1 SVG 공유 / P2 tokens.css 공유 / P3 표↔코드 게이트) = CII 문서. 상세 감사 = `docs/reports/260628_UIUX_기틀_분신술10인.html`.

**제44조(모달·팝업 셸·팝업 패턴)**
① 단일 셸: 헤더 띠 `.modal-head`(배경 `--modal-head-bg`.5+blur `--blur-l`+border-bottom `--line2`) · 탭 띠 `.modal-tabs`(`--modal-tabs-bg`.4·세부메뉴 있을 때만) · 내용 = 투명. 컨테이너 = 중앙 모달(`<dialog>`·딤) = `--modal-glass`(.64) / 앵커 팝업(`div`·딤 없음) = `--modal-glass-anchor`(.75). 값 SSOT = `:root` 셸토큰 4종(raw 창작 금지). 신규 팝업 = 이 셸 계승(재설계 금지).
② 프로스트 규칙1(flicker 방지·기본값): `<dialog>` 컨테이너·입력칸 위 오버레이 버튼(`.askclip`·`.iobtn-edge`) = backdrop-filter 금지(타이핑 리페인트 재샘플 = 모바일 번쩍). 프로스트감 = `::backdrop` blur7 + 헤더 띠 `--blur-l`만(정적 = 타이핑 무관). 스코프 = `<dialog>` 한정 — DIV 앵커 팝업(`.bpop`·`.qpop`·`.msgpop`·`.pmenu`·`.min-pick`·`.pubpop`)은 `::backdrop` 없음 = 컨테이너 blur가 유일 프로스트원 = 유지가 정본. 입력 담은 DIV 앵커(`#lockpop`·`#marqpop`·`.sc-rsn`) = 통일 미완 후속(별건 · thumb `.cfm`/`.abdlg`도 후속).
③ 규칙2(text-bleed 방지): 알파는 `::backdrop` 프로스트 여부로 도출 — `::backdrop` 투명 모달(`revdlg`·`crevdlg`·`tredodlg` = 뒤 원문 보이기용) = 컨테이너 알파 1 완전 불투명 필수 / `::backdrop` blur7 모달(`editdlg`.94·`pastedlg`.98·`pindlg`.72/.8) = 근불투명(.9+) 허용 / 뒤가 피드/이미지뿐(`#askdlg`·`#tooldlg`) = 반투명 `--modal-glass`. base `dialog` 배경 = 무채색 안전 기본값(override 안 하면 상속).
④ 별도 = `#dlg` 리더(이미지 배경 자기완결 = 셸 아님). 앵커 팝업 전폭 = `margin:-컨테이너padding`. 초록 시그니처 워시(`rgba(27,44,32`·`rgba(24,40,29`) = green_wash 하드게이트. 문자도형 `‹›`·`×` = nm-svg `CHEV_*_SVG` SSOT. 헤더 띠 별칭에 `#pindlg .pin-head` 포함. 시각 확립본 = `docs/reports/260704_모달셸팔레트_확립본.html`(스냅샷·값 정본 = :root).
⑤ 모달/팝업 + 폰 뒤로가기 = `<dialog>`+`history.pushState`(열 때)·`popstate` 닫기(`tooldlg` 패턴) — 백버튼 = 직전 화면 복귀. X/백드롭/ESC 닫기 = 푸시 칸 정리.
⑥ 프로필 앵커 목록 팝업 = 메시지함 `.msgpop` 계승: 무채색 글래스(`linear-gradient(165deg,rgba(24,25,28,.66),rgba(16,17,19,.74))`+`blur(26px) saturate(1.5)`) · `transform-origin:top right`+`msgUnfold` · 위치 = `placeMsgpop` 미러 · 닫힘 = 즉시 hidden · 헤더 = 제목+X.
⑦ 처리중(비동기 2~3초) = 타이틀 옆 인라인 `변경중`+`.gdots`(점 3개 순차 페이드·폭 1.1em 고정·기존 `--fs-*` 토큰·타이틀과 중앙높이 정렬). 전체화면 딤 오버레이 지양. 입력칸 = `.busy`+`pointer-events:none`.
⑧ 함정: `backdrop-filter`·`filter`·`transform`·`perspective`·`will-change` 가진 조상 = fixed/absolute 후손의 컨테이닝 블록 생성 — 모달 안 `position:fixed;inset:0` 전체화면 오버레이 = 그 모달에 갇힘(전역 `dialog{backdrop-filter}` 상속 주의).
⑨ 무채색 글래스 크롬(중립이어야 하는 팝업·드롭다운·선택 메뉴: `.sc-rsn`·`.bpop` 등) = 색 틴트·뒤 색 비침 금지 — 배경·테두리·hover·입력칸 전부 중립(`--fg`/`--mut`/흰 알파) · 충분히 불투명(.94+) 또는 `backdrop-filter`에 `saturate(0)`(blur 유지·색 0). 예외 = 색이 의미인 요소(PASS/PICK 버튼·상태색·on-state·콘텐츠) 유지 — 규칙은 크롬 한정.

**제45조(버튼)**
① 글래스 아이콘 버튼 = `.sbtn`(`--btn` 34px·blur·둥근사각·`:active` scale .85·`.ok` 플래시·`.busy`/`:disabled`). 아이콘 = SVG만(이모지 금지).
② 닫기(X) = `.tool-x`/`.dlg-x` 글래스(`--btn-sm` 30px·우상단·`.dlg-x` = absolute 우상단). SVG X path `M6 6 18 18M18 6 6 18`·stroke 1.8 단일 권장(잔존 문자 X = 점진 통일).
③ 다운로드 = `.dlbtn` 단일 표준(전 앱): 강조색+10% 반투명(`rgba(var(--accent-rgb),.10)`)+클릭 체크 팝+링 리플(위임 핸들러 `.dl-ack`). 새 다운로드 버튼 = 색·효과 따로 만들지 말고 클래스만 부착. 위치·크기 = 컨텍스트 클래스(`.imgdl`·`.timg-dl`·`.jvar-dl`·`.cref-dlall`·`.lbox-dl`·`.save` 등). 전체(순차) 버튼 `.cref-dlall` = `--accent-4` 네온레몬(개별↔전체 위계). 완료 표식 = `.dl-done` → `--mut` 영속(URL 키 Set `DLED`·`dlMark`/`dlSeen` 복원·전체 = `job._dled`·세션 단위 = 새로고침 초기화). CSS 정본 = `.dlbtn.dl-done{color:var(--mut)!important}`+고특이도 변형 덮기. 카드 위 다운로드류(저장 `.save`·오버레이 `.ovl`) = 빠짐없이 `.dlbtn`(액티브 통일 — 안 붙이면 누락).
④ 이미지 위 오버레이 아이콘 버튼 플레이트(다운로드·저장·삭제·수정·투표·재생성) = `var(--bg)` 완전 불투명 단판(구 반투명 3단 폐지) · hover/active 배경 변화 없음(피드백 = scale `--press-*`+아이콘 모션) · 테두리 transparent · accent = 아이콘 색만. 전역 `.dlbtn` 표준(10%)은 불변 → 컨텍스트 스코프 셀렉터로만 덮음(`.imgdl.dlbtn`·`.timg-dl.dlbtn`·`.slide-acts .save.dlbtn`·`.slide-acts .ovl.dlbtn`·`.lbox-bar .lbox-dl.dlbtn`·`.fileprev .filerm`·`.covimg .covimg-x`·thumb `.imgedit`/`.caredit`/`.gen-it .gen-dl.dlbtn`). 이미지 위 `.sbtn` 액션(`.slide-acts .sbtn`·`.timg-acts .sbtn`)도 동일 플레이트(한 행 혼재 금지·아이콘 흰색 유지). 고특이도 완료 덮기 = `.job .jvar-dl.dlbtn.dl-done`·`.cref-dlall.dlbtn.dl-done`. 발행본 SUMMARY_TPL `.nm-imslot .nm-imdl` = 문서 배경 동값 복사. 도먼트 = `.fi-saveall`(마크업 0)·구 `.jsave` 폐기. 다크 UI 배경 위 버튼 = accent 유지 · 토글 on-state 그린(`.sbtn.voted`) = 상태색 유지. 예외(플레이트 아님) = 이미지 위 라벨/배지(`.slide-no`·`.cap`·`.cref-no`)·항행 화살표(`.feednav`/`.carnav`)·판 넘김 텍스트(`.vbtn`)·첨부 X(`.askimg-x` = 닫기 패밀리).
⑤ 단독 토글·컨트롤 = 텍스트-온리 `.chip` 금지 → 그 표면의 버튼 패밀리 계승(수집함 = `.sc-tg` 글래스 필 · 피드 = `.sbtn` · 모달 = 가이드북 버튼). 계승 소스 = 생김새가 아니라 배치 맥락(형제 유무·표면). 피드 `.chip` = 칩 바 나란한 맥락 전제 — 단독 배치 = 고아 라벨. on-state = 그 패밀리 on 값(상태색 = 무채 룰 예외).

**제46조(아이콘·픽토그램)**
① 표지판성 도형(화살표 상하좌우·삼각형·X·체크) = 유니코드 문자 렌더 금지 → 반드시 대칭 viewBox SVG path 픽토그램(폰트 글리프 = 편심 = 정렬 깨짐). 문자 렌더 허용 = 순수 텍스트·숫자(개수 배지)만. 혼용 금지·잔존 발견 즉시 교체. 정본 = 위 화살표 `M12 19V6M6 12l6-6 6 6` · 닫기 X `M6 6 18 18M18 6 6 18`.
② 아이콘 SVG 상수 = `COPY_SVG`·`PASTE_SVG`·`CHECK_SVG`·`DOWNLOAD_SVG`·`EDIT_SVG`·`LAYERS_SVG`·`THUMBUP/THUMBDOWN_SVG` 등(없으면 24뷰박스·stroke 2·round 규격으로 추가).
③ 픽토그램 누름 = 시맨틱 아이콘 모션 — 위임 1핸들러(정본 = `viewer/index.html` click 캡처): 회전(`ic-spin` 기본) · 아래 바운스(`ic-bounce` 다운로드/저장) · 상승(`ic-rise` 이동/원문) · 체크 팝(`ic-check`). `data-motion` 명시 우선. 새 아이콘 버튼 = 자동 상속(개별 모션 코드 창작 금지 = 드리프트). reduced-motion 무효. 등재 = CII.
④ 정렬 = 셀(버튼 박스) 아닌 픽토그램 시각중심 기준(이식팩 = `디자인기틀/픽토그램_세로가로정렬_기틀_이식팩.md`):
  1. 맞출 셀들 = 동일 토큰 폭 + `place-items:center`(크기 달라도 중심 일치).
  2. 헤더 우측 기준 = 행 우측 기준 — 헤더 padding ↔ 행 padding = 짝, 한쪽 바꾸면 반드시 같이 재계산(정렬 = 여러 값의 연쇄 계산).
  3. 정렬 값(컨·헤더·행 padding·셀폭) = 연쇄 SSOT — 하나 바꾸면 전부 재검증(독립 변경 금지).
  4. 검증 = 눈짐작·스크린샷 금지 → path `getBBox` 시각중심 절대좌표 픽셀 실측 ±1px(서브픽셀 0.2~0.5px 잔여 = 반올림 불가피 — **1px 초과일 때만 값을 건드린다**. 틀어져 보임의 상당수 = 실측하면 서브픽셀).
⑤ 방향성 아이콘(↗ ↻ ▶) = viewBox 편심 → `getBBox` 측정해 `transform:translate` 보정 또는 대칭 viewBox 재정의(대칭 아이콘 X·휴지통·＋ = 편심 0).

**제47조(입력·복붙·첨부)**
① 평문 텍스트 입력칸 = OS 자동완성 끔 디폴트: 편집가능 `<input type="text">`(부제·제목·플랫폼·OPA 등) = `autocomplete="off" autocapitalize="off" autocorrect="off" spellcheck="false"` 4종 세트 필수(모바일 OS 자동완성 바·오입력 차단·readonly/disabled 제외). `<textarea>` = 3종 권장(autocomplete 무효). 맞춤법 = 브라우저 아닌 제작 시점 `functions/api/spellcheck.js`. 정본 패턴 = `viewer/thumb.html`(`#cSub`/`#cTitle`/`#cplat`/`#opaNum`·군/軍 매핑칸)·`viewer/index.html`(`#q` 검색 = type=search 1순위 위험·`.fb-comment`·`.sc-memo-in`·`.sc-rsn-in`)·`viewer/ly.html`(`#url`). 강제 = `check_refs.check_autocomplete()` rc=1.
② 복사/붙여넣기 = 컨텍스트 1버튼(`.askclip`+`updateAskClip`: 비면 PASTE·차면 COPY·성공 CHECK 플래시 · 입력칸 우상단). 아이콘 갱신 = 반드시 `setIcon(el,svg,key)` 경유(`el._icon` 동기) — 매 input `innerHTML` 직접 재대입 = SVG 파괴·재생성 번쩍임 금지·동일 아이콘 재대입 금지·CHECK 직접 대입 시 `el._icon='check'` 짝가드. 정본 패턴 = `attachCopyPaste`/`cardIo`/`updateAskClip`·k/ly `upd()`.
③ 이미지/파일 첨부행 = `viewer/ly.html .filerow` 정본: {빈 슬롯 = 배경 없는 픽토그램 flex1.4}+{버튼 flex1}·gap10·`align-items:stretch` 한 행(전폭 2행 금지). 빈 슬롯 = 텍스트 라벨 폐지·픽토만(`.file svg` 26px·active 축소). 버튼 안 화살표·픽토 금지 = 텍스트만. 상세 = `apps/thumbnail/01_지침_뷰어폼_v1 §4`.

**제48조(레이아웃·헤더·스크림)**
① 간격·마진 = 기존 값/`--sp-*` 재사용(임의 px 새로 정하지 말 것).
② 최외곽 박스 모서리 = `--r-l`(16px) 전 뷰 통일(레거시 `.scrap-cols` 패널 = 기준·뉴스요약 `.card`·SNS `.soc-item` — 뷰마다 다른 radius 창작 금지). 예외 = 모달·팝업 = `--r-modal`(22px). 내부 중첩 박스 = 각자 값 유지(최외곽만 통일).
③ 머리표 계층 = 제52조 + 타이포 토큰 정합.
④ 뷰어 상단 헤더 위계: 위 여백 `--sp-4`×2.25 → 워드마크(News Curation·PGSans 22.5px) → 메뉴 타이틀(영문 메뉴명·워드마크 스타일 15.3px·자간 동배율·시작점 = `--sp-3`) → 타이틀 라인(우측 클러스터·행 `--btn-sm`). 좌·우상단 코너 픽토그램 마진 한도 = 구 새로고침 슬롯(`.vh-acts right:calc(var(--btn-sm)+var(--sp-1))`·모바일 32px) — 최외곽(구 설정) 슬롯 금지. 클러스터 = 한도에 우측정렬(1개 = 한도 슬롯·2개 = +왼쪽·간격 4px). 우상단 상시 = 검색 · 피드 탭 = +필터(한도 = 필터 최우측·검색 = 그 왼쪽). **검색 슬롯 = 전 탭 고정** — 필터 = `display:none` 아닌 `visibility:hidden` 토글(슬롯 공간 상시 확보 = 탭 전환에 검색 위치 불변 · display:none = 흔들림 = FAIL). 필터 팝업 = 타이틀 `필터`+구분선 2단{카드 행(라벨+토글)/칩 그리드}·앵커 = 우측 한도 정렬. 새로고침·설정 기어 = 도먼트(`display:none`·JS 보존·프로필 앵커 팝업 = `vhAnchor()` 폴백). 하단 네비 라벨 = 제51조② 정본(scrap·trend = 영문 정식명 / feed·chan = 한글 — 운영자 260714 · 구 '전 탭 한글 줄임말' 폐지).
⑤ 라디얼(＋) 스크림 = `.rad-scrim` 불투명 밴드 끝 `--scrim-solid`가 라디얼 최상단 버튼(화면바닥 147px = `.radmenu` bottom 41 + `--rad-r` 106)보다 반드시 높게(낮으면 페이드 구간에 걸쳐 중간 검정). **뷰포트별 `@media` 분기 금지** — 버튼 px 고정 = 스크림도 PC·모바일 단일값(분기 = 한쪽만 조정돼 재발). 4변수(`--scrim-bottom/op/solid/fade`) 정본 = `.rad-scrim` 주석. 브리딩 `::after` = 스카이시안(`--bias-l1`) 안개(radBreath).

**제49조(캐러셀·스크롤)**
① 캐러셀/스와이프 = scroll-snap 트랙 + 드래그 넘김(`cardRefCarousel`/`feed-ui`).
② 스크롤바 = `viewer/cscroll.js` 커스텀 오버레이 단일 표준(전 표면: index 전 탭 + thumb·edit·ly·k·song·track·nb iframe): 보이는 바 = 오버레이(불투명 다크 알약 24~64px·스크롤 중 밝힘·pointer-events:none·모바일 동일). 네이티브 `::-webkit-scrollbar` = no-JS 폴백만(색 변경 = cscroll.js가 정본 — 네이티브 규칙 만져도 화면 안 바뀜). 내부 컨테이너 스크롤(모달 이식 탭) = `cscrollAttach(el)` 부착 모드(가까운 positioned 호스트 absolute — fixed 금지 = 제44조⑧ 함정). 새 뷰어 HTML = 폴백 블록+스크립트 두 줄 계승 필수(누락 = 드리프트). 예외 = 의도적 숨김 가로 레일(`.tpg`·`.feedtrack`·`.cartrack` 등 `scrollbar-width:none`).

**제50조(신규 UI 요소의 화면 등장 — 구 사전승인 폐지)**
① 라이브 화면에 새로 생기는 요소(새 버튼·칩·배지·토글·표시물) = 컴포넌트 계승(안 물음)과 별개 축. 묻지 않고 진행하되 3중 안전장치 필수:
  1. 그 요소만 단일 커밋 분리(롤백 = `git revert <sha>` 한 줄)
  2. 보고서에 ⚠배치 딱지 + 어디에·어떤 모양 1줄 + 전후 이미지
  3. `이 위치·모양으로 해석했다 — 의도 맞나?` 의도 확인이 운영자가 묻기 전에 먼저 도착.
② 배치 후보가 명백히 갈리면(2+안) = 제28조 변형 갤러리로 고르게 한다.
③ 스코프 = 새로 등장하는 요소만 — 기존 요소의 계승·수정·통일 = 종전대로(안 물음).

## 제9장 명명·표기·기록 (§표기표준[구 §명명표준]·§기록SSOT)

**제51조(용어집)**
① 앱명 정본 라벨: `news=에디터_뉴스` · `x=에디터_엑스` · `k=에디터_클링` · `1=썸네일_포스트` · `2=썸네일_릴스` · `3=오버레이_저작권` · `4=오버레이_경고문` · `th=썸네일과오버레이` · `comp=합성기_카드뉴스` · `ly=자막기` · `q=큐적재` · `insta=컨설턴트_인스타` · `track=트래킹` · `conv=변환` · `edit=편집기` · `nb=자료화`(track·conv·edit·nb = 뷰어 도구 탭·스킬 없음). 정본 spot = 이 표·스킬·지침 제목(본문 설명체 자유).
② 뷰어 메뉴(운영자 확정 · 쌍 구조): 메뉴1[scrap]=`LEGACY MEDIA`(레거시·수집함) · 메뉴2[feed]=`뉴스 요약`(요약·구 Top Picks) · 메뉴3[trend·id `linkfab` 불변]=`SNS DIGEST`(SNS — 그룹① `국내 커뮤니티` = 구 소셜 다이제스트[정렬 = 버스트 축·`soc-item` 문법 계승·무게이지]·위치 = TOP 레일과 구글 검색 사이) · 메뉴4[chan]=`채널 요약`(채널 — 데이터 = `viewer/insta_data.json`·수치 정본 = insta_signals·빈 상태 = 공백). 쌍 = {1 매체 수집↔2 뉴스 요약}·{3 소셜 수집↔4 채널 요약}. 하단 네비 시각 순서 = LEGACY MEDIA·뉴스 요약·(＋FAB)·SNS DIGEST·채널 요약(운영자 260714 재배치 "레거시·뉴스 요약 좌측 / SNS DIGEST·채널 요약 우측" — 쌍끼리 좌·우 군집·기본 착지 = feed 불변·메뉴 번호 체계 = 코드 참조 정본 = 시각 순서와 별개 축). 네비 라벨 = scrap·trend는 영문 정식명(`LEGACY MEDIA`·`SNS DIGEST` — 운영자 260714 "레거시 > LEGACY MEDIA · SNS > SNS DIGEST" · `.nv-en` = `--fs-xs` 축소·자간 0·nowrap) / feed·chan은 한글(`뉴스 요약`·`채널 요약` — 용어 그대로). 구 sns 딥링크·복원 = trend 자동 매핑. 구 표기(Legacy News·Social Digest·Top Picks·Trends·네비 한글 줄임말 `레거시`·`SNS`) = 이력 문서 전용.

**제52조(인덱싱·머리표)**
① 표기: 숫자 ID = 2자리 zero-pad(`L01`·`S01`·`ENV01` — `L1` 금지) · 코드 범위 = en-dash(`L01–L19`) · 하위 연속 = `~`(`01a~e`) · 비연속 나열 = `·`(`S-1·2·3`) · 화풍 애드덤 = `S-N`(하이픈)·샷 크기 = `S01`(무하이픈).
② 머리표 3계층 = 대주제(기사 제목 = 썸네일·카드 제목 동일 표현) → 부제(기호 없이 굵게/강조색) → 소머리 `•`. 숫자목록(`1.`)과 별개. 예외 = `이 기사의 시사점`(전구 유지).
③ 소머리 `•` 전역 표준(정본 = 뉴스 `index .cref-lbl`): 텍스트 = 흰(`--fg`)·`--fw-x` / 블릿 = 형광(`--accent`)·`--fw-b` / `--fs-label` — 크기·굵기 = 토큰으로(리터럴 13/800/700 금지). 블릿 색만 컨텍스트 분기 = 기본 형광·이력 컨텍스트(이전제작·최소화)만 `--hist-accent`. **회색(`--mut`) 소머리·블릿 없는 소머리 금지.** 적용 = `.cref-lbl`/`p.lbl`·`thumb .csec`(`::before '•'`+gap6)·k/ly/comp·thumb 포스트탭 `label.fl`(`::before '• '`·`.chk`/`.file` 제외 명시클래스)·`thumb .hist-bul`(이력색 span·aria-hidden). 예외 = `.gospec`(readout·의도적 회색). 강제 = `check_refs.check_soremeori()`.
④ 각 도구의 첫 소머리 블릿 = 모달 내 동일 화면선(실측 187px·영상 ly·k 기준 = 부모 `.tool-tabs` 아래 iframe wrap 최상단 `label:first-of-type margin-top:0`+wrap padding 18 · thumb = in-iframe `.tabs margin-bottom:26`이 탭바 높이차 흡수 = 실측 정합 — 건드리면 어긋남). 새 탭/폼도 이 화면선.
⑤ 토글(`.ovfmt`/`.onoff`) 붙는 `.csec` = `margin-block:-3px`로 행높이 기여 상쇄(토글 유무 무관 블릿 동일 화면선). check_soremeori 강제.
⑥ 헤더·타이틀·부제·버튼 라벨 = 텍스트와 SVG 픽토그램 공존 금지 — 단독 또는 상태 교체(투표취소 = 픽토↔`취소` 텍스트 교체·확인 팝업 폐지). 예외 = 시사점 이모지 · 외부링크 새 탭 어포던스 = CSS `::after` SVG(장식 pseudo = 공존 아님·유니코드 화살표 금지·정본 = `.pmenu a[target=_blank]::after`). **어포던스는 함부로 지우지 마라 — 제거·통일 시 대체 방법(SVG 어포던스) 먼저 마련.**
⑦ 구 말머리 기호 = 플랫폼 전역 폐지. 섹션 머리 = `•` 또는 부제만. k/ly/comp 입력폼 라벨 = `•` 소머리 통일(`label.fl::before` 형광 블릿·흰 800). k 비디오 엔진 = `• 스토리 구성` 옆 형광 인라인 태그(`.eng`·별도 `.sub` 줄 폐지).

**제53조(KST — 시각·타임스탬프)**
① 무조건 KST(Asia/Seoul·UTC+9): Actions 러너·Pages Functions·컨테이너 = 전부 UTC → 생각 없는 `new Date()`·`date`·`utcnow()` = 9시간 어긋남. 시각 박을 땐 항상 강제: JS = `new Date(Date.now()+9*3600e3).toISOString()`(정본 `functions/api/pick.js`) · 파이썬 = `ZoneInfo("Asia/Seoul")`(정본 `rate_record.py`) · 셸 = `TZ='Asia/Seoul' date +…`. 파싱 짝 = `+09:00`. 신규 코드·산출·ID·폴더명·로그 전부 필수(UTC mint 발견 = KST 교정).
② 예외 = submit→pending/ask 체인: 의도적 UTC 일관(가드·주석 있음) — 건드리면 대기열 시간 깨짐(그대로 둔다).

**제54조(기록 SSOT 지도 · §기록SSOT)**
① 컨텍스트는 못 믿는다(윈도 축소·압축 왜곡). 결정·판단·측정 결론·룰 변경 = 채팅에 두지 말고 그때그때 해당 SSOT에(코드만 고치고 문서 안 고침 = 다음 세션 방향 상실 = 드리프트):

| 기록 종류 | 위치 |
|---|---|
| 플랫폼 전역 불변·기틀·공용 UX·TODO | 이 라우터(매 세션 자동 로드) |
| 지시 원장(WORK TO DO·Q번호·유령작업) | `docs/요구사항_큐.md`(훅 기계 캡처 · 제2·22조) |
| 수집함 큐레이션(신호·임계·랭킹·실험·측정결론) | `docs/curation-algorithm.md`(§방향·§1·§5·§7·§8·§9) |
| 앱별 콘텐츠·파이프라인 룰 | `apps/<앱>/`(지침·MEMORY·CHANGELOG) |
| 작업 이력 + 개선 우선사항 | `docs/작업이력.md`(append-only) · HTML 보고서 = `docs/reports/` |
| 변경 백업(롤백) | `_versions/{yymmdd_HHmm}_{라벨}/` |
| 최종 진실원본 | git `main`(`git show origin/main:<파일>`) |

② 측정·진단 = 돌려보고 끝 금지 — 결론 1줄이라도 SSOT에 박아 다음 세션이 재측정·재방황 안 하게.
③ 완료된 사고·복구 기록 = `docs/인계_bare도구충돌_judge복구_프로세스개선.md`(judge 파손 진짜 원인 = `--bare` OAuth 미인식 · `--safe-mode` 복구·카나리아 규칙화 · 잔여 = 생성경로 dead `--bare` 정리).

**제55조(일일 점검 루틴)**
① 운영자가 각 섹션 할일/상태를 묻거나 하루 1회 점검 시점 = 메인이 먼저 `python3 scraper/daily_health.py`(읽기 전용·일 1회·같은 날 반복 금지). 한 화면 3가지 = 수집 이상(최신 수집 나이·backlog·구독자) · 알고리즘 향상(grade 분포·breaking·승격 — 심층 = `compare_collected.py`) · 롤백 검토(최근 커밋·분기 라벨 대비). 문제 = 원인 추적→알고리즘 탓이면 롤백 검토, 깨끗 = `정상·롤백 불요`. 결론 = curation §7/§8에 박기.
② 최신 알고리즘 분기 라벨 = `checkpoint/algo-260619-grade3-promote`(검증완 `f8f6f74`). 복원 = `git diff checkpoint/...origin/main` 검토 → revert 또는 `_versions/260618_1927_*`·`260619_0540_*`. 큰 분기마다 라벨 갱신(새 마커+이 줄 교체).

**제56조(TODO — 입력→큐→실패표면 후속)**
① 즉시수정 6종·파이프라인 기틀 3종·뷰어 표면작업·부가 = 전부 머지 완료. 폰 측 잔여 = `~/bin/queue-handler` 재배포 필요(레포 머지만으론 폰 미반영 — 플레이북 §5 재실행).
② 남은 후속(도먼트·별도 PR) = news-analyze git directory-rename 충돌 가드: 재픽이 `pending/failed/`를 약 2개 이하로 비우면 rename 추론 → `CONFLICT(file location)` → silent-loss(현재 dormant·안전임계 상향). 가드 = `pick.yml`/`news-analyze` rebase에 `-c merge.renames=false`.

## 제10장 뉴스 큐 파이프라인 (§파이프라인) — 상세 정본 = `docs/news-pipeline.md` + `apps/news/`. 여기엔 기틀·불변만.

**제57조(착수 전 필독 · SSOT 누적 · §curation)**
① 수집함 큐레이션(누적/최신 분류·정렬·신호 = `scFast`·`scScore`·`fastScore`·`gate_judge`·`breaking_judge`·`report_count`) 손대기 전 = `docs/curation-algorithm.md` 최상단 §방향 필독(신호 5종{cross·연속보도·burst·AI경중·[속보]태그}·두 컬럼·속보판정·랭킹곱). 방향 망각한 국소 임계 패치 = 도돌이표. 새 세션·압축 후 수집함 작업 = 이 문서부터.
② 개념·룰 변경 = 즉시 curation §방향·§8에 반영(= SSOT 누적 — 제일 중요). 기틀(신호 구조·두 컬럼·AI검증 2층·보수성) = 항상 보수 유지(불만 하나에 기틀 흔들지 말 것).

**제58조(두 축 — 자리·사건)**
① 누적 ≠ 긴급 = 두 축. 자리(시간 칼럼) = 신규(나이<4h)/누적(≥4h) — 화면 = `3시간 전`까지 신규·`4시간 전`부터 누적 · 경계 `FAST_MAX_H=4` 단일상수(최신·누적 진입·핀·토스트·배지 전부 결정).
② 사건(배지) = 긴급=속보 = `isBreaking` = AI 2층: `breaking_judge.py` 급발(방금 터진 대형·다수피해·전국주목 — 개별 형사 선고 제외·엄선) AND `gate_judge.py` grade≥2(정본 경로 = `.github/scripts/`). 긴급 사건도 나이 들면 자리만 이동(배지 = 긴급<4h ↔ 이슈 4h~24h · 1일+ = 무배지).
③ 누적 진입 = 4h + {cross≥8 OR 긴급(breaking) OR 연속보도 보조진입(cross≥4 AND [rc≥6 OR rc≥3+강지문≥2.0·잡음마커 제외] — grade 조건 없음·지문 완화 Q286)}. 신규 진입도 동일 원칙(cross≥2·fbJunk만 — grade 거부권 폐지 260720·grade0 저신호는 검증대기 접기 확장이 정리). grade = 긴급탐지지 경중 아님 → 랭킹·진입 = 발행량 우선(gradeW floor 0.5 · CROSS_POW 1.3 · FOLLOW_W 0.5 · 긴급부스트 ×3.0 · 지문 fpW ≤1.25 Q286). 긴급이었던 건 cross<8이어도 4h 후 누적 합류. 연속보도(`report_count`→`scFollow`) = 제일 무거운 가점(양 칼럼 곱).
④ 라벨 확정: 칼럼 = 누적/신규 · 배지 = 긴급(breaking·신규<4h)/이슈 = `([cross≥10(grade3만 8) AND grade(null 또는 ≥2) AND !badgeJunk] OR aged-breaking) AND 4h≤나이<24h`(신규엔 안 뜸). 옛 최신/속보/누적 폐기.
⑤ 이슈 배지 게이트 3종: cross≥10(grade3 = 옛 8 유지 = 저cross 국제 대형재난 구제) + grade 게이트(null 관용) + badgeJunk(시황출발·연성머리표·기업PR 정형구·사건어 가드 — `공시`·`유가족` substring 오컷 방지). 글로우(scLit 16) 동일 게이트 상속. **배지 표시 전용** — 누적 진입 cross≥8·랭킹·fbJunk veto 불변(컷 = 강조 해제일 뿐 칼럼 잔존). 정본 = `viewer/index.html` `issCross`·`scLit` + `build-viewer.mjs` `issEligible` = `check_refs.check_issue_badge_parity()` 하드게이트. 정적 10 확정(동적 임계 = 기각 묘비 · 감시 = daily_health 배지계기판 기준 13~20·30+ 재인플레 경보·0 과조임 경보 · **새 수집 소스 투입 머지일 = 임계 재검토 의무**).
⑥ 아웃라이어 감쇠하한(OUT): 강건 z(중앙값+MAD·병합 후 랭킹축 풀 cross≥8) 2.5~3.5 램프 AND 이슈 배지 자격(issCross) AND rc≥6 AND 나이<24h → 누적 랭킹 시간항(timeAcc)에 하한 = 나이 램프(12h까지 .560 → 24h 정상 timeAcc 선형 착지). 픽·확인 완료 카드 = 하한 제외 · 콜드스타트 풀<15 = OFF · 감시 = daily_health OUT 계기판. 정본 = curation §5·§8.

**제59조(흐름 — 한 기사 3단계)**
① 폰 공유→`pending/**` push → `news-analyze` analyze 잡 = 다이제스트→`queue/*.md` → 같은 워크플로 `card_plan` 잡(needs: analyze) = 자동 카드 프롬프트(텍스트만·`state=text_done`) → 뷰어에서 운영자 슛(버튼→`make-cards.js`→`card-make` mode=shoot) = 제미나이 렌더→이미지(`done`).
② 상태 = `generating`→`text_done`(프롬프트까지)→`done`(이미지)/`fired_partial`(대기)/`failed`(error.log).

**제60조(생성 계약 불변 — `효율화`로 부수지 마)**
① 지침 SSOT 강제주입 = `shared/inject_guidelines.sh` 단일 헬퍼가 live 지침을 프롬프트에 떠먹임(요약·카드 공용). `읽어라`(소프트)·프롬프트에 룰 하드코딩 = 금지. 산출물에 `guidelines_version`(주입블록 sha256) 도장 → 지침 1바이트 변경 = 같은 기사 재생성(조용한 드리프트 차단). 이게 파이프라인의 심장.
② 프롬프트 = stdin 전달(`printf | claude -p`) — 지침 주입이 커서 argv면 ARG_MAX(126) 전건 실패.
③ LLM frontmatter 출력 = 형식 보증 3층: 1. 생성측(`ask.sh`·`analyze.sh` 저장 직전 닫는 `---` 자동 삽입 awk) 2. 소비측 관용 파싱(`build-viewer.mjs parseFrontmatter` — 닫는 `---` 없어도 분리 · `thumb_gen.parse_md`류 엄격 파서 보호) 3. 회귀 카나리아(`digest_guard.py` WARN·비차단). 여닫이 요구 정규식 파서 = 한쪽 누락에 통째 실패 → 관용 폴백 필수. 신규 LLM 출력 파서·생성 스크립트 = 이 3층 계승.
④ 기계 분류 judge = `--safe-mode`(CLAUDE.md·skills·plugins·hooks·MCP 스킵 = cache_creation −97.2% 실측·판정 동일 · env `GATE_SAFE`/`BREAKING_SAFE` '1'). **`--bare` = 전 경로 절대 금지**: bare는 OAuth·keychain을 안 읽는다(ANTHROPIC_API_KEY 전용) — 이 레포 = 구독 OAuth 전용(종량 키 없음·워크플로가 API_KEY도 unset) → bare = 인증부터 rc=1 즉사. 강제 = `check_refs.check_judge_bare()`(judge bare emit·생성경로 bare 기본 ON = rc=1). 생성/도구 호출(analyze·ask·cardmake·revise·kmake·lymake·more_images) = `--safe-mode`도 신중(CLAUDE.md 컨텍스트·MCP/skills 의존 가능 — 정밀분석 후에만).

**제61조(플래그 카나리아 · 폴오버 · 실패 알림)**
① 라이브 파이프라인 플래그(env — judge `GATE_BARE`/`BREAKING_BARE`·생성 `CLAUDE_BARE` 류) = 카나리아 1건 후 승격: 기본 OFF로 머지(라이브 무영향) → `workflow_dispatch` 단건 → 로그 rc=0·의도효과·품질무손상 실측 → 그제서야 기본 ON(근거 첨부). 검증 없이 전면 기본 ON 머지 = 금지(--bare 사고 근본원인). 신규 라이브 플래그도 계승.
② `claude -p` = 폴오버 SSOT 단일 경유: 전 호출(analyze·ask·cardmake·revise/revise-cards·lymake·kmake·clipmake) = `shared/claude_transient.sh`(`is_quota`/`claude_failover`) 경유 · 파이썬 판정 = `shared/claude_py.py`. 쿼터 한도 = 4계정 체인(MUTENO→NOMUTEFB→EMS1130G→MUTENONA) 1단계씩 자동 전환. **자체 쿼터 정규식·자체 폴오버 금지**(한 곳 stale = 전건 실패). Claude가 한도 문구 바꾸면 `is_quota`(sh)+`_QUOTA`(py) 두 곳 즉시 추가. 서브3 = `CLAUDE_CODE_OAUTH_TOKEN_MUTENONA`(env `_ALT3` 고정·미설정 = no-op). 콜러 재시도 상한(`INLINE_TRIES`/`for _try`) = 체인 깊이 4에 맞춤. 한계 = 4계정 전부 주간한도면 무의미. 강제 = `check_refs.check_claude_failover()`.
③ claude -p 실패 알림 = 웹푸시 통일: `.github/scripts/analyze.sh`·`ask.sh` 실패 시 `msg.py set fail-<base>`(딥링크 `/?msg=fail-<base>`)+적재 → 워크플로 `notify_fail.sh`(`if: always()`·VAPID env) 발송. 옛 `claude_health.sh`(인앱 점등) = 폐지 — 재도입 금지(실패 표면화 = `emit_fail_msg`+notify_fail 패턴). 한계 = VAPID 시크릿+구독자 필요 · 뷰어 대기열 빨강 = 별개 유지.

**제62조(과금 잠금 · 썸네일 예외)**
① 자동 카드플랜 = 제미나이 0 이중잠금: Lock A = 자동 워크플로에 `GDRIVE_SA_JSON` env 부재 · Lock B = cardmake `text` 모드 unset. 제미나이/Zapier/Cloud Run = 유료 — 오직 슛(암호 게이트 `make-cards.js`)에서만. 자동경로에 GDRIVE 절대 부착 금지.
② 유일 자동 예외 = 썸네일: `news-analyze.yml` `thumb_gen` 잡(정본 = `.github/scripts/thumb_gen.py`) = `gemini-3.1-flash-image-preview` 직접 호출 · 픽당 2장(포토에디토리얼·웹툰극화 = photo/webtoon — 수채화·시사만평 폐지 = 헬퍼 dormant 보존·photo_close 폐지 유지) · `THUMB_MAX_BATCH=3`·`THUMB_SINCE` 신규건만. 프롬프트 룰 = 타이틀 오버레이 문구 이미지에 안 박음(현장 자연 글자 최소 허용) · full-bleed(빈/검정 띠·여백·레터박스 금지 — 구 `하단 1/3 비움` 폐기) · 핵심요소 상단 2/3 단일초점 · GOVERNING(결정적 순간·스톡 포즈 도피 금지) · 안전(선정·시신 금지 / 만평 = 한글 말풍선·라벨+공인 캐리커처+흰 여백 틀[full-bleed 예외] / 극화·수채 = 공인 닮음 허용[일러스트 전통·SUBJECT 실명] / photo 실사 = 익명 유지[딥페이크 인접] / 사인·피해자·미성년 익명·모욕 금지 = 하한 — 미래 세션 `안전 위반` 오판 되돌림 금지). 참조 체이닝 = THUMB_REF(기본 OFF 카나리아·극화·수채에 대표 실사진 첨부·photo 제외). 해상도 = 1K(썸네일·카드 공용 `gemini_image`). 게이트 = `GEMINI_API_KEY`(없으면 no-op). 카드 이중잠금과 무관·불간섭(GDRIVE 안 붙음·concurrency `thumb-make` 분리).
③ AI 생성 전역 스위치 = 뷰어 설정 `genImgOn`(`settings/app.json`): false = `analyze.sh`가 전 요약에 `no_thumb:"1"` 도장(ask = 뷰어 건별 실효값) → thumb_gen = 제미나이 생성만 스킵, **검색이미지(og:image·image_sources)는 항상 유지**. 도장 = 요약 생성 시점 스냅샷(재ON 소급 생성 없음 = 과금 서프라이즈 차단). 수동 `다시 만들기`(thumb-redo)·`+` 이미지생성·`+N장` = no_thumb 우회 = 항상 AI(수동 슈퍼권한).
④ 검색이미지 = 소스 무관: 우선순위 {원기사 og:image(대표·라벨 없음) → AI image_sources(분석단계 WebSearch 유추 관련기사·최대 3장 `유사` 라벨) → 클러스터 alt_urls → 마커매체}. paste(url 없음)·차단매체(403)도 image_sources로 채움. image_sources SSOT = `prompts/news-analysis.md` frontmatter → analyze.sh(보존) → thumb_gen.parse_md. **Google CSE JSON API = 死**(신규 고객 403 영구·우회 불가 — 키/CX/프로젝트 손대지 마·`GOOGLE_CSE_*` 미사용). og:image = 발행사 선언 신뢰(크기 면제)·본문 유사 = 엄격 필터 → R2 재호스팅(핫링크 차단 0). 전부 실패 = 빈 `search.json=[]` 1회(좀비 재fetch 차단)→AI썸네일 커버. 백필 게이트 = `url OR image_sources`(paste 누락 0). 보안 = SSRF 게이트(사설IP/메타데이터 차단)+매직바이트 검증(SVG/HTML 거부 = 저장형 XSS 방지).
⑤ 저장 = Cloudflare R2 공개 URL(`R2_ACCOUNT_ID`·`R2_BUCKET`·`R2_PUBLIC_BASE`·`R2_ACCESS_KEY_ID`·`R2_SECRET_ACCESS_KEY` 5 시크릿·egress 0). 5개 다 없으면 git 폴백(로컬 PNG 커밋).

**제63조(card_plan 계약)**
① concurrency group=`card-make`(같은 `cards/` 직렬화) · `MAX_BATCH=3` 최신 먼저 · 좀비 `generating` always-sweep(`sweep_stuck.sh`·영구 `프롬프팅 중` 차단) · 헤드리스 무중단(`--disallowedTools`/`--max-turns`) · 이미지 대기 적응형 상한 15분(정본 `apps/news/03_자동화_레퍼런스.md`).
② 건당 생성 타임아웃 = `CARD_TIMEOUT`(cardmake.sh 기본 1500s) ↔ news-analyze·news-ask card_plan `timeout-minutes: 165` = 한 쌍(비대칭 = 한쪽발 카드만 하드킬 림보 · 롤백 = env 900). error.log의 `workspace not trusted`·`MultiEdit` stderr = 무관 공지 노이즈(오진 주의 — reason 분기가 124 = 타임아웃 명시).
③ failed 카드 자동 재시도 = 3회(`CARD_FAIL_RETRY_MAX` env·`0`=끔·vars 노브): 실패 시 status.json `fails`+1(텍스트·렌더·좀비 스윕·수동 실패 전부 카운트·generating 전이 시 보존) → fails 1~3 AND 이미지 0장 = GVER 동일해도 all 배치 재편입(렌더 실패 = 장면 이미지 잔존 = 자연 제외 = 재슛은 운영자만). 4회째 = 수동(뷰어 ✓ 재시도 = 캡 무관 단일 지정). 성공 = fails 소멸. `fails` 부재 구 failed = 특례 소급 없음(GVER 부재·stale = 종전 게이트 그대로). 구 failed 복구 = status.json의 guidelines_version 제거. 수집함 실패 카드 버튼 = ✓ 체크 = 재시도(`fireFailRetry`) / Failed 칩 = 끄기(`requeueFail`).

**제64조(폰 입력 — URL·전문 둘 다 1급)**
① pending line1 = URL 또는 `paste:<sha1 12자>`(전문), 뒤 `# body:`에 전문/선-fetch 본문. 막힌 매체(데이터센터 IP에 WAF 403 — 헤더로 못 뚫음·폰 IP는 200) = 전체선택→텍스트 공유가 운영자 주 워크플로. **폰 핸들러·analyze.sh를 URL 전용으로 `효율화` 금지**(회귀 전례·복구됨).
② 한글 200자+ = 전문경로(media 라우팅 건너뜀·fetch 안 함·`paste:` = `art_url=""`·매체/날짜/기자 본문 추론). 한글량 = 로케일 무관 lead-byte 카운트. 전문경로 = `git`·`termux-api`만으로 동작.
③ 정본 = 플레이북 §5-2 + `docs/termux-share.sh` + `analyze.sh`(`paste:*`·`# body:` awk) + `prompts/news-analysis.md` §0(전체페이지 트림).

**제65조(새 세션 체크리스트)**
1. 작업 첫 손 = `git fetch origin main` — 라이브 파이프라인은 main에서만 돈다(브랜치/draft = 라이브 아님).
2. 요약 지침 해시 ≠ 카드 지침 해시 = 정상(파일집합 다름: 요약=01+MEMORY·카드=00+01+02+MEMORY).
3. `text_done`(텍스트·프롬프트·이미지 0) ≠ `done`(이미지).
4. 자동 = 텍스트까지, 이미지 = 운영자 슛.
5. 라이브 검증 = 머지 후 `git show origin/main:<파일>`로만.

**제66조(뷰어 캐시·배너)**
① `viewer/_headers` = HTML·JSON `Cache-Control: no-cache`(매 로드 재검증 = 새 배포 즉시 반영). 카드 이미지 = `?v=mtime` 버스트. 화면 안 바뀌면 코드보다 캐시·배포부터 의심.
② 세션에서 라이브 curl 검증 불가 — 라이브 = Cloudflare Access 게이트 뒤(curl = 로그인 페이지 약 32KB·PR preview 동일). 라이브 반영 확정 = read-back만, 실제 렌더 = 운영자 눈 또는 전파 대기(1~2분 — 머지 직후 curl 못 봐도 정상).
③ `sw.js` = 푸시 + index 셸 캐시: index(`/`·`/index.html`) 최상위 내비게이션만 stale-while-revalidate(콜드부트 즉시 페인트). **스코프 화이트리스트 = 기틀** — 도구 HTML(thumb/ly/k/comp/track)은 `?v=` 버스트+no-cache `항상 최신` 계약 = 캐시 대상 아님(넓히기 = 재검증 필수). 데이터 JSON·외부 JS·이미지 = SW 불간섭. index 셸만 배포 후 첫 진입 = 직전판 정상(두 진입 연속 옛것일 때만 조사) · `?nosw=1` = 캐시 우회 탈출구 · Access 만료 = SW 자가치유. 롤백 = 삭제 금지·무해화판 배포(정본 = `viewer/sw.js` 주석 런북).
④ 탑배너 = 단일 배너(전 탭 동일 — 탭별 자산·디졸브 레이어·글로우색 전환 폐지). `bannerframe`(글로우 브리딩 = 강조색 고정)›`bannerclip`›`.banner.layer-feed` 하나. 배너 이미지 교체 = 캐시버스트(`?v=`) 필수(이미지엔 no-cache 헤더 없음). 구 크로스페이드 디졸브 기틀 = 차등 배너 재도입 시 부활(즉시 교체·슬라이드 금지 유지).

**제67조(속보 — 검출·알림·푸시·자동픽)**
① 검출·판정: `knews_scraper` 클러스터별 burst(15분 내 동시 매체 최대) ≥3 = `breaking_candidate`(거대 over-merge 제외)+보수메이저 픽 → `breaking-judge.yml`(새 후보 시 디스패치·self-gate) → `breaking_judge.py` Claude 1콜 배치 급발 판정 → `breaking` 확정. rubric 해시 도장(`breaking_rubric`) = rubric 변경 = 자동 재판정. 임계 = `BURST_BREAKING`(env)·rubric.
② 긴급 = 방금 터진+{대형·다수피해·전국주목}: 국내 사고·화재·재난·다수 인명피해·테러 = 알림 / 행정공지·정책발표·의료정책·개별 사법결과(비전국·비고위)·해외 군사·전쟁(사망 10 이하·한국 무관)·개별 강력범죄 소수피해·규제·행정 처분(사고 미발생)·증시 변동성(사이드카·서킷브레이커·급등락 — 구조적 금융위기 제외)·수사 절차 후속(압수수색 등) = 컷(5게이트·엄선). cross≥7 격상 실험 = 롤백됨 — 긴급 = breaking(AI∧grade≥2)만(다수매체만으론 안 띄움).
③ 뷰어 = `breaking:true` 배지·주요도 최상단·프로필 빨강 펄스 링·포그라운드 토스트(seen 재알림 없음·120s 폴·visibilitychange). 정본 코드 = `scraper/knews_scraper.py`·`scraper/to_candidates.py`·`breaking_judge.py`·`viewer/index.html`.
④ 진짜 푸시(Phase 2·앱 꺼도 옴): `viewer/sw.js`(push/notificationclick) → 구독 토글(`#msgpop` 상단·`pushToggle`·VAPID 공개키) → `functions/api/push.js`(GH_TOKEN으로 `push/subscriptions.json` 커밋) → `.github/scripts/push_send.py`(pywebpush · **앱푸시긴급 = breaking AND grade≥3 AND cross≥2 AND <4h만** · dedup `push/sent.json`·죽은 구독 정리) → breaking-judge.yml 발송(+`test_push` dispatch). VAPID 3종 = Secrets(`VAPID_PRIVATE_KEY`·`VAPID_PUBLIC_KEY`·`VAPID_SUBJECT`). iOS = 홈화면 PWA 필수·안드 크롬 바로. 포그라운드 토스트/링 = 동일 grade≥3(`isAlert`).
⑤ 3단 위계: 앱푸시긴급{grade≥3 = 푸시+화면알림+자동픽} ⊂ 긴급{grade≥2 = 배지·핀} ⊂ 일반. 이슈(aged·cross≥10+grade+정형컷) = 별개 축.
⑥ 자동픽: breaking AND grade≥3 AND cross≥2 AND <4h(first_seen) = `scraper/auto_pick_breaking.py`가 판정 직후 `pending/` 적재 → 요약+썸네일까지 자동(카드 = `text_done` 유지 = Lock A/B 불변). 보수 가드 = 사건당 1회 영구 dedup(`push/autopick.json`·event_key/url+제목해시 다중키·KST) · 런2/일8 상한 · `pick_pending` load_active(수동픽 충돌 0). env = `AUTOPICK_MIN_GRADE=3`·`MIN_CROSS=2`·`MAX_PER_RUN=2`·`MAX_PER_DAY=8`. 큐레이션 알고리즘 무변경(downstream 소비만). grade≥2 통일 실험 = 롤백(grade≥3 복귀).

**제68조(카드 슛·edit · /k 레퍼런스 · 수정 요청 인증)**
① 카드 슛+edit = 레포 직영(정본 `.github/scripts/gen_cards.py`): Actions가 Gemini(`gemini-3.1-flash-image-preview`·4:5 텍스트-free 장면) + `apps/comp/card_news.py` 로컬합성(1080×1350) + R2 저장(없으면 git 폴백). 게이트 = `GEMINI_API_KEY`. 슛 = 뷰어 수동만·자동 card_plan = Lock A/B로 제미나이 0(cardmake 슛경로 = gen_cards 전용·레거시 drive_cards 폴백 제거).
② edit = `--edit-card N`: wish 있음/장면 없음 = Gemini 장면 1장 재생성, 아니면 텍스트만(장면 보존·제미나이 0) → card_news 합성 → R2 같은 키 덮어쓰기(`.r2_images.json` 불변·`?v=` 버스트)/`versions/card-NN` 보존·cards.md 갱신.
③ /k 레퍼런스 이미지 = 직영 `k_refgen.py`(prompt.md `## 레퍼런스` → `thumb_gen.gemini_image` → R2 `k_out/<id>/ref.jpg`+`ref.json`·뷰어 `k.html` = ref.json URL 우선). ∴ 카드·썸네일·/k = R2 직영 단일 파이프 = Apps Script·Drive·Cloud Run·`GDRIVE_SA_JSON` 호출자 0(레거시 `drive_cards.py`·`k_refmd.py`·`mint_drive_token.py` = 도먼트).
④ 뷰어 수정 요청 = 구독 OAuth(Actions) 경로 못박음: 이 레포의 Claude 인증 = Claude Max 구독 OAuth(`CLAUDE_CODE_OAUTH_TOKEN_{계정}` 4계정)뿐 — **종량제 API 키 없음** → 직접 Messages API(`x-api-key`) = 영영 불가(구독 OAuth = raw API 인증 미지원·`sk-ant-oat…` = Claude Code 전용·넣으면 401). 수정 요청 = 반드시 `claude -p` = GitHub Actions 경로(`news-ask`/`ask.sh` 패턴·구독·무료·비동기 1~2분). **재요약 금지** = 기존 요약(`a.body`) 입력으로 그 부분만 재작성(기사 재수집·재요약 = 쿼터 손해). 미래 세션 절대 주의: `구독 토큰으로 직접 API`·`종량제 키 사라` 권유 금지 — 구독은 오직 Actions+`claude -p`.
⑤ 지침 파일 rename = `inject_guidelines.sh`의 `guidelines_version` 해시 변경 = 카드 캐시 버스트(미촬영 재생성·촬영완료 보호) → rename = 공짜 아님(R6 의미해시가 경로헤더 제외 = 완화).

## 제11장 인프라·공통 UX

**제69조(제작 deps — 7일 캐시·타임아웃 필수)**
① 제작 워크플로(`*-make.yml`)의 무거운 deps(폰트·ffmpeg·pip·모델) = `.github/actions/runner-setup` 캐시 필수 — pip(`~/.cache/pip`)·whisper(`~/.cache/huggingface`)·apt .deb(`~/.cache/nomute-apt`). 워크플로 = `with: { setup, apt: '<패키지>', whisper: 'true' }`만(첫 런만 다운·7일 적중). 새 제작 앱·deps = `setup.sh` 생설치 말고 캐시 경로로.
② 네트워크 op 타임아웃 필수: `setup.sh`의 apt-get/pip/prefetch = `timeout`(+1회 재시도)로 감쌈(무한 행 대신 빠른 실패·재시도 = 캐시 적중). `*-make.yml` Setup = `timeout-minutes` 백스톱.
③ 정본 = `runner-setup/action.yml` + 각 `apps/*/setup.sh` + `*-make.yml`. 라이브 = thumb·comp(`fonts-noto-cjk`)·ly(`ffmpeg`). 클라 폴 타임아웃(제작 3분) 한 쌍.

**제70조(미디어 첨부 · §미디어첨부)**
① 첨부 받는 앱(`/1`·`/2`·`/comp`·`/ly`·`/news`·`/k`) = 경로 추측·하드코딩 금지 → `shared/attach.py`의 `latest_attachment()`(배치 = `session_images()`). 호출 = `sys.path.insert(0,'shared')` → `from attach import latest_attachment`. 로직 정본 = `shared/attach.py` 하나.
② 환경별: 안드 모바일 앱 = `~/.claude/uploads/<세션ID>/`+jsonl base64 / 안드 웹·PC웹·데스크탑 = 디스크 없음·jsonl만 / 구 코드실행 = `/mnt/user-data/uploads/`(지침 속 이 표기 = 레거시 예시). 동작 = 디스크 우선 → 세션 jsonl 최신 image base64 디코드.
③ 영상 = 디스크 환경(모바일 앱)만 — jsonl에 base64 미포함(실측 확정). 디스크 부재 = 영상 URL(yt-dlp)·SRT/STT·모바일 앱 우회. `latest_attachment(kinds=VID_EXT)` = 디스크만.
④ 입력(첨부) 한정 — 출력 경로(`/mnt/user-data/outputs/`) 무관.

**제71조(완료 알림 — deploy-then-notify · §완료알림)**
① 배포 게이트: 산출 목록(`_meta.json` 등)이 git 커밋→Pages 배포로 라이브 되는 제작물의 완료 푸시 = 그 커밋의 Pages 빌드 완료 확인 후에만 발송(정본 = `thumb-make.yml` 배포 게이트 스텝). 신호 = 커밋의 `Cloudflare Pages` 체크런 completed/success(이 레포 = Deployment·commit status 안 남김 — Deployments API 폴링 = 헛다리) · 산출 커밋 또는 후속 main 커밋(`merge-base --is-ancestor`) success = 통과 · 상한 8분·게이트는 잡을 실패시키지 않음(비치명). R2 직송 이미지 = 즉시지만 목록/매칭 JSON이 배포 소비면 게이트 대상. 신규 제작 앱 완료 푸시도 계승(알림 먼저·배포 나중 = 금지).
② 딥링크 = 쿼리(`?done=<id>`) — 해시(`#done`) 금지(서버 미전송 = Access 재로그인 리다이렉트에서 유실). 뷰어 = 구 `#done` 호환 파싱(`doneReq`·`viewer/thumb.html`).
③ 클라 재시도 사다리(`maybeOpenHist` ≤15회 ≈104s) = 폴백 안전망 유지(주 방어 = ①). 한계 = 체크런 미관측 시 180s 폴백 통과(그땐 ③이 커버). 실패 알림·속보 푸시 = 게이트 비대상.

## 제12장 파일 지도

**제72조(지도 — 요지·핵심 계약·정본 포인터)** — 각 행의 상세 규칙 = 우측 정본 문서(진입 시 로드). 서사 = 해설원장.

| 경로 | 요지·라우터급 계약 | 상세 정본 |
|---|---|---|
| `apps/news/` | 뉴스 에디터(00 운영·01 지침·02 라이브러리·03 자동화·04 구조·05 리뷰·`fact_guard.py` 수치 대조 게이트) | apps/news/ |
| `apps/thumbnail/` | 썸네일 /1~/4. **스크립트 4파일(`nomute_overlay.py`·`nomute_compose.py`·`nomute_copyright.py`·`nomute_reels2.py`) = **불변·import만**(reels2 = 헤더형·기본 2종 출력·`assets/reels2_base.png`).** `01_지침_뷰어폼_v1` = thumb.html 폼 기틀 — **뷰어 폼 작업 = 이 문서부터**. **AI/모델 토큰 0**(순수 PIL — 모델·effort 레버 해당 없음). 맞춤법 = `functions/api/spellcheck.js`(부산대 프록시·무료·비차단·`*별표*` 보존) | 00 v22.25·01·MEMORY·CHANGELOG |
| `apps/x/` | X 게시물(톤·스탠스·금기·트레이닝 로그) | 00 v2 |
| `apps/ly/` | 자막기. STT = **로컬 Whisper large-v3-turbo 단독**(외부 API·키 불필요)·짧은 조각 영한 1:1·의역 2패스 | 00 v2.8·setup.sh |
| `apps/comp/` | 카드뉴스 합성(1080×1350·폭검증 937px). **`card_news.py` = 불변·import만** | 00 v3·MEMORY |
| `apps/k/` | 영상 프롬프트(9단계·15초 서사·@참조·영문 테일·출력 = HTML+AI 오버레이). `apps/k/01_모델프로필_영상엔진.md` = kling/veo/seedance 방언·`[모델:]`/`[설정:]` 마커 해석 = `prompts/k-make.md` — **3면 패리티(k.html↔`functions/api/k.js`↔프로필) = `check_refs.check_k_models()` 게이트**. `assets/`(AI 오버레이 PNG·`make_overlay.py` 재생성). `library/` = 제38조 SSOT 투영(직접 수정 금지·지침 인라인 금지·ID 참조) | 00·01·CASES·MEMORY·library |
| `apps/insta/` | 인스타 컨설팅(5모드·5축·출력 골격 INVARIANT·날조 금지 3종) + 직결 `insta-fetch.yml`+`.github/scripts/insta_fetch.py`(Meta API·매일 06:25 KST·LLM 0콜·`IG_ACCESS_TOKEN` 없으면 no-op·토큰 60일 재발급)·`insta_signals.py`(축별 lift·강건 z·LLM 0콜→`signals.json`)→메뉴4 | 00 v1·docs/인스타_직결_세팅.md |
| `apps/track/` | 트래킹(편집 탭 `기타 기능` 핸드오프 서브뷰·전용 탭 폐지). 플로우 = **분석 1회 → {핀셋/모자이크/키잉/실루엣/크로마} 선택 → 렌더 N회**. 엔진 = `track_analyze.py`(YuNet+IoU+SFace+YOLO11n)·`track_render.py`·`track_chroma.py`·`track_maskfx.py`·`track_keying.py`(SAM2.1·캡 90s/4객체·torch = 키잉 잡 한정 캐시 분리). 순수 CV(토큰 0)+캡션 LLM 1콜(`track_caption.sh`·제26조). 모듈 계약 = `MODULES.md`(M1~M4·UI 무의존). **모델 다운 = media.githubusercontent만(raw = LFS 포인터 함정)**. 파이프 = `functions/api/track.js`+`track-make.yml`(ly-make 미러·up-<id> 업로드 브랜치) | 00 §1.6·§1-1-d·MODULES.md |
| `apps/conv/` | 변환(**재도먼트** — 편집 탭 카드가 전담·복원 = index `CAP_TABS` 1행·파일 보존). `conv_run.py` ffmpeg 단일(→R2→`viewer/conv_out/<id>/video.json`)·`functions/api/conv.js`·LLM 0·캡 300s/60fps 120s+예산 가드(단가 0.30s/출력프레임 실측) | 00 v1 |
| `apps/edit/` | 편집기(`+ > 영상` = **영상 스튜디오 3탭{편집·자막·프롬프팅} 확정** — 트래킹 = 서브뷰·변환 = 도먼트). 카드 스택·컴포지터 = `.github/scripts/ly_burn.py` 확장(편집기 축 결측 = 순수 ly 경로 회귀 0)·음량 = `audio_norm.py` 기본 ON·**자막 = 자막 탭 전담**(sub 카드 도먼트·복원 = ORDER 'sub' 1줄·음영 3축 = 크기 게이지·색 7칩 `OC_BGR`·폰트 4칩 `FONT_FAMILY`[러너 apt fonts-nanum·미설치 = 고딕 폴백])·클리퍼 = STT→claude 1콜(`clipmake.sh`·`prompts/clip-make.md` = 문장 경계·맥락 왜곡 금지)→`clips.json` 3~5개→후보 탭→**운영자 확정 렌더(자동 발행 금지)**·ly 텍스트 컷 = 자막 상세 편집기 `삭제 컷` 토글(기본 OFF·`del` 스팬 동봉 = 번인 시 실제 컷·정본 = apps/ly 지침 §번인)·캡 = 10분 정직 거절·재입히기 = `edit_opts` 승계·산출 = R2→`viewer/ly_out/<id>/video.json` 폴·발사 = `functions/api/edit.js`+`edit-make.yml`(ly-make 미러)·레이트리밋 = `api/_rate.js`(4파이프 공통·429 정직 거절·fail-open)·UI 선택값(발사 게이지·PREVIEW 유일 sticky·기타 옵션 카드 5장 등) 정본 = 지침+작업이력 260711~12 | 00 §1-b·§3·작업이력 |
| `apps/nb/` | 자료화(`+ > 요약 요청 > 미디어 요약` 탭 — 요약 셸 = 가상 탭·askdlg reparent 이식). **제1 기틀 = 전사 패스스루(claude = 분석만·전사 재출력 금지)**. 파이프 = 유튜브 URL→`nb-make.yml`{yt-dlp 자막 2패스(영상 다운 0·원어>ko>en)→없으면 Whisper(ly 레일·1h)}→`nb_sub.py`(VTT·롤링 중복 제거)→`nbmake.sh` claude 1콜(폴오버 SSOT·`prompts/nb-make.md` NB_GV 도장)→`viewer/nb_out/<id>/note.json`(뷰어 = `viewer/nb.html` song 골격·발사 = `functions/api/nb.js` rateGate). 제미나이·R2 = 0·캡 = 자막 4h/입력 12만자·`YT_COOKIES` 공유. 채널/재생목록 = 목록 모드(claude 0콜)·노트→뉴스 요약 승격 = `api/submit` 재사용 | 00 §1-1 |
| `.claude/skills/` | 진입 명령어(news·1·2·3·4·th·x·ly·comp·k·q·insta) | 각 스킬 |
| `.claude/hooks/` | `multi_intent.py`(제2조)·`bg_gate.py`(제29조)·`design_digest.py`(SessionStart 주입)·`design_gate.py`(제16조) | 각 파일 |
| `PROJECT_MEMORY.md` | 공용 메모리(브랜드 룰·결정 로그) — 전 앱 참조 | — |
| `assets/brand/` | CI·BI 정본: `logo.png`(로고 자리 = 항상 이거)·`main_background.jpg`(배너)·`main_profile.jpg`(아바타·파비콘). 빌드가 `viewer/assets/brand/` 복사 | — |
| `_versions/` · `_산출/` | 시스템 백업(수정 모드) · 뉴스 산출물 보관 | — |
| `docs/` | `news-pipeline.md`(파이프 입구)·`디자인기틀_SSOT.md`(규칙2)·`CII_컴포넌트계승인덱스.md`·`curation-algorithm.md`·`작업이력.md`·`요구사항_큐.md`·`라우터_해설원장.md`(구본 동결)·`플레이그라운드_포터블.md`·`기틀_복제팩_마스터라우터.md`(타 레포 이식 = 이 1개)·`픽토그램_세로가로정렬_기틀_이식팩.md`·`디자인토큰_제1핵심명령_복제전문.md`·시행착오로그·폰재구축플레이북·`termux-share.sh`·확립본들(`reports/`) | 각 문서 |
| `디자인기틀/구성도/` | 사인오프 9 HTML+`base.css`(거울)+공통규약+버튼 가이드북 | 제42조 |
| `shared/` | `attach.py`(제70조)·`audio_norm.py`(음량 SSOT = −14LUFS·TP−1.5·재구현 금지)·`check_refs.py`(커밋 전 게이트)·`check_env.sh`·`profile_session.py`·`claude_transient.sh`/`claude_py.py`(제61조)·`inject_guidelines.sh`(제60조)·`build_library.py`·`build_design_mirror.py`(거울+lock)·`report_render.py`(20열 정본)·`model_env.sh`(PIPE_MODEL)·`playground_template.html`(제28조 동결 골격)·`smoke_geni.js`(**genidlg·도구 셸·thumb 잡 카드 구조 변경 = 커밋 전 실행** — check_refs 정적과 한 쌍·폼 개편 = SEL 표만 갱신) | 각 파일 헤더 |
| `scraper/` | `knews_scraper.py`(burst)·`to_candidates.py`·`auto_pick_breaking.py`·`daily_health.py`·`compare_collected.py` | 제55·67조 |
| `viewer/` | 뷰어(index+도구 HTML+`sw.js`+`cscroll.js`+`_headers`)·빌드 = `build-viewer.mjs`(index.html 미변환) | 제42·66조 |

## 부칙
① (시행) 이 법령체판이 구본(산문판)을 대체한다. 구본 전문 = `docs/라우터_해설원장.md`(동결 스냅샷) + `_versions/260713_2122_법령체전환/CLAUDE.md`.
② (개정) 조 신설 = 제N조의2 방식 · 조 삭제 = 결번 명시(재부여 금지). 개정 경위·날짜 = `docs/작업이력.md` 원장(본문 날짜 스탬프 금지 = 제11조③).
③ (경과) 구 참조 호환: 구 §앵커·이모지 앵커 = 제11조④ · 구 `실행 계약 1~7` = 제1~7조(동일 순서) · 구 `절대명령 N`·`N절`·하위기호 = 해설원장 매핑표. 외부 문서·훅·스킬의 구 표기 = 유효한 참조로 해석(점진 갱신·일괄 rename 불요).
④ (강령 분리) 운영자 지시(핵심만·행동 우선)로 상시 로드층을 행동 강령(`CLAUDE.md`)으로 분리하고 이 전문은 상세 규정 정본으로 존치한다. 조 번호·앵커 효력 불변. 경위 = `docs/작업이력.md`.
