name: ly-make

# 뷰어 ly 폼(Pages Function /api/ly)이 발사 → SRT/STT 텍스트로 claude -p(지침 전문 프롬프트 인라인 · 260721 — 구 런타임 Read 왕복 폐지)
#   → 릴스 자막 md → viewer/ly_out/<id>/subs.md 커밋(폼이 폴링).
# 인증 = CLAUDE_CODE_OAUTH_TOKEN(구독 OAuth·무료). 입력 = SRT/STT 텍스트 또는 영상 URL/업로드 파일(yt-dlp/ffmpeg → Whisper large-v3 STT).
on:
  workflow_dispatch:
    inputs:
      id:
        description: '출력 id(타임스탬프)'
        type: string
      subs:
        description: 'SRT 또는 STT/자막 텍스트(URL 없을 때) · reburn=1이면 = 편집 자막 JSON(subs.json 대체) 또는 RESTORE(원본 스냅샷 복원) · 빈값 = 현행 subs.json 재사용(편집 반영 뒤엔 편집본 — 운영자 260710)'
        type: string
      url:
        description: '영상 URL(yt-dlp+Whisper STT) — 있으면 subs 무시'
        type: string
      file:
        description: '업로드 영상/오디오 파일 경로(uploads/<id>/src.*) — ffmpeg 오디오추출+Whisper'
        type: string
      r2_src:
        description: 'R2 직업로드 키(up_src/<id>.<ext> · 대용량 ≤2GB — api/upload 발급 · 빈값=미사용 · 보관 24h 뒤 자동 청소[260810 — 구 「잡 끝 삭제」 폐지]) — 20MB 초과 영상 합성 경로(edit-make 동문 · 260722)'
        type: string
        default: ''
      early_segs:
        description: '조기 segments.json 푸시(전사 즉시 편집기 선행 · 1=ON) — 기본 OFF(fail-closed) · 라이브 ON = functions/api/ly.js가 문자열 ''1''을 명시 전달(숫자/불리언 금지 = GH 강제변환으로 조용히 OFF · 롤백 = ly.js 한 줄)'
        type: string
        default: '0'
      opts:
        description: '뷰어 버튼 설정 JSON(자막 옵션+번인 스타일 · 빈값=종전 동작) — {"lang","tone","filler","burn","style","pos"(세로 위치 % 0=하단 100=상단 · 구 문자열 하위호환),"bg"(배경 불투명도 % 0=없음),"size","karaoke","keyword","cut"(무음 갭 자동 컷 · 발화 기준),"cutlv"(컷 세기 soft/std/hard · 미지=std),"bgm"(배경음 제거 = Demucs 보컬 분리 · 둘 다 켜면 배경음부터) — cut·bgm은 burn=true일 때만 실행 = 번인 하위 축}'
        type: string
        default: ''
      up_branch:
        description: '업로드 일회용 브랜치(up-<id> · 빈값=종전 main 커밋 경로) — ly.js가 업로드를 여기 커밋 → 러너가 회수 → 잡 끝에 삭제 = main 히스토리 비대 0'
        type: string
        default: ''
      reburn:
        description: '재합성 모드(1=ON) — 기존 ly_out/<id>/subs.json 의역 + video.json src(R2 원본 보관본)로 번인만 재실행(STT·claude 스킵 = LLM 0콜·1분급)'
        type: string
        default: '0'
      stt_prec:
        description: 'STT 연산 정밀도(A/B용 · 빈값=기본 int8_float32) — int8 / int8_float32(기본) / float32. ⚠ int8↔int8_float32는 260728 러너 A/B에서 **시간·어절확률·전사텍스트 전부 동일**로 나왔다(docs/reports/260728_1447_STT정밀도AB.md) = 이 노브로 정확도가 바뀐다고 기대하지 말 것. float32는 미검증. 같은 영상을 값만 바꿔 두 번 발사하면 대조가 되고, 산출 segments.json `prec` 키로 어느 설정인지 식별한다'
        type: string
        default: ''
      account:
        description: '이 실행에 쓸 계정(구독 OAuth)'
        type: choice
        default: MUTENO
        options:
          - NOMUTEFB
          - MUTENO
          - EMS1130G

concurrency:
  group: ly-make-${{ inputs.id }}
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  ly:
    runs-on: ubuntu-latest
    timeout-minutes: 85   # 콜드캐시(STT 환경 20)+STT(50)+claude+Demucs(12)+번인(35) 최악 겹침 재산정(260726 large-v3 승격 — turbo 대비 추론 ~4배 = MAX_DUR 10분 영상 최악 ~20분·구 60은 스택보다 짧아짐). 개별 폭주는 스텝 백스톱이 먼저 끊고 잡 캡 = 최후 보루
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          filter: blob:none   # 부분 클론(260721 속도) — 커밋 그래프는 전부(rebase·push 종전 그대로)·역사 블롭만 지연 회수 = 무거운 레포(이미지·zip 히스토리) 체크아웃 65s→수십초. 미회수 블롭은 필요 시 자동 fetch(promisor)
          # ⚡ 260808 sparse-checkout(평의회8 실측) — filter:blob:none 뒤 남은 시간의 정체는 **워킹트리 블롭 회수**다
          #   (로그 분해 = fetch 2.96s + 무출력 16.27s + 파일쓰기 2.91s · 1,424.7MiB ÷ 16.27s ≈ 87MB/s 로 정확히 맞는다).
          #   이 잡이 실제로 읽고 쓰는 경로는 **124.1MiB(8.7%)** 뿐이라 나머지는 순수 낭비였다.
          #   ⚠ 치명 실패 모드 = 「out-of-cone 파일이 인덱스에서 빠진 채 커밋 → push가 1만 파일을 삭제」.
          #     cone 모드는 그것들을 SKIP_WORKTREE로 인덱스에 남기므로 안 일어나고, **이 워크플로의 git add 는
          #     전부 명시 경로**(413·509·668·669행 · 무경로 `git add -A`/`git add .` 0건 = 전 워크플로 실측)라 전제도 성립한다.
          #     그 전제가 깨지면 사고가 나므로 신규 add 는 반드시 경로 지정으로 쓴다.
          #   ⚠ uploads 는 cone 에 **남긴다** — `Commit output` 의 `git rm -r uploads/<id>` 가 여기 의존한다
          #     (빼면 `|| true` 로 조용히 실패해 업로드가 영구 잔류 = 레포 비대).
          sparse-checkout: |
            .github
            shared
            apps/ly
            prompts
            assets/fonts
            viewer/ly_out
            metrics
            uploads
            .claude

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: '24'

      - name: Install claude-code
        run: npm install -g @anthropic-ai/claude-code

      # STT 경로(영상 URL/파일)에서만 — 파이썬 고정(site-packages 캐시 경로 안정화) + 설치분 캐시
      - name: Setup Python (STT)
        if: ${{ inputs.url != '' || inputs.file != '' || inputs.r2_src != '' }}
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      # site-packages 캐시 — setup.sh 의 faster-whisper(ctranslate2·onnxruntime 등) 재설치 회피.
      # 실측(파일 런): STT 환경 318s 중 faster-whisper 설치가 큰 몫(Whisper 모델 1.6GB 복원은 별도·바닥).
      # 워밍런 = 복원 → setup.sh `import faster_whisper` 통과 → pip 스킵. 무해(미적중이면 기존대로 재설치=회귀 0).
      - name: Restore python deps (site-packages 캐시)
        if: ${{ inputs.url != '' || inputs.file != '' || inputs.r2_src != '' }}
        uses: actions/cache@v4
        with:
          path: ${{ env.pythonLocation }}/lib/python3.12/site-packages
          key: nomute-ly-pydeps-${{ runner.os }}-py312-${{ hashFiles('apps/ly/setup.sh') }}-v1   # setup.sh 해시 키(track 정본 계승 · 평의회9) — 핀/패키지 변경 시 자동 재빌드(구 정적 키 = 변경분 미설치 드리프트) · -v1 = 수동 버스트 레버(track 동형 · 재평의회9)   # ↓ 근접 복원 폴백(260728 Q1001) — setup.sh **주석 한 줄만 고쳐도** hashFiles가 바뀌어 전체 재설치(콜드 318s)되던 구조 결함 봉합.
          #    정확 키 miss여도 접두사가 같은 직전 캐시를 복원 → setup.sh의 `import faster_whisper` 체크가 통과해 pip 스킵.
          #    ⚠ 의존성이 진짜 바뀐 경우도 옛 캐시를 복원하지만, setup.sh가 import 확인 후 없는 것만 설치하므로 신규 패키지는 정상 설치된다(= 원하는 동작).
          restore-keys: |
            nomute-ly-pydeps-${{ runner.os }}-py312-

      # 재합성(reburn) 전용 경량 준비 — ffmpeg + 한글 폰트(whisper·yt-dlp 불요). ⚠️ fonts-noto-cjk 없으면 libass가
      #   에러 없이 두부(□)로 조용히 렌더 → 정상본을 깨진 영상으로 덮어씀(평의회 불가 적발) = 폰트 확인까지 하드게이트.
      - name: ffmpeg+한글폰트 (재합성)
        if: ${{ inputs.reburn == '1' }}
        run: |
          timeout 300 sudo apt-get update -qq || true
          timeout 300 sudo apt-get install -y -qq ffmpeg fonts-noto-cjk fonts-nanum fonts-nanum-extra || { echo "::error::ffmpeg/폰트 설치 실패"; exit 1; }   # 나눔 2종 = 편집기 재입히기(reburn)의 폰트 축(nanum·pen) — 이 스텝이 reburn 유일 경로라 STT 스텝(186행)만으론 미도달(평의회 260711 적발 · 미설치 = 고딕 강등)
          fc-list | grep -qi "noto sans cjk" || { echo "::error::Noto Sans CJK 미탐지 — 두부 번인 차단"; exit 1; }

      # 재합성 소스 회수 — 기존 video.json의 src(R2 원본 보관본) → /tmp (subs.json 의역은 checkout에 이미 포함)
      - name: 재합성 소스 회수 (reburn)
        if: ${{ inputs.reburn == '1' }}
        env:
          IN_ID: ${{ inputs.id }}
        run: |
          set -e
          SRC="$(python3 -c "import json;print(json.load(open('viewer/ly_out/${IN_ID}/video.json')).get('src') or '')" 2>/dev/null || true)"
          case "$SRC" in https://*) ;; *) echo "재합성 원본 보관본이 없어 — 새로 생성해줘." > /tmp/ly_err.txt; echo "::error::src 없음/비https: $SRC"; exit 1;; esac
          EXT="${SRC##*.}"; case "$EXT" in mp4|webm|mov|mkv|m4v) ;; *) EXT=mp4;; esac
          curl -fsSL --max-time 300 "$SRC" -o "/tmp/upsrc.$EXT" || { echo "원본 다운로드 실패 — 새로 생성해줘." > /tmp/ly_err.txt; echo "::error::src 다운 실패"; exit 1; }
          echo "LY_VIDEO=/tmp/upsrc.$EXT" >> "$GITHUB_ENV"
          echo "REBURN=1" >> "$GITHUB_ENV"

      # 편집분 번인(운영자 260710) — 뷰어 상세 편집기 수정(단어 빼기·조각 삭제·타이밍·텍스트)을 반영해 다시 입히기.
      #   subs(디스패치 입력·reburn 전용 재사용) = 편집 조각 JSON → subs.json 대체(커밋 = 이후 reburn 정본도 편집본) / 'RESTORE' 센티널 = 첫 편집 때
      #   보존한 subs.orig.json 스냅샷으로 원복(초기화→다시 입히기 · 기능평의회2). fail-soft: 검증 실패 = 현행 subs.json 그대로(::warning:: 카나리아).
      #   개요(subs.md)는 불변(원본 의역 표시 = 의도적 분기). 반영/실패/복원은 LY_EDITED env → ly_burn note로 표면화(침묵 금지 · 기능평의회9).
      - name: 편집 자막 반영 (reburn)
        if: ${{ inputs.reburn == '1' && inputs.subs != '' }}
        env:
          IN_ID: ${{ inputs.id }}
          SUBS: ${{ inputs.subs }}
        run: |
          [ -n "$IN_ID" ] || exit 0
          [[ "$IN_ID" =~ ^[0-9]{12}-[0-9a-f]{6}$ ]] || { echo "id 형식 밖 — 스킵"; exit 0; }   # 두 분기(RESTORE·편집) 공통 상단 가드 = 검증 대칭 완결(재검4·5 — 파이썬 assert는 이중가드로 잔존)
          if [ "$SUBS" = "RESTORE" ]; then
            O="viewer/ly_out/${IN_ID}/subs.orig.json"
            if [ -s "$O" ]; then
              cp "$O" "viewer/ly_out/${IN_ID}/subs.json.tmp" && mv "viewer/ly_out/${IN_ID}/subs.json.tmp" "viewer/ly_out/${IN_ID}/subs.json"   # 원자 교체 = 편집 경로 os.replace와 대칭(재검3)
              echo "LY_EDITED=restore" >> "$GITHUB_ENV"; echo "원본 의역 복원"
            else echo "원본 스냅샷 없음 — 현행 subs.json 그대로(편집 반영 이력 없음 = 이미 원본)"; fi
            exit 0
          fi
          if python3 - "$IN_ID" <<'PY'
          import json, os, re, shutil, sys
          from math import isfinite
          vid = sys.argv[1]
          assert re.fullmatch(r"[0-9]{12}-[0-9a-f]{6}", vid), "id 형식"   # 소비자(ly_burn)와 검증 대칭 — 수동 dispatch 방어심층(기능평의회4·5)
          j = json.loads(os.environ.get("SUBS") or "")
          segs = []
          for s in (j.get("segs") or [])[:700]:   # 700 = ly.js 상한 미러(초과는 API가 이미 소리나게 거절 · 기능평의회8)
              try:
                  ss, ee = float(s["s"]), float(s["e"])
              except Exception:
                  continue
              ko = str(s.get("ko") or "").strip()[:200]
              if not (isfinite(ss) and isfinite(ee)) or ss < 0 or ee <= ss or not ko:   # isfinite = NaN/Inf가 subs.json 오염 못 하게(기능평의회5)
                  continue
              segs.append({"s": round(ss, 2), "e": round(ee, 2), "ko": ko})
          assert segs, "유효 조각 0"
          dels = []   # 대본 삭제 컷 스팬(운영자 260711 텍스트 컷 · ly.js del 검증 통과분) — segs와 동일 검증 대칭·불량 = 조용 드롭(컷은 부가 축)
          for dd in (j.get("del") or [])[:400]:
              try:
                  a, b = float(dd[0]), float(dd[1])
              except Exception:
                  continue
              if isfinite(a) and isfinite(b) and a >= 0 and b > a:
                  dels.append([round(a, 2), round(b, 2)])
          d = os.path.join("viewer", "ly_out", vid)
          os.makedirs(d, exist_ok=True)
          p = os.path.join(d, "subs.json")
          orig = os.path.join(d, "subs.orig.json")
          if os.path.isfile(p) and not os.path.isfile(orig):
              shutil.copyfile(p, orig + ".tmp")   # 첫 편집 반영 때 원본 의역 1회 보존 = 초기화 복원 소스(기능평의회2·3)
              os.replace(orig + ".tmp", orig)   # 스냅샷도 원자 확정 — 부분 복사 잔존이 isfile 가드에 고착되는 창 소거(재검2·3)
          tmp = p + ".tmp"
          doc = {"v": 1, "segs": segs}
          if dels:
              doc["del"] = dels   # 삭제 컷 스팬 동봉 → ly_burn load_del_spans가 그 구간을 영상에서 컷(260711)
          with open(tmp, "w", encoding="utf-8") as f:
              json.dump(doc, f, ensure_ascii=False, separators=(",", ":"))
          os.replace(tmp, p)   # 원자 교체 — truncate-쓰기 금지: 중간 실패(디스크·SIGKILL)에도 원본 무손상(기능평의회3 P2)
          print("편집 자막 반영: {}조각{}".format(len(segs), " · 삭제 컷 {}구간".format(len(dels)) if dels else ""))
          PY
          then echo "LY_EDITED=1" >> "$GITHUB_ENV"
          else
            echo "::warning::편집 자막 반영 실패 — 현행 subs.json으로 진행"
            echo "LY_EDITED=fail" >> "$GITHUB_ENV"
          fi

      # 영상 URL/파일일 때만 — ffmpeg+faster-whisper+yt-dlp(apps/ly/setup.sh 단일출처) + 모델 캐시
      - name: STT 환경 (영상 URL/파일)
        if: ${{ inputs.url != '' || inputs.file != '' || inputs.r2_src != '' }}
        timeout-minutes: 20   # 백스톱 — 첫 런 Whisper large-v3 3.1GB 다운 감안 여유(260726 승격·이후 캐시히트 즉시). 무한 행만 차단.
        uses: ./.github/actions/runner-setup
        with:
          setup: apps/ly/setup.sh
          # ⚡ 260808 — STT 기본 = Scribe v2(ly_stt.py) → large-v3는 **폴백 전용**이라 3.1GB 캐시·prefetch를 평소엔 안 붙인다.
          #   실측 = 이 스텝 48s(총 112s의 43%)가 Scribe 경로에선 한 번도 안 쓰이는 짐이었다(run 31231381993 분해).
          #   'true'로 되돌아가는 조건 = 킬스위치(vars.LY_STT_ENGINE=whisper) ∨ 키 부재 = **폴백이 유일 경로인 상황**.
          #   ⚠ ffmpeg·yt-dlp·faster_whisper(pip)는 그대로 설치된다 — 빠지는 건 모델 파일 3.1GB뿐(폴백 시 그 자리에서 회수).
          whisper: ${{ (vars.LY_STT_ENGINE == 'whisper' || secrets.ELEVENLABS_API_KEY == '') && 'true' || 'false' }}
          # ⚡ 260808 폰트 분리(운영자 지시) — 폰트 3종 88.3MiB(apt 페이로드의 47.3%)는 **번인 전용**이다
          #   (소비자 = ly_burn.py 단독 · STT·의역·커밋 경로는 폰트를 한 번도 안 읽는다 = 평의회8 전수 확인).
          #   받아쓰기만 하는 런에 매번 딸려오던 짐을 뺀다 → 복원 −1.8s + unpack −1.6s.
          #   ⚠ 평의회6·8이 공통 경고한 대가 = 「fonts-noto-cjk 없으면 libass가 **에러 없이 두부(□)로 조용히 렌더**」.
          #     그래서 폰트는 **번인 직전에 설치 + fc-list 하드게이트**로 옮겼다(아래 번인 스텝 · reburn 정본 100% 모방).
          #   ⚠ 부수이득 = apt 문자열이 nb·vidl·news-ask 와 같아져 **캐시 키를 공유**한다(콜드 다운 합산 1회).
          apt: ffmpeg

      - name: STT (영상 URL → Whisper 트랜스크립트)
        if: ${{ inputs.url != '' }}
        timeout-minutes: 50   # 다운로드+Whisper 백스톱(평의회9 260709 · 260726 large-v3 40 · 260728 40→50 · ⚠ 당초 근거였던 '정밀도 승격 감속'은 러너 A/B로 반증(int8과 동일~미세 빠름 · docs/reports/260728_1447_STT정밀도AB.md) → 현 근거 = 초장영상·콜드캐시 보수 여유) — 스톨 링크·초장영상 무한 행이 잡 캡까지 먹던 구멍 차단. ⚠ float32 A/B는 10분 영상서 이 백스톱을 위협(짧은 클립으로 대조할 것)
        env:
          # STT 엔진 = Scribe v2 우선 → 실패·키부재 시 large-v3 자동 폴백(ly_stt.py · 운영자 260808 승인)
          #   실측 = run 31230435185(한국어 19.6s): 28.1s→1.0s · 오인식 의심 11.5%→3.4% · 「검찰서가」→「경찰서 가서」 교정.
          #   킬스위치 = 레포 변수 LY_STT_ENGINE=whisper(즉시 종전 경로) · 미설정 = auto(키 있으면 Scribe).
          ELEVENLABS_API_KEY: ${{ secrets.ELEVENLABS_API_KEY }}
          LY_STT_ENGINE: ${{ vars.LY_STT_ENGINE }}
          LY_STT_MAX_SEC: ${{ vars.LY_STT_MAX_SEC }}   # 전사 길이 하드 캡(빈값 = 3600s) — Scribe 속도가 종전 비용 브레이크를 없앤 자리 방어
          URL: ${{ inputs.url }}
          YT_COOKIES: ${{ secrets.YT_T2_COOKIES }}   # 지금 쓰는 살아있는 칸 = 1순위(260811 · 죽은 칸을 먼저 두드리면 매 런 4번 헛시도 + 유튜브에 같은 주소로 실패를 반복해 보인다)
          YT_COOKIES_2: ${{ secrets.YT_T_COOKIES }}   # 예비 1 — 위가 죽으면 자동 회수
          YT_COOKIES_3: ${{ secrets.YT_COOKIES }}   # 예비 2(구 시크릿)
          IN_ID: ${{ inputs.id }}
          OPTS: ${{ inputs.opts }}
          LY_STT_PRECISION: ${{ inputs.stt_prec }}   # 빈값 = ly_stt.py 기본(int8_float32) · A/B 시에만 지정
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}   # STT 캐시(Q999) — 미설정이면 CKEY가 빈값 = 캐시 경로 자연 우회(fail-soft)
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
        run: |
          set -e
          # YouTube/IG 봇차단 우회 — YT_COOKIES 시크릿(쿠키 Netscape txt 본문) 있으면 사용
          COOKIES=""
          if [ -n "$YT_COOKIES" ]; then printf '%s\n' "$YT_COOKIES" > /tmp/ck.txt; COOKIES="--cookies /tmp/ck.txt"; fi
          # 번인 ON = 영상까지 다운(자막 합성용 · 실패 = 오디오-온리 폴백 → 자막 텍스트만 = 회귀 0)
          BURN="$(printf '%s' "${OPTS:-}" | jq -r 'try .burn catch empty' 2>/dev/null || true)"
          if [ "$BURN" = "true" ]; then
            # 시도 사다리 = .github/scripts/ytdlp_try.sh(①쿠키+기본 ②쿠키+대체 ③무쿠키+기본 ④무쿠키+대체 · 260811 · --socket-timeout 30은 래퍼 안에 있다 = 스톨 링크 무한 행 차단 유지)
            YTDLP_ERR=/tmp/yt_err.txt YTDLP_LABEL="번인 영상" bash .github/scripts/ytdlp_try.sh -f "bv*[height<=1920]+ba[ext=m4a]/bv*[height<=1920]+ba/b" --merge-output-format mp4 --no-playlist -o "/tmp/src.%(ext)s" "$URL" || true
            SRC="$(ls /tmp/src.* 2>/dev/null | head -1 || true)"
            if [ -n "$SRC" ]; then
              # 길이 선게이트(평의회9 260709) — 10분(MAX_DUR) 초과는 어차피 번인 거절 → 제일 비싼 스텝(STT·claude) *앞*에서 번인만 스킵 확정(자막 텍스트는 계속 · 사유 = video.json 선기록)
              DUR="$(ffprobe -v error -show_entries format=duration -of default=nw=1:nk=1 "$SRC" 2>/dev/null | cut -d. -f1 || true)"
              if [ -n "$IN_ID" ] && [ -n "$DUR" ] && [ "$DUR" -gt 600 ] 2>/dev/null; then   # IN_ID 가드 = 수동 dispatch id 공백 시 부유 video.json 차단(재평의회10 · 형제 스텝 동형)
                mkdir -p "viewer/ly_out/${IN_ID}"
                printf '{"skip":"영상 %s분 — 10분 이하만 합성(릴스/쇼츠용) · 자막 텍스트만"}' "$((DUR/60))" > "viewer/ly_out/${IN_ID}/video.json"
                echo "LY_NOBURN=1" >> "$GITHUB_ENV"
                ffmpeg -y -i "$SRC" -vn -ar 16000 -ac 1 -b:a 48k /tmp/audio.mp3 || rm -f /tmp/audio.mp3   # 오디오는 받은 영상서 추출(재다운 회피)
                rm -f "$SRC"; SRC=""
              else
                ffmpeg -y -i "$SRC" -vn -ar 16000 -ac 1 -b:a 48k /tmp/audio.mp3 || rm -f /tmp/audio.mp3
                if [ -s /tmp/audio.mp3 ]; then echo "LY_VIDEO=$SRC" >> "$GITHUB_ENV"; echo "번인용 영상 확보: $SRC"; fi
              fi
            fi
          fi
          [ -s /tmp/audio.mp3 ] || YTDLP_ERR=/tmp/yt_err.txt YTDLP_LABEL="오디오" bash .github/scripts/ytdlp_try.sh -x --audio-format mp3 --postprocessor-args "ffmpeg:-ar 16000 -ac 1 -b:a 48k" --no-playlist -o "/tmp/audio.%(ext)s" "$URL" \
            || { echo "유튜브 다운로드 실패 — 쿠키를 빼고도 다시 시도했는데 같은 벽이야. 파일 업로드나 SRT 텍스트로 해줘." > /tmp/ly_err.txt; echo "::error::yt-dlp 다운로드 실패(4단 후): $URL"; exit 1; }
          SEGJSON=""; [ -n "$IN_ID" ] && SEGJSON="viewer/ly_out/${IN_ID}/segments.json"   # word 타임스탬프 사이드카(상세 편집기) — 트랜스크립트 포맷 불변·타임코드/경계는 word 정렬로 미세 변동 가능(ly_stt.py 헤더 참조·위험 수용 기록)
          # ── STT 캐시(260728 Q999 · edit-make 정본 계승) — 키 = 추출 오디오 sha256(+모델+연산 정밀도).
          #    같은 소스를 다시 굽는 재작업이 large-v3를 통째로 재전사하던 낭비 제거(41초 클립 실측 STT 62초 · 10분급 ~7분).
          #    ⚠ 품질 무손실: 캐시는 **같은 정밀도로 뽑은 전사 결과 그대로**를 되쓴다(정밀도가 바뀌면 키가 갈려 자연 미스 = 재전사).
          #    전면 fail-soft: 캐시가 어떤 이유로든 안 되면 그냥 전사한다(회귀 0).
          CKEY=""; ENDP=""
          if [ -n "${R2_BUCKET:-}" ] && [ -n "${R2_ACCOUNT_ID:-}" ] && [ -n "$SEGJSON" ]; then
            export AWS_DEFAULT_REGION=auto AWS_REQUEST_CHECKSUM_CALCULATION=when_required AWS_RESPONSE_CHECKSUM_VALIDATION=when_required
            ENDP="https://${R2_ACCOUNT_ID}.r2.cloudflarestorage.com"
            CKEY="$(python3 .github/scripts/stt_cache.py key /tmp/audio.mp3 2>/dev/null || true)"
          fi
          if [ -n "$CKEY" ] && aws s3 cp "s3://$R2_BUCKET/$CKEY" "$SEGJSON" --endpoint-url "$ENDP" --only-show-errors 2>/dev/null; then
            if python3 .github/scripts/stt_cache.py rebuild "$SEGJSON" > /tmp/transcript.txt; then
              echo "LY_TRANSCRIPT=/tmp/transcript.txt" >> "$GITHUB_ENV"
              echo "STT 캐시 히트($CKEY) — 전사 건너뜀 · $(wc -l < /tmp/transcript.txt)줄"; exit 0
            fi
            echo "::warning::캐시 재구성 실패 — 정상 전사로 진행"; rm -f "$SEGJSON" /tmp/transcript.txt
          fi
          # ⚠ 비용 브레이크(평의회4 [높]) — 종전엔 large-v3 ~1.4×RT 라 **50분 스텝 백스톱이 곧 지출 상한**이었다
          #   (4시간 영상 = 물리적으로 실패 = 지출 $0). Scribe는 0.05×RT라 그 브레이크가 사라졌고 대체 캡이 없었다
          #   → 4시간 VOD 1건 $0.88 · 10시간 $2.20이 **성공한다**. 형제(nb-make)와 같은 문법으로 하드 캡을 세운다.
          LY_STT_MAX_SEC="${LY_STT_MAX_SEC:-3600}"
          ADUR="$(ffprobe -v error -show_entries format=duration -of default=noprint_wrappers=1:nokey=1 /tmp/audio.mp3 2>/dev/null | cut -d. -f1)"
          case "$ADUR" in ''|*[!0-9]*) ADUR=0;; esac
          if [ "$ADUR" -gt "$LY_STT_MAX_SEC" ] 2>/dev/null; then
            echo "소리 길이가 $((ADUR/60))분이라 전사 상한($((LY_STT_MAX_SEC/60))분)을 넘어 — 구간을 잘라서 다시 해줘." > /tmp/ly_err.txt
            echo "::error::STT 길이 상한 초과(${ADUR}s > ${LY_STT_MAX_SEC}s)"; exit 1
          fi
          python3 .github/scripts/ly_stt.py /tmp/audio.mp3 "$SEGJSON" > /tmp/transcript.txt \
            || { echo "전사에 실패했어 — 소리가 또렷한 소스로 다시 해줘(엔진 로그는 Actions 참조)." > /tmp/ly_err.txt; echo "::error::STT 실패"; exit 1; }
          echo "LY_TRANSCRIPT=/tmp/transcript.txt" >> "$GITHUB_ENV"
          echo "STT 완료 — $(wc -l < /tmp/transcript.txt)줄"
          if [ -n "$CKEY" ] && [ -s "$SEGJSON" ]; then
            aws s3 cp "$SEGJSON" "s3://$R2_BUCKET/$CKEY" --endpoint-url "$ENDP" --only-show-errors 2>/dev/null \
              && echo "STT 캐시 적재: $CKEY" || echo "::warning::STT 캐시 적재 실패(비치명)"
          fi

      # R2 직업로드 소스 회수(edit-make 동문 · 260722) — 20MB 초과 영상 = up_src/* 키로 회수해 아래 STT 파일 스텝에 인계(FILE 폴백)
      - name: R2 소스 회수 (r2_src → /tmp)
        if: ${{ inputs.r2_src != '' && inputs.reburn != '1' }}
        timeout-minutes: 8   # 2GB도 러너→R2 수십 MB/s면 수분 — 잡 캡 예산 압축(edit-make 평의회6 계승)
        env:
          R2_SRC: ${{ inputs.r2_src }}
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
        run: |
          set -e
          printf '%s' "$R2_SRC" | grep -Eqx 'up_src/[0-9]{12}-[a-f0-9]{6}\.(mp4|mov|m4v|webm|mkv|avi)' \
            || { echo "::error::잘못된 r2_src: $R2_SRC"; exit 1; }   # api 정규식과 동형 자체 재검증(상류 신뢰 대신 최후 방어선 · edit-make 평의회1 계승)
          [ -n "$R2_BUCKET" ] || { echo "저장소(R2)가 설정 안 돼 있어 — 관리자 설정 후 다시." > /tmp/ly_err.txt; echo "::error::R2 시크릿 미설정"; exit 1; }
          EXT="${R2_SRC##*.}"
          export AWS_DEFAULT_REGION=auto AWS_REQUEST_CHECKSUM_CALCULATION=when_required AWS_RESPONSE_CHECKSUM_VALIDATION=when_required
          aws s3 cp "s3://$R2_BUCKET/$R2_SRC" "/tmp/upsrc.$EXT" --endpoint-url "https://${R2_ACCOUNT_ID}.r2.cloudflarestorage.com" --only-show-errors \
            || { echo "업로드 파일 회수 실패 — 다시 올려줘." > /tmp/ly_err.txt; echo "::error::R2 소스 회수 실패: $R2_SRC"; exit 1; }
          echo "LY_UPSRC=/tmp/upsrc.$EXT" >> "$GITHUB_ENV"

      - name: STT (업로드 파일 → ffmpeg 오디오추출 → Whisper)
        if: ${{ inputs.file != '' || inputs.r2_src != '' }}
        timeout-minutes: 50   # Whisper 백스톱(평의회9 260709 · 260726 large-v3 25→40 · 260728 40→50(정밀도 근거는 A/B 반증 · 보수 여유로 유지)) — URL 스텝과 동일 원칙
        env:
          # STT 엔진 = Scribe v2 우선 → 실패·키부재 시 large-v3 자동 폴백(ly_stt.py · 운영자 260808 승인)
          #   실측 = run 31230435185(한국어 19.6s): 28.1s→1.0s · 오인식 의심 11.5%→3.4% · 「검찰서가」→「경찰서 가서」 교정.
          #   킬스위치 = 레포 변수 LY_STT_ENGINE=whisper(즉시 종전 경로) · 미설정 = auto(키 있으면 Scribe).
          ELEVENLABS_API_KEY: ${{ secrets.ELEVENLABS_API_KEY }}
          LY_STT_ENGINE: ${{ vars.LY_STT_ENGINE }}
          LY_STT_MAX_SEC: ${{ vars.LY_STT_MAX_SEC }}   # 전사 길이 하드 캡(빈값 = 3600s) — Scribe 속도가 종전 비용 브레이크를 없앤 자리 방어
          FILE: ${{ inputs.file }}
          IN_ID: ${{ inputs.id }}
          UP_BRANCH: ${{ inputs.up_branch }}
          LY_STT_PRECISION: ${{ inputs.stt_prec }}   # 빈값 = ly_stt.py 기본(int8_float32) · A/B 시에만 지정
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}   # STT 캐시(Q999) — 미설정이면 CKEY가 빈값 = 캐시 경로 자연 우회(fail-soft)
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
        run: |
          set -e
          # 일회용 업로드 브랜치(up-<id>) 경로 = 파일을 브랜치에서 /tmp로 회수(main 워킹트리엔 없음 · 잡 끝에 브랜치 삭제)
          if [ -n "$UP_BRANCH" ]; then
            case "$UP_BRANCH" in up-*) ;; *) echo "::error::up_branch는 up-* 만 허용: $UP_BRANCH"; exit 1;; esac
            git fetch origin "$UP_BRANCH" --depth=1 || { echo "업로드 파일이 유실됐어 — 다시 올려줘." > /tmp/ly_err.txt; echo "::error::업로드 브랜치 fetch 실패: $UP_BRANCH"; exit 1; }
            EXT="${FILE##*.}"
            git show "FETCH_HEAD:$FILE" > "/tmp/upsrc.$EXT" || { echo "업로드 파일 회수 실패 — 다시 올려줘." > /tmp/ly_err.txt; echo "::error::브랜치서 파일 회수 실패: $FILE"; exit 1; }
            FILE="/tmp/upsrc.$EXT"
          fi
          [ -n "$FILE" ] || FILE="${LY_UPSRC:-}"   # R2 직업로드 경로(r2_src) = 위 R2 소스 회수 스텝이 /tmp에 내려둔 원본 인계(260722 — file 입력 공백일 때만)
          [ -f "$FILE" ] || { echo "::error::업로드 파일 없음: $FILE"; exit 1; }
          VID=""
          case "$FILE" in
            *.mp4|*.mov|*.m4v|*.webm|*.mkv|*.avi|*.3gp) VID="$FILE";;   # +3gp(260721) — 뷰어 첨부 허용 목록(ly.html isVideo)과 정합: 빠지면 원본 업로드해도 '영상 확보 실패 → 자막만'
          esac
          if [ -n "$VID" ]; then
            # 길이 선게이트(평의회9 260709) — URL 경로와 동일: 10분 초과는 번인만 스킵(자막 텍스트 계속·사유 선기록)
            DUR="$(ffprobe -v error -show_entries format=duration -of default=nw=1:nk=1 "$VID" 2>/dev/null | cut -d. -f1 || true)"
            if [ -n "$IN_ID" ] && [ -n "$DUR" ] && [ "$DUR" -gt 600 ] 2>/dev/null; then   # IN_ID 가드 = URL 경로와 동형(재평의회10)
              mkdir -p "viewer/ly_out/${IN_ID}"
              printf '{"skip":"영상 %s분 — 10분 이하만 합성(릴스/쇼츠용) · 자막 텍스트만"}' "$((DUR/60))" > "viewer/ly_out/${IN_ID}/video.json"
              echo "LY_NOBURN=1" >> "$GITHUB_ENV"
            else
              echo "LY_VIDEO=$VID" >> "$GITHUB_ENV"; echo "번인용 영상 확보(업로드): $VID"
            fi
          fi
          ffmpeg -y -i "$FILE" -vn -ar 16000 -ac 1 -b:a 48k /tmp/audio.mp3 \
            || { echo "오디오 추출 실패 — 지원 안 되는 형식일 수 있어. 다른 파일이나 SRT 텍스트로 해줘." > /tmp/ly_err.txt; echo "::error::ffmpeg 오디오 추출 실패: $FILE"; exit 1; }
          SEGJSON=""; [ -n "$IN_ID" ] && SEGJSON="viewer/ly_out/${IN_ID}/segments.json"   # word 타임스탬프 사이드카(상세 편집기) — 트랜스크립트 포맷 불변·타임코드/경계는 word 정렬로 미세 변동 가능(ly_stt.py 헤더 참조·위험 수용 기록)
          # ── STT 캐시(260728 Q999 · edit-make 정본 계승) — 키 = 추출 오디오 sha256(+모델+연산 정밀도).
          #    같은 소스를 다시 굽는 재작업이 large-v3를 통째로 재전사하던 낭비 제거(41초 클립 실측 STT 62초 · 10분급 ~7분).
          #    ⚠ 품질 무손실: 캐시는 **같은 정밀도로 뽑은 전사 결과 그대로**를 되쓴다(정밀도가 바뀌면 키가 갈려 자연 미스 = 재전사).
          #    전면 fail-soft: 캐시가 어떤 이유로든 안 되면 그냥 전사한다(회귀 0).
          CKEY=""; ENDP=""
          if [ -n "${R2_BUCKET:-}" ] && [ -n "${R2_ACCOUNT_ID:-}" ] && [ -n "$SEGJSON" ]; then
            export AWS_DEFAULT_REGION=auto AWS_REQUEST_CHECKSUM_CALCULATION=when_required AWS_RESPONSE_CHECKSUM_VALIDATION=when_required
            ENDP="https://${R2_ACCOUNT_ID}.r2.cloudflarestorage.com"
            CKEY="$(python3 .github/scripts/stt_cache.py key /tmp/audio.mp3 2>/dev/null || true)"
          fi
          if [ -n "$CKEY" ] && aws s3 cp "s3://$R2_BUCKET/$CKEY" "$SEGJSON" --endpoint-url "$ENDP" --only-show-errors 2>/dev/null; then
            if python3 .github/scripts/stt_cache.py rebuild "$SEGJSON" > /tmp/transcript.txt; then
              echo "LY_TRANSCRIPT=/tmp/transcript.txt" >> "$GITHUB_ENV"
              echo "STT 캐시 히트($CKEY) — 전사 건너뜀 · $(wc -l < /tmp/transcript.txt)줄"; exit 0
            fi
            echo "::warning::캐시 재구성 실패 — 정상 전사로 진행"; rm -f "$SEGJSON" /tmp/transcript.txt
          fi
          # ⚠ 비용 브레이크(평의회4 [높]) — 종전엔 large-v3 ~1.4×RT 라 **50분 스텝 백스톱이 곧 지출 상한**이었다
          #   (4시간 영상 = 물리적으로 실패 = 지출 $0). Scribe는 0.05×RT라 그 브레이크가 사라졌고 대체 캡이 없었다
          #   → 4시간 VOD 1건 $0.88 · 10시간 $2.20이 **성공한다**. 형제(nb-make)와 같은 문법으로 하드 캡을 세운다.
          LY_STT_MAX_SEC="${LY_STT_MAX_SEC:-3600}"
          ADUR="$(ffprobe -v error -show_entries format=duration -of default=noprint_wrappers=1:nokey=1 /tmp/audio.mp3 2>/dev/null | cut -d. -f1)"
          case "$ADUR" in ''|*[!0-9]*) ADUR=0;; esac
          if [ "$ADUR" -gt "$LY_STT_MAX_SEC" ] 2>/dev/null; then
            echo "소리 길이가 $((ADUR/60))분이라 전사 상한($((LY_STT_MAX_SEC/60))분)을 넘어 — 구간을 잘라서 다시 해줘." > /tmp/ly_err.txt
            echo "::error::STT 길이 상한 초과(${ADUR}s > ${LY_STT_MAX_SEC}s)"; exit 1
          fi
          python3 .github/scripts/ly_stt.py /tmp/audio.mp3 "$SEGJSON" > /tmp/transcript.txt \
            || { echo "전사에 실패했어 — 소리가 또렷한 소스로 다시 해줘(엔진 로그는 Actions 참조)." > /tmp/ly_err.txt; echo "::error::STT 실패"; exit 1; }
          echo "LY_TRANSCRIPT=/tmp/transcript.txt" >> "$GITHUB_ENV"
          echo "STT(파일) 완료 — $(wc -l < /tmp/transcript.txt)줄"
          if [ -n "$CKEY" ] && [ -s "$SEGJSON" ]; then
            aws s3 cp "$SEGJSON" "s3://$R2_BUCKET/$CKEY" --endpoint-url "$ENDP" --only-show-errors 2>/dev/null \
              && echo "STT 캐시 적재: $CKEY" || echo "::warning::STT 캐시 적재 실패(비치명)"
          fi

      # 조기 segments.json 푸시(LY-EARLY) — STT가 끝나는 즉시 전사 사이드카만 먼저 커밋
      #   → 뷰어 편집기가 의역(claude 1~5분)을 안 기다리고 바로 뜸(260706 평의회R2 E7 배점미달 후속 = 별도 카나리아 트랙 · 카나리아 e2etest-early 실측 = 전사 89초 선행).
      #   best-effort: 어떤 실패든 exit 0(마지막 Commit output이 어차피 포함 = 회귀 0) · push 경쟁만 짧게 재시도.
      #   ⚠️ 이 if는 암묵 success() 게이트 필수 — always()/failure() 추가 금지(STT 실패 시 부분 segments.json 조기 push 방지 · 평의회8).
      - name: 조기 segments.json 커밋 (전사 즉시 편집기 선행)
        if: ${{ inputs.early_segs == '1' && (inputs.url != '' || inputs.file != '' || inputs.r2_src != '') }}
        env:
          IN_ID: ${{ inputs.id }}
          BRANCH: ${{ github.ref_name }}
        run: |
          SEG="viewer/ly_out/${IN_ID}/segments.json"
          [ -s "$SEG" ] || { echo "segments.json 없음 — 조기 푸시 스킵"; exit 0; }
          git config user.name "nomute-bot"
          git config user.email "bot@nomute.local"
          git add "$SEG"
          git commit -m "ly: ${IN_ID} 전사 조기 커밋 (편집기 선행)" || exit 0
          for i in 1 2 3; do
            if git fetch origin "$BRANCH" && git rebase "origin/$BRANCH" && git push origin "HEAD:$BRANCH"; then
              echo "조기 푸시 완료 → 뷰어 편집기 선행 표시"; exit 0
            fi
            git rebase --abort 2>/dev/null || true; sleep $((i*2))
          done
          echo "조기 푸시 실패(push 경쟁) — 최종 Commit output에 포함 예정(무해)"; exit 0

      # 원문 패스트패스(운영자 260721 "수정을 안 하면 그렇게 진행" · Q392) — 언어 칩 '원문만(src)' = AI 다듬기 자체를 생략(무LLM·0콜):
      #   STT 결과(segments.json)로 표시용 subs.md만 기계 생성 → claude 스텝 스킵 → 번인은 ly_burn segments.json 폴백(STT word 1:1 = 최상 싱크).
      #   subs.json은 일부러 안 만든다 — 기존 폴백 경로(전투 검증분) 재사용 + 번인 note '받아쓴 자막(원문)으로 합성' 정직 표면화.
      #   segments.json 없음(STT 실패·SRT 텍스트 입력) 또는 생성 실패 = 미발동 → 종전 claude 경로 그대로(회귀 0 · fail-soft).
      - name: 원문 패스트패스 (무LLM · lang=src)
        if: ${{ inputs.reburn != '1' && contains(inputs.opts, '"lang":"src"') && (inputs.url != '' || inputs.file != '' || inputs.r2_src != '') }}
        env:
          IN_ID: ${{ inputs.id }}
        run: |
          [ -n "$IN_ID" ] || exit 0
          SEG="viewer/ly_out/${IN_ID}/segments.json"
          [ -s "$SEG" ] || { echo "segments.json 없음 — 패스트패스 미발동(claude 경로)"; exit 0; }
          if python3 - "$IN_ID" <<'PY'
          import json, sys
          vid = sys.argv[1]
          d = "viewer/ly_out/" + vid
          j = json.load(open(d + "/segments.json", encoding="utf-8"))
          segs = [s for s in (j.get("segs") or [])
                  if isinstance(s.get("s"), (int, float)) and isinstance(s.get("e"), (int, float)) and (s.get("t") or "").strip()]
          assert segs, "유효 조각 0"
          def mmss(x):
              x = int(round(float(x)))
              return "{}:{:02d}".format(x // 60, x % 60)
          dur = j.get("dur") or segs[-1]["e"]
          nunc = sum(1 for s in segs if s.get("unc"))   # 뭉개짐 의심(ly_stt lp/ns 휴리스틱 · Q581) — 오인식 후보 표면화
          L = ["# 받아쓴 원문 자막 (무편집 · 빠른 처리)", "",
               "## ⓪ 개요",
               "- **모드**: 원문만 — AI 다듬기·교정 생략(패스트패스 · 받아쓴 문장 그대로 입힘).",
               "- **메타**: {} · 조각 {}개 · 언어 {}".format(mmss(dur), len(segs), j.get("lang") or "?")]
          if nunc:
              L.append("- **⚠ 뭉개짐 의심 {}조각** — 발화가 뭉개져 오인식일 수 있어. 아래 ⚠ 조각만 상세 편집기에서 고치고 '다시 입히기'하면 그 부분만 다시 구워져.".format(nunc))
          L += ["", "## ① 전체 표", "", "| # | 시간 | 자막 |", "|---|---|---|"]
          for i, s in enumerate(segs, 1):
              L.append("| {} | {}~{} | {}{} |".format(i, mmss(s["s"]), mmss(s["e"]), (s.get("t") or "").strip().replace("|", "·"), " ⚠" if s.get("unc") else ""))
          L += ["", "---", "", "## ② 조각별 복사 블록", ""]
          for i, s in enumerate(segs, 1):
              L += ["{}. {}~{}{}".format(i, mmss(s["s"]), mmss(s["e"]), " ⚠(뭉개짐 의심 — 들리는 대로 교정 추천)" if s.get("unc") else ""), "```text", (s.get("t") or "").strip(), "```", ""]
          open(d + "/subs.md", "w", encoding="utf-8").write("\n".join(L) + "\n")
          print("패스트패스 subs.md: {}조각".format(len(segs)))
          PY
          then echo "LY_FASTPATH=1" >> "$GITHUB_ENV"
          else echo "::warning::패스트패스 생성 실패 — claude 경로로 진행"; fi

      - name: Make ly subtitles
        if: ${{ inputs.reburn != '1' && env.LY_FASTPATH != '1' }}
        timeout-minutes: 35   # claude 백스톱(평의회9 260709) — 폴오버 4계정 × 콜당 timeout 900s 최악 스택(60분)이 잡을 다 먹기 전 차단(실측 정상 1~5분 · 킬 시 failure() 실패 기록이 표면화)
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets[format('CLAUDE_CODE_OAUTH_TOKEN_{0}', inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO')] }}
          CLAUDE_CODE_OAUTH_TOKEN_ALT: ${{ (inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO') == 'NOMUTEFB' && secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130G || secrets.CLAUDE_CODE_OAUTH_TOKEN_NOMUTEFB }}   # 서브1 폴오버(활성 다음 우선순위 · 체인 MUTENO→NOMUTEFB→EMS1130G→MUTENONA)
          CLAUDE_CODE_OAUTH_TOKEN_ALT2: ${{ (inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO') == 'MUTENO' && secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130G || secrets.CLAUDE_CODE_OAUTH_TOKEN_MUTENO }}   # 서브2 폴오버(체인 3번째)
          CLAUDE_CODE_OAUTH_TOKEN_ALT3: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_MUTENONA }}   # 서브3 폴오버(체인 실꼬리 = 항상 MUTENONA · 앞 3계정 소진 후)
          ACTIVE_ACCOUNT: ${{ inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO' }}
          SUBS: ${{ inputs.subs }}
          IN_ID: ${{ inputs.id }}
          OPTS: ${{ inputs.opts }}   # 뷰어 버튼 설정 → lymake.sh가 [옵션] 지시줄로 치환(빈값 = 종전 프롬프트)
          # 의역 속도 A/B 노브(운영자 260808 "클로드 의역을 줄이는 방법") — 레포 **변수**(Settings▸Variables)로 조절한다.
          #   ⚠ workflow_dispatch inputs 신설이 아니다: 이 워크플로는 이미 inputs 11개라 12번째는 디스패치 실패 위험(rate.yml 10/10 선례).
          #   미설정(빈 문자열) = lymake.sh 기본값 그대로 = 종전 동작 100%(LY_MODEL→PIPE_MODEL=opus · LY_EFFORT→high).
          #   A/B = 변수 하나 바꾸고 자막 1건 발사 → 되돌리기 = 변수 삭제. 코드 변경 0.
          LY_MODEL: ${{ vars.LY_MODEL }}
          LY_EFFORT: ${{ vars.LY_EFFORT }}
        run: |
          [ -n "$IN_ID" ] || { echo "::error::id 없음"; exit 1; }
          if [ -z "$CLAUDE_CODE_OAUTH_TOKEN" ]; then
            echo "::error::CLAUDE_CODE_OAUTH_TOKEN_<계정> 시크릿이 비어있음"; exit 1
          fi
          # 영상 URL 경로면 STT 트랜스크립트를 입력(SUBS)으로 덮어씀
          if [ -n "${LY_TRANSCRIPT:-}" ] && [ -s "${LY_TRANSCRIPT}" ]; then
            SUBS="$(cat "${LY_TRANSCRIPT}")"; export SUBS
          fi
          unset ANTHROPIC_API_KEY ANTHROPIC_AUTH_TOKEN
          bash .github/scripts/lymake.sh "$IN_ID"

      # 자막 텍스트 선공개(번인 전 조기 커밋) — 번인(인코딩 1~3분)을 안 기다리고 자막부터 표시(LY-EARLY 패턴 계승 · best-effort exit 0).
      #   ⚠️ 암묵 success() 게이트 유지 — lymake 실패 시 부분 산출 조기 push 방지(early_segs와 동일 원칙).
      - name: 조기 subs 커밋 (번인 전 자막 선공개)
        if: ${{ env.LY_VIDEO != '' && inputs.reburn != '1' }}
        env:
          IN_ID: ${{ inputs.id }}
          OPTS: ${{ inputs.opts }}
          BRANCH: ${{ github.ref_name }}
        run: |
          BURN="$(printf '%s' "${OPTS:-}" | jq -r 'try .burn catch empty' 2>/dev/null || true)"
          [ "$BURN" = "true" ] || { echo "번인 OFF — 조기 커밋 불필요(최종 커밋이 바로)"; exit 0; }
          git config user.name "nomute-bot"
          git config user.email "bot@nomute.local"
          git add "viewer/ly_out/${IN_ID}" 2>/dev/null || true
          git commit -m "ly: ${IN_ID} 자막 조기 커밋 (번인 전 선공개)" || exit 0
          for i in 1 2 3; do
            if git fetch origin "$BRANCH" && git rebase "origin/$BRANCH" && git push origin "HEAD:$BRANCH"; then
              echo "조기 푸시 완료 → 자막 먼저 표시"; exit 0
            fi
            git rebase --abort 2>/dev/null || true; sleep $((i*2))
          done
          echo "조기 푸시 실패(push 경쟁) — 최종 Commit output에 포함(무해)"; exit 0

      # 배경음 제거(opts.bgm) 요청 런에서만 — Demucs(보컬 분리·로컬·키 불필요) 설치 + 모델·휠 7일 캐시(§9 캐시·타임아웃 원칙).
      #   실패 = 경고 후 계속(fail-soft — ly_burn.strip_bgm이 미설치를 감지해 원본 소리로 합성·잡 안 죽임). torch = CPU 휠(CUDA 대형휠 회피).
      - name: Demucs 캐시 (배경음 제거)
        if: ${{ contains(inputs.opts, '"bgm":true') && contains(inputs.opts, '"burn":true') && env.LY_NOBURN != '1' && env.LY_VIDEO != '' }}   # bgm은 번인 하위 축 — burn OFF·길이 선-스킵(LY_NOBURN)·영상 미확보(LY_VIDEO 빈값 = 번인이 어차피 skip 기록만 · 260721)면 설치도 스킵(3~7분 헛설치 차단 · 평의회9·재평의회1) · contains = 뷰어 JSON.stringify 고정 직렬화 전제(기존 bgm 게이트 동일) · env 참조 = 조기 subs 커밋 스텝의 LY_VIDEO 선례
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/torch
            ~/.cache/pip
          key: nomute-ly-demucs-${{ runner.os }}-htdemucs-torch2121-demucs401-v1   # 핀 버전을 키에 반영(핀 갱신 = 키 동행 갱신 · 평의회2)
      - name: Demucs 설치 (배경음 제거)
        if: ${{ contains(inputs.opts, '"bgm":true') && contains(inputs.opts, '"burn":true') && env.LY_NOBURN != '1' && env.LY_VIDEO != '' }}   # 캐시 스텝과 동일 게이트(평의회9·재평의회1 · +영상 미확보 스킵 260721)
        timeout-minutes: 12   # 설치 폭주 백스톱(재시도 포함 최악 20분 → 12분 컷 · 실측 콜드 3~7분)
        run: |
          ok=0
          # demucs = sdist 전용(휠 없음) → setup.py 빌드에 setuptools 필수인데 러너 py3.12엔 미탑재
          #   ("Can not execute setup.py since setuptools is not available" = 라이브 카나리아 #28874896795 실측) → 선설치.
          #   ⚠️ SETUPTOOLS_USE_DISTUTILS=stdlib 금지 — py3.12는 stdlib distutils 삭제라 오히려 파손(3.11 데비안 전용 우회였음).
          for i in 1 2; do
            if timeout 120 python3 -m pip install -q --upgrade setuptools wheel \
               && timeout 300 python3 -m pip install -q torch==2.12.1 torchaudio==2.11.0 torchcodec==0.14.0 --index-url https://download.pytorch.org/whl/cpu \
               && timeout 300 python3 -m pip install -q demucs==4.0.1 soundfile==0.14.0; then ok=1; break; fi   # 버전 핀 = 무핀 상류 추종의 조용한 기능 사망 차단(torchcodec↔torch ABI 강결합 · 평의회2 P1) · torchcodec = 신 torchaudio save 백엔드(로컬 실측 260707)
            sleep 5
          done
          [ "$ok" = 1 ] && python3 -c "import demucs.separate; print('demucs OK')" \
            || echo "::warning::Demucs 설치 실패 — 배경음 제거 스킵(원본 소리로 합성)"

      # 조기 교정본 픽업(합성 전 반영 · Q586 — 운영자 "두 번 굽지 말고 합성 전에 저장") — 조기 전사(LY-EARLY) 창에서
      #   뷰어가 /api/ly earlyfix로 main에 커밋한 subs.early.json을 번인 *직전* fetch해 subs.json 대체(변환 = '편집 자막 반영' 스텝 미러).
      #   늦은 저장(이 스텝 통과 후 커밋) = 자연 미반영 → 종전 '다시 입히기' 폴백(유실 0 · 레이스 무해). fresh 런만(reburn은 종전 스텝이 담당).
      - name: 조기 교정본 픽업 (합성 전 반영)
        if: ${{ inputs.reburn != '1' && (inputs.url != '' || inputs.file != '' || inputs.r2_src != '') }}
        env:
          IN_ID: ${{ inputs.id }}
        run: |
          [ -n "$IN_ID" ] || exit 0
          [[ "$IN_ID" =~ ^[0-9]{12}-[0-9a-f]{6}$ ]] || { echo "id 형식 밖 — 스킵"; exit 0; }
          git fetch origin main -q 2>/dev/null || { echo "::warning::fetch 실패 — 조기 교정 픽업 생략(현행 자막으로 진행)"; exit 0; }
          if ! git show "origin/main:viewer/ly_out/${IN_ID}/subs.early.json" > /tmp/early_fix.json 2>/dev/null || [ ! -s /tmp/early_fix.json ]; then
            echo "조기 교정 없음 — 현행 자막으로 진행"; exit 0
          fi
          if SUBS="$(cat /tmp/early_fix.json)" python3 - "$IN_ID" <<'PY'
          import json, os, re, shutil, sys
          from math import isfinite
          vid = sys.argv[1]
          assert re.fullmatch(r"[0-9]{12}-[0-9a-f]{6}", vid), "id 형식"   # '편집 자막 반영' 스텝 미러(검증 대칭 · 원자 교체까지 동문)
          j = json.loads(os.environ.get("SUBS") or "")
          segs = []
          for s in (j.get("segs") or [])[:700]:
              try:
                  ss, ee = float(s["s"]), float(s["e"])
              except Exception:
                  continue
              ko = str(s.get("ko") or "").strip()[:200]
              if not (isfinite(ss) and isfinite(ee)) or ss < 0 or ee <= ss or not ko:
                  continue
              segs.append({"s": round(ss, 2), "e": round(ee, 2), "ko": ko})
          assert segs, "유효 조각 0"
          d = os.path.join("viewer", "ly_out", vid)
          os.makedirs(d, exist_ok=True)
          p = os.path.join(d, "subs.json")
          orig = os.path.join(d, "subs.orig.json")
          if os.path.isfile(p) and not os.path.isfile(orig):
              shutil.copyfile(p, orig + ".tmp")   # 첫 반영 때 원본 의역 1회 보존(reburn 복원 소스와 동일 계약)
              os.replace(orig + ".tmp", orig)
          tmp = p + ".tmp"
          with open(tmp, "w", encoding="utf-8") as f:
              json.dump({"v": 1, "segs": segs}, f, ensure_ascii=False, separators=(",", ":"))
          os.replace(tmp, p)
          print("조기 교정 반영: {}조각".format(len(segs)))
          PY
          then echo "LY_EDITED=early" >> "$GITHUB_ENV"; cp /tmp/early_fix.json "viewer/ly_out/${IN_ID}/subs.early.json"   # 워킹트리 사본 = Commit output에 기록 동봉(반영분 감사 추적)
          else echo "::warning::조기 교정 반영 실패 — 현행 자막으로 진행"; fi

      # 자막 번인(영상 합성) — 자막을 영상에 입혀 R2 업로드 → video.json(뷰어 폴링). fail-soft = 실패해도 자막 텍스트 산출은 유지(rc 0).
      #   영상 확보 실패(LY_VIDEO 빈값)여도 ly_burn이 video.json에 스킵 사유 기록 → 뷰어 헛폴링 종료.
      - name: 자막 번인 (영상 합성 → R2)
        if: ${{ inputs.url != '' || inputs.file != '' || inputs.r2_src != '' || inputs.reburn == '1' }}
        timeout-minutes: 35   # 내부 최악 스택(bgm 분리 ~12m + 인코딩 900s + 폴백 600s = ~37m)에 근접 확보(구 30 = 스택보다 짧아 SIGKILL이 ly_burn 자체 기록을 우회 · 평의회6 260709) — 그래도 킬리면 아래 백필 스텝이 사유 기록
        continue-on-error: true   # 번인 실패/타임아웃 = 자막 텍스트 산출은 지킨다(기록 = ly_burn fail-soft + 아래 백필)
        env:
          IN_ID: ${{ inputs.id }}
          OPTS: ${{ inputs.opts }}
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
          R2_PUBLIC_BASE: ${{ secrets.R2_PUBLIC_BASE }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
        run: |
          [ -z "${LY_NOBURN:-}" ] || { echo "번인 선-스킵(길이 초과 · video.json 선기록 유지)"; exit 0; }
          BURN="$(printf '%s' "${OPTS:-}" | jq -r 'try .burn catch empty' 2>/dev/null || true)"
          [ "$BURN" = "true" ] || { echo "번인 OFF"; exit 0; }
          # ── 한글 폰트 = 번인이 실제로 돌 때만 설치(260808 폰트 분리 · 부품 = 100행 reburn 스텝 100% 모방) ──
          #   ⚠ burn 판정은 **JSON 파싱**으로 한다 — `contains(opts,'"burn":true')` 문자열 판정은 260711 Demucs 게이트에서
          #     평의회가 적발한 문법이고, 오판의 대가가 「무증상 두부 번인」이라 추측 판정을 쓰면 안 된다.
          #   ⚠ 파싱 실패·opts 빈값 = **설치**(안전측) = 종전 동작. 설치·검문 실패는 exit 1 = 두부 대신 정직한 실패
          #     (이 스텝은 continue-on-error 라 자막 텍스트 산출은 그대로 살아난다 = fail-soft 계약 보존).
          NEEDF="$(OPTS="${OPTS:-}" python3 -c 'import json,os;r=(os.environ.get("OPTS") or "").strip();o=json.loads(r) if r else {};print("0" if o.get("burn", True) is False else "1")' 2>/dev/null || echo 1)"
          if [ "$NEEDF" = "1" ]; then
            # ⚠ 검문 축 = **noto ∧ nanum 둘 다**(260808 실런 적발) — 러너 기본 이미지에 Noto CJK가 이미 있어
            #   noto만 보면 검문이 통과해버리고, 그 상태에서 style=nanum/pen 자막은 그대로 두부가 난다.
            #   (나눔 2종 = 편집기 폰트 축 nanum·pen = ly_burn 스타일 옵션. reburn 정본이 3종을 같이 까는 이유.)
            { fc-list | grep -qi "noto sans cjk" && fc-list | grep -qi "nanum"; } || {
              timeout 300 sudo apt-get update -qq || true
              timeout 300 sudo apt-get install -y -qq fonts-noto-cjk fonts-nanum fonts-nanum-extra || { echo "::error::한글 폰트 설치 실패"; exit 1; }
            }
            fc-list | grep -qi "noto sans cjk" || { echo "::error::Noto Sans CJK 미탐지 — 두부 번인 차단"; exit 1; }
            fc-list | grep -qi "nanum" || { echo "::error::나눔 폰트 미탐지 — style=nanum/pen 두부 번인 차단"; exit 1; }
          else
            echo "번인 OFF(opts.burn=false) — 폰트 설치 생략"
          fi
          python3 .github/scripts/ly_burn.py "$IN_ID" "${LY_VIDEO:-}" || echo "::warning::번인 실패 — 자막 텍스트는 정상"

      # 번인 기록 백필(평의회6 260709) — 번인 스텝 하드킬(timeout SIGKILL)은 continue-on-error라 실패 기록 스텝도 안 돎
      #   → video.json 부재면 사유를 백필해 뷰어 영구 '입히는 중' 차단. 정상 경로 = ly_burn이 항상 기록(백필 노터치).
      - name: 번인 기록 백필 (하드킬 대비)
        if: ${{ always() && (inputs.url != '' || inputs.file != '' || inputs.r2_src != '' || inputs.reburn == '1') }}
        env:
          IN_ID: ${{ inputs.id }}
          OPTS: ${{ inputs.opts }}
        run: |
          [ -n "$IN_ID" ] || exit 0
          BURN="$(printf '%s' "${OPTS:-}" | jq -r 'try .burn catch empty' 2>/dev/null || true)"
          { [ "$BURN" = "true" ] || [ "${REBURN:-}" = "1" ]; } || exit 0
          [ -s "viewer/ly_out/${IN_ID}/subs.md" ] || exit 0   # 자막 산출 자체가 없으면(STT·claude 단계 실패) 백필 부적절 — '자막 텍스트는 정상' 오기록 차단(재평의회6 잔여 의문 봉합 · 그 경로는 실패 기록 스텝이 error.log로 표면화)
          VJ="viewer/ly_out/${IN_ID}/video.json"
          [ -s "$VJ" ] || { mkdir -p "viewer/ly_out/${IN_ID}"; printf '{"error":"번인 중단(시간 초과 추정) — 자막 텍스트는 정상"}' > "$VJ"; echo "백필: $VJ"; }

      - name: 실패 기록(error.log) — 프론트가 폴링해 즉시 표시
        if: failure()
        env:
          IN_ID: ${{ inputs.id }}
        run: |
          [ -n "$IN_ID" ] || exit 0
          mkdir -p "viewer/ly_out/${IN_ID}"
          if [ -s /tmp/ly_err.txt ]; then cp /tmp/ly_err.txt "viewer/ly_out/${IN_ID}/error.log";
          elif [ ! -s "viewer/ly_out/${IN_ID}/error.log" ]; then echo "자막 생성 실패 — Actions 로그를 확인해줘." > "viewer/ly_out/${IN_ID}/error.log"; fi
          # ↑ elif = lymake.sh가 남긴 상세 error.log(exit_code·stderr·stdout head) 보존 — 일반 문구로 덮지 않음(평의회6 260709)
          echo "error.log: $(cat viewer/ly_out/${IN_ID}/error.log)"

      - name: Commit output
        if: always()
        env:
          IN_ID: ${{ inputs.id }}
          BRANCH: ${{ github.ref_name }}
        run: |
          git config user.name "nomute-bot"
          git config user.email "bot@nomute.local"
          git rm -r "uploads/${IN_ID}" 2>/dev/null || true   # 업로드 영상/오디오 = 오디오 추출 후 불필요(git 비대화 방지)
          git add "viewer/ly_out/${IN_ID}" 2>/dev/null || true
          git add metrics 2>/dev/null || true   # 토큰 계측 shard(claude_meter) 동반 커밋 — 있을 때만
          git commit -m "ly: ${IN_ID} 자막 출력" \
            || { [ -z "$(git log --oneline "origin/${BRANCH}..HEAD" 2>/dev/null | head -1)" ] && { echo "커밋할 것 없음"; exit 0; }; echo "신규 커밋 없음 — 미푸시 선행 커밋(조기 subs) push 진행"; }   # 조기 커밋 push 경쟁 실패 잔존분 유실 방지
          pushed=0
          for i in 1 2 3 4 5; do
            if git fetch origin "$BRANCH" && git rebase "origin/$BRANCH" && git push origin "HEAD:$BRANCH"; then pushed=1; break; fi
            git rebase --abort 2>/dev/null || true; sleep $((i*3))
          done
          [ "$pushed" = 1 ] || { echo "::error::push ${BRANCH} 5회 실패 — 출력 유실"; exit 1; }

      # 업로드 일회용 브랜치 삭제(성공·실패 무관) — 삭제되면 업로드 블롭이 unreachable = 클론·main 히스토리에 안 남음(비대 0).
      #   가드 = up-* 만(임의 브랜치·main 오삭제 차단). 실패해도 무해(고아 브랜치 잔존일 뿐 — 발견 시 수동 정리).
      - name: 업로드 브랜치 정리
        if: ${{ always() && inputs.up_branch != '' }}
        env:
          UP_BRANCH: ${{ inputs.up_branch }}
        run: |
          case "$UP_BRANCH" in up-*) ;; *) echo "up-* 아님 — 스킵"; exit 0;; esac
          git push origin --delete "$UP_BRANCH" || echo "브랜치 정리 실패(무해 — 수동 정리 대상): $UP_BRANCH"

      - name: 임시 업로드 청소 (R2 up_src · 나이 지난 것만)
        # (260810 운영자 "한번 올린거 바로 휘발시키지말고 냅둬봐") 종전 = 이 잡이 쓴 원본을 그 자리에서 `aws s3 rm` →
        #   러너는 원본을 **읽기만** 하는데도 같은 영상 두 번째 작업이 전체 재업로드였다(1GB면 1GB 다시).
        #   지금 = 남겨두고 **나이 든 것만** 청소(정본 = shared/r2_upsrc_gc.sh · TTL 기본 24h · 삭제 상한 200).
        #   만료 뒤 재발사는 api 쪽 head 검증이 "업로드 파일이 없어" 를 주고 뷰어가 자동 재업로드 = 종전 동작으로 폴백.
        if: ${{ always() }}
        env:
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
        run: |
          [ -f shared/r2_upsrc_gc.sh ] && bash shared/r2_upsrc_gc.sh || echo "청소기 없음(체크아웃 전 실패) — 스킵"