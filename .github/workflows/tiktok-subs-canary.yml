# 틱톡 구독 브라우저 카나리아 — workflow_dispatch 전용(§📰-e 카나리아 룰: 실측 성공 전 cron 금지)
# 목적(운영자 260721 "무조건 최신"): 구독 큐레이션의 폰 의존 0 러너 자립 최신 수집 실측.
# v2 = tikwm 제거·tiktok.com 프로필 직수집(브라우저 XHR 가로채기 — v1 tikwm user/posts는 러너 stdlib·
# 실브라우저·폰 3면 전멸 실측 260721 = 창구 폐쇄). 전면 fail-soft(실패 = 산출 무변·warning).
# 성공 실측되면 승격 검토(sns-trends 주기 편입 — 운영자 확인 후 · tiktok-canary 관용구 동일).
# 브랜치에서 dispatch = 측정 전용(Commit 스텝 main 한정 게이트 — 브랜치 체크아웃을 main에 푸시하는 사고 차단).
name: tiktok-subs-canary
on:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: sns-trends   # sns_trends.json 동시 기록 직렬화(같은 파일 · sns-trends/tiktok-canary와 동일 그룹)
  cancel-in-progress: false

jobs:
  canary:
    runs-on: ubuntu-latest
    timeout-minutes: 12   # v2 = 프로필 30페이지 렌더(계정당 goto+4s) ≈ 4~6분 + 설치 여유
    steps:
      - uses: actions/checkout@v4   # ref 미지정 = dispatch한 ref 그대로(브랜치 실측 가능)

      - name: Playwright(시스템 크롬 channel — 브라우저 다운로드 0 · §9 timeout 감쌈)
        run: timeout 300 pip install -q playwright || (sleep 5 && timeout 300 pip install -q playwright)

      - name: Collect
        run: timeout 600 python3 scraper/tiktok_subs_browser.py

      - name: Commit
        if: github.ref == 'refs/heads/main'   # 브랜치 dispatch = 측정 전용(데이터 착지는 main 실행만)
        run: |
          git config user.name "nomute-bot"
          git config user.email "bot@users.noreply.github.com"
          git add viewer/sns_trends.json 2>/dev/null || true
          git diff --cached --quiet && { echo "산출 없음(카나리아 실패/무변) — 커밋 생략"; exit 0; }
          git commit -m "tiktok-subs-canary: 구독 최신 수집(브라우저)"
          for i in 1 2 3 4; do
            # ⚠ 260816 무해 명시 = sns-trends 와 같은 concurrency 그룹 = 이미 직렬화 —
            #   이 자리는 `-X ours` 드랍이 무해하다 — sns_trends 를 쓰는 형제들과 **같은 concurrency 그룹으로 이미 직렬화**돼 있고 데이터 churn 이라 다음 회차가 전량 덮는다(260816 실측).
            git pull --rebase --autostash -X ours origin main || echo "::warning::pull--rebase 실패(사유 = 위 로그) — 재시도 계속"
            git push origin HEAD:main && exit 0 || { echo "push 재시도 $i"; sleep $((2**i)); }
          done
          # ⚠ 260816 봉합 — 구판은 회수 경로를 말하지 않아 조용한 유실과 구분이 안 됐다.
          #   이 축은 데이터 churn 이라 다음 회차가 전량 덮는다 = 실패로 잡을 자리가 아니고,
          #   빠진 건 그 사실을 말하는 것뿐이다(rc 를 올리면 붐빌 때마다 거짓 경보).
          echo "::warning::push 실패(재시도 소진) — 카나리아 관측치는 다음 회차가 전량 다시 만들어 회수한다"
