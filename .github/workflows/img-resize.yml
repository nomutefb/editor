# 이미지 비율 재구성(리사이즈) v1 — workflow_dispatch 전용(유료 Gemini 수동 발사만 · §📰 불변)
# 발사 = functions/api/resize.js(uploads/<id>/src 선커밋 → src_sha 레이스 가드 → dispatch · compose.js 계승)
# 산출 = R2 resize/<id>/ (또는 git viewer/gen_out/) + viewer/gen_out/resize.json → 뷰어 폴링.
name: img-resize
on:
  workflow_dispatch:
    inputs:
      id:
        description: '작업 id (uploads/<id>/)'
        required: true
        type: string
      src:
        description: '업로드 원본 경로 (uploads/<id>/src.ext)'
        required: true
        type: string
      src_sha:
        description: '업로드 커밋 SHA — dispatch 레이스 가드(comp-make 계승)'
        required: false
        type: string
      opts:
        description: '옵션 JSON {aspect,size,lock,fill}'
        required: false
        type: string

permissions:
  contents: write   # 결과 커밋 push(exp-resize 403 실측 계승)

concurrency:
  group: img-resize-${{ inputs.id }}
  cancel-in-progress: false

jobs:
  resize:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.src_sha || 'main' }}   # 업로드 src 커밋 직접 체크아웃 = dispatch 레이스 원천 차단(comp-make.yml:40 계승)

      - name: Deps (timeout 감쌈 · §9 인프라)
        run: timeout 300 pip install -q pillow numpy || (sleep 5 && timeout 300 pip install -q pillow numpy)

      - name: Resize
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
          R2_PUBLIC_BASE: ${{ secrets.R2_PUBLIC_BASE }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          RESIZE_ID: ${{ inputs.id }}
          RESIZE_SRC: ${{ inputs.src }}
          RESIZE_OPTS: ${{ inputs.opts || '{}' }}
          # 이미지 모델 축(260808 · 운영자 "3.1 pro를 쓰든 3.1 flash를 쓰든 다 테스트해서") — repo variable로
          # 갈아끼운다(빈값 = thumb_gen MODEL 정본 = 종전 동작). 씨앗·프롬프트·판정을 고정한 채 모델만 바꿔야
          # 비교가 성립하므로 판정 모델은 별도 변수로 고정할 수 있게 둔다.
          RESIZE_IMG_MODEL: ${{ vars.RESIZE_IMG_MODEL }}
          RESIZE_JUDGE_MODEL: ${{ vars.RESIZE_JUDGE_MODEL }}
          RESIZE_SEED: ${{ vars.RESIZE_SEED }}
          RESIZE_TRIES: ${{ vars.RESIZE_TRIES }}   # 생성 시도 상한(빈값 = 3 · 260808 실측 근거는 resize_image.py TRIES 주석)
        run: python3 .github/scripts/resize_image.py

      - name: Commit results
        run: |
          git config user.name "nomute-bot"
          git config user.email "bot@users.noreply.github.com"
          git add viewer/gen_out/ 2>/dev/null || true
          git diff --cached --quiet && { echo "산출 없음(no-op)"; exit 0; }
          git commit -m "resize: 비율 재구성 산출 (${{ inputs.id }})"
          for i in 1 2 3 4; do
            git pull --rebase --autostash -X ours origin main || echo "::warning::pull--rebase 실패(사유 = 위 로그) — 재시도 계속"
            # resize.json 유실 봉합 — rebase -X ours(원격 승) 시 신규 항목 드랍 → /tmp 임시본 재병합(imggen.yml 계승)
            #   ⚠ --amend 금지(260803 실측 = filltest 동시 3발사 중 후발 2런 전패 · 런 30779914067): 자기 커밋이 rebase에서
            #   'already upstream'으로 드랍되면 --amend가 **남의 푸시된 커밋**을 고쳐 non-ff 영구 거절 = 산출 유실.
            #   봉합 = imggen 원조처럼 재병합분을 **새 커밋**으로만 싣는다(스테이징 diff 있을 때만 — 자기 커밋 생존 시 diff 0 = no-op).
            if [ -f /tmp/resize_new.json ]; then
              python3 -c 'import json,os;p="viewer/gen_out/resize.json";new=json.load(open("/tmp/resize_new.json"));cur=json.load(open(p,encoding="utf-8")) if os.path.exists(p) else [];urls=set(i.get("url") for i in cur);json.dump(([i for i in new if i.get("url") not in urls]+cur)[:24],open(p,"w",encoding="utf-8"),ensure_ascii=False,indent=2)'
              git add viewer/gen_out/ 2>/dev/null || true
              git diff --cached --quiet || git commit -m "resize: 비율 재구성 산출 재병합 (${{ inputs.id }})"
            fi
            git push origin HEAD:main && exit 0 || { echo "push 재시도 $i"; sleep $((2**i)); }
          done
          echo "::error::push 실패(재시도 소진) — 산출 유실"; exit 1
          # ⚠️ push = HEAD:main (detached HEAD 대응) — checkout이 inputs.src_sha(comp-make 계승 = 특정 커밋 detached)라
          #    `git push origin main`은 로컬 main 브랜치 부재로 `src refspec main does not match any` 실패(런 29068702013 실측).
          #    comp/thumb/ly-make 3형제 전부 이 상황(src_sha detached)에서 HEAD:main 사용. imggen의 'HEAD:main 금지'는
          #    *브랜치 ref 디스패치* 우려인데, src_sha는 resize.js가 branch:main으로 PUT한 main 계보 커밋 해시라 무해(브랜치 누출 0).
