name: edit-make

# 뷰어 편집기 폼(Pages Function /api/edit)이 발사 — 업로드 1번·1잡: 영상(URL/업로드) → [자막·컷 ON이면 STT · 자막 ON에만 claude 의역]
#   → ly_burn.py 단일 컴포지터{자막 번인·무음 컷·배경음 제거 + 편집기 축: 트림·비율 크롭/여백·해상도·fps(60i 보간)·음량 통일}
#   → R2 ly_out/<id>/subbed.mp4 → viewer/ly_out/<id>/video.json 커밋(폼이 폴링). 골격 = ly-make 미러(스텝 게이트만 편집기화).
#   기존 ly-make·conv-make 무접촉(평의회6 점진 이관 · 운영자 2단계 확정 260710).
on:
  workflow_dispatch:
    inputs:
      id:
        description: '작업 id(타임스탬프-hex6)'
        type: string
      url:
        description: '영상 URL(yt-dlp)'
        type: string
        default: ''
      file:
        description: '업로드 영상 파일 경로(uploads/<id>/src.*)'
        type: string
        default: ''
      up_branch:
        description: '업로드 일회용 브랜치(up-<id> · 빈값=main 커밋 경로) — ly/track/conv 동일'
        type: string
        default: ''
      r2_src:
        description: 'R2 직업로드 키(up_src/<id>.<ext> · 대용량 ≤2GB — api/upload 발급 · 빈값=미사용 · 보관 24h 뒤 자동 청소[260810 — 구 「잡 끝 삭제」 폐지])'
        type: string
        default: ''
      opts:
        description: '편집 옵션 JSON — ly 자막 축{burn,lang,tone,filler,style,pos,size,bg,karaoke,pop,keyword,cut,cutlv,bgm,outline,pad,oc,font,dual_small} + 편집기 축{vid_ar,vid_fit(crop/pad/blur),vid_pos,vid_res,vid_fps,vid_t0,vid_t1,vid_segs([[s,e],…] n구간 이어붙기 · 260728),vid_xfade(이음매 디졸브 % · 260728),aud_norm} + 클리퍼{clip=배타 스캔: STT→구간픽→clips.json·컴포즈 스킵 · 260711} + 컷 강화{cutfill=필러 컷·take=반복 테이크 감지(claude)·cutscan=컷 미리보기 스캔(컴포즈 스킵→cuts.json)·cutref/cutoff=승인 컷 소비 · 260727}'
        type: string
        default: '{}'
      account:
        description: '이 실행에 쓸 계정(구독 OAuth · 자막 의역용)'
        type: choice
        default: MUTENO
        options:
          - NOMUTEFB
          - MUTENO
          - EMS1130G

concurrency:
  group: edit-make-${{ inputs.id }}
  cancel-in-progress: false

permissions:
  contents: write
  checks: read   # 배포 게이트(deploy-then-notify) — Pages 'Cloudflare Pages' 체크런 조회용(thumb-make 정본 계승 · §완료알림 기틀)

jobs:
  edit:
    runs-on: ubuntu-latest
    timeout-minutes: 105   # 최악 스택{체크아웃·환경 ~20 + R2 소스 8 + STT 45(large-v3 260726) + 컴포즈 60(4K enc 2400s 포함)} + 우아한 실패·커밋 창 — 잡 캡 = 최후 보루(ly-make 문법 · 4K 재산정 평의회6 260711 · large-v3 재산정 260726)
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          filter: blob:none   # 부분 클론 — ly-make(260721)엔 있는데 여기만 빠져 있던 누락(260728 Q1001 실측: 편집 실런 Checkout 81s = 전체의 26%로 STT보다 큼). 커밋 그래프는 전부 받으므로 rebase·push 종전 그대로 · 역사 블롭만 지연 회수(필요 시 promisor 자동 fetch)
          # ⚡ 260808 sparse-checkout(ly-make 정본 계승 · 카나리아 run 31234731214 = Checkout 21s→6s · 삭제 0건 실증)
          #   ⚠ 전제 = 무경로 `git add -A`/`git add .` 0건(check_stt_engine_chain ⑥이 강제) · 신규 add 는 경로 지정.
          #   ⚠ uploads·metrics 는 cone 유지 — 커밋 스텝의 `git rm -r uploads/<id>`·metrics shard 가 의존한다.
          sparse-checkout: |
            .github
            shared
            apps/ly
            apps/conv
            apps/track
            prompts
            assets/fonts
            viewer/ly_out
            metrics
            uploads
            .claude

      # ── 자막·클리퍼 ON 경로 전용 준비(claude 의역/구간픽) — 둘 다 아니면 Node·claude 설치 안 함(러너 분 절약)
      - name: Setup Node (자막)
        if: ${{ contains(inputs.opts, '"burn":true') || contains(inputs.opts, '"clip":true') || contains(inputs.opts, '"take":true') }}
        uses: actions/setup-node@v5
        with:
          node-version: '24'
      - name: Install claude-code (자막)
        if: ${{ contains(inputs.opts, '"burn":true') || contains(inputs.opts, '"clip":true') || contains(inputs.opts, '"take":true') }}
        run: npm install -g @anthropic-ai/claude-code

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Restore python deps (site-packages 캐시 · 자막·컷·클리퍼)
        if: ${{ contains(inputs.opts, '"burn":true') || contains(inputs.opts, '"cut":true') || contains(inputs.opts, '"clip":true') || contains(inputs.opts, '"cutfill":true') || contains(inputs.opts, '"take":true') || contains(inputs.opts, '"cutscan":true') }}
        uses: actions/cache@v4
        with:
          path: ${{ env.pythonLocation }}/lib/python3.12/site-packages
          key: nomute-ly-pydeps-${{ runner.os }}-py312-${{ hashFiles('apps/ly/setup.sh') }}-v1   # ly-make와 캐시 공유(동일 키 = 재설치 0)   # ↓ 근접 복원 폴백(260728 Q1001) — setup.sh **주석 한 줄만 고쳐도** hashFiles가 바뀌어 전체 재설치(콜드 318s)되던 구조 결함 봉합.
          #    정확 키 miss여도 접두사가 같은 직전 캐시를 복원 → setup.sh의 `import faster_whisper` 체크가 통과해 pip 스킵.
          #    ⚠ 의존성이 진짜 바뀐 경우도 옛 캐시를 복원하지만, setup.sh가 import 확인 후 없는 것만 설치하므로 신규 패키지는 정상 설치된다(= 원하는 동작).
          restore-keys: |
            nomute-ly-pydeps-${{ runner.os }}-py312-

      - name: STT 환경 (자막·컷·클리퍼 = whisper+ffmpeg+한글폰트)
        if: ${{ contains(inputs.opts, '"burn":true') || contains(inputs.opts, '"cut":true') || contains(inputs.opts, '"clip":true') || contains(inputs.opts, '"cutfill":true') || contains(inputs.opts, '"take":true') || contains(inputs.opts, '"cutscan":true') }}
        timeout-minutes: 20   # 첫 런 Whisper large-v3 3.1GB 다운 감안(260726 승격 · 캐시히트 즉시)
        uses: ./.github/actions/runner-setup
        with:
          setup: apps/ly/setup.sh
          # ⚡ 260808 — STT 기본 = Scribe v2(ly_stt.py) → large-v3는 **폴백 전용**이라 3.1GB 캐시·prefetch를 평소엔 안 붙인다.
          #   실측 = 이 스텝 48s(총 112s의 43%)가 Scribe 경로에선 한 번도 안 쓰이는 짐이었다(run 31231381993 분해).
          #   'true'로 되돌아가는 조건 = 킬스위치(vars.LY_STT_ENGINE=whisper) ∨ 키 부재 = **폴백이 유일 경로인 상황**.
          #   ⚠ ffmpeg·yt-dlp·faster_whisper(pip)는 그대로 설치된다 — 빠지는 건 모델 파일 3.1GB뿐(폴백 시 그 자리에서 회수).
          whisper: ${{ (vars.LY_STT_ENGINE == 'whisper' || secrets.ELEVENLABS_API_KEY == '') && 'true' || 'false' }}
          apt: ffmpeg fonts-noto-cjk fonts-nanum fonts-nanum-extra   # 나눔 2종 = 자막 폰트 축(gothic/serif = noto-cjk 안 · nanum=나눔고딕 · pen=Nanum Pen Script는 -extra) — apt 캐시 키에 목록 포함 = 첫 런만 다운(260711)

      - name: 변환 환경 (자막·컷·클리퍼 없음 = ffmpeg+yt-dlp만)
        if: ${{ !contains(inputs.opts, '"burn":true') && !contains(inputs.opts, '"cut":true') && !contains(inputs.opts, '"clip":true') && !contains(inputs.opts, '"cutfill":true') && !contains(inputs.opts, '"take":true') && !contains(inputs.opts, '"cutscan":true') }}
        timeout-minutes: 10
        uses: ./.github/actions/runner-setup
        with:
          setup: apps/conv/setup.sh
          apt: ffmpeg

      - name: 소스 확보 (URL)
        if: ${{ inputs.url != '' }}
        timeout-minutes: 10
        env:
          URL: ${{ inputs.url }}
          YT_COOKIES: ${{ secrets.YT_COOKIES }}
          YT_COOKIES_2: ${{ secrets.YT_T_COOKIES }}    # 다운로더 슬롯 1 — 구 YT_COOKIES가 죽었을 때 회수(260811 · 건강검진이 보는 건 이쪽뿐이라 구 쿠키는 죽어도 무경보였다)
          YT_COOKIES_3: ${{ secrets.YT_T2_COOKIES }}   # 다운로더 슬롯 2(미설정이면 빈값 = 건너뜀)
          YTF: ${{ contains(inputs.opts, '"vid_res":"src"') && 'bv*[height<=2160]+ba[ext=m4a]/bv*[height<=2160]+ba/b' || 'bv*[height<=1920]+ba[ext=m4a]/bv*[height<=1920]+ba/b' }}   # 해상도 '원본(4K)' 선택 시에만 4K 페치(그 외 1920 = 비용 보호) · ba[ext=m4a] 선행 = AAC 강제(알몸 ba는 Opus를 골라 mp4에 그대로 들어가고 프리미어가 오디오 트랙을 조용히 무시한다 · 260805)
        run: |
          set -e
          COOKIES=""
          if [ -n "$YT_COOKIES" ]; then printf '%s\n' "$YT_COOKIES" > /tmp/ck.txt; COOKIES="--cookies /tmp/ck.txt"; fi
          # 시도 사다리 = .github/scripts/ytdlp_try.sh(①쿠키+기본 ②쿠키+대체 ③무쿠키+기본 ④무쿠키+대체 · 260811)
          YTDLP_ERR=/tmp/yt_err.txt YTDLP_LABEL="영상 받기" bash .github/scripts/ytdlp_try.sh -f "$YTF" --merge-output-format mp4 --no-playlist -o "/tmp/src.%(ext)s" "$URL" \
            || { echo "영상 다운로드 실패 — 쿠키를 빼고도 다시 시도했는데 안 됐어. 파일 업로드로 해줘." > /tmp/edit_err.txt; echo "::error::yt-dlp 실패(4단 후): $URL"; exit 1; }
          SRC="$(ls /tmp/src.* 2>/dev/null | head -1 || true)"
          [ -n "$SRC" ] || { echo "영상 다운로드 실패 — 파일 업로드로 해줘." > /tmp/edit_err.txt; echo "::error::다운 산출 없음"; exit 1; }
          echo "EDIT_SRC=$SRC" >> "$GITHUB_ENV"

      - name: 소스 확보 (업로드 파일)
        if: ${{ inputs.file != '' }}
        env:
          FILE: ${{ inputs.file }}
          UP_BRANCH: ${{ inputs.up_branch }}
        run: |
          set -e
          if [ -n "$UP_BRANCH" ]; then
            case "$UP_BRANCH" in up-*) ;; *) echo "::error::up_branch는 up-* 만 허용: $UP_BRANCH"; exit 1;; esac
            git fetch origin "$UP_BRANCH" --depth=1 || { echo "업로드 파일이 유실됐어 — 다시 올려줘." > /tmp/edit_err.txt; echo "::error::업로드 브랜치 fetch 실패: $UP_BRANCH"; exit 1; }
            EXT="${FILE##*.}"
            git show "FETCH_HEAD:$FILE" > "/tmp/upsrc.$EXT" || { echo "업로드 파일 회수 실패 — 다시 올려줘." > /tmp/edit_err.txt; echo "::error::브랜치서 파일 회수 실패: $FILE"; exit 1; }
            FILE="/tmp/upsrc.$EXT"
          fi
          [ -f "$FILE" ] || { echo "업로드 파일이 없어 — 다시 올려줘." > /tmp/edit_err.txt; echo "::error::업로드 파일 없음: $FILE"; exit 1; }
          echo "EDIT_SRC=$FILE" >> "$GITHUB_ENV"

      - name: 소스 확보 (R2 직업로드 — 대용량 ≤2GB)
        if: ${{ inputs.r2_src != '' }}
        timeout-minutes: 8   # 2GB도 러너→R2 수십 MB/s면 수분 — 잡 캡 예산 압축(평의회6)
        env:
          R2_SRC: ${{ inputs.r2_src }}
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
        run: |
          set -e
          [ -z "${EDIT_SRC:-}" ] || { echo "이미 소스 확보 — 스킵(다중 입력 방어)"; exit 0; }
          printf '%s' "$R2_SRC" | grep -Eqx 'up_src/[0-9]{12}-[a-f0-9]{6}\.(mp4|mov|m4v|webm|mkv|avi)' \
            || { echo "::error::잘못된 r2_src: $R2_SRC"; exit 1; }   # api 정규식과 동형 자체 재검증(개행·탭·확장자·문자셋 — 상류 신뢰 대신 최후 방어선 · 평의회1)
          [ -n "$R2_BUCKET" ] || { echo "저장소(R2)가 설정 안 돼 있어 — 관리자 설정 후 다시." > /tmp/edit_err.txt; echo "::error::R2 시크릿 미설정"; exit 1; }
          EXT="${R2_SRC##*.}"
          export AWS_DEFAULT_REGION=auto AWS_REQUEST_CHECKSUM_CALCULATION=when_required AWS_RESPONSE_CHECKSUM_VALIDATION=when_required
          aws s3 cp "s3://$R2_BUCKET/$R2_SRC" "/tmp/upsrc.$EXT" --endpoint-url "https://${R2_ACCOUNT_ID}.r2.cloudflarestorage.com" --only-show-errors \
            || { echo "업로드 파일 회수 실패 — 다시 올려줘." > /tmp/edit_err.txt; echo "::error::R2 소스 회수 실패: $R2_SRC"; exit 1; }
          echo "EDIT_SRC=/tmp/upsrc.$EXT" >> "$GITHUB_ENV"

      - name: 길이 선게이트 (차등 캡 — 운영자 260711 "잘라서 10분 이하로" 확정. 트림 구간≤10분+전사 없음 = 원본 60분 · 자막/컷/클리퍼 = 원본 20분{STT가 원본 전체 전사 = 비용 원본 비례·Whisper large-v3 ~0.8−2×RT 추정(260726 turbo→승격) = 스텝 45분 내·초과 = 정직 실패} · 그 외 통짜 = 종전 10분)
        env:
          IN_ID: ${{ inputs.id }}
          OPTS: ${{ inputs.opts }}
        run: |
          set -e
          [ -n "${EDIT_SRC:-}" ] || { echo "영상 입력이 없어 — URL이나 파일을 넣어줘." > /tmp/edit_err.txt; echo "::error::EDIT_SRC 빈값"; exit 1; }
          DUR="$(ffprobe -v error -show_entries format=duration -of default=nw=1:nk=1 "$EDIT_SRC" 2>/dev/null | cut -d. -f1 || true)"
          case "$DUR" in ''|*[!0-9]*) DUR="";; esac   # ffprobe가 webm 등에서 내는 'N/A' 정규화(재검③ 260728 — 비숫자면 아래 float()가 터져 segs 합산까지 -1로 떨어진다)
          SPAN="$(DUR="$DUR" python3 -c "import json,os;o=json.loads(os.environ.get('OPTS') or '{}');d=float(os.environ.get('DUR') or 0);c=(lambda v: min(v,d) if d>0 else v);sg=o.get('vid_segs');t1=o.get('vid_t1');t0=float(o.get('vid_t0') or 0);print(int(sum(max(0.0,c(float(b))-float(a)) for a,b in sg)) if isinstance(sg,list) and sg else (int(c(float(t1))-t0) if t1 else (int(d-t0) if (o.get('vid_t0') and d>t0) else -1)))" 2>/dev/null || echo -1)"   # n구간 = 합산 길이(260728) · **끝값을 실길이로 클램프**(재검③ 260728 — 「끝 공란」이 3600 센티널로 오는데 SPAN만 클램프를 안 해 SPAN이 부풀고 CAP이 600으로 강등돼 **구간 카드를 이미 쓴 사용자를 되레 더 많이 거절**했다 · 러너 ly_burn `min(t1_req,dur)`와 동일 산식) · 「시작만 지정」도 실길이로 산출
          STT="$(python3 -c "import json,os;o=json.loads(os.environ.get('OPTS') or '{}');print(1 if (o.get('burn') or o.get('cut') or o.get('clip') or o.get('cutfill') or o.get('take') or o.get('cutscan')) else 0)" 2>/dev/null || echo 9)"   # 파싱 실패 = 9(미상) → 아래 분기 전부 미적중 = 종전 10분 보수 폴백
          if [ "$STT" = "1" ]; then CAP=1200; MSG="자막·컷·클리퍼는 20분 이하만(전사가 원본 전체를 돎) — 더 길면 구간만 자르거나 20분 이하로."
          elif [ "$SPAN" -ge 0 ] && [ "$SPAN" -le 600 ] 2>/dev/null; then CAP=3600; MSG="구간 편집도 원본 60분까지야 — 더 길면 잘라서 올려줘."
          else CAP=600; MSG="편집기는 10분 이하만(릴스/쇼츠용) — 긴 영상은 구간 카드로 잘라줘."
          fi
          if [ -n "$DUR" ] && [ "$DUR" -gt "$CAP" ] 2>/dev/null; then
            echo "영상 $((DUR/60))분 — ${MSG}" > /tmp/edit_err.txt; echo "::error::길이 초과 ${DUR}s (캡 ${CAP}s)"; exit 1
          fi

      - name: STT (자막·컷·클리퍼 — 오디오 추출 → Whisper)
        if: ${{ contains(inputs.opts, '"burn":true') || contains(inputs.opts, '"cut":true') || contains(inputs.opts, '"clip":true') || contains(inputs.opts, '"cutfill":true') || contains(inputs.opts, '"take":true') || contains(inputs.opts, '"cutscan":true') }}
        timeout-minutes: 45   # large-v3 승격(260726) — 원본 20분 캡 × ~2×RT 최악 + 여유. 무한 행만 차단
        env:
          # STT 엔진 = Scribe v2 우선 → 실패·키부재 시 large-v3 자동 폴백(ly_stt.py · 운영자 260808 승인)
          #   실측 = run 31230435185(한국어 19.6s): 28.1s→1.0s · 오인식 의심 11.5%→3.4% · 「검찰서가」→「경찰서 가서」 교정.
          #   킬스위치 = 레포 변수 LY_STT_ENGINE=whisper(즉시 종전 경로) · 미설정 = auto(키 있으면 Scribe).
          ELEVENLABS_API_KEY: ${{ secrets.ELEVENLABS_API_KEY }}
          LY_STT_ENGINE: ${{ vars.LY_STT_ENGINE }}
          IN_ID: ${{ inputs.id }}
          OPTS: ${{ inputs.opts }}
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
        run: |
          set -e
          case "$IN_ID" in *[!0-9a-f-]*) echo "::error::잘못된 id"; exit 1;; esac   # 문자셋 가드 = 형제 스텝 동형(260731 봉합 — 이 스텝과 아래 '자막 의역'만 빠져 대칭이 깨져 있었다: 수동 dispatch로 id="../../../tmp/x"를 주면 `mkdir -p viewer/ly_out/$IN_ID`가 트리 밖에 segments.json을 떨궜고, 커밋 스텝은 가드에 걸려 스킵되니 잡은 green인데 산출은 0이 됐다 · 평의회4 260731)
          HARD=0; case "$OPTS" in *'"burn":true'*) HARD=1;; esac; case "$OPTS" in *'"clip":true'*) HARD=1;; esac
          # 컷 단독(STT-only 260711) = 전사 실패해도 잡 살림(fail-soft) — segments.json 없으면 ly_burn이 '무음 컷 건너뜀' note.
          #   자막·클리퍼 ON = 정직 거절(전사가 산출 원료 — 없으면 잡 의미 없음 · clip은 api 배타 정규화라 cut과 혼합 불가).
          if ! ffmpeg -y -i "$EDIT_SRC" -vn -ar 16000 -ac 1 -b:a 48k /tmp/audio.mp3; then
            [ "$HARD" = 1 ] && { echo "오디오 추출 실패 — 소리 없는 영상이면 자막·클리퍼 카드를 빼고 다시." > /tmp/edit_err.txt; echo "::error::ffmpeg 오디오 추출 실패"; exit 1; }
            echo "::warning::오디오 추출 실패 — 컷 건너뜀(소리 없는 영상?)"; exit 0
          fi
          SEGJSON="viewer/ly_out/${IN_ID}/segments.json"
          mkdir -p "viewer/ly_out/${IN_ID}"
          # ── STT 캐시(운영자 260727 ②) — 키 = 추출 오디오 sha256(+모델+연산 정밀도 · 260728 정밀도 합류). 같은 소스 재렌더·후보 확정 렌더가
          #    large-v3를 통째로 다시 돌던 낭비(20분 = 16~40분) 제거. 전면 fail-soft: 캐시가 어떤 이유로든 안 되면 그냥 전사한다.
          CKEY=""; ENDP=""
          if [ -n "${R2_BUCKET:-}" ] && [ -n "${R2_ACCOUNT_ID:-}" ]; then
            export AWS_DEFAULT_REGION=auto AWS_REQUEST_CHECKSUM_CALCULATION=when_required AWS_RESPONSE_CHECKSUM_VALIDATION=when_required
            ENDP="https://${R2_ACCOUNT_ID}.r2.cloudflarestorage.com"
            CKEY="$(python3 .github/scripts/stt_cache.py key /tmp/audio.mp3 2>/dev/null || true)"
          fi
          if [ -n "$CKEY" ] && aws s3 cp "s3://$R2_BUCKET/$CKEY" "$SEGJSON" --endpoint-url "$ENDP" --only-show-errors 2>/dev/null; then
            if python3 .github/scripts/stt_cache.py rebuild "$SEGJSON" > /tmp/transcript.txt; then
              echo "STT 캐시 히트($CKEY) — 전사 건너뜀 · $(wc -l < /tmp/transcript.txt)줄"; exit 0
            fi
            echo "::warning::캐시 재구성 실패 — 정상 전사로 진행"; rm -f "$SEGJSON" /tmp/transcript.txt
          fi
          if ! python3 .github/scripts/ly_stt.py /tmp/audio.mp3 "$SEGJSON" > /tmp/transcript.txt; then
            [ "$HARD" = 1 ] && { echo "Whisper STT 실패 — 오디오 인식 불가. 자막·클리퍼 카드를 빼거나 다른 영상으로." > /tmp/edit_err.txt; echo "::error::Whisper STT 실패"; exit 1; }
            rm -f "$SEGJSON"; echo "::warning::STT 실패 — 컷 건너뜀(부분 산출 정리)"; exit 0
          fi
          echo "STT 완료 — $(wc -l < /tmp/transcript.txt)줄"
          if [ -n "$CKEY" ] && [ -s "$SEGJSON" ]; then
            aws s3 cp "$SEGJSON" "s3://$R2_BUCKET/$CKEY" --endpoint-url "$ENDP" --only-show-errors 2>/dev/null \
              && echo "STT 캐시 적재: $CKEY" || echo "::warning::STT 캐시 적재 실패(비치명)"
          fi

      - name: 자막 의역 (claude)
        if: ${{ contains(inputs.opts, '"burn":true') && !contains(inputs.opts, '"clip":true') }}   # clip 배타 러너 백스톱(검증⑤ — api 정규화와 이중 방어 · 수동 dispatch 혼합 opts 시 토큰 낭비 차단)
        timeout-minutes: 35
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets[format('CLAUDE_CODE_OAUTH_TOKEN_{0}', inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO')] }}
          CLAUDE_CODE_OAUTH_TOKEN_ALT: ${{ (inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO') == 'NOMUTEFB' && secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130G || secrets.CLAUDE_CODE_OAUTH_TOKEN_NOMUTEFB }}
          CLAUDE_CODE_OAUTH_TOKEN_ALT2: ${{ (inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO') == 'MUTENO' && secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130G || secrets.CLAUDE_CODE_OAUTH_TOKEN_MUTENO }}
          CLAUDE_CODE_OAUTH_TOKEN_ALT3: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_MUTENONA }}
          ACTIVE_ACCOUNT: ${{ inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO' }}
          IN_ID: ${{ inputs.id }}
          OPTS: ${{ inputs.opts }}
        run: |
          [ -n "$IN_ID" ] || { echo "::error::id 없음"; exit 1; }
          case "$IN_ID" in *[!0-9a-f-]*) echo "::error::잘못된 id"; exit 1;; esac   # 문자셋 가드 = 형제 스텝 동형(260731 봉합 — lymake.sh도 `mkdir -p viewer/ly_out/${ID}`를 하므로 STT 스텝과 같은 트리 밖 기록 통로였다 · 평의회4)
          [ -n "$CLAUDE_CODE_OAUTH_TOKEN" ] || { echo "::error::CLAUDE_CODE_OAUTH_TOKEN_<계정> 시크릿이 비어있음"; exit 1; }
          SUBS="$(cat /tmp/transcript.txt)"; export SUBS
          unset ANTHROPIC_API_KEY ANTHROPIC_AUTH_TOKEN
          bash .github/scripts/lymake.sh "$IN_ID"

      # ── 클리퍼 구간픽(claude · 260711 v0) — STT 전사 → 하이라이트 후보 3~5개 clips.json(후보 *제안*만 · 자동 발행 없음 —
      #    선택·렌더 = 뷰어 클리퍼 카드에서 운영자 확정 = track 분석→확정 미러). 의역(lymake)과 배타(clip = 배타 스캔 · api 정규화).
      - name: 클리퍼 구간픽 (claude)
        if: ${{ contains(inputs.opts, '"clip":true') }}
        timeout-minutes: 15
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets[format('CLAUDE_CODE_OAUTH_TOKEN_{0}', inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO')] }}
          CLAUDE_CODE_OAUTH_TOKEN_ALT: ${{ (inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO') == 'NOMUTEFB' && secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130G || secrets.CLAUDE_CODE_OAUTH_TOKEN_NOMUTEFB }}
          CLAUDE_CODE_OAUTH_TOKEN_ALT2: ${{ (inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO') == 'MUTENO' && secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130G || secrets.CLAUDE_CODE_OAUTH_TOKEN_MUTENO }}
          CLAUDE_CODE_OAUTH_TOKEN_ALT3: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_MUTENONA }}
          ACTIVE_ACCOUNT: ${{ inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO' }}
          IN_ID: ${{ inputs.id }}
          SRC_URL: ${{ inputs.url }}
          OPTS: ${{ inputs.opts }}
        run: |
          [ -n "$IN_ID" ] || { echo "::error::id 없음"; exit 1; }
          case "$IN_ID" in *[!0-9a-f-]*) echo "::error::잘못된 id"; exit 1;; esac   # 문자셋 가드 = 형제 스텝(컴포즈·커밋) 동형(검증⑧ 심층방어 대칭)
          [ -n "$CLAUDE_CODE_OAUTH_TOKEN" ] || { echo "::error::CLAUDE_CODE_OAUTH_TOKEN_<계정> 시크릿이 비어있음"; exit 1; }
          SUBS="$(cat /tmp/transcript.txt)"; export SUBS
          EDIT_DUR="$(ffprobe -v error -show_entries format=duration -of default=nw=1:nk=1 "$EDIT_SRC" 2>/dev/null | cut -d. -f1 || true)"; export EDIT_DUR
          # 클리퍼 감독 모델(운영자 260722 · 뷰어 clip_model fable/opus → 모델 ID) · 미지정 = clipmake.sh 기본(Fable 5)
          _cm="$(python3 -c "import json,os;o=json.loads(os.environ.get('OPTS') or '{}');m=o.get('clip_model');print(m if m in ('fable','opus') else '')" 2>/dev/null || true)"
          case "$_cm" in fable) export CLIP_MODEL=claude-fable-5;; opus) export CLIP_MODEL=claude-opus-5;; esac
          unset ANTHROPIC_API_KEY ANTHROPIC_AUTH_TOKEN
          bash .github/scripts/clipmake.sh "$IN_ID"

      # ── 테이크 감지(claude · 260727 ④) — 전사에서 '같은 말 다시 한 자리'를 찾아 버릴 구간 takes.json 제안.
      #    비치명: 실패해도 컷·자막 잡은 그대로 진행(테이크 컷만 빠짐 — 조용한 무력화가 아니라 note로 표면화됨).
      - name: 테이크 감지 (claude)
        if: ${{ contains(inputs.opts, '"take":true') && !contains(inputs.opts, '"clip":true') }}
        timeout-minutes: 15
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets[format('CLAUDE_CODE_OAUTH_TOKEN_{0}', inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO')] }}
          CLAUDE_CODE_OAUTH_TOKEN_ALT: ${{ (inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO') == 'NOMUTEFB' && secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130G || secrets.CLAUDE_CODE_OAUTH_TOKEN_NOMUTEFB }}
          CLAUDE_CODE_OAUTH_TOKEN_ALT2: ${{ (inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO') == 'MUTENO' && secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130G || secrets.CLAUDE_CODE_OAUTH_TOKEN_MUTENO }}
          CLAUDE_CODE_OAUTH_TOKEN_ALT3: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_MUTENONA }}
          ACTIVE_ACCOUNT: ${{ inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO' }}
          IN_ID: ${{ inputs.id }}
          OPTS: ${{ inputs.opts }}
        run: |
          [ -n "$IN_ID" ] || { echo "::warning::id 없음 — 테이크 감지 스킵"; exit 0; }
          case "$IN_ID" in *[!0-9a-f-]*) echo "::warning::잘못된 id — 테이크 감지 스킵"; exit 0;; esac   # 문자셋 가드 = 형제 스텝 동형
          [ -s /tmp/transcript.txt ] || { echo "::warning::전사 없음 — 테이크 감지 스킵"; exit 0; }
          [ -n "$CLAUDE_CODE_OAUTH_TOKEN" ] || { echo "::warning::OAuth 토큰 없음 — 테이크 감지 스킵"; exit 0; }
          SUBS="$(cat /tmp/transcript.txt)"; export SUBS
          EDIT_DUR="$(ffprobe -v error -show_entries format=duration -of default=nw=1:nk=1 "$EDIT_SRC" 2>/dev/null | cut -d. -f1 || true)"; export EDIT_DUR
          unset ANTHROPIC_API_KEY ANTHROPIC_AUTH_TOKEN
          bash .github/scripts/takemake.sh "$IN_ID" || echo "::warning::테이크 감지 실패 — 테이크 컷 없이 진행(비치명)"

      # ── 컷 미리보기 스캔(260727 ③) — 렌더 없이 '자를 목록'만 뽑는다(cuts.json). 컴포즈 스킵 = 클리퍼와 같은 2단 흐름.
      #    운영자가 뷰어에서 뺄 걸 빼고 확정하면 일반 렌더 잡이 opts{cutref,cutoff}로 그 승인본 그대로 자른다.
      - name: 컷 미리보기 스캔 (cuts.json)
        if: ${{ contains(inputs.opts, '"cutscan":true') && !contains(inputs.opts, '"clip":true') }}
        timeout-minutes: 10
        env:
          IN_ID: ${{ inputs.id }}
          OPTS: ${{ inputs.opts }}
          SRC_URL: ${{ inputs.url }}
        run: |
          set -e
          [ -n "$IN_ID" ] || { echo "::error::id 없음"; exit 1; }
          case "$IN_ID" in *[!0-9a-f-]*) echo "::error::잘못된 id"; exit 1;; esac
          python3 .github/scripts/cut_scan.py "$IN_ID" "${EDIT_SRC:-}"

      # ⚠ 이 스텝은 반드시 **클리퍼 구간픽·컷 미리보기 스캔 뒤**에 온다(260731 봉합) — 아래 파이썬이 clips.json/cuts.json을
      #   찾아서 그 옆에 원본을 보관하는데, 구 배치는 cuts.json을 만드는 스텝보다 **69줄 앞**이라 cutscan 경로에서
      #   항상 "보관 조건 미충족 … 스킵"으로 빠졌다(로그엔 정상 스킵처럼 찍혀 조용했다). 그 결과 cuts.json에 src가
      #   안 박혀 컷 확정 후 재렌더가 원본을 잃고 2GB 재업로드를 요구했다 = 사실상 기능 무력화(평의회4 260731).
      #   clip 경로는 구간픽(243행)이 앞이라 정상이었다 — cutscan만 구조적으로 100% 미스.
      # 클리퍼 원본 보관(R2 ≤400MB) — 파일/R2 업로드 소스는 잡 끝에 삭제(일회용)라, 후보 확정 후 *재렌더*가 소스를 잃음 →
      #   스캔 성공 시 원본을 ly_out/<id>/src.*로 보관하고 clips.json src에 URL 기입(URL 소스는 clipmake가 원 URL 승계 = 보관 불요).
      #   전면 fail-soft: 실패 = 후보 목록은 정상·재렌더만 파일 재선택(뷰어 안내).
      - name: 클리퍼·컷스캔 원본 보관 (R2)
        if: ${{ (contains(inputs.opts, '"clip":true') || contains(inputs.opts, '"cutscan":true')) && inputs.url == '' }}
        timeout-minutes: 8   # R2 PUT 스톨 백스톱(형제 R2 스텝 동형 · 검증⑤)
        env:
          IN_ID: ${{ inputs.id }}
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
          R2_PUBLIC_BASE: ${{ secrets.R2_PUBLIC_BASE }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
        run: |
          case "$IN_ID" in *[!0-9a-f-]*) echo "::error::잘못된 id"; exit 1;; esac   # 문자셋 가드 = 형제 스텝 동형(검증⑧)
          python3 - "$IN_ID" "$EDIT_SRC" <<'PY' || echo "::warning::클리퍼 원본 보관 실패(비치명 — 재렌더 시 파일 재선택)"
          import json
          import os
          import sys
          sys.path.insert(0, ".github/scripts")
          import thumb_gen as tg   # r2_upload·R2_ON 재사용(ly_burn 동형)
          vid, src = sys.argv[1], sys.argv[2]
          p = ""
          for cand in ("clips.json", "cuts.json"):   # 클리퍼 후보 · 컷 미리보기 = 둘 다 '확정 후 재렌더'가 원본을 필요로 함(260727 ③)
              q = os.path.join("viewer", "ly_out", vid, cand)
              if os.path.isfile(q):
                  p = q
                  break
          if not (tg.R2_ON and p and src and os.path.isfile(src)):
              print("보관 조건 미충족(R2 미설정/산출·소스 없음) — 스킵")
              raise SystemExit(0)
          if os.path.getsize(src) > 400 * 1024 * 1024:
              print("원본 400MB 초과 — 보관 생략(재렌더 = 파일 재선택)")
              raise SystemExit(0)
          ext = (os.path.splitext(src)[1] or ".mp4").lower()
          ctype = {"webm": "video/webm", "mov": "video/quicktime", "mkv": "video/x-matroska"}.get(ext.lstrip("."), "video/mp4")
          url = tg.r2_upload(open(src, "rb").read(), "ly_out/{}/src{}".format(vid, ext), ctype)
          if url:
              j = json.load(open(p, encoding="utf-8"))
              j["src"] = url
              tmp = p + ".tmp"
              with open(tmp, "w", encoding="utf-8") as f:
                  json.dump(j, f, ensure_ascii=False, separators=(",", ":"))
              os.replace(tmp, p)   # 원자 교체 = 레포 표준(truncate-쓰기 금지 · 검증⑤)
              print("원본 보관:", url)
          PY

      # ── 스캔 산출 R2 즉시 게시(운영자 260731 "클립·컷 스캔도 즉시") — video.json 즉시 경로(ly_burn 260728)의 스캔판.
      #    clips.json/cuts.json을 R2 ly_out/<id>/에 PUT → /api/edit?recent=(발견)·?stat&k=(열람)가 Pages 배포 랙 없이 서빙.
      #    ⚠ 반드시 위 '원본 보관' 스텝 **뒤**(src URL 기입이 끝난 최종본을 올린다 — 앞이면 src 없는 사본이 R2에 남아
      #    확정 재렌더가 원본을 잃는 평의회4 사고의 R2판 재발). 전면 fail-soft: 실패 = 종전 Pages 배포 경로 그대로(즉시성만 상실).
      - name: 스캔 산출 R2 즉시 게시 (clips/cuts.json)
        if: ${{ contains(inputs.opts, '"clip":true') || contains(inputs.opts, '"cutscan":true') }}
        timeout-minutes: 4   # JSON 소형 PUT — R2 스톨 백스톱(형제 스텝 동형)
        env:
          IN_ID: ${{ inputs.id }}
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
          R2_PUBLIC_BASE: ${{ secrets.R2_PUBLIC_BASE }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
        run: |
          case "$IN_ID" in *[!0-9a-f-]*) echo "::error::잘못된 id"; exit 1;; esac   # 문자셋 가드 = 형제 스텝 동형
          python3 - "$IN_ID" <<'PY' || echo "::warning::스캔 산출 R2 게시 실패(비치명 — Pages 배포 경로가 커버 · 즉시성만 상실)"
          import os
          import sys
          sys.path.insert(0, ".github/scripts")
          import thumb_gen as tg   # r2_upload·R2_ON 재사용(원본 보관 스텝 동형)
          vid = sys.argv[1]
          if not tg.R2_ON:
              print("R2 미설정 — 스킵")
              raise SystemExit(0)
          n = 0
          for cand in ("clips.json", "cuts.json"):
              p = os.path.join("viewer", "ly_out", vid, cand)
              if not os.path.isfile(p):
                  continue
              if tg.r2_upload(open(p, "rb").read(), "ly_out/{}/{}".format(vid, cand), "application/json"):
                  n += 1
                  print("R2 게시:", cand)
          print("스캔 산출 R2 게시 {}건".format(n))   # 집계 1줄(§관측 — 0건도 로그에 남긴다)
          PY

      - name: Demucs 캐시 (배경음 제거)
        if: ${{ contains(inputs.opts, '"bgm":true') }}
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/torch
            ~/.cache/pip
          key: nomute-ly-demucs-${{ runner.os }}-htdemucs-torch2121-demucs401-v1   # ly-make와 캐시 공유(동일 키)
      - name: Demucs 설치 (배경음 제거)
        if: ${{ contains(inputs.opts, '"bgm":true') }}
        timeout-minutes: 12
        run: |
          ok=0
          for i in 1 2; do
            if timeout 120 python3 -m pip install -q --upgrade setuptools wheel \
               && timeout 300 python3 -m pip install -q torch==2.12.1 torchaudio==2.11.0 torchcodec==0.14.0 --index-url https://download.pytorch.org/whl/cpu \
               && timeout 300 python3 -m pip install -q demucs==4.0.1 soundfile==0.14.0; then ok=1; break; fi
            sleep 5
          done
          [ "$ok" = 1 ] && python3 -c "import demucs.separate; print('demucs OK')" \
            || echo "::warning::Demucs 설치 실패 — 배경음 제거 스킵(원본 소리로 합성)"

      # ── 자동 가림·키잉·크로마키(운영자 260808 "모자이크 누르고 옵션 선택한 다음에 생성 누르면 트래킹해서 모자이크까지 자동으로")
      #   ⚠ 2단계로 갈린 이유(운영자 260809 "모자이크가 자막 위로 올라가버려서 자막이 가려져") —
      #     구판은 컴포즈 **뒤** 한 지점에서 전부 처리해서, 자막을 이미 구운 그림 위에 모자이크가 덮였다.
      #     ⓐ 픽셀에 굽는 계열(모자이크·핀셋) = 컴포즈 **앞**(pre) → ly_burn이 그 위에 자막을 얹는다 = 자막이 위
      #        · 좌표는 원본 기준으로 잡지만 가림이 픽셀에 이미 구워져 있어 뒤따르는 크롭·해상도 변경을 그대로 따라간다
      #     ⓑ 알파를 만드는 계열(키잉·실루엣·크로마키) = 컴포즈 **뒤**(post) — 알파 산출은 종점이라 자막 번인 뒤가 맞다
      #   전면 fail-soft = 자동 가림이 실패해도 편집 산출물은 그대로 살아 있다(스크립트 rc 항상 0).
      # 트래킹 캐시 = **track-make와 동일 키**(같은 레포 캐시 풀 공유 = 그쪽이 채운 걸 그대로 받고, 여기서 채운 것도 그쪽이 쓴다).
      #   ⚠ 없으면 편집 경로는 **매 실행** 모델·스택을 새로 받는다(운영자 260809 "지금 미리 설치해놓으면 안돼" — 캐시가 그 답).
      #   키의 h 접미 = 헤비/경량 분리(모자이크·크로마키가 1.6GB 헤비 캐시 복원 비용을 안 낸다 · track-make 관례 그대로).
      - name: 트래킹 python deps 캐시
        if: ${{ contains(inputs.opts, '"xtr"') && !contains(inputs.opts, '"clip":true') && !contains(inputs.opts, '"cutscan":true') }}
        uses: actions/cache@v4
        with:
          path: ${{ env.pythonLocation }}/lib/python3.12/site-packages
          key: nomute-track-pydeps-${{ runner.os }}-py312-${{ hashFiles('apps/track/setup.sh') }}-h${{ (contains(inputs.opts, '"keying":true') || contains(inputs.opts, '"silh":true')) && '1' || '' }}-v1

      - name: 트래킹 모델 캐시
        if: ${{ contains(inputs.opts, '"xtr"') && !contains(inputs.opts, '"clip":true') && !contains(inputs.opts, '"cutscan":true') }}
        uses: actions/cache@v4
        with:
          path: ~/.cache/nomute-track
          key: nomute-track-models-${{ hashFiles('apps/track/setup.sh') }}-h${{ (contains(inputs.opts, '"keying":true') || contains(inputs.opts, '"silh":true')) && '1' || '' }}-v1

      - name: 트래킹 환경 (자동 가림·키잉·크로마키)
        if: ${{ contains(inputs.opts, '"xtr"') && !contains(inputs.opts, '"clip":true') && !contains(inputs.opts, '"cutscan":true') }}
        timeout-minutes: 15   # 콜드런 pip+모델 다운 여유(이후 캐시히트 즉시) — track-make 동문
        continue-on-error: true   # 환경 설치 실패 = 자동 가림만 스킵(편집 잡은 살린다)
        uses: ./.github/actions/runner-setup
        with:
          setup: apps/track/setup.sh
          apt: ffmpeg fonts-noto-cjk   # 핀셋 이름표 한글 폰트(모자이크·크로마키는 불요지만 .deb 캐시 공유라 비용 0)
        env:
          TRACK_HEAVY: ${{ (contains(inputs.opts, '"keying":true') || contains(inputs.opts, '"silh":true')) && '1' || '' }}   # 키잉·실루엣만 torch+SAM2(모자이크·핀셋·크로마키 = 경량)

      - name: 키잉 스택 (torch+SAM2 · 키잉·실루엣만)
        if: ${{ (contains(inputs.opts, '"keying":true') || contains(inputs.opts, '"silh":true')) && !contains(inputs.opts, '"clip":true') && !contains(inputs.opts, '"cutscan":true') }}
        timeout-minutes: 20   # 콜드런 torch+ultralytics+SAM2(~1.6GB)
        continue-on-error: true   # 설치 실패 = 키잉만 스킵(편집 잡은 살린다)
        env:
          TRACK_MODE: render
          TRACK_HEAVY: '1'
        run: bash apps/track/setup.sh   # setup.sh 헤비 블록 정본 재사용(파일 무접촉 = track-make 회귀 0)
        # ⚠ 왜 별도 스텝인가(260809 실사고 = 키잉이 라이브에서 통째로 안 돌았다) —
        #   ⓐ 위 「트래킹 환경」은 `uses:`(composite)인데 **그 스텝의 env는 액션 내부 run 스텝에 전달되지 않는다**.
        #      track-make는 TRACK_MODE·TRACK_HEAVY를 **job level**에 둬서 상속시키는데, 여기선 스텝 level이라 setup.sh가 둘 다 못 봤다.
        #   ⓑ setup.sh는 `TRACK_MODE=render`가 **아니면 헤비 블록 자체를 안 탄다** → torch·SAM2가 어떤 경우에도 안 깔렸다.
        #   ⓒ 그렇다고 job level에 TRACK_MODE=render를 두면 이번엔 **분석 모델(YuNet·SFace)을 건너뛴다**(setup.sh render 분기가 즉시 exit).
        #      edit_track은 분석+렌더를 **둘 다** 하므로 그 조합이 필요한데 setup.sh엔 없다 → 경량(분석) 스텝 뒤에 헤비만 덧대는 이 순서가 답.

      - name: 자동 가림 pre (모자이크·핀셋 = 자막보다 먼저 굽는다)
        if: ${{ contains(inputs.opts, '"xtr"') && !contains(inputs.opts, '"clip":true') && !contains(inputs.opts, '"cutscan":true') }}
        timeout-minutes: 45   # 분석 300s캡 + 모자이크 최악 ~17분(track-make 실측) — 잡 105분 캡 전 우아한 실패 창
        continue-on-error: true   # 이중 안전벨트(네이티브 크래시·OOM도 편집본을 못 죽이게)
        env:
          IN_ID: ${{ inputs.id }}
          OPTS: ${{ inputs.opts }}
        run: |
          [ -n "$IN_ID" ] || exit 0
          case "$IN_ID" in *[!0-9a-f-]*) exit 0;; esac
          [ -s "${EDIT_SRC:-}" ] || { echo "::warning::원본 소스 없음 — pre 가림 스킵"; exit 0; }
          python3 .github/scripts/edit_track.py "$IN_ID" "$EDIT_SRC" pre
          # 산출이 나왔으면 컴포즈 입력을 그것으로 갈아끼운다 = ly_burn이 **가려진 그림 위에** 자막을 얹는다
          if [ -s /tmp/edit_track_pre.txt ]; then
            NEWSRC="$(cat /tmp/edit_track_pre.txt)"
            if [ -s "$NEWSRC" ]; then echo "EDIT_SRC=$NEWSRC" >> "$GITHUB_ENV"; echo "[xtr] 컴포즈 입력 교체 → $NEWSRC"; fi
          fi

      - name: 컴포즈 (ly_burn 단일 컴포지터 → R2 → video.json)
        if: ${{ !contains(inputs.opts, '"clip":true') && !contains(inputs.opts, '"cutscan":true') }}   # 클리퍼·컷 미리보기 = 스캔 전용(산출 = clips.json/cuts.json — 렌더는 확정 후 별도 잡)
        timeout-minutes: 60   # 내부 백스톱 최악 스택{probe 60 + Demucs 분리 780 + 인코딩 enc_to 2400(4K)~2250(60i) + 음량 270 + R2} ≈ 59분에 정렬(4K 경로 260711) — 잡 85분 캡 전 우아한 실패 기록 창 확보
        env:
          IN_ID: ${{ inputs.id }}
          OPTS: ${{ inputs.opts }}
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
          R2_PUBLIC_BASE: ${{ secrets.R2_PUBLIC_BASE }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
        run: |
          set -e
          [ -n "$IN_ID" ] || { echo "::error::id 없음"; exit 1; }
          case "$IN_ID" in *[!0-9a-f-]*) echo "::error::잘못된 id"; exit 1;; esac
          python3 .github/scripts/ly_burn.py "$IN_ID" "${EDIT_SRC:-}"

      - name: 자동 가림 post (키잉·실루엣·크로마키 = 알파 종점)
        if: ${{ contains(inputs.opts, '"xtr"') && !contains(inputs.opts, '"clip":true') && !contains(inputs.opts, '"cutscan":true') }}
        timeout-minutes: 45   # 키잉 ≈25분(track-make 실측)
        continue-on-error: true
        env:
          IN_ID: ${{ inputs.id }}
          OPTS: ${{ inputs.opts }}
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
          R2_PUBLIC_BASE: ${{ secrets.R2_PUBLIC_BASE }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
        run: |
          [ -n "$IN_ID" ] || exit 0
          case "$IN_ID" in *[!0-9a-f-]*) exit 0;; esac
          # 입력 = 컴포즈 최종본(ly_burn 도장 → 음량통일본 → 합성본) → 최후 폴백 = 컴포즈 입력(pre 산출 또는 원본)
          #   ⚠ 최후 폴백이 실효 조건 = 가림만 켜면 편집 축이 0이라 ly_burn이 합성물을 안 만든다(실측 260808).
          SRC_IN=""
          if [ -s /tmp/ly_final_path.txt ]; then SRC_IN="$(cat /tmp/ly_final_path.txt)"; fi
          if [ ! -s "$SRC_IN" ]; then for c in /tmp/ly_an.mp4 /tmp/ly_subbed.mp4; do [ -s "$c" ] && SRC_IN="$c" && break; done; fi
          if [ ! -s "$SRC_IN" ] && [ -s "${EDIT_SRC:-}" ]; then SRC_IN="$EDIT_SRC"; echo "[xtr] 컴포즈 산출 없음 → 직전 산출에 직접 적용"; fi
          [ -s "$SRC_IN" ] || { echo "::warning::영상 소스를 못 찾아 자동 가림 스킵"; exit 0; }
          python3 .github/scripts/edit_track.py "$IN_ID" "$SRC_IN" post

      - name: 실패 기록(error.log) — 프론트가 폴링해 즉시 표시
        if: failure()
        env:
          IN_ID: ${{ inputs.id }}
        run: |
          [ -n "$IN_ID" ] || exit 0
          case "$IN_ID" in *[!0-9a-f-]*) exit 0;; esac
          mkdir -p "viewer/ly_out/${IN_ID}"
          if [ -s /tmp/edit_err.txt ]; then cp /tmp/edit_err.txt "viewer/ly_out/${IN_ID}/error.log";
          elif [ -s /tmp/ly_err.txt ]; then cp /tmp/ly_err.txt "viewer/ly_out/${IN_ID}/error.log";
          elif [ ! -s "viewer/ly_out/${IN_ID}/error.log" ]; then echo "편집 실패 — Actions 로그를 확인해줘." > "viewer/ly_out/${IN_ID}/error.log"; fi
          echo "error.log: $(cat viewer/ly_out/${IN_ID}/error.log)"

      - name: Commit output
        if: always()
        env:
          IN_ID: ${{ inputs.id }}
          BRANCH: ${{ github.ref_name }}
        run: |
          [ -n "$IN_ID" ] || { echo "id 없음 — 커밋 스킵"; exit 0; }
          case "$IN_ID" in *[!0-9a-f-]*) echo "잘못된 id — 커밋 스킵"; exit 0;; esac
          git config user.name "nomute-bot"
          git config user.email "bot@nomute.local"
          git rm -r "uploads/${IN_ID}" 2>/dev/null || true
          git add "viewer/ly_out/${IN_ID}" 2>/dev/null || true
          git add metrics 2>/dev/null || true
          git commit -m "edit: ${IN_ID} 편집 출력" || { echo "커밋할 것 없음"; exit 0; }
          pushed=0
          for i in 1 2 3 4 5; do
            if git fetch origin "$BRANCH" && git rebase "origin/$BRANCH" && git push origin "HEAD:$BRANCH"; then pushed=1; break; fi
            git rebase --abort 2>/dev/null || true; sleep $((i*3))
          done
          [ "$pushed" = 1 ] || { echo "::error::push ${BRANCH} 5회 실패 — 출력 유실"; exit 1; }

      # 배포 게이트(deploy-then-notify · §완료알림 기틀 · thumb-make 정본 계승 · 운영자 260717 "알림을 무조건 퍼졌을때") —
      #   알림이 배포보다 먼저 도착해 '편집 화면만 열리고 영상 없음(video.json 미전파)'이 되던 것 차단: 산출 커밋(video.json/error.log)이
      #   포함된 Pages 빌드 완료를 확인한 *뒤에만* 완료 푸시로 진행. 신호 = 커밋의 'Cloudflare Pages' 체크런(이 레포는 Deployment 미생성·체크런만).
      #   산출 커밋 자체 또는 그걸 포함한 후속 main 커밋(빌드 병합 대비)의 success면 통과. 게이트는 절대 잡을 실패시키지 않음(모든 경로 exit 0 = 비치명).
      #   if: always() = 성공(video.json)·실패(error.log) 둘 다 전파 후 통지(실패 알림 탭도 사유 화면 착지 보장 · 편집기 딥링크는 error.log도 openJob이 표시).
      - name: 배포 게이트 (Pages 반영 확인)
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          SHA=$(git rev-parse HEAD)   # 산출 커밋(커밋 no-op 재실행이면 기존 HEAD = 이미 배포됐을 가능성 → 즉시 통과)
          DEADLINE=$((SECONDS+480)); SEEN=0   # 상한 8분(평시 배포 30~90s) · 체크런 자체가 안 보이면 180s 폴백(연동 변경 대비)
          chk() {   # $1=sha → 그 커밋의 'Cloudflare Pages' 체크런이 completed/success면 0
            local out; out=$(gh api "repos/${{ github.repository }}/commits/$1/check-runs?per_page=50" \
              --jq '[.check_runs[] | select(.name=="Cloudflare Pages")][0] | (.status//"")+"/"+(.conclusion//"")' 2>/dev/null || echo '')
            [ -n "$out" ] && [ "$out" != "/" ] && SEEN=1
            [ "$out" = "completed/success" ]
          }
          while [ $SECONDS -lt $DEADLINE ]; do
            chk "$SHA" && { echo "배포 확인 — 산출 커밋 빌드 success (${SECONDS}s)"; exit 0; }
            git fetch -q origin main 2>/dev/null || true
            H=$(git rev-parse origin/main 2>/dev/null || echo '')
            if [ -n "$H" ] && [ "$H" != "$SHA" ] && git merge-base --is-ancestor "$SHA" "$H" 2>/dev/null; then
              chk "$H" && { echo "배포 확인 — 후속 커밋($H) 빌드에 포함 success (${SECONDS}s)"; exit 0; }   # 산출 커밋 빌드가 후속 커밋에 병합·대체돼도 그 빌드가 우리 내용 포함
            fi
            [ $SECONDS -ge 180 ] && [ $SEEN -eq 0 ] && { echo "::warning::Cloudflare Pages 체크런 미관측(연동 변경?) — 180s 폴백 통과(뷰어 재시도 사다리가 커버)"; exit 0; }
            sleep 15
          done
          echo "::warning::배포 success 8분 미확인 — 폴백 통과(뷰어 재시도 사다리가 커버)"

      # 편집 완료/실패 푸시 — 편집기 잡은 길다(60i 보간 수십 분) → 창 닫아도 완성·실패를 구독자(프로필 알림 ON)에게 통지.
      #   thumb 제작완료 푸시 문법 계승(?done=<id> 딥링크 = Access 재로그인 생존 · 잡별 고유 tag = 알림 누적) + 실패도 통지(장시간 잡 = 부재 중 실패 표면화).
      #   비치명(|| true 계열) · 운영자 260711 "웹푸시까지 해줘".
      - name: 편집 완료 푸시 (pywebpush)
        if: always()
        env:
          IN_ID: ${{ inputs.id }}
          VAPID_PRIVATE_KEY: ${{ secrets.VAPID_PRIVATE_KEY }}
          VAPID_SUBJECT: ${{ secrets.VAPID_SUBJECT }}
        run: |
          [ -n "$IN_ID" ] || exit 0
          case "$IN_ID" in *[!0-9a-f-]*) exit 0;; esac
          python3 -m pip install --quiet --break-system-packages pywebpush || { echo "::warning::pywebpush 미설치 — 푸시 생략"; exit 0; }
          KJ="viewer/ly_out/${IN_ID}/cuts.json"
          if [ -s "$KJ" ]; then   # 컷 미리보기 스캔 = cuts.json이 산출(video.json 없음 · 260727 ③)
            N="$(python3 -c "import json;print(len(json.load(open('$KJ')).get('rm') or []))" 2>/dev/null || echo 0)"
            if [ "$N" -gt 0 ] 2>/dev/null; then BODY="자를 곳 ${N}군데 — 탭해서 확인"; else BODY="자를 곳 없음 — 그대로 편집해도 돼"; fi
            python3 .github/scripts/push_send.py --notify "영상" "$BODY" --url "/edit.html?done=${IN_ID}" --tag "nomute-make-${IN_ID}" || echo "::warning::완료 푸시 실패(비치명)"
            exit 0
          fi
          CJ="viewer/ly_out/${IN_ID}/clips.json"
          if [ -s "$CJ" ]; then   # 클리퍼 스캔 = clips.json이 산출(video.json 없음) — 실패면 clips.json 부재 → 아래 error.log 경로가 받음
            N="$(python3 -c "import json;print(len(json.load(open('$CJ')).get('clips') or []))" 2>/dev/null || echo 0)"
            if [ "$N" -gt 0 ] 2>/dev/null; then BODY="클립 후보 ${N}개 — 탭해서 고르기"; else BODY="클립 후보 없음 — 다른 영상으로"; fi
            python3 .github/scripts/push_send.py --notify "영상" "$BODY" --url "/edit.html?done=${IN_ID}" --tag "nomute-make-${IN_ID}" || echo "::warning::완료 푸시 실패(비치명)"
            exit 0
          fi
          VJ="viewer/ly_out/${IN_ID}/video.json"; EL="viewer/ly_out/${IN_ID}/error.log"
          OK=0; MSG=""
          if [ -s "$VJ" ]; then
            OK="$(python3 -c "import json;print(1 if json.load(open('$VJ')).get('url') else 0)" 2>/dev/null || echo 0)"
            [ "$OK" = "1" ] || MSG="$(python3 -c "import json;d=json.load(open('$VJ'));print((d.get('error') or d.get('skip') or '')[:60])" 2>/dev/null || true)"
          fi
          if [ "$OK" != "1" ] && [ -z "$MSG" ] && [ -s "$EL" ]; then MSG="$(head -c 60 "$EL")"; fi
          if [ "$OK" = "1" ]; then
            # sub(자막 실번인) + dur(길이초)를 한 번에 읽어 알림 본문 스펙 한 줄 구성(운영자 260717 "자막 삽입 포함 · 길이")
            META="$(python3 -c "import json;d=json.load(open('$VJ'));du=d.get('dur');print(('1' if d.get('sub') else '0')+' '+(str(int(round(du))) if isinstance(du,(int,float)) and du>0 else ''))" 2>/dev/null || echo '0 ')"
            SUB="${META%% *}"; DUR="${META#* }"; SP=""; [ -n "$DUR" ] && SP=" · ${DUR}초"
            if [ "$SUB" = "1" ]; then BODY="영상 완성 · 자막 삽입 완료${SP} — 탭해서 확인"; else BODY="영상 완성${SP} — 탭해서 확인"; fi
          else BODY="영상 실패 — ${MSG:-로그 확인}"; fi   # 성공("영상 완성")과 프레이밍 통일 = '영상 알람'(운영자 260717 "편집 알람이 아니라 영상 알람")
          python3 .github/scripts/push_send.py --notify "영상" "$BODY" --url "/edit.html?done=${IN_ID}" --tag "nomute-make-${IN_ID}" || echo "::warning::완료 푸시 실패(비치명)"

      - name: 업로드 브랜치 정리
        if: ${{ always() && inputs.up_branch != '' }}
        env:
          UP_BRANCH: ${{ inputs.up_branch }}
        run: |
          case "$UP_BRANCH" in up-*) ;; *) echo "up-* 아님 — 스킵"; exit 0;; esac
          git push origin --delete "$UP_BRANCH" || echo "브랜치 정리 실패(무해 — 수동 정리 대상): $UP_BRANCH"

      - name: 임시 업로드 청소 (R2 up_src · 나이 지난 것만)
        # (260810 운영자 "한번 올린거 바로 휘발시키지말고 냅둬봐") 종전 = 이 잡이 쓴 원본을 그 자리에서 `aws s3 rm` →
        #   러너는 원본을 **읽기만** 하는데도 같은 영상 두 번째 작업이 전체 재업로드였다(1GB면 1GB 다시).
        #   지금 = 남겨두고 **나이 든 것만** 청소(정본 = shared/r2_upsrc_gc.sh · TTL 기본 24h · 삭제 상한 200).
        #   만료 뒤 재발사는 api 쪽 head 검증이 "업로드 파일이 없어" 를 주고 뷰어가 자동 재업로드 = 종전 동작으로 폴백.
        if: ${{ always() }}
        env:
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
        run: |
          [ -f shared/r2_upsrc_gc.sh ] && bash shared/r2_upsrc_gc.sh || echo "청소기 없음(체크아웃 전 실패) — 스킵"