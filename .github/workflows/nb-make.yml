name: nb-make

# 뷰어 자료 탭(Pages Function /api/nb)이 발사 — 유튜브 자료화 v1(운영자 260712 · 평의회 11인 수렴):
#   유튜브 URL → 자막 우선(영상 다운로드 없이 자막만 = 경량) → 없으면 오디오만 받아 Whisper STT(ly 레일)
#   → claude 1콜(분석 노트만 — 전사는 nb_sub.py 산출 기계 삽입 = 패스스루) → viewer/nb_out/<id>/note.json.
#   골격 = song-make 미러(+ ly-make STT 레일 조건부). LLM = 영상당 1콜(구독 OAuth · 과금 0).
#   캡 = 자막 경로 4시간(NB_MAX_SEC) · STT 폴백 1시간(NB_STT_MAX_SEC) — 정직 거절(error.log).
on:
  workflow_dispatch:
    inputs:
      id:
        description: '작업 id(타임스탬프-hex6)'
        type: string
      url:
        description: '유튜브(영상) URL — yt-dlp 지원 사이트'
        type: string
      ask:
        description: '운영자 정리 지시(선택 — 관점·초점)'
        type: string
        default: ''
      mode:
        description: '소스 갈래 — url(유튜브·기본) / file(영상·음성 업로드 → Whisper large-v3) / text(사진 OCR·붙여넣기·기존 요약본 기사)'
        type: string
        default: 'url'
      text:
        description: 'text 모드 본문(OCR·붙여넣기·기사 본문 — 8000자 상한)'
        type: string
        default: ''
      title:
        description: 'text·file 모드 제목(파일명·기사 제목 — 노트 헤더)'
        type: string
        default: ''
      kind:
        description: 'text 모드 출처 갈래 — ocr / text / article(뷰어 신뢰도 배너 원천)'
        type: string
        default: 'text'
      file:
        description: 'file 모드 업로드 경로(uploads/<id>/src.<ext> · 20MB 이하 base64 경로)'
        type: string
        default: ''
      r2_src:
        description: 'file 모드 R2 직업로드 키(up_src/<id>.<ext> · 20MB 초과)'
        type: string
        default: ''
      account:
        description: '이 실행에 쓸 계정(구독 OAuth)'
        type: choice
        default: MUTENO
        options:
          - NOMUTEFB
          - MUTENO
          - EMS1130G

concurrency:
  group: nb-make-${{ inputs.id }}
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  nb:
    runs-on: ubuntu-latest
    timeout-minutes: 100   # 자막 경로 ≈3~6분 · STT 폴백(1h 오디오 whisper) 백스톱 · 파일 소스(회수 8 + 환경 15 + large-v3 50) 합산 여유(260801)
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          filter: blob:none   # 부분 클론(ly-make 260721 정본 계승) — 커밋 그래프는 전부(rebase·push 종전)·역사 블롭만 지연 회수
          # ⚡ 260808 sparse-checkout(ly-make 카나리아 run 31234731214 = Checkout 21s→6s · 삭제 0건 실증)
          #   ⚠ 전제 = 무경로 `git add -A`/`git add .` 0건(check_stt_engine_chain ⑥이 강제) · 신규 add 는 경로 지정.
          #   ⚠ uploads·metrics 는 cone 유지 — 업로드 정리·계측 shard 가 의존한다.
          sparse-checkout: |
            .github
            shared
            apps/ly
            apps/nb
            prompts
            assets/fonts
            viewer/nb_out
            metrics
            uploads
            .claude

      # claude CLI — song-make 미러(노트 생성이 무조건 claude 1콜 = 무조건 설치)
      - name: Setup Node (claude)
        uses: actions/setup-node@v5
        with:
          node-version: '24'

      - name: Install claude-code
        run: npm install -g @anthropic-ai/claude-code

      - name: Setup Deno (yt-dlp JS 챌린지 솔버)   # 운영자 260723: YouTube 서명/nsig 챌린지 = yt-dlp 순수파이썬 solver 실패("supported JavaScript runtime" 요구) → Deno JS 런타임 제공(EJS)으로 포맷 추출 복구 · 설치 실패해도 비치명(순수 파이썬 폴백)
        run: |
          curl -fsSL https://deno.land/install.sh | timeout 120 sh 2>/dev/null || echo "::warning::Deno 설치 실패 — 순수 파이썬 solver 폴백"
          echo "$HOME/.deno/bin" >> "$GITHUB_PATH"

      - name: 메타·자막 추출 (yt-dlp — 영상 다운로드 없음)
        if: ${{ inputs.mode == '' || inputs.mode == 'url' }}   # 5입구 확장(운영자 260801) — 파일·텍스트 소스는 유튜브 경로를 통째로 건너뜀
        timeout-minutes: 12
        env:
          URL: ${{ inputs.url }}
          IN_ID: ${{ inputs.id }}
          YT_COOKIES: ${{ secrets.YT_COOKIES }}
          YT_COOKIES_2: ${{ secrets.YT_T_COOKIES }}    # 다운로더 슬롯 1 — 구 YT_COOKIES가 죽었을 때 회수(260811 · 건강검진이 보는 건 이쪽뿐이라 구 쿠키는 죽어도 무경보였다)
          YT_COOKIES_3: ${{ secrets.YT_T2_COOKIES }}   # 다운로더 슬롯 2(미설정이면 빈값 = 건너뜀)
          NB_MAX_SEC: ${{ vars.NB_MAX_SEC || 14400 }}
          NB_STT_MAX_SEC: ${{ vars.NB_STT_MAX_SEC || 3600 }}
          NB_LIST_MAX: ${{ vars.NB_LIST_MAX || 200 }}
        run: |
          set -e
          [ -n "$IN_ID" ] || { echo "::error::id 없음"; exit 1; }
          case "$IN_ID" in *[!0-9a-f-]*) echo "::error::잘못된 id"; exit 1;; esac
          [ -n "$URL" ] || { echo "::error::url 없음"; exit 1; }
          mkdir -p "viewer/nb_out/${IN_ID}"
          timeout 240 pip3 install -q -U --pre "yt-dlp[default]" || { sleep 3; timeout 240 pip3 install -q -U --pre "yt-dlp[default]"; }   # 나이틀리+default extras = YouTube nsig/서명 챌린지 대응 최신(Deno와 함께 서명 해결 · 운영자 260723) · 네트워크 타임아웃(§인프라 b)
          COOKIES=""
          if [ -n "$YT_COOKIES" ]; then printf '%s\n' "$YT_COOKIES" > /tmp/ck.txt; COOKIES="--cookies /tmp/ck.txt"; fi
          ELOG="viewer/nb_out/${IN_ID}/error.log"
          # ── 자가치유 yt-dlp 래퍼(운영자 260723 · 260811 4단 확장) — 시도 사다리 정본 = .github/scripts/ytdlp_try.sh
          #    ①쿠키+기본 → ②쿠키+대체클라이언트(nsig 우회) → ③무쿠키+기본 → ④무쿠키+대체
          #    ⚠ ③④ 신설 사유(260811 실사고) = 이 URL이 **쿠키 없이는 정상 취득**되는데 죽은 쿠키를 붙여서 봇 검문에
          #      걸려 실패했다(실측). 종전 2단은 둘 다 쿠키를 붙여서 그 자리를 영영 못 빠져나갔다.
          #    ⚠ 원문 오류는 래퍼가 로그 그룹으로 찍는다(종전엔 분류 문구만 남아 오진을 검증할 길이 없었다 ·
          #      260723 판례: 서명 챌린지를 '봇 인증'으로 오진해 반나절 소요).
          ydl() {   # $1=출력파일 $2=라벨 나머지=yt-dlp 인자
            local out="$1" label="$2"; shift 2
            YTDLP_ERR=/tmp/nb_err.txt YTDLP_LABEL="$label" bash .github/scripts/ytdlp_try.sh "$@" > "$out" && return 0
            local e; e="$(tail -c 800 /tmp/nb_err.txt | tr '\n' ' ')"
            local msg
            case "$e" in
              # ⚠ 여기 오면 **쿠키를 뺀 시도까지 전부 실패**한 상태다 = 쿠키 재발급으로 안 풀린다(260811 실측 봉합 ·
              #    종전 문구는 무조건 "쿠키 갱신해줘"라 나흘간 같은 헛처방을 반복시켰다).
              *"Sign in to confirm"*|*"not a bot"*|*"consent"*) msg="유튜브가 이 영상에 로그인 확인을 요구해 — 쿠키를 빼고도 다시 시도해봤는데 같은 벽이야. 서버 IP가 잠깐 찍힌 상태일 수 있으니 몇 분 뒤 다시 해줘.";;
              *Signature*|*nsig*|*JavaScript*|*"format is not available"*|*"Only images"*) msg="yt-dlp가 유튜브 서명 잠금을 못 풀었어 — 유튜브가 또 바꾼 거라 나이틀리 갱신 대기 중일 수 있어. 몇 시간 뒤 다시 하면 대개 자동 복구돼.";;
              *429*|*"Too Many Requests"*) msg="유튜브 요청 과다(429) — 서버 IP가 잠깐 막혔어. 잠시 후 다시 해줘.";;
              *"Private video"*|*"members-only"*|*removed*|*unavailable*) msg="이 영상을 볼 수 없어 — 비공개·삭제·회원전용·지역제한이거나 링크가 잘못됐어.";;
              *) msg="유튜브 정보 조회 실패 — $(printf '%s' "$e" | head -c 140)";;
            esac
            printf '%s\n' "$msg" > "$ELOG"; echo "::error::${label} 실패(자가치유 재시도 후): $URL"; return 1
          }
          # ── 1차 flat 판정(운영자 260712 3차 "채널 넣으면 유튜브 다 긁어지게") — 채널/재생목록 = entries만 초고속 추출(영상 메타 전수 긁기·스크롤링 0)
          ydl /tmp/nb_flat.json "flat 판정" --flat-playlist --skip-download --dump-single-json "$URL" || exit 1
          NB_LIST_MAX="${NB_LIST_MAX:-200}" IN_ID="$IN_ID" python3 - <<'PY'
          import json, os
          from datetime import datetime, timedelta, timezone
          y = json.load(open("/tmp/nb_flat.json", encoding="utf-8"))
          if y.get("_type") != "playlist":
              raise SystemExit(0)   # 단건 영상 = 기존 노트 파이프 계속
          cap = int(os.environ.get("NB_LIST_MAX", "200"))
          items = []
          def walk(entries):   # 채널 루트는 {Videos/Shorts/Live} 하위 목록이 중첩 playlist로 옴 — 평탄화
              for e in entries or []:
                  if not isinstance(e, dict):
                      continue
                  if e.get("_type") == "playlist":
                      walk(e.get("entries"))
                      continue
                  vid = e.get("id") or ""
                  if not vid or any(x["id"] == vid for x in items):
                      continue
                  items.append({"id": vid, "url": e.get("url") if str(e.get("url", "")).startswith("http") else f"https://www.youtube.com/watch?v={vid}",
                                "t": (e.get("title") or "")[:200], "d": int(e.get("duration") or 0)})
                  if len(items) >= cap:
                      return
          walk(y.get("entries"))
          try:
              from zoneinfo import ZoneInfo
              ts = datetime.now(ZoneInfo("Asia/Seoul")).isoformat(timespec="seconds")
          except Exception:
              ts = datetime.now(timezone(timedelta(hours=9))).isoformat(timespec="seconds")
          doc = {"v": 1, "ts": ts, "kind": "list",
                 "src": {"url": y.get("webpage_url") or "", "title": (y.get("title") or "")[:200], "channel": (y.get("channel") or y.get("uploader") or "")[:100]},
                 "n": len(items), "items": items}
          d = f"viewer/nb_out/{os.environ['IN_ID']}"
          os.makedirs(d, exist_ok=True)
          tmp = d + "/list.json.tmp"
          json.dump(doc, open(tmp, "w", encoding="utf-8"), ensure_ascii=False, separators=(",", ":"))
          os.replace(tmp, d + "/list.json")   # 원자 교체 = 레포 표준
          open("/tmp/nb_islist.txt", "w").write("1")
          print(f"채널/목록 감지 — 영상 {len(items)}개 목록화(캡 {cap})")
          PY
          if [ -f /tmp/nb_islist.txt ]; then echo "NB_LIST=1" >> "$GITHUB_ENV"; echo "목록 모드 — 노트 생성 스킵(운영자가 목록서 골라 발사)"; exit 0; fi
          # ── 메타(제목·채널·길이·자막 유무) — 단건 영상(봇 인증 실패 = ly와 동일 안내)
          ydl /tmp/nb_yt.json "메타" --skip-download --no-playlist --dump-single-json "$URL" || exit 1
          python3 - <<'PY'
          import json, os, sys
          y = json.load(open("/tmp/nb_yt.json", encoding="utf-8"))
          up = y.get("upload_date") or ""
          meta = {
              "yt_id": y.get("id") or "", "url": y.get("webpage_url") or "",
              "title": (y.get("title") or "")[:300], "channel": (y.get("channel") or y.get("uploader") or "")[:100],
              "uploaded": f"{up[:4]}-{up[4:6]}-{up[6:8]}" if len(up) == 8 else "",
              "dur": int(y.get("duration") or 0), "lang": (y.get("language") or "")[:8],
          }
          if y.get("is_live"):
              open("/tmp/nb_gate.txt", "w").write("라이브 방송 중이야 — 방송 끝난 뒤(다시보기) 링크로 줘.")
          cap = int(os.environ.get("NB_MAX_SEC", "14400"))
          if meta["dur"] and meta["dur"] > cap:
              open("/tmp/nb_gate.txt", "w").write(f"영상 {meta['dur']//60}분 — {cap//60}분 이하만 지원해(자료화 상한).")
          man = set((y.get("subtitles") or {}).keys())
          auto = set((y.get("automatic_captions") or {}).keys())
          meta["has_subs"] = bool(man) or bool(auto)
          json.dump(meta, open("/tmp/nb_meta.json", "w", encoding="utf-8"), ensure_ascii=False)
          print("메타:", meta["title"][:60], "·", meta["dur"], "s · 수동자막", len(man), "· 자동자막", len(auto))
          PY
          if [ -s /tmp/nb_gate.txt ]; then cat /tmp/nb_gate.txt > "viewer/nb_out/${IN_ID}/error.log"; echo "::error::게이트: $(cat /tmp/nb_gate.txt)"; exit 1; fi
          # ── 자막 2패스(수동 → 자동 · 디렉터리 분리 = 출처 라벨 확정) — 영상 다운로드 없음
          #    언어 우선순위 = 원어(meta.lang) > ko > en — 다운로드·선택 둘 다 이 순서(외국어 영상 원어 자막 우선 · 자동번역 자막 회피)
          LANGP="$(python3 -c "import json;m=json.load(open('/tmp/nb_meta.json'));l=m.get('lang') or '';print((l+','+l.split('-')[0]+',' if l else '')+'ko,en')")"
          mkdir -p /tmp/nbsub_man /tmp/nbsub_auto
          YTDLP_ERR=/tmp/nb_err.txt YTDLP_LABEL="수동자막" bash .github/scripts/ytdlp_try.sh --skip-download --no-playlist \
            --write-subs --no-write-auto-subs --sub-langs "$LANGP" --sub-format "vtt/srt/best" \
            -o "/tmp/nbsub_man/x.%(ext)s" "$URL" || true
          python3 .github/scripts/nb_sub.py --vtt /tmp/nbsub_man "$LANGP" subs > /tmp/nb_tr.json || echo '{"src":"","rows":[]}' > /tmp/nb_tr.json
          ROWS="$(python3 -c "import json;print(len(json.load(open('/tmp/nb_tr.json')).get('rows') or []))")"
          if [ "$ROWS" -lt 5 ]; then
            YTDLP_ERR=/tmp/nb_err.txt YTDLP_LABEL="자동자막" bash .github/scripts/ytdlp_try.sh --skip-download --no-playlist \
              --write-auto-subs --no-write-subs --sub-langs "$LANGP" --sub-format "vtt/srt/best" \
              -o "/tmp/nbsub_auto/x.%(ext)s" "$URL" || true
            python3 .github/scripts/nb_sub.py --vtt /tmp/nbsub_auto "$LANGP" subs-auto > /tmp/nb_tr.json || echo '{"src":"","rows":[]}' > /tmp/nb_tr.json
            ROWS="$(python3 -c "import json;print(len(json.load(open('/tmp/nb_tr.json')).get('rows') or []))")"
          fi
          echo "자막 전사 ${ROWS}줄"
          if [ "$ROWS" -lt 5 ]; then
            DUR="$(python3 -c "import json;print(json.load(open('/tmp/nb_meta.json')).get('dur') or 0)")"
            if [ "$DUR" -gt "${NB_STT_MAX_SEC}" ] 2>/dev/null; then
              echo "자막이 없는 영상인데 ${DUR%%.*}초(길이)라 STT 상한(${NB_STT_MAX_SEC}s)을 넘어 — 자막 있는 영상이나 더 짧은 영상으로." > "viewer/nb_out/${IN_ID}/error.log"
              echo "::error::자막 없음 + STT 상한 초과"; exit 1
            fi
            echo "NB_NEED_STT=1" >> "$GITHUB_ENV"
            echo "자막 없음 → Whisper STT 폴백 예정"
          fi

      # ── 텍스트 소스(사진 OCR·붙여넣은 전문·기존 요약본 기사) = 전사 단계 없이 노트 파이프의 입력 2파일만 만들어 준다(운영자 260801) ──
      #    타임코드가 없는 소스라 rows.s = 전부 0(뷰어 [00:00] 표기) · tr_src = kind(ocr/text/article)가 신뢰도 배너 원천.
      - name: 텍스트 소스 수용 (OCR·붙여넣기·기사 참조)
        if: ${{ inputs.mode == 'text' }}
        env:
          IN_ID: ${{ inputs.id }}
          NB_TEXT: ${{ inputs.text }}
          NB_TITLE: ${{ inputs.title }}
          NB_KIND: ${{ inputs.kind }}
        run: |
          set -e
          [ -n "$IN_ID" ] || { echo "::error::id 없음"; exit 1; }
          case "$IN_ID" in *[!0-9a-f-]*) echo "::error::잘못된 id"; exit 1;; esac
          mkdir -p "viewer/nb_out/${IN_ID}"
          python3 - <<'PY'
          import json, os, re
          txt = (os.environ.get("NB_TEXT") or "").strip()
          if len(txt) < 40:
              open("viewer/nb_out/%s/error.log" % os.environ["IN_ID"], "w", encoding="utf-8").write("내용이 너무 짧아 — 더 긴 본문으로 다시 해줘.")
              raise SystemExit("텍스트 소스 부족")
          kind = os.environ.get("NB_KIND") or "text"
          if kind not in ("ocr", "text", "article"):
              kind = "text"
          title = (os.environ.get("NB_TITLE") or "").strip()[:200]
          if not title:   # 제목 미지정 = 첫 줄을 제목으로(노트 헤더 공백 방지)
              title = re.split(r"[\n。.!?]", txt.strip())[0][:80] or "붙여넣은 자료"
          ch = {"ocr": "사진 글자 인식(OCR)", "text": "붙여넣은 텍스트", "article": "기존 요약본 기사"}[kind]
          json.dump({"yt_id": "", "url": "", "title": title, "channel": ch, "uploaded": "", "dur": 0, "lang": ""},
                    open("/tmp/nb_meta.json", "w", encoding="utf-8"), ensure_ascii=False)
          # 문단 단위 rows — 전사 패스스루 계약(nbmake.sh)이 rows를 그대로 note.transcript로 싣는다
          paras = [p.strip() for p in re.split(r"\n{2,}", txt) if p.strip()] or [txt]
          rows = []
          for p in paras:
              for i in range(0, len(p), 1200):   # nbmake.sh의 t 캡(1200자)과 동수 = 절단 0
                  rows.append({"s": 0, "t": p[i:i + 1200]})
          json.dump({"src": kind, "lang": "", "rows": rows}, open("/tmp/nb_tr.json", "w", encoding="utf-8"), ensure_ascii=False)
          print(f"텍스트 소스 수용 — {kind} · {len(txt)}자 · {len(rows)}조각")
          PY

      # ── 파일 소스(영상·음성 업로드) 회수 — R2 직업로드(20MB 초과) 또는 uploads/<id>/src.<ext>(base64) ──
      - name: 파일 소스 회수 (업로드 → /tmp)
        if: ${{ inputs.mode == 'file' }}
        timeout-minutes: 8
        env:
          IN_ID: ${{ inputs.id }}
          NB_FILE: ${{ inputs.file }}
          R2_SRC: ${{ inputs.r2_src }}
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
        run: |
          set -e
          [ -n "$IN_ID" ] || { echo "::error::id 없음"; exit 1; }
          case "$IN_ID" in *[!0-9a-f-]*) echo "::error::잘못된 id"; exit 1;; esac
          mkdir -p "viewer/nb_out/${IN_ID}"
          ELOG="viewer/nb_out/${IN_ID}/error.log"
          if [ -n "$R2_SRC" ]; then
            printf '%s' "$R2_SRC" | grep -Eqx 'up_src/[0-9]{12}-[a-f0-9]{6}\.(mp4|mov|m4v|webm|mkv|avi|mp3|m4a|wav|ogg|aac|flac)' \
              || { echo "::error::잘못된 r2_src: $R2_SRC"; exit 1; }   # api 정규식 자체 재검증(상류 신뢰 대신 최후 방어선 · ly-make 계승)
            [ -n "$R2_BUCKET" ] || { echo "저장소(R2)가 설정 안 돼 있어 — 관리자 설정 후 다시." > "$ELOG"; echo "::error::R2 시크릿 미설정"; exit 1; }
            EXT="${R2_SRC##*.}"
            export AWS_DEFAULT_REGION=auto AWS_REQUEST_CHECKSUM_CALCULATION=when_required AWS_RESPONSE_CHECKSUM_VALIDATION=when_required
            aws s3 cp "s3://$R2_BUCKET/$R2_SRC" "/tmp/nbsrc.$EXT" --endpoint-url "https://${R2_ACCOUNT_ID}.r2.cloudflarestorage.com" --only-show-errors \
              || { echo "업로드 파일 회수 실패 — 다시 올려줘." > "$ELOG"; echo "::error::R2 소스 회수 실패: $R2_SRC"; exit 1; }
          else
            printf '%s' "$NB_FILE" | grep -Eqx 'uploads/[0-9a-f-]{12,32}/src\.(mp4|mov|m4v|webm|mkv|avi|mp3|m4a|wav|ogg|aac|flac)' \
              || { echo "::error::잘못된 file 경로: $NB_FILE"; exit 1; }
            [ -f "$NB_FILE" ] || { echo "업로드 파일을 못 찾았어 — 다시 올려줘." > "$ELOG"; echo "::error::업로드 파일 없음: $NB_FILE"; exit 1; }
            EXT="${NB_FILE##*.}"
            cp "$NB_FILE" "/tmp/nbsrc.$EXT"
          fi
          echo "NB_SRC=/tmp/nbsrc.$EXT" >> "$GITHUB_ENV"
          ls -la "/tmp/nbsrc.$EXT"

      # 자막 없는 영상만 — ffmpeg+faster-whisper+yt-dlp(apps/ly/setup.sh 단일출처) + 모델 캐시(ly 레일 그대로)
      - name: STT 환경 (자막 없음 폴백)
        if: ${{ env.NB_NEED_STT == '1' || inputs.mode == 'file' }}   # 파일 소스도 같은 환경(faster-whisper large-v3 + ffmpeg) 공유 = 스텝 신설 0
        timeout-minutes: 15
        uses: ./.github/actions/runner-setup
        with:
          setup: apps/ly/setup.sh
          # ⚡ 260808 — STT 기본 = Scribe v2(ly_stt.py) → large-v3는 **폴백 전용**이라 3.1GB 캐시·prefetch를 평소엔 안 붙인다.
          #   실측 = 이 스텝 48s(총 112s의 43%)가 Scribe 경로에선 한 번도 안 쓰이는 짐이었다(run 31231381993 분해).
          #   'true'로 되돌아가는 조건 = 킬스위치(vars.LY_STT_ENGINE=whisper) ∨ 키 부재 = **폴백이 유일 경로인 상황**.
          #   ⚠ ffmpeg·yt-dlp·faster_whisper(pip)는 그대로 설치된다 — 빠지는 건 모델 파일 3.1GB뿐(폴백 시 그 자리에서 회수).
          whisper: ${{ (vars.LY_STT_ENGINE == 'whisper' || secrets.ELEVENLABS_API_KEY == '') && 'true' || 'false' }}
          apt: ffmpeg

      - name: STT (오디오만 다운 → Whisper 전사)
        if: ${{ env.NB_NEED_STT == '1' }}
        timeout-minutes: 45
        env:
          # STT 엔진 = Scribe v2 우선 → 실패·키부재 시 large-v3 자동 폴백(ly_stt.py · 운영자 260808 승인)
          #   실측 = run 31230435185(한국어 19.6s): 28.1s→1.0s · 오인식 의심 11.5%→3.4% · 「검찰서가」→「경찰서 가서」 교정.
          #   킬스위치 = 레포 변수 LY_STT_ENGINE=whisper(즉시 종전 경로) · 미설정 = auto(키 있으면 Scribe).
          ELEVENLABS_API_KEY: ${{ secrets.ELEVENLABS_API_KEY }}
          LY_STT_ENGINE: ${{ vars.LY_STT_ENGINE }}
          URL: ${{ inputs.url }}
          IN_ID: ${{ inputs.id }}
          YT_COOKIES: ${{ secrets.YT_COOKIES }}
          YT_COOKIES_2: ${{ secrets.YT_T_COOKIES }}    # 다운로더 슬롯 1 — 구 YT_COOKIES가 죽었을 때 회수(260811 · 건강검진이 보는 건 이쪽뿐이라 구 쿠키는 죽어도 무경보였다)
          YT_COOKIES_3: ${{ secrets.YT_T2_COOKIES }}   # 다운로더 슬롯 2(미설정이면 빈값 = 건너뜀)
        run: |
          set -e
          # 시도 사다리 = .github/scripts/ytdlp_try.sh(①쿠키+기본 ②쿠키+대체 ③무쿠키+기본 ④무쿠키+대체 · 260811)
          YTDLP_ERR=/tmp/nb_err.txt YTDLP_LABEL="오디오" bash .github/scripts/ytdlp_try.sh \
            -x --audio-format mp3 --postprocessor-args "ffmpeg:-ar 16000 -ac 1 -b:a 48k" --no-playlist \
            -o "/tmp/audio.%(ext)s" "$URL" \
            || { echo "유튜브 오디오 다운로드 실패 — 쿠키를 빼고도 다시 시도했는데 안 됐어(유튜브 서명 잠금이거나 링크 문제). 자막 있는 영상으로 해줘." > "viewer/nb_out/${IN_ID}/error.log"; echo "::error::yt-dlp 오디오 실패(자가치유 4단 후): $URL"; exit 1; }
          python3 .github/scripts/ly_stt.py /tmp/audio.mp3 "" > /tmp/nb_stt.txt \
            || { echo "Whisper STT 실패 — 오디오 인식 불가. 자막 있는 영상으로 해줘." > "viewer/nb_out/${IN_ID}/error.log"; echo "::error::Whisper STT 실패"; exit 1; }
          python3 .github/scripts/nb_sub.py --stt /tmp/nb_stt.txt > /tmp/nb_tr.json
          echo "STT 전사 $(python3 -c "import json;print(len(json.load(open('/tmp/nb_tr.json')).get('rows') or []))")줄"

      # ── 파일 소스 전사 = ffmpeg 오디오 추출(16k mono) → faster-whisper large-v3(ly_stt.py 정본 그대로) ──
      - name: 파일 STT (ffmpeg 오디오 추출 → Whisper large-v3)
        if: ${{ inputs.mode == 'file' }}
        timeout-minutes: 50   # ly-make STT 백스톱 동값(large-v3 · 260728)
        env:
          # STT 엔진 = Scribe v2 우선 → 실패·키부재 시 large-v3 자동 폴백(ly_stt.py · 운영자 260808 승인)
          #   실측 = run 31230435185(한국어 19.6s): 28.1s→1.0s · 오인식 의심 11.5%→3.4% · 「검찰서가」→「경찰서 가서」 교정.
          #   킬스위치 = 레포 변수 LY_STT_ENGINE=whisper(즉시 종전 경로) · 미설정 = auto(키 있으면 Scribe).
          ELEVENLABS_API_KEY: ${{ secrets.ELEVENLABS_API_KEY }}
          LY_STT_ENGINE: ${{ vars.LY_STT_ENGINE }}
          IN_ID: ${{ inputs.id }}
          NB_TITLE: ${{ inputs.title }}
          NB_STT_MAX_SEC: ${{ vars.NB_STT_MAX_SEC || 3600 }}
        run: |
          set -e
          ELOG="viewer/nb_out/${IN_ID}/error.log"
          [ -n "$NB_SRC" ] && [ -f "$NB_SRC" ] || { echo "업로드 파일을 못 읽었어 — 다시 올려줘." > "$ELOG"; echo "::error::NB_SRC 없음"; exit 1; }
          DUR="$(ffprobe -v error -show_entries format=duration -of default=noprint_wrappers=1:nokey=1 "$NB_SRC" 2>/dev/null | cut -d. -f1)"
          case "$DUR" in ''|*[!0-9]*) DUR=0;; esac
          if [ "$DUR" -gt "$NB_STT_MAX_SEC" ] 2>/dev/null; then
            echo "파일이 $((DUR/60))분이라 전사 상한($((NB_STT_MAX_SEC/60))분)을 넘어 — 더 짧게 잘라서 올려줘." > "$ELOG"
            echo "::error::파일 길이 상한 초과"; exit 1
          fi
          ffmpeg -nostdin -v error -y -i "$NB_SRC" -vn -ar 16000 -ac 1 -b:a 48k /tmp/nb_audio.mp3 \
            || { echo "오디오를 추출하지 못했어 — 소리가 있는 영상·음성 파일로 다시 올려줘." > "$ELOG"; echo "::error::ffmpeg 오디오 추출 실패"; exit 1; }
          python3 .github/scripts/ly_stt.py /tmp/nb_audio.mp3 "" > /tmp/nb_stt.txt \
            || { echo "Whisper STT 실패 — 소리가 또렷한 파일로 다시 해줘." > "$ELOG"; echo "::error::Whisper STT 실패"; exit 1; }
          python3 .github/scripts/nb_sub.py --stt /tmp/nb_stt.txt > /tmp/nb_tr.json
          DUR="$DUR" python3 - <<'PY'
          import json, os
          title = (os.environ.get("NB_TITLE") or "").strip()[:200] or "업로드 파일"
          json.dump({"yt_id": "", "url": "", "title": title, "channel": "업로드 파일", "uploaded": "",
                     "dur": int(os.environ.get("DUR") or 0), "lang": ""},
                    open("/tmp/nb_meta.json", "w", encoding="utf-8"), ensure_ascii=False)
          tr = json.load(open("/tmp/nb_tr.json", encoding="utf-8"))
          tr["src"] = "stt-file"   # 뷰어 신뢰도 배너 = '업로드 파일 Whisper'(유튜브 stt와 구분)
          json.dump(tr, open("/tmp/nb_tr.json", "w", encoding="utf-8"), ensure_ascii=False)
          print(f"파일 전사 {len(tr.get('rows') or [])}줄 · {title}")
          PY
          rm -f /tmp/nb_audio.mp3 "$NB_SRC"

      - name: 자료 노트 생성 (claude 1콜 — 전사 패스스루)
        if: ${{ env.NB_LIST != '1' }}
        timeout-minutes: 25
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets[format('CLAUDE_CODE_OAUTH_TOKEN_{0}', inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO')] }}
          CLAUDE_CODE_OAUTH_TOKEN_ALT: ${{ (inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO') == 'NOMUTEFB' && secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130G || secrets.CLAUDE_CODE_OAUTH_TOKEN_NOMUTEFB }}
          CLAUDE_CODE_OAUTH_TOKEN_ALT2: ${{ (inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO') == 'MUTENO' && secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130G || secrets.CLAUDE_CODE_OAUTH_TOKEN_MUTENO }}
          CLAUDE_CODE_OAUTH_TOKEN_ALT3: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_MUTENONA }}
          ACTIVE_ACCOUNT: ${{ inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO' }}
          IN_ID: ${{ inputs.id }}
          NB_ASK: ${{ inputs.ask }}
        run: |
          [ -n "$IN_ID" ] || { echo "::error::id 없음"; exit 1; }
          case "$IN_ID" in *[!0-9a-f-]*) echo "::error::잘못된 id"; exit 1;; esac
          [ -n "$CLAUDE_CODE_OAUTH_TOKEN" ] || { echo "::error::CLAUDE_CODE_OAUTH_TOKEN_<계정> 시크릿이 비어있음"; exit 1; }
          unset ANTHROPIC_API_KEY ANTHROPIC_AUTH_TOKEN
          export NB_GV="$(sha256sum prompts/nb-make.md | cut -c1-12)"   # 지침 도장(드리프트 추적 — §📰 정신)
          bash .github/scripts/nbmake.sh "$IN_ID"

      - name: 실패 기록(error.log) — 프론트가 폴링해 즉시 표시
        if: failure()
        env:
          IN_ID: ${{ inputs.id }}
        run: |
          [ -n "$IN_ID" ] || exit 0
          case "$IN_ID" in *[!0-9a-f-]*) exit 0;; esac
          mkdir -p "viewer/nb_out/${IN_ID}"
          [ -s "viewer/nb_out/${IN_ID}/error.log" ] || echo "자료화 실패 — Actions 로그를 확인해줘." > "viewer/nb_out/${IN_ID}/error.log"
          echo "error.log: $(cat viewer/nb_out/${IN_ID}/error.log)"

      - name: Commit output
        if: always()
        env:
          IN_ID: ${{ inputs.id }}
          BRANCH: ${{ github.ref_name }}
        run: |
          [ -n "$IN_ID" ] || { echo "id 없음 — 커밋 스킵"; exit 0; }
          case "$IN_ID" in *[!0-9a-f-]*) echo "잘못된 id — 커밋 스킵"; exit 0;; esac
          git config user.name "nomute-bot"
          git config user.email "bot@nomute.local"
          git add "viewer/nb_out/${IN_ID}" 2>/dev/null || true
          git add metrics 2>/dev/null || true
          git rm -r -q --ignore-unmatch "uploads/${IN_ID}" 2>/dev/null || true   # 파일 소스 원본 = 일회용(전사 끝 = 즉시 회수 · ly up-브랜치 수명 규칙과 동축 · 260801)
          git commit -m "nb: ${IN_ID} 자료 노트" || { echo "커밋할 것 없음"; exit 0; }
          pushed=0
          for i in 1 2 3 4 5; do
            if git fetch origin "$BRANCH" && git rebase "origin/$BRANCH" && git push origin "HEAD:$BRANCH"; then pushed=1; break; fi
            git rebase --abort 2>/dev/null || true; sleep $((i*3))
          done
          [ "$pushed" = 1 ] || { echo "::error::push ${BRANCH} 5회 실패 — 출력 유실"; exit 1; }
