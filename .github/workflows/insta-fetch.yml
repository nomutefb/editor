# 인스타 직결 수집 — Meta Instagram API(Instagram Login) · LLM 0콜 · 시크릿 미등록 = no-op 스캐폴드
# 정본 = apps/insta/00_지침_컨설턴트_인스타_v1.md §1-2 · 세팅/토큰 재발급 = docs/인스타_직결_세팅.md
# 롤백 = schedule 블록 제거(수동 전용) 또는 파일 삭제 · 수집분(apps/insta/data/)은 잔존.
name: insta-fetch
on:
  schedule:
    - cron: '25 */3 * * *'   # AI 브리프 캐던스 = 3시간마다(:25 오프셋 · UTC 0/3/…/21시 → KST 09/12/…/06시 :25) — 운영자 260718 "채널 요약 AI요약 3시간마다 갱신". ⚠️ GitHub schedule = best-effort라 이것 단독이면 드롭됨(실측 260720: 09:25 KST 슬롯 통째 드롭 07:11→13:02 KST 갭 6h·17:41 KST run 실패 = 채널요약 종일 공백 근본원인). 데이터 숫자 신선도는 아래 push 하트비트가 담당 · 브리프는 여기(schedule) + 다이제스트 동일 시 스킵(토큰 0)으로 3h 유지.
  push:
    branches: [main]
    paths:
      - 'viewer/sns_subs_phone.json'   # 폰(muteno-phone·실기기 PAT) 구독수집 커밋 = 실측 ~30분 규칙 하트비트 편승 — 실기기 cron이라 GitHub schedule 드롭과 무관 → 채널요약(팔로워·조회수) 수집이 편승(지연·수동발사 근본해소 260720 · sns-trends/social-scan 동형 이식). 폰 전용 파일만 감시 = 자기 산출(insta_data/fb_data/chan_brief) 재트리거 0(무한루프 없음). ⚠️ push(하트비트) 런은 데이터 수집만 = 브리프(LLM) OFF(아래 브리프 스텝 github.event_name != 'push' 게이트) → 숫자는 ~30분 신선, 비싼 AI 브리프는 3h schedule 캐던스 그대로 보존(운영자 260718 토큰 절약 의도 불변).
  workflow_dispatch:
    inputs:
      backfill:
        description: '전 게시물 백필(페이지네이션+인사이트 확장 · 약 20~40콜)'
        type: boolean
        default: false
      children:
        description: '캐러셀 낱장 백필(카드뉴스 뒷장 URL 수집 · 약 20콜)'
        type: boolean
        default: false
      brief:
        description: '채널 AI 브리프(chan_brief.sh · 1=생성) — §📰-e 카나리아: 기본 OFF 머지 → dispatch 실측 → 승격'
        type: string
        default: '0'
      follow_backfill:
        description: '팔로우/취소 일별 소급 일수(0=평시 · 180=반년 회수 · 운영자 260811 — 하루창 1콜×일수)'
        type: string
        default: '0'
permissions:
  contents: write
concurrency:
  group: insta-fetch
  cancel-in-progress: false
env:
  PAGES_COALESCE: ${{ vars.PAGES_COALESCE }}   # [CF-Pages-Skip] 코얼레싱 킬스위치(0=off · 부재=ON) — 정본 = git_land.sh 헤더 · Q1331
jobs:
  fetch:
    runs-on: ubuntu-latest
    timeout-minutes: 50   # 20→35(260717)→50(260724 평의회): 브리프 2개 순차(chan_brief+fb_brief · 각 시도당 900s+풀재시도 최악 ~31분) 수용 = 최악 합 ~62분 근사에 헤드룸(평시 각 ~9분=18분 무영향). 근본 보호 = chan_brief 직후 중간 커밋(fb_brief 하드킬해도 IG 브리프 生존) · 브리프 정상 실측 ~9분 · OFF 시 종전 소요
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      # ⚠ continue-on-error = 인스타 열쇠 하나가 죽었을 때 **이 잡 전체가 같이 죽던 것**의 봉합(260816 실사고).
      #   실측 = IG_ACCESS_TOKEN 거절(190/Cannot parse) → 이 스텝 rc=1 → 뒤 스텝이 통째로 미실행 →
      #   페이스북 페이지 수집·FB 주제 분류·**FB 채널 브리프**·알고리즘 회차 원장까지 같이 정지했다
      #   (인스타와 무관한 축인데 같은 잡에 얹혀 있다는 이유만으로 죽었다 = 곁다리 사망).
      #   `|| true` 가 아니라 continue-on-error 인 이유 = 스텝은 **빨간 X로 남겨야** 한다(초록 위장 금지 ·
      #   실패는 실패라고 쓴다). 아래 인스타 전용 스텝들은 steps.igfetch.outcome 으로 자기 축만 건너뛴다.
      - name: 수집(시크릿 게이트 · stdlib only = 설치 0)
        id: igfetch
        continue-on-error: true
        env:
          IG_ACCESS_TOKEN: ${{ secrets.IG_ACCESS_TOKEN }}
          IG_USER_ID: ${{ vars.IG_USER_ID }}
          IG_FOLLOW_BACKFILL: ${{ github.event_name == 'workflow_dispatch' && inputs.follow_backfill || '0' }}   # 평시 0 = 결측일만 소수 채움 · 수동 발사 시만 소급(하루창 1콜×일수 · 실측 보관 경계 최소 180일)
        run: python3 .github/scripts/insta_fetch.py
      # ── 노뮤트 **본계정** 스레드 지표(운영자 260816 "ig랑 분리하고 싶지는 않고, thread 구독자도 ig아래에 표기") ──
      # 같은 회차에 얹는 이유 = 운영자가 두 채널을 **한 채널로** 본다. 별도 워크플로로 떼면 수집 시각이 갈려
      #   화면에 「인스타 17:03 · 스레드 14:20」처럼 서로 다른 시점 숫자가 나란히 뜬다(= 비교가 거짓말이 된다).
      # ⚠ 열쇠 = TH_ACCESS_TOKEN **단독**. 루시 THREADS_ACCESS_TOKEN 을 폴백으로 넣으면 남의 계정 팔로워가
      #   노뮤트 화면에 뜬다(세 열쇠가 전부 다른 계정 = 260816 실측). 폴백 금지가 계약이다.
      # fail-soft = 스레드는 참고 축이라 실패해도 인스타 회차를 죽이지 않는다(산출은 다음 회차에 따라잡는다).
      # 산출 apps/insta/data/threads_own.json = 아래 커밋 스텝이 `apps/insta/data` 통째로 실어 나른다(인자 추가 불요).
      - name: 스레드 본계정 지표(참고 축 · fail-soft · 시크릿 미등록 = no-op)
        env:
          TH_ACCESS_TOKEN: ${{ secrets.TH_ACCESS_TOKEN }}
        run: python3 .github/scripts/threads_own.py || echo "::warning::스레드 본계정 지표 실패 — 인스타 회차는 계속(다음 회차 재시도)"
      # ── 열쇠 사망 알림 착지(260816 실사고 봉합 · insta_fetch.token_alert 의 짝 = 선택 아님) ──
      # 왜 별도 스텝인가 = 위 수집 스텝이 exit 1 이면 잡이 그 자리에서 멈춰 **뒤 커밋 스텝이 통째로 안 돈다**.
      #   그래서 종전엔 열쇠가 죽어도 알림 파일이 러너에서 같이 증발했고 운영자에게 가는 통지가
      #   GitHub 실패 메일 하나뿐이었다(앱 알림함 무점등 = 260816 실측 = 스크린샷으로만 발견).
      #   if: failure() = 이 한 스텝만 실패 뒤에도 돌아 알림을 main 에 착지시킨다. 잡 결론은 실패 그대로다
      #   (초록으로 덮지 않는다 = 무증상화 금지).
      # 스코프 = messages 단독 = 수집 산출물 무접촉(반쪽 데이터 착지 0).
      # ⚠ [CF-Pages-Skip] 접두를 붙이지 않는다 = 알림은 「조기 반영」 축(금지 목록)이다. 붙이면 다음 비스킵
      #   빌드까지 최대 17분 화면에 안 떠서, 열쇠가 죽은 걸 알리는 알림 자신이 늦게 뜨는 자기모순이 된다.
      # ⚠ 조건에 steps.igfetch 축을 **같이** 둔다(260816 2차 = 곁다리 사망 차단과의 통합).
      #   위 수집 스텝이 continue-on-error 가 되면서 그 실패는 잡 상태를 실패로 만들지 않는다 →
      #   `failure()` 단독이면 이 알림 착지가 **조용히 안 돈다**(먼저 착지한 봉합이 그대로 죽는 자리).
      #   두 축 OR = 수집 실패든 뒤 스텝 실패든 알림은 반드시 착지한다. 잡 결론이 실패로 남는 건
      #   맨끝 「수집 실패 최종 표시」 스텝이 책임진다(초록 위장 금지 계약은 그대로 유지).
      - name: 실패 시 알림만 착지(if failure · 앱 알림함 점등)
        if: ${{ failure() || steps.igfetch.outcome == 'failure' }}
        run: |
          bash .github/scripts/git_land.sh "insta: 접속 열쇠 사망 알림 (bot)" messages || true
      - name: 전 게시물 백필(수동 발사 전용 · 운영자 260713 "기존꺼 파악")
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.backfill }}
        env:
          IG_ACCESS_TOKEN: ${{ secrets.IG_ACCESS_TOKEN }}
          IG_USER_ID: ${{ vars.IG_USER_ID }}
        run: python3 .github/scripts/insta_backfill.py
      - name: 캐러셀 낱장 백필(수동 발사 전용 · 운영자 260811 "낱장 조회하고 기존거 더 꺼내자" — 카드뉴스 뒷장은 이 축뿐 = 공개 커버·임베드는 표지만 준다)
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.children }}
        env:
          IG_ACCESS_TOKEN: ${{ secrets.IG_ACCESS_TOKEN }}
          IG_USER_ID: ${{ vars.IG_USER_ID }}
        run: python3 .github/scripts/insta_children.py
      - name: 페이스북 페이지 수집(채널 요약 1-2 · 시크릿 게이트 = 미등록 no-op · 운영자 260718 Q155 — 세팅 = docs/페이스북_직결_세팅.md)
        env:
          FB_PAGE_TOKEN: ${{ secrets.FB_PAGE_TOKEN }}
          FB_PAGE_ID: ${{ vars.FB_PAGE_ID || '809122025627220' }}   # Nomute 페이지 id 기본값(운영자 260724 · 시스템유저 토큰 자기페이지 자동발견 불가 → 명시 · fb-fetch.yml과 동일)
          IG_ACCESS_TOKEN: ${{ secrets.IG_ACCESS_TOKEN }}   # 겸용 프로브(운영자 260718 · fb-fetch.yml 동조 — FB 토큰 부재 시 IG 토큰 페이지 자동 연동 시도)
        run: python3 .github/scripts/fb_fetch.py || true
      # ⚠ 수집 실패면 이 스텝을 **건너뛰는 게 계약**이다(260816). 이유 = 이 스크립트가 찍는 generated_kst 가
      #   화면 좌상단 「수집 시각」이라, 수집이 실패한 회차에 신호만 다시 계산하면 내용은 어제 것 그대로인데
      #   시각만 방금으로 갱신돼 **화면이 「방금 받아왔다」고 거짓말**을 한다(옛 숫자 + 새 시각 = 가장 조용한 사고).
      #   건너뛰면 시각도 옛것 그대로 남아 「멈춰 있다」가 화면에 정직하게 보인다.
      - name: 신호 계산(뷰어 소비본 포함 · fail-soft)
        if: ${{ steps.igfetch.outcome == 'success' }}
        run: python3 apps/insta/insta_signals.py || true
      # ── 수집·신호분 즉시 커밋(1차) — 뒤 채널 브리프(chan_brief.sh · LLM 1콜 timeout 600s) 스텝이 job timeout/하드킬로 죽어도 insta_data·신호(메뉴4 데이터)는 이미 라이브 확보(운영자 260714 "데이터 잘 받아오게" 인스타 점검 = sns-trends.yml 1차 커밋 미러). 근본원인 = 맨끝 단일 커밋이라 LLM 단계 사망 시 하루치 수집분 동반 유실 · insta는 cron 1일 1회라 유실 = 종일 공백(트렌드보다 타격 큼 · Q11 벽시계 캡은 부분 봉합이었음). insta-fetch는 push 트리거 없음(cron만) = 재트리거 0. 브리프분(chan_brief.json)은 맨끝 커밋이 2차로 마저 커밋.
      - name: 커밋 수집분(1차 · 유실 방지)
        # git_land = 최신 main 재기점 재적층(구 `pull --rebase -X ours`+4회 재시도의 리베이스 꼬임·경쟁 소진 → 산출물 no-op 유실 봉합 · 260716 브리프 3일 정지 사고 근본교정). insta 산출물 단독 소유라 재적층 안전.
        run: |
          bash .github/scripts/git_land.sh "insta: 계정 인사이트 스냅샷 (bot)" apps/insta/data viewer/insta_data.json viewer/insta_covers viewer/fb_data.json messages
      - name: Install Claude Code CLI (채널 브리프 스텝용 — LLM 0콜 수집 레인이라 브리프 게이트와 동기 · sns-trends.yml 관용구)
        if: ${{ github.event_name != 'push' && (inputs.brief || '1') == '1' }}   # 기본 ON 승격(§📰-e 이행 — 카나리아 run 29263671640 실측 260714: rc=0·브리프 1,355자 생성) · push(하트비트) 런 = 브리프 install 스킵(데이터 수집만 · 3h schedule 캐던스 보존 260720) · 롤백 = || '0'
        run: npm install -g @anthropic-ai/claude-code
      # ── 표지 제목 판독(운영자 260812 "오버레이가 가장 정확한 내용이거든") ──
      # 요약이 «…» 로 게시물을 지목할 때 쓰던 이름 = 글 첫 줄인데, 노뮤트는 카드 제목과 글 첫 줄을 각각 따로
      # 쓴다(실측 = 「🚨 엿새 만에 다시 쐈다…」 ↔ 카드 「북한, 엿새 만에 '또 쐈다'」) → 표지에 박힌 제목을 읽어
      # 원장에 굽고, 신호를 다시 계산해 그 이름을 insta_data.json 에 싣는다(브리프가 그 파일을 읽는다 = 순서 계약).
      # 비용 = 읽을 바이트가 이미 우리 것(viewer/insta_covers) + 미디어 id별 1회만 = 평시 1콜 · 종량제 0(구독 축).
      # 재계산이 필요한 이유 = 커버를 굽는 것도 이름을 붙이는 것도 insta_signals 라 판독 전 산출엔 이름이 못 실린다.
      - name: 표지 제목 판독 + 신호 재계산(채널 브리프 입력 · fail-soft = 실패해도 종전 이름 그대로)
        # steps.igfetch 조건 = 신호 계산 스텝과 같은 이유(거짓 「수집 시각」 차단 · 260816) — 이 스텝도 끝에서
        # insta_signals 를 다시 돌려 generated_kst 를 찍는다. 수집이 죽은 회차엔 판독할 새 커버도 없다.
        if: ${{ github.event_name != 'push' && (inputs.brief || '1') == '1' && steps.igfetch.outcome == 'success' }}   # claude CLI 설치 스텝과 동일 게이트(브리프 레인 편승) · push 하트비트 = 스킵(원장 적중분은 그대로 붙는다)
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets[format('CLAUDE_CODE_OAUTH_TOKEN_{0}', vars.ACTIVE_ACCOUNT || 'MUTENO')] }}
          CLAUDE_CODE_OAUTH_TOKEN_ALT: ${{ (vars.ACTIVE_ACCOUNT || 'MUTENO') == 'NOMUTEFB' && secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130G || secrets.CLAUDE_CODE_OAUTH_TOKEN_NOMUTEFB }}
          CLAUDE_CODE_OAUTH_TOKEN_ALT2: ${{ (vars.ACTIVE_ACCOUNT || 'MUTENO') == 'MUTENO' && secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130G || secrets.CLAUDE_CODE_OAUTH_TOKEN_MUTENO }}
          CLAUDE_CODE_OAUTH_TOKEN_ALT3: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130M }}
        run: |
          python3 .github/scripts/insta_cover_ocr.py || true
          python3 apps/insta/insta_signals.py || true
      - name: 채널 AI 브리프(메뉴4 · 성장 서사+전략 · 운영자 260714) — 입력 동일 = 스킵(토큰 0) · 실패 = 직전 유지(fail-soft) · SNS 트렌드 브리프와 별개 축
        env:
          # 수집 실패 회차 = '0' 강제(260816) — 인스타 데이터가 어제 것 그대로인데 브리프를 새로 쓰면
          # 「오늘 0장」 같은 오늘 상태를 옛 숫자로 단정한다(chan_brief.sh 의 입력 동일=스킵 계약과 같은 축을
          # 명시로 굳힌 것 · 페이스북 브리프는 아래에서 그대로 돈다 = 곁다리 사망 없음).
          CHAN_BRIEF: ${{ (github.event_name == 'push' || steps.igfetch.outcome != 'success') && '0' || (inputs.brief || '1') }}   # 기본 ON(§📰-e 이행 — 카나리아 run 29263671640 실측 260714: 성장 서사+전략 4줄 정상 · schedule = inputs 없음 → '1') · dispatch brief='0' = 일시 OFF · push(하트비트) = '0' 강제(데이터 수집만 · 비싼 AI 브리프는 3h schedule 전용 · 260720) · 롤백 = inputs.brief || '1'
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets[format('CLAUDE_CODE_OAUTH_TOKEN_{0}', vars.ACTIVE_ACCOUNT || 'MUTENO')] }}
          CLAUDE_CODE_OAUTH_TOKEN_ALT: ${{ (vars.ACTIVE_ACCOUNT || 'MUTENO') == 'NOMUTEFB' && secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130G || secrets.CLAUDE_CODE_OAUTH_TOKEN_NOMUTEFB }}   # 서브1 폴오버(체인 MUTENO→NOMUTEFB→EMS1130G→EMS1130M · sns-trends 관용구)
          CLAUDE_CODE_OAUTH_TOKEN_ALT2: ${{ (vars.ACTIVE_ACCOUNT || 'MUTENO') == 'MUTENO' && secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130G || secrets.CLAUDE_CODE_OAUTH_TOKEN_MUTENO }}   # 서브2 폴오버(체인 3번째)
          CLAUDE_CODE_OAUTH_TOKEN_ALT3: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130M }}   # 서브3 폴오버(실꼬리 = 항상 EMS1130M)
        run: bash .github/scripts/chan_brief.sh   # 게이트·스킵·fail-soft 전부 스크립트 내(rc 항상 0 = 수집 커밋 비차단)
      - name: 커밋(IG 브리프 즉시 착지 · 평의회 260724) — 뒤 직렬 fb_brief 하드킬(잡 timeout·폴오버)로부터 방금 생성한 chan_brief.json 보호 = '브리프 정지' 사고 클래스 재발방지(1차/2차 분리 패턴 계승 · fb_brief는 별 프로세스라 $SECONDS 리셋으로 잡 누적시간 미인지)
        run: |
          bash .github/scripts/git_land.sh "insta: 채널 브리프 스냅샷 (bot)" viewer/chan_brief.json viewer/chan_brief_log.jsonl "metrics/usage/${GITHUB_RUN_ID}-${GITHUB_JOB}-${GITHUB_RUN_ATTEMPT}.jsonl"
      - name: FB 주제 LLM 분류(운영자 260724 · Haiku·캐시 · 정확 주제별 반응 승격) — 재작성 헤드라인이라 키워드 분류 빈약(기타 80%)을 LLM으로 교정 · fail-soft(rc0 · 실패 시 키워드 폴백)
        if: ${{ github.event_name != 'push' && (inputs.brief || '1') == '1' }}   # claude CLI 설치 스텝과 동일 게이트(브리프 레인 편승) · push 하트비트 = 스킵(캐시라 3h 캐던스로 충분)
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets[format('CLAUDE_CODE_OAUTH_TOKEN_{0}', vars.ACTIVE_ACCOUNT || 'MUTENO')] }}   # 브리프 동일 활성계정(분류 = Haiku 경량 · 실패 = 폴백이라 체인 생략)
        run: python3 .github/scripts/fb_classify.py || true
      - name: FB 채널 AI 브리프(운영자 260724 "페이스북도 요약 동일 조건") — chan_brief 자매·반응 기반 · fb_classify 뒤(분류 topics 반영) · 입력 동일=스킵(토큰0) · 실패=직전 유지(fail-soft)
        if: ${{ github.event_name != 'push' && (inputs.brief || '1') == '1' }}   # chan_brief와 동일 게이트(브리프 레인 편승) · push 하트비트 = 스킵
        env:
          CHAN_BRIEF: ${{ github.event_name == 'push' && '0' || (inputs.brief || '1') }}
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets[format('CLAUDE_CODE_OAUTH_TOKEN_{0}', vars.ACTIVE_ACCOUNT || 'MUTENO')] }}
          CLAUDE_CODE_OAUTH_TOKEN_ALT: ${{ (vars.ACTIVE_ACCOUNT || 'MUTENO') == 'NOMUTEFB' && secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130G || secrets.CLAUDE_CODE_OAUTH_TOKEN_NOMUTEFB }}
          CLAUDE_CODE_OAUTH_TOKEN_ALT2: ${{ (vars.ACTIVE_ACCOUNT || 'MUTENO') == 'MUTENO' && secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130G || secrets.CLAUDE_CODE_OAUTH_TOKEN_MUTENO }}
          CLAUDE_CODE_OAUTH_TOKEN_ALT3: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130M }}
        run: bash .github/scripts/fb_brief.sh   # 게이트·스킵·fail-soft 전부 스크립트 내(rc 항상 0 = 수집 커밋 비차단)
      - name: 회차 원장 append(알고리즘 인사이트 · 운영자 260802 · 평의회 합의) — 요약 전문+쓴 데이터+그 시점 실검·커뮤니티 키워드를 apps/insta/data/algo_runs/에 1줄 누적. 배치 = fb_brief 뒤·맨끝 커밋 앞(중간 git_land의 reset --hard가 인자 밖 append를 되돌리는 자리 회피 · 맨끝 git_land의 apps/insta/data 디렉터리 인자가 자동 착지 = 인자 수정 0). stdlib·네트워크0·LLM 0콜·실측 수십 ms · 멱등(dk+run_id) · 롤백 = repo variable ALGO_LEDGER=0
        if: ${{ github.event_name != 'push' && (vars.ALGO_LEDGER || '1') != '0' }}   # 하트비트(push) 런 배제 = 40회/일 중복 스냅샷 차단 · 브리프 레인(schedule·dispatch)만. ⚠ vars 미설정 = null인데 GitHub 표현식은 null·'0' 둘 다 숫자 0으로 강제 = null != '0' 이 거짓 → 미설정에서 통째 스킵(실측 run 30745853205 · || '1' 관용구가 교정)
        timeout-minutes: 2          # 행(hang)이 잡 50분 예산을 먹는 경로 봉쇄
        continue-on-error: true     # 어떤 죽음도 뒤 커밋(브리프·수집 착지) 비차단
        env:
          GH_RUN: ${{ github.run_id }}
          GH_ATT: ${{ github.run_attempt }}
          GH_EV: ${{ github.event_name }}
        run: |
          git fetch -q origin main 2>/dev/null || true
          git checkout -q origin/main -- apps/insta/data/algo_runs/ 2>/dev/null || true   # append 기준선 = origin/main 최신(스텝 순서·직전 reset 의존 제거)
          python3 .github/scripts/algo_ledger.py || true
      - name: 알고리즘 인사이트 주간 집계(결정론·LLM 0콜) — 원장 40줄+ 도달 후 KST ISO주 바뀔 때 1회 자동(운영자 절차 0) · 산출 = apps/insta/data/algo_insight.{json,md} · 미달·기집계 = 조용히 스킵
        if: ${{ github.event_name != 'push' && (vars.ALGO_LEDGER || '1') != '0' }}
        timeout-minutes: 3
        continue-on-error: true
        run: python3 apps/insta/algo_insight.py || true
      - name: 커밋(브리프 포함 · 맨끝 2차)
        # 브리프분(chan_brief.json·아카이브 로그)까지 최신 main 재기점 재적층으로 착지. 종전 관용구는 같은 파일을 다른 봇이 먼저 밀면 꼬인 리베이스로 브리프가 증발(→ 화면 정지)하던 자리 = git_land가 원천 봉합. chan_brief_log.jsonl도 add 대상 편입(종전 누락).
        run: |
          bash .github/scripts/git_land.sh "insta: 계정 인사이트 스냅샷 (bot)" apps/insta/data viewer/insta_data.json viewer/insta_covers viewer/fb_data.json viewer/chan_brief.json viewer/chan_brief_log.jsonl viewer/fb_cat_cache.json viewer/chan_brief_fb.json viewer/chan_brief_fb_log.jsonl messages "metrics/usage/${GITHUB_RUN_ID}-${GITHUB_JOB}-${GITHUB_RUN_ATTEMPT}.jsonl"
      # ── 수집이 죽은 회차는 잡도 빨갛게 끝난다(260816 · continue-on-error 의 짝 = 선택 아님) ──
      # continue-on-error 는 뒤 축(페이스북 수집·FB 브리프·회차 원장)을 살리려고 붙인 것이지 실패를
      # 없던 일로 만들려는 게 아니다. 이 스텝이 없으면 잡 결론이 success 가 되어 **초록으로 위장**된다
      # (「실패는 실패라고 쓴다」 · 위 알림 착지 스텝 주석의 무증상화 금지 계약과 같은 축).
      # 자리 = 맨 끝 = 여기까지 와야 살릴 축을 전부 살린 뒤에 실패를 선언하게 된다.
      - name: 수집 실패 최종 표시(초록 위장 금지)
        if: ${{ steps.igfetch.outcome == 'failure' }}
        run: |
          echo "::error::인스타 수집 스텝이 실패했다 — 뒤 축(페이스북·회차 원장)은 살렸지만 이 잡은 실패로 끝낸다. 원인·조치 = 위 수집 스텝 로그와 앱 알림함(insta-token-dead)."
          exit 1
