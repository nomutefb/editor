name: news-ask

# 뷰어 ✨요약 요청(asks/*.json = 자연어 + 캡처) → Claude 헤드리스가 해석·기사검색(WebSearch)·캡처판독(Read)
# → 큐레이션 다이제스트를 queue/ 에 커밋(= 뉴스요약에 뜸). Termux 대체 멀티모달 입구.
# news-analyze(pending/** = URL)와 별개 트랙 — analyze 불변 안 건드림. 출력 포맷·지침주입은 analyze 재사용.
on:
  push:
    branches: [main]
    paths:
      - 'asks/**'
  workflow_dispatch:
    inputs:
      account:
        description: '이 실행에 쓸 계정(구독 OAuth)'
        type: choice
        default: MUTENO
        options:
          - NOMUTEFB
          - MUTENO
          - EMS1130G
      min_age_min:
        description: '스윕에서 이 분(min) 미만 신선 ask 스킵(0=전건 · pending-sweep 백스톱=40 — 자기 스코프 런이 처리 중인 신선 건과의 겹침 배제)'
        type: string
        default: '0'

# 병렬 처리(운영자 260720 "앞에 뭐 있어도 여러 개 동시에" — 직렬 그룹 폐지 2단 진화):
#   ① 260720 오전: 런 레벨 그룹(news-ask)을 card_plan 꼬리(상한 165분)가 인질로 쥐어 새 요청이 착수도 못 함
#      → 잡 레벨(news-ask-job)로 1차 완화(analyze 260717 미러).
#   ② 260720 지금: 잡 레벨 직렬도 폐지 — ask 잡에 concurrency 그룹 자체가 없다(클로드코드 새 세션처럼 독립 병렬).
#      병렬 안전장치 = '푸시 범위 스코프': push 런은 자기 푸시가 추가한 asks/*.json 만 처리(ASK_ONLY →
#      ask.sh)라 런끼리 같은 파일을 안 집는다(중복 소비·이중 과금 0). 산출·커밋 충돌은 ⓐ queue 파일명이
#      ask base 유래 결정적 ⓑ metrics shard = 런·잡별 고유 파일 ⓒ Commit 스텝 rebase -X theirs ×4 백오프로 흡수.
#      런 증발(러너 유실·범위 산출 실패)의 고아 = pending-sweep 가 45분 임계로 news-ask 스윕 디스패치(백스톱).
#      dispatch(수동·백스톱) = ASK_ONLY 없이 잔류 전건 스윕. 롤백 레버 = ask 잡에 job 레벨
#      concurrency(news-ask-job · cancel-in-progress:false) 한 블록 재부착이면 ①상태로 복귀.
#   card_plan(card-make)·thumb_gen(thumb-make)은 자기 잡 그룹 유지 = 공유 자원(cards/) 직렬화 불변 —
#      다중 런 폭주 시 카드 잡 pending 슬롯 퇴출(eviction)이 나도 cardmake 'all' = 상태 스윕이라 뒤 런이 전건 커버.

permissions:
  contents: write

jobs:
  ask:
    runs-on: ubuntu-latest
    timeout-minutes: 100   # 60 → 100(운영자 260731 링크 전사 레일): 최악 스택 = 셋업 + large-v3 STT(1시간 미디어 ≈ 최대 50분·ASK_LINK_STT_TIMEOUT 3000s 캡) + 요약 배치(ASK_JOB_DEADLINE 2200s). 링크 없는 평시 런은 종전과 동일하게 수 분에 끝난다(캡은 최후 보루).
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 1   # 최신 1커밋만 — 구 0은 원격 브랜치 567개 전체 히스토리를 받아 실측 105s를 먹었다(260810 · imggen run 30972520521 = 체크아웃 108s 중 워킹트리 쓰기는 3s뿐)
          # ⚡ 260810 sparse-checkout(scrape 카나리아 실증 = 같은 잡 A/B 20s→9s · 삭제 커밋 0건) — 목록 = 이 잡이 실행줄에서 여는 폴더뿐.
          #   빠지는 대형 = _versions 437M · docs 90M · 자료 75M · _산출 74M(어느 잡도 안 연다) · ⚠ git add/rm 대상은 전건 포함 검문 통과분.
          sparse-checkout: |
            .github
            push
            assets/brand
            shared
            apps
            asks
            cards
            messages
            metrics
            queue
          # 트리거 커밋(github.sha)이 아니라 체크아웃 시점의 최신 main을 받는다 — 순차 큐에서 뒤 run이 옛 커밋을
          # 체크아웃하면 앞 run이 이미 처리한 ask를 재분석(중복 다이제스트·add/add 충돌 · analyze run#5 동일 원인).
          # 구 주석은 fetch-depth:0 만으로 '최신 main 기준'이라 적었으나 ref 없으면 github.sha 고정 = 오인.
          ref: main

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: '24'

      - name: Install Claude Code CLI
        run: npm install -g @anthropic-ai/claude-code   # 러너엔 claude 미설치 — analyze와 동일하게 설치(없으면 exit 127)

      # ── 링크 레일(운영자 260731 "링크가 원문이면 원문 활용 · 미디어면 large v3 전사") ──
      # 잔류 asks 중 '미디어' 링크가 하나라도 있으면 STT 환경·모델(3.1GB)을 캐시로 미리 깐다.
      #   ⚠ 스코프(ASK_ONLY)는 아래 스텝에서 산출되므로 여기선 전건을 훑는다 = 과탐지해도 캐시 워밍뿐(무해),
      #     미탐지면 ask.sh 인라인 전사가 apps/ly/setup.sh 를 직접 깔아 진행(fail-soft 이중화).
      - name: 미디어 링크 감지 (전사 필요 여부)
        id: linkscan
        run: |
          NEED=0
          shopt -s nullglob
          for p in asks/*.json; do
            if python3 .github/scripts/ask_link.py --scan "$p" | cut -f2 | grep -qx media; then NEED=1; break; fi
          done
          echo "need_stt=$NEED" >> "$GITHUB_OUTPUT"
          echo "미디어 링크 전사 필요: $NEED"

      - name: STT 환경 (미디어 링크 — ffmpeg+faster-whisper+large-v3 · 캐시)
        if: ${{ steps.linkscan.outputs.need_stt == '1' }}
        timeout-minutes: 20   # 첫 런 large-v3 3.1GB 다운 감안(nb-make·edit-make 동일 · 캐시히트 즉시)
        uses: ./.github/actions/runner-setup
        with:
          setup: apps/ly/setup.sh
          # ⚡ 260808 — STT 기본 = Scribe v2(ly_stt.py) → large-v3는 **폴백 전용**이라 3.1GB 캐시·prefetch를 평소엔 안 붙인다.
          #   실측 = 이 스텝 48s(총 112s의 43%)가 Scribe 경로에선 한 번도 안 쓰이는 짐이었다(run 31231381993 분해).
          #   'true'로 되돌아가는 조건 = 킬스위치(vars.LY_STT_ENGINE=whisper) ∨ 키 부재 = **폴백이 유일 경로인 상황**.
          #   ⚠ ffmpeg·yt-dlp·faster_whisper(pip)는 그대로 설치된다 — 빠지는 건 모델 파일 3.1GB뿐(폴백 시 그 자리에서 회수).
          whisper: ${{ (vars.LY_STT_ENGINE == 'whisper' || secrets.ELEVENLABS_API_KEY == '') && 'true' || 'false' }}
          apt: ffmpeg

      - name: Process asks
        env:
          # STT 엔진 = Scribe v2 우선 → 실패·키부재 시 large-v3 자동 폴백(ly_stt.py · 운영자 260808 승인)
          #   실측 = run 31230435185(한국어 19.6s): 28.1s→1.0s · 오인식 의심 11.5%→3.4% · 「검찰서가」→「경찰서 가서」 교정.
          #   킬스위치 = 레포 변수 LY_STT_ENGINE=whisper(즉시 종전 경로) · 미설정 = auto(키 있으면 Scribe).
          ELEVENLABS_API_KEY: ${{ secrets.ELEVENLABS_API_KEY }}
          LY_STT_ENGINE: ${{ vars.LY_STT_ENGINE }}
          # ⚠ 평의회1 F3 — 이 경로만 `ask_link_stt.sh`가 **setup.sh를 다시 부른다**. 그 셸엔 runner-setup이 주입한
          #   LY_WHISPER_PREFETCH가 없어 기본 'true'로 떨어지고, 캐시는 꺼져 있으니 **매 런 3.1GB 신규 다운로드**가 된다
          #   (= 이 최적화의 정반대 · 그것도 Scribe 성공 여부와 무관하게 ly_stt 실행 **전에** 지불).
          LY_WHISPER_PREFETCH: ${{ (vars.LY_STT_ENGINE == 'whisper' || secrets.ELEVENLABS_API_KEY == '') && 'true' || 'false' }}
          # 인증 = 구독 OAuth(API 키 미사용) — analyze와 동일 동적 계정 선택
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets[format('CLAUDE_CODE_OAUTH_TOKEN_{0}', inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO')] }}
          CLAUDE_CODE_OAUTH_TOKEN_ALT: ${{ (inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO') == 'NOMUTEFB' && secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130G || secrets.CLAUDE_CODE_OAUTH_TOKEN_NOMUTEFB }}   # 서브1 폴오버(활성 다음 우선순위 · 체인 MUTENO→NOMUTEFB→EMS1130G→EMS1130M)
          CLAUDE_CODE_OAUTH_TOKEN_ALT2: ${{ (inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO') == 'MUTENO' && secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130G || secrets.CLAUDE_CODE_OAUTH_TOKEN_MUTENO }}   # 서브2 폴오버(체인 3번째)
          CLAUDE_CODE_OAUTH_TOKEN_ALT3: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130M }}   # 서브3 폴오버(체인 실꼬리 = 항상 EMS1130M · 앞 3계정 소진 후)
          ACTIVE_ACCOUNT: ${{ inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO' }}
          SUMMARY_LEN_GUARD: ${{ vars.SUMMARY_LEN_GUARD || '' }}   # 분량 가드(IG/Thread 과소 시 1회 보강) — 기본 OFF·repo 변수 '1'로 카나리아→승격(§파이프라인 e · 260705)
          EVT_NAME: ${{ github.event_name }}
          EVT_BEFORE: ${{ github.event.before || '' }}   # push 직전 head — 병렬 스코프 범위 산출용(헤더 주석 ②)
          EVT_SHA: ${{ github.sha }}
          ASK_MIN_AGE_MIN: ${{ inputs.min_age_min || '0' }}   # 스윕 신선 배제(dispatch 전용 — push 스코프엔 무관)
          YT_COOKIES: ${{ secrets.YT_T_COOKIES }}   # 지금 쓰는 살아있는 칸 = 1순위(260811 · 죽은 칸을 먼저 두드리면 매 런 4번 헛시도 + 유튜브에 같은 주소로 실패를 반복해 보인다)
          YT_COOKIES_NAME: YT_T_COOKIES
          YT_COOKIES_2: ${{ secrets.YT_T2_COOKIES }}   # 예비 1 — 위가 죽으면 자동 회수
          YT_COOKIES_2_NAME: YT_T2_COOKIES
          ASK_LINK_DIR: /tmp/asklink   # 미디어 링크 전사문 캐시 경로(ask.sh 링크 레일 · 운영자 260731)
          # 길이 정책(운영자 260731 실측 확정 · "자막있으면 영상길이 상관없이 / 자막없으면 영상길이 제한, 단 강제 활성화 가능"):
          #   실측 = 총소요 ≈ 요약 오버헤드 500s + 전사 1.5×영상길이(러너 large-v3 A/B 260728 = 40s 이상 구간 1.39×RT · 오늘 ask 2건 = 428s·594s).
          #   → 자막 경로는 길이와 사실상 무관(다운로드뿐) = 24시간 = 상한 해제 / 전사 경로만 제한(30분 = 총 53분) ·
          #     운영자가 링크칸에서 '전사 강행'을 켜면 ASK_LINK_STT_FORCE_SEC(55분 = 총 98분 · 잡 캡 100분 물리 한계)까지 허용.
          ASK_LINK_MAX_SEC: ${{ vars.ASK_LINK_MAX_SEC || 86400 }}          # 자막 경로 = 길이 무제한(24h = 사실상 해제)
          ASK_LINK_STT_MAX_SEC: ${{ vars.ASK_LINK_STT_MAX_SEC || 1800 }}   # 전사 경로 기본 상한 = 영상 30분(총 ≈53분)
          ASK_LINK_STT_FORCE_SEC: ${{ vars.ASK_LINK_STT_FORCE_SEC || 3300 }}   # '전사 강행' 시 상한 = 영상 55분(총 ≈98분 = 잡 캡 100분 한계 · 이 이상은 물리적으로 못 담아 강행도 거절)
        run: |
          echo "활성 계정: ${ACTIVE_ACCOUNT}"
          if [ -z "${CLAUDE_CODE_OAUTH_TOKEN}" ]; then
            echo "::error::CLAUDE_CODE_OAUTH_TOKEN_${ACTIVE_ACCOUNT} 시크릿 비어있음"; exit 1
          fi
          unset ANTHROPIC_API_KEY ANTHROPIC_AUTH_TOKEN
          # 병렬 스코프(헤더 주석 ② · 적대검증 3인 260720 수렴 반영): push 런 = 이 푸시가 추가·변경한 asks/*.json 만.
          #   ⓐ diff 성공 + 0건 = no-op 종료 — 삭제·정리 전용 푸시(재시도 버튼의 PAT DELETE 커밋 최대 3발 등)가
          #      전건 스윕으로 새면 신규 ask 를 스코프 런과 2~4중 처리(중복 과금 폭풍 · 검증 A1/B3/C5 확정)라 차단.
          #   ⓑ 전건 스윕 폴백은 '산출 불가'(zero-SHA·force-push 이력 부재·diff 실패)에만 — 유실 0 은 45분 백스톱이 담보.
          #   ⓒ pathspec = :(glob) — 기본 pathspec 의 * 는 / 를 넘어 asks/failed/ 격리본까지 매치(실측 · 검증 C5ⓒ)
          #      → 격리본 부활 처리 차단. dispatch 런 = ASK_ONLY 없음(잔류 스윕 · ASK_MIN_AGE_MIN 신선 배제).
          ASK_ONLY=""
          if [ "${EVT_NAME}" = "push" ]; then
            Z="0000000000000000000000000000000000000000"
            if [ -n "${EVT_BEFORE}" ] && [ "${EVT_BEFORE}" != "$Z" ] \
               && git cat-file -e "${EVT_BEFORE}^{commit}" 2>/dev/null && git cat-file -e "${EVT_SHA}^{commit}" 2>/dev/null; then
              if ASK_ONLY="$(git diff --name-only --diff-filter=AM "${EVT_BEFORE}" "${EVT_SHA}" -- ':(glob)asks/*.json' 2>/dev/null)"; then
                if [ -z "${ASK_ONLY// }" ]; then
                  echo "이 푸시에 신규 요약요청 없음(삭제·정리 전용 푸시) — no-op 종료"
                  exit 0
                fi
              else
                ASK_ONLY=""
                echo "::warning::범위 diff 실패 — 전건 스윕 폴백(파일명 결정성 = 중복 자기덮어쓰기 무해)"
              fi
            else
              echo "::warning::push 범위 SHA 미해결(force-push 등) — 전건 스윕 폴백"
            fi
          fi
          export ASK_ONLY
          bash .github/scripts/ask.sh

      - name: Commit results
        if: always()
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add queue asks
          git add metrics 2>/dev/null || true   # 토큰 계측 shard(claude_meter) 동반 커밋 — 있을 때만(없어도 무해)
          git add -A messages 2>/dev/null || true   # claude 시스템성 알림 = msg.py 가 messages/<id>.json(git 추적 = 빌드 입력)에 씀 → 배포 빌드가 viewer/messages.json 합성. -A = set/clear/prune 삭제까지 반영. (구 `git add viewer/messages.json` 은 gitignore 산출물이라 조용히 무시 = 알림 미반영 버그 260711 복구)
          if git diff --cached --quiet; then echo "변경 없음"; exit 0; fi
          git commit -m "ask: 요약 요청 큐레이션"
          for i in 1 2 3 4; do
            git fetch --deepen=100 origin main 2>/dev/null || true   # 얕은 클론(fetch-depth:1) 짝 — 원격 새 커밋과 로컬 HEAD 사이가 끊겨 merge-base를 못 찾으면 아래 rebase가 통째로 실패한다(= 산출 유실). main 100커밋만이라 수 초 · 실패해도 무해(260810)
            if git pull --rebase --autostash -X theirs origin main && git push origin HEAD:main; then exit 0; fi
            git rebase --abort 2>/dev/null || true
            sleep $((2**i))
          done
          echo "push 실패"; exit 1

      # 요약 요청 완료 푸시 — 요약이 준비되면 통지. 탭하면 그 요약 창이 열림(/?a=<파일>). 비치명(analyze와 공용 스크립트).
      - name: 요약 완료 푸시 (요약 열기)
        if: success()
        env:
          VAPID_PRIVATE_KEY: ${{ secrets.VAPID_PRIVATE_KEY }}
          VAPID_SUBJECT: ${{ secrets.VAPID_SUBJECT }}
        run: bash .github/scripts/notify_summary.sh

      # 요약 요청 실패 푸시 — ask.sh 가 /tmp/analyzed_fail_msgs.txt 에 적재한 실패 base 를 통지(탭→메시지함 실패 메시지). news-analyze 와 동일 패턴(운영자 260629 ask 경로 푸시 통일).
      - name: 요약 실패 알림 (요청 미완료 통지)
        if: always()
        env:
          VAPID_PRIVATE_KEY: ${{ secrets.VAPID_PRIVATE_KEY }}
          VAPID_SUBJECT: ${{ secrets.VAPID_SUBJECT }}
        run: bash .github/scripts/notify_fail.sh

      - name: Surface failures
        if: always()
        run: |
          # ⚠️ 이번 런에서 새로 실패한 것만 빨강(asks/failed/ 누적 전체 아님 — stale-red 차단·260620).
          # 옛 실패는 asks/failed/에 남아도 뷰어 대기열(api/pending)이 24h 표면화 → CI는 깨끗.
          RUNLIST="${RUNNER_TEMP:-/tmp}/ask_fail_run"
          if [ -s "$RUNLIST" ]; then
            echo "::error::요약 요청 처리 실패(이번 런 $(wc -l < "$RUNLIST")건) — asks/failed/ 로그 확인"
            while read -r b; do echo "── $b ──"; cat "asks/failed/$b.log" 2>/dev/null | head -40; done < "$RUNLIST"
            exit 1
          fi
          echo "이번 런 실패 없음"

  card_plan:
    needs: ask
    if: always()              # 분석 일부 실패해도 성공 큐는 카드 프롬프트 생성
    runs-on: ubuntu-latest
    timeout-minutes: 165      # 텍스트 카드 MAX_BATCH(3)건 × Opus — CARD_TIMEOUT 1500s와 한 쌍{3건×(1500+재시도1500)=150분 + 헤드룸 15분 · news-analyze card_plan과 동일값(비대칭이면 ask발 카드만 하드킬 림보 재개봉 · 전반평의회⑨ 260711)}
    concurrency:
      group: card-make        # 버튼 렌더(card-make.yml)와 같은 그룹 = 같은 cards/ 직렬화(-X theirs 무경고 덮어쓰기 방지)
      cancel-in-progress: false
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 1   # 최신 1커밋만 — 구 0은 원격 브랜치 567개 전체 히스토리를 받아 실측 105s를 먹었다(260810 · imggen run 30972520521 = 체크아웃 108s 중 워킹트리 쓰기는 3s뿐)
          ref: main           # ask가 push한 최신 queue 포함
      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: '24'
      - name: Install Claude Code CLI
        run: npm install -g @anthropic-ai/claude-code

      - name: Card-plan (텍스트 카드 프롬프트 — 제미나이 0)
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets[format('CLAUDE_CODE_OAUTH_TOKEN_{0}', inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO')] }}
          CLAUDE_CODE_OAUTH_TOKEN_ALT: ${{ (inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO') == 'NOMUTEFB' && secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130G || secrets.CLAUDE_CODE_OAUTH_TOKEN_NOMUTEFB }}   # 서브1 폴오버(활성 다음 우선순위 · 체인 MUTENO→NOMUTEFB→EMS1130G→EMS1130M)
          CLAUDE_CODE_OAUTH_TOKEN_ALT2: ${{ (inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO') == 'MUTENO' && secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130G || secrets.CLAUDE_CODE_OAUTH_TOKEN_MUTENO }}   # 서브2 폴오버(체인 3번째)
          CLAUDE_CODE_OAUTH_TOKEN_ALT3: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130M }}   # 서브3 폴오버(체인 실꼬리 = 항상 EMS1130M · 앞 3계정 소진 후)
          ACTIVE_ACCOUNT: ${{ inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO' }}
          CARD_COV_GUARD: ${{ vars.CARD_COV_GUARD || '1' }}   # 카드 커버리지 가드(고신호 증발 시 1회 회수) — 기본 ON(승격 260802) · 끄기 = repo 변수 CARD_COV_GUARD를 '1' 외 값(off 권장)으로(§파이프라인 e)
          CARD_SYS_PROMPT: ${{ vars.CARD_SYS_PROMPT || '1' }}   # 카드 고정부(지침) 시스템 프롬프트 캐시 봉합 — 기본 ON(평의회 260803 · 실물 프로브 138K 히트) · 끄기 = repo 변수 CARD_SYS_PROMPT를 '1' 외 값으로
          # CARD_FAIL_RETRY_MAX 미주입(운영자 260722 · 평의회 P1) — 자동 재시도 봉인은 cardmake.sh 기본값 0이 단일 정본. repo 변수 stale('3' 등) 오버라이드 차단·수동(card-make)과 대칭. 재개는 cardmake.sh:36 기본값에서.
          # 🔒 Lock A — 이 잡엔 GDRIVE_SA_JSON·GEMINI_API_KEY env 자체가 없음(유료 생성 발사 불가). + cardmake text모드 unset(Lock B).
        run: |
          [ -n "${CLAUDE_CODE_OAUTH_TOKEN}" ] || { echo "토큰 없음 — 카드플랜 생략"; exit 0; }
          unset ANTHROPIC_API_KEY ANTHROPIC_AUTH_TOKEN GDRIVE_SA_JSON GEMINI_API_KEY
          # text 모드 = cards/ 미존재·지침 stale 큐만 골라 카드 MD까지(제미나이 0, state=text_done).
          bash .github/scripts/cardmake.sh all text || echo "::warning::카드플랜 일부 실패(다이제스트는 보존됨)"

      # 좀비 generating 차단 — 위 스텝이 크래시/중단되면 status가 영영 'generating'(모달 '프롬프팅 중' 영구).
      # always로 잔류 generating 을 failed 로 스윕·커밋해 모달이 멈추지 않게 한다.
      - name: Sweep stuck generating (좀비 → failed)
        if: always()
        run: |
          # 스윕 본문 = 공유 SSOT .github/scripts/sweep_stuck.sh(3 워크플로 공용 — 구 3벌 인라인이 fails·지침해시를 덮어쓰던 것 봉합 · 평의회 260711)
          bash .github/scripts/sweep_stuck.sh

      # 큐레이션 즉시 전달 — 분석·카드 커밋 후 Cloudflare 재빌드 1회 트리거 → 피드에 바로 반영
      # (정기 빌드[deploy-hook 12h] 안 기다림). 픽은 드물어 빌드 한도 영향 미미. 시크릿 없으면 no-op(다음 정기빌드).
      - name: 큐레이션 즉시 전달 (Cloudflare 재빌드)
        if: always()
        env:
          CF_DEPLOY_HOOK: ${{ secrets.CF_DEPLOY_HOOK }}
        run: |
          if [ -z "${CF_DEPLOY_HOOK}" ]; then
            echo "CF_DEPLOY_HOOK 미설정 — 즉시 재빌드 생략(다음 정기 빌드 때 피드 반영)"; exit 0
          fi
          if curl -fsS -X POST "${CF_DEPLOY_HOOK}" -o /dev/null; then
            echo "Cloudflare 재빌드 트리거 — 큐레이션 결과 곧 피드 반영"
          else
            echo "::warning::재빌드 트리거 실패(다음 정기 빌드 때 반영)"
          fi

  # 3단계 = AI 썸네일 후보 2화풍(픽한 기사만·자동). 카드(외부 Apps Script/Drive)와 분리된
  # 레포 내 경로 = Actions가 Gemini를 직접 호출, 한국어 글자도 이미지에 직접 렌더(compose 불필요).
  # ⚠️ 비용잠금(Lock A) 해제 지점 = 픽=Gemini 2장 자동과금. GEMINI_API_KEY 시크릿 없으면 no-op(스캐폴드).
  thumb_gen:
    needs: ask
    if: always()              # 분석 일부 실패해도 성공 큐는 썸네일 생성
    runs-on: ubuntu-latest
    timeout-minutes: 30
    concurrency:
      group: thumb-make       # card-make와 분리(card_plan과 강제 직렬·pending 취소 방지). cards/ 쓰기 경합은 각 잡 rebase -X theirs가 흡수(파일 비중첩).
      cancel-in-progress: false
    permissions:
      contents: write
      actions: write          # 검색이미지 미달 보충 = moreimg workflow_dispatch(GITHUB_TOKEN은 dispatch류만 재트리거 허용 축 · 운영자 260726)
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 1   # 최신 1커밋만 — 구 0은 원격 브랜치 567개 전체 히스토리를 받아 실측 105s를 먹었다(260810 · imggen run 30972520521 = 체크아웃 108s 중 워킹트리 쓰기는 3s뿐)
          ref: main           # ask가 push한 최신 queue 포함
      - name: 썸네일 후보 — 검색이미지(og:image) + AI 2화풍 생성 (신규 픽만)
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          # ✅ AI 썸네일 2화풍(포토에디토리얼·극화) 생성 ON(운영자 260630 재가동 — 260622 임시 OFF 해제).
          #    news-analyze.yml과 쌍둥이 경로 동일. 일시 차단하려면 THUMB_AI_OFF: '1' 추가(검색이미지는 유지). 수동 thumb-redo는 항상 AI.
          # ⚠️ Google CSE JSON API = 2025 신규고객 차단(死) → 폐기. 검색이미지는 기사 og:image 추출(망만 필요, 키 무관).
          # 저장소 = Cloudflare R2(설정 시 공개 URL 서빙·레포 비대 회피). 5개 다 있어야 R2, 아니면 git 폴백.
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
          R2_PUBLIC_BASE: ${{ secrets.R2_PUBLIC_BASE }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          THUMB_MAX_BATCH: '3'
          THUMB_SINCE: '260703'   # 활성화 기준일 — 4화풍 재편(260703)부터. 260619~0702 백로그는 2화풍 유지(4화풍 소급 재과금 차단 — 원하면 날짜 내리기).
          THUMB_REF: '1'          # 참조 체이닝 승격(260703 카나리아 통과 — 극화·수채가 대표 실사진으로 실제 얼굴 재현). photo 제외·사인/피해자/미성년 익명 프롬프트 유지. 롤백=이 줄 제거.
        run: |
          # AI_OFF면 키 없어도 검색이미지는 돌린다(검색=망 fetch·키 무관). 평소(AI on)엔 키 없으면 스캐폴드 no-op.
          if [ -z "${GEMINI_API_KEY}" ] && [ "${THUMB_AI_OFF}" != "1" ]; then
            echo "GEMINI_API_KEY 미설정 — 썸네일 생성 생략(스캐폴드)"; exit 0
          fi
          python3 .github/scripts/thumb_gen.py || echo "::warning::썸네일 생성 일부 실패(다이제스트·카드는 보존됨)"
      - name: Commit thumbnails
        if: always()
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add cards 2>/dev/null || true
          if git diff --cached --quiet; then echo "썸네일 변경 없음"; exit 0; fi
          git commit -m "thumbs: AI 썸네일 후보 생성"
          for i in 1 2 3 4; do
            git fetch --deepen=100 origin main 2>/dev/null || true   # 얕은 클론(fetch-depth:1) 짝 — 이 잡은 ask와 **별도 러너·별도 클론**이라 앞 잡의 경계 확장이 안 미친다. 경계가 끊기면 merge-base 실종 = rebase 실패 = 썸네일 산출 유실(260810)
            git pull --rebase --autostash -X theirs origin main && git push origin HEAD:main && exit 0
            git rebase --abort 2>/dev/null || true; sleep $((2**i))
          done
          # ⚠ 260816 봉합 — 구판은 warning 만 남기고 rc 0(초록)으로 끝났다. 이 축은 산출이 과금분이고
          #   발사가 일회성이라 자동 재시도가 0이라서, 조용히 끝나면 썸네일이 그대로 유실되고
          #   화면은 「제작중」에 멈춘다(그림 자체는 R2 에 살지만 완료 신호·이력이 사라진다).
          echo "::error::썸네일 push 실패 — 썸네일 미착지"; exit 1
      # 검색이미지 7장 미달·원문 url 부재 건 = moreimg 자동 보충(운영자 260726 "원래 7개" + "검색으로 원문 URL 공유에 링크").
      # 대상 = thumb_gen.py가 RUNNER_TEMP/thumb_topup.txt 마커로 지목(이번 런 신규 기록분만). 커밋·push *뒤* 발사 =
      # moreimg 체크아웃이 이번 search.json 포함(즉발이면 add/add 경합). 실패 = 경고만(수동 '+N장 더' 폴백 불변) · 중단 = 이 스텝 제거.
      - name: 검색이미지 미달 자동 보충 (moreimg 디스패치)
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          F="${RUNNER_TEMP:-/tmp}/thumb_topup.txt"
          [ -s "$F" ] || { echo "보충 대상 없음"; exit 0; }
          sort -u "$F" | while read -r stem want; do
            [ -n "$stem" ] || continue
            curl -fsS -X POST -H "Authorization: Bearer ${GH_TOKEN}" \
              -H "Accept: application/vnd.github+json" -H "X-GitHub-Api-Version: 2022-11-28" \
              "https://api.github.com/repos/${GITHUB_REPOSITORY}/actions/workflows/moreimg.yml/dispatches" \
              -d "{\"ref\":\"main\",\"inputs\":{\"stem\":\"${stem}\",\"want\":\"${want}\"}}" \
              && echo "↻ ${stem} 보충 발사(want=${want})" || echo "::warning::${stem} 보충 발사 실패(수동 '+N장 더' 폴백)"
          done
      - name: 큐레이션 즉시 전달 (Cloudflare 재빌드)
        if: always()
        env:
          CF_DEPLOY_HOOK: ${{ secrets.CF_DEPLOY_HOOK }}
        run: |
          [ -n "${CF_DEPLOY_HOOK}" ] || { echo "CF_DEPLOY_HOOK 미설정 — 즉시 재빌드 생략"; exit 0; }
          curl -fsS -X POST "${CF_DEPLOY_HOOK}" -o /dev/null && echo "Cloudflare 재빌드 트리거 — 썸네일 곧 반영" || echo "::warning::재빌드 트리거 실패"
