name: news-analyze

# pending/ 에 URL(.txt)이 들어오면 Claude Code 헤드리스로 큐레이션 다이제스트를 만들어 queue/ 에 커밋.
# - queue/ 커밋이 다시 트리거하지 않도록 paths 필터는 pending/** 만.
# - GITHUB_TOKEN 푸시는 기본적으로 워크플로를 재트리거하지 않음(이중 안전).
on:
  push:
    branches: [main]
    paths:
      - 'pending/**'
  workflow_dispatch:        # 수동 실행 — account 선택 시만 1회성 오버라이드(변수 안 건드림) · 무선택 = 활성계정(vars)
    inputs:
      account:
        description: '이 실행에 쓸 계정(구독 OAuth · 미선택 = 활성계정 자동)'
        type: choice
        # ⚠️ default 금지(평의회 260727 만장일치) — 구 `default: MUTENO`는 pick·pending-sweep·scrape·breaking-judge 의
        #   무입력 gh dispatch 에도 채워져 sticky 승격(vars.ACTIVE_ACCOUNT)을 가리고 승격 스텝(`!inputs.account`)을
        #   상시 스킵시켰다(소진기 런마다 폴오버 공회전). 삭제 = 전 호출부 diff 0 중앙 수리.
        #   options 첫 줄 = MUTENO(무default 수동 UI가 첫 옵션을 선선택해도 종전 수동 동작 보존).
        options:
          - MUTENO
          - NOMUTEFB
          - EMS1130G

# 동시 실행 충돌 방지 — ⚠️ 잠금은 '잡' 레벨(운영자 260717 대기열 기아 실측): 종전 워크플로(런) 레벨 그룹은
#   card_plan(상한 165분)이 끝날 때까지 런 전체가 그룹을 쥐고 있어, 그 사이 제출된 pending이 분석 착수조차
#   못 하고 스피너로 수 시간 대기(10~15분 실패 표면화 계약 파괴 · 실측 = 15:38 런의 card_plan이 17:28 제출 3건을
#   2.5h 인질). analyze 잡만 자기 그룹으로 직렬화하면 카드 생성 중에도 새 분석·실패 격리가 즉시 돈다.
#   card_plan(card-make)·thumb_gen(thumb-make)은 원래 자기 잡 그룹 보유 = 공유 자원별 직렬화는 종전과 동일.
#   그룹명 news-analyze → news-analyze-job 개명 = 구 런(런 레벨 잠금 보유 중)과의 전환기 교착 회피.

permissions:
  contents: write

jobs:
  analyze:
    runs-on: ubuntu-latest
    timeout-minutes: 90   # 6건 직렬 분석 대비(건당 timeout 900s — 콘텐츠 초안 포함이라 상향)
    concurrency:
      group: news-analyze-job   # pending 소비·queue/ 커밋 직렬화(위 헤더 주석 = 잡 레벨 사유)
      cancel-in-progress: false
    outputs:
      force_stems: ${{ steps.force.outputs.stems }}   # force 재분석(전문 직접 입력)으로 덮어쓴 카드 stem(공백 구분) → card_plan 이 단일 프롬프트 갱신(운영자 260628)
    steps:
      - name: Checkout
        uses: actions/checkout@v5   # Node24 (6/16 러너 디폴트 전환 대응)
        with:
          fetch-depth: 1   # 최신 1커밋만 — 구 0은 원격 브랜치 567개 전체 히스토리를 받아 실측 105s를 먹었다(260810 · imggen run 30972520521 = 체크아웃 108s 중 워킹트리 쓰기는 3s뿐)
          # ⚡ 260810 sparse-checkout(scrape 카나리아 실증 = 같은 잡 A/B 20s→9s · 삭제 커밋 0건) — 목록 = 이 잡이 실행줄에서 여는 폴더뿐.
          #   빠지는 대형 = _versions 437M · docs 90M · 자료 75M · _산출 74M(어느 잡도 안 연다) · ⚠ git add/rm 대상은 전건 포함 검문 통과분.
          sparse-checkout: |
            .github
            shared
            cards
            messages
            metrics
            pending
            queue
          # 트리거 커밋(github.sha)이 아니라 체크아웃 시점의 최신 main을 받는다.
          # concurrency 순차 큐에서 뒤 run이 옛 커밋을 체크아웃하면 앞 run이 이미
          # 처리한 pending을 재분석 → failed 로그 add/add 충돌로 push 실패(run#5 원인).
          ref: main

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: '24'

      # npm 캐시(운영자 260727 셋업 단축) — CLI 타르볼 재다운로드 회피(런당 ~1분↓ 추정). 미스 = 현행과 동일(무해).
      # ⚠️ 정적 키 금지(페이블 검토 260727) — primary key 적중 시 잡 종료 후 재저장이 없어 CLI 릴리스에 곧 stale
      #   (ly-make.yml "구 정적 키 = 드리프트" 정본 동형). run_id 가변 키 + restore-keys 접두 복원 = 매 런 최신 재저장.
      - name: npm 캐시 (셋업 단축)
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: npm-claude-code-${{ runner.os }}-${{ github.run_id }}
          restore-keys: |
            npm-claude-code-${{ runner.os }}-

      - name: Install Claude Code CLI
        run: npm install -g @anthropic-ai/claude-code

      - name: Analyze pending articles
        env:
          # 인증 = 구독 OAuth 토큰(API 키 미사용). 계정 동적 선택:
          #   수동 실행 inputs.account → 리포 변수 vars.ACTIVE_ACCOUNT → 기본 MUTENO(서브1=NOMUTEFB·서브2=EMS1130G·서브3=EMS1130M·쿼터 한도 시 3단 자동 폴오버)
          #   (secrets[format(...)] 동적 인덱싱·vars||기본값 = 공식 지원 문법)
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets[format('CLAUDE_CODE_OAUTH_TOKEN_{0}', inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO')] }}
          CLAUDE_CODE_OAUTH_TOKEN_ALT: ${{ (inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO') == 'NOMUTEFB' && secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130G || secrets.CLAUDE_CODE_OAUTH_TOKEN_NOMUTEFB }}   # 서브1 폴오버(활성 다음 우선순위 · 체인 MUTENO→NOMUTEFB→EMS1130G→EMS1130M)
          CLAUDE_CODE_OAUTH_TOKEN_ALT2: ${{ (inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO') == 'MUTENO' && secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130G || secrets.CLAUDE_CODE_OAUTH_TOKEN_MUTENO }}   # 서브2 폴오버(체인 3번째)
          CLAUDE_CODE_OAUTH_TOKEN_ALT3: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130M }}   # 서브3 폴오버(체인 실꼬리 = 항상 EMS1130M · 앞 3계정 소진 후)
          ACTIVE_ACCOUNT: ${{ inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO' }}
          SUMMARY_LEN_GUARD: ${{ vars.SUMMARY_LEN_GUARD || '' }}   # 분량 가드(IG/Thread 과소 시 1회 보강) — 기본 OFF·repo 변수 '1'로 카나리아→승격(§파이프라인 e · 260705)
          PREFLIGHT_TIMEOUT: '25'   # 계정 생존 핑 상한(초) 60→25(평의회 260727 신규6) — 산 계정 = 수초 응답이라 25s 충분, 죽은 계정 침묵 행 판정만 빨라짐(핑당 35s↓). 오판 스왑은 다음 기사 reset 이 복원 = 수십초 우회로 그침 · 롤백 = 이 줄 삭제
        run: |
          echo "활성 계정: ${ACTIVE_ACCOUNT}"
          if [ -z "${CLAUDE_CODE_OAUTH_TOKEN}" ]; then
            echo "::error::CLAUDE_CODE_OAUTH_TOKEN_${ACTIVE_ACCOUNT} 시크릿이 비어있음 — 등록 확인"; exit 1
          fi
          # 디버그: 값 노출 없이 잘림 여부만 배제(길이 + 표준 prefix 12자 — sk-ant-oat01 까지라 비민감)
          echo "token len=${#CLAUDE_CODE_OAUTH_TOKEN} prefix=${CLAUDE_CODE_OAUTH_TOKEN:0:12}"
          # OAuth를 조용히 덮어쓰는 변수 방어적 제거(anthropics/claude-code#34826)
          unset ANTHROPIC_API_KEY ANTHROPIC_AUTH_TOKEN
          bash .github/scripts/analyze.sh

      # force 재분석(전문 직접 입력)으로 덮어쓴 카드 stem 을 잡 output 으로 노출 → card_plan 이 그 카드 프롬프트만 갱신.
      # always = 위 분석이 일부 실패해도 성공한 force 건은 갱신 신호 전달. 비면 빈 문자열(card_plan 가드).
      - name: Force-refresh stems output
        id: force
        if: always()
        run: |
          s="$(tr '\n' ' ' < /tmp/force_regen_files.txt 2>/dev/null || true)"
          echo "stems=${s}" >> "$GITHUB_OUTPUT"

      # 활성 계정(vars.ACTIVE_ACCOUNT)이 이번 런에 쿼터로 폴오버됐으면 누적 카운트 → 2회+ 면 체인 다음
      # 계정으로 자동 전진(sticky failover). GH_TOKEN(Variables 쓰기 PAT) 없으면 no-op = 라이브 무해.
      # 수동 실행(inputs.account 오버라이드)은 변수 안 건드림(스킵). 정본 = docs/oauth_계정_자동승격_이식가이드.md
      - name: 활성 계정 자동 승격(쿼터 누적 시)
        if: ${{ always() && !inputs.account }}
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          ACTIVE_ACCOUNT: ${{ vars.ACTIVE_ACCOUNT || 'MUTENO' }}
        run: python3 shared/account_failover.py

      # viewer 빌드 스텝 없음(의도) — Cloudflare Pages가 push마다 `node build-viewer.mjs`를 직접 실행.
      # 커밋이 분석 직후·무조건이라 빌드류 실패가 Opus 분석물을 증발시킬 수 없다(run#2 교훈).
      - name: Commit results
        if: always()
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add queue pending
          git add metrics 2>/dev/null || true   # 토큰 계측 shard(claude_meter) 동반 커밋 — 있을 때만(없어도 무해)
          git add -A messages 2>/dev/null || true   # claude 시스템성 알림 = msg.py 가 messages/<id>.json(git 추적 = 빌드 입력)에 씀 → 배포 빌드가 viewer/messages.json 합성. -A = set/clear/24h prune 삭제까지 반영. (구 `git add viewer/messages.json` 은 gitignore 산출물이라 조용히 무시 = 알림 미반영 버그 260711 복구)
          if git diff --cached --quiet; then
            echo "변경 없음 — 커밋 생략"
            exit 0
          fi
          TITLES=$(cat /tmp/analyzed_titles.txt 2>/dev/null | paste -sd'; ' - || echo "기사")
          git commit -m "analyze: ${TITLES:-기사}"
          # 커밋 범위(queue·pending·metrics·messages) 밖 잔여 변경 = pull --rebase 사전 거부 원인
          # (260714 04:08 run 실측: "cannot pull with rebase: You have unstaged changes"로 4회 전패 → 분석물 로컬 커밋째 유실·pending 잔존).
          # 커밋은 이미 끝났으니 잔여물은 stash 대피(러너 일회용 = 폐기 무해) — 정체는 status 로그로 관측(재발 시 원인 파일 특정).
          echo "── 커밋 범위 밖 잔여물(있으면 아래 목록 = 유실 원인 후보) ──"; git status --porcelain | head -20
          git stash --include-untracked 2>/dev/null || true
          # 동시 푸시 경합 대비 rebase 재시도.
          # -X theirs = rebase 충돌 시 이 run의 분석 결과 우선(분석물 보존 최우선).
          # 실패하면 rebase --abort로 깨끗이 되돌린 뒤 재시도 — abort 없이 pull만 반복하면
          # "unmerged files" fatal로 전 재시도가 무력화된다(run#5 원인).
          for i in 1 2 3 4; do
            git fetch --deepen=100 origin main 2>/dev/null || true   # 얕은 클론(fetch-depth:1) 짝 — 원격 새 커밋과 로컬 HEAD 사이가 끊겨 merge-base를 못 찾으면 아래 rebase가 통째로 실패한다(= 산출 유실). main 100커밋만이라 수 초 · 실패해도 무해(260810)
            if git pull --rebase --autostash -X theirs origin main && git push origin HEAD:main; then exit 0; fi
            git rebase --abort 2>/dev/null || true
            sleep $((2**i))
          done
          echo "push 실패"; exit 1

      # 분석 완료 푸시 — 요약이 준비되면 구독자(프로필 알림 ON)에게 통지. 탭하면 그 요약 창이 열림(/?a=<파일>).
      # 비치명(실패해도 파이프 안 깸). 새 요약(analyzed_files) 있을 때만. 각 요약 건별 딥링크(≤PUSH_SUM_MAX=6) · 초과분만 묶음 피드.
      - name: 분석 완료 푸시 (요약 열기)
        if: success()
        env:
          VAPID_PRIVATE_KEY: ${{ secrets.VAPID_PRIVATE_KEY }}
          VAPID_SUBJECT: ${{ secrets.VAPID_SUBJECT }}
        run: bash .github/scripts/notify_summary.sh

      # 수집 실패 알림 — '내용이 안 들어와 대기열 미등록'을 준비된 시점에 웹푸시(탭→메시지함 실패 메시지).
      # always() = 잡이 빨개지는 Surface failures 전에 실행돼야 함(메시지함은 위 Commit 에서 이미 커밋됨).
      - name: 수집 실패 알림 (대기열 미등록 통지)
        if: always()
        env:
          VAPID_PRIVATE_KEY: ${{ secrets.VAPID_PRIVATE_KEY }}
          VAPID_SUBJECT: ${{ secrets.VAPID_SUBJECT }}
        run: bash .github/scripts/notify_fail.sh

      # 분석 실패가 있으면 잡을 빨갛게 → GitHub 푸시 알림 = '조용한 실패' 차단(Commit·푸시 스텝 이후라 성공분은 이미 저장됨).
      - name: Surface failures (실패 시 잡 빨갛게 → 알림)
        if: always()
        run: |
          if [ -s /tmp/analyzed_failures.txt ]; then
            N=$(wc -l < /tmp/analyzed_failures.txt)
            echo "::error::큐레이션 분석 실패 ${N}건 — pending/failed/ 로그 확인:"
            cat /tmp/analyzed_failures.txt
            exit 1
          fi
          echo "실패 없음"

  # 2단계 = 카드뉴스 프롬프팅(자동·텍스트만·제미나이 0). 다이제스트(analyze) 직후 별도 잡으로
  # 돌려 timeout 독립(분석 잡을 안 늘림). 제미나이/Zapier는 사용자 '슛'(card-make.yml full)에서만.
  card_plan:
    needs: analyze
    if: always()              # 분석 일부 실패해도 성공 큐는 카드 프롬프트 생성
    runs-on: ubuntu-latest
    timeout-minutes: 165      # 텍스트 카드 MAX_BATCH(3)건 × Opus — CARD_TIMEOUT 1500s(cardmake.sh)와 한 쌍{3건 × (1500+재시도1500) = 이론최악 150분 + 셋업·건별 커밋 헤드룸 15분(정확히 150이면 삼중행 시 마지막 카드가 잡 하드킬로 generating 림보 · 전반평의회②⑦ 260711) · 통상 3건 ~45분 · 최악 = 운영자 슛(card-make 그룹 공유)이 그만큼 직렬 대기 = 실패감소 우선 트레이드오프}
    concurrency:
      group: card-make        # 버튼 렌더(card-make.yml)와 같은 그룹 = 같은 cards/ 직렬화(-X theirs 무경고 덮어쓰기 방지)
      cancel-in-progress: false
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 1   # 최신 1커밋만 — 구 0은 원격 브랜치 567개 전체 히스토리를 받아 실측 105s를 먹었다(260810 · imggen run 30972520521 = 체크아웃 108s 중 워킹트리 쓰기는 3s뿐)
          ref: main           # analyze가 push한 최신 queue 포함
      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: '24'
      # npm 캐시(운영자 260727 셋업 단축) — analyze 잡과 동일 키 축 공유(무해 · 미스 = 현행 동일 · 가변 키 사유는 analyze 잡 주석)
      - name: npm 캐시 (셋업 단축)
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: npm-claude-code-${{ runner.os }}-${{ github.run_id }}
          restore-keys: |
            npm-claude-code-${{ runner.os }}-
      - name: Install Claude Code CLI
        run: npm install -g @anthropic-ai/claude-code

      - name: Card-plan (텍스트 카드 프롬프트 — 제미나이 0)
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets[format('CLAUDE_CODE_OAUTH_TOKEN_{0}', inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO')] }}
          CLAUDE_CODE_OAUTH_TOKEN_ALT: ${{ (inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO') == 'NOMUTEFB' && secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130G || secrets.CLAUDE_CODE_OAUTH_TOKEN_NOMUTEFB }}   # 서브1 폴오버(활성 다음 우선순위 · 체인 MUTENO→NOMUTEFB→EMS1130G→EMS1130M)
          CLAUDE_CODE_OAUTH_TOKEN_ALT2: ${{ (inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO') == 'MUTENO' && secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130G || secrets.CLAUDE_CODE_OAUTH_TOKEN_MUTENO }}   # 서브2 폴오버(체인 3번째)
          CLAUDE_CODE_OAUTH_TOKEN_ALT3: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130M }}   # 서브3 폴오버(체인 실꼬리 = 항상 EMS1130M · 앞 3계정 소진 후)
          ACTIVE_ACCOUNT: ${{ inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO' }}
          CARD_COV_GUARD: ${{ vars.CARD_COV_GUARD || '1' }}   # 카드 커버리지 가드(고신호 증발 시 1회 회수) — 기본 ON(승격 260802) · 끄기 = repo 변수 CARD_COV_GUARD를 '1' 외 값(off 권장)으로(§파이프라인 e)
          CARD_SYS_PROMPT: ${{ vars.CARD_SYS_PROMPT || '1' }}   # 카드 고정부(지침) 시스템 프롬프트 캐시 봉합 — 기본 ON(평의회 260803 · 실물 프로브 138K 히트) · 끄기 = repo 변수 CARD_SYS_PROMPT를 '1' 외 값으로
          # CARD_FAIL_RETRY_MAX 미주입(운영자 260722 · 평의회 P1) — 자동 재시도 봉인은 cardmake.sh 기본값 0이 단일 정본. repo 변수 stale('3' 등) 오버라이드 차단·수동(card-make)과 대칭. 재개는 cardmake.sh:36 기본값에서.
          # 🔒 Lock A — 이 잡엔 GDRIVE_SA_JSON·GEMINI_API_KEY env 자체가 없음(유료 생성 발사 불가). + cardmake text모드 unset(Lock B).
        run: |
          [ -n "${CLAUDE_CODE_OAUTH_TOKEN}" ] || { echo "토큰 없음 — 카드플랜 생략"; exit 0; }
          unset ANTHROPIC_API_KEY ANTHROPIC_AUTH_TOKEN GDRIVE_SA_JSON GEMINI_API_KEY
          # text 모드 = cards/ 미존재·지침 stale 큐만 골라 카드 MD까지(제미나이 0, state=text_done).
          bash .github/scripts/cardmake.sh all text || echo "::warning::카드플랜 일부 실패(다이제스트는 보존됨)"

      # force 재분석(전문 직접 입력)으로 요약이 바뀐 카드 = all 게이트가 GVER 동일이라 스킵 → 그 카드 프롬프트만 단일 갱신.
      # done/이미지(슛) 카드는 보호(슛 이미지·편집 보존 → 요약만 반영, 카드는 운영자 재촬영). news-revise.yml card_refresh 패턴 계승.
      - name: Force card refresh (전문 재제출로 바뀐 요약의 카드 프롬프트 갱신 — done/이미지 보호)
        if: ${{ needs.analyze.outputs.force_stems != '' }}
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets[format('CLAUDE_CODE_OAUTH_TOKEN_{0}', inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO')] }}
          CLAUDE_CODE_OAUTH_TOKEN_ALT: ${{ (inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO') == 'NOMUTEFB' && secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130G || secrets.CLAUDE_CODE_OAUTH_TOKEN_NOMUTEFB }}
          CLAUDE_CODE_OAUTH_TOKEN_ALT2: ${{ (inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO') == 'MUTENO' && secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130G || secrets.CLAUDE_CODE_OAUTH_TOKEN_MUTENO }}
          CLAUDE_CODE_OAUTH_TOKEN_ALT3: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_EMS1130M }}   # 서브3 폴오버(체인 실꼬리 = 항상 EMS1130M · 앞 3계정 소진 후)
          ACTIVE_ACCOUNT: ${{ inputs.account || vars.ACTIVE_ACCOUNT || 'MUTENO' }}
          CARD_COV_GUARD: ${{ vars.CARD_COV_GUARD || '1' }}   # 카드 커버리지 가드(고신호 증발 시 1회 회수) — 기본 ON(승격 260802) · 끄기 = repo 변수 CARD_COV_GUARD를 '1' 외 값(off 권장)으로(§파이프라인 e)
          CARD_SYS_PROMPT: ${{ vars.CARD_SYS_PROMPT || '1' }}   # 카드 고정부(지침) 시스템 프롬프트 캐시 봉합 — 기본 ON(평의회 260803 · 실물 프로브 138K 히트) · 끄기 = repo 변수 CARD_SYS_PROMPT를 '1' 외 값으로
          FORCE_STEMS: ${{ needs.analyze.outputs.force_stems }}   # env 경유 = run 스크립트 직접 인터폴레이션(주입) 회피
          # 🔒 Lock A — 이 잡엔 GDRIVE_SA_JSON·GEMINI_API_KEY env 없음(유료 발사 불가) + 아래 unset(Lock B) = 제미나이 0
        run: |
          [ -n "${CLAUDE_CODE_OAUTH_TOKEN}" ] || { echo "토큰 없음 — force 카드 갱신 생략"; exit 0; }
          unset ANTHROPIC_API_KEY ANTHROPIC_AUTH_TOKEN GDRIVE_SA_JSON GEMINI_API_KEY
          for stem in ${FORCE_STEMS}; do
            stem="${stem%.md}"
            [ -n "${stem}" ] || continue
            [ -f "queue/${stem}.md" ] || { echo "queue/${stem}.md 없음 — 생략"; continue; }
            st="$(grep -o '"state":[[:space:]]*"[^"]*"' "cards/${stem}/status.json" 2>/dev/null | cut -d'"' -f4)"
            imgs="$(ls cards/${stem}/*.jpg cards/${stem}/*.png cards/${stem}/scenes/*.jpg 2>/dev/null | head -1)"
            if [ "${st}" = "done" ] || [ "${st}" = "fired_partial" ] || [ -n "${imgs}" ]; then
              echo "force 카드 보호(state='${st:-?}', 이미지=${imgs:-없음}) — 자동 재생성 생략(요약은 반영됨·카드는 운영자 재촬영)"; continue
            fi
            echo "force 카드 프롬프트 갱신: ${stem}"
            bash .github/scripts/cardmake.sh "${stem}.md" text || echo "::warning::force 카드 갱신 실패(요약은 반영됨): ${stem}"
          done

      # 좀비 generating 차단 — 위 스텝이 크래시/중단되면 status가 영영 'generating'(모달 '프롬프팅 중' 영구).
      # always로 잔류 generating 을 failed 로 스윕·커밋해 모달이 멈추지 않게 한다.
      - name: Sweep stuck generating (좀비 → failed)
        if: always()
        run: |
          # 스윕 본문 = 공유 SSOT .github/scripts/sweep_stuck.sh(3 워크플로 공용 — 구 3벌 인라인이 fails·지침해시를 덮어쓰던 것 봉합 · 평의회 260711)
          bash .github/scripts/sweep_stuck.sh

      # 큐레이션 즉시 전달 — 분석·카드 커밋 후 Cloudflare 재빌드 1회 트리거 → 피드에 바로 반영
      # (정기 빌드[deploy-hook 12h] 안 기다림). 픽은 드물어 빌드 한도 영향 미미. 시크릿 없으면 no-op(다음 정기빌드).
      - name: 큐레이션 즉시 전달 (Cloudflare 재빌드)
        if: always()
        env:
          CF_DEPLOY_HOOK: ${{ secrets.CF_DEPLOY_HOOK }}
        run: |
          if [ -z "${CF_DEPLOY_HOOK}" ]; then
            echo "CF_DEPLOY_HOOK 미설정 — 즉시 재빌드 생략(다음 정기 빌드 때 피드 반영)"; exit 0
          fi
          if curl -fsS -X POST "${CF_DEPLOY_HOOK}" -o /dev/null; then
            echo "Cloudflare 재빌드 트리거 — 큐레이션 결과 곧 피드 반영"
          else
            echo "::warning::재빌드 트리거 실패(다음 정기 빌드 때 반영)"
          fi

  # 3단계 = AI 썸네일 후보 2화풍(픽한 기사만·자동). 카드(외부 Apps Script/Drive)와 분리된
  # 레포 내 경로 = Actions가 Gemini를 직접 호출, 한국어 글자도 이미지에 직접 렌더(compose 불필요).
  # ⚠️ 비용잠금(Lock A) 해제 지점 = 픽=Gemini 2장 자동과금. GEMINI_API_KEY 시크릿 없으면 no-op(스캐폴드).
  thumb_gen:
    needs: analyze
    if: always()              # 분석 일부 실패해도 성공 큐는 썸네일 생성
    runs-on: ubuntu-latest
    timeout-minutes: 30
    concurrency:
      group: thumb-make       # card-make와 분리(card_plan과 강제 직렬·pending 취소 방지). cards/ 쓰기 경합은 각 잡 rebase -X theirs가 흡수(파일 비중첩).
      cancel-in-progress: false
    permissions:
      contents: write
      actions: write          # 검색이미지 미달 보충 = moreimg workflow_dispatch(GITHUB_TOKEN은 dispatch류만 재트리거 허용 축 · 운영자 260726)
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 1   # 최신 1커밋만 — 구 0은 원격 브랜치 567개 전체 히스토리를 받아 실측 105s를 먹었다(260810 · imggen run 30972520521 = 체크아웃 108s 중 워킹트리 쓰기는 3s뿐)
          ref: main           # analyze가 push한 최신 queue 포함
      - name: 썸네일 후보 — 검색이미지(og:image) + AI 2화풍 생성 (신규 픽만)
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          # ✅ AI 썸네일 2화풍(포토에디토리얼·극화) 생성 ON(운영자 260630 재가동 — 260622 임시 OFF 해제).
          #    일시 차단하려면 THUMB_AI_OFF: '1' 추가(검색이미지는 유지·키 무관). 수동 '다시 만들기' thumb-redo.yml은 항상 AI.
          # ⚠️ Google CSE JSON API = 2025 신규고객 차단(死) → 폐기. 검색이미지는 기사 og:image 추출(망만 필요, 키 무관).
          # 저장소 = Cloudflare R2(설정 시 공개 URL 서빙·레포 비대 회피). 5개 다 있어야 R2, 아니면 git 폴백.
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
          R2_PUBLIC_BASE: ${{ secrets.R2_PUBLIC_BASE }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          THUMB_MAX_BATCH: '3'
          THUMB_SINCE: '260703'   # 활성화 기준일 — 4화풍 재편(260703)부터. 260619~0702 백로그는 2화풍 유지(4화풍 소급 재과금 차단 — 원하면 날짜 내리기).
          THUMB_REF: '1'          # 참조 체이닝 승격(260703 카나리아 통과 — 극화·수채가 대표 실사진으로 실제 얼굴 재현). photo 제외·사인/피해자/미성년 익명 프롬프트 유지. 롤백=이 줄 제거.
        run: |
          # AI_OFF면 키 없어도 검색이미지는 돌린다(검색=망 fetch·키 무관). 평소(AI on)엔 키 없으면 스캐폴드 no-op.
          if [ -z "${GEMINI_API_KEY}" ] && [ "${THUMB_AI_OFF}" != "1" ]; then
            echo "GEMINI_API_KEY 미설정 — 썸네일 생성 생략(스캐폴드)"; exit 0
          fi
          pip install --quiet Pillow 2>/dev/null || pip3 install --quiet Pillow 2>/dev/null || echo "Pillow 설치 실패 — 매체 로고필터 no-op(검색이미지 무회귀)"
          python3 .github/scripts/thumb_gen.py || echo "::warning::썸네일 생성 일부 실패(다이제스트·카드는 보존됨)"
      - name: Commit thumbnails
        if: always()
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add cards 2>/dev/null || true
          if git diff --cached --quiet; then echo "썸네일 변경 없음"; exit 0; fi
          git commit -m "thumbs: AI 썸네일 후보 생성"
          for i in 1 2 3 4; do
            git fetch --deepen=100 origin main 2>/dev/null || true   # 얕은 클론(fetch-depth:1) 짝 — 이 잡은 analyze와 **별도 러너·별도 클론**이라 앞 잡의 경계 확장이 안 미친다. 경계가 끊기면 merge-base 실종 = rebase 실패 = 썸네일 산출 유실(260810)
            git pull --rebase --autostash -X theirs origin main && git push origin HEAD:main && exit 0
            git rebase --abort 2>/dev/null || true; sleep $((2**i))
          done
          echo "::warning::썸네일 push 실패"
      # 검색이미지 7장 미달·원문 url 부재 건 = moreimg 자동 보충(운영자 260726 "원래 7개" + "검색으로 원문 URL 공유에 링크").
      # 대상 = thumb_gen.py가 RUNNER_TEMP/thumb_topup.txt 마커로 지목(이번 런 신규 기록분만). 커밋·push *뒤* 발사 =
      # moreimg 체크아웃이 이번 search.json 포함(즉발이면 add/add 경합). 실패 = 경고만(수동 '+N장 더' 폴백 불변) · 중단 = 이 스텝 제거.
      - name: 검색이미지 미달 자동 보충 (moreimg 디스패치)
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          F="${RUNNER_TEMP:-/tmp}/thumb_topup.txt"
          [ -s "$F" ] || { echo "보충 대상 없음"; exit 0; }
          sort -u "$F" | while read -r stem want; do
            [ -n "$stem" ] || continue
            curl -fsS -X POST -H "Authorization: Bearer ${GH_TOKEN}" \
              -H "Accept: application/vnd.github+json" -H "X-GitHub-Api-Version: 2022-11-28" \
              "https://api.github.com/repos/${GITHUB_REPOSITORY}/actions/workflows/moreimg.yml/dispatches" \
              -d "{\"ref\":\"main\",\"inputs\":{\"stem\":\"${stem}\",\"want\":\"${want}\"}}" \
              && echo "↻ ${stem} 보충 발사(want=${want})" || echo "::warning::${stem} 보충 발사 실패(수동 '+N장 더' 폴백)"
          done
      - name: 큐레이션 즉시 전달 (Cloudflare 재빌드)
        if: always()
        env:
          CF_DEPLOY_HOOK: ${{ secrets.CF_DEPLOY_HOOK }}
        run: |
          [ -n "${CF_DEPLOY_HOOK}" ] || { echo "CF_DEPLOY_HOOK 미설정 — 즉시 재빌드 생략"; exit 0; }
          curl -fsS -X POST "${CF_DEPLOY_HOOK}" -o /dev/null && echo "Cloudflare 재빌드 트리거 — 썸네일 곧 반영" || echo "::warning::재빌드 트리거 실패"
