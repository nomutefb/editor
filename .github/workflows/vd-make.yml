name: vd-make

# 영상 스튜디오 「큐영상」 탭(Pages Function /api/vd)이 발사 — 단일 잡:
#   queue/<file>.md → video/build.mjs(팔레트·행 산출) → hyperframes 렌더(1080×1920·6s)
#   → R2 vd_res/<id>/<file>.mp4 → viewer/vd_out/<id>/video.json 커밋(폼이 폴링).
# LLM 0콜 · 업로드 0(소스가 이미 레포 안) · 골격 = conv-make 미러.
on:
  workflow_dispatch:
    inputs:
      id:
        description: '작업 id(타임스탬프-hex6)'
        type: string
      files:
        description: '큐 파일명 쉼표 목록(확장자 없이 · 최대 12건 · api/vd.js가 정규식 검증)'
        type: string
        default: ''
      opts:
        description: '효과 컨트롤 JSON {"format":"mp4|mov|webm|gif","res":"portrait|portrait-4k","fps":"24|30|60","quality":"draft|standard|high","pack":"off|on"} — api/vd.js 화이트리스트 통과분'
        type: string
        default: '{}'

concurrency:
  group: vd-make-${{ inputs.id }}
  cancel-in-progress: false

permissions:
  contents: write
  checks: read   # 배포 게이트(deploy-then-notify) — Pages 체크런 조회(conv/edit-make 정본 계승 · §완료알림 기틀)

jobs:
  vd:
    runs-on: ubuntu-latest
    timeout-minutes: 40   # 최악 스택{환경 ~10 + 렌더 25} + 커밋 창 — 무한 행만 차단(§9 · conv 문법)
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 1   # 최신 1커밋만 — 구 0은 원격 브랜치 567개 전체 히스토리를 받아 실측 105s를 먹었다(260810 · imggen run 30972520521 = 체크아웃 108s 중 워킹트리 쓰기는 3s뿐)
          # ⚡ 260810 sparse-checkout(scrape 카나리아 실증 = 같은 잡 A/B 20s→9s · 삭제 커밋 0건) — 목록 = 이 잡이 실행줄에서 여는 폴더뿐.
          #   빠지는 대형 = _versions 437M · docs 90M · 자료 75M · _산출 74M(어느 잡도 안 연다) · ⚠ git add/rm 대상은 전건 포함 검문 통과분.
          sparse-checkout: |
            .github
            shared
            apps
            viewer

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: '22'   # hyperframes 요구 = Node 22+

      - name: 렌더 환경 (hyperframes + ffmpeg)
        timeout-minutes: 10
        uses: ./.github/actions/runner-setup
        with:
          setup: video/setup.sh
          apt: ffmpeg

      - name: 렌더 (queue → MP4)
        timeout-minutes: 25   # 스텝 자체 캡 — 잡 40분 하드캔슬 전에 failure() error.log 표면화(conv 문법)
        env:
          IN_ID: ${{ inputs.id }}
          IN_FILES: ${{ inputs.files }}
          IN_OPTS: ${{ inputs.opts }}
        run: |
          set -e
          [ -n "$IN_ID" ] || { echo "::error::id 없음"; exit 1; }
          case "$IN_ID" in *[!0-9a-f-]*) echo "::error::잘못된 id"; exit 1;; esac
          # api/vd.js 정규식과 동형 자체 재검증(상류 신뢰 대신 최후 방어선 · conv r2_src 선례)
          printf '%s' "$IN_FILES" | grep -Eqx '[0-9]{6}-[0-9]{4}-[A-Za-z0-9._-]{1,100}(,[0-9]{6}-[0-9]{4}-[A-Za-z0-9._-]{1,100}){0,11}' \
            || { { echo "고른 기사가 이상해 — 다시 골라줘."; [ -s /tmp/yt_diag.txt ] && cat /tmp/yt_diag.txt || true; } > /tmp/vd_err.txt; echo "::error::잘못된 files: $IN_FILES"; exit 1; }
          case "$IN_FILES" in *..*) echo "::error::경로 탈출 시도"; exit 1;; esac

          cd video
          . ./env.sh
          node build.mjs --files "$IN_FILES"
          COUNT=$(node -e "console.log(require('./.build/rows.json').length)")
          [ "$COUNT" -gt 0 ] || { { echo "고른 기사에 훅·제목이 없어 — 요약이 끝난 기사를 골라줘."; [ -s /tmp/yt_diag.txt ] && cat /tmp/yt_diag.txt || true; } > /tmp/vd_err.txt; echo "::error::rows 0"; exit 1; }
          echo "렌더 대상 ${COUNT}건"
          rm -rf out && mkdir -p out

          # 효과 컨트롤 → render 플래그. 상류(api/vd.js)를 신뢰하지 않고 여기서 한 번 더 화이트리스트(conv 선례).
          OPTS_LINE=$(node -e 'const o=JSON.parse(process.env.IN_OPTS||"{}");const p=(v,a,d)=>a.includes(String(v))?String(v):d;console.log([p(o.format,["mp4","mov","webm","gif"],"mp4"),p(o.res,["portrait","portrait-4k"],"portrait"),p(o.fps,["24","30","60"],"30"),p(o.quality,["draft","standard","high"],"standard")].join(" "))')
          set -- $OPTS_LINE
          FMT=$1; RES=$2; FPS=$3; QUA=$4
          echo "효과 컨트롤: format=$FMT resolution=$RES fps=$FPS quality=$QUA"

          # --workers 1 고정 = 2 이상이면 청크 경계 프레임 1장이 t=0 상태로 깨진다(실측 260801 · video/README §함정)
          # ⚠ 실측 260802: --resolution 은 알파 경로(mov/webm/gif)와 결합 불가(CLI가 컴파일 단계서 거부).
          #    컴포지션이 이미 1080×1920이라 portrait는 플래그 자체가 불필요 → 4K + mp4 일 때만 얹는다.
          RESFLAG=""
          [ "$RES" = "portrait-4k" ] && [ "$FMT" = "mp4" ] && RESFLAG="--resolution portrait-4k"
          [ "$RES" = "portrait-4k" ] && [ "$FMT" != "mp4" ] && echo "::warning::4K는 MP4에서만 — ${FMT}는 1080으로 렌더"
          node_modules/.bin/hyperframes render . --workers 1 \
            --format "$FMT" $RESFLAG --fps "$FPS" --quality "$QUA" \
            --batch .build/rows.json -o "out/{name}.$FMT" \
            || echo "::warning::배치 렌더가 비정상 종료 — 산출 개수로 재판정"
          ls -la out || true
          MADE=$(find out -maxdepth 1 -type f ! -name 'manifest.json' 2>/dev/null | wc -l)
          # 부분 실패 관측(로컬 4회 중 1회 · 원인 미규명) = 전량 실패만 잡을 실패로, 부분은 만든 것만 내보내고 경고.
          [ "$MADE" -gt 0 ] || { { echo "영상이 하나도 안 만들어졌어 — 다시 시도해줘."; [ -s /tmp/yt_diag.txt ] && cat /tmp/yt_diag.txt || true; } > /tmp/vd_err.txt; echo "::error::산출 0/${COUNT}"; exit 1; }
          [ "$MADE" -eq "$COUNT" ] || echo "::warning::부분 산출 ${MADE}/${COUNT} — 만들어진 것만 내보낸다"

      - name: 숏폼 팩 동봉 (opts.pack=on)
        timeout-minutes: 3
        env:
          IN_FILES: ${{ inputs.files }}
          IN_OPTS: ${{ inputs.opts }}
        run: |
          set -e
          # 5축 「숏폼 팩」 = 렌더 플래그가 아니라 별개 산출 — 같은 큐 파일로 대본·자막·Seedance 샷 프롬프트 .md 를 뽑아
          # video/out 에 얹으면 아래 R2 루프가 mp4와 같이 걷어가 같은 다운로드 칸에 뜬다(LLM 0콜 = 결정론 조립).
          PACK=$(node -e 'const o=JSON.parse(process.env.IN_OPTS||"{}");console.log(o.pack==="on"?"on":"off")')
          [ "$PACK" = "on" ] || { echo "숏폼 팩 = 안 함(스킵)"; exit 0; }
          # 전 구간 fail-soft — 팩이 안 나와도 영상 납품은 그대로(vd '만들어진 것만 내보낸다' 축 동형)
          python3 apps/shorts/make_shorts_pack.py --files "$IN_FILES" --out video/out \
            || echo "::warning::숏폼 팩 생성 실패 — 영상만 내보낸다"
          ls -la video/out || true

      - name: R2 업로드 → video.json
        timeout-minutes: 8
        env:
          IN_ID: ${{ inputs.id }}
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
          R2_PUBLIC_BASE: ${{ secrets.R2_PUBLIC_BASE }}
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
        run: |
          set -e
          case "$IN_ID" in *[!0-9a-f-]*) echo "::error::잘못된 id"; exit 1;; esac
          [ -n "$R2_BUCKET" ] || { { echo "저장소(R2)가 설정 안 돼 있어 — 관리자 설정 후 다시."; [ -s /tmp/yt_diag.txt ] && cat /tmp/yt_diag.txt || true; } > /tmp/vd_err.txt; echo "::error::R2 시크릿 미설정"; exit 1; }
          export AWS_DEFAULT_REGION=auto AWS_REQUEST_CHECKSUM_CALCULATION=when_required AWS_RESPONSE_CHECKSUM_VALIDATION=when_required
          mkdir -p "viewer/vd_out/${IN_ID}"
          : > /tmp/vd_urls.txt
          for f in video/out/*; do
            [ -f "$f" ] || continue
            b=$(basename "$f")
            case "$b" in manifest.json) continue;; esac
            case "${b##*.}" in mp4) CT=video/mp4;; mov) CT=video/quicktime;; webm) CT=video/webm;; gif) CT=image/gif;; md) CT='text/markdown; charset=utf-8';; *) CT=application/octet-stream;; esac
            aws s3 cp "$f" "s3://$R2_BUCKET/vd_res/${IN_ID}/${b}" --content-type "$CT" \
              --endpoint-url "https://${R2_ACCOUNT_ID}.r2.cloudflarestorage.com" --only-show-errors \
              || { { echo "결과 업로드에 실패했어 — 다시 시도해줘."; [ -s /tmp/yt_diag.txt ] && cat /tmp/yt_diag.txt || true; } > /tmp/vd_err.txt; echo "::error::R2 업로드 실패: $b"; exit 1; }
            echo "${R2_PUBLIC_BASE}/vd_res/${IN_ID}/${b}" >> /tmp/vd_urls.txt
          done
          [ -s /tmp/vd_urls.txt ] || { { echo "만들어진 영상이 없어."; [ -s /tmp/yt_diag.txt ] && cat /tmp/yt_diag.txt || true; } > /tmp/vd_err.txt; echo "::error::산출 0"; exit 1; }
          node -e '
            const fs = require("fs");
            const files = fs.readFileSync("/tmp/vd_urls.txt", "utf8").split("\n").filter(Boolean);
            const out = { id: process.env.IN_ID, generated: new Date().toISOString(), files };
            fs.writeFileSync("viewer/vd_out/" + process.env.IN_ID + "/video.json", JSON.stringify(out, null, 2) + "\n");
          '
          cat "viewer/vd_out/${IN_ID}/video.json"

      - name: 실패 기록(error.log) — 프론트가 폴링해 즉시 표시
        if: failure()
        env:
          IN_ID: ${{ inputs.id }}
        run: |
          [ -n "$IN_ID" ] || exit 0
          case "$IN_ID" in *[!0-9a-f-]*) exit 0;; esac
          mkdir -p "viewer/vd_out/${IN_ID}"
          if [ -s /tmp/vd_err.txt ]; then MSG=$(cat /tmp/vd_err.txt); else MSG="영상 만들기에 실패했어 — Actions 로그를 확인해줘."; fi
          export MSG
          node -e '
            const fs = require("fs");
            fs.writeFileSync("viewer/vd_out/" + process.env.IN_ID + "/video.json",
              JSON.stringify({ id: process.env.IN_ID, error: process.env.MSG }, null, 2) + "\n");
          '
          echo "error: $MSG"

      - name: Commit output
        if: always()
        env:
          IN_ID: ${{ inputs.id }}
          BRANCH: ${{ github.ref_name }}
        run: |
          [ -n "$IN_ID" ] || { echo "id 없음 — 커밋 스킵"; exit 0; }
          case "$IN_ID" in *[!0-9a-f-]*) echo "잘못된 id — 커밋 스킵"; exit 0;; esac
          git config user.name "nomute-bot"
          git config user.email "bot@nomute.local"
          git add "viewer/vd_out/${IN_ID}" 2>/dev/null || true
          git commit -m "vd: ${IN_ID} 큐영상 출력" || { echo "커밋할 것 없음"; exit 0; }
          pushed=0
          for i in 1 2 3 4 5; do
            git fetch --deepen=100 origin main 2>/dev/null || true   # 얕은 클론(fetch-depth:1) 짝 — 원격 새 커밋과 로컬 HEAD 사이가 끊겨 merge-base를 못 찾으면 아래 rebase가 통째로 실패한다(= 산출 유실). main 100커밋만이라 수 초 · 실패해도 무해(260810)
            if git fetch origin "$BRANCH" && git rebase "origin/$BRANCH" && git push origin "HEAD:$BRANCH"; then pushed=1; break; fi
            git rebase --abort 2>/dev/null || true; sleep $((i*3))
          done
          [ "$pushed" = 1 ] || { echo "::error::push ${BRANCH} 5회 실패 — 출력 유실"; exit 1; }
