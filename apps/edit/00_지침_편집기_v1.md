# 편집기(edit) 지침 v1 — 작은 영상 편집기(업로드 1번·1잡 · 골격 B 레시피+처리 스택)

> **260712 2차(운영자)**: ① 레시피 4칩 = **프리뷰 좌하단 `Preset` 여닫이 오버레이로 이관**(`#pvPre` — 글자만·도형 0·wmsg 그림자 · 탭 = 촤르륵 펼침[platpop 스태거 계승]·선택 = `recipe()`+역스태거 접힘·바깥 탭 = 접힘 · 영상 캔버스 좌변+12px 정렬 = `pvSync` · 구 `.recrow` 행 폐지 = CSS 도먼트·롤백 = 행 복원 · **첨부 전엔 프리뷰가 없어 프리셋도 비노출** = 첨부 후 흐름 전제) ② **프리뷰 음영 제거** = `.pvslot .pvstage` 배경·테두리 투명(모달 투명 body 위 솔리드 패널이 음영으로 읽힘 — 캔버스 순검정 = 영상 경계 유지) ③ **스크롤바 숨김** = `scrollbar-width:none`(트렌드 레인 계승·스크롤 기능 불변). PR #2003(`5e9daae`) · 백업 `_versions/260712_0952_편집기_프리셋오버레이/`.
>
> 진입 = 뷰어 `+ > 영상(영상 스튜디오) > 편집` 탭(최좌측 · **3탭 세그{편집·자막·프롬프팅} — 260711 3차 통합 확정**{2차 5탭을 운영자 *영상 통합 3분류*가 뒤집음 · 트래킹 = 아래 기타 기능 핸드오프 서브뷰 · 변환 = 재도먼트}). 과금 = 자막 의역(claude 구독)만 · 나머지 순수 ffmpeg/Whisper(Actions 분 + R2).
>
> **260711 3분류 배선(운영자 플레이그라운드 선택값 — 정본 = `docs/작업이력.md` 260711)**: **자막(sub) 카드 = 자막 탭 전담 이관**(m1Sub=m2 — ORDER서 제거 · SUBPRE·음영 3축·자막 상세 편집기·reburn 코드 = 도먼트 보존 · 복원 = ORDER에 'sub' 한 줄) · 레시피 = 자막 제외 4칩{릴스 완성(컷+9:16 블러+음량)·포맷만·화질만·쇼츠 클립(클리퍼)} ★표시 제거 · 비율 기본 방식 = 여백(블러) · 미리보기 = 비율 카드 귀속(pvSlot) · 크롭 드래그 버튼 미노출(게이지만 · scrub 코드 보존) · 생성형 채움 칩 = 노출·비활성(dim — 영상 생성 API 미보유 = 준비중) · 발사 = 폼 끝 인라인(하단 고정 폐지 · `.go.glive` 게이지 불변 · 40px) · ETA 병기 제거(etaCalc 보존) · 소스 행 상단 고정 + `≤2GB` 병기 · URL 붙여넣기 버튼 hidden · 클리퍼 후보 적용 = 구간+비율+음량(자막 제외) · **기타 기능 섹션 신설**(스택 밖 — 모자이크/핀셋/키잉 칩 + 모양·크기·페더·키잉 페더 인라인 → [트래킹에서 이어서] = `nm_track_preset` 일회용 핸드오프 · 발사 옵션 아님 = 트래킹 파이프 재사용 · **260712 2차 확장(운영자 `한수 ㄱㄱ`) = 실루엣·크로마키 카드 · 행배치 규칙(운영자 260712) = 관련 계열끼리만 가로 2단{모자이크·실루엣 = 가림 / 키잉·크로마키 = 분리}·트래킹 = 세로 단독(우측 빈 슬롯 `.cysp`)·ON/OFF 전환 = `.cyv` min-width 3ch 폭 고정(구 3분할 = 4자 라벨 70px wrap 무너짐 실측 → 2단 40px 불변)**{실루엣 = 채움(블록↔가면 칩 3종)+페더 · 크로마 = 색(그린/블루)·강도·테두리·부드럽게 · 프리셋에 fill·preset·sfe·ck 동봉 → track.html 소비 대칭(mode 5종) · 다중 ON 발사 = 크로마>실루엣>키잉>핀셋>모자이크 우선 — 서브뷰서 모드 재전환 자유}) · 지오메트리 = 칩 34px(--btn)/13px(--fs-label)·레시피 30px(--btn-sm)·카드 --r-s·좌우 12px 등.

## 1. 동작 (운영자 확정 — 260710)
- a) **흐름**: 소스{파일 ≤2GB — R2 직업로드(픽토 슬롯 · **좌측 = 더 기본** · 운영자 확정 · R2 미바인딩 = ≤30MB 폴백 · 260711) 또는 URL(우측 · yt-dlp)} → 레시피 원탭 4칩{릴스 완성 = 컷+9:16 여백(블러)+음량 / 포맷만 = 9:16 여백(블러)+음량 / 화질만 = 60fps 보간+음량 / 쇼츠 클립 = 클리퍼 배타 — 자막 제외·★ 없음 · 260711 2차} 또는 [+ 처리 추가] 바텀시트(64%) → **처리 스택**(카드 = 적용 순서 고정 · 탭=펼침 · X=제거) → 폼 끝 인라인 [편집 시작](40px · ETA 병기 없음 · 게이지 .go.glive 불변 · 260711 2차) → `edit-make.yml` 1잡 → R2 → `viewer/ly_out/<id>/video.json` 폴 → 결과 영상+note+`.dlbtn`.
- a-1) **완료·실패 웹푸시(운영자 260711)**: 잡이 길어서(60i 보간 수십 분) 창 닫아도 통지 — `edit-make.yml` 완료 푸시 스텝(`push_send.py --notify` · thumb 제작완료 문법 · 비치명)이 성공 = `편집 완료` / 실패 = `편집 실패 — 사유 60자`를 구독자(프로필 알림 ON)에게 발송. 탭 = `/edit.html#done=<id>` 딥링크 → 그 작업 결과 직행(pend보다 우선) · 잡별 고유 tag = 알림 누적. 구독 OFF·VAPID 미설정 = 조용히 생략.
- b) **처리 축(활성 카드 8종 · 서버 순서 고정 — 자막(sub) = 자막 탭 전담 이관·도먼트 보존 260711 2차{아래 자막 상술 = 도먼트 코드 정본·복원 = ORDER 'sub' 한 줄})**: 자막(STT+claude 의역+ASS 번인 — **프리셋 3칩{속보체=박스+키워드 · 예능체=팝+큰글씨 · 담백체=깔끔 무강조} = 기존 토글·게이지 조합 원탭 점등(신규 렌더 축 0 · 운영자 260711)** + 언어4·톤2·군더더기·모양3·카라오케/팝/키워드·배경/위치/크기 게이지 + **음영 3축(운영자 260711 "주변부 음영 크기·색·폰트")**{음영 크기 게이지 25~300%[**100% = 종전 편집기 렌더 바이트 그대로**(outline ×1.0·pad 0.10 = 미전송 기본과 동일 = 기존 기능 불변) · bg=0 외곽선 배율 `outline` + bg>0 줄박스 패딩 `pad` 한 손잡이] · 음영 색 7칩[검정 기본·흰·그린·핑크·블루·레몬·레드 = ly_burn `OC_BGR` 닫힌 집합 — 외곽선/그림자/줄박스 단일 축·콘텐츠 산출물 색 §핵심명령 3-b-1] · 폰트 4칩[기본 고딕·명조·나눔고딕·손글씨 = `FONT_FAMILY` 닫힌 집합 · 러너 apt fonts-nanum(-extra) = edit-make·ly-make 자막 경로 · 미설치 = 고딕 폴백+note] — 라이브 미리보기(d-1)가 3축 실시간 미러 · 재입히기(`lyOptsNow`)도 동일 승계}) · 배경음 제거(Demucs) · 컷 편집(무음 컷 · 세기 3 · **필러 컷(cutfill)·테이크 컷(take) 동반 가능 = 260727 아래 b-2** · **자막 없이도 가능 = STT-only 260711**{전사(Whisper)만 돌려 스팬으로 컷 · claude 의역 0·번인 0 — 뷰어가 `burn:false` 명시 = ly_burn 번인 억제 신호 · 전사 실패 = 컷만 건너뛰고 잡 살림(fail-soft) · 컷 *단독* 스택에서 전사 실패 = 정직 에러}) · 구간(트림 — **자막과 동시 지원 260711**: 트림을 컷·자막보다 선확정하고 자막 조각·word·전사 스팬을 트림 시간축으로 동행 리맵{구간 밖 드롭·경계 클립·구간 내 자막 0 = note} = 컷 remap과 동일 정신) · 비율(9:16/1:1/4:5/16:9 × 자르기/여백(검정 중앙)/**여백(블러) = 원본 블러 확대 배경 패드{숏폼 표준 미감 · ffmpeg split+boxblur+overlay · 생성 0 = 사실왜곡 0 · 260711}**) · 해상도{`원본(4K)`=캡 3840 · 1080 · 720 — 카드 없음 = 종전 1920 캡 · 침묵 다운스케일은 note 표면화(260711)} · 프레임(60fps 보간·30·24 — 보간은 1080p까지 · 4K = 정직 스킵 note) · 음량 통일(−14LUFS·TP−1.5·L/R = `shared/audio_norm.py` SSOT) · **클리퍼(아래 b-1 — 배타 스캔)**.
- b-1) **클리퍼(clip · 쇼츠 클리퍼 v0 — 운영자 260711 승인)**: 긴 클립(≤10분)에서 쇼츠 하이라이트 **후보 3~5개 제안** — 자동 발행 없음(track 분석→확정 미러). 흐름 = 클리퍼 카드 단독 ON(배타 — 다른 카드 자동 해제·api도 정규화) → 발사 = STT(whisper) → `clipmake.sh`(claude 구간픽 1콜 · `prompts/clip-make.md` = 문장 경계 스냅·15~60초 지시{파서 하드넷은 보수 여유 6~90초 = 지시 밖 후보도 운영자 확정이 거름 · 검증⑩ P4}·맥락 왜곡 금지(부정어 절단·반쪽 질문·오귀속 경계 금지 = 사실 무결성)·폴오버 SSOT 경유) → `viewer/ly_out/<id>/clips.json`{v·ts·dur·src·clips[{s,e,title,why}]} → 뷰어 후보 목록 → **후보 탭 = 구간(t0/t1)+쇼츠 기본 스택(자막·9:16·음량) 자동 적용 → 운영자가 스택 확인 후 일반 렌더 발사**(렌더 = 기존 트림 레일 그대로 = 신규 렌더 경로 0). 재렌더 소스 = URL 소스면 원 URL 승계 · 파일 업로드면 스캔 잡이 R2 `ly_out/<id>/src.*` 보관(≤400MB · 실패 = 파일 재선택 fail-soft — 뷰어가 후보 적용 시 소스 공백을 선제 경고) → clips.json `src`. **보관 잔존물 정책(정직 · 검증⑩ P1)**: `ly_out/<id>/src.*`는 재렌더용으로 잡 이후에도 R2에 남는다 — v0 = 자동 정리 없음(ly 재합성 src 보관과 동일 계열 · 미확정 후보의 고아 포함). 후속 = R2 lifecycle(`ly_out/*/src.*` N일 만료) 또는 수동 정리 대상.
- b-2) **컷 강화 3종(운영자 260727 — IG '러프컷' 대비 격차 반영)**: ① **필러 컷**(`cutfill` · 컷 카드 서브 토글) = segments.json **어절 타임스탬프 재사용**(신규 모델·API 0)로 감탄·주저음을 발화 *안*에서 제거 — 2단 사전{HARD = 어·음·엄·에·흠·uh/um 등 = 단독 컷 / SOFT = 그·이제·뭐·좀·막 등 = **주저 갭 게이트**('그 사람'의 관형사 오컷 방어)} · 어절 ≤1.5초만 · 정본 = `ly_burn.filler_scan`(컴포지터·스캔 공용 1벌). **강도 = 무음 컷 `cutlv` 계승**(운영자 260728 · 신규 UI 0 · `FILLER_LEVELS`): 살짝 = SOFT 전면 보존{꼬리 0.30·천장 8%} / **기본 = 갭 0.18s·꼬리 0.50·천장 12%(현행값 = 회귀 0)** / 많이 = 갭 0.10·꼬리 0.70·천장 20%. **기본값 근거 = 레포 실코퍼스 실측**(`viewer/ly_out` 24작업·3,798어절·37.3분): SOFT 80회의 앞뒤 최대 갭 = 중앙값 0.00·90%지점 0.14·최대 0.64초 → 0.18s가 그중 8%(6/80 = 확실히 뜸 들인 것)만 집는다 · 강도별 산출 = 살짝 4곳 1.7초 / 기본 10곳 4.2초 / 많이 15곳 5.1초 → 임계를 더 낮춰도 얻는 건 1초 남짓·오컷 위험만 상승 = 기본이 최적. ⚠ **필러 컷의 진짜 천장은 임계가 아니라 전사다** — 같은 코퍼스에서 HARD 필러는 3,798어절 중 **4개**(Whisper large-v3가 군말을 거의 안 적음 · 24작업 중 필러 적중 2건). 임계 조정으로 못 넘는 선. ② **테이크 컷**(`take`) = claude 1콜(`prompts/take-make.md` → `.github/scripts/takemake.sh` = clipmake 미러) → `takes.json{drop:[{s,e,why}]}` = 반복 촬영분 중 **버릴 쪽**(기본 = 마지막 테이크 생존) · 하드넷{전사 범위 클램프 · <0.3초 드롭 · 총 ≤50%} · **전면 비치명**(실패 = 테이크 컷만 빠지고 잡 생존). ③ **컷 미리보기**(`cutscan` · 카드 [자를 곳 먼저 보기]) = 렌더 없이 자를 목록만 뽑는 **배타 스캔**(클리퍼 2단 흐름 미러 · api가 렌더 축 제거 정규화 · 컴포즈 스킵) → `.github/scripts/cut_scan.py` → `cuts.json{v,ts,dur,keep_dur,cutlv,note,rm:[{s,e,k(sil|fil|take),t?}],src?}` → 뷰어 `#cutlist`(탭 = 그 구간 제외 토글) → [이대로 편집하기] = 일반 렌더를 `opts{cutref:<스캔id>, cutoff:"3,7"}`로 발사 → `ly_burn.load_ref_cuts`가 **승인본 그대로** 차감(자동 무음·필러·테이크 재계산 전면 생략 = 승인과 렌더 불일치 0) · **전사 재실행 0**(자막 OFF면 STT 스텝 자체가 안 돌아 잡이 수 분대로 끝난다). ④ 이음매 오디오 = 컷 경계 ±25ms 마이크로 페이드(`cut_xfade` · 스플라이스 클릭 억제 · ⚠ `volume eval=frame` = 오디오 프레임 ~21ms 단위 근사 · 이음매 120개 초과 = 생략). ⑤ **STT 캐시** = 추출 오디오 sha256+모델 태그 키(`stt_cache.py`)로 R2 `stt/<sha>-<모델>.json` 왕복 — 히트 시 전사 스텝 통째 건너뜀(재렌더·후보 확정 렌더가 large-v3를 다시 돌던 §3-g 한계 봉합) · 전면 fail-soft(R2 미설정·키 실패 = 그냥 전사).
- c) **백엔드 = ly_burn.py 단일 컴포지터**(평의회2 260710): 한 ffmpeg 파이프{(트림)→컷 select→크롭→스케일→fps→패드→ASS} + 음량 후처리(비디오 copy). **자막은 최종 캔버스(PlayRes) 기준 렌더 = 검정 여백 위에도 앉음**(릴스 문법). 편집기 축 전부 결측 = 순수 ly 경로와 동일(회귀 0 — 로컬 9케이스 실측).
- d) **워크플로 = `edit-make.yml`**(ly-make 미러 · 기존 ly-make/conv-make 무접촉): 자막·컷·클리퍼 ON에 Whisper(STT) · 자막·클리퍼 ON에만 Node/claude 설치(컷 = STT-only 의역 0 · 러너 분 절약 · 260711) · Demucs = bgm ON에만 · pydeps/Demucs 캐시 = ly-make와 키 공유. 산출 계약 = `viewer/ly_out/<id>/`(id 유일 = ly와 충돌 0).
- e) **발사 레이트리밋(260711 · 평의회 권고)**: `functions/api/_rate.js` `rateGate` 공용 — 해당 워크플로 queued+in_progress ≥ 3이면 429 정직 거절(러너 무료분 남용·연타 방지). 업로드 *전* 게이트(up-<id> 고아 방지) · 조회 실패 = fail-open(가용성 게이트 아님) · 4파이프 공통(edit·ly·conv·track).
- f) **작업 내역(제작 라이브러리 · 운영자 260712 "이미지 생성기처럼 최근·기존 제작 라이브러리화")**: 폼 끝 `작업 내역` 게시판 = 자막 탭 260707 게시판 원문 계승 — 데이터 = `ly_out/index.json`{build-viewer 스캔 · 서버측 = 기기간 공유·만료 없음 · 편집 잡 라벨 = subs.md 타이틀 없으면 video.json `edit_opts` 레시피 요약("16:9 · 블러 채움 · 60fps 보간 · 음량 통일" 식) · `d` = 영상 길이 초 · `clip` 상태 = 클립 후보(구 gen 영구 오표기 정정)} · 행 = 일시(KST)·라벨·길이·상태 · 행 탭 = `openJob(id)`(완료 푸시 딥링크 #done과 공용 — video.json→결과 표시+자막 편집기 활성 / clips.json→후보 목록 / error.log→사유 표시) · 자막-전용(st=subs) 잡 = 자막 탭 전담이라 제외 · 생성 진행 중(`_polling`) = 행 탭 차단(진행 UI 보호 = ly LY_GEN 계승) · 상한 60행(구 20 · 전체 보관 정합 260712) · 빈 목록 = 섹션째 숨김(§디자인 빈 상태) · **표시 = 기본 접힘 토글 + 펼침 촤르륵(운영자 260712 "영상도 이미지 계승")**{헤더 = 라임 블릿(`--hist-accent` 이력색 = index·thumb 동값 inline 복사)+개수+쉐브론 · 펼침 = 행 스태거 `histIn`+타이틀 점등+섹션 랜딩 · 닫힘 = `histOut` 후 hidden(thumb `histH` 로직 계승 · 닫힘 상단 복귀만 미계승 = 폼 제자리 유지 의도 divergence) · ly.html 게시판 동형}. 왜: 종전 과거 산출 접근로 = {발사 직후 폴 · pend 24h · 푸시 딥링크}뿐 → 시간 지나면 재열람 불가(산출물은 R2+git 영구 보존인데 순수 UI 공백)였던 것 봉합.

## 2. 캡·예산 (기틀 — 완화 = 운영자 확인)
- a) 영상 길이 = **차등 캡(운영자 260711 "잘라서 10분 이하로 만드는 개념" 확정 — 원본 기준 선게이트 exit 1 → error.log · 클라 게이트 동형)**: ① 전사 카드(자막·컷·클리퍼) = **원본 ≤20분**{STT가 원본 전체를 전사 = 비용 원본 비례 · Whisper turbo ~0.2~0.5×RT 추정 = 스텝 25분 캡 내 · 20분 실측 전 보수 추정 — 실패 잦으면 하향} ② 트림 구간 ≤10분 + 전사 없음 = **원본 ≤60분**(ffmpeg `-ss/-t` = 구간만 디코드·인코딩 = 원본 길이 무관) ③ 그 외 통짜 = **종전 10분**(트림 없는 장편 통변환은 편집기 용도 아님). 업로드 = **R2 직업로드 ≤2GB**{`api/upload` 32MB 조각 멀티파트 · `up_src/*` 일회용(잡 끝 삭제 · 미소비 고아 = 뷰어 delete 정리 + 수명규칙 1일 권장) · **Pages R2 바인딩(변수명 `R2` · 러너 `R2_BUCKET`과 같은 버킷) + 재배포 1회 필요 — 미바인딩 = 종전 30MB up-브랜치 폴백(회귀 0)** · conv 공용 `viewer/upload.js` · 260711}. **4K = 해상도 카드 `원본(4K)`**{**4K급 판별 = 캔버스 픽셀 > FHD 2배**(긴 변>1920 판별은 세로 1080×2340 폰영상을 오분류 = 평의회4 교체) → **180초 캡**(`EDIT_4K_MAX_SEC`) · 60i 보간 제외(정직 스킵 note) · enc 백스톱 = 픽셀 비례(FHD 900s→4K 2400s) · URL 소스는 `원본(4K)` 선택 시에만 yt-dlp 2160 페치}.
- b) **60fps 보간 = 예산 가드**: est = 길이×60×0.30s/프레임×(해상도/1080×1920) ≤ **900초** — 초과 = 보간만 건너뛰고 note(다른 처리는 진행 = 잡 살림 · conv의 die와 의도적 상이). 인코딩 백스톱 = 픽셀비례 베이스(FHD 900·4K 2400 캡)+est×1.5s · 컴포즈 스텝 60분 · 잡 85분(R2 선스텝 8분 포함 재산정 · 평의회6).
- c) 뷰어 폴 = 예산 2700s(하드 ×2 = 5400s ≥ 잡 캡 85분) · 재개 게이트 24h(pend `nm_edit_pend`).

## 3. 알려진 한계 (정직 · v1)
- a) 트림×자막 동시 = 지원(260711 리맵) — 단 STT·의역은 **전체 영상** 기준으로 돌고 번인만 구간 반영(구간 밖 의역 = 비용 낭비 소폭 · 트림 선반영 STT는 후속).
- b) 컷 편집 자막 의존 = 해제(260711 STT-only — §1-b). 잔여 한계 = 컷 단독도 STT(전사) 비용은 든다(Whisper 수 분) · 전사 실패 시 컷 스킵 note(다른 처리 진행) · 컷 *단독* 스택 + 전사 실패 = 정직 에러(산출할 게 없음).
- c) **다시 입히기·상세 자막 편집기·자막 텍스트 뷰 = 자막 탭 전담(260711 2차 — 배치 B-2 이식본은 편집기 안 도먼트 보존·외부 자막 산출 #done 딥링크 열람 시에만 표출)**: (이하 = 도먼트 코드 정본 서술) 완료 시 subs.md 있으면(자막·컷 산출) 결과 탭{상세 = 조각 카드·어절 칩(탭=빼기·더블탭=수정·분열·병합)·타이밍 조절·찾아바꾸기·미리보기 싱크(파일 소스 objectURL) / 개요 = 완성 자막+조각 복사 블록} + 완성 영상(lyBurn)·다시 입히기·삭제 컷 — 이중 표시 방지 = 자막 작업은 lyBurn이 완성 영상 전담(vstage·dlVid 숨김·비자막 = 종전 vstage 회귀 0). **다시 입히기 옵션 = 스택 카드(P/ON)가 정본(`lyOptsNow()` — 운영자 "관련된 거에 카테고라이징": ly 행 덩어리 → 자막 카드(언어·톤·모양·강조·게이지)/컷/배경음 카드 분류로 대체)**. 편집 스냅샷 키(nm_lyedit_<id>) = ly 탭과 공유(같은 작업 이어짐) · pend·last 키는 격리(nm_editsub_*). **재입히기 편집 설정 승계(260711 후보7)**: 편집기 산출 video.json에 `edit_opts` 스냅샷(vid_*·aud_norm) 도장 → reburn 시 ly_burn이 자동 병합 = 여백·해상도·보간·음량·트림 유지(이번 opts에 편집기 축 명시 = 병합 안 함 · note "이전 편집 설정 승계" 표면화). **자막 스타일 축(음영 outline/pad·oc·font)은 스냅샷 비대상** — 매 reburn마다 카드 설정(`lyOptsNow`)에서 재전송(편집기 반복 reburn = 유지 · ly 탭 reburn = 기본 복귀 — ly에 그 UI 없음 = 의도 비대칭 · v2평의회3) · reburn 러너 폰트 = ly-make 재합성 스텝이 나눔 2종 포함 설치(260711 봉합).
- d) 크롭 위치 미리보기(드래그 프레임) = **이식 완료(260711 배치 B-1 — conv `.scrub` 그대로)**: 비율 카드 자르기 + 파일 소스일 때 `위치 미리보기` 버튼 → 전체화면 오버레이{장면 시킹 + 크롭 프레임 드래그 = 위치 게이지 라이브 동기 · 취소 = 오픈 전 복원}. URL 소스 = 게이지만(서버만 치수 인지 = conv 동일 정직).
- d-1) **라이브 미리보기(운영자 260711 풀 프리뷰 · 배치 4차 = 비율 카드 귀속{3분류 선택값 pvPlace=rsz — 자막 카드 이관으로 이주 · 카드 접힘/비율 미적용 = 미표시 · 구현 = 영속 #pv 노드를 비율 카드 #pvSlot으로 이주(video 재생성 0·재생 재개 가드) · 자막 박스 미러 = 자막 카드 도먼트라 비활성}**: 파일 첨부 + 비율 카드 펼침 시 `#pv` 스테이지(프레임 = `.vstage` 콤마 편승 = 신규 raw 0) — 캔버스 = 선택 비율(비율 카드 없음 = 원본비) · 자르기 = cover+잘리는 축 팬(vid_pos 동기) · 여백(검정/블러) = contain 중앙(블러 질감만 결과에서 = 근사·배치 동일) · **자막 카드 ON = 위치·크기·배경띠 박스를 서버 산식 미러로 오버레이**{바닥 = H×(0.02+0.0082×pos) · 글자 = H×(size‰) · 좌우 마진 7.4% · bg 알파 = 게이지% · 배경0 = bold 외곽선 0.064fs/clean·box 0.032fs — 정본 = `ly_burn.py build_ass`}. 스택·게이지 변경 = 실시간 동기(`pvSync`). **+음영 3축 미러(260711)**: 음영 크기 = 외곽선/박스 패딩 산식에 배율·계수 동승(클램프까지 서버 `coef` 미러) · 음영 색 = 외곽선·배경띠 색(`ocRGB` = :root 계승 var 실측) · 폰트 = 동명 구글 웹폰트 지연 로드(기본 고딕 = 시스템 근사 = 외부요청 0 · 오프라인 = 폴백 스택 = 근사). 정직 한계 = 1줄 샘플(다줄 상단캡 비반영)·키워드 그린/카라오케 채색 미표시 · URL 소스 = 프리뷰 불가(d와 동일) · 발사·결과 표시 중 = 숨김(중복 방지).
- e) 컷 + 60fps 보간 동시 = 컷 이음매에서 보간 워블 가능(minterpolate 장면전환 감지가 완화 · 원리 한계).
- e-1) **컷 강화 3종 한계(260727 · 정직)**: ① 필러 컷은 **소리만** 지운다 — 자막을 같이 켜면 그 자리 글자는 남을 수 있다(자막 쪽 군더더기 제거 = 자막 카드 `filler` 의역 옵션이 담당 · 두 축 독립). ② 필러 사전 = 닫힌 집합(한국어+영어 상용) — 사투리·개인 말버릇은 못 잡는다. ③ 테이크 감지는 LLM 판단이라 **오검출 가능** → 그래서 기본 흐름은 컷 미리보기로 확인하는 것(자동 반영은 되지만 확인 없이 쓰면 위험). ④ 승인 컷(cutref)은 **그 스캔의 시간축**이 정본 — 승인 후 트림 구간을 바꾸면 트림 좌표로 동행 리맵되긴 하나 스캔 당시와 다른 그림이 될 수 있다. ⑤ 이음매 페이드는 프레임 단위 근사(위 b-2 ④). ⑥ EDL 익스포트(`shared/cut_export.py`)는 **배선만** — cuts.json 옆에 `cuts.edl`(CMX3600)이 같이 나오지만 **뷰어 UI 없음** · FCPXML·Premiere·Resolve는 레지스트리 자리만 있고 **미구현**(호출 시 사유 반환 = 빈 파일 안 만듦).
- f) 예상 소요 = 근사(60fps는 파일이면 길이 기반·URL이면 중간값) — 실측은 대기 캡션 경과 초.
- g) **클리퍼 v0 한계**: 후보 확정 후 렌더는 새 잡 = 자막 ON이면 STT 재실행(스캔 segments.json 재사용 = 후속 최적화) · 원본 보관 400MB 캡(초과 = 파일 재선택) · 입력 캡 = **20분**(§2-a 차등 캡 ① — 운영자 260711 롱폼 완화 · 추가 상향 = 운영자 확인 필요 기틀).

## 4. 3자 계약 (뷰어 ↔ api ↔ 러너 — 키 이름 변경 = 기틀)
- a) 페이로드 `opts` = **플랫**{ly 자막 축: burn,lang,tone,filler,style,pos,size,bg,karaoke,pop,keyword,cut,cutlv,bgm,**outline,pad(음영 게이지 산출 연속 계수),oc(음영 색 키),font(폰트 키) — 260711 · 재입히기(/api/ly reburn)도 동일 4키 통과** + 편집기 축: vid_ar,vid_fit(crop/pad/**blur**),vid_pos,vid_res(`src`=원본4K/1080/720),vid_fps,vid_t0,vid_t1,aud_norm,vid_sdr(HDR→SDR 톤매핑 · 260913) + **clip(배타 — api가 clip:true면 나머지 키 제거 정규화 · 260711)** + **컷 강화(260727): cutfill·take(불리언 · cut과 독립 소비 가능) · cutscan(배타 스캔 — api가 {cutscan,cut,cutlv,cutfill,take}만 남김) · cutref(스캔 id `YYMMDDHHMMSS-hex6`)·cutoff(제외 인덱스 CSV — 러너가 실측 재검증)**} — `vid_`/`aud_` 접두로 충돌 0(pos↔vid_pos 분리 · 평의회2). api 화이트리스트 + ly_burn 실측 재클램프 = 이중 방어. 빈 스택 = api 400 거절(컷 단독 = 유효 · STT-only 260711). 대용량 = 페이로드 `r2key`{api가 `up_src/*` 정규식+존재(head)+크기 검증 → 워크플로 입력 `r2_src`}.
- b) 산출 = `viewer/ly_out/<id>/{video.json(url·note·error·skip·ts KST — ts는 전 경로 무조건 도장 · **edit_opts** = 편집기 축 스냅샷{성공 시·EDIT_KEYS 존재 시만} — reburn 승계 왕복 키 = §3-c), error.log, (자막 시) subs.md·subs.json·segments.json, (클리퍼 시) clips.json(v·ts·dur·src·clips[{s,e,title,why}] — 컴포즈 스킵 = video.json 없음), (컷 스캔 시) cuts.json + cuts.edl(⑥ 배선 · 컴포즈 스킵), (테이크 ON 시) takes.json}` — ly 계약 재사용. 뷰어 폴 = url/error/skip 3분기 완료 처리(fail-soft가 rc0이라 error.log 대신 video.json.error로 오는 경로 수용) · 클리퍼 폴 = clips.json(ts 신선도 동일).
- c) id = `YYMMDDHHMMSS-hex6`(KST) · 업로드 = R2 `up_src/<id>.<ext>`(잡 끝 `aws s3 rm` · 일회용 = 발사 소진 → 재발사는 뷰어가 `_lastFile` 자동 재업로드) 또는 폴백 `uploads/<id>/src.*`(up-<id> 일회용 브랜치 · 잡 끝 정리) · R2 = `ly_out/<id>/subbed.mp4`.
