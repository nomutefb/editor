# 04 · 노뮤트 구조 / 아키텍처 지도

## 한 장 요약
```
기사 → [01_지침 로직] → 텍스트(0단계·앵글·IG·Thread·시사점, 맨 제목=썸네일)
     → '4' → 카드뉴스(4-A 브리프·페어표 → 4-B 카드) → 카드별 🍌+✳️ 프롬프트
              (여기서 [02_라이브러리] 카메라·조명 온디맨드 참조)
     → 'ㄱ' → Drive 저장(03_자동화) → Apps Script → 이미지
```

## 3-티어 참조 (효율 핵심 — "필요한 것만, 안 비싸게")
| Tier | 무엇 | 어디 | 언제 로드 |
|---|---|---|---|
| **0** | 역할·자동진행·포맷·윤리·**2 화풍·악센트 #0FFD02** | `00_에디터_뉴스_운영.md` 인라인 | **`/news` 진입 시**(작고 고정) |
| **1** | 카메라(샷 S·앵글 H·방향 O·관계 R·화각 L §1-5)·조명(§6)·디스패치(§8) | 02_라이브러리 | **Step 4-B에서 그 카드에 필요한 코드만** |
| **2** | parsePrompts·과금·Zapier·STYLE_PROMPT·GPT 수동 | 03_자동화 | Drive 저장 4-C |

> 원칙: **라이브러리 전체 참조 금지.** 화풍 = 항상 2개 인라인 / 카메라·조명 = 그 순간 골라서. = 손해 없이 필요한 것만.

## 훅 (라이브러리가 로직에 꽂히는 지점)
- **화풍 판정**: 지침 Step 1 [아트 스타일 판정] → 00_운영 §3 인라인 2종으로 결정(라이브러리 불필요).
- **카메라·조명**: 지침 Step 4-B "감정 맺힘점 6단계 ⑤(샷·앵글·화각)·⑥(조명)" → **02_라이브러리 §1-6에서 그 카드 코드만 펼침.** ← 화풍-무관·순간 묘사 모듈이 들어가는 가장 효과적인 자리.
- **악센트**: 모든 카드 → 00_운영 §4 공식 적용(#0FFD02 또는 상징색).

## 메모리 2층 (둘 다 작동)
- **세션 자동로드(≒ 챗 메모리)** = 라우터 `CLAUDE.md`(플랫폼 공통·매 세션 자동 로드) → `/news` 진입 시 `00_에디터_뉴스_운영.md`(뉴스 Tier 0).
- **지침 참조 메모리(≒ 지침 메모리)** = `PROJECT_MEMORY.md` — 브랜드 룰·결정 로그; 지침/에디터가 읽어 따른다.

## 새 세션 사용법 (딸깍)
1. Claude Code에서 **레포(`nomutefb/editor`)를 연다** (Opus 권장) → **`/news`** 또는 기사 원문 바로 던지기(라우터가 뉴스 에디터로 진입).
2. **기사를 던진다** → 자동으로 시사점까지.
3. **'4'** → 카드 + 🍌/✳️ 프롬프트 + 🚦STOP. **'ㄱ'** → Drive 저장 + 이미지 발사 + 검수.
4. **✳️ 프롬프트는 GPT Image 2에 수동 복붙**(자동화 대상 아님).

## 파일 일람
라우터 `CLAUDE.md`(자동로드·앱 라우팅·플랫폼 공통 정본) · `00_에디터_뉴스_운영.md`(뉴스 운영·Tier 0) · `01_지침_*`(본체 로직) · `02_라이브러리_*`(카메라/조명 코드) · `03_자동화_*`(파이프라인·과금·Zapier·GPT수동) · `04_구조_*`(이 문서) · `05_리뷰_*`(베스트 큐레이션) · `PROJECT_MEMORY.md`(고정 사실·결정) · `_산출/`(생성 카드 MD)
